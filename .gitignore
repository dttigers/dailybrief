.DS_Store
/.build
/Packages
node_modules/
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
.env
*.env
.env.local
.env.*.local
.env.bak.*
*.env.bak.*
.env.local.bak*
*.env.local.bak*

# IDE
.idea/
.vscode/

# Build artifacts and archives — never commit
*.xcarchive/
DailyBriefMonitor */
DailyBriefMonitor.dSYM/
*.dSYM/
dist/
.claude/
.vercel
.env*.local

# evenhub-simulator screenshot output (📸 button writes to its cwd as
# `glasses_YYYYMMDDHHMMSS.png` — these are operator-verification artifacts,
# not source). Surfaced during Phase 124 D-14 PNG-equality verification.
glasses_*.png

# Provisioning profiles — bind to one developer account + embed Team ID.
# Generated by developer.apple.com, never check in. See Phase 999.1.
Entitlements/*.provisionprofile
