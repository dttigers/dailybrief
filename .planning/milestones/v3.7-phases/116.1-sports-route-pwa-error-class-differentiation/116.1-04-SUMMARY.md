---
phase: 116.1-sports-route-pwa-error-class-differentiation
plan: "04"
subsystem: api
tags: [sports, brief-assembly, posthog, telemetry, tdd, placeholder]

# Dependency graph
requires:
  - phase: 116.1-sports-route-pwa-error-class-differentiation
    plan: "01"
    provides: "UpstreamError with stable message format `Upstream sports provider failed (${kind})`; LeagueResult.status='error'"
provides:
  - "mapSports placeholder branch for status='error' leagues — BriefSportLeague with teamName='{LEAGUE} data temporarily unavailable.' (D-05)"
  - "mapSports all-failed short-circuit — single BriefSportLeague{sport='all-failed', displayName='Sports', teamName='Sports data temporarily unavailable...'} when every non-disabled league errors (D-07)"
  - "extractErrorClass regex recovering UpstreamError.kind from Plan 01 stable message format for PostHog properties"
  - "PostHog sports_league_fetch_failed event fired per failed league with {league, status, error_class} from assembleAndRender (D-06)"
  - "trackEventFn injectable dep on BriefAssemblyDeps (test seam for PostHog telemetry)"
  - "4 SPORTS-01b-brief-* tests: per-league placeholder + all-failed single block + disabled cascade + PostHog event assertion"
affects:
  - pdf-service (drawSportSection already handles null recentGame/upcomingGame + empty standings — no change needed)
  - brief-assembly-service tests (Test 5 updated to expect NFL placeholder instead of silent omission)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Placeholder-not-omit: status='error' leagues render a visible BriefSportLeague entry instead of silently dropping — user always sees what's missing and why"
    - "All-failed short-circuit: D-07 fires when nonDisabled.length > 0 && errored.length === nonDisabled.length — single block beats 4 placeholders for full-outage UX"
    - "extractErrorClass regex pattern: stable message format from Plan 01 lets Plan 04 recover kind for telemetry without touching LeagueResult shape"
    - "trackEventFn test seam: injectable dep on factory deps struct mirrors existing callClaudeFn/parseAIJsonFn pattern — analytics tested without mocking module"

key-files:
  created: []
  modified:
    - vigil-core/src/services/brief-assembly-service.ts
    - vigil-core/src/services/brief-assembly-service.test.ts

key-decisions:
  - "extractErrorClass via regex on Plan 01's stable message format — avoids modifying LeagueResult shape (out of scope); falls back to 'unknown' for non-Upstream errors (option d + better option from plan context)"
  - "Keep existing console.log for sportsR.status==='rejected' (catastrophic Promise failure); PostHog fires separately inside fulfilled branch for per-league failures — they're orthogonal, not duplicate"
  - "[Rule 1 - Bug] Updated Test 5 (pre-existing) to expect 2 entries (MLB ok + NFL placeholder) because the old silent-omit behavior that the test was asserting is exactly what this plan replaces"
  - "trackEventFn injectable at factory level (deps.trackEventFn ?? trackEvent) captures once as trackEventImpl — same pattern as callClaudeFn; no closure re-definition per call"

patterns-established:
  - "mapSports explicit status branches: disabled→continue, error→placeholder, off_season/other→continue, ok→full render — replaces implicit 'status!==ok' catch-all so future status values require explicit handling"

requirements-completed: [SPORTS-01b]

# Metrics
duration: 20min
completed: 2026-04-30
---

# Phase 116.1 Plan 04: mapSports Placeholder + All-Failed Short-Circuit + PostHog Telemetry Summary

**mapSports now renders per-league placeholders for status='error' (D-05), short-circuits to a single all-failed block when every non-disabled league errors (D-07), and fires a PostHog sports_league_fetch_failed event per failed league with extractErrorClass-parsed error_class (D-06)**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-30T00:00:00Z
- **Completed:** 2026-04-30T00:21:40Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments

- `PER_LEAGUE_FAILURE_COPY` / `ALL_FAILED_COPY` constants + `buildSportsPlaceholder` helper added; mapSports body rewritten with explicit `disabled → continue`, `error → placeholder`, `off_season → continue`, `ok → full render` branches
- D-07 all-failed short-circuit fires when `nonDisabled.length > 0 && errored.length === nonDisabled.length` — returns `[{sport:'all-failed', displayName:'Sports', teamName:'Sports data temporarily unavailable...'}]`
- `extractErrorClass` regex recovers UpstreamError `.kind` from Plan 01's stable `Upstream sports provider failed (${kind})` message — falls back to `"unknown"` for non-standard throws
- `trackEventFn` injectable dep on `BriefAssemblyDeps`; `trackEventImpl = deps.trackEventFn ?? trackEvent` captured at factory level; PostHog `sports_league_fetch_failed` fires per-league inside the `sportsR.status === 'fulfilled'` branch of `assembleAndRender`
- 4 new `SPORTS-01b-brief-*` tests covering: per-league placeholder shape, all-failed single block, disabled cascade preserved, PostHog event fires with correct {league, status, error_class}
- D-18 cascade preserved structurally — `disabled → continue` branch preserves empty-array path to pdf-service.ts:281 guard
- pdf-service.ts UNCHANGED — drawSportSection already handles `recentGame=null`, `upcomingGame=null`, `standings=[]`

## Task Commits

1. **Task 1 RED: 4 failing SPORTS-01b-brief-* tests** - `3d61458` (test)
2. **Task 1 GREEN: mapSports placeholder + all-failed + PostHog implementation** - `6661f3f` (feat)

## Files Created/Modified

- `vigil-core/src/services/brief-assembly-service.ts` — Added trackEvent import, trackEventFn dep field on BriefAssemblyDeps, PER_LEAGUE_FAILURE_COPY/ALL_FAILED_COPY/extractErrorClass/buildSportsPlaceholder module-scope helpers, rewrote mapSports body with explicit status branches, captured trackEventImpl in factory, added PostHog event loop in assembleAndRender
- `vigil-core/src/services/brief-assembly-service.test.ts` — Added 4 SPORTS-01b-brief-* tests (2 describe blocks), updated pre-existing Test 5 to expect NFL placeholder entry (Rule 1 fix)

## Decisions Made

- **extractErrorClass via regex**: Plan 01 chose a stable message format `Upstream sports provider failed (${kind})` — regex extraction lets Plan 04 recover kind without touching the LeagueResult type. Non-match falls back to `"unknown"` (safe default for PostHog; no info leak).
- **Keep both diagnostic paths**: `console.log` for catastrophic `sportsR.status==='rejected'` + PostHog for per-league `lr.status==='error'` inside fulfilled — orthogonal cases, not duplicates. CONTEXT D-06's "replaces" referred to per-league console logs that didn't exist yet.
- **trackEventFn test seam at factory level**: `const trackEventImpl = deps.trackEventFn ?? trackEvent` captured once, not per-call. Mirrors existing `callClaudeFn`/`parseAIJsonFn` pattern so analytics is testable without module mocking.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated pre-existing Test 5 assertion to match new correct behavior**
- **Found during:** Task 1 GREEN (running tests after implementation)
- **Issue:** `Test 5: maps fulfilled SportsResponse with mlb ok and nfl error` asserted `result.length === 1` — it was testing the old silent-omit behavior where `status === 'error'` fell through the `status !== 'ok'` filter. The new implementation correctly renders a placeholder for NFL, producing 2 entries.
- **Fix:** Updated Test 5 to `assert.equal(result.length, 2)`, added `mlb` / `nfl` lookups by sport key, added NFL placeholder shape assertions (teamName, recentGame=null, upcomingGame=null, standings=[])
- **Files modified:** `vigil-core/src/services/brief-assembly-service.test.ts`
- **Verification:** 28/28 tests pass after fix
- **Committed in:** `6661f3f` (feat — same commit as GREEN implementation)

---

**Total deviations:** 1 auto-fixed (Rule 1 - pre-existing test asserting old silent-omit behavior)
**Impact on plan:** Necessary update — the test was asserting exactly the behavior being replaced. New assertions verify both the preserved happy-path (MLB full data) and the new placeholder behavior (NFL placeholder shape). No scope creep.

## Issues Encountered

None — implementation followed plan exactly. The Rule 1 deviation was anticipated (plan's context noted that Test 5's `nfl: { status: "error" }` fixture would need updating).

## Known Stubs

None — placeholder copies are fully implemented literal strings. All 4 new tests assert exact string values. No mock data flows to UI.

## Threat Flags

No new trust boundaries introduced.

- T-116.1-04-01: Placeholder copy uses only `league.toUpperCase()` interpolation (4 hardcoded keys) — no BDL response content flows to PDF
- T-116.1-04-02: extractErrorClass emits only the kind enum or "unknown" — error string itself never reaches PostHog
- T-116.1-04-03: PostHog properties are enum literals (league key, literal "error", kind enum) — T-73-01 preserved

## Self-Check: PASSED

- FOUND: vigil-core/src/services/brief-assembly-service.ts
- FOUND: vigil-core/src/services/brief-assembly-service.test.ts
- FOUND: .planning/phases/116.1-sports-route-pwa-error-class-differentiation/116.1-04-SUMMARY.md
- FOUND commit: 3d61458 (RED)
- FOUND commit: 6661f3f (GREEN)
- 28/28 tests pass
- tsc --noEmit exits 0
- 31/31 sports-service regression tests pass
