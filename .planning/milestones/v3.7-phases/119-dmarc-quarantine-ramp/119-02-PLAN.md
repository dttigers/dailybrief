---
phase: 119-dmarc-quarantine-ramp
plan: 02
type: execute
wave: 2
depends_on:
  - 119-01
files_modified:
  - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md
autonomous: false
requirements:
  - OPS-02
must_haves:
  truths:
    - "On or after 2026-05-06, the auto-eval routine trig_01RZLcj1jpxvDQAwnFmUG9d9 produces a PASS / FAIL / DEFERRED determination, and that determination is captured verbatim in 119-RUNBOOK.md Section 6"
    - "On PASS branch only: _dmarc.vigilhub.io TXT record on Cloudflare DNS shows v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com (verified by dig)"
    - "On PASS branch: both legs of the D-05 two-path smoke (forgot-password real send + resend-verification real send) show dmarc=pass in raw Gmail headers"
    - "On FAIL or DEFERRED branch: NO Cloudflare DNS edit fires; deferral reason and re-eval date are documented in 119-RUNBOOK.md Section 6"
    - "If any D-06 rollback trigger fires within 14 days post-ramp: TXT record reverts to v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com (verified by dig); rollback timestamp + reason logged in 119-RUNBOOK.md Section 7"
  artifacts:
    - path: ".planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md"
      provides: "Post-execution evidence appended to Section 1 (re-snapshotted dig), Section 4 (post-ramp dig), Section 5 (Authentication-Results headers from both smoke legs), Section 6 (PASS/FAIL/DEFERRED determination with date)"
      contains: "PASS|FAIL|DEFERRED branch annotation with date YYYY-MM-DD and operator-pasted evidence"
  key_links:
    - from: "Cloudflare dashboard TXT edit (Section 2 click-path)"
      to: "_dmarc.vigilhub.io public DNS record"
      via: "manual dashboard save → DNS propagation"
      pattern: "p=quarantine.*rua=mailto:jamesonmorrill1@gmail.com"
    - from: "Production API real sends (forgot-password + resend-verification)"
      to: "Gmail Inbox at jamesonmorrill1@gmail.com"
      via: "Resend → SES → Gmail; raw headers must contain Authentication-Results: ...dmarc=pass"
      pattern: "Authentication-Results.*dmarc=pass"
    - from: "Auto-PR from trig_01RZLcj1jpxvDQAwnFmUG9d9"
      to: "119-RUNBOOK.md Section 6 (PASS/FAIL/DEFERRED branches)"
      via: "operator-merged auto-PR appending evidence into existing runbook"
      pattern: "auto-PR|auto-eval"
---

<objective>
Execute the actual DMARC ramp action on or after 2026-05-06 — gated on the existing auto-eval routine `trig_01RZLcj1jpxvDQAwnFmUG9d9` producing a PASS determination — and capture all post-execution evidence (re-snapshotted pre-ramp dig, dashboard edit confirmation, post-ramp dig, two-path smoke headers, PASS/FAIL/DEFERRED branch annotation) into the existing `119-RUNBOOK.md` from Plan 01.

Purpose: Plan 01 produced the static runbook before the gate fires. This plan IS the operator-facing execution work — every task is `checkpoint:human-verify` or `checkpoint:human-action` because each step requires real-world interaction (Cloudflare dashboard, Gmail raw-header inspection, prod sends, waiting for the 5-06 routine to fire). Per D-03, NO API tooling is used; the dashboard click-path is the canonical execution surface.

Output: 119-RUNBOOK.md updated in-place with operator-pasted evidence in Sections 1 (re-snapshot), 4 (post-ramp dig), 5 (Authentication-Results from both smoke legs), and 6 (PASS/FAIL/DEFERRED branch annotation). On rollback path: Section 7 also updated with timestamp + reason + which D-06 trigger fired.

Important branching: The phase has three terminal outcomes — PASS (ramp fires + smokes pass), FAIL (gate denies; no DNS edit; re-eval date documented), DEFERRED (partial determination; routine self-reschedules). Tasks below explicitly handle the branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md
@.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm auto-eval routine determination from trig_01RZLcj1jpxvDQAwnFmUG9d9</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md (D-01 — the routine's evidence bar IS the safety sample; D-04 runbook contents)
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 6 — PASS/FAIL/DEFERRED branches; the auto-PR from the routine appends evidence here)
    - .planning/REQUIREMENTS.md OPS-02 (gate language: ≥7 days clean aggregate reports + ≥3 days verify-email production volume)
  </read_first>
  <what-built>
    Plan 01 landed the static `119-RUNBOOK.md` before 2026-05-06. The existing routine `trig_01RZLcj1jpxvDQAwnFmUG9d9` (DO NOT recreate — see memory `project_seed_003_dmarc_routine.md`) is responsible for evaluating the gate on 2026-05-06: ≥7 days of clean DMARC aggregate reports AND ≥3 days of real verify-email production volume AND ≥50 sends/day. Operator inspects the routine's auto-PR to determine which branch (PASS / FAIL / DEFERRED) to follow.
  </what-built>
  <action>
1. After 2026-05-06, check Gmail / GitHub for the auto-PR opened by `trig_01RZLcj1jpxvDQAwnFmUG9d9`. The PR title/body should explicitly state PASS, FAIL, or PARTIAL/DEFERRED.
2. Read the PR's evidence summary — confirm the evidence aligns with what the gate language in REQUIREMENTS.md OPS-02 requires (≥7 days clean rua reports listed, verify-email send count ≥ 3 days).
3. Open `.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md` Section 6. Paste the auto-PR's determination + date verbatim under the matching branch heading (PASS branch / FAIL branch / DEFERRED branch). Leave the other two branches empty/marked "N/A".
4. If the auto-PR has NOT fired (e.g., today is before 2026-05-06, or the routine failed to run): STOP. Report the exact state to the user. Do NOT proceed to Task 2.
5. If the determination is FAIL: skip to Task 6 (FAIL/DEFERRED documentation only). NO DNS edit fires. Document the deferral reason + re-eval date in Section 6.
6. If the determination is DEFERRED/PARTIAL: skip to Task 6. The routine self-reschedules per its existing logic. NO DNS edit fires.
7. If the determination is PASS: proceed to Task 2.
  </action>
  <how-to-verify>
    Per the steps in `<action>` above. Operator confirms the determination verbally + by pasting evidence into Section 6.
  </how-to-verify>
  <verify>
    Manual checkpoint. Operator must confirm: (a) auto-PR observed, (b) classification is one of PASS/FAIL/DEFERRED, (c) Section 6 contains a date + branch annotation under exactly one branch heading, (d) other two branch headings contain `N/A`.
  </verify>
  <acceptance_criteria>
    - Auto-PR from `trig_01RZLcj1jpxvDQAwnFmUG9d9` is observed, with explicit PASS / FAIL / DEFERRED classification
    - 119-RUNBOOK.md Section 6 contains a date in `YYYY-MM-DD` format under exactly one of the three branch headings (PASS branch / FAIL branch / DEFERRED branch)
    - 119-RUNBOOK.md Section 6 contains the auto-PR's evidence summary verbatim (or a link to the PR with the evidence inlined)
    - If FAIL or DEFERRED: 119-RUNBOOK.md Section 6 contains the substring `re-eval` or `re-evaluation` and a date for the next attempt
    - User explicitly confirms which branch applies before proceeding
  </acceptance_criteria>
  <done>
    Branch determination captured in Section 6 with auto-PR evidence inlined; user has selected next path (PASS → Task 2; FAIL/DEFERRED → Task 6; not-yet-fired → halt).
  </done>
  <resume-signal>Type "PASS — proceed to ramp", "FAIL — document and stop", "DEFERRED — document and stop", or "auto-PR not yet fired — wait"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Re-snapshot pre-ramp dig + execute Cloudflare dashboard TXT edit (PASS branch only)</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md (D-02 minimal flip; D-03 manual dashboard edit — NO CF_API_TOKEN, NO flarectl)
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 1 pre-ramp dig + Section 2 click-path + Section 3 before/after table)
    - memory/project_secret_drift.md (the Anthropic key sprawl lesson that drove D-03 — manual edit prevents new secret surface)
  </read_first>
  <what-built>
    Plan 01 wrote the runbook click-path. This task IS the manual dashboard edit. There is NO CLI/API alternative — D-03 explicitly rejects `CF_API_TOKEN` and scripted DNS tooling because adding a new secret surface for a one-record one-time edit would join the Anthropic-key-sprawl drift pattern (config.json + .env + plist + Railway). For one record, one direction (or one rollback), the cost-benefit is upside down. This is a genuine `checkpoint:human-action` — Cloudflare's dashboard is the only execution surface this phase tolerates.
  </what-built>
  <action>
1. Re-run the pre-ramp dig RIGHT NOW (do not trust Plan 01's 2026-05-01 snapshot — drift may have occurred):
   ```bash
   dig TXT _dmarc.vigilhub.io +short
   ```
   Expected output: `"v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com"` (matches Plan 01's pre-ramp expected). Paste the actual output verbatim into 119-RUNBOOK.md Section 1 under "Re-snapshot at ramp time".
   - If the output differs from `p=none` (e.g., `p=quarantine` already set by something else, or `p=reject`, or NXDOMAIN): STOP. Drift detected. Investigate before proceeding.
2. Open `https://dash.cloudflare.com/` in a browser. Login.
3. Click `vigilhub.io` zone.
4. Sidebar → `DNS` → `Records`.
5. Search/filter for `_dmarc` (TXT). There should be exactly one matching row.
6. Click `Edit` on the `_dmarc` TXT row.
7. In the `Content` field, replace the existing string verbatim with:
   ```
   v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com
   ```
   Make absolutely sure: only the `p=` token changes (`none` → `quarantine`). Other tags preserved per D-02. NO `pct=`, NO `adkim=s`, NO `sp=quarantine`, NO `ruf=`.
8. Leave `Type` (TXT), `Name` (`_dmarc`), `TTL` (`Auto`), `Proxy status` (`DNS only`) unchanged.
9. Click `Save`.
10. Confirm Cloudflare's success toast / record list now shows the new value.
  </action>
  <how-to-verify>
    Operator visually confirms Cloudflare dashboard shows new `Content` value verbatim AND pastes the re-snapshot dig output into Section 1.
  </how-to-verify>
  <verify>
    Manual confirmation that: (a) re-snapshot dig still shows `p=none` (drift check), (b) dashboard `Content` field now reads `v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com` exactly, (c) the edit was performed manually (no API, no `CF_API_TOKEN`).
  </verify>
  <acceptance_criteria>
    - 119-RUNBOOK.md Section 1 "Re-snapshot at ramp time" contains a verbatim `dig` output matching `"v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com"` (drift check passed)
    - Cloudflare dashboard shows the `_dmarc` TXT record's `Content` is now exactly `v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com` (operator screenshot or verbal confirmation)
    - User explicitly confirms: only the `p=` tag changed, no other tags added or removed
    - User explicitly confirms: the dashboard edit was performed manually (no script, no API, no `CF_API_TOKEN` introduced — D-03 invariant preserved)
  </acceptance_criteria>
  <done>
    Cloudflare TXT record save succeeded; pre-ramp dig snapshot captured in Section 1; ready for Task 3 propagation verification.
  </done>
  <resume-signal>Type "saved — proceed to dig verification" or describe drift / edit problem</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify post-ramp dig propagation (PASS branch)</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 4 post-ramp dig instructions + Section 3 expected after-value)
    - .planning/ROADMAP.md line 467 (SC #2 — verified via `dig TXT _dmarc.vigilhub.io +short`)
  </read_first>
  <what-built>
    Cloudflare's edit propagates to public DNS resolvers in seconds (default TTL ~300s, but new edits take effect immediately for fresh queries). This task confirms propagation and that the wire-format value matches expectation BEFORE running smoke sends — if the dig output is wrong, smoke sends would be testing an unintended state.
  </what-built>
  <action>
1. Wait ~30-60 seconds after the dashboard `Save` for propagation.
2. Run:
   ```bash
   dig TXT _dmarc.vigilhub.io +short
   ```
   Expected output: `"v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com"`
3. If the output still shows `p=none`: wait another 60s and retry. Cloudflare propagation is usually fast but can lag. After 5 minutes of `p=none` post-save, treat as a save failure and revisit Task 2.
4. If the output shows `p=quarantine`: paste the dig output verbatim into 119-RUNBOOK.md Section 4. Note observed TTL if visible (use `dig TXT _dmarc.vigilhub.io` without `+short` to see TTL — typically 300).
5. Sanity check: the output must NOT contain `pct=`, `adkim=`, `sp=`, or `ruf=` — D-02 minimal flip enforced.
  </action>
  <how-to-verify>
    Operator runs `dig` and pastes verbatim output into Section 4; substring checks confirm `p=quarantine` present and `pct=`/`adkim=`/`sp=`/`ruf=` absent.
  </how-to-verify>
  <verify>
    `dig TXT _dmarc.vigilhub.io +short` returns string containing `v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com` AND not containing `pct=` or `adkim=` or `sp=` or `ruf=`.
  </verify>
  <acceptance_criteria>
    - `dig TXT _dmarc.vigilhub.io +short` returns a string containing `v=DMARC1; p=quarantine; rua=mailto:jamesonmorrill1@gmail.com`
    - The dig output does NOT contain `pct=` (D-01 — defaults to 100%)
    - The dig output does NOT contain `adkim=` or `sp=` or `ruf=` (D-02 minimal flip)
    - 119-RUNBOOK.md Section 4 contains the verbatim post-ramp dig output pasted by the operator
    - User confirms propagation occurred within reasonable window (under 5 minutes)
  </acceptance_criteria>
  <done>
    Post-ramp dig confirms `p=quarantine` propagated; Section 4 updated with verbatim output; ready for two-path smoke.
  </done>
  <resume-signal>Type "propagated — proceed to smoke" or describe propagation issue</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run two-path smoke (D-05) — both forgot-password and resend-verification real sends</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md (D-05 — two-path smoke definition; both legs MUST pass)
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 5 two-path smoke procedure)
    - vigil-core/scripts/smoke-test-forgot-password.ts (Leg A — POSTs to prod /v1/auth/forgot-password, real send fires)
  </read_first>
  <what-built>
    Both `verify-email` and `forgot-password` flow through the same Resend send path but emit from different `email-service.ts` call sites — testing one would miss alignment regressions specific to the other (per CONTEXT.md `<specifics>`). Note: `smoke-test-verify-email.ts` is DB-token-only and does NOT trigger a real send — for Leg B, use the bearer-authed `POST /v1/auth/resend-verification` endpoint as documented in 119-RUNBOOK.md Section 5.
  </what-built>
  <action>
**Leg A — forgot-password (real Resend send):**
1. Run from the vigil-core directory:
   ```bash
   cd vigil-core
   npx tsx scripts/smoke-test-forgot-password.ts jamesonmorrill1@gmail.com
   ```
2. Wait ~10s for delivery.
3. In Gmail, open the latest "Reset your Vigil password" message.
4. Three-dots menu → `Show original`.
5. Search the raw headers for `Authentication-Results:`.
6. Confirm that line contains the substring `dmarc=pass`.
7. Paste the relevant `Authentication-Results` line(s) verbatim into 119-RUNBOOK.md Section 5 under "Leg A".

**Leg B — verify-email via resend-verification (real Resend send):**
1. Acquire a JWT for the seed user (one acceptable form):
   ```bash
   JWT=$(curl -s -X POST https://api.vigilhub.io/v1/auth/login \
     -H 'Content-Type: application/json' \
     -d '{"email":"jamesonmorrill1@gmail.com","password":"<seed-password>"}' \
     | jq -r .token)
   ```
2. Trigger a real resend-verification send:
   ```bash
   curl -i -X POST https://api.vigilhub.io/v1/auth/resend-verification \
     -H "Authorization: Bearer $JWT"
   ```
   Expect HTTP 200 with `{"ok":true}`.
3. Wait ~10s for the background send to complete.
4. In Gmail, open the latest "Verify your Vigil email" message.
5. Three-dots menu → `Show original`.
6. Confirm `Authentication-Results:` contains `dmarc=pass`.
7. Paste the relevant header lines verbatim into 119-RUNBOOK.md Section 5 under "Leg B".

**If EITHER leg shows `dmarc=fail` or `dmarc=quarantine` (anything other than `dmarc=pass`):**
- Rollback trigger #1 fired (D-06). DO NOT proceed to Task 5.
- Jump immediately to Task 7 (rollback) and document which leg failed + the exact `Authentication-Results` value.
  </action>
  <how-to-verify>
    Operator inspects raw Gmail headers for both messages and pastes verbatim `Authentication-Results:` lines into Section 5; both must contain `dmarc=pass`.
  </how-to-verify>
  <verify>
    Both Leg A and Leg B `Authentication-Results:` lines contain the substring `dmarc=pass`. Both messages delivered to Inbox (not spam folder).
  </verify>
  <acceptance_criteria>
    - 119-RUNBOOK.md Section 5 "Leg A" contains a verbatim `Authentication-Results:` line from a real Gmail-delivered forgot-password email, AND that line contains the substring `dmarc=pass`
    - 119-RUNBOOK.md Section 5 "Leg B" contains a verbatim `Authentication-Results:` line from a real Gmail-delivered verify-email message, AND that line contains the substring `dmarc=pass`
    - Both messages were delivered to Gmail Inbox (not spam folder) — operator visually confirms
    - User explicitly confirms both legs returned `dmarc=pass` before resuming
    - Rate-limit policy from Phase 117 (AUTH-13 5/hr per-email cap) was not exceeded — at most one forgot-password send + one resend-verification send required
  </acceptance_criteria>
  <done>
    Both smoke legs verified `dmarc=pass`; Section 5 contains verbatim header evidence for both; ready for PASS-branch annotation. (If either leg failed: jump to Task 7.)
  </done>
  <resume-signal>Type "both legs dmarc=pass — phase closing" OR "leg X failed — initiate rollback"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Annotate PASS branch in Section 6 + close PASS path</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 6 PASS branch heading)
    - .planning/ROADMAP.md lines 466-469 (SC #1, #2, #4 — what the PASS branch must capture)
  </read_first>
  <what-built>
    With Tasks 1-4 complete, the runbook contains the live evidence (re-snapshotted dig, post-ramp dig, both `Authentication-Results` lines). This task formalizes the PASS branch annotation in Section 6 so the auto-PR from `trig_01RZLcj1jpxvDQAwnFmUG9d9` (which appends evidence here per D-04) has a final, signed-off determination to merge against.
  </what-built>
  <action>
1. Open 119-RUNBOOK.md Section 6 "PASS branch".
2. Append a paragraph stating:
   - Auto-eval routine fire date (from Task 1)
   - Ramp execution date + operator (today + your handle)
   - Cross-references: re-snapshot dig is in Section 1, post-ramp dig is in Section 4, both smoke `Authentication-Results` are in Section 5
   - Phase determination: PASS (covers ROADMAP SC #1, SC #2, SC #4)
3. Confirm the FAIL branch and DEFERRED branch sub-sections contain `N/A — PASS branch fired`.
4. Save the runbook.
  </action>
  <how-to-verify>
    Operator confirms Section 6 PASS branch contains a date + evidence summary; FAIL/DEFERRED contain `N/A`.
  </how-to-verify>
  <verify>
    Section 6 PASS branch contains `YYYY-MM-DD` date + the substring `PASS` + cross-references to Sections 1, 4, 5. FAIL + DEFERRED branches contain `N/A`.
  </verify>
  <acceptance_criteria>
    - 119-RUNBOOK.md Section 6 PASS branch contains a date in `YYYY-MM-DD` format AND the substring `PASS`
    - 119-RUNBOOK.md Section 6 PASS branch references Sections 1, 4, and 5 by name (so the audit trail is self-contained)
    - 119-RUNBOOK.md Section 6 FAIL branch and DEFERRED branch contain `N/A`
    - User explicitly confirms phase is closed on PASS path
  </acceptance_criteria>
  <done>
    Section 6 PASS branch annotated with date, evidence cross-refs, and explicit determination. Phase 119 closed on PASS path. OPS-02 satisfied (all four ROADMAP SCs reached).
  </done>
  <resume-signal>Type "PASS branch annotated — phase closed" or describe issue</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: FAIL or DEFERRED branch — document deferral and stop (skip if PASS)</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md (D-04 — runbook covers all three branches; out-of-scope items)
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 6 FAIL branch / DEFERRED branch headings)
    - .planning/ROADMAP.md line 468 (SC #3 — if gate FAILS: ramp does NOT fire; deferral reason documented; re-eval date captured)
  </read_first>
  <what-built>
    SKIP THIS TASK if Task 1 returned PASS. Only execute on FAIL or DEFERRED determinations from the auto-eval routine. There is no DNS mutation in this branch — the gate denies the ramp by design. Documentation is the only output, satisfying ROADMAP SC #3.
  </what-built>
  <action>
**If Task 1 determination was FAIL:**
1. Open 119-RUNBOOK.md Section 6 "FAIL branch".
2. Append: routine fire date, the specific gate criterion that failed (paste verbatim from auto-PR — e.g., "rua aggregate report 2026-05-04 shows DKIM=fail on 2 sends from `mxa-001b29a01.mxa.r.mlsend.com` indicating SPF subdomain misalignment" OR "verify-email send count 2026-05-03 → 2026-05-05 was 2 days, below ≥3 day threshold").
3. Set re-eval date (typically gate fire date + 7 days). Add the substring `re-eval YYYY-MM-DD` literally.
4. Confirm PASS branch and DEFERRED branch sub-sections contain `N/A — FAIL branch fired`.
5. Capture as a follow-up todo via `/gsd-add-todo` so the re-eval doesn't fall through the cracks.

**If Task 1 determination was DEFERRED/PARTIAL:**
1. Open 119-RUNBOOK.md Section 6 "DEFERRED branch".
2. Append: routine fire date, which criteria were partial (e.g., "≥7 days clean rua met, but verify-email volume was only 2 days at fire time").
3. Note that the routine self-rescheduled per its existing logic — read the auto-PR to extract the new fire date and paste it verbatim.
4. Confirm PASS branch and FAIL branch sub-sections contain `N/A — DEFERRED branch fired`.

**CRITICAL — both FAIL and DEFERRED branches:**
- DO NOT touch Cloudflare DNS. The TXT record stays at `p=none`.
- DO NOT run the two-path smoke (Task 4) — there is nothing to validate; the policy did not change.
- The phase enters a "documentation-only" terminal state. Re-execution is a future event scheduled by the routine itself.
  </action>
  <how-to-verify>
    Operator confirms appropriate branch (FAIL or DEFERRED) annotated with date + reason + re-eval date; other branches contain `N/A`; `dig TXT _dmarc.vigilhub.io +short` STILL returns `p=none`.
  </how-to-verify>
  <verify>
    `dig TXT _dmarc.vigilhub.io +short` returns string containing `p=none` (no mutation occurred). Section 6 FAIL or DEFERRED branch contains date + reason + (for FAIL) literal `re-eval` substring + future date.
  </verify>
  <acceptance_criteria>
    - If FAIL: 119-RUNBOOK.md Section 6 FAIL branch contains a date AND the literal substring `re-eval` AND a deferral reason
    - If DEFERRED: 119-RUNBOOK.md Section 6 DEFERRED branch contains a date AND the next routine fire date pasted verbatim from the auto-PR
    - The unrelated branches (PASS + the other one) contain `N/A`
    - `dig TXT _dmarc.vigilhub.io +short` STILL returns the pre-ramp value `"v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com"` (no DNS mutation occurred)
    - User explicitly confirms NO DNS edit was performed in this branch
    - For FAIL: a follow-up todo was created via `/gsd-add-todo` capturing the re-eval date
  </acceptance_criteria>
  <done>
    Section 6 FAIL or DEFERRED branch annotated with reason + re-eval date; no DNS mutation occurred; follow-up todo opened (FAIL only). Phase enters documentation-only terminal state; OPS-02 remains "Pending — re-eval YYYY-MM-DD" in REQUIREMENTS.md.
  </done>
  <resume-signal>Type "FAIL/DEFERRED documented — phase closed (no ramp)" or describe issue</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Rollback execution (D-06 trigger fired — only if Task 4 leg failed OR rua/user-report rollback within 14 days)</name>
  <files>.planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/119-dmarc-quarantine-ramp/119-CONTEXT.md (D-06 — three rollback triggers; rollback action mirrors ramp in reverse)
    - .planning/phases/119-dmarc-quarantine-ramp/119-RUNBOOK.md (Section 7 — rollback procedure with exact rollback TXT value)
  </read_first>
  <what-built>
    SKIP THIS TASK unless one of the three D-06 rollback triggers fired:
    1. Either Task 4 smoke leg shows `dmarc=fail` or `dmarc=quarantine` in Gmail headers (pre-phase-close detection)
    2. Within 14 days post-ramp, any rua report shows non-zero `disposition=quarantine` on legit Vigil-origin mail (DKIM=pass + SPF=pass but quarantined)
    3. User-reported "I never got my verify email / reset link" + Gmail spam folder confirms quarantine
    The rollback action is the same Cloudflare dashboard click-path in reverse — no new tooling, no new secrets (D-03 invariant preserved on rollback path too).
  </what-built>
  <action>
1. Identify which D-06 trigger fired (1, 2, or 3). Note the trigger number and timestamp.
2. Open `https://dash.cloudflare.com/` → `vigilhub.io` zone → DNS → Records → `_dmarc` (TXT).
3. Click `Edit`.
4. Replace `Content` field with verbatim:
   ```
   v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com
   ```
5. Save.
6. Wait ~30-60 seconds for propagation, then:
   ```bash
   dig TXT _dmarc.vigilhub.io +short
   ```
   Expected output: `"v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com"`
7. Re-run BOTH smoke legs from Task 4 to confirm legitimate mail flows again — both legs MUST show `dmarc=pass` post-rollback (this is the same expectation as pre-ramp under `p=none`).
8. Open 119-RUNBOOK.md Section 7. Append:
   - Rollback timestamp (`YYYY-MM-DD HH:MMZ`)
   - Which D-06 trigger fired (1, 2, or 3) — verbatim trigger language from CONTEXT.md
   - Reason narrative (which leg failed / which rua report / which user report)
   - Post-rollback dig output verbatim
   - Post-rollback two-path smoke `Authentication-Results` verbatim (both legs)
9. Open a follow-up todo via `/gsd-add-todo` capturing root-cause investigation tasks before any future re-ramp attempt. Do NOT re-attempt the ramp without root cause.
10. Phase enters "rolled back" terminal state. ROADMAP SC #2 + #4 are NOT achieved on this path — that's intentional and per D-06.
  </action>
  <how-to-verify>
    Operator confirms post-rollback dig returns `p=none`, post-rollback smokes both `dmarc=pass`, Section 7 contains timestamp + trigger # + reason + verbatim evidence, follow-up todo opened.
  </how-to-verify>
  <verify>
    `dig TXT _dmarc.vigilhub.io +short` returns string containing `p=none` post-rollback. Section 7 contains literal substring `trigger 1`, `trigger 2`, or `trigger 3`. Section 7 contains both post-rollback `Authentication-Results:` lines, both with `dmarc=pass`. Follow-up todo file exists in `.planning/todos/pending/`.
  </verify>
  <acceptance_criteria>
    - 119-RUNBOOK.md Section 7 contains a rollback timestamp in `YYYY-MM-DD HH:MMZ` format
    - 119-RUNBOOK.md Section 7 explicitly identifies which of the three D-06 triggers fired (the literal substring `trigger 1`, `trigger 2`, or `trigger 3`)
    - 119-RUNBOOK.md Section 7 contains a verbatim post-rollback `dig` output matching `"v=DMARC1; p=none; rua=mailto:jamesonmorrill1@gmail.com"`
    - 119-RUNBOOK.md Section 7 contains BOTH legs of post-rollback smoke `Authentication-Results` lines, BOTH showing `dmarc=pass`
    - `dig TXT _dmarc.vigilhub.io +short` returns the pre-ramp `p=none` value
    - User explicitly confirms rollback was manual (Cloudflare dashboard, no API tooling) — D-03 preserved on rollback path
    - A follow-up todo was created capturing root-cause investigation
  </acceptance_criteria>
  <done>
    DNS reverted to `p=none` (verified by dig); legitimate mail flows again under `p=none` (verified by post-rollback smokes); Section 7 captures full rollback evidence; root-cause todo opened. Phase enters "rolled back" terminal state — OPS-02 remains "Pending — root cause TBD" in REQUIREMENTS.md.
  </done>
  <resume-signal>Type "rollback complete — root cause todo opened" or describe issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's hands → Cloudflare dashboard → public DNS resolvers | TXT record edit is publicly visible the moment Cloudflare propagates. Mistakes are visible to the entire internet. Mitigation: Plan 01 pinned the exact target value verbatim; Task 2 + Task 7 use copy-paste, not composition. |
| Resend → recipient mail server (Gmail) under new `p=quarantine` policy | The whole point of this phase is to test that this boundary still works. Section 5 two-path smoke is the validation gate. |
| Operator's local shell → prod API for smoke triggers | Real prod sends fire; rate-limited by Phase 117 AUTH-13 (5/hr per-email cap). Tasks 4 + 7 stay well under the cap. |
| Auto-eval routine `trig_01RZLcj1jpxvDQAwnFmUG9d9` → repo (auto-PR) | The routine writes to git via auto-PR. Trusted infrastructure (per memory `project_seed_003_dmarc_routine.md`); operator merges manually so PR contents are reviewable before they land in `main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02-01 | Denial of Service | False-positive quarantine of legitimate Vigil mail blocking signups + password resets (THE PRIMARY PHASE RISK per `<critical_phase_shape>`) | mitigate | Three layers: (1) auto-eval routine's evidence bar (≥7 days clean rua + ≥3 days verify-email volume + ≥50 sends/day) is a pre-ramp gate; (2) Task 4 two-path smoke catches false-positive quarantine within 30 seconds of ramp; (3) Task 7 rollback procedure with three explicit triggers (D-06) including a 14-day passive rua window. The PRIMARY mitigation is the evidence bar — the smokes are confirmation, not the gate. |
| T-119-02-02 | Tampering | Operator pastes wrong TXT value into Cloudflare (e.g., introduces `p=reject` or strips `rua=`) | mitigate | Plan 01 pinned the exact target string verbatim. Task 2 instructs copy-paste with a "only `p=` token changes" sanity check. Task 3 dig verification catches any wire-format divergence (e.g., if `pct=` or `adkim=` accidentally crept in). Acceptance criteria explicitly assert absence of those substrings. |
| T-119-02-03 | Spoofing | Operator misreads auto-PR determination (e.g., treats DEFERRED as PASS) | mitigate | Task 1 requires the operator to type the exact branch name as the resume signal (PASS / FAIL / DEFERRED), forcing explicit classification. Task 5 + Task 6 are mutually exclusive — only one branch's documentation gets written. |
| T-119-02-04 | Repudiation | Operator forgets why they rolled back six months from now | mitigate | Task 7 acceptance criteria require: timestamp, trigger number (1/2/3), reason narrative, post-rollback dig output, post-rollback smoke headers. Plus a follow-up todo for root cause. The runbook becomes the single source of truth. |
| T-119-02-05 | Elevation of Privilege | Operator creates a `CF_API_TOKEN` "for convenience" mid-execution, defeating D-03 | mitigate | Tasks 2, 3, 7 explicitly forbid scripted DNS automation. Acceptance criteria for Task 2 + Task 7 require operator confirmation that the edit was manual. Memory `project_secret_drift.md` is in `read_first` for those tasks as a constant reminder of why the manual constraint exists. |
| T-119-02-06 | Information Disclosure | Operator pastes seed-user password verbatim into runbook while documenting the JWT-acquisition step in Task 4 | mitigate | Task 4 instructions show the JWT-acquisition curl with `<seed-password>` placeholder explicitly — operator never pastes the literal password into the runbook; only the curl pattern. The `Authentication-Results` line pasted into Section 5 contains no secrets (it's a public-by-definition mail header). |

</threat_model>

<verification>
**On PASS path:**
- 119-RUNBOOK.md Section 1 contains re-snapshot dig output (`p=none`)
- 119-RUNBOOK.md Section 4 contains post-ramp dig output (`p=quarantine`)
- 119-RUNBOOK.md Section 5 contains both smoke legs' `Authentication-Results` lines, both with `dmarc=pass`
- 119-RUNBOOK.md Section 6 PASS branch contains date + evidence reference; FAIL + DEFERRED contain `N/A`
- `dig TXT _dmarc.vigilhub.io +short` returns string containing `p=quarantine`

**On FAIL path:**
- 119-RUNBOOK.md Section 6 FAIL branch contains date + reason + re-eval date; PASS + DEFERRED contain `N/A`
- 119-RUNBOOK.md Sections 1, 4, 5 may be blank (no ramp executed)
- `dig TXT _dmarc.vigilhub.io +short` STILL returns `p=none` (no mutation)
- Follow-up todo exists capturing re-eval date

**On DEFERRED path:**
- 119-RUNBOOK.md Section 6 DEFERRED branch contains date + partial-criteria reason + next routine fire date; PASS + FAIL contain `N/A`
- `dig TXT _dmarc.vigilhub.io +short` STILL returns `p=none`

**On rollback path (post-PASS but trigger fired):**
- 119-RUNBOOK.md Section 7 contains timestamp + trigger # + reason + post-rollback dig + post-rollback smoke headers
- `dig TXT _dmarc.vigilhub.io +short` returns `p=none` post-rollback
- Follow-up todo exists capturing root-cause investigation
</verification>

<success_criteria>
1. **ROADMAP SC #1** — Auto-eval routine produced a documented PASS / FAIL / DEFERRED determination, captured in 119-RUNBOOK.md Section 6.
2. **ROADMAP SC #2** (PASS branch only) — Cloudflare TXT record at `_dmarc.vigilhub.io` was updated `p=none` → `p=quarantine` with other tags preserved; verified via `dig TXT _dmarc.vigilhub.io +short` returning the new value.
3. **ROADMAP SC #3** (FAIL/DEFERRED branch) — Ramp did not fire; deferral reason and re-eval date documented in Section 6.
4. **ROADMAP SC #4** (PASS branch) — Both smoke legs (forgot-password + resend-verification) produced Gmail-delivered messages with `dmarc=pass` in raw `Authentication-Results` headers, captured verbatim in Section 5.
5. **D-06 rollback path** (only if triggered) — TXT reverted to `p=none` via manual dashboard edit; rollback evidence captured in Section 7; root-cause follow-up todo opened.
6. Across all paths: D-03 preserved (no `CF_API_TOKEN`, no scripted DNS tooling — manual dashboard only).
</success_criteria>

<output>
After completion, create `.planning/phases/119-dmarc-quarantine-ramp/119-02-SUMMARY.md` capturing:
- Which terminal branch (PASS / FAIL / DEFERRED / rollback) was reached
- Auto-eval routine fire date and verbatim determination
- If PASS: post-ramp dig output + both smoke legs' `Authentication-Results`
- If FAIL/DEFERRED: deferral reason and re-eval date
- If rollback: trigger number, reason, post-rollback evidence
- Confirmation that 119-RUNBOOK.md was updated in-place (no new file)
- Note: phase requirement OPS-02 is "satisfied" only on PASS path; FAIL/DEFERRED leave OPS-02 as "Pending — re-eval YYYY-MM-DD" in REQUIREMENTS.md traceability table
</output>
