---
phase: 97-mac-cli-print-reliability
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified: []
autonomous: false
requirements: [FIX-03]

must_haves:
  truths:
    - "Running dailybrief generate locally prints the brief to the HP printer"
    - "The printed output is actual-size (not scaled to fill Letter paper)"
    - "Monitor shows red badge when printer is paused and CLI is run"
    - "Doctor subcommand shows all checks passing including printer reachability"
    - "Legacy LaunchAgent is fully removed"
  artifacts: []
  key_links:
    - from: "StatusChecker.refresh()"
      to: "Monitor title bar icon"
      via: "didFailNonStale drives red exclamationmark.circle.fill"
      pattern: "didFailNonStale"
---

<objective>
Verify the full print chain end-to-end per D-01: run the CLI, confirm physical print output at 100% scale, verify Monitor badge behavior on success and failure, and confirm Doctor passes all checks.

Purpose: D-01 requires full chain verification — CLI-only testing is insufficient. This checkpoint confirms the code changes from Plan 01 actually work with the physical printer.
Output: Verified working print path, ready for next scheduled run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-mac-cli-print-reliability/97-CONTEXT.md
@.planning/phases/97-mac-cli-print-reliability/97-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CLI binary and install to ~/.local/bin</name>
  <files></files>
  <read_first>
    - Sources/DailyBrief/Utilities/PrintService.swift
    - Sources/DailyBrief/DailyBrief.swift
  </read_first>
  <action>
Build the CLI in release mode and install:

```bash
cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief"
swift build -c release 2>&1
cp .build/release/DailyBrief ~/.local/bin/DailyBrief
```

Then run the Doctor check to confirm all checks pass including the new printer reachability:
```bash
~/.local/bin/DailyBrief doctor
```

Expected output: all checks PASS including "Printer reachable (HP_OfficeJet_9120e_Series)".
  </action>
  <verify>
    <automated>~/.local/bin/DailyBrief doctor 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `swift build -c release` exits 0
    - `~/.local/bin/DailyBrief` file exists and is newer than the build
    - `dailybrief doctor` output contains "[PASS] Printer reachable"
    - `dailybrief doctor` exits 0 (all checks pass)
  </acceptance_criteria>
  <done>CLI binary rebuilt with all fixes and installed, Doctor passes all checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full print chain end-to-end (D-01)</name>
  <files></files>
  <action>
Human verification of the full print chain. The executor presents the verification steps to the user and waits for confirmation. No code changes in this task.

Steps for the user:
1. Print test: Run `~/.local/bin/DailyBrief generate` in Terminal
2. Failure badge test: Pause printer in System Settings, run CLI, observe Monitor
3. Doctor check: Run `~/.local/bin/DailyBrief doctor`
4. Legacy cleanup: Run `launchctl list com.jameson.dailysheet-print 2>&1`
5. 404 fallback: If server brief is missing, CLI should auto-regenerate
  </action>
  <verify>User confirms all checks pass</verify>
  <done>User has verified physical print output is actual-size, Monitor badge works, Doctor passes, legacy agent removed</done>
  <what-built>
Fixed print path: PrintService throws on lpr failure, checks printer reachability, uses 100% scale flags, CLI falls back to POST generate on 404, Monitor shows red badge on failure, Doctor checks printer, legacy LaunchAgent removed.
  </what-built>
  <read_first>
    - Sources/DailyBrief/Utilities/PrintService.swift
    - Sources/DailyBrief/DailyBrief.swift
  </read_first>
  <how-to-verify>
1. **Print test:** Run `~/.local/bin/DailyBrief generate` in Terminal. Confirm:
   - Terminal shows "PDF received" and "PDF sent to printer" (no errors)
   - Paper comes out of the HP OfficeJet 9120e
   - Output is actual-size (3.75" x 7.5" content area, NOT scaled to fill Letter paper)

2. **Failure badge test:** Pause the printer in System Settings > Printers & Scanners, then run `~/.local/bin/DailyBrief generate`. Confirm:
   - Terminal shows an error about printer not reachable
   - Monitor menu bar icon shows red exclamation mark (not monochrome)
   - Resume the printer after testing

3. **Doctor check:** Run `~/.local/bin/DailyBrief doctor` and confirm "Printer reachable" shows PASS

4. **Legacy cleanup:** Run `launchctl list com.jameson.dailysheet-print 2>&1` and confirm it says "Could not find service"

5. **404 fallback (if testable):** If the server brief is missing (404 on GET), the CLI should automatically trigger POST /v1/brief/generate and succeed. This may only be testable if the server was recently restarted.
  </how-to-verify>
  <acceptance_criteria>
    - Physical print output is actual-size (not scaled)
    - Monitor shows red badge when printer is paused
    - Doctor reports all checks PASS
    - Legacy LaunchAgent is gone
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is verification-only — no new code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05 | Tampering | CLI binary install | accept | User copies from their own build output to their own ~/.local/bin — same trust domain |
</threat_model>

<verification>
1. `swift build -c release` succeeds
2. `dailybrief doctor` passes all checks including printer reachability
3. Physical print output confirmed actual-size by user
4. Monitor badge confirmed visually by user
5. Legacy agent confirmed removed
</verification>

<success_criteria>
- CLI binary rebuilt and installed
- Doctor passes all checks
- User confirms physical print is actual-size
- User confirms Monitor red badge on failure
- User confirms legacy agent removed
</success_criteria>

<output>
After completion, create `.planning/phases/97-mac-cli-print-reliability/97-02-SUMMARY.md`
</output>
