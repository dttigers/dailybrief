---
phase: 42-mac-app-server-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Config/AppConfig.swift
  - Sources/JarvisCore/Services/VigilAPIClient.swift
  - Sources/DailyBriefMonitor/AppDelegate.swift
  - Sources/DailyBrief/DailyBrief.swift
autonomous: true
---

<objective>
Add Bearer token authentication to the Mac app's VigilAPIClient and point it at the production Railway server URL.

Purpose: Enable the Mac app (both CLI and menu bar monitor) to connect to the deployed Vigil Core API with proper authentication, completing the server migration for this client.
Output: Mac app sends Bearer auth headers on all API requests, default config points at production URL, build succeeds.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 39 deployed the production server — need the URL and auth pattern:
@.planning/phases/39-railway-deployment/39-02-SUMMARY.md

# Phase 41 did the same migration for G2 plugin — follow same Bearer auth pattern:
@.planning/phases/41-g2-plugin-production-url/41-01-SUMMARY.md

# Key source files to modify:
@Sources/JarvisCore/Config/AppConfig.swift
@Sources/JarvisCore/Services/VigilAPIClient.swift
@Sources/DailyBriefMonitor/AppDelegate.swift
@Sources/DailyBrief/DailyBrief.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API key to VigilConfig and inject Bearer auth into VigilAPIClient</name>
  <files>Sources/JarvisCore/Config/AppConfig.swift, Sources/JarvisCore/Services/VigilAPIClient.swift, Sources/DailyBriefMonitor/AppDelegate.swift, Sources/DailyBrief/DailyBrief.swift</files>
  <action>
    1. In AppConfig.swift VigilConfig struct (~line 390):
       - Add `public var apiKey: String` field (default empty string)
       - Update init to include `apiKey: String = ""`
       - Update default apiBaseUrl from `http://localhost:3001/v1` to `https://vigil-core-production.up.railway.app/v1`

    2. In VigilAPIClient.swift:
       - Add `private let apiKey: String?` property
       - Update init to accept `apiKey: String? = nil`
       - Create private helper `private func applyHeaders(_ request: inout URLRequest)` that:
         - Sets Accept: application/json
         - If apiKey is non-nil and non-empty, sets `Authorization: Bearer {apiKey}`
       - Refactor all HTTP methods (get, post, put, delete, postNoResponse) to call `applyHeaders(&request)` instead of manually setting Accept header on each method. Content-Type for POST/PUT stays inline since only those methods need it.

    3. In AppDelegate.swift: Update VigilAPIClient initialization to pass apiKey from config:
       - Read `config.vigil?.apiKey` alongside existing `apiBaseURL`
       - Pass to `VigilAPIClient(baseURL: ..., apiKey: apiKey)`

    4. In DailyBrief.swift: Same update — pass apiKey from config to VigilAPIClient init.

    Pattern reference: G2 plugin's api.ts uses the same Bearer auth pattern with `Authorization: Bearer ${apiKey}` header.
  </action>
  <verify>
    swift build 2>&1 | tail -5
    Expect: "Build complete!" with no errors
  </verify>
  <done>VigilAPIClient accepts optional apiKey and attaches Bearer header to all requests. Both entry points pass apiKey from config. Production URL is the new default. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update user config and validate end-to-end</name>
  <files>~/.config/dailybrief/config.json</files>
  <action>
    1. Update user's config.json vigil section:
       - Set `api_base_url` to `https://vigil-core-production.up.railway.app/v1`
       - Add `api_key` field (user will need to provide their production API key — use empty string as placeholder and note in verification)

    2. Validate the production API is reachable from the Mac app by running a quick curl test:
       ```
       curl -s https://vigil-core-production.up.railway.app/v1/health
       ```
       Expected: `{"status":"ok","database":"connected"}`

    3. Verify the build runs clean:
       ```
       swift build 2>&1 | tail -5
       ```

    Note: The actual API key value is stored in the user's config file. The key prefix `vk_e2e2fae0` was generated in phase 39 — the full key must be retrieved from wherever the user saved it. Set api_key to empty string and note that user must fill it in.
  </action>
  <verify>
    curl -s https://vigil-core-production.up.railway.app/v1/health | grep -q "ok" && echo "API reachable" || echo "API unreachable"
    swift build 2>&1 | tail -3
  </verify>
  <done>Config points at production URL. Health check confirms API reachable. Build succeeds. User informed they need to add their API key to config.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] VigilAPIClient attaches Bearer header when apiKey provided
- [ ] VigilAPIClient omits Authorization header when apiKey is nil/empty (backward compat for local dev)
- [ ] Both AppDelegate.swift and DailyBrief.swift pass apiKey to client
- [ ] Production health endpoint returns OK
- [ ] User config updated with production URL
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Mac app VigilAPIClient sends Bearer auth on all requests when API key configured
- Default config URL points at production Railway server
- Local dev still works without API key (graceful fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/42-mac-app-server-migration/42-01-SUMMARY.md`
</output>
