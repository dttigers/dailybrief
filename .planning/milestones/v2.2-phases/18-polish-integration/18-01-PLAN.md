---
phase: 18-polish-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Sources/JarvisCore/Config/AppConfig.swift, Sources/DailyBrief/Services/GmailService.swift, Sources/DailyBrief/DailyBrief.swift, Sources/DailyBriefMonitor/Settings/SettingsView.swift, Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift]
autonomous: true
---

<objective>
Make IMAP connection configurable instead of hardcoded to Gmail. Rename GmailConfig → EmailConfig with host/port/TLS fields so user can connect directly to any IMAP server (e.g., work email) without Gmail forwarding.

Purpose: Eliminates Gmail as middleman for work email, simplifies setup, enables direct IMAP access to any server.
Output: Configurable IMAP settings with backward-compatible config migration, updated Settings UI labels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/imap-direct-work-email.md

@Sources/JarvisCore/Config/AppConfig.swift
@Sources/DailyBrief/Services/GmailService.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBriefMonitor/Settings/SettingsView.swift
@Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename GmailConfig → EmailConfig with configurable IMAP fields</name>
  <files>Sources/JarvisCore/Config/AppConfig.swift, Sources/DailyBrief/Services/GmailService.swift, Sources/DailyBrief/DailyBrief.swift</files>
  <action>
1. In AppConfig.swift, rename `GmailConfig` to `EmailConfig`. Add fields:
   - `imapHost: String` (default: "imap.gmail.com")
   - `imapPort: Int` (default: 993)
   - `useTLS: Bool` (default: true)
   Keep existing fields (email, appPassword, searchSubjectPattern, lookbackDays).
   Add CodingKeys with `gmail` as alternate key for backward compatibility — implement custom `init(from decoder:)` on AppConfig so existing configs with `"gmail": {...}` key decode into the new `email` property.
   The AppConfig property should be renamed from `gmail` to `email`.

2. In GmailService.swift, rename to EmailService (rename file too). Update the IMAPClient actor to use `config.imapHost` and `config.imapPort` instead of hardcoded "imap.gmail.com" and 993. Pass host/port into the Python IMAP script arguments. Update the Python script template to accept host and port parameters.

3. In DailyBrief.swift (CLI), update references from `config.gmail` to `config.email` and `GmailService` to `EmailService`.

Important: The Python subprocess IMAP script currently hardcodes `imap.gmail.com:993`. Update it to use passed-in host and port arguments. Ensure credentials are still passed the same way (as script arguments — the security concern is noted but not in scope for this task).
  </action>
  <verify>swift build succeeds with no errors. Existing config.json files with "gmail" key still load correctly.</verify>
  <done>EmailConfig replaces GmailConfig with configurable host/port/TLS. Backward-compatible config loading. EmailService uses configurable IMAP connection parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Update Settings UI from "Gmail" to "Email/IMAP"</name>
  <files>Sources/DailyBriefMonitor/Settings/SettingsView.swift, Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift</files>
  <action>
1. In SettingsViewModel.swift, rename gmail-related @Published properties:
   - `gmailEmail` → `emailAddress` (or similar)
   - `gmailAppPassword` → `emailAppPassword`
   - Add `imapHost`, `imapPort`, `useTLS` published properties
   - Update loadConfig/saveConfig to use `config.email` instead of `config.gmail`

2. In SettingsView.swift, update the Gmail section:
   - Section header: "Email / IMAP" instead of "Gmail"
   - Add IMAP Host text field (placeholder: "imap.gmail.com")
   - Add IMAP Port text field (placeholder: "993")
   - Add Use TLS toggle (default on)
   - Rename "Gmail Address" label to "Email Address"
   - Rename "App Password" label to "IMAP Password"
   - Keep search subject pattern and lookback days as-is

Layout: Group the connection settings (host, port, TLS) together, then credentials (email, password), then search settings (pattern, lookback).
  </action>
  <verify>swift build succeeds. Settings UI shows Email/IMAP section with all configurable fields.</verify>
  <done>Settings UI shows "Email / IMAP" section with configurable host, port, TLS, email, password fields. Config saves and loads correctly with new field names.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] Existing config.json with `"gmail"` key loads without error (backward compat)
- [ ] New config saves with `"email"` key containing host/port/useTLS fields
- [ ] Settings UI shows "Email / IMAP" section with all fields
</verification>

<success_criteria>

- All tasks completed
- GmailConfig fully renamed to EmailConfig
- IMAP host/port/TLS are configurable (not hardcoded)
- Backward-compatible config loading from "gmail" key
- Settings UI updated with new labels and fields
- No compiler errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/18-polish-integration/18-01-SUMMARY.md`
</output>
