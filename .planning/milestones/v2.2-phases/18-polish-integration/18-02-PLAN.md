---
phase: 18-polish-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [Sources/JarvisCore/Utilities/ImageConversion.swift, Sources/JarvisCore/Services/FolderWatcherService.swift, Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift]
autonomous: true
---

<objective>
Extract duplicated JPEG conversion logic into a shared utility and perform cross-feature code cleanup from v1.2 phases.

Purpose: Reduce code duplication introduced during rapid feature development in phases 14-17. Clean shared utility for HEIC/TIFF/BMP → JPEG conversion.
Output: Shared ImageConversion utility in JarvisCore, deduplicated callers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Services/FolderWatcherService.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared JPEG conversion utility</name>
  <files>Sources/JarvisCore/Utilities/ImageConversion.swift, Sources/JarvisCore/Services/FolderWatcherService.swift, Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift</files>
  <action>
1. Create `Sources/JarvisCore/Utilities/ImageConversion.swift` with a public enum `ImageConversion` containing:
   - `static func convertToJPEG(from url: URL) throws -> URL` — converts HEIC/TIFF/BMP to JPEG, returns path to converted file. Reuse the existing CoreGraphics-based conversion logic from either FolderWatcherService or DashboardViewModel (they should be equivalent).
   - `static func needsConversion(_ url: URL) -> Bool` — checks file extension against convertible types (heic, heif, tiff, tif, bmp).

2. In FolderWatcherService.swift, replace the inline JPEG conversion code with a call to `ImageConversion.convertToJPEG(from:)`. Remove the duplicated conversion function.

3. In DashboardViewModel.swift, replace the inline JPEG conversion code with a call to `ImageConversion.convertToJPEG(from:)`. Remove the duplicated conversion function.

Keep error handling the same — if conversion fails, callers should handle it as they do now (skip file or show error).
  </action>
  <verify>swift build succeeds. Grep for the old conversion function names to confirm they're removed from both callers.</verify>
  <done>Single ImageConversion utility in JarvisCore used by both FolderWatcherService and DashboardViewModel. No duplicate conversion logic.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-feature cleanup audit</name>
  <files>Sources/DailyBrief/DailyBrief.swift, Sources/JarvisCore/Services/CloudKitManager.swift</files>
  <action>
1. Verify the CloudKit availability guard added in 17-04 is properly placed — `CloudKitManager.isAvailable` should be checked before any CloudKit operations to prevent SIGILL on unsigned/non-entitled builds. Confirm the guard exists and is correct; no changes needed if it's already there.

2. In DailyBrief.swift (CLI main generate flow), verify that multi-sport data flows correctly end-to-end:
   - AppConfig loads sport configs for all 4 leagues
   - ESPNSportsService is called for enabled non-MLB sports
   - DailyBriefData.additionalSports is populated
   - PageTwoRenderer receives and renders all sport data
   Read through the flow and confirm it's clean. Only make changes if there are actual issues found.

3. Check that the task status workflow (Phase 16) integrates cleanly with CloudKit sync — TaskStatus should sync correctly. Read the relevant sync mapping code and confirm taskStatus is included.

This is an audit task — only modify code if actual issues are found. Don't add features or refactor working code.
  </action>
  <verify>swift build succeeds. No regressions in any integration points.</verify>
  <done>Cross-feature integration verified: CloudKit guard in place, multi-sport data flow correct, task status syncs properly. Any issues found are fixed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] ImageConversion utility exists in JarvisCore
- [ ] No duplicate JPEG conversion code in FolderWatcherService or DashboardViewModel
- [ ] CloudKit, multi-sport, and task status integrations verified
</verification>

<success_criteria>

- All tasks completed
- JPEG conversion deduplicated into shared utility
- Cross-feature integration points verified
- No compiler errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/18-polish-integration/18-02-SUMMARY.md`
</output>
