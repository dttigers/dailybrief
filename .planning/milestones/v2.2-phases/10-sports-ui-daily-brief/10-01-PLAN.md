---
phase: 10-sports-ui-daily-brief
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Models/MLBTeamData.swift
  - Sources/JarvisCore/Config/AppConfig.swift
  - Sources/DailyBriefMonitor/Settings/SettingsView.swift
  - Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift
autonomous: true
---

<objective>
Replace raw numeric MLB ID steppers in Sports settings with a team name picker dropdown, and update the config to store team/division names alongside IDs.

Purpose: Users shouldn't need to know MLB Stats API IDs to pick their team. Selecting a team by name should auto-populate teamId, divisionId, leagueId, and store the team/division names for use by the PDF renderer.
Output: MLBTeamData model with all 30 teams, updated SportsConfig, and a team picker UI in Settings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Config/AppConfig.swift
@Sources/JarvisCore/Models/GameScore.swift
@Sources/JarvisCore/Models/StandingsEntry.swift
@Sources/DailyBriefMonitor/Settings/SettingsView.swift
@Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MLBTeamData model and update SportsConfig</name>
  <files>Sources/JarvisCore/Models/MLBTeamData.swift, Sources/JarvisCore/Config/AppConfig.swift</files>
  <action>
  1. Create `MLBTeamData.swift` in JarvisCore/Models with:
     - `public struct MLBTeam` with fields: `id: Int`, `name: String`, `abbreviation: String`, `divisionId: Int`, `divisionName: String`, `leagueId: Int`, `leagueName: String`
     - `public enum MLBTeamData` with a static `allTeams: [MLBTeam]` array containing all 30 MLB teams. Use the MLB Stats API team IDs. Group by division for readability.
     - Static helper: `team(forId:) -> MLBTeam?`
     - Static helper: `teams(inDivision:) -> [MLBTeam]` (returns teams in a given divisionId)
     - Divisions: AL East (201), AL Central (202), AL West (200), NL East (204), NL Central (205), NL West (203)

  MLB Teams (2025 roster):
  - AL East (201, leagueId 103): Baltimore Orioles (110), Boston Red Sox (111), New York Yankees (147), Tampa Bay Rays (139), Toronto Blue Jays (141)
  - AL Central (202, leagueId 103): Chicago White Sox (145), Cleveland Guardians (114), Detroit Tigers (116), Kansas City Royals (118), Minnesota Twins (142)
  - AL West (200, leagueId 103): Houston Astros (117), Los Angeles Angels (108), Oakland Athletics (133), Seattle Mariners (136), Texas Rangers (140)
  - NL East (204, leagueId 104): Atlanta Braves (144), Miami Marlins (146), New York Mets (121), Philadelphia Phillies (143), Washington Nationals (120)
  - NL Central (205, leagueId 104): Chicago Cubs (112), Cincinnati Reds (113), Milwaukee Brewers (158), Pittsburgh Pirates (134), St. Louis Cardinals (138)
  - NL West (203, leagueId 104): Arizona Diamondbacks (109), Colorado Rockies (115), Los Angeles Dodgers (119), San Diego Padres (135), San Francisco Giants (137)

  Abbreviations: ARI, ATL, BAL, BOS, CHC, CWS, CIN, CLE, COL, DET, HOU, KC, LAA, LAD, MIA, MIL, MIN, NYM, NYY, OAK, PHI, PIT, SD, SF, SEA, STL, TB, TEX, TOR, WSH

  2. Update `SportsConfig` in AppConfig.swift:
     - Add `teamName: String` (default: "Detroit Tigers")
     - Add `divisionName: String` (default: "AL Central")
     - Keep existing `teamId`, `divisionId`, `leagueId` fields for backward compatibility
     - Existing configs without teamName/divisionName will decode fine using defaults (Codable with defaults handles this)
  </action>
  <verify>swift build compiles without errors</verify>
  <done>MLBTeamData contains all 30 teams with correct IDs. SportsConfig has teamName and divisionName fields that default correctly for existing configs.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Sports settings with team name picker</name>
  <files>Sources/DailyBriefMonitor/Settings/SettingsView.swift, Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift</files>
  <action>
  1. Update SettingsViewModel:
     - Replace `sportsTeamId`, `sportsDivisionId`, `sportsLeagueId` Int properties with a single `sportsSelectedTeamId: Int` property
     - Add computed `sportsTeamName: String` and `sportsDivisionName: String` derived from MLBTeamData.team(forId:)
     - In `loadConfig()`: set `sportsSelectedTeamId` from config.sports.teamId
     - In `save()`: look up selected team from MLBTeamData, write teamId + divisionId + leagueId + teamName + divisionName to config
     - The save should auto-populate all 5 fields from the single team selection

  2. Update SettingsView Sports tab:
     - Replace the 3 Stepper controls with a single `Picker` control
     - Use `MLBTeamData.allTeams` as the data source
     - Group teams by division in the picker (use SwiftUI `Section` headers with division names)
     - Display selected team's division info below the picker as read-only context: "Division: AL Central | League: American League"
     - Bind picker selection to `viewModel.sportsSelectedTeamId`
     - Use `.pickerStyle(.menu)` for a dropdown, or `.pickerStyle(.inline)` with a List if menu doesn't support sections well. Test which looks better — menu style is simpler.

  Style: Follow existing settings tab patterns (VStack alignment, spacing, font choices). Look at how other tabs (Gmail, AI, Folders) are structured for consistency.
  </action>
  <verify>swift build compiles. Open Settings > Sports tab and verify team picker dropdown appears with all 30 teams. Selecting a team should show the correct division info.</verify>
  <done>Sports settings tab shows team name picker instead of numeric steppers. Selecting any team auto-populates all config fields (teamId, divisionId, leagueId, teamName, divisionName).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for both targets
- [ ] MLBTeamData contains exactly 30 teams with correct IDs
- [ ] SportsConfig includes teamName and divisionName fields
- [ ] Sports settings tab shows team picker (not steppers)
- [ ] Saving config writes all 5 sports fields correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Selecting a team in Settings auto-populates teamId, divisionId, leagueId, teamName, divisionName
- Existing config.json files without teamName/divisionName load without error (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/10-sports-ui-daily-brief/10-01-SUMMARY.md`
</output>
