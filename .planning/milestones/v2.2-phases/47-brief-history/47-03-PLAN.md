---
phase: 47-brief-history
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified: [Sources/DailyBriefMonitor/Dashboard/BriefHistoryView.swift, Sources/DailyBriefMonitor/Dashboard/BriefHistoryViewModel.swift, Sources/DailyBriefMonitor/Dashboard/DashboardView.swift, Sources/JarvisCore/Services/VigilAPIClient.swift]
autonomous: true
---

<objective>
Add brief history browsing to the SwiftUI dashboard — list past briefs and open/reprint from the UI.

Purpose: Users can browse past daily briefs from the dashboard window, see what was generated on any date, and reprint PDFs directly.
Output: Brief history list view and detail view integrated into the existing dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-brief-history/47-01-SUMMARY.md

# Dashboard structure and patterns:
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/AppDelegate.swift
@Sources/JarvisCore/Services/VigilAPIClient.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BriefHistoryView and ViewModel</name>
  <files>Sources/DailyBriefMonitor/Dashboard/BriefHistoryView.swift, Sources/DailyBriefMonitor/Dashboard/BriefHistoryViewModel.swift</files>
  <action>
1. Create `BriefHistoryViewModel` as an @Observable class:
   - Properties: `briefs: [BriefRecord]` (array of brief history entries), `isLoading: Bool`, `selectedBrief: BriefRecord?`, `errorMessage: String?`
   - `BriefRecord` struct (Codable): date, thoughtCount, taskCount, pdfFilename, summary (decoded JSON with category counts, top tasks, affirmation, etc.), createdAt
   - `loadHistory()` — calls VigilAPIClient to GET /v1/briefs, populates array
   - `openPDF(for brief: BriefRecord)` — finds PDF at configured output directory + pdfFilename, opens with NSWorkspace.shared.open()
   - `reprintPDF(for brief: BriefRecord)` — finds PDF file and sends to printer via `lpr` Process (follow PrintService pattern)
   - Inject VigilAPIClient (or the relevant protocol) via init

2. Add `fetchBriefHistory` method to VigilAPIClient if not already added by Plan 02 (check first — Plan 02 adds this too, but this plan runs in parallel so add it if missing):
   - GET /v1/briefs?limit=60
   - Decode response into [BriefRecord]

3. Create `BriefHistoryView` SwiftUI view:
   - List of briefs grouped by month (use SwiftUI Section with month/year headers)
   - Each row shows: date (formatted nicely, e.g., "Saturday, April 5"), thought count, task count, brief status indicators
   - Tapping a row shows detail in a sheet or inline:
     - Date and generation time
     - Summary stats: N thoughts (breakdown by category), N open tasks
     - Top tasks listed
     - Affirmation text
     - Sports summary (if present)
     - Buttons: "Open PDF" (opens in Preview), "Reprint" (sends to printer)
   - Empty state: "No brief history yet. Generate your first brief with `dailybrief generate`."
   - Pull-to-refresh or refresh button to reload

Follow existing SwiftUI patterns from DashboardView (NavigationSplitView style, AppKit colors, similar spacing/typography).
  </action>
  <verify>
1. `swift build` succeeds
2. BriefHistoryView renders without crashes (can verify by opening dashboard)
  </verify>
  <done>BriefHistoryView displays past briefs grouped by month with detail view, open PDF, and reprint actions. ViewModel handles API loading and local PDF access.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate brief history into dashboard navigation</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardView.swift</files>
  <action>
Add a "Brief History" section/tab to the existing DashboardView. Looking at the current DashboardView structure (NavigationSplitView with sidebar):

1. Add a sidebar item for "Brief History" — could be:
   - A new sidebar section below the existing filters (e.g., a "History" label with a clock icon)
   - Or a toolbar button that presents the history view

2. When the user selects "Brief History" from the sidebar, show BriefHistoryView in the detail area (replacing the thoughts list).

3. Wire up the BriefHistoryViewModel with the same VigilAPIClient instance used by the rest of the dashboard (from AppDelegate's dependency injection).

4. Load brief history when the view appears (onAppear → loadHistory()).

Keep it simple — a single sidebar entry that switches the detail view content. Don't overcomplicate the navigation.
  </action>
  <verify>
1. `swift build` succeeds
2. Dashboard opens, Brief History appears in sidebar
3. Clicking Brief History shows the list (may be empty if no briefs saved yet)
  </verify>
  <done>Brief History is accessible from the dashboard sidebar. Clicking it shows the history list with detail view, PDF opening, and reprint functionality. Fully integrated with existing dashboard navigation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] Dashboard shows Brief History in sidebar
- [ ] Brief history list loads from API
- [ ] Open PDF button works (opens in Preview)
- [ ] Reprint button sends to printer
</verification>

<success_criteria>
- All tasks completed
- Brief History accessible from dashboard sidebar
- List view shows past briefs grouped by month
- Detail view shows summary stats and actions
- Open PDF and Reprint buttons functional
- No regressions in existing dashboard functionality
</success_criteria>

<output>
After completion, create `.planning/phases/47-brief-history/47-03-SUMMARY.md`
</output>
