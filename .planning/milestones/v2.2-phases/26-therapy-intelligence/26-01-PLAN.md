---
phase: 26-therapy-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Models/Thought.swift
  - Sources/JarvisCore/Storage/DatabaseManager.swift
  - Sources/JarvisCore/Services/TherapyClassificationService.swift
  - Sources/JarvisCore/Services/CloudKitManager.swift
  - Sources/JarvisCore/Services/SyncService.swift
  - Sources/JarvisCore/Storage/ThoughtStore.swift
autonomous: true
---

<objective>
Add TherapyClassification enum, database migration, CloudKit sync support, and TherapyClassificationService actor for classifying therapy-category thoughts as self-learnable vs bring-to-therapist.

Purpose: Foundation for therapy intelligence — model, storage, sync, and AI classification service.
Output: TherapyClassification enum on Thought model, v4 DB migration, CloudKit mapping, classification service actor, ThoughtStore query support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Models/Thought.swift
@Sources/JarvisCore/Storage/DatabaseManager.swift
@Sources/JarvisCore/Services/TriageService.swift
@Sources/JarvisCore/Services/CloudKitManager.swift
@Sources/JarvisCore/Services/SyncService.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TherapyClassification enum, Thought model field, DB migration, CloudKit sync, and ThoughtStore query support</name>
  <files>Sources/JarvisCore/Models/Thought.swift, Sources/JarvisCore/Storage/DatabaseManager.swift, Sources/JarvisCore/Services/CloudKitManager.swift, Sources/JarvisCore/Storage/ThoughtStore.swift</files>
  <action>
**Thought.swift:**
1. Add `TherapyClassification` enum (before `SyncStatus`) with cases: `selfLearnable`, `bringToTherapist`. Conform to `String, Codable, Sendable, DatabaseValueConvertible` with raw values `"selfLearnable"` and `"bringToTherapist"`.
2. Add `therapyClassification: TherapyClassification?` property to `Thought` struct (after `taskStatus`). Nil for non-therapy thoughts and therapy thoughts not yet classified.
3. Add `Columns.therapyClassification` static let to the Columns enum.
4. Add `therapyClassification` parameter (default nil) to `init`.

**DatabaseManager.swift:**
Register migration `"v4-therapy-classification"` after `"v3-task-status"`:
```swift
migrator.registerMigration("v4-therapy-classification") { db in
    try db.alter(table: "thoughts") { t in
        t.add(column: "therapyClassification", .text)
    }
}
```
No backfill needed — existing therapy thoughts get nil (classified on next triage or manual action).

**CloudKitManager.swift:**
1. Add `therapyClassification: TherapyClassification?` to `ThoughtRecord` struct.
2. In `toCKRecord`: add `record["therapyClassification"] = thought.therapyClassification?.rawValue as NSString?`
3. In `fromCKRecord`: parse `therapyClassification` the same way `taskStatus` is parsed — read optional String, flatMap to `TherapyClassification(rawValue:)`.
4. Pass `therapyClassification` through to the returned `ThoughtRecord`.

**SyncService.swift:**
In any place where ThoughtRecord fields are mapped to/from Thought (look for where taskStatus is handled in the merge/apply logic), add therapyClassification with the same pattern.

**ThoughtStore.swift:**
Add method `fetchTherapyThoughts(classification: TherapyClassification?, limit: Int, offset: Int) async throws -> [Thought]` that fetches thoughts where `category = 'therapy'` and optionally filters by `therapyClassification`. Follow the existing `fetchFiltered` pattern.

Also add `bulkUpdateTherapyClassification(ids: Set<Int64>, classification: TherapyClassification) throws -> Int` following the exact pattern of `bulkUpdateCategory`.
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>TherapyClassification enum exists, Thought has therapyClassification field, DB migration registered, CloudKit maps the new field, ThoughtStore can query and bulk-update therapy classifications</done>
</task>

<task type="auto">
  <name>Task 2: Create TherapyClassificationService actor</name>
  <files>Sources/JarvisCore/Services/TherapyClassificationService.swift</files>
  <action>
Create a new actor `TherapyClassificationService` in JarvisCore/Services/ following the exact pattern of TriageService:

```swift
public actor TherapyClassificationService {
    private let apiKey: String
    private let model: String

    public init(apiKey: String, model: String) { ... }

    public func classify(_ content: String) async throws -> TherapyClassificationResult { ... }
}
```

**TherapyClassificationResult** (define in same file):
```swift
public struct TherapyClassificationResult: Codable, Sendable {
    public var classification: TherapyClassification
    public var confidence: Double
    public var reasoning: String  // Brief explanation for user transparency
}
```

**System prompt** — this is the critical design element. Use this carefully crafted prompt:

```
You are a therapy thought classifier for a personal journaling tool. Your role is to help the user understand which thoughts they can explore independently vs which would benefit from discussing with their therapist.

IMPORTANT: You are a categorization tool, NOT a therapist. Your classifications are suggestions to help organize therapy prep, not clinical advice.

Classify the thought into one of two categories:

- selfLearnable: The user can likely work through this independently. Includes: self-reflection exercises, mindfulness practices, journaling prompts, cognitive reframing the user could do alone, gratitude practices, goal-setting, habit tracking observations, general emotional check-ins.

- bringToTherapist: This would benefit from professional guidance. Includes: recurring distressing patterns, trauma-related content, relationship conflicts needing mediation perspective, feelings of hopelessness or being stuck, topics the user keeps circling back to without resolution, strong emotional reactions they don't understand, anything involving safety concerns.

When in doubt, classify as bringToTherapist — it's better to over-recommend professional support than under-recommend.

Respond with ONLY a JSON object:
{"classification": "<selfLearnable|bringToTherapist>", "confidence": <0.0-1.0>, "reasoning": "<1 sentence explaining why>"}
```

**API call:** Same pattern as TriageService — POST to Claude API, parse outer response, extract text, parse inner JSON.

**Error handling:** Create `TherapyClassificationError` enum with `apiError(String)` and `parseError(String)` cases, matching TriageError pattern.

**Parse logic:** Map `classification` string to `TherapyClassification` enum. Clamp confidence to 0.0-1.0. Extract reasoning string.
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>TherapyClassificationService actor exists with classify() method, uses Claude API with carefully designed prompt, returns TherapyClassificationResult with classification + confidence + reasoning</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] TherapyClassification enum has selfLearnable and bringToTherapist cases
- [ ] Thought model has optional therapyClassification field
- [ ] DatabaseManager has v4-therapy-classification migration
- [ ] CloudKitManager maps therapyClassification in both directions
- [ ] ThoughtStore has fetchTherapyThoughts and bulkUpdateTherapyClassification
- [ ] TherapyClassificationService compiles with classify() method
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- New service follows exact TriageService actor pattern
- Prompt design errs on side of recommending therapist (safety-first)
</success_criteria>

<output>
After completion, create `.planning/phases/26-therapy-intelligence/26-01-SUMMARY.md`
</output>
