---
phase: 14-launchagent-folder-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Config/AppConfig.swift
  - Sources/JarvisCore/Services/FolderWatcherService.swift
  - Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift
  - Sources/DailyBriefMonitor/Settings/SettingsView.swift
autonomous: true
---

<objective>
Add auto-delete of watched folder files after successful processing.

Purpose: Audio and image files accumulate indefinitely in watched folders (~/Jarvis/Audio, ~/Jarvis/Images). After successful transcription/description and thought capture, the source files should be automatically removed to prevent disk space waste. This follows the same pattern as PDF cleanup (keepDays-based deletion).

Output: FolderWatchingConfig gains `autoDeleteAfterProcessing` flag, FolderWatcherService deletes files after successful processing, Settings UI exposes the toggle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Config/AppConfig.swift
@Sources/JarvisCore/Services/FolderWatcherService.swift
@Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift
@Sources/DailyBriefMonitor/Settings/SettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-delete config and implement cleanup in FolderWatcherService</name>
  <files>Sources/JarvisCore/Config/AppConfig.swift, Sources/JarvisCore/Services/FolderWatcherService.swift</files>
  <action>
1. **AppConfig.swift** — Add config property to `FolderWatchingConfig`:
   - Add `public var autoDeleteAfterProcessing: Bool` property
   - Default to `false` in the init (opt-in, not opt-out — user must explicitly enable deletion)
   - This keeps backward compatibility with existing config files (missing key defaults to false via Codable)

2. **FolderWatcherService.swift** — Delete files after successful processing:
   - In `scanFolder(path:type:)`, after `processedFiles.insert(filename)` and `Self.saveManifest(processedFiles)` succeed, check `config.autoDeleteAfterProcessing`
   - If true, delete the source file using `FileManager.default.removeItem(atPath: fullPath)`
   - Wrap deletion in do/catch — log on failure but don't treat as error (file already processed successfully)
   - Log: `NSLog("FolderWatcherService: Auto-deleted processed file %@", filename)`
   - On delete failure: `NSLog("FolderWatcherService: Failed to auto-delete %@: %@", filename, error.localizedDescription)`
   - Do NOT remove from processedFiles manifest on deletion — keep the record so the filename is still tracked even if a new file with the same name appears later
  </action>
  <verify>
    - Build succeeds: `cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && swift build 2>&1 | tail -5`
    - FolderWatchingConfig has autoDeleteAfterProcessing property
    - FolderWatcherService checks config before deleting
  </verify>
  <done>FolderWatchingConfig has autoDeleteAfterProcessing (default false). FolderWatcherService deletes source files after successful processing when enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-delete toggle to folder watching Settings UI</name>
  <files>Sources/DailyBriefMonitor/Settings/SettingsViewModel.swift, Sources/DailyBriefMonitor/Settings/SettingsView.swift</files>
  <action>
1. **SettingsViewModel.swift**:
   - Add property: `var autoDeleteAfterProcessing: Bool = false`
   - In `loadConfig()`, set `autoDeleteAfterProcessing = config.folderWatching.autoDeleteAfterProcessing`
   - In `buildConfig()`, include `autoDeleteAfterProcessing: autoDeleteAfterProcessing` in the FolderWatchingConfig init

2. **SettingsView.swift** — In the Folder Watching section (inside the `if viewModel.folderWatchingEnabled` block):
   - Add a Toggle: `Toggle("Auto-delete files after processing", isOn: $viewModel.autoDeleteAfterProcessing)`
   - Add helper text below the toggle: `Text("Removes audio and image files from watched folders after they've been successfully captured as thoughts.").font(.caption).foregroundColor(.secondary)`
   - Place it after the existing folder path fields, before the restart note
  </action>
  <verify>
    - Build succeeds: `cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && swift build 2>&1 | tail -5`
    - SettingsViewModel loads and saves autoDeleteAfterProcessing
    - SettingsView has the toggle in folder watching section
  </verify>
  <done>Settings UI exposes auto-delete toggle with explanatory text. Config round-trips through load/save correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] FolderWatchingConfig has autoDeleteAfterProcessing property defaulting to false
- [ ] FolderWatcherService conditionally deletes files after processing
- [ ] Settings UI shows toggle when folder watching is enabled
- [ ] Config saves and loads the new property correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Auto-delete defaults to off (safe default)
- Files are only deleted after successful processing
- Settings toggle controls the behavior
</success_criteria>

<output>
After completion, create `.planning/phases/14-launchagent-folder-cleanup/14-02-SUMMARY.md`
</output>
