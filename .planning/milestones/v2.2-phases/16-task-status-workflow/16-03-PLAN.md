---
phase: 16-task-status-workflow
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
  - Sources/DailyBriefMonitor/CaptureView.swift
autonomous: true
---

<objective>
Add task status UI controls to the dashboard — status indicators on task rows, click-to-cycle status, and status filter in sidebar.

Purpose: Let users manage task status (open → in progress → done) directly from the dashboard.
Output: Interactive status controls on task thoughts, status filtering, visual status indicators.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-task-status-workflow/16-01-SUMMARY.md

@Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/CaptureView.swift
@Sources/JarvisCore/Models/Thought.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status indicator and toggle to ThoughtRowView</name>
  <files>Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift, Sources/DailyBriefMonitor/CaptureView.swift</files>
  <action>
  1. In CaptureView.swift (where ThoughtCategory extensions live), add display extensions for TaskStatus:
     ```
     extension TaskStatus {
         var displayName: String { ... }  // "Open", "In Progress", "Done"
         var displayColor: Color { ... }  // .blue, .orange, .green
         var systemImage: String { ... }  // "circle", "circle.dotted.circle", "checkmark.circle.fill" (or similar SF Symbols)
     }
     ```
  
  2. In ThoughtRowView.swift, add a status indicator for task-category thoughts:
     - Accept an `onStatusToggle: (() -> Void)?` closure parameter (nil for non-tasks)
     - For thoughts where category == .task AND taskStatus != nil:
       - Show a clickable status icon (SF Symbol) to the LEFT of content text
       - Icon color matches TaskStatus.displayColor
       - Clicking the icon calls onStatusToggle
     - For non-task thoughts: no status icon (unchanged layout)
     - When taskStatus == .done, apply strikethrough + secondary color to content text
  
  3. Build to verify: swift build
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>Task rows show clickable status icon, done tasks show strikethrough, non-tasks unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add status cycling in ViewModel and status filter in DashboardView</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift, Sources/DailyBriefMonitor/Dashboard/DashboardView.swift</files>
  <action>
  1. In DashboardViewModel.swift, add a method to cycle task status:
     ```
     func cycleTaskStatus(for thought: Thought) async
     ```
     - Cycle: open → inProgress → done → open
     - Call ThoughtStore.updateTaskStatus(id:status:)
     - Refresh the thoughts list after update
  
  2. In DashboardViewModel.swift, add a published property for task status filter:
     - `@Published var taskStatusFilter: TaskStatus? = nil`
     - When category filter is .task AND taskStatusFilter is set, use ThoughtStore.fetchTasks(status:) instead of fetchAll(category:)
     - Update the refresh/load method to respect taskStatusFilter
  
  3. In DashboardViewModel.swift, add task status counts:
     - Add method or computed property to get counts per status (for sidebar display)
     - Call ThoughtStore.countTasks(status:) for open/inProgress/done
  
  4. In DashboardView.swift, update the sidebar:
     - When "Task" category is selected, show sub-filter pills below the category pills:
       "All" | "Open" (count) | "In Progress" (count) | "Done" (count)
     - Tapping a sub-filter sets taskStatusFilter
     - Style: smaller pills, color-coded to match TaskStatus.displayColor
  
  5. In DashboardView.swift, update the List to pass onStatusToggle to ThoughtRowView:
     - For task thoughts: `onStatusToggle: { Task { await viewModel.cycleTaskStatus(for: thought) } }`
     - For non-tasks: `onStatusToggle: nil`
  
  6. Build to verify: swift build
  </action>
  <verify>swift build succeeds for DailyBriefMonitor target</verify>
  <done>Dashboard has status cycling, sub-filter pills when viewing tasks, and task status counts in sidebar</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for DailyBriefMonitor target
- [ ] Task thoughts show clickable status icon
- [ ] Clicking status icon cycles open → inProgress → done → open
- [ ] Done tasks show strikethrough text
- [ ] Task category sidebar shows status sub-filters with counts
- [ ] Non-task thoughts are visually unaffected
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Task status is fully manageable from dashboard UI
  </success_criteria>

<output>
After completion, create `.planning/phases/16-task-status-workflow/16-03-SUMMARY.md`
</output>
