---
phase: 16-task-status-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Models/Thought.swift
  - Sources/JarvisCore/Storage/DatabaseManager.swift
  - Sources/JarvisCore/Storage/ThoughtStore.swift
  - Sources/JarvisCore/Services/CloudKitManager.swift
autonomous: true
---

<objective>
Add TaskStatus enum to Thought model with database migration, store methods, and CloudKit sync support.

Purpose: Foundation for task status workflow — all UI and PDF changes depend on this model layer.
Output: TaskStatus enum (open/inProgress/done), v3 migration, status query/update methods, CloudKit field mapping.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Models/Thought.swift
@Sources/JarvisCore/Storage/DatabaseManager.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
@Sources/JarvisCore/Services/CloudKitManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TaskStatus enum and database migration</name>
  <files>Sources/JarvisCore/Models/Thought.swift, Sources/JarvisCore/Storage/DatabaseManager.swift</files>
  <action>
  1. In Thought.swift, add a new enum after SyncStatus:
     ```
     public enum TaskStatus: String, Codable, Sendable, DatabaseValueConvertible {
         case open
         case inProgress
         case done
     }
     ```
  2. Add `taskStatus` property to Thought struct (optional — nil for non-task thoughts):
     - `public var taskStatus: TaskStatus?`
     - Add to Columns enum: `public static let taskStatus = Column(CodingKeys.taskStatus)`
     - Add to init with default `taskStatus: TaskStatus? = nil`
  3. In DatabaseManager.swift, register migration "v3-task-status":
     - ALTER TABLE thoughts ADD COLUMN taskStatus TEXT (nullable, no default)
     - Backfill existing task-category rows: UPDATE thoughts SET taskStatus = 'open' WHERE category = 'task'
     - This ensures existing tasks get a status while non-task thoughts remain nil
  4. Build to verify: swift build
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>TaskStatus enum exists, Thought has taskStatus property, migration backfills existing tasks as "open"</done>
</task>

<task type="auto">
  <name>Task 2: Add ThoughtStore status methods and CloudKit sync mapping</name>
  <files>Sources/JarvisCore/Storage/ThoughtStore.swift, Sources/JarvisCore/Services/CloudKitManager.swift</files>
  <action>
  1. In ThoughtStore.swift, add method to update task status:
     ```
     public func updateTaskStatus(id: Int64, status: TaskStatus) throws -> Thought
     ```
     - Fetch thought by id, set taskStatus to new value, set modifiedAt to Date(), set syncStatus to .pending, save via update()
  
  2. In ThoughtStore.swift, add method to fetch tasks by status:
     ```
     public func fetchTasks(status: TaskStatus? = nil, limit: Int = 100) async throws -> [Thought]
     ```
     - Filter by category == .task, optionally also by taskStatus
     - Order by createdAt descending
  
  3. In ThoughtStore.swift, add method to count tasks by status:
     ```
     public func countTasks(status: TaskStatus? = nil) async throws -> Int
     ```
     - Filter by category == .task, optionally also by taskStatus
  
  4. In CloudKitManager.swift, update record(from:) to include taskStatus:
     - Add: `record["taskStatus"] = thought.taskStatus?.rawValue as NSString?`
  
  5. In CloudKitManager.swift, update thoughtValues(from:) to parse taskStatus:
     - Add: `let taskStatusRaw = record["taskStatus"] as? String`
     - Add: `let taskStatus = taskStatusRaw.flatMap { TaskStatus(rawValue: $0) }`
  
  6. In CloudKitManager.swift, add taskStatus to ThoughtCloudData struct:
     - Add `public let taskStatus: TaskStatus?` property
     - Update init and all call sites
  
  7. Verify SyncService uses ThoughtCloudData correctly (it should pick up the new field automatically via the existing update pattern).
  
  8. Build to verify: swift build
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>ThoughtStore has updateTaskStatus/fetchTasks/countTasks methods, CloudKit maps taskStatus field bidirectionally</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for both targets (DailyBrief + DailyBriefMonitor)
- [ ] TaskStatus enum has 3 cases: open, inProgress, done
- [ ] Thought model has optional taskStatus property
- [ ] Migration backfills existing tasks as "open"
- [ ] ThoughtStore has updateTaskStatus, fetchTasks, countTasks methods
- [ ] CloudKit record mapping includes taskStatus
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TaskStatus model layer complete and ready for UI/PDF consumption
  </success_criteria>

<output>
After completion, create `.planning/phases/16-task-status-workflow/16-01-SUMMARY.md`
</output>
