---
phase: 20-folder-watcher-manual-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
  - Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
autonomous: true
---

<objective>
Add a manual re-triage button to each thought in the dashboard, allowing the user to re-run AI categorization on any thought at any time.

Purpose: Currently thoughts can only be categorized during capture or file import. If the AI gets it wrong or the user wants to re-categorize after editing, there's no way to trigger re-triage from the dashboard.
Output: Working re-triage button on each thought row with loading state and category update.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Services/TriageService.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
@Sources/JarvisCore/Models/Thought.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/AppDelegate.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reTriageThought method to DashboardViewModel</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift</files>
  <action>
Add a public async method `reTriageThought(_ thought: Thought)` to DashboardViewModel that:
1. Guards on `thought.id` and `triageService` being non-nil
2. Calls `triageService.triage(thought.content)` to get a new TriageResult
3. Fetches the thought from store by ID, updates its `category` and `confidence` fields
4. Calls `store.update(thought)` to persist
5. Calls `loadThoughts()` to refresh the list

Add a `@Published var retriagingThoughtId: Int64?` property to track which thought is currently being re-triaged (for loading state in UI). Set it before the triage call, clear it in a defer block.

Follow the existing pattern from batch import triage (around lines 277-289) — same TriageService call pattern, same error handling with NSLog.
  </action>
  <verify>Project builds with `swift build` — no compilation errors</verify>
  <done>DashboardViewModel has reTriageThought method with loading state tracking</done>
</task>

<task type="auto">
  <name>Task 2: Add re-triage button to ThoughtRowView and wire in DashboardView</name>
  <files>Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift, Sources/DailyBriefMonitor/Dashboard/DashboardView.swift</files>
  <action>
In ThoughtRowView:
1. Add a new callback property: `var onRetriage: (() -> Void)?`
2. Add a new property: `var isRetriaging: Bool = false`
3. In the bottom metadata HStack (where category pill and timestamp are), add a small button with `arrow.trianglehead.2.clockwise` SF Symbol (or `arrow.clockwise` if unavailable) that calls `onRetriage?()` when tapped
4. When `isRetriaging` is true, replace the category pill with a ProgressView spinner and "Categorizing..." text — follow the same visual pattern used in CaptureView (lines 148-161) for the categorizing state
5. When `isRetriaging` is false and `onRetriage` is nil, hide the button (no triage service available)
6. Style the button subtly — use `.secondary` foreground color, small font size, no background. It should not dominate the row.

In DashboardView:
1. Where ThoughtRowView is instantiated in the List/ForEach, pass the new callbacks:
   - `onRetriage: { Task { await viewModel.reTriageThought(thought) } }`
   - `isRetriaging: viewModel.retriagingThoughtId == thought.id`
  </action>
  <verify>swift build succeeds. Launch app, open dashboard — re-triage button visible on each thought row. Click it — category pill shows spinner then updates.</verify>
  <done>Each thought row has a re-triage button. Clicking it calls Claude API, shows loading state, and updates the category pill with the new result.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors or warnings
- [ ] Re-triage button appears on thought rows in dashboard
- [ ] Clicking re-triage shows loading spinner on that row only
- [ ] After triage completes, category pill updates with new category and confidence
- [ ] Other rows remain unaffected during re-triage
- [ ] If triageService is nil (no API key), button is hidden gracefully
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Manual re-triage works end-to-end from dashboard UI
</success_criteria>

<output>
After completion, create `.planning/phases/20-folder-watcher-manual-triage/20-01-SUMMARY.md`
</output>
