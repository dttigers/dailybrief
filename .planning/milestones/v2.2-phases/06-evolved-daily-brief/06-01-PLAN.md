---
phase: 06-evolved-daily-brief
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Sources/JarvisCore/Models/DailyBriefData.swift, Sources/DailyBrief/DailyBrief.swift, Sources/DailyBrief/PDF/PageThreeRenderer.swift, Sources/DailyBrief/PDF/PDFGenerator.swift]
autonomous: true
---

<objective>
Integrate captured thoughts into the daily PDF brief as a new third page showing unprocessed items, today's task priorities, and recent captures.

Purpose: BRIEF-01 — the daily brief becomes the single physical anchor where captured thoughts surface automatically, so nothing stays buried in the database.
Output: DailyBriefData carries thought arrays, Generate command fetches from ThoughtStore, PDF renders a new "Captured Thoughts" page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Models/DailyBriefData.swift
@Sources/JarvisCore/Models/Thought.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
@Sources/JarvisCore/Storage/DatabaseManager.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBrief/PDF/PDFGenerator.swift
@Sources/DailyBrief/PDF/PageOneRenderer.swift
@Sources/DailyBrief/PDF/PageTwoRenderer.swift
@Sources/DailyBrief/PDF/PDFStyles.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thought fields to DailyBriefData and fetch in Generate command</name>
  <files>Sources/JarvisCore/Models/DailyBriefData.swift, Sources/DailyBrief/DailyBrief.swift</files>
  <action>
  1. In DailyBriefData.swift, add three new properties:
     - `unprocessedThoughts: [Thought]` — thoughts with `category == nil` (not yet triaged)
     - `taskThoughts: [Thought]` — thoughts with `category == .task` (today's priorities)
     - `recentThoughts: [Thought]` — thoughts captured in last 24h that ARE triaged and NOT tasks (for awareness)
     Add these to the init with default empty arrays so existing callers don't break.

  2. In DailyBrief.swift Generate.run():
     - Initialize DatabaseManager with default path (try DatabaseManager()). Wrap in do/catch — if DB init fails, log warning and continue with empty thought arrays (graceful degradation, same pattern as 05-03 ConfigLoader.load() failure).
     - Create ThoughtStore with the DatabaseManager.
     - Fetch thoughts concurrently with other data using async let:
       a. `unprocessed`: fetchAll(category: nil) — but ThoughtStore.fetchAll filters by category when non-nil, so for nil-category thoughts, need to fetch all and filter where category == nil. Use fetchAll(limit: 20) then filter { $0.category == nil }.
       b. `tasks`: fetchAll(category: .task, limit: 10)
       c. `recent24h`: fetchAll(limit: 50) then filter to last 24 hours where category != nil && category != .task
     - Pass results into DailyBriefData constructor.
     - Add thoughts to the dry-run printSummary output (new sections: UNPROCESSED, TODAY'S TASKS, RECENT CAPTURES).

  Note: ThoughtStore is an actor in JarvisCore (already a dependency of DailyBrief target). DatabaseManager init can throw — handle gracefully.
  </action>
  <verify>swift build succeeds with no errors. Run `swift run DailyBrief --dry-run` to see thought sections in output (will be empty if no thoughts in DB, which is fine).</verify>
  <done>DailyBriefData carries thought arrays. Generate command fetches from ThoughtStore. Dry run prints thought sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create PageThreeRenderer for captured thoughts in PDF</name>
  <files>Sources/DailyBrief/PDF/PageThreeRenderer.swift, Sources/DailyBrief/PDF/PDFGenerator.swift</files>
  <action>
  1. Create PageThreeRenderer.swift following the exact same enum-with-static-draw pattern as PageOneRenderer and PageTwoRenderer:
     - Import CoreGraphics, CoreText, Foundation, JarvisCore.
     - `enum PageThreeRenderer` with `static func draw(context: CGContext, data: DailyBriefData)`.
     - Use PDFStyles constants and PDFGenerator helper methods (drawText, drawTextRight, cgY).

  2. Page 3 layout (within the same 270x540 content area):
     a. **Header**: "Captured Thoughts" title + date string (same style as Page 1 header)
     b. **Unprocessed Items** section (category == nil):
        - Section header "Unprocessed" in headerFont
        - List each thought with bullet "•", truncated content (max ~35 chars per line, wrap to 2 lines if needed), and source indicator (text/voice/image) in tinySize mono
        - Show up to 5 items. If none: "All caught up!" in medGray
     c. **Divider line**
     d. **Today's Tasks** section (category == .task):
        - Section header "Tasks" in headerFont
        - Checkbox + content for each (same checkbox style as Page 1 To Do section)
        - Show up to 8 items. If none: "No task thoughts captured" in medGray
     e. **Divider line**
     f. **Recent Captures** section (last 24h, categorized, non-task):
        - Section header "Recent" in headerFont
        - Category label in smallSize mono (e.g., "idea", "therapy") + content truncated
        - Show up to 5 items. If none: skip section entirely

  3. In PDFGenerator.swift, add Page 3 rendering ONLY if there are any thoughts to show (any of the three arrays non-empty). This way the PDF stays 2 pages when there are no captured thoughts (backward compatible):
     ```swift
     let hasThoughts = !data.unprocessedThoughts.isEmpty || !data.taskThoughts.isEmpty || !data.recentThoughts.isEmpty
     if hasThoughts {
         context.beginPage(mediaBox: &mediaBox)
         drawDashedBorder(context: context)
         PageThreeRenderer.draw(context: context, data: data)
         context.endPage()
     }
     ```

  Keep the layout clean and minimal — this is printed B&W on a small traveler's notebook page (3.75" wide). Use bodySize (8pt) for content, smallSize (7pt) for metadata. No color — grayscale only.
  </action>
  <verify>swift build succeeds. If test thoughts exist in DB, `swift run DailyBrief` generates a 3-page PDF. If no thoughts, PDF remains 2 pages.</verify>
  <done>Page 3 renders unprocessed items, task priorities, and recent captures. PDF conditionally adds page 3 only when thoughts exist.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors or warnings
- [ ] `swift run DailyBrief --dry-run` shows thought sections (empty arrays OK)
- [ ] DailyBriefData backward compatible (existing callers unaffected by default empty arrays)
- [ ] PDF generation works with 0 thoughts (stays 2 pages)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- DailyBriefData carries captured thought data
- PDF conditionally renders thought page
- BRIEF-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-evolved-daily-brief/06-01-SUMMARY.md`
</output>
