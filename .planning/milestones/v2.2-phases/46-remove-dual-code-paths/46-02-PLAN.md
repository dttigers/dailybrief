---
phase: 46-remove-dual-code-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Services/TriageService.swift
  - Sources/JarvisCore/Services/InsightService.swift
  - Sources/JarvisCore/Services/ImageDescriptionService.swift
  - Sources/JarvisCore/Services/TherapyClassificationService.swift
  - Sources/JarvisCore/Services/TherapyPatternService.swift
  - Sources/JarvisCore/Services/TherapyPrepService.swift
  - Sources/JarvisCore/Config/AppConfig.swift
autonomous: true
---

<objective>
Remove local Claude AI service implementations and the vigil.useApi config toggle, making the API backend the only mode.

Purpose: Eliminate the 6 local AI services that call Claude directly (replaced by API-proxied equivalents) and the config flag that controls which mode to use.
Output: Only API-backed AI service implementations remain. Config simplified to always-API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Services/AIServiceProtocols.swift
@Sources/JarvisCore/Services/APIAIServices.swift
@Sources/JarvisCore/Services/TriageService.swift
@Sources/JarvisCore/Services/InsightService.swift
@Sources/JarvisCore/Services/ImageDescriptionService.swift
@Sources/JarvisCore/Services/TherapyClassificationService.swift
@Sources/JarvisCore/Services/TherapyPatternService.swift
@Sources/JarvisCore/Services/TherapyPrepService.swift
@Sources/JarvisCore/Config/AppConfig.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete local AI service implementations</name>
  <files>
    Sources/JarvisCore/Services/TriageService.swift,
    Sources/JarvisCore/Services/InsightService.swift,
    Sources/JarvisCore/Services/ImageDescriptionService.swift,
    Sources/JarvisCore/Services/TherapyClassificationService.swift,
    Sources/JarvisCore/Services/TherapyPatternService.swift,
    Sources/JarvisCore/Services/TherapyPrepService.swift
  </files>
  <action>
    Delete these 6 files — each is a local Claude API caller that duplicates functionality now handled by Vigil Core API:

    1. `TriageService.swift` — local Claude triage. Replaced by `APITriageService` in APIAIServices.swift.
    2. `InsightService.swift` — local Claude insights. Replaced by `APIInsightService`.
    3. `ImageDescriptionService.swift` — local Claude image description. Replaced by `APIImageDescriptionService`.
    4. `TherapyClassificationService.swift` — local Claude therapy classification. Replaced by `APITherapyClassificationService`.
    5. `TherapyPatternService.swift` — local Claude therapy patterns. Replaced by `APITherapyPatternService`.
    6. `TherapyPrepService.swift` — local Claude therapy prep. Replaced by `APITherapyPrepService`.

    Keep these files (API-backed, still needed):
    - `AIServiceProtocols.swift` — protocol definitions (TriageProviding, InsightProviding, etc.)
    - `APIAIServices.swift` — all 6 API implementations
    - `VigilAPIClient.swift` — HTTP client
    - `CaptureService.swift` — uses ThoughtRepository protocol (works with API)
    - `TranscriptionService.swift` — uses Apple SFSpeechRecognizer (device-local, not a dual-path issue)
    - `VoiceCaptureService.swift` — audio recording (device-local)
    - `GoogleCalendarService.swift` — Google Calendar integration (independent)
  </action>
  <verify>
    Confirm files are deleted:
    ```
    ! test -f Sources/JarvisCore/Services/TriageService.swift
    ! test -f Sources/JarvisCore/Services/InsightService.swift
    ! test -f Sources/JarvisCore/Services/ImageDescriptionService.swift
    ! test -f Sources/JarvisCore/Services/TherapyClassificationService.swift
    ! test -f Sources/JarvisCore/Services/TherapyPatternService.swift
    ! test -f Sources/JarvisCore/Services/TherapyPrepService.swift
    ```
    Confirm API implementations still exist:
    ```
    test -f Sources/JarvisCore/Services/APIAIServices.swift
    test -f Sources/JarvisCore/Services/AIServiceProtocols.swift
    ```
  </verify>
  <done>All 6 local AI service files deleted. Only API-backed implementations remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove vigil.useApi config toggle</name>
  <files>Sources/JarvisCore/Config/AppConfig.swift</files>
  <action>
    Read AppConfig.swift fully, then modify:

    1. **Remove the `VigilConfig` struct** (the nested struct with `useApi`, `apiBaseUrl`, `apiKey` fields).
    2. **Promote API connection fields to top-level `AppConfig`** or create a simpler struct:
       - Add `apiBaseUrl: String` with default `"https://vigil-core-production.up.railway.app/v1"` to AppConfig
       - Add `apiKey: String` (required, no default) to AppConfig
       - Remove the `vigil: VigilConfig?` optional field from AppConfig
    3. **Remove the `useApi` boolean entirely** — API is now the only mode.
    4. **Update CodingKeys** to map `api_base_url` and `api_key` from JSON.
    5. **Remove `cloudSync` config section** if it exists (CloudKit sync is being removed in Plan 01).
    6. **Remove `folderWatching` config section** if it exists (FolderWatcherService is being removed in Plan 01).

    The new config shape should look like:
    ```swift
    public struct AppConfig: Codable, Sendable {
        public var apiBaseUrl: String
        public var apiKey: String
        public var email: EmailConfig
        public var reminders: RemindersConfig
        public var sports: SportsConfig
        public var ai: AIConfig        // Keep — still used by CLI for local-only affirmation cache path
        public var pdf: PDFConfig
        public var printing: PrintingConfig
        public var googleCalendar: GoogleCalendarConfig
        public var insights: InsightsConfig
        // ... other existing sections
    }
    ```

    **Important:** Keep `ai: AIConfig` (with claudeApiKey, claudeModel) — the CLI target still references it for non-API operations. It will be cleaned up when the CLI is migrated in Plan 04, but removing it now would break both targets simultaneously.

    **Important:** Be careful with the Codable init — the existing manual decoder may need updating. If AppConfig uses automatic Codable synthesis, just update the properties. If it has manual `init(from:)`, update that too.
  </action>
  <verify>
    `grep -n "useApi\|useAPI\|VigilConfig" Sources/JarvisCore/Config/AppConfig.swift` returns no results.
    `grep -n "apiBaseUrl\|apiKey" Sources/JarvisCore/Config/AppConfig.swift` returns the new top-level fields.
  </verify>
  <done>
    Config toggle removed. `apiBaseUrl` and `apiKey` are top-level required fields. No more optional `vigil?` wrapper. CloudSync and FolderWatching config sections removed.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 local AI service files deleted
- [ ] `grep -rn "TriageService\b" Sources/JarvisCore/Services/` only returns APIAIServices.swift and AIServiceProtocols.swift references
- [ ] No `useApi` or `VigilConfig` references in AppConfig.swift
- [ ] `apiBaseUrl` and `apiKey` exist as direct AppConfig fields
- [ ] AIServiceProtocols.swift and APIAIServices.swift unchanged
</verification>

<success_criteria>
- Local AI service code paths completely removed
- Config is mode-free (API is the only mode)
- Protocol definitions preserved for API implementations
- No compile errors in modified files themselves
</success_criteria>

<output>
After completion, create `.planning/phases/46-remove-dual-code-paths/46-02-SUMMARY.md`
</output>
