---
phase: 35-mac-migration-ai
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/DailyBrief/Services/AIService.swift
  - Sources/DailyBrief/Services/WorkOrderPrioritizer.swift
  - Sources/DailyBrief/DailyBrief.swift
autonomous: true
---

<objective>
Migrate DailyBrief CLI AI services (affirmation + work order prioritization) to support Vigil Core API backend.

Purpose: The DailyBrief CLI executable uses ClaudeAIProvider and WorkOrderPrioritizer which call Anthropic directly. These need API-backed variants that call Vigil Core, with config-driven selection.
Output: API-backed AIProvider and WorkOrderPrioritizer implementations, DailyBrief CLI conditionally uses them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-mac-migration-first/34-04-SUMMARY.md

# DailyBrief CLI services:
@Sources/DailyBrief/Services/AIService.swift
@Sources/DailyBrief/Services/WorkOrderPrioritizer.swift
@Sources/DailyBrief/DailyBrief.swift

# Config and API client:
@Sources/JarvisCore/Config/AppConfig.swift
@Sources/JarvisCore/Services/VigilAPIClient.swift

# Vigil Core API routes:
@vigil-core/src/routes/affirmation.ts
@vigil-core/src/routes/prioritize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API-backed AIProvider for affirmation</name>
  <files>Sources/DailyBrief/Services/AIService.swift</files>
  <action>
    The `AIProvider` protocol already exists in AIService.swift: `func generateAffirmation(recentThoughts: [String]) async throws -> String`.

    Add a new `APIAIProvider` actor in the same file that conforms to `AIProvider`:
    - Takes `VigilAPIClient` in init
    - POST to `/affirmation` with body `{"thoughts": ["thought1", "thought2", ...]}`
    - Response: `{"affirmation": "Your affirmation text..."}`
    - Extract the affirmation string from response
    - On error, return the same fallback affirmation that ClaudeAIProvider uses

    Import JarvisCore at the top of file to access VigilAPIClient (check if it's already imported).

    Do NOT modify ClaudeAIProvider — it should continue working for local mode.
  </action>
  <verify>`swift build` compiles without errors. APIAIProvider conforms to AIProvider protocol.</verify>
  <done>APIAIProvider actor created, conforms to AIProvider, calls Vigil Core /affirmation endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Create API-backed WorkOrderPrioritizer + wire DailyBrief CLI</name>
  <files>
    Sources/DailyBrief/Services/WorkOrderPrioritizer.swift,
    Sources/DailyBrief/DailyBrief.swift
  </files>
  <action>
    **WorkOrderPrioritizer migration:**

    First, read WorkOrderPrioritizer.swift to understand its full interface (method signature for `prioritize`). Then:

    1. Create a `WorkOrderPrioritizing` protocol with the prioritize method signature
    2. Make existing `WorkOrderPrioritizer` conform to `WorkOrderPrioritizing`
    3. Create `APIWorkOrderPrioritizer` actor conforming to `WorkOrderPrioritizing`:
       - Takes `VigilAPIClient` in init
       - POST to `/prioritize` with body `{"workOrders": [{caseNumber, description, ...}]}` — match the exact shape the Vigil Core route expects (read prioritize.ts)
       - Response: `{"priorities": ["CASE-001", "CASE-002", ...]}` or similar — check the route
       - Map to existing return type

    **DailyBrief CLI wiring:**

    In DailyBrief.swift's `Generate.run()`:
    - After loading config, check `config.vigil?.useAPI == true`
    - If API mode: create `VigilAPIClient` with `config.vigil.apiBaseURL`, use `APIAIProvider(client:)` and `APIWorkOrderPrioritizer(client:)` 
    - If local mode: use existing `ClaudeAIProvider(config:)` and `WorkOrderPrioritizer(config:)`
    - The rest of the code uses the protocol types, so no other changes needed

    Update variable types in `run()` from concrete to protocol:
    - `let aiProvider: any AIProvider = ...`
    - `let prioritizer: any WorkOrderPrioritizing = ...`
  </action>
  <verify>`swift build` compiles without errors. DailyBrief CLI conditionally creates API-backed providers.</verify>
  <done>WorkOrderPrioritizing protocol created. API implementations created. DailyBrief CLI uses config toggle to select backend. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] AIProvider protocol unchanged (already existed)
- [ ] APIAIProvider calls Vigil Core /affirmation endpoint
- [ ] WorkOrderPrioritizing protocol defined, both implementations conform
- [ ] DailyBrief CLI selects backend based on config.vigil?.useAPI
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Local mode (default) behavior unchanged
- API mode calls Vigil Core instead of Anthropic directly
</success_criteria>

<output>
After completion, create `.planning/phases/35-mac-migration-ai/35-02-SUMMARY.md`
</output>
