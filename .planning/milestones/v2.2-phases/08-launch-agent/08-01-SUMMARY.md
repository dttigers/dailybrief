---
phase: 08-launch-agent
plan: 01
subsystem: infra
tags: [launchagent, shell-script, binary-discovery, macos]

requires:
  - phase: 07-google-calendar
    provides: working CLI and monitor app builds
provides:
  - install.sh script for release binary installation
  - stable binary discovery in StatusChecker (~/.local/bin)
affects: [08-launch-agent]

tech-stack:
  added: []
  patterns: [installed-binary-path-convention]

key-files:
  created: [Scripts/install.sh]
  modified: [Sources/DailyBriefMonitor/StatusChecker.swift]

key-decisions:
  - "Install path: ~/.local/bin/ following XDG convention"
  - "Binary discovery order: installed > release > debug"
  - "Default to installed path even if binary not yet present"

patterns-established:
  - "Installed binary path: ~/.local/bin/{target} as canonical location"
  - "LaunchAgent generated by install.sh rather than checked-in plist"

duration: 4min
completed: 2026-04-02
---

# Phase 8 Plan 01: Install Script and Stable Binary Discovery

**Idempotent install.sh builds release binaries to ~/.local/bin and StatusChecker discovers CLI at installed path first**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-02
- **Completed:** 2026-04-02
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Created install.sh that builds, installs binaries, generates LaunchAgent plist, and loads the monitor
- Updated StatusChecker to prioritize ~/.local/bin/DailyBrief over dev build paths
- Build verified clean with no errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Create install.sh script** - `714576e` (feat)
2. **Task 2: Update StatusChecker binary discovery** - `ac94682` (feat)

## Files Created/Modified
- `Scripts/install.sh` - Builds release, installs to ~/.local/bin, generates LaunchAgent, loads via launchctl
- `Sources/DailyBriefMonitor/StatusChecker.swift` - Binary discovery: installed path > release > debug

## Decisions Made
- Install to ~/.local/bin/ following common convention for user-local binaries
- Binary discovery falls through candidates array, defaults to installed path if none found
- LaunchAgent plist generated dynamically by install.sh (uses actual $HOME path)

## Deviations from Plan

None - plan executed exactly as written

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- install.sh ready to use (run `Scripts/install.sh` to build and install)
- StatusChecker works in both dev and installed modes
- Ready for plan 08-02 (monitor-managed LaunchAgent for CLI scheduler)

---
*Phase: 08-launch-agent*
*Completed: 2026-04-02*
