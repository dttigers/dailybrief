---
phase: 13-polish-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/DailyBrief/DailyBrief.swift
  - Sources/DailyBrief/Models/DailyBriefData.swift
  - Sources/DailyBrief/PDF/PageThreeRenderer.swift
autonomous: true
---

<objective>
Add AI-generated insights to the daily brief PDF so smart suggestions appear in the printed output.

Purpose: InsightService (Phase 11) should surface insights in the daily brief alongside captured thoughts. The CLI already has ThoughtStore access — this plan adds insight generation and a PDF section.
Output: Insights section in daily brief PDF (page 3, below captured thoughts).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-smart-suggestions/11-01-SUMMARY.md

@Sources/JarvisCore/Services/InsightService.swift
@Sources/JarvisCore/Models/Insight.swift
@Sources/JarvisCore/Config/AppConfig.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBrief/Models/DailyBriefData.swift
@Sources/DailyBrief/PDF/PageThreeRenderer.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch insights in CLI generate command and add to DailyBriefData</name>
  <files>Sources/DailyBrief/DailyBrief.swift, Sources/DailyBrief/Models/DailyBriefData.swift</files>
  <action>
  **DailyBriefData.swift:**
  - Add `let insights: [Insight]` property (import JarvisCore if not already imported)
  - Add default value of `[]` in the init so existing callers don't break
  - If DailyBriefData uses a memberwise init, add `insights: [Insight] = []` parameter

  **DailyBrief.swift (generate command):**
  - After the ThoughtStore fetch block (around line 77), add insight generation:
    - Check `config.insights.enabled`
    - If enabled, create `InsightService(apiKey: config.ai.claudeApiKey, model: config.ai.claudeModel)`
    - Call `insightService.generateInsights(thoughts: allRecent, lookbackDays: config.insights.lookbackDays)` — use the `allRecent` array already fetched from ThoughtStore
    - Wrap in do/catch, default to empty array on failure, log error
    - Store result in `var insights: [Insight] = []`
  - Pass `insights: insights` to DailyBriefData constructor
  - Do NOT use `async let` for insights — it depends on allRecent being available first. Run sequentially after thoughts are fetched.
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>Insights fetched during brief generation when enabled; passed to DailyBriefData</done>
</task>

<task type="auto">
  <name>Task 2: Render insights section in PDF page 3</name>
  <files>Sources/DailyBrief/PDF/PageThreeRenderer.swift</files>
  <action>
  **PageThreeRenderer.swift:**
  - Read the existing file to understand the layout structure and current y-position tracking
  - Add an "AI Insights" section AFTER the existing captured thoughts sections (unprocessed, tasks, recent)
  - Only render if `data.insights` is non-empty
  - Section header: "AI Insights" with the same styling as other section headers (use PDFStyles)
  - For each insight (max 5 to prevent overflow):
    - Render insight type as a label prefix: "Pattern:", "Connection:", "Action:", "Trend:" in bold
    - Render title on same line after the type label
    - Render message on the next line in regular weight, slightly indented
    - Add vertical spacing between insights (same spacing as between thought rows)
  - Track y-position carefully — if running out of page space, stop rendering more insights
  - Follow the exact same CoreGraphics/CoreText rendering patterns used for thoughts on page 3
  </action>
  <verify>swift build succeeds; run `swift run dailybrief --dry-run` to verify insights are fetched (will show in log output)</verify>
  <done>Insights section renders in daily brief PDF when insights are available; gracefully handles empty insights or page overflow</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for all targets
- [ ] DailyBriefData has `insights` property with default empty array
- [ ] CLI fetches insights when config.insights.enabled is true
- [ ] PageThreeRenderer renders insights section when data.insights is non-empty
- [ ] Existing brief generation still works when insights are empty or disabled
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Insights appear in daily brief PDF when enabled and thoughts exist
- Backward-compatible — existing config without insights key still works
</success_criteria>

<output>
After completion, create `.planning/phases/13-polish-integration/13-02-SUMMARY.md`
</output>
