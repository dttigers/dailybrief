---
phase: 27-therapy-prep-patterns
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
  - Sources/DailyBriefMonitor/Dashboard/TherapyPrepView.swift
  - Sources/DailyBriefMonitor/AppDelegate.swift
autonomous: true
---

<objective>
Add a therapy prep view to the dashboard that shows therapy patterns and generates exportable session prep.

Purpose: Give the user an interactive way to view therapy patterns, generate session prep on demand, and copy/export the prep for their therapist appointments.
Output: TherapyPrepView in dashboard with pattern display, prep generation, and copy-to-clipboard export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-therapy-prep-patterns/27-01-SUMMARY.md

@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
@Sources/DailyBriefMonitor/AppDelegate.swift
@Sources/JarvisCore/Models/TherapyPattern.swift
@Sources/JarvisCore/Models/TherapyPrep.swift
@Sources/JarvisCore/Services/TherapyPatternService.swift
@Sources/JarvisCore/Services/TherapyPrepService.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add therapy prep generation to DashboardViewModel</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift, Sources/DailyBriefMonitor/AppDelegate.swift</files>
  <action>
  **AppDelegate.swift:**
  - Create TherapyPatternService and TherapyPrepService actors alongside existing services (using same apiKey/model pattern from AppConfig)
  - Pass them to DashboardViewModel (or make accessible for the view model to use)

  **DashboardViewModel.swift:**
  - Add published properties:
    - `therapyPatterns: [TherapyPattern] = []`
    - `therapyPrep: TherapyPrep? = nil`
    - `isLoadingTherapyPrep: Bool = false`
  - Add `generateTherapyPrep()` async method:
    1. Set isLoadingTherapyPrep = true
    2. Fetch therapy thoughts from last 30 days via ThoughtStore
    3. Fetch bringToTherapist thoughts from last 30 days
    4. Call TherapyPatternService.detectPatterns
    5. Call TherapyPrepService.generatePrep with patterns
    6. Update published properties on MainActor
    7. Set isLoadingTherapyPrep = false
    8. Wrap in do/catch, log errors
  - Add `therapyPrepAsText() -> String` method that formats the prep as readable text for clipboard:
    ```
    Therapy Session Prep — {date}
    
    Suggested Focus: {suggestedFocus}
    
    Patterns:
    • {theme} ({trend}) — {description}
    
    Discussion Topics:
    1. [{urgency}] {topic}
       {context}
    ```
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>DashboardViewModel can generate therapy prep on demand and format it as copyable text.</done>
</task>

<task type="auto">
  <name>Task 2: Create TherapyPrepView and integrate into dashboard</name>
  <files>Sources/DailyBriefMonitor/Dashboard/TherapyPrepView.swift, Sources/DailyBriefMonitor/Dashboard/DashboardView.swift</files>
  <action>
  **TherapyPrepView.swift** (new file):
  Create a SwiftUI view that shows therapy patterns and session prep. Layout:

  - Header: "Therapy Prep" with a "Generate" button (or "Regenerate" if prep exists)
  - If isLoadingTherapyPrep: ProgressView with "Analyzing therapy thoughts..."
  - If therapyPatterns is non-empty:
    - "Patterns (last 30 days)" sub-header
    - For each pattern: HStack with theme text, trend badge (colored: increasing=orange, stable=blue, decreasing=green), frequency count
  - If therapyPrep exists:
    - "Session Prep" sub-header
    - "Suggested Focus:" in bold, followed by suggestedFocus text
    - "Topics:" section with each item showing:
      - Urgency indicator (circle: red=high, orange=medium, gray=low)
      - Topic in bold
      - Context in secondary text color
    - "Copy to Clipboard" button at bottom that copies therapyPrepAsText()
    - Show brief "Copied!" confirmation using a local @State bool
  - If no prep and not loading: Text("Tap Generate to create therapy session prep from your recent thoughts")

  Use existing SwiftUI patterns from DashboardView (color scheme, spacing, font sizes).

  **DashboardView.swift:**
  - Add a way to access TherapyPrepView. Options (pick the simplest that fits):
    - Add a "Therapy Prep" button in the toolbar (next to existing toolbar items) that opens TherapyPrepView in a sheet
    - OR add TherapyPrepView as a section that appears when the therapy sidebar filter is selected
  - Choose whichever approach integrates most naturally with the existing sidebar filter pattern. Since we already have therapy sub-filters (Self-work, Therapist, Unclassified), a toolbar button opening a sheet is cleaner — keeps the thought list and prep view separate.
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>TherapyPrepView renders patterns and prep with generate/copy functionality. Dashboard has a toolbar button to open therapy prep sheet.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] TherapyPrepView shows patterns with trend badges
- [ ] TherapyPrepView shows prep items with urgency indicators
- [ ] Generate button triggers AI analysis
- [ ] Copy to clipboard works with formatted text
- [ ] Dashboard toolbar has therapy prep button
- [ ] No regression on existing dashboard functionality
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- User can generate therapy prep on demand from dashboard
- User can copy prep to clipboard for sharing/printing
- Patterns display with meaningful trend indicators
</success_criteria>

<output>
After completion, create `.planning/phases/27-therapy-prep-patterns/27-03-SUMMARY.md`
</output>
