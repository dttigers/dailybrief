---
phase: 33-g2-screens-navigation
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: [vigil-g2-plugin/src/screens/work-orders.ts, vigil-g2-plugin/src/screens/affirmation.ts, vigil-g2-plugin/src/navigation.ts]
autonomous: true
---

<objective>
Build work orders and affirmation screens with real layout for Even G2 display.

Purpose: Replace navigation placeholder screens with real UI — work orders as a scrollable list of open tasks, affirmation as full-screen motivational display. Both use data shapes from the API client (Plan 01).
Output: work-orders.ts and affirmation.ts screen builders, navigation.ts updated to use real builders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-g2-screens-navigation/33-01-SUMMARY.md

# Source files
@vigil-g2-plugin/src/screens/home.ts
@vigil-g2-plugin/src/types.ts
@vigil-g2-plugin/src/constants.ts
@vigil-g2-plugin/src/navigation.ts
@vigil-g2-plugin/src/api.ts

# API response shapes
@vigil-core/src/routes/brief.ts
@vigil-core/src/routes/affirmation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build work orders screen</name>
  <files>vigil-g2-plugin/src/screens/work-orders.ts</files>
  <action>
Create `vigil-g2-plugin/src/screens/work-orders.ts` — displays open tasks from the brief API.

**Layout (576x288 greyscale display):**
- Header (y:0, h:40): "WORK ORDERS" + count label, divider below
- List body (y:40, h:210): Use `ListContainerProperty` with `ListItemContainerProperty` to show open tasks as a scrollable list. Each item: task content truncated to ~45 chars. Show up to 6 items (display limit). Set `isEventCapture: 1` on the list container to receive scroll events.
- Footer (y:250, h:38): "↑ home  ↓ affirmation" nav hints

**Function signature:**
```
export function buildWorkOrdersScreen(tasks: VigilBrief['openTasks']): RebuildPageContainer
```

Takes the openTasks array from VigilBrief. If empty, show a text container body instead: "No open tasks — nice work!"

Use `RebuildPageContainer` (not CreateStartUpPageContainer — this is navigated to, not startup). Import TextContainerProperty, ListContainerProperty, ListItemContainerProperty from SDK.

Use container IDs from constants.ts (WORK_ORDERS_HEADER, WORK_ORDERS_LIST, WORK_ORDERS_FOOTER).

Follow the same pattern as home.ts for header/footer text containers. The body is the key difference: a ListContainerProperty when tasks exist, a TextContainerProperty when empty.
  </action>
  <verify>cd vigil-g2-plugin && npx tsc --noEmit</verify>
  <done>buildWorkOrdersScreen accepts open tasks array, returns RebuildPageContainer with list layout for G2 display. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build affirmation screen + wire real builders into navigation</name>
  <files>vigil-g2-plugin/src/screens/affirmation.ts, vigil-g2-plugin/src/navigation.ts</files>
  <action>
**Part A — Affirmation screen:**

Create `vigil-g2-plugin/src/screens/affirmation.ts` — full-screen motivational display.

Layout (576x288 greyscale):
- Header (y:0, h:40): "VIGIL" + current time (reuse formatTime pattern from home.ts — extract to a shared util or inline), divider
- Body (y:40, h:210): Centered affirmation text. Use a single TextContainerProperty. The affirmation string from VigilAffirmation. If no affirmation available, show fallback: "You are capable, you are enough."
- Footer (y:250, h:38): "↑ work orders  ↓ home" nav hints

Function signature:
```
export function buildAffirmationScreen(affirmation: string): RebuildPageContainer
```

Use container IDs from constants.ts (AFFIRMATION_HEADER, AFFIRMATION_BODY, AFFIRMATION_FOOTER).

**Part B — Wire into navigation.ts:**

Update `navigateTo()` in navigation.ts:
- Remove placeholder screen builders
- Import `buildWorkOrdersScreen` from screens/work-orders.ts
- Import `buildAffirmationScreen` from screens/affirmation.ts
- Import `fetchBrief`, `fetchAffirmation` from api.ts
- For WORK_ORDERS: call `fetchBrief()`, pass `result.openTasks` to `buildWorkOrdersScreen()`
- For AFFIRMATION: call `fetchAffirmation()`, pass `result.affirmation` to `buildAffirmationScreen()`
- For HOME: call `fetchSummary()`, pass data to `buildHomeScreen()` — BUT home.ts still uses mock data internally. For now, just call the existing `buildHomeScreen()` without changes (Plan 03 refactors home to accept data).
- Since navigateTo now does async API calls, ensure it's already async (it should be from Plan 01).

Make navigateTo calls await the API fetch. On fetch error (api.ts returns fallback data), the screens will still render with empty/fallback content — no special error handling needed in navigation.
  </action>
  <verify>cd vigil-g2-plugin && npx tsc --noEmit && npm run build</verify>
  <done>Work orders screen shows task list, affirmation screen shows motivational text, navigation.ts uses real builders with live API data for work orders and affirmation screens. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd vigil-g2-plugin && npx tsc --noEmit` passes
- [ ] `cd vigil-g2-plugin && npm run build` succeeds
- [ ] work-orders.ts exports buildWorkOrdersScreen
- [ ] affirmation.ts exports buildAffirmationScreen
- [ ] navigation.ts imports and calls real screen builders
- [ ] Navigation fetches live data from Vigil Core API for work orders and affirmation
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- Build succeeds
- Work orders screen renders open tasks as list
- Affirmation screen renders motivational text
- Navigation uses real API data for work orders + affirmation screens
</success_criteria>

<output>
After completion, create `.planning/phases/33-g2-screens-navigation/33-02-SUMMARY.md`
</output>
