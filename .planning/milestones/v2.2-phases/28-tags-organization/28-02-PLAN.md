---
phase: 28-tags-organization
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
  - Sources/DailyBriefMonitor/Dashboard/CaptureView.swift
autonomous: true
---

<objective>
Add tags and favorites UI to the dashboard — tag pills on thoughts, favorite star toggle, sidebar tag filter, favorites filter.

Purpose: Let the user organize thoughts with custom tags and pin important ones as favorites, with full filtering support.
Output: Tag management UI, favorite toggle, sidebar filters for both features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-tags-organization/28-01-SUMMARY.md

@Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/Dashboard/CaptureView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: ThoughtRowView tag pills + favorite toggle + ViewModel methods</name>
  <files>Sources/DailyBriefMonitor/Dashboard/ThoughtRowView.swift, Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift</files>
  <action>
  **ThoughtRowView updates:**
  1. Add a favorite star button (SF Symbol "star" / "star.fill") to the left of the existing category pill area. Tapping toggles isFavorited via callback. Use yellow color for filled star. Keep it subtle — small icon, no label.

  2. Below the thought content (after category pill + confidence badge row), add a horizontal tag pills row:
     - Show each tag as a small rounded capsule (`.background(Color.secondary.opacity(0.2))`, `.clipShape(Capsule())`)
     - Each pill shows tag text + small "xmark" button to remove tag
     - At the end, show a "+" button that presents a small popover/sheet for adding a new tag
     - Tag add popover: TextField for new tag name + list of existing tags (from allUniqueTags) for quick selection
     - Only show tag row if thought has tags OR user is hovering/interacting (keep clean by default)

  3. Add new callbacks to ThoughtRowView:
     - `onToggleFavorite: () -> Void`
     - `onAddTag: (String) -> Void`
     - `onRemoveTag: (String) -> Void`

  **DashboardViewModel updates:**
  1. Add properties:
     - `var tagFilter: String?` — currently selected tag filter (nil = all)
     - `var showFavoritesOnly: Bool = false`
     - `var allTags: [String] = []` — cached unique tags list for sidebar + tag picker
     - `var favoritesCount: Int = 0`

  2. Add methods:
     - `func toggleFavorite(thoughtId: Int64)` — Call store.toggleFavorite, refresh thoughts list
     - `func addTag(thoughtId: Int64, tag: String)` — Call store.addTag, refresh thoughts + allTags
     - `func removeTag(thoughtId: Int64, tag: String)` — Call store.removeTag, refresh thoughts + allTags
     - `func refreshTags()` — Fetch allUniqueTags from store, update allTags and favoritesCount

  3. Update `loadThoughts()` to pass tagFilter and showFavoritesOnly to store.fetchFiltered.
  4. Update `loadCounts()` to refresh favoritesCount and allTags.

  Wire ThoughtRowView callbacks in DashboardView's ForEach where ThoughtRowView is instantiated.
  </action>
  <verify>swift build succeeds. ThoughtRowView renders tag pills and star icon without layout issues.</verify>
  <done>Tag pills display on thoughts with add/remove. Favorite star toggles. ViewModel manages tag/favorite state.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar tags section + favorites filter + filter wiring</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardView.swift, Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift</files>
  <action>
  **DashboardView sidebar updates:**
  1. Add a "Favorites" row in the sidebar, above or below the category filter section:
     - SF Symbol "star.fill" with yellow color
     - Badge showing favoritesCount
     - Tapping toggles viewModel.showFavoritesOnly
     - Visual feedback: opacity 1.0 when active, 0.6 when inactive (matching existing filter pattern)

  2. Add a "Tags" section in the sidebar (below source/date filters):
     - Section header "Tags" with SF Symbol "tag"
     - List each tag from viewModel.allTags as a tappable row
     - Tapping a tag sets viewModel.tagFilter to that tag (or nil if already selected, to deselect)
     - Selected tag has opacity 1.0, others 0.6
     - If no tags exist yet, show a subtle "No tags yet" placeholder text
     - Each tag row shows the tag name — no count badge needed (keep it simple)

  3. Filter reset behavior (follow existing patterns from DashboardView lines 68-90):
     - Changing category filter resets tagFilter and showFavoritesOnly
     - Changing tagFilter clears selection (selectedThoughtIds)
     - Toggling showFavoritesOnly clears selection
     - showFavoritesOnly and tagFilter work as additive filters alongside existing category/source/date filters

  4. Update the detail/content area header to show active tag filter as a dismissible chip:
     - If tagFilter is set, show "Tag: {tagname} x" chip above the thoughts list
     - Tapping "x" clears the tagFilter
     - If showFavoritesOnly, show "Favorites x" chip similarly

  **Bulk actions integration:**
  - Add "Add Tag" to bulk action toolbar (when in selection mode). Opens tag picker, applies tag to all selected thoughts via store.bulkAddTag.
  </action>
  <verify>swift build succeeds. Sidebar shows tags section and favorites filter. Filters compose correctly with existing category/source/date filters.</verify>
  <done>Sidebar has tags section with dynamic tag list + favorites toggle. Filters compose additively. Bulk tag add works. Active filters shown as dismissible chips.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for both targets
- [ ] Tag pills appear on thoughts that have tags
- [ ] Favorite star toggles correctly
- [ ] Sidebar shows tags section with unique tags
- [ ] Sidebar favorites filter works
- [ ] Tag filter + favorites filter compose with existing filters
- [ ] Bulk "Add Tag" works in selection mode
- [ ] No layout regressions in ThoughtRowView
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Tags and favorites fully functional in UI
- Existing dashboard features unaffected
- Build succeeds without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/28-tags-organization/28-02-SUMMARY.md`
</output>
