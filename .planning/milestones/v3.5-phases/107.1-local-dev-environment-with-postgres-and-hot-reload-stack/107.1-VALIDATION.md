---
phase: 107.1
slug: local-dev-environment-with-postgres-and-hot-reload-stack
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-21
---

# Phase 107.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | shell assertions + `scripts/smoke-test.ts` pattern (no unit-test framework for shell-script phase) |
| **Config file** | none — Wave 0 writes inline shell assertions |
| **Quick run command** | `bash scripts/dev-setup.sh --check` (idempotent re-run, exits 0 on healthy state) |
| **Full suite command** | `bash scripts/dailybrief-doctor.sh && bash scripts/dev-setup.sh` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `{quick run command}` (populated by planner per-task)
- **After every plan wave:** Run `bash scripts/dailybrief-doctor.sh` (picks up new INFORMATIONAL row)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| P01-T1 | 01 | 1 | REQ-DEV-LOCAL-ENV | T-LOCAL-5 | postgresql@16 (16.13) installed + vigil_dev exists + bound to localhost (T-LOCAL-4) | manual+automated | `brew list postgresql@16 && psql -h localhost -d vigil_dev -c 'SELECT 1' -tA` | /usr/local/var/postgresql@16/PG_VERSION | ⬜ pending |
| P01-T2 | 01 | 1 | REQ-DEV-LOCAL-ENV | T-LOCAL-1, T-LOCAL-5 | .env.example shows LOCAL-ONLY shape (6 keys, no Railway hostnames) | automated | task-embedded `grep` chain | vigil-core/.env.example | ⬜ pending |
| P02-T1 | 02 | 1 | REQ-DEV-LOCAL-ENV | T-LOCAL-4 | seed-local.ts idempotent via onConflictDoNothing + count-guards; seed email matches allowlist | automated | `test -f vigil-core/scripts/seed-local.ts && tsc --noEmit` + embedded grep chain | vigil-core/scripts/seed-local.ts | ⬜ pending |
| P02-T2 | 02 | 1 | REQ-DEV-LOCAL-ENV | — | seed:local npm script uses `tsx --env-file=.env` (research finding #1) | automated | `grep -F '"seed:local": "tsx --env-file=.env"' vigil-core/package.json` | vigil-core/package.json | ⬜ pending |
| P03-T1 | 03 | 2 | REQ-DEV-LOCAL-ENV | T-LOCAL-3 | preflight cannot be bypassed; 4 checks each print fix command | automated | `bash -n scripts/preflight-check.sh && ! grep -qE 'SKIP_PREFLIGHT|--skip|FORCE_DEV' scripts/preflight-check.sh` | scripts/preflight-check.sh | ⬜ pending |
| P03-T2 | 03 | 2 | REQ-DEV-LOCAL-ENV | T-LOCAL-2 | dev-setup.sh backs up .env before write; uses VITE_API_BASE (not VITE_API_URL) | automated | embedded grep chain | scripts/dev-setup.sh, scripts/dev-reset.sh | ⬜ pending |
| P04-T1 | 04 | 1 | REQ-DEV-LOCAL-ENV | T-LOCAL-6 | plist XML preserved BEFORE deletion (reversibility); daemon un-registered; port 3001 free | manual+automated | `launchctl print gui/$UID/com.jamesonmorrill.vigilcore` non-zero + `grep '<plist' 107.1-daemon-retirement.md` | .planning/phases/107.1-.../107.1-daemon-retirement.md | ⬜ pending |
| P05-T1 | 05 | 2 | REQ-DEV-LOCAL-ENV | — | Root package.json thin orchestrator (no workspaces); concurrently --kill-others; tsx --env-file=.env fix | automated | embedded grep+JSON-parse chain | package.json, vigil-core/package.json | ⬜ pending |
| P05-T2 | 05 | 2 | REQ-DEV-LOCAL-ENV | — | `npm run dev` starts both servers; Ctrl+C kills both; no DATABASE_URL warning | manual | human-verify checkpoint | vigil-pwa/.env.local | ⬜ pending |
| P06-T1 | 06 | 3 | REQ-DEV-LOCAL-ENV | — | Anthropic dev workspace with $20/mo cap exists | manual | human-action checkpoint; Anthropic Console only | — (external service state) | ⬜ pending |
| P06-T2 | 06 | 3 | REQ-DEV-LOCAL-ENV | T-LOCAL-1, T-LOCAL-5 | vigil-core/.env LOCAL-ONLY, no Railway hostnames, backup exists and is gitignored | automated | embedded grep+ls+git ls-files chain | vigil-core/.env, .gitignore | ⬜ pending |
| P06-T3 | 06 | 3 | REQ-DEV-LOCAL-ENV | — | sync-anthropic-key.sh: --include-config-env opt-in; plist block replaced with retirement notice | automated | embedded grep+smoke-test chain | scripts/sync-anthropic-key.sh | ⬜ pending |
| P07-T1 | 07 | 3 | REQ-DEV-LOCAL-ENV | — | doctor D-19 INFORMATIONAL row for local vigil_dev DB; no DRIFT_COUNT increment | automated | `awk '/Phase 107.1 D-19/,/^fi$/' scripts/dailybrief-doctor.sh | grep -c 'DRIFT_COUNT='` returns 0 | scripts/dailybrief-doctor.sh | ⬜ pending |
| P07-T2 | 07 | 3 | REQ-DEV-LOCAL-ENV | — | RUNBOOK.md + README.md document new local-dev workflow | automated | embedded grep chain | vigil-core/RUNBOOK.md, README.md | ⬜ pending |
| P07-T3 | 07 | 3 | REQ-DEV-LOCAL-ENV | — | REQ-DEV-LOCAL-ENV added to Traceability table; coverage updated 13→14 | automated | embedded grep chain | .planning/REQUIREMENTS.md | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Validation Dimensions (from RESEARCH.md)

| # | Dimension | Verification Command / Check |
|---|-----------|------------------------------|
| 1 | Idempotency — `dev-setup.sh` re-run | `bash scripts/dev-setup.sh && bash scripts/dev-setup.sh` both exit 0 |
| 2 | Daemon retirement — iMac + MacBook | `launchctl print gui/$UID/com.jamesonmorrill.vigilcore` returns non-zero; plist absent from `~/Library/LaunchAgents/` |
| 3 | Pre-flight check — fails loud on each broken precondition | Simulate: daemon loaded → exit 1 with fix; `:3001` taken → exit 1 with fix; `DATABASE_URL` points at prod → exit 1 with fix; Postgres stopped → exit 1 with fix |
| 4 | Unified dev — `npm run dev` at repo root | Both servers reachable (`curl :3001/v1/ping`, `curl :5173`); Ctrl+C kills both (verified by `pgrep` delta) |
| 5 | Local DB reachability — seed round-trip | `psql -d vigil_dev -c 'SELECT count(*) FROM users'` returns ≥1; seed API key authenticates against local core |
| 6 | Drift-doctor integration — new INFORMATIONAL row | `bash scripts/dailybrief-doctor.sh` output contains local-DB row; exit code unchanged when local DB down (informational only) |
| 7 | Env hygiene — LOCAL-only `.env` | `grep 'DATABASE_URL' vigil-core/.env | grep -q localhost`; `grep -v` any Railway internal hostname |
| 8 | HUMAN-UAT — day-of-dev use | User runs dev-setup on one machine, uses `npm run dev` for a full session without accidental prod mutation |

---

## Wave 0 Requirements

- [ ] `scripts/dev-setup.sh` — idempotent first-time setup (D-07)
- [ ] `scripts/seed-local.ts` — fixture seed (D-06)
- [ ] Root `package.json` with `concurrently` devDep (D-13)
- [ ] Dimension check scripts (inline within `dev-setup.sh --check` or separate `scripts/verify-phase-107.1.sh`)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Daemon bootout on iMac | D-09 | Must run in GUI Terminal session (RESEARCH finding — `LimitLoadToSessionType=Aqua`). SSH invocation won't work. | User runs `launchctl bootout gui/$UID/com.jamesonmorrill.vigilcore` in Terminal.app on iMac |
| Anthropic dev workspace + budget cap | D-18 | Anthropic console has no CLI; workspace creation + `$20/mo` limit set via web UI | User creates "dev" workspace, generates key, sets monthly limit via Console → Settings → Workspaces → [dev] → Limits |
| Daemon plist deletion (reversible) | D-09 | Destructive filesystem action; keep a copy in CONTEXT.md/RUNBOOK before deletion | User `mv ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist /tmp/` then confirms no regression over 24h |
| Day-of-dev use without prod mutation | D-10, D-17 | Long-horizon behavioral check; cannot be automated | User develops normally for one session, confirms prod Railway DB unchanged via `railway run psql -c 'SELECT max(updated_at) FROM users'` pre/post |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** per-task rows populated by planner 2026-04-21 — pending execution/sampling
