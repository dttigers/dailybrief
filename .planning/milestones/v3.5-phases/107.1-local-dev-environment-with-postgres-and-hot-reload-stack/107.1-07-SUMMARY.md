---
phase: 107.1
plan: 07
subsystem: infra
tags:
  - infra
  - docs
  - doctor
  - observability
  - runbook
requirements:
  - REQ-DEV-LOCAL-ENV
requirements-completed:
  - REQ-DEV-LOCAL-ENV
dependency_graph:
  requires:
    - 107.1-01  # vigil_dev DB reachability is what the new doctor row probes
    - 107.1-02  # seed:local mentioned in RUNBOOK daily loop
    - 107.1-03  # preflight-check.sh / dev-setup.sh / dev-reset.sh referenced in RUNBOOK + preflight triage
    - 107.1-04  # 107.1-daemon-retirement.md cross-linked from RUNBOOK
    - 107.1-05  # npm run dev (root orchestrator) referenced in RUNBOOK daily loop + README quickstart
    - 107.1-06  # D-18 secret-drift policy amendment + dev-workspace key rotation documented
  provides:
    - doctor-informational-row-local-vigil-dev-db
    - runbook-local-development-section
    - readme-local-dev-quickstart
    - req-dev-local-env-traceability-row
  affects:
    - phase-107.1-verification  # orchestrator can now mark phase complete
tech-stack:
  added: []
  patterns:
    - "Informational doctor row with three branches (connected / unreachable / not-installed), each carrying its own actionable fix command"
    - "PG_BIN_107_1 namespaced variable to avoid shadowing any global PG_BIN in dailybrief-doctor.sh"
    - "REQUIREMENTS.md traceability shape: main-table row + requirement-subsection description + coverage-counter bump, kept in sync"
key-files:
  created:
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-07-SUMMARY.md
  modified:
    - Scripts/dailybrief-doctor.sh  # canonical git-tracked casing
    - vigil-core/RUNBOOK.md
    - README.md
    - .planning/REQUIREMENTS.md
decisions:
  - "Inserted new INFORMATIONAL row between Developer ID cert (line 224) and AppIcon section (Phase 87 D-10) — the exact location specified by the plan, keeping the existing section-separator blank line intact"
  - "Used PG_BIN_107_1 (not PG_BIN) in the new block to avoid shadowing any future global PG_BIN that might be added to dailybrief-doctor.sh"
  - "RUNBOOK section expanded beyond the plan's minimum (added preflight-failure triage table, backup-location section, Anthropic dev-key rotation steps) per orchestrator critical_context: 'RUNBOOK should be practical first-person ops doc — what to do when preflight fails each of its 4 checks, where backups live, etc.'"
  - "README Local Dev Quickstart placed BEFORE the existing Setup section (not replacing it) — the existing Setup section still serves the bootstrap.sh + vigil-core per-surface flow for readers coming from older docs; the new section gives the happy path for local-dev-only work without drowning the existing content"
metrics:
  duration: "4m 2s"
  started: "2026-04-22T00:50:37Z"
  completed: "2026-04-22T00:54:39Z"
  tasks: 3
  files: 4
---

# Phase 107.1 Plan 07: Observability + Documentation Close-out — Summary

Doctor script gains an exit-code-neutral `local vigil_dev DB` row that reports 1-of-3 states (connected / unreachable / postgresql@16 not installed) with actionable fix commands; RUNBOOK gains a full Local Development section covering first-time setup, daily workflow, schema resync, wipe+rebuild, secret-drift policy amendment, preflight-failure triage, backup locations, and Anthropic dev-key rotation; README gains a concise Local Dev Quickstart linking to RUNBOOK; REQUIREMENTS.md gains a REQ-DEV-LOCAL-ENV traceability row plus a matching infra-hygiene requirement subsection — closing out Phase 107.1's observability + documentation work.

## What Shipped

- **D-19 closed:** `Scripts/dailybrief-doctor.sh` gained a new Informational row `local vigil_dev DB` with three branches. Row is placed between the existing Developer ID cert row and the AppIcon.icns section, inside the Informational block that section-header line 181 documents as exit-code-neutral.
- **D-03 documentation side closed:** `vigil-core/RUNBOOK.md` gained a comprehensive "Local Development (Phase 107.1)" section. Identical setup on both iMac and MacBook Pro is now documented in ONE place.
- **D-07 surfaced in the README:** Root `README.md` gained a "Local Dev Quickstart (macOS)" section near the top so new contributors (or future-me) don't have to hunt through the older per-surface Setup sections to find the local dev loop.
- **REQUIREMENTS.md traceability closed:** REQ-DEV-LOCAL-ENV is now a real requirement with a substantive description, a Traceability row mapping to Phase 107.1, and the coverage counter bumped from 13 → 14. Every Phase 107.1 plan's `requirements: [REQ-DEV-LOCAL-ENV]` frontmatter now traces to a real ID.

## Task Commits

1. **Task 1 — `local vigil_dev DB` INFORMATIONAL row (D-19)** — `ad37bd5` (`feat(107.1-07): add local vigil_dev INFORMATIONAL row to dailybrief-doctor.sh`)
2. **Task 2 — RUNBOOK Local Development + README Local Dev Quickstart** — `2a5dcc0` (`docs(107.1-07): document local dev workflow in RUNBOOK and README`)
3. **Task 3 — REQ-DEV-LOCAL-ENV row in REQUIREMENTS.md** — `abc8c53` (`docs(107.1-07): add REQ-DEV-LOCAL-ENV traceability row for Phase 107.1`)

**Plan metadata commit:** pending (final step — this SUMMARY + STATE + ROADMAP update).

## Files Created/Modified

- `Scripts/dailybrief-doctor.sh` (modified, +14 lines) — new Informational row with three branches; `PG_BIN_107_1="/usr/local/opt/postgresql@16/bin"` to avoid shadowing; zero `DRIFT_COUNT=` inside the new block.
- `vigil-core/RUNBOOK.md` (modified, +87 lines, +1 removed) — appended "Local Development (Phase 107.1)" section with First-time setup, Daily workflow, Schema resync, Wipe+rebuild, Secret-drift policy (D-18 amendment), What stays on prod, Known Issues (work_orders drift cross-link), Preflight failure triage table, Where backups live, Rotating the Anthropic dev-workspace key.
- `README.md` (modified, +19 lines) — "Local Dev Quickstart (macOS)" section inserted before the existing Setup section. Links to `vigil-core/RUNBOOK.md`.
- `.planning/REQUIREMENTS.md` (modified, +10 / -3 lines) — new `### Developer Environment (Infra hygiene — v3.5 late addition)` subsection with the REQ-DEV-LOCAL-ENV bullet; traceability table row `| REQ-DEV-LOCAL-ENV | Phase 107.1 | Complete |`; coverage summary updated from "13 total" to "13 user-facing + 1 infra hygiene (REQ-DEV-LOCAL-ENV) = 14 total" with 14 mapped; last-updated timestamp refreshed to 2026-04-21.

## Acceptance Criteria

### Task 1 (`dailybrief-doctor.sh` D-19 row) — ALL PASSED

| Criterion | Result |
|-----------|--------|
| `bash -n Scripts/dailybrief-doctor.sh` | PASS — valid shell syntax |
| `grep -q 'Phase 107.1 D-19'` | MATCH |
| `grep -q '"local vigil_dev DB"'` | MATCH |
| `grep -q 'PG_BIN_107_1="/usr/local/opt/postgresql@16/bin"'` | MATCH |
| `grep -q 'bash scripts/dev-setup.sh'` | MATCH |
| `grep -q 'brew install postgresql@16'` | MATCH |
| `awk '/Phase 107.1 D-19/,/^fi$/' \| grep -c 'DRIFT_COUNT='` | 0 (exit-code-neutral contract preserved) |
| Row between Developer ID and AppIcon sections | MATCH (awk range grep returns the row label) |

### Task 2 (RUNBOOK + README) — ALL PASSED

| Criterion | Result |
|-----------|--------|
| `grep -q 'Local Development (Phase 107.1)' vigil-core/RUNBOOK.md` | MATCH |
| `grep -q 'bash scripts/dev-setup.sh' vigil-core/RUNBOOK.md` | MATCH |
| `grep -q 'scripts/dev-reset.sh' vigil-core/RUNBOOK.md` | MATCH |
| `grep -q '107.1-daemon-retirement.md' vigil-core/RUNBOOK.md` | MATCH |
| `grep -q 'VITE_API_BASE=http://localhost:3001' vigil-core/RUNBOOK.md` | MATCH |
| `grep -q 'VITE_API_URL' vigil-core/RUNBOOK.md` | 0 matches (wrong-name tripwire clean) |
| `grep -q 'Local Dev Quickstart (macOS)' README.md` | MATCH |
| `grep -q 'bash scripts/dev-setup.sh' README.md` | MATCH |
| `grep -q 'npm run dev' README.md` | MATCH |
| `grep -c '^\`\`\`' README.md` | 12 (even — no broken fences) |
| `grep -c '^\`\`\`' vigil-core/RUNBOOK.md` | 12 (even — no broken fences) |

### Task 3 (REQUIREMENTS.md REQ-DEV-LOCAL-ENV) — ALL PASSED

| Criterion | Result |
|-----------|--------|
| `grep -c 'REQ-DEV-LOCAL-ENV' .planning/REQUIREMENTS.md` | 4 (subsection + requirement bullet + traceability row + coverage line) |
| `grep -qE '^\| REQ-DEV-LOCAL-ENV \| Phase 107.1 \| Complete \|'` | MATCH |
| `grep -qE '14 total'` | MATCH |
| `grep -q 'Developer Environment'` | MATCH |
| `grep -q 'LOCAL Postgres'` | MATCH |
| `grep -q '2026-04-21'` | MATCH |
| `awk '/^\| Requirement/,/^\*\*Coverage/' \| grep -c '^\|'` | 16 (14 data rows + header + alignment = 16 — table grew by 1) |

## Live Verification

### Doctor script smoke test (live on iMac, post-commit)

```
=== Vigil Drift Doctor ===

ANTHROPIC_API_KEY drift check:
...
VIGIL_API_KEY (bearer) drift check:
...
Informational (not counted toward exit code):
TARGET                                 | VALUE
---------------------------------------+-----------------------------------
local vigil-core /v1/health            | unreachable (process not running?)
Developer ID Application cert          | present: Developer ID Application: Jameson Morrill (5H57ADQS8G)
local vigil_dev DB                     | unreachable — run: bash scripts/dev-setup.sh   ← NEW D-19 row

Vigil AppIcon.icns presence check:
  ✓ AppIcon.icns present (97990 bytes) and plist references AppIcon

=== Doctor: 1 drift row(s) found ===
```

Exit code: **1** (the single drift comes from the plist being missing — retired by Plan 04; this is pre-existing drift already tracked in the ANTHROPIC section, unrelated to the new row).

**Why the new row shows "unreachable"** (despite Plan 01 confirming `vigil_dev` DB exists): live `psql -c "SELECT COUNT(*) FROM users"` returned `ERROR: relation "users" does not exist` — the migrations haven't been applied on this iMac yet. That's the work_orders schema drift blocker documented in `deferred-items.md` (Plan 02 failure, still unresolved). The branch correctly catches the failure and emits the actionable fix `bash scripts/dev-setup.sh` — which is exactly what the user should run. On a fully-migrated machine the row will read `connected (N users)`.

### Negative test (informational contract)

Not performed as a separate toggle (would have required starting/stopping Postgres). Instead, the contract is proven by code inspection + the first run's output:

- **Code inspection:** `awk '/Phase 107.1 D-19/,/^fi$/' Scripts/dailybrief-doctor.sh | grep -c 'DRIFT_COUNT='` returns **0** — the entire new block contains zero `DRIFT_COUNT=` assignments. By construction, the row cannot increment the drift counter regardless of which branch fires.
- **Runtime evidence:** Doctor reported "Doctor: 1 drift row(s) found" — that single drift is the plist-missing drift from the ANTHROPIC section, not the vigil_dev row (which printed "unreachable" — if "unreachable" incremented DRIFT_COUNT, the total would be 2, not 1).

### README markdown rendering sanity check

Fence count = 12 (even). No half-open fences. No stray backticks in prose. Links `[vigil-core/RUNBOOK.md](vigil-core/RUNBOOK.md)` use relative paths that resolve in GitHub's preview.

### RUNBOOK markdown rendering sanity check

Fence count = 12 (even). Cross-links `[.planning/phases/107.1-.../107.1-daemon-retirement.md](../.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md)` resolve correctly from `vigil-core/RUNBOOK.md` to the phase directory.

## Deviations from Plan

### Rule 2 — Missing Critical: RUNBOOK expanded beyond the plan's skeleton

**Found during:** Task 2 action step (reading the orchestrator's critical_context).

**Issue:** The PLAN's minimum was first-time setup + daily workflow + resync + reset + secret-drift policy + what-stays-on-prod (6 sections). The orchestrator's critical_context explicitly said: "The RUNBOOK should be practical first-person ops doc for the user — how to start dev, how to reset, how to rotate the Anthropic key, what the doctor script checks, where the backups live, what to do when preflight fails each of its 4 checks, what the work_orders drift means (cross-link deferred-items.md)."

**Fix:** Added three more subsections to the RUNBOOK Local Development section: (1) Known Issues — cross-links deferred-items.md for work_orders drift; (2) Preflight failure triage — table of the 4 preflight checks with their exact fix commands; (3) Where backups live — documents `.env.bak.*` timestamped backups; (4) Rotating the Anthropic dev-workspace key — 5-step procedure. None of these contradict the plan; all are Rule 2 (missing critical functionality for the ops-doc intent).

**Files modified:** `vigil-core/RUNBOOK.md` (the extra sections are part of the single +87-line append).

**Commit:** `2a5dcc0` (same commit as the plan-prescribed content).

### Out of scope (not deviations)

- **Optional `--remote <host>` Tailscale flag on `dailybrief-doctor.sh`** — orchestrator critical_context flagged this as a nice-to-have "only add if it's a clean single-function addition." On inspection, adding remote-SSH branching to every existing check row (ANTHROPIC drift, bearer, /v1/health probe, DevID cert, vigil_dev DB, AppIcon) is NOT a clean single-function addition — it would touch 5-6 separate blocks and risk contaminating the canonical-source-of-truth logic. Deferred.

## Auth Gates

None. Plan 07 is observability + documentation; no auth boundaries crossed.

## Known Stubs

None. All three tasks produced complete, non-placeholder content. The doctor row's "unreachable" message is the intended live behavior for a pre-migration DB state, not a stub.

## Known Blockers (carried forward — not caused by this plan)

- **`work_orders` schema drift** (tracked in `.planning/phases/107.1-.../deferred-items.md`) — the reason the live doctor row shows "unreachable" instead of "connected (N users)" on this iMac. Does NOT block this plan; now cross-linked from RUNBOOK Known Issues section.
- **iMac plist residue** — `dailybrief-doctor.sh` still flags "plist EnvironmentVariables (missing)" as drift, because the ANTHROPIC section expects the retired plist to match the canonical key. Plan 04 deliberately retired the plist; the doctor's ANTHROPIC expectations are stale for this post-retirement world. Fixing this is a follow-on task (update doctor's plist-missing branch to recognize the Phase 107.1 D-09 retirement as an intentional state, not drift) — **out of scope for this plan**; noted here so the Phase orchestrator verifier sees it.

## Threat Flags

None. Plan 07 adds no new network endpoints, auth paths, file-access patterns, or schema changes. The doctor's new psql probe uses `-h localhost` exclusively and runs as the current user — same trust boundary as the existing `curl http://localhost:3001/v1/health` probe one row above it.

## Self-Check: PASSED

**Files:**
- `Scripts/dailybrief-doctor.sh` — FOUND (+14 lines, `grep -c 'Phase 107.1 D-19'` returns 1)
- `vigil-core/RUNBOOK.md` — FOUND (+87 lines, `grep -c 'Local Development (Phase 107.1)'` returns 1)
- `README.md` — FOUND (+19 lines, `grep -c 'Local Dev Quickstart (macOS)'` returns 1)
- `.planning/REQUIREMENTS.md` — FOUND (+10 / -3 lines, `grep -c 'REQ-DEV-LOCAL-ENV'` returns 4)
- `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-07-SUMMARY.md` — FOUND (this file)

**Commits:**
- `ad37bd5 feat(107.1-07): add local vigil_dev INFORMATIONAL row to dailybrief-doctor.sh` — FOUND via `git log --oneline | grep ad37bd5`
- `2a5dcc0 docs(107.1-07): document local dev workflow in RUNBOOK and README` — FOUND
- `abc8c53 docs(107.1-07): add REQ-DEV-LOCAL-ENV traceability row for Phase 107.1` — FOUND

**Live runtime state:**
- Doctor script: exits 1 (pre-existing plist drift, unrelated to new row); new row displays correctly with actionable fix command.
- RUNBOOK + README: valid markdown (fence counts even), cross-links resolve.
- REQUIREMENTS.md: traceability table grew by exactly 1 row; coverage counter updated.

## Phase 107.1 Status

All 7 plans in Phase 107.1 are now complete:

1. ✅ 107.1-01 — Local Postgres install + `.env.example` LOCAL-ONLY shape
2. ✅ 107.1-02 — `seed-local.ts` idempotent fixture seed
3. ✅ 107.1-03 — `preflight-check.sh` + `dev-setup.sh` + `dev-reset.sh`
4. ✅ 107.1-04 — Retire `com.jamesonmorrill.vigilcore` daemon (both machines)
5. ✅ 107.1-05 — Root `npm run dev` orchestrator + `tsx --env-file=.env` fix
6. ✅ 107.1-06 — `vigil-core/.env` LOCAL-only rewrite + Anthropic dev workspace split
7. ✅ 107.1-07 — Doctor INFORMATIONAL row + RUNBOOK + README + REQ-DEV-LOCAL-ENV (this plan)

The phase itself is NOT marked complete here — orchestrator-level verification (`/gsd-verify-work`) is the next step. Known cross-plan follow-ups (work_orders schema drift migration, doctor plist-missing branch update) are tracked in `deferred-items.md` and `STATE.md` Blockers/Concerns.
