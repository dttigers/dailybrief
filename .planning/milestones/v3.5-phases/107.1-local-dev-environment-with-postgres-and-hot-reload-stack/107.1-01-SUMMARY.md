---
phase: 107.1
plan: 01
subsystem: infra
tags:
  - infra
  - postgres
  - homebrew
  - env-hygiene
requirements:
  - REQ-DEV-LOCAL-ENV
dependency_graph:
  requires: []
  provides:
    - local-postgres-16-cluster
    - vigil-core-env-template-local-only
  affects:
    - 107.1-02  # seed-local.ts needs vigil_dev DB + VIGIL_ALLOWED_EMAILS template
    - 107.1-03  # dev-setup.sh copies .env.example to .env, runs migrations against vigil_dev
    - 107.1-06  # .env rewrite (prod → local) uses this template as its source
tech-stack:
  added:
    - "postgresql@16 (Homebrew, 16.13)"
  patterns:
    - "localhost-bound Postgres matches Railway-pinned 16.x (D-02 dialect parity)"
    - "env template separates dev workspace keys from Railway canonical (D-17, D-18)"
key-files:
  created: []
  modified:
    - vigil-core/.env.example
decisions:
  - "Task 1 executed manually on both iMac + MacBook Pro per D-03 (user confirmed 'postgres ready on both machines'); iMac verified live in this session, MacBook Pro status accepted on user confirmation (Aqua-session constraint prevents remote verification per RESEARCH Pitfall 2)"
  - "Rule 1 auto-fix applied: plan <action> text contained the literal strings 'Railway proxy' and 'rlwy.net' in instructional comments, which the plan's own <automated> verification chain (forbids 'proxy', 'rlwy.net', 'railway.app', 'hopper') rejected. Rephrased comments to 'NOT a remote production host' / 'DO NOT put a remote hostname here' — preserves T-LOCAL-1 educational intent without tripping the acceptance regex."
  - "Traceability comment 'Phase 107.1 Plan 03' retained exactly once on line 3 (satisfies acceptance criterion)"
metrics:
  duration: "1m 34s"
  completed_date: "2026-04-21"
  tasks: 2
  files: 1
---

# Phase 107.1 Plan 01: Local Postgres Install + .env.example Local-Only Shape — Summary

Homebrew `postgresql@16` (16.13) installed and started on iMac + MacBook Pro with `vigil_dev` created; `vigil-core/.env.example` rewritten from 2 keys to the 6-key LOCAL-ONLY template (DATABASE_URL pointed at localhost, JWT_SECRET placeholder meeting the 32-char gate, VIGIL_ALLOWED_EMAILS pinned to the seed user, POSTHOG_API_KEY left blank per Phase 103 key-absence gate, PORT=3001 matching the retired daemon's Google OAuth redirect).

## Task Breakdown

### Task 1: Install postgresql@16 + create vigil_dev (manual — iMac + MacBook Pro)

**Type:** `checkpoint:human-action`
**Status:** Complete — verified live on iMac; MacBook Pro accepted on user confirmation.

**User resume signal:** "postgres ready on both machines"

**iMac verification (live in this session, 2026-04-21):**

| Criterion | Command | Result |
|-----------|---------|--------|
| 1. Formula installed | `brew list postgresql@16` | PASS (exit 0) |
| 2. Brew service started | `brew services list \| grep postgresql@16` | PASS — `started  jamesonmorrill  ~/Library/LaunchAgents/homebrew.mxcl.postgresql@16.plist` |
| 3. Database exists | `psql -h localhost -lqt \| cut -d'\|' -f1 \| grep -qw vigil_dev` | PASS (exit 0) |
| 4. pg_isready | `/usr/local/opt/postgresql@16/bin/pg_isready -h localhost -q` | PASS (exit 0) |
| 5. Version string | `psql -h localhost -d vigil_dev -c "SELECT version();"` | `PostgreSQL 16.13 (Homebrew) on x86_64-apple-darwin23.6.0, compiled by Apple clang version 16.0.0 (clang-1600.0.26.6), 64-bit` |
| 6. Data directory | `cat /usr/local/var/postgresql@16/PG_VERSION` | `16` |

**Port binding (T-LOCAL-4 mitigation confirmed):**
```
lsof -iTCP:5432 -sTCP:LISTEN →
postgres 13259 jamesonmorrill  7u IPv6 … TCP localhost:postgresql (LISTEN)
postgres 13259 jamesonmorrill  8u IPv4 … TCP localhost:postgresql (LISTEN)
```
Postgres bound to `localhost` only (IPv4 + IPv6 loopback) — not reachable from LAN/WAN.

**MacBook Pro (second dev machine, per D-03):**
User confirmed "postgres ready on both machines" (2026-04-21). Live verification from this agent session is **not** possible — per RESEARCH Pitfall 2, `launchctl`/`brew services` operations require an Aqua (GUI) session; SSH into the MacBook Pro runs in a different launchd domain and cannot exercise the keg-only `brew services list` output reliably. User confirmation is accepted as authoritative. Plan 04 (verify script, wave 1) or a future in-person session will re-run the 6-criterion check on the MacBook Pro and flag any drift.

**No path deviations observed.** iMac binaries are at `/usr/local/opt/postgresql@16/bin` (Intel keg-only path, as predicted by RESEARCH Pattern 4 Pitfall 3).

### Task 2: Rewrite vigil-core/.env.example to LOCAL-ONLY shape

**Type:** `auto`
**Status:** Complete.
**Commit:** `20921c4` — `feat(107.1-01): rewrite .env.example to LOCAL-ONLY shape`

**Changes:** expanded from 2 lines (DATABASE_URL, ANTHROPIC_API_KEY) to the 6-key template required by D-17:

| Key | Value |
|-----|-------|
| DATABASE_URL | `postgresql://localhost:5432/vigil_dev` |
| JWT_SECRET | `replace-me-with-32-plus-random-bytes-for-local-dev-only` (placeholder, 53 chars — passes the `>=32` gate in `src/utils/jwt.ts`) |
| VIGIL_ALLOWED_EMAILS | `jamesonmorrill1@gmail.com` (T-LOCAL-4 mitigation — pinned to real seed user per RESEARCH Pitfall 7) |
| ANTHROPIC_API_KEY | `sk-ant-dev-workspace-key-here` (placeholder; comment flags D-18 dev-vs-prod split for doctor script) |
| POSTHOG_API_KEY | empty string (Phase 103 key-absence gate — capture disabled locally) |
| PORT | `3001` (D-11 — matches retired daemon, Google OAuth redirect URI stays valid) |

**Verification (all passed):**
```
grep -qE '^DATABASE_URL=postgresql://localhost:5432/vigil_dev$' vigil-core/.env.example   # PASS
grep -qE '^JWT_SECRET=' vigil-core/.env.example                                            # PASS
grep -qE '^VIGIL_ALLOWED_EMAILS=jamesonmorrill1@gmail.com$' vigil-core/.env.example        # PASS
grep -qE '^ANTHROPIC_API_KEY=' vigil-core/.env.example                                     # PASS
grep -qE '^POSTHOG_API_KEY=$' vigil-core/.env.example                                      # PASS
grep -qE '^PORT=3001$' vigil-core/.env.example                                             # PASS
! grep -qiE '(rlwy\.net|proxy|railway\.app|hopper)' vigil-core/.env.example                # PASS
grep -c '^[A-Z_]*=' vigil-core/.env.example                                                # 6
grep -c 'Phase 107.1 Plan 03' vigil-core/.env.example                                      # 1
grep -icE 'rlwy\.net|proxy\.rlwy|railway\.app' vigil-core/.env.example                     # 0
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan `<action>` text contradicted plan `<automated>` verification**

- **Found during:** Task 2 verification run
- **Issue:** The plan's `<action>` block specified a file body with two comment lines containing the literal strings `NOT Railway proxy` and `rlwy.net or proxy hostname here` (instructional — warning devs against those patterns). The plan's own `<automated>` verification chain includes `! grep -qiE '(rlwy\.net|proxy|railway\.app|hopper)'` — matching **any** occurrence, including instructional comments. Writing the plan's prescribed content verbatim caused verification to fail.
- **Fix:** Rephrased the two comment lines to use "remote production host" and "remote hostname" instead of "Railway proxy" and "rlwy.net". Preserves T-LOCAL-1 educational intent (tell devs not to paste prod values) without tripping the acceptance regex. All six keys and values remain exactly as specified by the plan.
- **Files modified:** `vigil-core/.env.example` (lines 7–8)
- **Commit:** `20921c4` (single commit — fix applied inline before first commit)

### Auth Gates

None occurred during this plan. Task 1 is a `checkpoint:human-action` gate (manual install), which resolved cleanly on "postgres ready on both machines".

## Known Stubs

None. The `.env.example` is explicitly a placeholder template — the placeholders are the intended final state of this file. `.env` (the real consumer) is written by Plan 03 `dev-setup.sh` from this template.

## Self-Check: PASSED

**Files:**
- `vigil-core/.env.example` — FOUND (35 lines, 6 key=value pairs, zero forbidden hostname patterns)
- `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-01-SUMMARY.md` — FOUND (this file)

**Commits:**
- `20921c4 feat(107.1-01): rewrite .env.example to LOCAL-ONLY shape` — FOUND via `git log --oneline | grep 20921c4`

**Runtime state (iMac):**
- `postgresql@16` brew service: started
- `vigil_dev` database: exists, `PostgreSQL 16.13`
- Listener: `localhost:5432` (localhost-only, IPv4 + IPv6)

Downstream plans (02 seed-local.ts, 03 dev-setup.sh, 06 .env rewrite) can proceed — the local Postgres cluster and the env template contract are both ready.
