---
phase: 107.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md
autonomous: false
requirements:
  - REQ-DEV-LOCAL-ENV
threat_refs:
  - T-LOCAL-6
tags:
  - infra
  - daemon
  - launchctl

must_haves:
  truths:
    - "`launchctl print gui/$UID/com.jamesonmorrill.vigilcore` exits non-zero on the iMac (daemon un-registered)"
    - "`~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist` does not exist on the iMac"
    - "Before plist deletion, a COPY of the plist contents is preserved in `.planning/phases/107.1-.../107.1-daemon-retirement.md` — retirement is reversible (T-LOCAL-6 mitigation)"
    - "MacBook Pro is confirmed already-retired (per memory reference_macbook_pro.md) or retired in this session"
    - "Port 3001 is free for local vigil-core on both machines after retirement"
  artifacts:
    - path: ".planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md"
      provides: "Preserved plist contents + retirement instructions + verification log — reversibility per D-09 'reversible' and T-LOCAL-6 mitigation"
      min_lines: 40
      contains: "com.jamesonmorrill.vigilcore"
  key_links:
    - from: "launchctl bootout"
      to: "com.jamesonmorrill.vigilcore"
      via: "gui/$UID service target (Aqua session — must run in Terminal.app, not SSH)"
      pattern: "gui/.*com.jamesonmorrill.vigilcore"
    - from: "retirement markdown"
      to: "Phase 107.1 VALIDATION D-09"
      via: "plist XML preserved for reversibility"
      pattern: "<plist"
---

<objective>
Retire the vestigial `com.jamesonmorrill.vigilcore` launchd daemon on both dev machines (iMac + MacBook Pro) per D-09. Preserve the plist contents in a phase-local markdown file FIRST so retirement remains reversible (T-LOCAL-6 mitigation — CONTEXT says "reversible" but bare bootout + rm without backup breaks that).

Purpose: The daemon serves no real traffic today — Mac apps point at `api.vigilhub.io` ([Sources/JarvisCore/Config/AppConfig.swift:28](Sources/JarvisCore/Config/AppConfig.swift#L28), [Sources/DailyBriefMonitor/AppDelegate.swift:66](Sources/DailyBriefMonitor/AppDelegate.swift#L66)). The daemon currently fails to start (`LastExitStatus=256` per live iMac check in RESEARCH) and occupies :3001, forcing a `launchctl bootout` dance before every local dev session. Retirement removes the friction.

Output:
- `.planning/phases/107.1-.../107.1-daemon-retirement.md` with preserved plist XML + retirement steps + verification log
- Daemon un-registered on the iMac (manual step — must run in Terminal.app per Aqua constraint)
- MacBook Pro verified already-retired or retired in this session
- Plist file removed from `~/Library/LaunchAgents/` on retired machines
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-CONTEXT.md
@.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md Pattern 3 + Pitfall 2 + Pitfall 4 (all VERIFIED live 2026-04-21) -->

iMac current state:
  - launchctl list com.jamesonmorrill.vigilcore → daemon registered (PID observed 8368, LastExitStatus=256)
  - plist at: ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist
  - LimitLoadToSessionType = Aqua  (MUST retire from GUI Terminal.app, not SSH)

MacBook Pro current state (per memory reference_macbook_pro.md):
  - daemon already unloaded — only needs verification, not retirement
  - the plist may still exist on disk (to confirm, not assume)

Correct retirement sequence (Pattern 3, VERIFIED):
  # Step 1: bootout while plist still exists (service-target form preferred — Form 2)
  launchctl bootout gui/$UID/com.jamesonmorrill.vigilcore
  # Step 2: delete plist
  rm ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist

Verification after retirement:
  launchctl print gui/$UID/com.jamesonmorrill.vigilcore   # should exit non-zero
  launchctl list com.jamesonmorrill.vigilcore              # should exit non-zero
  ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist   # should say "No such file"
  lsof -iTCP:3001 -sTCP:LISTEN                             # should be empty
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Preserve plist XML in phase markdown + retire daemon on both machines</name>
  <files>.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md</files>
  <read_first>
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-RESEARCH.md (Pattern 3 bootout sequence; Pitfall 2 Aqua-session constraint; Pitfall 4 "LastExitStatus=256 still needs bootout")
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-CONTEXT.md (D-09 retirement is reversible — planner note: "future phase could reinstate if a caller emerges")
    - ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist (on the iMac — the file whose contents must be preserved BEFORE deletion)
  </read_first>
  <what-built>
    Human runs the retirement sequence in Terminal.app on each dev machine (Aqua-session constraint from RESEARCH Pitfall 2 — SSH retirement fails silently). Before running `rm`, the human dumps the plist to the preserved markdown so D-09's "reversible" property is maintained.
  </what-built>
  <action>
    **Run these steps in Terminal.app on the LOCAL Mac desktop (NOT via SSH) on each machine. Start with the iMac since the plist is known to exist there.**

    ### Step 1 (iMac only — preserve plist BEFORE deletion):

    In Terminal.app on the iMac:

    ```bash
    # Read current plist contents
    cat ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist
    ```

    Copy the full XML output. Then, inside a working editor on any machine, create `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` with EXACTLY this structure (fill the `[iMac plist XML here]` block with the XML you just copied):

    ```markdown
    # com.jamesonmorrill.vigilcore Daemon Retirement (Phase 107.1 D-09)

    **Retired:** 2026-04-21 (fill actual date)
    **Reversibility:** D-09 locks retirement as reversible. This file preserves the plist contents so a future phase can reinstate the daemon if a real caller emerges (T-LOCAL-6 mitigation).

    ## Preserved plist contents (iMac, as of retirement)

    ```xml
    [iMac plist XML here]
    ```

    ## Preserved plist contents (MacBook Pro, as of retirement)

    ```xml
    [MacBook Pro plist XML here OR note "plist already absent — per memory reference_macbook_pro.md, daemon was unloaded in prior session"]
    ```

    ## Retirement sequence applied

    On each dev machine, in Terminal.app on the local desktop (NOT via SSH — LimitLoadToSessionType=Aqua):

    ```bash
    launchctl bootout gui/$UID/com.jamesonmorrill.vigilcore
    rm ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist
    ```

    ## Post-retirement verification (each machine)

    | Check | iMac | MacBook Pro |
    |-------|------|-------------|
    | `launchctl print gui/$UID/com.jamesonmorrill.vigilcore` exits non-zero | PASS / FAIL | PASS / FAIL |
    | `ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist` says "No such file" | PASS / FAIL | PASS / FAIL |
    | `lsof -iTCP:3001 -sTCP:LISTEN` returns empty | PASS / FAIL | PASS / FAIL |

    ## Reversibility path (if ever needed)

    1. Copy the preserved XML above into `~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist`
    2. `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist`
    3. `launchctl kickstart gui/$UID/com.jamesonmorrill.vigilcore`
    ```

    ### Step 2 (iMac — retire daemon):

    In Terminal.app on the iMac (NOT via SSH — retirement will fail silently in SSH per RESEARCH Pitfall 2):

    ```bash
    # Bootout WHILE plist still exists (service-target form — Pattern 3 preferred form)
    launchctl bootout gui/$UID/com.jamesonmorrill.vigilcore

    # Verify bootout succeeded
    launchctl print gui/$UID/com.jamesonmorrill.vigilcore   # expect: exits non-zero, prints "Could not find service"

    # Delete plist (retirement is now permanent on disk; preserved in markdown above)
    rm ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist

    # Verify plist gone
    ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist 2>&1   # expect: "No such file or directory"

    # Verify port 3001 is free
    lsof -iTCP:3001 -sTCP:LISTEN 2>&1   # expect: empty output
    ```

    ### Step 3 (MacBook Pro — verify or retire):

    In Terminal.app on the MacBook Pro (per memory: daemon is already unloaded, but plist may still exist):

    ```bash
    # Check if daemon registered
    launchctl list com.jamesonmorrill.vigilcore 2>&1
    # If output contains "Label" → daemon still registered → run bootout:
    #   launchctl bootout gui/$UID/com.jamesonmorrill.vigilcore
    # If output is "Could not find service" → skip bootout, daemon already retired

    # Check if plist still on disk
    ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist 2>&1
    # If present → capture XML into retirement markdown (same as Step 1), then:
    #   rm ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist
    # If absent → note this in the markdown table

    # Verify port 3001 free
    lsof -iTCP:3001 -sTCP:LISTEN 2>&1   # expect: empty
    ```

    ### Step 4 (this agent — commit the retirement markdown):

    After the human finishes Steps 1-3 and provides:
    - The iMac plist XML
    - The MacBook Pro plist XML (or confirmation it's absent)
    - Verification output from each machine

    This agent writes `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` with the captured XML and populated verification table, then commits it.
  </action>
  <how-to-verify>
    On each dev machine, after the human runs the steps:

    1. `launchctl print gui/$UID/com.jamesonmorrill.vigilcore 2>&1 | head` → contains "Could not find service" OR exits non-zero
    2. `launchctl list com.jamesonmorrill.vigilcore 2>&1 | head` → prints "Could not find service 'com.jamesonmorrill.vigilcore' in domain for port" OR exits non-zero
    3. `ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist 2>&1` → says "No such file or directory"
    4. `lsof -iTCP:3001 -sTCP:LISTEN` → returns empty (no process listening on 3001)
    5. `test -f .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` → exits 0
    6. `grep -c '<plist' .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` → >=1 (plist XML preserved)

    If the MacBook Pro had the daemon already unloaded and plist absent, check 3 is "plist absent was the starting state" (note in markdown); checks 1-2-4 still must pass.
  </how-to-verify>
  <acceptance_criteria>
    - `test -f .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` exits 0 (retirement record exists)
    - `grep -c '<plist' .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` returns >=1 (at least iMac XML preserved — T-LOCAL-6 reversibility)
    - `grep -q 'launchctl bootout gui/\$UID/com.jamesonmorrill.vigilcore' .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` matches (retirement sequence documented)
    - `grep -q 'launchctl bootstrap gui/\$UID' .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md` matches (reversibility path documented)
    - On the current machine (iMac OR MacBook): `launchctl print gui/$UID/com.jamesonmorrill.vigilcore` exits non-zero (daemon un-registered on THIS machine)
    - On the current machine: `test ! -f ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist` exits 0 (plist removed on THIS machine)
    - On the current machine: `lsof -iTCP:3001 -sTCP:LISTEN` returns empty (port 3001 reclaimed — prerequisite for Plan 05's unified dev)
    - For the OTHER machine (iMac if running on MacBook, or vice versa): status is recorded in the retirement markdown's verification table as PASS OR as "deferred" with a clear note for a follow-up session
  </acceptance_criteria>
  <resume-signal>Type "daemon retired on iMac" / "daemon retired on both" / "daemon retired on current machine, other machine deferred" — include the preserved plist XML blocks (copy-paste `cat ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist` output) and the verification outputs so this agent can populate the retirement markdown.</resume-signal>
  <done>Retirement markdown exists with preserved plist XML + verification table + reversibility path. Daemon is un-registered on at least the current machine. Port 3001 is free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launchctl bootout → launchd registry | Destructive registry mutation; requires Aqua GUI session; cannot be auto-invoked from non-Aqua context (SSH) |
| rm plist → filesystem | Destructive file removal; guarded by pre-read of the plist into markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LOCAL-6 | T (Tampering / irreversibility) | plist deletion | mitigate | Task 1 REQUIRES the plist XML to be preserved in `.planning/phases/107.1-.../107.1-daemon-retirement.md` BEFORE the `rm` step. Markdown also contains the reversibility commands (`launchctl bootstrap`, `launchctl kickstart`) so a future phase can restore the daemon from scratch. D-09's "reversible" promise is now backed by an artifact, not just intent. |
</threat_model>

<verification>
After the human completes Task 1 on each machine:

1. `launchctl print gui/$UID/com.jamesonmorrill.vigilcore 2>&1 | head -3` → exit non-zero (no such service)
2. `ls -la ~/Library/LaunchAgents/ | grep vigilcore` → empty (no vigilcore plist on disk)
3. `lsof -iTCP:3001 -sTCP:LISTEN` → empty (port free for Plan 05 unified dev)
4. Retirement markdown exists and contains at least one `<plist` XML block.
5. Cross-machine coverage: markdown's verification table is populated for BOTH iMac and MacBook Pro rows. If one machine was deferred, the markdown clearly notes which and what's needed for follow-up.

Runtime state after this plan:
- BTM / launchd registry: com.jamesonmorrill.vigilcore absent
- ~/Library/LaunchAgents/: plist file removed
- Port 3001: free
- Reversibility: one file read away (`cat .planning/phases/107.1-.../107.1-daemon-retirement.md`)
</verification>

<success_criteria>
- Daemon is un-registered on the current dev machine
- Plist is removed from `~/Library/LaunchAgents/` on the current dev machine
- Port 3001 is free (no process listening — prerequisite for Plan 05 unified dev)
- Plist XML is preserved in the retirement markdown for each retired machine
- The retirement markdown documents the exact reversibility sequence (`launchctl bootstrap` + `launchctl kickstart`)
- Cross-machine status is recorded; any deferred machine is explicitly flagged with a follow-up action
</success_criteria>

<output>
After completion, create `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-04-SUMMARY.md` documenting:
- Which machine(s) were retired this session (iMac / MacBook Pro / both)
- Whether the MacBook Pro starting state matched memory (already-unloaded) or differed
- Any output anomalies from `launchctl bootout` (warnings, non-fatal errors)
- Location and git status of `107.1-daemon-retirement.md`
- Open item: if one machine was deferred, what the follow-up command sequence is
</output>
