---
phase: 107.1
plan: 05
type: execute
wave: 2
depends_on:
  - 107.1-03
  - 107.1-04
files_modified:
  - package.json
  - package-lock.json
  - vigil-core/package.json
  - vigil-pwa/.env.local
autonomous: false
requirements:
  - REQ-DEV-LOCAL-ENV
threat_refs: []
tags:
  - infra
  - concurrently
  - dev-orchestrator

must_haves:
  truths:
    - "A minimal root package.json exists with only two fields beyond `name`/`private`: `scripts.dev` and `devDependencies.concurrently`. NO `workspaces` field (D-13: not a monorepo)."
    - "`npm install` at the repo root installs `concurrently` at version 9.2.x"
    - "`npm run dev` at the repo root runs preflight-check.sh FIRST, then concurrently with the correct --kill-others flag"
    - "vigil-core/package.json `dev` script is `tsx --env-file=.env watch src/index.ts` (research finding #1 — without --env-file DATABASE_URL is invisible)"
    - "vigil-pwa/.env.local contains `VITE_API_BASE=http://localhost:3001` (research finding #2 — NOT VITE_API_URL)"
    - "Ctrl+C in the `npm run dev` terminal stops BOTH vigil-core and vigil-pwa processes cleanly"
    - "D-15 respected: root dev starts ONLY vigil-core + vigil-pwa; Monitor/Safari/G2 surfaces untouched"
  artifacts:
    - path: "package.json"
      provides: "Thin-orchestrator root package.json"
      contains: "concurrently"
    - path: "package-lock.json"
      provides: "Lockfile for concurrently devDep"
      min_lines: 10
    - path: "vigil-core/package.json"
      provides: "dev script with --env-file=.env flag"
      contains: "tsx --env-file=.env watch"
    - path: "vigil-pwa/.env.local"
      provides: "PWA env pointing at local vigil-core"
      contains: "VITE_API_BASE=http://localhost:3001"
  key_links:
    - from: "root `npm run dev`"
      to: "scripts/preflight-check.sh"
      via: "&& shell chain — preflight blocks concurrently on any failure"
      pattern: "preflight-check.sh && concurrently"
    - from: "root `npm run dev`"
      to: "concurrently 9.2.x"
      via: "--kill-others flag (research finding #5 — NOT --kill-others-on-fail)"
      pattern: "concurrently --kill-others"
    - from: "vigil-core/src/index.ts"
      to: "process.env.DATABASE_URL"
      via: "tsx --env-file=.env makes DATABASE_URL visible"
      pattern: "tsx --env-file"
---

<objective>
Wire the unified dev orchestrator (D-13, D-14, D-15, D-16) plus fix the silent `tsx` env-loading bug (research finding #1):

1. Create a root `package.json` as a **thin orchestrator** (NOT a monorepo — no `workspaces` field).
2. Install `concurrently@^9.2.1` as a devDep at the root.
3. Root `dev` script: `bash scripts/preflight-check.sh && concurrently --kill-others --names 'core,pwa' --prefix-colors 'blue,magenta' 'npm --prefix vigil-core run dev' 'npm --prefix vigil-pwa run dev'`.
4. Fix `vigil-core/package.json` dev script: `tsx watch src/index.ts` → `tsx --env-file=.env watch src/index.ts` (CRITICAL — without this, DATABASE_URL is invisible to the server per RESEARCH Pitfall 1).
5. Ensure `vigil-pwa/.env.local` contains `VITE_API_BASE=http://localhost:3001` (research finding #2 — the var is `VITE_API_BASE`, NOT `VITE_API_URL` as stated in CONTEXT D-11).

Purpose: One-command dev startup (`npm run dev` at repo root) with pre-flight safety, hot-reload, colored logs, unified Ctrl+C. Removes the current two-terminal, two-directory, forget-to-bootout-the-daemon workflow.

Output:
- Root `package.json` + `package-lock.json`
- Updated `vigil-core/package.json` dev script
- Verified `vigil-pwa/.env.local` content
- Live smoke test: `npm run dev` starts both servers, Ctrl+C kills both
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-CONTEXT.md
@.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-RESEARCH.md
@vigil-core/package.json
@vigil-pwa/package.json
@vigil-pwa/.env.local

<interfaces>
<!-- From RESEARCH.md Pattern 2 + research finding #5 — concurrently 9.2.1 flags [VERIFIED] -->
concurrently invocation:
  concurrently --kill-others --names 'core,pwa' --prefix-colors 'blue,magenta' \
    'npm --prefix vigil-core run dev' \
    'npm --prefix vigil-pwa run dev'

  --kill-others: when ANY process exits (crash or Ctrl+C), kill the other. Correct for dev.
  --kill-others-on-fail: only on NON-ZERO exit. Wrong for dev (Ctrl+C exits 0, should kill both).

<!-- From research finding #1 — tsx --env-file is REQUIRED to load .env -->
vigil-core dev script:
  CURRENT: "dev": "tsx watch src/index.ts"                       ← BROKEN after Plan 06 .env repoint
  TARGET:  "dev": "tsx --env-file=.env watch src/index.ts"       ← REQUIRED

  Without --env-file, process.env.DATABASE_URL is undefined, connection.ts sets db=null,
  server starts on 3001 but every DB query silently 500s.

<!-- From research finding #2 — the PWA env var is VITE_API_BASE, NOT VITE_API_URL -->
Correct env var (confirmed in vigil-pwa/src/api/client.ts + live vigil-pwa/.env.local):
  VITE_API_BASE=http://localhost:3001    ← correct
  VITE_API_URL=...                        ← WRONG — PWA would ignore this and default to api.vigilhub.io

<!-- From D-13 root package.json shape — NOT a monorepo -->
Root package.json MUST NOT contain a `workspaces` field. Minimal shape:
  {
    "name": "dailybrief",
    "private": true,
    "scripts": { "dev": "..." },
    "devDependencies": { "concurrently": "^9.2.1" }
  }

<!-- From D-16 — root dev does NOT auto-start brew services postgresql@16 -->
preflight-check.sh (Plan 03) verifies Postgres is running; does NOT start it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create root package.json + install concurrently + update vigil-core dev script</name>
  <files>package.json, package-lock.json, vigil-core/package.json</files>
  <read_first>
    - vigil-core/package.json (the current `dev` line at line 6 — `"dev": "tsx watch src/index.ts"` must change)
    - vigil-pwa/package.json (confirm its `dev` script is `vite` — no change needed there)
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-RESEARCH.md (Pattern 2 concurrently; Pattern 6 tsx --env-file; Pitfall 1 silent tsx .env ignore; research finding #5 --kill-others)
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-CONTEXT.md (D-13 no workspaces, D-14 preflight first, D-15 only core+pwa, D-16 no auto-start brew)
  </read_first>
  <action>
    **Step A — Create root `package.json`** (file does NOT currently exist — per `ls` of repo root, there's no package.json at `/Users/jamesonmorrill/Desktop/Local AI/dailybrief/package.json`). Create it using the `Write` tool with this exact content:

    ```json
    {
      "name": "dailybrief",
      "private": true,
      "description": "Vigil monorepo orchestrator (NOT a workspaces monorepo per Phase 107.1 D-13 — thin orchestrator only)",
      "scripts": {
        "dev": "bash scripts/preflight-check.sh && concurrently --kill-others --names 'core,pwa' --prefix-colors 'blue,magenta' 'npm --prefix vigil-core run dev' 'npm --prefix vigil-pwa run dev'"
      },
      "devDependencies": {
        "concurrently": "^9.2.1"
      }
    }
    ```

    **Verify:** this file has NO `workspaces` field, NO `dependencies`, and exactly ONE npm script named `dev`.

    **Step B — Install concurrently** at the repo root:

    ```bash
    cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && npm install
    ```

    This creates:
    - `node_modules/concurrently/` (package tree)
    - `package-lock.json` (lockfile pinning exact versions)

    Ensure `package-lock.json` is NOT in any .gitignore; the lockfile should be committed so both machines resolve to the same concurrently version.

    **Step C — Fix vigil-core/package.json dev script** (research finding #1 — CRITICAL). Use the `Edit` tool on `vigil-core/package.json` line 6:

    Change:
    ```json
        "dev": "tsx watch src/index.ts",
    ```

    To:
    ```json
        "dev": "tsx --env-file=.env watch src/index.ts",
    ```

    **Rationale:** Without `--env-file=.env`, `tsx 4.x` does NOT auto-load `.env`. After Plan 06 rewrites `vigil-core/.env` to local values, the server would start on :3001 but every DB query would silently 500 because DATABASE_URL is undefined. The flag is the single-character fix that makes the whole chain work.

    **Do NOT modify any other line in vigil-core/package.json.** The `seed:local` script added in Plan 02 stays. All other scripts untouched.
  </action>
  <verify>
    <automated>test -f package.json && test -f package-lock.json && test -d node_modules/concurrently && node -e 'const p=JSON.parse(require("fs").readFileSync("package.json","utf8")); if (p.workspaces) { console.error("FAIL: workspaces field present"); process.exit(1); } if (!p.scripts.dev) { console.error("FAIL: no dev script"); process.exit(1); } if (!p.devDependencies.concurrently) { console.error("FAIL: no concurrently devDep"); process.exit(1); } console.log("root package.json OK");' && grep -q 'preflight-check.sh && concurrently --kill-others' package.json && grep -q "\\-\\-names 'core,pwa'" package.json && grep -q '"dev": "tsx --env-file=.env watch src/index.ts"' vigil-core/package.json && node -e 'JSON.parse(require("fs").readFileSync("vigil-core/package.json","utf8"))' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f package.json` exits 0 (root package.json created)
    - `test -f package-lock.json` exits 0 (lockfile created by npm install)
    - `test -d node_modules/concurrently` exits 0 (package installed)
    - `node -e 'const p=JSON.parse(require("fs").readFileSync("package.json","utf8")); console.log(!!p.workspaces)'` prints `false` (D-13: NOT a monorepo)
    - `grep -F 'preflight-check.sh && concurrently --kill-others' package.json` matches (D-14: preflight first; research finding #5: --kill-others NOT --kill-others-on-fail)
    - `grep -F "--names 'core,pwa'" package.json` matches (named streams)
    - `grep -F "--prefix-colors 'blue,magenta'" package.json` matches (D-13 suggested colors)
    - `grep -F "npm --prefix vigil-core run dev" package.json && grep -F "npm --prefix vigil-pwa run dev" package.json` both match (D-15: only core + pwa, nothing else)
    - `grep -F 'Monitor' package.json` returns 0 matches (D-15: Monitor untouched)
    - `grep -F '"dev": "tsx --env-file=.env watch src/index.ts"' vigil-core/package.json` matches (research finding #1 fix applied)
    - `node -e 'JSON.parse(require("fs").readFileSync("vigil-core/package.json","utf8"))'` exits 0 (JSON still valid after edit)
    - `node -e 'const p=JSON.parse(require("fs").readFileSync("node_modules/concurrently/package.json","utf8")); console.log(p.version)'` prints `9.2.x` (concurrently installed at expected major version)
  </acceptance_criteria>
  <done>Root package.json exists with thin-orchestrator shape (no workspaces), concurrently@9.2.x installed and lockfiled, vigil-core dev script patched with --env-file=.env, JSON valid on both modified package.json files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live smoke test — `npm run dev` starts both, Ctrl+C kills both; verify vigil-pwa/.env.local has VITE_API_BASE</name>
  <files>vigil-pwa/.env.local</files>
  <read_first>
    - vigil-pwa/.env.local (current content — should already be `VITE_API_BASE=http://localhost:3001` per live check 2026-04-21; if not, fix it)
    - vigil-pwa/src/api/client.ts (confirms which env var name the code reads)
    - .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-RESEARCH.md (research finding #2, Pitfall 5)
  </read_first>
  <what-built>
    Before the checkpoint, this agent has:
    - Created root package.json + installed concurrently (Task 1 complete)
    - Patched vigil-core dev script with --env-file=.env (Task 1 complete)
    - Verified vigil-pwa/.env.local content is `VITE_API_BASE=http://localhost:3001` (single line — if it's wrong, fix it with Write tool)

    At this checkpoint, the human runs `npm run dev` and verifies both servers start, logs stream with colored prefixes, and Ctrl+C stops everything.
  </what-built>
  <action>
    **Agent actions BEFORE handing off to human:**

    1. Read `vigil-pwa/.env.local`. If its content is NOT exactly `VITE_API_BASE=http://localhost:3001\n`, use the `Write` tool to overwrite with that exact content. If the file contains `VITE_API_URL=...` (the wrong name from CONTEXT D-11 which is a typo per research finding #2), REPLACE with `VITE_API_BASE=...`.

    2. Confirm preconditions for live smoke test (these require Plan 01 + Plan 04 complete on this machine):
       - `pg_isready -h localhost -q` exits 0 (Plan 01 Task 1 done)
       - `launchctl list com.jamesonmorrill.vigilcore 2>&1 | grep -q Label` returns non-zero (Plan 04 done — daemon gone)
       - `lsof -iTCP:3001 -sTCP:LISTEN` returns empty (port 3001 free)

       If any precondition fails, print a clear message and STOP — do not hand off to the human for a smoke test that will only expose a known-unready environment.

    **Human-verify instructions (for the checkpoint):**

    Run this from the repo root (`/Users/jamesonmorrill/Desktop/Local AI/dailybrief`) in a fresh Terminal.app window:

    ```bash
    npm run dev
    ```

    Expected output (within ~15 seconds):

    1. Preflight section: 4 PASS lines (`[preflight] Check 1/2/3/4 ... PASS`) and `preflight: all checks passed`.
    2. Concurrently handoff: colored `[core]` (blue) and `[pwa]` (magenta) prefixes start streaming.
    3. `[core]` prefix lines include something like `listening on 3001` or `Server ready on port 3001` (from vigil-core).
    4. `[pwa]` prefix lines include Vite's ready message — something like `VITE v... ready in NNN ms` and `Local: http://localhost:5173`.
    5. No `DATABASE_URL not set` warning in the [core] stream (confirms research finding #1 fix worked).
    6. Hit `http://localhost:5173` in a browser — PWA should load and (if logged in locally) should make API calls to localhost:3001. If the browser's network tab shows calls going to `api.vigilhub.io`, something is wrong — likely `VITE_API_BASE` env var is wrong.

    Then press `Ctrl+C` in the terminal. Expected:
    - Both `[core]` and `[pwa]` streams stop.
    - Shell prompt returns.
    - `lsof -iTCP:3001 -sTCP:LISTEN` and `lsof -iTCP:5173 -sTCP:LISTEN` both return empty (both processes killed cleanly).

    If any of these is wrong, describe the observed behavior in the resume signal so this agent can diagnose.
  </action>
  <how-to-verify>
    Before handing off, agent verifies via automated checks:
    - `grep -q '^VITE_API_BASE=http://localhost:3001$' vigil-pwa/.env.local` matches
    - `! grep -q 'VITE_API_URL' vigil-pwa/.env.local` (the wrong name is not present)
    - Preconditions (Plan 01 + Plan 04 state) all pass

    During checkpoint, human verifies:
    - `npm run dev` prints preflight PASS lines then both streams start
    - Colored prefixes [core]=blue, [pwa]=magenta visible in output
    - No "DATABASE_URL not set" warning in [core] stream
    - Both ports reachable (localhost:3001 answers for vigil-core /v1/health, localhost:5173 serves PWA)
    - Browser requests from localhost:5173 hit localhost:3001 (not api.vigilhub.io)
    - Ctrl+C kills both; `lsof -iTCP:3001 -sTCP:LISTEN` empty AND `lsof -iTCP:5173 -sTCP:LISTEN` empty after

    Post-checkpoint automated:
    - `lsof -iTCP:3001 -sTCP:LISTEN` exits with no output (port freed after Ctrl+C)
    - `pgrep -f 'tsx.*watch.*src/index.ts'` returns nothing
    - `pgrep -f 'vite'` (the pwa watcher) returns nothing
  </how-to-verify>
  <acceptance_criteria>
    - `grep -E '^VITE_API_BASE=http://localhost:3001$' vigil-pwa/.env.local` matches exactly (correct env var per research finding #2)
    - `grep -F 'VITE_API_URL' vigil-pwa/.env.local` returns 0 matches (the wrong name is nowhere)
    - Human confirms (in resume signal): "preflight passed, [core] and [pwa] both streamed logs, Ctrl+C killed both cleanly, PWA talks to localhost:3001"
    - Human confirms NO occurrence of `DATABASE_URL not set — PostgreSQL features unavailable` in the [core] stream (this is the Pitfall 1 warning signal; its absence confirms --env-file fix worked live)
    - After Ctrl+C, agent verifies `lsof -iTCP:3001 -sTCP:LISTEN` and `lsof -iTCP:5173 -sTCP:LISTEN` are both empty (both child processes terminated — --kill-others worked)
  </acceptance_criteria>
  <resume-signal>Describe what happened: (a) did preflight pass? (b) did both servers start? (c) did colored prefixes appear? (d) any DATABASE_URL warning? (e) did browser at localhost:5173 talk to localhost:3001? (f) did Ctrl+C kill both? Paste any error or unexpected line.</resume-signal>
  <done>`npm run dev` at repo root: preflight passes, both servers start with colored prefixes, PWA talks to localhost:3001, Ctrl+C kills both, no DATABASE_URL warning in [core] stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root `npm run dev` → preflight → child processes | Orchestration only; inherits trust boundary from Plan 03 preflight-check.sh |
| concurrently → child npm processes | Standard npm package; fork + pipe; no elevated privileges |

## STRIDE Threat Register

No new trust boundaries or threats introduced. Plan 05 is pure orchestration. Threat mitigations from Plan 03 (T-LOCAL-3 — preflight cannot be bypassed) are inherited because root `dev` chains `bash scripts/preflight-check.sh &&` before concurrently — if preflight exits non-zero, concurrently never launches. This is verified by the acceptance_criteria grep for `preflight-check.sh && concurrently`.
</threat_model>

<verification>
After both tasks complete:

1. Static: Task 1 `<automated>` chain passes (root package.json valid, no workspaces, preflight && concurrently wiring, vigil-core --env-file fix).
2. Live (Task 2): human confirms `npm run dev` → both start → Ctrl+C → both stop.
3. No DATABASE_URL-undefined regression: if Plan 06 hasn't run yet, vigil-core/.env may still have prod values — in that case, the [core] stream won't show the "DATABASE_URL not set" warning (because it IS set, just to prod). Plan 06 runs AFTER this plan and rewrites .env to local. The --env-file fix here is a prerequisite — it ensures the eventual local DATABASE_URL gets loaded by tsx.
4. Cross-check: `git diff vigil-core/package.json` shows ONLY the `dev` script line changed (no collateral edits).
</verification>

<success_criteria>
- Root `package.json` is a thin orchestrator (no `workspaces`), with `dev` script chaining preflight + concurrently (--kill-others)
- `concurrently@^9.2.1` installed, lockfile committed
- vigil-core dev script has `tsx --env-file=.env watch src/index.ts` (research finding #1)
- vigil-pwa/.env.local has `VITE_API_BASE=http://localhost:3001` (research finding #2)
- Live smoke test: both servers start, colored prefixes, no DATABASE_URL warning, Ctrl+C kills both
- Only vigil-core + vigil-pwa are started — Monitor, Safari extension, G2 plugin are not referenced (D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-05-SUMMARY.md` documenting:
- concurrently exact version installed (from node_modules/concurrently/package.json)
- Whether live smoke test was performed (yes/no — depends on Plan 01 + Plan 04 done on this machine)
- Any deviation in prefix colors or check behavior
- Pre-existing vigil-pwa/.env.local state (was it already correct, or did the agent fix VITE_API_URL → VITE_API_BASE?)
- Human observations from Task 2 checkpoint (paste verbatim)
</output>
