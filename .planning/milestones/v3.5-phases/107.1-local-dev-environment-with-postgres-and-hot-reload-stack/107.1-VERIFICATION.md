---
phase: 107.1-local-dev-environment-with-postgres-and-hot-reload-stack
verified: 2026-04-21T19:15:00Z
status: human_needed
score: 8/8 must-haves verified (live preflight green; dual-stream smoke awaits first real dev session)
overrides_applied: 0
human_verification:
  - test: "Run `npm run dev` at repo root in a fresh Terminal and watch both streams start"
    expected: "4 preflight PASS lines, then colored [core] (blue) + [pwa] (magenta) streams; [core] prints server listening on 3001 with NO 'DATABASE_URL not set' warning; [pwa] prints Vite ready on 5173; Ctrl+C stops both cleanly and lsof on 3001 + 5173 returns empty"
    why_human: "concurrently dual-stream orchestration + hot-reload + Ctrl+C signal forwarding can only be observed in a live interactive dev session — Plan 05 Task 2 (checkpoint:human-verify) was formally deferred by user approval to this natural first dev session"
  - test: "Browser at http://localhost:5173 makes API calls to http://localhost:3001 (not api.vigilhub.io)"
    expected: "Network tab shows requests to localhost:3001 for /v1/* endpoints"
    why_human: "VITE_API_BASE consumption is only observable via a running PWA + browser"
  - test: "After confirmed stable dev use (~24h), delete vigil-core/.env.bak.20260421-184421"
    expected: "File removed; .env.bak.* gitignore pattern continues to protect any future backups"
    why_human: "Backup preserves pre-rewrite prod credentials on disk — user's call when rollback window has closed (gitignored, local-only, so risk is bounded but still a hygiene follow-up)"
deferred:
  - truth: "Seed fixture data (1 user, 1 vk_, 5 thoughts, 1 project, 1 work order) actually lands in vigil_dev via npm run seed:local"
    addressed_in: "Follow-on migration plan (drizzle/0013_work_orders_drift_repair.sql) — tracked in deferred-items.md"
    evidence: "Plan 02 deferred-items.md documents the pre-existing work_orders schema drift (notes/archived_at/last_change_at/last_change_summary columns in schema.ts not in drizzle/ migrations). Drift predates Phase 107.1 and affects Railway prod too. Seed script itself is correct and will succeed end-to-end once migration lands. Doctor row correctly shows 'unreachable — run: bash scripts/dev-setup.sh' as the actionable fix."
---

# Phase 107.1: Local Dev Environment Verification Report

**Phase Goal:** Local Postgres + one-command dev orchestrator for vigil-core + vigil-pwa, with daemon retired and prod secrets off the dev disk — so `npm run dev` at repo root no longer mutates Railway.

**Verified:** 2026-04-21T19:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Goal-Backward Decomposition)

The phase goal decomposes into 8 observable truths. Each is verified against the live codebase; ROADMAP has no explicit Success Criteria block, so truths are derived from the goal sentence plus the must_haves frontmatter of each of the 7 plans (deduplicated).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `npm run dev` at repo root is wired and gates `concurrently` behind `preflight-check.sh` | ✓ VERIFIED | `package.json` dev script chains `bash scripts/preflight-check.sh && concurrently --kill-others --names 'core,pwa' --prefix-colors 'blue,magenta' 'npm --prefix vigil-core run dev' 'npm --prefix vigil-pwa run dev'`. `concurrently@9.2.1` installed (`node_modules/concurrently/package.json`). Root `package.json` has NO `workspaces` field (D-13 thin orchestrator). Monitor/G2/Safari absent from dev script (D-15). |
| 2 | `vigil-core/.env` is LOCAL-ONLY (no Railway hostnames) and gitignored | ✓ VERIFIED | Live audit: 6 keys present (DATABASE_URL, JWT_SECRET, VIGIL_ALLOWED_EMAILS, ANTHROPIC_API_KEY, POSTHOG_API_KEY, PORT). `DATABASE_URL=postgresql://localhost:5432/vigil_dev`. Zero matches for `rlwy.net|railway.app|proxy.rlwy|hopper`. JWT_SECRET length 64 (≥32 gate). POSTHOG blank (Phase 103 key-absence gate). PORT=3001. File mode 600. `git ls-files vigil-core/.env vigil-core/.env.bak.*` returns empty — neither file tracked. `.gitignore` lines 10–15 cover `.env`, `*.env`, `.env.bak.*`, `*.env.bak.*`. |
| 3 | `com.jamesonmorrill.vigilcore` daemon is retired on this machine (port 3001 free) | ✓ VERIFIED | Live: `launchctl list com.jamesonmorrill.vigilcore` → "Could not find service" (exit 113). `~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist` → No such file. `lsof -iTCP:3001 -sTCP:LISTEN` → empty. Retirement preserved with plist XML + `launchctl bootstrap` reversibility path in `107.1-daemon-retirement.md` (115 lines, 1 `<plist>` XML block, `launchctl bootstrap gui/$UID` reversibility documented). MacBook Pro retirement confirmed in 04-SUMMARY (corrected stale memory claim). |
| 4 | postgresql@16 (16.13) provisioned with `vigil_dev` DB accepting localhost connections | ✓ VERIFIED | Live: `brew services list` → `postgresql@16 started`. `/usr/local/opt/postgresql@16/bin/psql -h localhost -d vigil_dev -c "SELECT version()"` → `PostgreSQL 16.13 (Homebrew) on x86_64-apple-darwin23.6.0`. Matches Railway pin (D-02). Preflight Check 3 live PASS. |
| 5 | Anthropic dev-workspace key separated from prod (`$20/mo cap`) | ✓ VERIFIED | `vigil-core/.env` ANTHROPIC_API_KEY prefix `sk-ant-api03`, length 108 (plan spec). D-18 documented in `sync-anthropic-key.sh` header. `--include-config-env` opt-in flag present. Default path no longer syncs local .env. Plist sync block replaced with Phase 107.1 D-09 retirement notice. User confirmed workspace + $20 cap in Plan 06 resume signal. Full key held in user's password manager only (never in commits/summaries per redaction grep). |
| 6 | Three dev scripts (dev-setup, preflight-check, dev-reset) present, executable, structurally sound | ✓ VERIFIED | `scripts/preflight-check.sh` (104 lines, exec, 4 distinct `info "Check` lines, 0 bypass flag/env var — T-LOCAL-3 clean). `scripts/dev-setup.sh` (127 lines, exec, T-LOCAL-2 backup via `cp "$CORE_ENV" "$BACKUP"`, preflight called at end, detects-not-retires daemon, uses VITE_API_BASE correctly, calls `db:migrate` + `seed:local`). `scripts/dev-reset.sh` (26 lines, exec, `dropdb --if-exists vigil_dev`, interactive `read -r` y/N confirm). `bash -n` clean on all three. |
| 7 | RUNBOOK.md + README.md document the local-dev workflow | ✓ VERIFIED | `vigil-core/RUNBOOK.md`: "Local Development (Phase 107.1)" section present with first-time setup, daily workflow, schema resync, dev-reset.sh, daemon-retirement.md cross-link, `VITE_API_BASE=http://localhost:3001` correct (zero `VITE_API_URL` occurrences), secret-drift D-18 amendment, preflight failure triage, backup locations, Anthropic key rotation. `README.md`: "Local Dev Quickstart (macOS)" section at line 63 with `bash scripts/dev-setup.sh` + `npm run dev` + RUNBOOK link. Fence count even on both files. |
| 8 | REQ-DEV-LOCAL-ENV exists in REQUIREMENTS.md with traceability row | ✓ VERIFIED | `.planning/REQUIREMENTS.md` line 52: `REQ-DEV-LOCAL-ENV` subsection with substantive description. Line 111: traceability row `\| REQ-DEV-LOCAL-ENV \| Phase 107.1 \| Complete \|`. Line 114: coverage counter updated to "13 user-facing + 1 infra hygiene = 14 total". Last-updated timestamp 2026-04-21. |

**Score:** 8/8 truths verified

### Deferred Items

Items explicitly acknowledged as out of scope for Phase 107.1 and tracked for follow-on work.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Live end-to-end `npm run seed:local` producing real fixture rows (1 user, 1 vk_, 5 thoughts, 1 project, 1 work order) in `vigil_dev` | Follow-on migration plan (drizzle/0013 work_orders drift repair) | `deferred-items.md`: "work_orders drift" — `schema.ts:232-252` declares `notes`, `archived_at`, `last_change_at`, `last_change_summary` columns; `drizzle/*.sql` migrations don't create them. Drift predates 107.1 and exists in Railway prod too. Seed script is correct; will succeed once migration lands. Doctor row correctly prints actionable fix `bash scripts/dev-setup.sh`. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` (root) | Thin orchestrator, no workspaces, `dev` chains preflight + concurrently | ✓ VERIFIED | 11 lines, no `workspaces` field, `concurrently@^9.2.1` devDep, dev script exactly matches D-13/D-14/D-15 contract |
| `package-lock.json` (root) | Lockfile pinning concurrently | ✓ VERIFIED | 327 lines, concurrently@9.2.1 pinned |
| `vigil-core/package.json` | `dev` = `tsx --env-file=.env watch src/index.ts`; `seed:local` wired | ✓ VERIFIED | Both scripts present with correct `--env-file=.env` flag (RESEARCH finding #1) |
| `vigil-core/.env` | LOCAL-ONLY 6-key shape, gitignored | ✓ VERIFIED | 745 bytes, mode 600, 6 keys, localhost DATABASE_URL, no Railway hostnames, not tracked in git |
| `vigil-core/.env.example` | Template for `dev-setup.sh` | ✓ VERIFIED | 6 keys, localhost, no prod hostnames, contains "Phase 107.1 Plan 03" traceability comment |
| `vigil-core/scripts/seed-local.ts` | Idempotent fixture seed | ✓ VERIFIED | 205 lines, type-check clean (tsc --noEmit exit 0), onConflictDoNothing + existence guards present, all 5 thought categories fixtured |
| `vigil-pwa/.env.local` | `VITE_API_BASE=http://localhost:3001` | ✓ VERIFIED | Exact single-line match; no VITE_API_URL |
| `scripts/preflight-check.sh` | 4 fail-loud checks, no bypass | ✓ VERIFIED | 104 lines, exec, 4 distinct checks, 0 bypass patterns, syntactically valid |
| `scripts/dev-setup.sh` | Idempotent first-time bootstrap with T-LOCAL-2 backup | ✓ VERIFIED | 127 lines, exec, T-LOCAL-2 backup present, preflight final-call, detects-not-retires daemon |
| `scripts/dev-reset.sh` | Interactive confirm destructive wipe + rebuild | ✓ VERIFIED | 26 lines, exec, `dropdb --if-exists` + `read -r` confirm. **Note:** CR-01 gap (missing PGHOST=localhost pin) is a known follow-up; see Anti-Patterns |
| `scripts/sync-anthropic-key.sh` | No plist write, default-off local .env | ✓ VERIFIED | D-18 header, `--include-config-env` opt-in, plist retirement notice, `--skip-launchagent` no-op |
| `scripts/dailybrief-doctor.sh` | D-19 INFORMATIONAL row (exit-code-neutral) | ✓ VERIFIED | `PG_BIN_107_1` namespaced, 3 branches, 0 `DRIFT_COUNT=` inside the new block |
| `vigil-core/RUNBOOK.md` | Local Development section | ✓ VERIFIED | "Local Development (Phase 107.1)" heading + practical ops content (setup, daily, resync, reset, secret-drift, triage, backups, Anthropic rotation) |
| `README.md` | Local Dev Quickstart | ✓ VERIFIED | "Local Dev Quickstart (macOS)" section inserted before existing Setup content |
| `.planning/REQUIREMENTS.md` | REQ-DEV-LOCAL-ENV row | ✓ VERIFIED | Subsection + traceability row + coverage 13→14 + last-updated 2026-04-21 |
| `107.1-daemon-retirement.md` | Plist XML + reversibility sequence preserved | ✓ VERIFIED | 115 lines, 1 `<plist>` XML block, `launchctl bootstrap gui/$UID` reversibility documented |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| root `npm run dev` | `scripts/preflight-check.sh` | `&&` shell chain (preflight blocks concurrently on any failure) | ✓ WIRED | `grep 'preflight-check.sh && concurrently'` matches in package.json; live run confirms preflight executes first and blocks concurrently on FAIL (Plan 03 live test with pre-rewrite Railway .env) |
| root `npm run dev` | `concurrently@9.2.1` | `--kill-others` flag with `--names 'core,pwa'` | ✓ WIRED | Exact flag verified in package.json and via `node -e "require('./node_modules/concurrently/package.json').version"` → `9.2.1` |
| `vigil-core/src/db/connection.ts` | `process.env.DATABASE_URL` | `tsx --env-file=.env` | ✓ WIRED | `npx tsx --env-file=.env -e 'console.log(!!process.env.DATABASE_URL)'` returns `true` live; DATABASE_URL starts with `postgresql://localhost` |
| `scripts/dev-setup.sh` | `preflight-check.sh` | sourced at completion | ✓ WIRED | `grep 'preflight-check.sh' scripts/dev-setup.sh` matches |
| `scripts/dev-setup.sh` | `seed:local` + `db:migrate` | `npm --prefix vigil-core run ...` | ✓ WIRED | Both grep matches |
| `vigil-pwa` API client | `localhost:3001` | `VITE_API_BASE` env var | ✓ WIRED (static) | env var correct in .env.local; runtime browser→API verification is in human_verification section |
| `scripts/dailybrief-doctor.sh` D-19 row | `vigil_dev` DB | `psql -h localhost -d vigil_dev -c "SELECT COUNT(*) FROM users"` | ✓ WIRED | Live doctor run shows row with "unreachable — run: bash scripts/dev-setup.sh" (correct failure branch fires; users table absent due to deferred work_orders drift, doctor surfaces actionable fix) |

### Data-Flow Trace (Level 4)

Not applicable at the artifact level for this phase — the deliverables are infrastructure (env files, shell scripts, documentation, package.json). The one dynamic-data surface (the doctor's D-19 row) is verified WIRED and its "unreachable" branch fires correctly; the "connected (N users)" branch awaits the deferred migration fix.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Preflight 4-check gate runs and exits 0 on healthy env | `bash scripts/preflight-check.sh; echo exit=$?` | All 4 PASS lines, `exit=0` | ✓ PASS |
| tsx --env-file actually loads DATABASE_URL | `cd vigil-core && npx tsx --env-file=.env -e 'console.log(!!process.env.DATABASE_URL)'` | `true`, starts with `postgresql://localhost` | ✓ PASS |
| concurrently is installed at pinned version | `node -e "console.log(require('./node_modules/concurrently/package.json').version)"` | `9.2.1` | ✓ PASS |
| Postgres accepting localhost connections | `/usr/local/opt/postgresql@16/bin/pg_isready -h localhost -q; echo $?` | `0` | ✓ PASS |
| vigil_dev database exists | `psql -h localhost -lqt \| grep vigil_dev` | `vigil_dev \| jamesonmorrill \| UTF8 \| ...` | ✓ PASS |
| Daemon un-registered | `launchctl list com.jamesonmorrill.vigilcore 2>&1` | `Could not find service` | ✓ PASS |
| Plist absent | `ls ~/Library/LaunchAgents/com.jamesonmorrill.vigilcore.plist 2>&1` | `No such file or directory` | ✓ PASS |
| Port 3001 free | `lsof -iTCP:3001 -sTCP:LISTEN` | empty | ✓ PASS |
| vigil-core/.env gitignored | `git check-ignore -v vigil-core/.env` | `.gitignore:11:*.env vigil-core/.env` | ✓ PASS |
| .env.bak gitignored | `git check-ignore -v vigil-core/.env.bak.20260421-184421` | `.gitignore:15:*.env.bak.*` | ✓ PASS |
| vigil-core type-check clean | `cd vigil-core && npx tsc --noEmit` | exit 0 | ✓ PASS |
| doctor D-19 row renders | `bash scripts/dailybrief-doctor.sh \| grep 'local vigil_dev DB'` | `local vigil_dev DB \| unreachable — run: bash scripts/dev-setup.sh` | ✓ PASS |
| sync-anthropic-key.sh D-18 rewrite valid | `bash -n scripts/sync-anthropic-key.sh` | exit 0 | ✓ PASS |
| Live `npm run dev` dual-stream + Ctrl+C | interactive | not run | ? SKIP — routed to human_verification (Plan 05 Task 2 deferred) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-DEV-LOCAL-ENV | 107.1-01 through 107.1-07 (all 7 plans) | Local development for vigil-core + vigil-pwa runs against a LOCAL Postgres instance with LOCAL secrets — no prod DATABASE_URL or prod ANTHROPIC_API_KEY on the dev disk. Unified `npm run dev` at repo root boots both servers with pre-flight safety check. Setup is a one-command bootstrap (`bash scripts/dev-setup.sh`). Documented once in RUNBOOK.md. | ✓ SATISFIED | All 8 observable truths verified; REQ-DEV-LOCAL-ENV traceability row present with Complete status. Last remaining surface (live dual-stream boot) routed to human_verification per Plan 05 Task 2 user-approved deferral. |

No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/dev-reset.sh` | ~8–21 | Missing `export PGHOST=localhost` pin; no preflight call; no target-host echo before destructive drop | ⚠️ Warning (CR-01) | If invoking shell has `PGHOST` exported (possible on MacBook Pro per memory note), `dropdb --if-exists vigil_dev` + `createdb vigil_dev` + `db:migrate` + `seed:local` could target a REMOTE Postgres. Mitigated in practice by: (a) preflight-check.sh already verifies DATABASE_URL is localhost in the `npm run dev` path, (b) dev-reset.sh is not invoked by npm run dev, (c) user doesn't currently have PGHOST exported. Tracked as follow-up in REVIEW.md. Does NOT regress the phase goal (prod secrets off dev disk, one-command orchestrator) because the phase goal is achieved by the `npm run dev` path which does gate correctly. Per context: "code review flagged 1 critical + 4 warnings — tracked for follow-up, not blocking." |
| `scripts/dev-setup.sh` | ~100–107 | No unreplaced JWT_SECRET placeholder detection (WR-01) | ℹ️ Info | Ergonomic footgun — if operator never edits .env after first `dev-setup.sh` run, they get a known JWT_SECRET. Bounded blast radius (local only). Non-blocking. |
| `scripts/dev-setup.sh` | ~75–80 | Warns on Railway hostname in existing .env but continues (WR-03) | ℹ️ Info | `db:migrate` + `seed:local` could run against Railway if operator misses the yellow warning. Final preflight catches it *after* the destructive steps. Non-blocking per context. |
| `scripts/sync-anthropic-key.sh` | ~85–101 | Non-atomic write to `~/.config/dailybrief/.env` (WR-04) | ℹ️ Info | Pre-existing; only runs with `--include-config-env` opt-in. Non-blocking. |
| `vigil-core/scripts/seed-local.ts` | 39 | `SEED_PASSWORD = "dev-password-1234"` literal | ℹ️ Info (IN-02) | Intended behavior for deterministic local seed. Comment could be more emphatic. Non-blocking. |

All warnings tracked in 107.1-REVIEW.md; none block the phase goal.

### Human Verification Required

1. **Live `npm run dev` dual-stream smoke test** — open a fresh Terminal at repo root, run `npm run dev`. Expect: 4 preflight PASS lines, then colored [core] (blue) and [pwa] (magenta) streams, no `DATABASE_URL not set` warning in [core], Vite ready on 5173, press Ctrl+C and confirm both ports freed (`lsof -iTCP:3001 -sTCP:LISTEN` and `lsof -iTCP:5173 -sTCP:LISTEN` both empty). Plan 05 Task 2 was formally deferred here per user-approved path A; this is its natural resolution.

2. **Browser → localhost:3001 routing** — open http://localhost:5173 in a browser, confirm the network tab shows API calls landing on localhost:3001 (not api.vigilhub.io). Confirms VITE_API_BASE consumption.

3. **Backup cleanup after stable dev window (~24h)** — delete `vigil-core/.env.bak.20260421-184421` once confident the localhost dev config is stable. File is gitignored so commit risk is zero, but it still holds pre-rewrite prod DATABASE_URL + Anthropic key until deleted.

### Gaps Summary

No gaps. All 8 observable truths supporting the phase goal are verified against the live codebase:

- **Prod mutation risk eliminated** (goal's primary clause): vigil-core/.env is LOCAL-ONLY with 6 keys, zero Railway hostnames, gitignored along with its timestamped backup. Preflight Check 4 is a hard gate on the npm run dev path with no bypass (T-LOCAL-3 mitigation verified: 0 matches for `SKIP_PREFLIGHT|--skip|FORCE_DEV|--force|--bypass` across scripts).
- **Local Postgres provisioned** (D-01, D-02, D-03): postgresql@16 16.13 brew-managed, vigil_dev DB, matches Railway pin; D-03 identical setup on iMac + MacBook Pro (MacBook Pro daemon retired over SSH in this session, stale memory corrected).
- **Daemon retired** (D-09): launchctl un-registered, plist removed, port 3001 free, reversibility artifact preserved with full plist XML + `launchctl bootstrap` restoration sequence.
- **One-command orchestrator** (D-13, D-14, D-15): root package.json is a thin orchestrator (no workspaces), chains preflight → concurrently --kill-others, starts only vigil-core + vigil-pwa, Monitor/Safari/G2 untouched.
- **Silent tsx bug preempted** (RESEARCH finding #1): `tsx --env-file=.env` verified live loads DATABASE_URL correctly — this was the load-bearing fix that makes the whole phase usable.
- **Anthropic dev-workspace split** (D-18): workspace + $20 cap + dev key in .env, sync script aware of expected divergence.
- **Observability + docs** (D-19, D-03 doc side): doctor D-19 row present and exit-code-neutral; RUNBOOK Local Development section covers setup + daily + resync + reset + drift + triage + backups + rotation; README Quickstart present; REQUIREMENTS.md traceability row + coverage 14/14.

One plan-layer deferral (Plan 05 Task 2 live dual-stream smoke) and one follow-up deferral (CR-01 dev-reset.sh PGHOST pin) are tracked but do not prevent goal achievement. Live preflight runs green 4/4 on this machine; the deferred smoke test is the natural next interactive dev session per Plan 05's documented user-approved path. The work_orders schema drift (pre-existing, not caused by this phase) blocks live seed completion but has an actionable path via a follow-on migration plan — the doctor row correctly surfaces this with `bash scripts/dev-setup.sh` as the displayed fix command.

**Status:** `human_needed` — NOT `passed`, because the live dual-stream smoke test (items 1 and 2 in Human Verification) can only be observed in an interactive session. All automated checks are green. If the user runs `npm run dev` and the dual-stream behavior matches expectations, the phase is effectively passed.

---

_Verified: 2026-04-21T19:15:00Z_
_Verifier: Claude (gsd-verifier)_
