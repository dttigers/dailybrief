---
phase: 107.1
plan: 06
subsystem: infra
tags:
  - infra
  - secrets
  - env-hygiene
  - anthropic
dependency-graph:
  requires:
    - 107.1-01  # .env.example 6-key LOCAL-ONLY template
    - 107.1-03  # preflight-check.sh Check 4; .env.bak.* gitignore patterns
    - 107.1-04  # plist retirement (makes plist-sync block obsolete in sync script)
    - 107.1-05  # concurrently orchestrator + tsx --env-file=.env watch
  provides:
    - LOCAL-ONLY vigil-core/.env (D-17 env hygiene closed)
    - Dev-workspace Anthropic key with $20/mo spend cap (D-18 blast radius bound)
    - sync-anthropic-key.sh no-longer-treats-local/prod-split-as-drift semantics (D-18 sync contract)
    - Preflight 4/4 green live — Plan 05 Task 2 deferred dual-stream smoke now unblocked
  affects:
    - Phase 107.1 Plan 07 (drift-doctor) — prerequisite that sync script no longer touches plist + recognizes split
tech-stack:
  added: []
  patterns:
    - "Secret never touches disk outside gitignored file: temp file → shell var → .env → unset + rm temp in single shell block"
    - "Default-off opt-in flag (--include-config-env) for cross-surface secret syncs that post-phase may legitimately diverge"
    - "Retirement tripwire: if [[ -f retired-path ]] warn-but-noop preserves resurrection signal without re-enabling deleted behavior"
key-files:
  created: []
  modified:
    - "vigil-core/.env (GITIGNORED — not tracked; 6-key LOCAL-ONLY shape)"
    - "vigil-core/.env.bak.20260421-184421 (GITIGNORED — pre-rewrite backup)"
    - "Scripts/sync-anthropic-key.sh (D-18 rewrite: default-off local .env sync, plist retirement notice)"
decisions:
  - "Accepted backup preserves prod creds on disk ~24h for rollback safety (threat register T-LOCAL-1 acceptance) — user deletes .env.bak.* after confirmed stable dev"
  - "No vigil-core/config.json exists — 'clean config.json of ANTHROPIC_API_KEY' instruction is moot; canonical prod key stays in ~/.config/dailybrief/config.json under ai.claude_api_key as the Mac-app / Railway source"
  - "JWT_SECRET generated via openssl rand -hex 32 → 64 hex chars (32 bytes entropy) exceeding plan's >=32 char floor by 2x"
metrics:
  duration: "2m 36s"
  completed: "2026-04-22T00:46:46Z"
  tasks_completed: 3
  files_modified: 1  # only Scripts/sync-anthropic-key.sh is git-tracked; vigil-core/.env + backup are gitignored
  commits: 1         # Task 1 human-action; Task 2 produced no committable artifacts (all gitignored)
---

# Phase 107.1 Plan 06: Env Hygiene + Anthropic Dev-Workspace Split Summary

LOCAL-ONLY `vigil-core/.env` rewritten from prod Railway pointing to localhost Postgres + dev-workspace Anthropic key ($20/mo cap); `sync-anthropic-key.sh` updated so local/prod key divergence is no longer flagged as drift.

## What Shipped

- **D-17 closed:** `vigil-core/.env` contains 6 keys, all LOCAL-ONLY (`DATABASE_URL=postgresql://localhost:5432/vigil_dev`, 64-hex `JWT_SECRET`, seed-user allowlist, dev-workspace Anthropic key, blank `POSTHOG_API_KEY`, `PORT=3001`). Zero Railway hostnames remain. Pre-rewrite 824-byte backup preserved at `vigil-core/.env.bak.20260421-184421` (gitignored via Plan 03's `.env.bak.*` pattern).
- **D-18 closed:** Anthropic dev workspace created with $20/mo spend cap; dev-workspace key (`sk-a...pAAA`, prefix `sk-ant-api03`, 108 chars) written into `vigil-core/.env` exclusively. Temp stash file `/tmp/vigil-dev-anthropic-key` deleted immediately after .env write.
- **sync-anthropic-key.sh updated:** default no longer touches `~/.config/dailybrief/.env` (new `--include-config-env` opt-in); plist-sync block replaced with retirement notice; step numbers renumbered 1/3,2/3,3/3 → 1/2,2/2; `--skip-launchagent` retained as backwards-compat no-op.
- **Live preflight smoke GREEN:** `scripts/preflight-check.sh` all 4 checks PASS, exit 0 — this is the deferred live smoke from Plan 05 Task 2, now unblocked.

## Anthropic Dev Workspace (D-18)

| Field | Value |
|---|---|
| Workspace name (human-chosen) | `Vigil Dev` |
| Monthly spend cap | $20 |
| Key name | `vigil-core-local-dev` (assumed per plan spec; human-owned detail) |
| Key prefix (first 12 chars) | `sk-ant-api03` |
| Key redacted | `sk-a...pAAA` (len=108) |
| Full key location | `vigil-core/.env` (gitignored) — user's password manager |
| Full key NOT in | commits, summaries, state files, terminal logs, shell history (used via `$KEY` var, immediately `unset`) |

## Verification Evidence

### Task 2 — `.env` rewrite (10/10 acceptance greps PASS)

```
DATABASE_URL localhost: PASS
key-count: 6 (expect 6)
no-prod-hostnames: PASS
JWT_SECRET >=32: PASS
ANTHROPIC_API_KEY sk-ant- prefix: PASS
POSTHOG_API_KEY blank: PASS
PORT=3001: PASS
VIGIL_ALLOWED_EMAILS: PASS
git-untracked: PASS
backup-exists: PASS
```

Structural audit (values redacted):
```
  DATABASE_URL              = postgresql://localhost:5432/vigil_dev
  JWT_SECRET                = (len=64 redacted)
  VIGIL_ALLOWED_EMAILS      = jamesonmorrill1@gmail.com
  ANTHROPIC_API_KEY         = sk-a...pAAA (len=108)
  POSTHOG_API_KEY           = (blank)
  PORT                      = 3001
```

### Task 3 — sync-anthropic-key.sh (9/9 static + 2/2 behavior PASS)

Static:
```
syntax: PASS
D-18 header: PASS
--include-config-env flag: PASS
default-off guard: PASS
plist retirement notice: PASS
--skip-launchagent no-op: PASS
--skip-launchagent kept: PASS
[1/2] count: 3 (expect >=1)
[2/2] count: 5 (expect >=1)
```

Behavior (live, --skip-railway invocations with config-key redacted):
```
# Default path (bare --skip-railway)
[1/2] skipped ~/.config/dailybrief/.env (Phase 107.1 D-18 — local dev key is EXPECTED to differ from prod)
      to force-sync anyway: ./scripts/sync-anthropic-key.sh --include-config-env
[2/2] skipped Railway (--skip-railway)

# Opt-in path (--skip-railway --include-config-env)
[1/2] synced .env             → /Users/jamesonmorrill/.config/dailybrief/.env (--include-config-env)
[2/2] skipped Railway (--skip-railway)
```

### Live preflight smoke (Plan 05 Task 2 deferred)

```
[preflight] Check 1: com.jamesonmorrill.vigilcore daemon not registered
  PASS — daemon not registered
[preflight] Check 2: port 3001 is free for vigil-core
  PASS — port 3001 is free
[preflight] Check 3: postgresql@16 is accepting connections on localhost
  PASS — Postgres is accepting connections
[preflight] Check 4: vigil-core/.env DATABASE_URL points at localhost
  PASS — DATABASE_URL points at local Postgres
preflight: all checks passed — dev environment is healthy
exit=0
```

This is the deferred live smoke from 107.1-05 Task 2 — 4/4 checks green against the fully-LOCAL `.env` that 107.1-06 just produced. Plan 05 Task 2 unblocked.

### `npm run dev` dual-stream smoke

**Not performed in this plan** — the live preflight PASS (above) is the deterministic precondition for `npm run dev` success. Per plan spec, `npm run dev` is run interactively by the user as the first real dev session; Plan 07's `verify-phase-107.1.sh` D4 test is the formal automated boot check.

## Deviations from Plan

### Rule 1 — Bug: plan's Step A gitignore update was already resolved upstream

**Found during:** Task 2 Step A
**Issue:** Plan required adding `.env.bak*` + `*.env.bak*` patterns to root `.gitignore` "as a separate small commit". Live check showed all four patterns (`.env`, `*.env`, `.env.bak.*`, `*.env.bak.*`) already present — added by Plan 107.1-03.
**Fix:** Skipped the gitignore commit (nothing to change); verified all four patterns match + backup file is gitignored before writing the backup.
**Files modified:** none
**Commit:** none (skip — no diff)

### Rule 3 — Scope: Task 2 produced no committable artifacts (by design)

**Found during:** Task 2 final commit step
**Issue:** Per `task_commit_protocol` each task normally commits. Task 2 modifies `vigil-core/.env` (gitignored) and creates a backup (gitignored). There is nothing to stage.
**Fix:** Did not force a metadata-only commit. Task 2 is accounted for in SUMMARY.md + preflight live evidence.
**Files modified:** (gitignored: `vigil-core/.env`, `vigil-core/.env.bak.20260421-184421`)
**Commit:** none (all Task 2 artifacts are gitignored — no ephemeral work-tree additions)

### Rule 3 — Instruction reconciliation: `vigil-core/config.json` does not exist

**Found during:** Task 2 pre-write scan
**Issue:** Orchestrator prompt said "Clean vigil-core/config.json: remove any ANTHROPIC_API_KEY field per D-18". No such file exists in the repo. The canonical Mac-app config lives at `~/.config/dailybrief/config.json` under `ai.claude_api_key` — it IS the prod source-of-truth that `sync-anthropic-key.sh` reads to push into Railway. Removing it would break the prod sync path.
**Fix:** Left `~/.config/dailybrief/config.json` untouched (it is the canonical prod source, not a duplicate to clean). D-18's "single source is .env now" intent applies to the LOCAL key only — the local `.env` is the single source for the dev-workspace key; the user-level config.json remains the single source for the prod key.
**Files modified:** none
**Commit:** none

## Authentication / Secret Handling

No auth gates failed. Secret handling discipline observed:

- Temp key file `/tmp/vigil-dev-anthropic-key` (mode 600, 108 bytes) read via `tr -d '\n\r' < file` into `$KEY` shell var
- Guard against wrong-file paste: `case "$KEY" in sk-ant-*) : ;; *) abort ;;` — passed (`sk-ant-api03` prefix matched)
- `JWT_SECRET` generated via `openssl rand -hex 32` (64 hex chars) into `$JWT` shell var, never logged
- `.env` written via `printf '%s\n' ... > vigil-core/.env` (no `echo`, no here-doc persistence)
- `chmod 600 vigil-core/.env` set
- `unset KEY JWT` + `rm -f /tmp/vigil-dev-anthropic-key` in the same shell block that wrote .env
- SUMMARY + commit message redact to `sk-a...pAAA` (first-4 + last-4), never full key
- Verification outputs piped through `sed 's/sk-ant-[A-Za-z0-9_-]*/sk-ant-REDACTED/g'` before echoing
- `git ls-files vigil-core/.env vigil-core/.env.bak.*` confirmed returns empty (0 tracked)

## Known Stubs

None. All six `.env` keys have live values. No placeholder strings remain.

## Threat Flags

No new threat surface beyond what's documented in the plan's `<threat_model>`. T-LOCAL-1 and T-LOCAL-5 both closed per plan's mitigation plan.

## Follow-ups

- **User: delete `vigil-core/.env.bak.20260421-184421` after ~24h of stable local dev use.** File is gitignored (no commit risk) but holds the pre-rewrite prod DATABASE_URL + prod Anthropic key — rollback safety window only.
- **Plan 07 assumes this plan shipped:** `dailybrief-doctor.sh` drift checks will now be aligned against a `sync-anthropic-key.sh` that no longer writes to the plist and no longer flags the local/prod Anthropic split.
- **`npm run dev` dual-stream live smoke** is the user's first "real dev session" — expected to green based on preflight 4/4 PASS + Plan 05 tsx --env-file=.env fix.

## Self-Check: PASSED

- Files claimed:
  - `vigil-core/.env` — FOUND (745 bytes, mode 600, gitignored, 6 keys, no Railway hostnames)
  - `vigil-core/.env.bak.20260421-184421` — FOUND (824 bytes, gitignored)
  - `Scripts/sync-anthropic-key.sh` (canonical git casing) — MODIFIED (47 insertions, 39 deletions)
- Commits claimed:
  - `0516127` (chore(107.1-06): update sync-anthropic-key.sh for D-18 local/prod split) — FOUND via `git log --oneline -5`
- Temp file claimed deleted:
  - `/tmp/vigil-dev-anthropic-key` — CONFIRMED DELETED
- Live preflight:
  - `bash scripts/preflight-check.sh` — exit 0, 4/4 PASS
