---
phase: 107
plan: 01
type: execute
wave: 1
depends_on: [107-00]
files_modified:
  - vigil-safari-extension/Vigil Capture/Info.plist
autonomous: true
requirements: [EXT-01]
must_haves:
  truths:
    - "Container app launches in accessory mode (no Dock icon, no menu bar, no window flash)"
    - "plutil confirms LSUIElement key is present and boolean true"
  artifacts:
    - path: "vigil-safari-extension/Vigil Capture/Info.plist"
      provides: "LSUIElement=true accessory-mode declaration for the Safari container app"
      contains: "<key>LSUIElement</key>"
  key_links:
    - from: "Info.plist"
      to: "AppKit activation policy"
      via: "LSUIElement key declaration"
      pattern: "LSUIElement.*true"
---

<objective>
Flip the Vigil Capture container app to accessory-mode activation policy by adding `<key>LSUIElement</key><true/>` to `vigil-safari-extension/Vigil Capture/Info.plist`. No window on launch, no Dock icon, no menu bar — ever.

Purpose: D-01 prerequisite for Plan 02 (AppDelegate register() call runs silently at login) and Plan 03 (status pill renders only when user explicitly opens prefs from Safari). Per Pitfall 2 in 107-RESEARCH.md, the storyboard + ViewController instantiation continue to work — the window is merely not visible on launch.

Output: One Info.plist file with two new lines. Zero Swift code changes. Zero project.pbxproj changes (project-level MACOSX_DEPLOYMENT_TARGET = 15.7 already satisfies the macOS 13+ SMAppService requirement — verified via grep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-safari-extension-persistence/107-CONTEXT.md
@.planning/phases/107-safari-extension-persistence/107-RESEARCH.md
@vigil-safari-extension/Vigil Capture/Info.plist
@scripts/verify-phase-107.sh

<interfaces>
<!-- Current Info.plist state (before this plan) -->
Path: vigil-safari-extension/Vigil Capture/Info.plist
Current full contents (8 lines):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>SFSafariWebExtensionConverterVersion</key>
    <string>26.3</string>
</dict>
</plist>
```

<!-- Other Info.plist keys are synthesized at build time from project.pbxproj settings: -->
<!-- INFOPLIST_KEY_CFBundleDisplayName = "Vigil Capture" -->
<!-- INFOPLIST_KEY_NSMainStoryboardFile = Main -->
<!-- INFOPLIST_KEY_NSPrincipalClass = NSApplication -->
<!-- (from project.pbxproj L511-515 container Debug, L553-556 container Release) -->
<!-- GENERATE_INFOPLIST_FILE = YES merges the literal file with synthesized keys. -->

<!-- Deployment target check (already satisfied — no edit needed) -->
Project-level (project.pbxproj L365, L422): MACOSX_DEPLOYMENT_TARGET = 15.7
Extension target override (L447, L481): MACOSX_DEPLOYMENT_TARGET = 10.14 (extension target does NOT call SMAppService — 10.14 is fine)
Container target: NO override, inherits project-level 15.7 (>= 13.0 required for SMAppService.mainApp) ✓
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add LSUIElement=true to container Info.plist</name>
  <files>vigil-safari-extension/Vigil Capture/Info.plist</files>
  <read_first>
    - vigil-safari-extension/Vigil Capture/Info.plist (full current contents — only 8 lines; confirm only one existing key is SFSafariWebExtensionConverterVersion)
    - .planning/phases/107-safari-extension-persistence/107-RESEARCH.md (§"Pattern 2: LSUIElement Info.plist edit (D-01)" — canonical XML snippet; §"Pitfall 2" — storyboard coexistence; §"Alternatives Considered" — why edit the literal file vs. INFOPLIST_KEY build setting)
    - .planning/phases/107-safari-extension-persistence/107-CONTEXT.md (D-01 full text)
    - vigil-safari-extension/Vigil Capture.xcodeproj/project.pbxproj L498-568 (container target config — confirm GENERATE_INFOPLIST_FILE=YES and INFOPLIST_KEY_NSMainStoryboardFile=Main so executor understands the storyboard is still wired even with LSUIElement)
  </read_first>
  <action>
Modify `vigil-safari-extension/Vigil Capture/Info.plist` to add the `LSUIElement` boolean true key inside the existing `<dict>`. Use the Edit tool to insert exactly two new lines after the existing `<string>26.3</string>` line, before the closing `</dict>`.

Final file contents MUST be exactly this (10 lines total):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>SFSafariWebExtensionConverterVersion</key>
    <string>26.3</string>
    <key>LSUIElement</key>
    <true/>
</dict>
</plist>
```

Preserve the existing tab indentation (4 spaces shown in the current file — verify with `cat -A` if unsure; current indentation uses literal tabs per Xcode default, but the file above uses 4 spaces to match what Read returns. Use whatever the current file uses consistently).

DO NOT:
- Add additional keys (no CFBundleDisplayName, no NSMainStoryboardFile — these are synthesized by GENERATE_INFOPLIST_FILE=YES, see project.pbxproj L511-515)
- Edit project.pbxproj INFOPLIST_KEY_LSUIElement — research chose the literal Info.plist edit path for diff visibility (see RESEARCH §Alternatives Considered)
- Add a .entitlements file — not needed for LSUIElement (per D-deferred and RESEARCH Pitfall 5)
- Change any SafariWebExtension-related keys

After the edit, verify with: `plutil -lint "vigil-safari-extension/Vigil Capture/Info.plist"` (must return "OK") and `plutil -extract LSUIElement raw "vigil-safari-extension/Vigil Capture/Info.plist"` (must print `true`).

Per Pitfall 2 in RESEARCH: DO NOT delete Main.storyboard. DO NOT remove INFOPLIST_KEY_NSMainStoryboardFile from project.pbxproj. The storyboard continues to instantiate ViewController (which Plan 03 extends); LSUIElement only suppresses the visible window at launch — the WKWebView bridge still works when the user later clicks "Open preferences" from Safari.
  </action>
  <verify>
    <automated>plutil -lint "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture/Info.plist" &amp;&amp; plutil -extract LSUIElement raw "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture/Info.plist" | grep -q '^true$'</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint "vigil-safari-extension/Vigil Capture/Info.plist"` outputs "OK" with exit 0
    - `plutil -extract LSUIElement raw "vigil-safari-extension/Vigil Capture/Info.plist"` stdout is exactly `true` (with trailing newline) and exit 0
    - `plutil -extract SFSafariWebExtensionConverterVersion raw "vigil-safari-extension/Vigil Capture/Info.plist"` still outputs `26.3` (existing key preserved)
    - `grep -c '<key>' "vigil-safari-extension/Vigil Capture/Info.plist"` returns exactly 2 (SFSafariWebExtensionConverterVersion + LSUIElement)
    - `wc -l "vigil-safari-extension/Vigil Capture/Info.plist"` returns 10 (± 1 for trailing newline conventions)
    - Running `bash scripts/verify-phase-107.sh --static` now passes Check 1 (LSUIElement) and Check 2 (deployment target); Checks 3 and 4 still fail (AppDelegate not yet modified — that's Plan 02)
    - `xcodebuild build -project "vigil-safari-extension/Vigil Capture.xcodeproj" -scheme "Vigil Capture" -configuration Debug -quiet` succeeds (Info.plist change is non-breaking)
    - No changes to project.pbxproj (confirm via `git diff --stat vigil-safari-extension/Vigil\ Capture.xcodeproj/project.pbxproj` returns empty)
    - No .entitlements file created (`find vigil-safari-extension -name '*.entitlements'` returns empty)
  </acceptance_criteria>
  <done>
    LSUIElement=true lands in Info.plist. plutil validates and extracts `true`. Project builds clean. Static verify script Checks 1 and 2 pass; 3 and 4 remain failing pending Plan 02. No collateral changes to project.pbxproj or other files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Info.plist → AppKit launch | Declarative Info.plist key read by AppKit at process start; no user input, no network, no IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01-01 | Elevation | LSUIElement | accept | LSUIElement cannot be exploited directly — it is a UI activation hint, not a privilege boundary. Accessory-mode apps can still show NSAlert (Plan 02 uses this); no privilege escalation path exists. Explicitly called out as low-severity in security_requirements block for this phase. |
| T-107-01-02 | Tampering | Info.plist | accept | File is version-controlled; changes require code review via git commit. Same trust posture as every other plist in the repo. |
| T-107-01-03 | Info Disclosure | LSUIElement=true as public fact | accept | Accessory-mode status is observable via `ps -axco command,state` or Activity Monitor regardless of plist content. Zero disclosure risk. |

No high-severity threats for this plan. LSUIElement is a well-documented UX primitive in the same class as CFBundleDisplayName. ASVS L1 compliant.
</threat_model>

<verification>
- `plutil -lint` on Info.plist returns OK
- `plutil -extract LSUIElement raw` prints `true`
- `xcodebuild build` succeeds
- Static verify script's LSUIElement check passes
- No diff in project.pbxproj
</verification>

<success_criteria>
- Info.plist contains `<key>LSUIElement</key>` immediately followed by `<true/>`
- Both existing key (SFSafariWebExtensionConverterVersion) and new key (LSUIElement) validate via plutil
- Xcode Debug build succeeds (no broken references)
- scripts/verify-phase-107.sh --static advances Check 1 to green
</success_criteria>

<output>
After completion, create `.planning/phases/107-safari-extension-persistence/107-01-SUMMARY.md` capturing: exact Info.plist diff, plutil output, any xcodebuild warnings observed, remaining verify-phase-107 check state (1+2 green, 3+4 red pending Plan 02, 5+6 unreachable pending Plan 04).
</output>
