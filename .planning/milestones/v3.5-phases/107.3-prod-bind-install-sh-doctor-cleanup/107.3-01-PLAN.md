---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 01
type: execute
wave: 2
depends_on: ["107.3-04"]
files_modified:
  - vigil-core/src/index.ts
autonomous: true

must_haves:
  truths:
    - "vigil-core deployed to Railway auto-binds to 0.0.0.0 when RAILWAY_SERVICE_ID is present in the runtime environment — the Railway proxy can reach the container"
    - "vigil-core run locally (no RAILWAY_SERVICE_ID, no VIGIL_BIND_HOST override) still binds to 127.0.0.1 — security bias unchanged for dev"
    - "Explicit VIGIL_BIND_HOST in .env still wins (local dev can force 0.0.0.0 for Tailscale exposure)"
    - "https://api.vigilhub.io/v1/health returns HTTP 200 after Railway auto-redeploys this commit"
    - "Running Scripts/verify-phase-107.sh --external (landed by Plan 107.3-04) returns PASS after deploy propagation"
  artifacts:
    - path: "vigil-core/src/index.ts"
      provides: "HTTP server with Railway-aware bind-host detection via RAILWAY_SERVICE_ID presence check"
      contains: "RAILWAY_SERVICE_ID"
      min_lines: 190
  key_links:
    - from: "vigil-core/src/index.ts hostname resolution (lines 180-190 area)"
      to: "process.env.RAILWAY_SERVICE_ID (Railway-injected UUID)"
      via: "!!process.env.RAILWAY_SERVICE_ID presence check"
      pattern: "RAILWAY_SERVICE_ID"
    - from: "vigil-core deployed container on Railway"
      to: "Railway ingress proxy → https://api.vigilhub.io/v1/health"
      via: "0.0.0.0 bind allowing external proxy reach"
      pattern: "HTTP 200 from api.vigilhub.io/v1/health post-deploy"
---

<objective>
Fix the vigil-core production bind-host default that caused the 502 "Application failed to respond" outage on api.vigilhub.io (2026-04-22). Current code at `vigil-core/src/index.ts:186` defaults `VIGIL_BIND_HOST` to `127.0.0.1`, and the comment at lines 183-184 incorrectly claims Railway's proxy can reach a loopback-bound container — it cannot. Manual Railway env-var patch (`VIGIL_BIND_HOST=0.0.0.0`) restored prod; this plan makes the fix durable in code so a fresh deploy (or a Railway env-var wipe) does not reintroduce the outage.

Purpose: Make Railway detection infrastructure-driven (not user-convention-driven), preserve the local-dev 127.0.0.1 security default, and verify the fix end-to-end against live prod via the new `--external` probe (landed by Plan 107.3-04).

Output: `vigil-core/src/index.ts` updated with `isRailway` detection via `RAILWAY_SERVICE_ID` presence; the misleading comment replaced; post-deploy verification confirms HTTP 200 on api.vigilhub.io/v1/health.
</objective>

<execution_context>
@/Users/jamesonmorrill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesonmorrill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-RESEARCH.md
@vigil-core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bind-host default with Railway-aware detection via RAILWAY_SERVICE_ID</name>
  <files>vigil-core/src/index.ts</files>
  <action>
Open `vigil-core/src/index.ts` and locate lines 180-190. First VERIFY the block exists unchanged by running:
  sed -n '180,190p' vigil-core/src/index.ts

EXACT OLD BLOCK (lines 180-190 inclusive, copy verbatim — must match):
```
const port = Number(process.env.PORT) || 3001;
// Phase 107.2 D-B1/D-B2 — env-gated bind hostname. Default 127.0.0.1 (safe:
// localhost-only). Local dev .env sets VIGIL_BIND_HOST=0.0.0.0 to expose on
// Tailscale/LAN. Railway prod leaves VIGIL_BIND_HOST unset → defaults to
// 127.0.0.1 behind Railway's proxy. Literal IPv4 (not 'localhost') avoids
// the macOS/Linux IPv6 resolution quirk documented in Pitfall 1.
const hostname = process.env.VIGIL_BIND_HOST ?? "127.0.0.1";

serve({ fetch: app.fetch, port, hostname }, () => {
  console.log(`Vigil Core API running on ${hostname}:${port}`);
});
```

EXACT NEW BLOCK (replace with — matches RESEARCH.md Code Examples "Fix 1" verbatim + the serve() call unchanged):
```
const port = Number(process.env.PORT) || 3001;
// Phase 107.3 Fix 1: Bind host strategy.
// Railway injects RAILWAY_SERVICE_ID (UUID) into every container; it is never
// set locally. When running on Railway, force 0.0.0.0 so the proxy (external
// to the container) can reach us. Default 127.0.0.1 for all non-Railway envs
// (safe: localhost-only). Explicit VIGIL_BIND_HOST always wins so local dev
// can override to 0.0.0.0 for Tailscale exposure.
// Literal IPv4 (not 'localhost') avoids the macOS/Linux IPv6 resolution quirk
// documented in Phase 107.2-01 Pitfall 1.
// Prior claim ("Railway prod leaves VIGIL_BIND_HOST unset → defaults to
// 127.0.0.1 behind Railway's proxy") was wrong and caused the 2026-04-22
// 502 outage on api.vigilhub.io — the proxy cannot reach a loopback-bound
// container. Verified via Railway docs
// (https://docs.railway.com/reference/variables) that RAILWAY_SERVICE_ID
// is always present in container runtime and never set locally.
const isRailway = !!process.env.RAILWAY_SERVICE_ID;
const hostname = process.env.VIGIL_BIND_HOST ?? (isRailway ? "0.0.0.0" : "127.0.0.1");

serve({ fetch: app.fetch, port, hostname }, () => {
  console.log(`Vigil Core API running on ${hostname}:${port}`);
});
```

LOCKED decisions (do NOT relitigate — enforced by RESEARCH):
- Detection var is `RAILWAY_SERVICE_ID` (NOT `RAILWAY_ENVIRONMENT` which doesn't exist, NOT `RAILWAY_ENVIRONMENT_NAME` which is user-named-string-dependent)
- Presence check `!!process.env.RAILWAY_SERVICE_ID` — presence alone, no string comparison
- Local default remains `127.0.0.1` (security bias preserved from Phase 107.2)
- Explicit `VIGIL_BIND_HOST` override still wins (dev can still do Tailscale)

DO NOT change:
- Anything above line 180 (imports, routes, error handlers, etc.)
- Anything below line 190 (generate scheduler, etc.)
- The `serve()` call signature or console.log message
  </action>
  <verify>
1. Syntax + type check:
   (cd vigil-core && npx tsc --noEmit)
   Expected: no errors.

2. Build still works:
   (cd vigil-core && npm run build 2>&1 | tail -20)
   Expected: clean build, emits dist/index.js.

3. Local-default behavior preserved — launch vigil-core locally WITHOUT RAILWAY_SERVICE_ID and WITHOUT VIGIL_BIND_HOST:
   (cd vigil-core && (unset RAILWAY_SERVICE_ID VIGIL_BIND_HOST; timeout 3 npm run dev 2>&1) | head -20)
   Expected: log line `Vigil Core API running on 127.0.0.1:3001` (or on whatever PORT is).

4. Railway simulation — set RAILWAY_SERVICE_ID and confirm bind flips to 0.0.0.0:
   (cd vigil-core && (unset VIGIL_BIND_HOST; RAILWAY_SERVICE_ID=fake-uuid-test timeout 3 npm run dev 2>&1) | head -20)
   Expected: log line `Vigil Core API running on 0.0.0.0:3001`.

5. Explicit override still wins — VIGIL_BIND_HOST explicit value beats the Railway detection:
   (cd vigil-core && VIGIL_BIND_HOST=192.168.1.1 RAILWAY_SERVICE_ID=fake timeout 3 npm run dev 2>&1 | head -20)
   Expected: log line `Vigil Core API running on 192.168.1.1:3001`.
   (EADDRINUSE or EACCES after the log line is fine — we only need the log line to prove the hostname resolution.)

6. Git diff is scoped:
   git diff vigil-core/src/index.ts
   Expected: only lines 180-190 area changed; no unrelated edits.
  </verify>
  <done>
- Type-check passes
- Build passes
- Three local simulation cases match expectations (127.0.0.1 / 0.0.0.0 / explicit override)
- Diff is confined to lines 180-190
- Ready to commit and push (push-hook will auto-deploy to Railway per workflow.deploy_targets=["vigil-core"])
  </done>
</task>

<task type="auto">
  <name>Task 2: Post-deploy verification via --external probe after Railway auto-redeploys</name>
  <files>(no files modified — this task runs verification commands)</files>
  <action>
After Task 1 commits and pushes, the Phase 56 push-hook auto-pushes to origin → Railway auto-redeploys vigil-core. This task verifies the deploy actually restored prod reachability.

CRITICAL CONTEXT: This is the exact scenario the phase exists to prevent. Phase 107.2-01 shipped with the broken default because its local `NODE_ENV=production` probe never tested the Railway proxy → container path. We will NOT make the same mistake here.

Sequence:
1. Confirm the commit from Task 1 is pushed to origin. Run:
   git log --oneline -1 origin/main -- vigil-core/src/index.ts 2>/dev/null || git log --oneline -1 origin/main
   If the latest commit touching `vigil-core/src/index.ts` (or the top origin/main commit) is NOT the Task 1 commit, run:
   git push origin main
   (The Phase 56 push-hook should have done this automatically — run explicitly if not.)

2. Wait for Railway to propagate the deploy. Railway auto-deploys take ~60-120 seconds from push to serving new code.
   Command:
     echo "Waiting 90s for Railway redeploy..."; sleep 90

3. Confirm Railway is serving new code. Run the --external probe (landed by Plan 107.3-04):
   bash Scripts/verify-phase-107.sh --external

   Expected output: `PASS — api.vigilhub.io/v1/health returned HTTP 200`
   Expected exit: 0.

4. If the probe FAILS with non-200, the deploy may still be propagating, or something else is wrong. Retry logic:
   for i in 1 2 3; do
     if bash Scripts/verify-phase-107.sh --external; then
       echo "External probe PASS on attempt $i"; break
     fi
     echo "Attempt $i failed — waiting 30s..."; sleep 30
   done

   If all 3 attempts fail, this is a real failure — Railway may still have the manual `VIGIL_BIND_HOST=0.0.0.0` env var override from the 2026-04-22 manual fix, which means our code-level fix is not actually being tested. In that case:
     - Inspect Railway env vars: `(cd vigil-core && railway variables 2>&1 | grep VIGIL_BIND_HOST)` (requires railway CLI logged in)
     - If `VIGIL_BIND_HOST=0.0.0.0` is set manually in Railway, REMOVE it so the new code default takes over:
         (cd vigil-core && railway variables --unset VIGIL_BIND_HOST) — ONLY do this if the user authorizes (safety.always_confirm_external_services=true). Output a warning, do NOT auto-unset.
     - Record the finding in the SUMMARY regardless.

5. Capture the live response body for the summary:
   curl -s --max-time 10 https://api.vigilhub.io/v1/health
   Expected shape: `{"status":"ok","timestamp":"...","version":"0.1.0","database":"connected"}`
  </action>
  <verify>
- `bash Scripts/verify-phase-107.sh --external` returns exit 0 (PASS) after deploy propagation
- `curl https://api.vigilhub.io/v1/health` returns HTTP 200 with a valid JSON body containing `"status":"ok"` and `"database":"connected"`
- Railway redeploy visibly reflects the new code (check Railway dashboard or `railway logs` if available)
  </verify>
  <done>
- External probe PASS captured
- Live /v1/health response body recorded in summary
- If manual Railway env-var override was detected, its removal (or decision to keep as belt-and-suspenders) is logged in summary with user rationale
  </done>
</task>

</tasks>

<verification>
This phase exists because Phase 107.2-01's local-only probe missed a real prod outage. The phase goal is NOT satisfied by a green build — it is satisfied only when the external HTTP probe against https://api.vigilhub.io/v1/health returns 200 AFTER Railway has redeployed this plan's commit.

End-to-end chain:
  Task 1 edit → commit → push-hook → origin → Railway auto-deploy → ~90s propagation → Task 2 `--external` probe PASS

If Task 2 probe fails, this plan is NOT complete even if Task 1's code edit was correct. The whole point of the phase is that a green local probe is not sufficient evidence.
</verification>

<success_criteria>
- `vigil-core/src/index.ts` detects Railway via `!!process.env.RAILWAY_SERVICE_ID`
- Comment block at lines 181-188 (new) explains the detection, cites Railway docs URL, references the 2026-04-22 outage that motivated the fix
- Local dev (no RAILWAY_SERVICE_ID, no VIGIL_BIND_HOST) still binds 127.0.0.1
- Explicit VIGIL_BIND_HOST override still wins
- After Railway auto-redeploys, `Scripts/verify-phase-107.sh --external` returns HTTP 200 (exit 0)
- Summary captures the live /v1/health response body as evidence
</success_criteria>

<output>
After completion, create `.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-01-SUMMARY.md` using the summary template. Key fields:
- subsystem: vigil-core/http-server
- tech-stack.added: (none — uses existing process.env)
- affects: vigil-core/src/index.ts, api.vigilhub.io deployment
- decisions:
  - Detect Railway via `RAILWAY_SERVICE_ID` presence (LOCKED by RESEARCH — NOT RAILWAY_ENVIRONMENT, NOT NODE_ENV)
  - Local default stays 127.0.0.1 (security bias from Phase 107.2 preserved)
  - Explicit VIGIL_BIND_HOST override still wins
- patterns: Infrastructure-injected env var presence check > user-convention-named env var value comparison
- Post-deploy evidence: paste `curl https://api.vigilhub.io/v1/health` output captured in Task 2.
- Mark traceability: closes the "external probe" pitfall from RESEARCH Pitfall 1.
</output>
