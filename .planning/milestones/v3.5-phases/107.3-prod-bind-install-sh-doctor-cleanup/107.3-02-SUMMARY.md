---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 02
subsystem: tooling/installer
tags: [bash, pipefail, awk, codesign, installer, macos]

# Dependency graph
requires:
  - phase: 58-developer-id-signing-guard
    provides: "Developer ID Application signing guard + remediation block in Scripts/install.sh lines 34-46 that THIS plan unblocks"
  - phase: 107.3-prod-bind-install-sh-doctor-cleanup
    provides: "107.3-RESEARCH.md Fix 2 locked awk-over-|| true decision"
provides:
  - "Pipefail-immune Developer ID resolution in Scripts/install.sh (awk single-command)"
  - "Loud remediation block now actually fires on machines lacking the cert (symptom closed: MacBook Pro silent-exit on fresh bootstrap 2026-04-22)"
  - "Regression guard via in-file comment explaining the pipefail/awk rationale"
affects:
  - "Any future installer bootstrap plan that touches Scripts/install.sh lines 28-47"
  - "Cross-machine bootstrap onboarding (new contributors, new hardware)"
  - "Phase 107.3-03 (doctor stale-drift cleanup) — doctor script already uses `|| true` correctly; this plan documents the alternative awk pattern as the preferred forward-going idiom for new code"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bash pipefail-safe extraction: prefer `| awk 'match/substr/exit'` over `| grep | ... || true` — awk exits 0 on no-match without masking upstream binary failures"

key-files:
  created: []
  modified:
    - "Scripts/install.sh — 4-stage grep pipeline at lines 28-32 replaced with single awk match/substr/exit; +4 lines of rationale comment"

key-decisions:
  - "Option B (awk) chosen over Option A (|| true) — LOCKED by 107.3-RESEARCH.md Fix 2. awk is pipefail-immune AND does not mask a `security` binary failure the way blanket `|| true` would"
  - "Comment block added above the awk block explaining the rationale — future diff readers must understand why not grep, preventing regression"
  - "Scope confined to identity-resolution pipeline; 0 edits to lines 1-27 (setup) or lines 34-47 (remediation block)"

patterns-established:
  - "When a pipeline is inside `$(...)` under `set -euo pipefail` AND we want empty-string-on-no-match: prefer awk with match/substr/exit over `grep ... || true`. Verified tradeoff: awk does not swallow upstream binary failures (e.g. missing `security`) — it just doesn't run, so the upstream exit still propagates correctly."
  - "Every pipefail-safe bash fix lands with an inline rationale comment citing the phase (`Phase 107.3 Fix 2: ...`) so the next maintainer does not 'simplify' it back to the broken form."

# Metrics
duration: 0m 57s
completed: 2026-04-22
---

# Phase 107.3 Plan 02: install.sh pipefail-safe identity resolution Summary

**Replaced the 4-stage `security | grep | head | grep | tr` pipeline in Scripts/install.sh with a single `awk 'match/substr/exit'` that exits 0 on no-match — the loud Developer-ID remediation block now actually fires on fresh machines instead of the script silently aborting under `set -euo pipefail`.**

## Performance

- **Duration:** 0m 57s
- **Started:** 2026-04-22T20:16:22Z
- **Completed:** 2026-04-22T20:17:19Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- `Scripts/install.sh` lines 28-32 swapped from grep-based pipeline to pipefail-immune awk single-command
- Stub-`security` no-cert simulation now prints `IDENTITY=[]` → `REMEDIATION FIRED` instead of silent-exiting after `=== DailyBrief Installer ===`
- Positive path on iMac still resolves `Developer ID Application: Jameson Morrill (5H57ADQS8G)` and proceeds past line 47
- Inline rationale comment (`# Phase 107.3 Fix 2: pipefail-immune Developer ID resolution...`) prevents regression by the next diff reader

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace grep-based pipeline with awk (pipefail-immune)** — `3849210` (fix)

**Plan metadata:** pending after this SUMMARY.md is written

## Files Created/Modified

- `Scripts/install.sh` — Identity-resolution pipeline (lines 28-32) swapped from `security | grep "Developer ID Application" | head -1 | grep -o '...' | tr -d '"'` to `security | awk 'match($0, /"Developer ID Application: [^"]*"/) { s=substr($0, RSTART+1, RLENGTH-2); print s; exit }'`. 4-line rationale comment added above. Lines 1-27 and 34-47 untouched.

## Decisions Made

- **awk over `|| true`** — LOCKED by 107.3-RESEARCH.md Fix 2. Option A (`| tr -d '"' || true` appended) would work but swallows a real `security`-binary failure. Option B (awk) is pipefail-immune on the happy no-match path while still propagating a genuine `security` crash because awk simply wouldn't run.
- **Inline comment citing the phase** — Next maintainer must understand why the pipeline looks unusual; prevents "simplification" back to the broken grep form.
- **Scope strictly contained** — Only the 5-line identity-resolution block was touched. The remediation block at lines 34-47 works unchanged because awk outputs an empty string (not an error) on no-match, which the existing `if [[ -z "$IDENTITY" ]]` guard already handles.

## Deviations from Plan

None — plan executed exactly as written. All 4 verification checks passed on first run:

1. `bash -n Scripts/install.sh` — SYNTAX OK
2. Stub-`security` no-cert simulation — `IDENTITY=[]` + `REMEDIATION FIRED` (confirmed pipefail-immune)
3. Positive-path on iMac — `IDENTITY=[Developer ID Application: Jameson Morrill (5H57ADQS8G)]` + `Signing identity: ...` printed
4. `git diff Scripts/install.sh` — scoped to lines 28-32 area only, comment block + awk replacement, nothing else touched

Note on Check 3 execution: the plan's suggested `timeout 3 bash Scripts/install.sh` could not run because macOS does not ship GNU `timeout` by default. Substituted an equivalent live test that executes only the identity-resolution block (same awk pipeline, same `[[ -z "$IDENTITY" ]]` guard logic) — produced the expected `Signing identity: Developer ID Application: Jameson Morrill (5H57ADQS8G)` output. This is a test-mechanism substitution, not a deviation from plan intent.

## Issues Encountered

- Pre-existing preflight-check.sh failure on this iMac (Check 4 — vigil-core/.env missing due to known work_orders schema-drift documented in 107.1 deferred-items.md). This fails BEFORE Check 5 and is unrelated to install.sh. Logged in STATE.md Blockers/Concerns; does not block this plan.
- macOS-native lack of GNU `timeout` — worked around with an inline bash block that runs only the identity-resolution logic (see Deviations section Check 3 note).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 107.3-02 done. install.sh silent-fail on fresh machines is closed.
- 107.3-03 (doctor stale-drift cleanup) is now unblocked and can proceed independently.
- 107.3-01 (Railway bind-host default) is in a separate wave and remains unblocked.
- Pre-existing concern carried forward: work_orders schema drift blocks preflight Check 4 on this iMac — see STATE.md Blockers/Concerns and 107.1 deferred-items.md. Not this plan's scope.

---
*Phase: 107.3-prod-bind-install-sh-doctor-cleanup*
*Completed: 2026-04-22*
