---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/install.sh
autonomous: true

must_haves:
  truths:
    - "Running Scripts/install.sh on a machine with NO Developer ID Application cert prints the remediation block to stderr and exits 1 (instead of silently exiting after '=== DailyBrief Installer ===')"
    - "Running Scripts/install.sh on a machine WITH a valid Developer ID Application cert resolves the identity and proceeds past line 47"
    - "The identity-resolution pipeline no longer trips set -euo pipefail when grep finds no match"
  artifacts:
    - path: "Scripts/install.sh"
      provides: "Installer entrypoint with pipefail-safe Developer ID resolution"
      contains: "awk 'match"
  key_links:
    - from: "Scripts/install.sh identity-resolution pipeline (lines 28-32)"
      to: "if [[ -z \"$IDENTITY\" ]] guard (line 34)"
      via: "awk match-or-empty-string (pipefail-immune)"
      pattern: "IDENTITY=\\$\\(security find-identity.*awk 'match"
---

<objective>
Fix the install.sh silent-fail when no Developer ID Application certificate is present. The current pipeline at lines 28-32 runs `security find-identity | grep ... | head -1 | grep -o ... | tr -d '"'` under `set -euo pipefail`. When the first grep finds no match it exits 1, pipefail propagates, the script aborts BEFORE reaching the `if [[ -z "$IDENTITY" ]]` remediation block at line 34. The user sees only `=== DailyBrief Installer ===` and silent exit.

Purpose: Ensure the loud remediation message ("ERROR: No Developer ID Application certificate found in login keychain.") actually fires on a fresh machine that lacks the cert — observed symptom on MacBook Pro cross-machine bootstrap 2026-04-22.

Output: `Scripts/install.sh` updated with a pipefail-immune single-awk replacement for the identity-resolution pipeline. The existing `if [[ -z "$IDENTITY" ]]` guard works unchanged.
</objective>

<execution_context>
@/Users/jamesonmorrill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesonmorrill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-RESEARCH.md
@Scripts/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace grep-based pipeline with awk (pipefail-immune)</name>
  <files>Scripts/install.sh</files>
  <action>
Open `Scripts/install.sh` and replace lines 28-32 (the current IDENTITY assignment) with the awk-based single-command replacement from RESEARCH.md Code Examples — Fix 2 (Option B, recommended).

EXACT OLD BLOCK (lines 28-32, copy verbatim):
```
IDENTITY=$(security find-identity -v -p codesigning \
           | grep "Developer ID Application" \
           | head -1 \
           | grep -o '"Developer ID Application: [^"]*"' \
           | tr -d '"')
```

EXACT NEW BLOCK (replace with):
```
# Phase 107.3 Fix 2: pipefail-immune Developer ID resolution. awk returns exit 0
# even when no match is found (grep returns 1 on no-match, which pipefail would
# propagate and abort the script before the [[ -z "$IDENTITY" ]] remediation
# block below could fire). awk prints the identity WITHOUT surrounding quotes.
IDENTITY=$(security find-identity -v -p codesigning \
           | awk 'match($0, /"Developer ID Application: [^"]*"/) {
               s=substr($0, RSTART+1, RLENGTH-2); print s; exit
             }')
```

Rationale (do NOT relitigate — locked by RESEARCH.md Fix 2):
- awk exits 0 on no match, so pipefail never triggers
- Output format matches the old pipeline (identity string without surrounding quotes)
- The existing `if [[ -z "$IDENTITY" ]]` guard at line 34 works unchanged
- Avoids the `|| true` masking-risk pattern (Option A fallback)

DO NOT change:
- Lines 1-27 (shebang, set -euo pipefail, variable setup, comment block)
- Lines 34-47 (the remediation block that must fire)
- Anything else in the file
  </action>
  <verify>
Run these four checks in order:

1. Syntax check — awk script parses:
   bash -n Scripts/install.sh

2. Pipefail-immune verification — simulate the no-match case with a fake PATH that has a stub `security`:
   TMPDIR_P107_3=$(mktemp -d)
   cat > "$TMPDIR_P107_3/security" <<'STUB'
   #!/bin/bash
   echo "Policy: Code Signing"
   echo "  Matching identities"
   echo "  0 identities found"
   exit 0
   STUB
   chmod +x "$TMPDIR_P107_3/security"
   PATH="$TMPDIR_P107_3:$PATH" bash -c 'set -euo pipefail; IDENTITY=$(security find-identity -v -p codesigning | awk "match(\$0, /\"Developer ID Application: [^\"]*\"/) { s=substr(\$0, RSTART+1, RLENGTH-2); print s; exit }"); echo "IDENTITY=[$IDENTITY]"; [[ -z "$IDENTITY" ]] && echo "REMEDIATION FIRED" || echo "REMEDIATION NOT REACHED"'
   rm -rf "$TMPDIR_P107_3"

   Expected: `IDENTITY=[]` followed by `REMEDIATION FIRED`.
   Current (broken) behavior would exit before printing either.

3. Positive-path check on THIS machine (the iMac HAS the cert):
   bash Scripts/install.sh 2>&1 | head -5
   Expected: output includes `Signing identity: Developer ID Application: Jameson Morrill (5H57ADQS8G)` (or similar), script proceeds past the identity check.

   If you want to abort install after the identity check succeeds (so we don't actually rebuild the app), prefix with: `timeout 3 bash Scripts/install.sh` — the `timeout` exit is fine, we only need to see the "Signing identity:" line.

4. Git diff is scoped correctly:
   git diff Scripts/install.sh | head -30
   Expected: only lines 28-32 area changed; no unrelated edits.
  </verify>
  <done>
- `bash -n Scripts/install.sh` passes
- Simulated no-cert scenario prints `REMEDIATION FIRED` (not silent exit)
- Positive-path run on iMac prints `Signing identity: ...` line
- Diff is confined to the identity-resolution block (lines 28-32)
  </done>
</task>

</tasks>

<verification>
Single-task plan, verification is covered by the task's own <verify> block. Additionally:

Final integration check — run the full preflight-check script (which on a healthy machine should still pass cleanly):
  bash scripts/preflight-check.sh 2>&1 | tail -5 || true

The change only touches the identity-resolution pipeline in install.sh; no other scripts or code paths consume it, so scope is naturally contained.
</verification>

<success_criteria>
- `Scripts/install.sh` line 28-32 block replaced with awk single-command
- Comment block above the new assignment explains the pipefail/awk rationale (prevents regression)
- Simulated no-cert run exits with the loud remediation block (verified via stub `security` test)
- Healthy-machine run of install.sh still resolves the identity and proceeds
- No unrelated edits in the diff
</success_criteria>

<output>
After completion, create `.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-02-SUMMARY.md` using the summary template. Key fields:
- subsystem: tooling/installer
- tech-stack.added: (none — pure shell refactor)
- affects: Scripts/install.sh
- decisions: awk chosen over `|| true` (LOCKED by RESEARCH Fix 2 — awk is pipefail-immune without masking `security` binary failures)
- patterns: pipefail-safe bash idiom — prefer awk over grep for no-match-tolerant extraction
</output>
