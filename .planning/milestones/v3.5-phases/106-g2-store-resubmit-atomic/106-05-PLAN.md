---
phase: 106
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - vigil-g2-plugin/store-assets/01-work-orders.png
  - vigil-g2-plugin/store-assets/02-affirmation.png
  - .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md
  - vigil-g2-plugin/vigil.ehpk
autonomous: false
requirements: [G2-01, G2-02, G2-03]
requirements_addressed: [G2-01, G2-02, G2-03]
tags: [g2, simulator, verification, atomic-gate, ehpk-pack, checkpoint]
must_haves:
  truths:
    - "`vigil-g2-plugin/store-assets/01-work-orders.png` exists at native 576×288, captured from Even simulator v0.6.2+ with VITE_SCREENSHOT_MODE=1 (shows 3 DEMO_BRIEF tasks + VIGIL header + greyscale border + exit-gesture footer)"
    - "`vigil-g2-plugin/store-assets/02-affirmation.png` exists at native 576×288, captured from Even simulator v0.6.2+ (shows DEMO_AFFIRMATION text + VIGIL header + greyscale border + exit-gesture footer)"
    - "`.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md` has `Verified: <ISO 8601>` line replaced with a real timestamp within the last 24 hours, all three G2-01/G2-02/G2-03 checkboxes ticked, and observed simulator behavior documented"
    - "Double-tap on home screen rendered the host exit-confirmation dialog on simulator (or diverged behavior documented in VERIFIED.md per D-04 + RESEARCH A3)"
    - "Double-tap on work-orders / affirmation / task-detail screens still navigated to home (no regression per D-02)"
    - "`vigil-g2-plugin/vigil.ehpk` produced by `npm run package:ehpk` (gate passes)"
    - "Stale-gate negative test confirmed: backdating VERIFIED.md >24h caused `check-verified.mjs` to exit 1 (test performed and restored)"
    - "`.env.production` re-verified unchanged — still contains only VITE_API_URL and VITE_API_KEY (T8-leak-1 final check)"
  artifacts:
    - path: "vigil-g2-plugin/store-assets/01-work-orders.png"
      provides: "Work-orders screen store screenshot"
      contains: "PNG 576x288"
    - path: "vigil-g2-plugin/store-assets/02-affirmation.png"
      provides: "Affirmation screen store screenshot"
      contains: "PNG 576x288"
    - path: ".planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md"
      provides: "Filled-in verification checklist with real ISO timestamp + ticked checkboxes + simulator notes"
      contains: "Verified: 20"
    - path: "vigil-g2-plugin/vigil.ehpk"
      provides: "Packed plugin artifact ready for manual upload to Even Hub (D-14: this plan does NOT auto-upload)"
      min_bytes: 10000
  key_links:
    - from: ".planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md"
      to: "vigil-g2-plugin/scripts/check-verified.mjs"
      via: "npm run package:ehpk invokes the gate script which parses Verified: line"
      pattern: "Verified:\\s*20"
    - from: "vigil-g2-plugin/store-assets/*.png"
      to: "Even Hub store listing (manual upload by user per D-09, D-14)"
      via: "User drags files to Even Hub store dashboard after this plan completes"
      pattern: "store-assets.*\\.png"
verification:
  quick: "cd vigil-g2-plugin && node scripts/check-verified.mjs"
  full: "cd vigil-g2-plugin && npm run package:ehpk && test -f vigil.ehpk"
security_enforcement: true
---

<objective>
Run the single simulator verification session that gates the entire phase. Capture work-orders and affirmation screenshots at 576×288 (D-09: user uploads home + task-detail himself), fill in `VERIFIED.md` with real ISO timestamp + ticked checkboxes + observed simulator behavior, confirm the stale-gate fails-closed on backdated timestamps, then run `npm run package:ehpk` to produce `vigil.ehpk`.

Purpose: This is the atomic gate promised by the phase goal. No partial resubmission (CONTEXT §"Atomic Gate" D-12 through D-14). If ANY of G2-01/G2-02/G2-03 fails during this session, the user does NOT ship — the relevant Wave 1 plan is revised and this plan re-runs. The phase does NOT upload the `.ehpk` to Even Hub; D-14 explicitly stops at the packaged file.

Output: Two PNGs + filled VERIFIED.md + packed `vigil.ehpk`. Checkpoint pauses for user to confirm screenshots look right before the final pack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md
@.planning/phases/106-g2-store-resubmit-atomic/106-RESEARCH.md
@.planning/phases/106-g2-store-resubmit-atomic/106-VALIDATION.md
@.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md
@vigil-g2-plugin/scripts/check-verified.mjs
@vigil-g2-plugin/.env.screenshot.example
@vigil-g2-plugin/package.json

<interfaces>
<!-- Artifacts from Wave 1 that Plan 05 verifies. -->

From Plan 02 (navigation.ts):
- Double-tap on home → `void bridge.shutDownPageContainer(1)` → host confirmation dialog
- Double-tap on work-orders/affirmation/task-detail → navigateTo(HOME)

From Plan 03 (screens/*.ts + header.ts):
- Every screen: unified `VIGIL <right-side>` header via buildVigilHeader, body with `borderWidth: 1, borderColor: 15`, footer with `⌾ double-tap` gesture
- Exact fallback strings: work-orders `"No work orders open. Capture one when it finds you."` / affirmation `"Brief unavailable. Retry when you're ready."` / task-detail `"Task not found. Swipe to return."`

From Plan 04 (api.ts):
- `VITE_SCREENSHOT_MODE=1` → `fetchBrief` returns DEMO_BRIEF (3 tasks: PR-4827 review, Q2 OKRs, kitchen sink)
- `VITE_SCREENSHOT_MODE=1` → `fetchAffirmation` returns `"You are exactly where you need to be today."`

From Plan 01 (gate infra):
- `node vigil-g2-plugin/scripts/check-verified.mjs` → exit 0 only if VERIFIED.md's `Verified:` ISO timestamp is within 24h
- `cd vigil-g2-plugin && npm run package:ehpk` → gate → build → `evenhub pack ... -o vigil.ehpk`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Even simulator → local `.png` files | Screenshots are trusted local artifacts; user handles upload manually per D-09 and D-14 |
| Local env → build | `.env.production` must NOT have `VITE_SCREENSHOT_MODE`; `.env.screenshot` (gitignored) has it for local captures |
| Gate script → pack pipeline | `check-verified.mjs` is the only gate; it trusts the timestamp we write. Tampering = self-harm (see T-106-01-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05-01 (= RESEARCH T8-leak-1) | Information Disclosure | Final `.ehpk` ships demo data because `.env.production` was accidentally edited during the screenshot session | mitigate | Pre-pack acceptance criterion greps `.env.production` for `VITE_SCREENSHOT_MODE` and asserts ABSENT. Also verify `dist/assets/*.js` after production build does NOT contain `'Follow up on PR-4827 review'` (Plan 04's dead-code-elimination signal). |
| T-106-05-02 (= RESEARCH T8-leak-2) | Information Disclosure | `evenhub login` credentials or personal API keys committed alongside screenshots | mitigate | VERIFIED.md runbook includes the reminder (from Plan 01). Acceptance criterion: `git status` at commit time shows no files under `~/.config/evenhub/` or any `.env*` besides `.env.screenshot.example`. |
| T-106-05-03 | Tampering | User manually ticks the checkboxes without actually running the simulator tests | accept | Self-harm only; framed as a discipline gate per RESEARCH §Security. The 24h staleness + checkbox commitment is the pragmatic gate, not a cryptographic attestation. |
| T-106-05-04 | Information Disclosure | Retina-scaled 1152×576 screenshot shipped instead of native 576×288 — upscaling may reveal identifying pixel artifacts or fail store review | mitigate | Use `sips -g pixelWidth -g pixelHeight` to verify dimensions. If retina, downscale with `sips -z 288 576`. Acceptance criterion enforces exact 576×288. |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Prerequisites confirmation (simulator access, .env.screenshot setup)</name>
  <read_first>
    - .planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md §"Decisions" D-09, D-10, D-11
    - .planning/phases/106-g2-store-resubmit-atomic/106-RESEARCH.md §"Code Examples" Example 4 (screenshot workflow)
    - .planning/phases/106-g2-store-resubmit-atomic/106-RESEARCH.md §"Assumptions Log" A1, A2, A3 (simulator unknowns)
    - vigil-g2-plugin/.env.screenshot.example
  </read_first>
  <what-built>
    Wave 1 has landed: handleNavEvent branch (Plan 02), unified headers + borders + fallbacks (Plan 03), VITE_SCREENSHOT_MODE guard (Plan 04), and Wave 0 scaffold (Plan 01). Nothing captured yet — this session produces the PNGs + VERIFIED.md + vigil.ehpk.
  </what-built>
  <how-to-verify>
    Before the simulator session, confirm prerequisites on this machine:

    1. **Even Realities iPhone app** is installed and updated to **≥ v0.6.2** (G2-01 hard requirement). If unknown, the user opens the app, checks Settings → About. If < 0.6.2, the user updates via App Store before continuing.

    2. **iPhone (or iPhone simulator)** is paired/active with the Even simulator view visible. Per RESEARCH A1: the "Even simulator" is the iPhone-app's built-in G2 canvas, NOT a standalone macOS Xcode simulator.

    3. **Screenshot mechanism known** — per RESEARCH A2 this is a first-time discovery. Acceptable options:
       - (a) iOS hardware screenshot (Power + Volume Up) captures the entire phone screen; we crop to the G2 canvas region later.
       - (b) Even app has a built-in "export glasses canvas" button (check v0.6.2 release notes — unknown today).
       - (c) xcrun simctl io booted screenshot (only applicable if using Xcode iOS Simulator).

    4. **Local screenshot env set up:**
       ```bash
       cd vigil-g2-plugin
       cp .env.screenshot.example .env.screenshot
       # Confirm the file exists and has VITE_SCREENSHOT_MODE=1
       cat .env.screenshot
       ```

    5. **Verify .env.production untouched:**
       ```bash
       grep -c '^VITE_' vigil-g2-plugin/.env.production  # expect: 2 (API_URL + API_KEY)
       ! grep -q VITE_SCREENSHOT_MODE vigil-g2-plugin/.env.production  # expect: success (absent)
       ```

    6. **Build + dev server ready** — user decides dev-server vs. full sideload-to-simulator based on Even app's workflow:
       ```bash
       cd vigil-g2-plugin
       # Option A: dev server for rapid iteration
       export $(grep -v '^#' .env.screenshot | xargs) && npm run dev
       # Option B: full build + sideload for simulator
       export $(grep -v '^#' .env.screenshot | xargs) && npm run build
       # then sideload dist/ via Even Hub developer flow — exact path lives in Even Hub docs
       ```

    **Blocking confirmation:** user replies with their simulator version (must be ≥ 0.6.2) and which capture mechanism they'll use. If version < 0.6.2 or no capture mechanism is available, the session pauses and the user either updates or uses the iOS hardware screenshot as fallback (with a later `sips -z 288 576` downscale — see Task 2).
  </how-to-verify>
  <resume-signal>
    User replies with: (a) Even app version (e.g., `0.6.3`), (b) chosen capture mechanism (e.g., `iOS hardware screenshot + sips downscale` OR `Even app export button`), (c) confirmation that `.env.screenshot` is staged and `.env.production` is untouched.
  </resume-signal>
  <files>vigil-g2-plugin/.env.screenshot</files>
  <action>
Prerequisites checkpoint — user confirms (a) Even app version ≥ 0.6.2, (b) capture mechanism choice, (c) `.env.screenshot` staged locally (gitignored), (d) `.env.production` untouched. See `<how-to-verify>` for the full confirmation script; nothing is committed or packed in this task.
  </action>
  <verify>
    <automated>
test -f vigil-g2-plugin/.env.screenshot.example &&
! grep -q 'VITE_SCREENSHOT_MODE' vigil-g2-plugin/.env.production &&
(test -f vigil-g2-plugin/.env.screenshot || echo "user will create from .env.screenshot.example")
    </automated>
  </verify>
  <acceptance_criteria>
    - Even app version confirmed by user (≥ 0.6.2) — recorded in session transcript; will later land in VERIFIED.md Task 3
    - Screenshot capture mechanism identified by user — recorded for VERIFIED.md runbook section
    - `vigil-g2-plugin/.env.screenshot.example` exists (Plan 04 artifact)
    - `.env.production` does NOT contain `VITE_SCREENSHOT_MODE` — grep asserts absent (T8-leak-1 pre-check)
    - User has either created `vigil-g2-plugin/.env.screenshot` OR explicitly deferred until screen-by-screen capture in Task 2
  </acceptance_criteria>
  <done>User has confirmed prerequisites and is ready to capture screenshots in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture + verify both screenshots at native 576×288</name>
  <read_first>
    - .planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md §"Decisions" D-09, D-10 (division of labor, 576×288 spec)
    - .planning/phases/106-g2-store-resubmit-atomic/106-RESEARCH.md §"Code Examples" Example 4 (capture + sips workflow)
    - .planning/phases/106-g2-store-resubmit-atomic/106-VALIDATION.md row 106-04-03 (sips verification)
  </read_first>
  <what-built>
    Screenshot capture happens here. The plugin is running with VITE_SCREENSHOT_MODE=1, so the simulator displays DEMO_BRIEF's 3 tasks and DEMO_AFFIRMATION's string. Both PNGs get committed to `vigil-g2-plugin/store-assets/`.
  </what-built>
  <how-to-verify>
    Working session steps (user drives the simulator; assistant runs shell verification):

    **(a) Capture 01-work-orders.png**

    1. On simulator, navigate to WORK ORDERS screen (swipe down from home).
    2. Capture the G2 canvas image by the mechanism confirmed in Task 1.
    3. Save to `~/Desktop/01-work-orders-raw.png` (or wherever your capture mechanism writes).
    4. Verify dimensions:
       ```bash
       sips -g pixelWidth -g pixelHeight ~/Desktop/01-work-orders-raw.png
       ```
       - If output is `pixelWidth: 576` AND `pixelHeight: 288` → move to `store-assets/`:
         ```bash
         mv ~/Desktop/01-work-orders-raw.png vigil-g2-plugin/store-assets/01-work-orders.png
         ```
       - If output is `pixelWidth: 1152` AND `pixelHeight: 576` (retina 2x) → downscale:
         ```bash
         sips -z 288 576 ~/Desktop/01-work-orders-raw.png --out vigil-g2-plugin/store-assets/01-work-orders.png
         ```
       - If output is anything else (e.g., a cropped iPhone-screen capture) → the user crops manually or re-captures.
    5. Re-verify final file:
       ```bash
       sips -g pixelWidth -g pixelHeight vigil-g2-plugin/store-assets/01-work-orders.png
       # MUST report: pixelWidth: 576, pixelHeight: 288
       ```

    **(b) Capture 02-affirmation.png**

    1. On simulator, navigate to AFFIRMATION screen (swipe down again).
    2. Repeat capture + dimension-check + move/downscale steps above, writing to `vigil-g2-plugin/store-assets/02-affirmation.png`.

    **(c) Visual review (checkpoint gate):**

    Open both PNGs in Preview. The user confirms:
    - [ ] Both screenshots show the unified `VIGIL` wordmark header (left-side) + screen-specific label (right-side: "3 open" on work-orders; time on affirmation)
    - [ ] Body container has a visible 1-pixel border (greyscale/green on hardware)
    - [ ] Footer shows `⌾ double-tap to exit`
    - [ ] Work-orders shows 3 demo tasks (PR-4827 review, Q2 OKRs, kitchen sink) NOT real user data
    - [ ] Affirmation shows "You are exactly where you need to be today."
    - [ ] No blank/placeholder/loading states visible
    - [ ] Copy reads in Vigil voice (calm, direct, first-person; no productivity jargon)

    If ANY checkbox fails: user identifies which Wave 1 plan to re-run (02/03/04) before coming back to this task. Do NOT proceed to Task 3 with bad screenshots.
  </how-to-verify>
  <resume-signal>
    User replies "approved" after Preview review, OR describes specific issue (e.g., "affirmation body border not visible" → re-run Plan 03 Task 2) and which plan needs revision.
  </resume-signal>
  <files>
    vigil-g2-plugin/store-assets/01-work-orders.png,
    vigil-g2-plugin/store-assets/02-affirmation.png
  </files>
  <action>
Capture both PNGs (work-orders + affirmation) from the Even simulator running with `VITE_SCREENSHOT_MODE=1`, verify each is native 576×288 via `sips -g pixelWidth -g pixelHeight`, downscale retina captures with `sips -z 288 576 <in> --out <store-assets>/<name>.png` if needed, then have the user visually review both in Preview. See `<how-to-verify>` for the full capture + dimension-check + visual-review sequence. Do NOT proceed if any image is off-spec or contains real user data instead of DEMO_BRIEF content.
  </action>
  <verify>
    <automated>
test -f vigil-g2-plugin/store-assets/01-work-orders.png &&
test -f vigil-g2-plugin/store-assets/02-affirmation.png &&
test "$(sips -g pixelWidth vigil-g2-plugin/store-assets/01-work-orders.png | grep pixelWidth | awk '{print $2}')" = "576" &&
test "$(sips -g pixelHeight vigil-g2-plugin/store-assets/01-work-orders.png | grep pixelHeight | awk '{print $2}')" = "288" &&
test "$(sips -g pixelWidth vigil-g2-plugin/store-assets/02-affirmation.png | grep pixelWidth | awk '{print $2}')" = "576" &&
test "$(sips -g pixelHeight vigil-g2-plugin/store-assets/02-affirmation.png | grep pixelHeight | awk '{print $2}')" = "288"
    </automated>
  </verify>
  <acceptance_criteria>
    - `vigil-g2-plugin/store-assets/01-work-orders.png` exists, exactly 576×288 (VALIDATION.md task 106-04-03)
    - `vigil-g2-plugin/store-assets/02-affirmation.png` exists, exactly 576×288
    - User visually confirmed: VIGIL header wordmark visible on both, screen-specific label on right, 1px greyscale body border visible, footer shows `⌾ double-tap to exit`
    - Work-orders PNG shows the 3 DEMO_BRIEF task strings (PR-4827, Q2 OKRs, kitchen sink) — NOT real user data
    - Affirmation PNG shows `"You are exactly where you need to be today."`
    - No blank/placeholder/loading states visible in either capture
  </acceptance_criteria>
  <done>Both PNGs committed to `store-assets/` at native 576×288 and user-approved for visual quality.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify G2-02 exit-confirm behavior on simulator and record observation</name>
  <files>.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md</files>
  <read_first>
    - .planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md §"Decisions" D-01, D-02, D-03, D-04 (exit-confirm spec and simulator-vs-hardware stance)
    - .planning/phases/106-g2-store-resubmit-atomic/106-RESEARCH.md §"Assumptions Log" A3 (simulator may render differently — not a regression signal)
    - .planning/phases/106-g2-store-resubmit-atomic/106-VALIDATION.md §"Manual-Only Verifications" rows 1-2
    - vigil-g2-plugin/src/navigation.ts (Plan 02's edit)
  </read_first>
  <action>
**Assistant drives this task** — user performs the simulator gestures while the assistant records observations in VERIFIED.md.

**(a) G2-02 positive test — home screen double-tap:**

Instruct the user:
> "On the simulator, navigate to the HOME screen. Perform a double-tap on the temple touchpad area (or equivalent simulator gesture)."

Record in VERIFIED.md the observed behavior. Expected: host-rendered exit-confirmation dialog appears (title, one or two buttons, some auto-dismiss timer). Possible variations to document per D-04 / RESEARCH A3:
- Dialog appears and user confirms → plugin exits (navigateTo never fires). ✅ expected.
- Dialog appears and user cancels or waits → plugin stays on home. ✅ expected.
- Dialog does NOT appear (simulator stub per RESEARCH Pitfall 3 / A3) → document observed behavior; flag for physical hardware retest on ~2026-04-24. Proceeding with the G2-02 checkbox ticked IS acceptable because (1) D-04 authorizes "planner documents the observed behavior in VERIFIED.md rather than trying to match the roadmap text literally" and (2) code-side the call site is verified by Plan 02's static grep — the rejection was about the code contract, not simulator fidelity.

**(b) G2-02 regression test — non-home double-tap:**

Instruct the user to test each non-home screen:
> "From the HOME screen, swipe down to WORK ORDERS. Double-tap. Expect navigation back to HOME."
> "Swipe down to AFFIRMATION. Double-tap. Expect navigation back to HOME."
> "Tap a task to enter TASK DETAIL. Double-tap. Expect navigation back to HOME."

Record each in VERIFIED.md. If any non-home screen unexpectedly triggers the exit dialog, Plan 02's edge was misplaced → roll back and revise Plan 02.

**(c) Write observations into VERIFIED.md:**

Update the `## Observed Behavior Notes` section of `.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md` with concrete observations, e.g.:

```markdown
## Observed Behavior Notes

- **G2-02 host dialog appearance (home double-tap):** [e.g., "Dialog shown: title 'Exit Vigil?', two buttons 'Cancel' and 'Exit', auto-dismissed after ~4 seconds of inactivity" OR "Dialog not rendered on simulator v0.6.2 — per D-04 + RESEARCH A3, this is acceptable for resubmit; call site statically verified; physical retest ~2026-04-24 will confirm"]
- **G2-02 non-home regression:**
  - work-orders double-tap → navigates to home ✅
  - affirmation double-tap → navigates to home ✅
  - task-detail double-tap → navigates to home ✅
- **Simulator version / OS:** Even iPhone app v[version], iOS [version]
- **Screenshot capture mechanism used (runbook):** [e.g., "iOS hardware screenshot (Power + Volume Up), then sips -z 288 576 for downscale"]
- **Figma spec review (RESEARCH Q1):** [tick and note any divergences found; or "Not divergent from our borders-only + VIGIL-wordmark choices"]
```

Leave the placeholder `Verified:` line as-is for now. Task 4 replaces it with the real ISO timestamp AFTER all checkboxes are confirmed — keeps the gate honest (any user who aborts mid-session before Task 4 can't ship).
  </action>
  <verify>
    <automated>
grep -q '## Observed Behavior Notes' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
grep -qE 'G2-02.*(home|dialog)' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
grep -qE 'work-orders.*(home|navigates)' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
grep -qE 'affirmation.*(home|navigates)' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
grep -qE 'task-detail.*(home|navigates)' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md
    </automated>
  </verify>
  <acceptance_criteria>
    - VERIFIED.md `## Observed Behavior Notes` section is populated (not `_describe..._` placeholder)
    - All three non-home screens have a regression observation recorded (work-orders, affirmation, task-detail)
    - Home double-tap outcome recorded (dialog visible OR divergence documented per D-04/A3)
    - Simulator version and OS recorded
    - Screenshot capture mechanism recorded in runbook section
    - Figma spec review outcome recorded (ticked or noted)
    - `Verified:` line still contains `PLACEHOLDER_REPLACE_WITH_ISO_8601_AT_VERIFY_TIME` (not yet replaced — Task 4 replaces after all checkboxes are final)
    - `node vigil-g2-plugin/scripts/check-verified.mjs` STILL exits 1 (fail-closed state preserved until Task 4 flips the timestamp)
  </acceptance_criteria>
  <done>
    All G2-02 manual test outcomes recorded in VERIFIED.md. Placeholder timestamp intact — gate still fails-closed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Tick all G2-01/02/03 checkboxes, stamp Verified: ISO timestamp, run atomic pack</name>
  <files>
    .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md,
    vigil-g2-plugin/vigil.ehpk
  </files>
  <read_first>
    - .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md (current — after Task 3 edits)
    - vigil-g2-plugin/scripts/check-verified.mjs (gate script — from Plan 01)
    - vigil-g2-plugin/package.json (package:ehpk script chain — from Plan 01)
    - .planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md §"Decisions" D-13 (24h staleness gate), D-14 (no auto-upload)
  </read_first>
  <action>
**(a) Tick all three G2 checkboxes in VERIFIED.md:**

Open `.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md` and change:

- `- [ ] **G2-01**` → `- [x] **G2-01**`
- `- [ ] **G2-02**` → `- [x] **G2-02**`
- `- [ ] **G2-03**` → `- [x] **G2-03**`

Tick only if the corresponding behavior was actually observed. If ANY test failed, STOP — do not tick, do not stamp the timestamp, do not run the pack. The user decides: re-run the failed Wave 1 plan, or abort this phase.

Also tick the four "Resubmission Readiness" checkboxes after verifying:
- `[x] npm run package:ehpk exits 0` — verified at end of this task
- `[x] vigil-g2-plugin/vigil.ehpk exists and is > 10KB` — verified at end of this task
- `[x] No untracked ~/.config/evenhub/ artifacts in git status`
- `[x] Ready for manual upload to Even Hub` (D-14: phase stops here)

**(b) Re-verify `.env.production` untouched (final T8-leak-1 check):**

```bash
grep -c '^VITE_' vigil-g2-plugin/.env.production
# Expected: 2 (VITE_API_URL + VITE_API_KEY only)

! grep -q 'VITE_SCREENSHOT_MODE' vigil-g2-plugin/.env.production
# Expected: success (absent)
```

If either check fails: revert `.env.production` via `git checkout vigil-g2-plugin/.env.production` and re-run before proceeding.

**(c) Clean up `.env.screenshot` (local file — gitignored, but confirm no leak):**

```bash
rm -f vigil-g2-plugin/.env.screenshot
git status vigil-g2-plugin/.env.screenshot
# Expected: untracked or file absent — never staged or committed
```

**(d) Replace the `Verified:` placeholder with a real ISO 8601 timestamp:**

Generate the current timestamp and write it into VERIFIED.md:

```bash
TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
# Replace the placeholder — sed in-place or Edit tool
```

Using the `Edit` tool, replace the exact line:
- Old: `Verified: PLACEHOLDER_REPLACE_WITH_ISO_8601_AT_VERIFY_TIME`
- New: `Verified: <TIMESTAMP>` (e.g., `Verified: 2026-04-19T22:45:00Z`)

**(e) Sanity check — gate now passes:**

```bash
node vigil-g2-plugin/scripts/check-verified.mjs
# Expected: stdout "[package:ehpk] VERIFIED.md fresh (Nm old). Proceeding." AND exit 0
```

If it fails (e.g., "Unparseable timestamp"), check the format — it must match ISO 8601 per the script's `Date.parse()` fallback (e.g., `2026-04-19T22:45:00Z` or `2026-04-19T22:45:00-07:00`).

**(f) Run the full atomic pack:**

```bash
cd vigil-g2-plugin
npm run package:ehpk
```

This chains:
1. `node scripts/check-verified.mjs` (should exit 0 now)
2. `npm run release` = `npm run build:prod && npm run pack`
3. `tsc && vite build --mode production`
4. `evenhub pack app.json dist -o vigil.ehpk`

**(g) Verify the pack output:**

```bash
test -f vigil-g2-plugin/vigil.ehpk
ls -la vigil-g2-plugin/vigil.ehpk  # Expect size > 10KB
```

**(h) Dead-code elimination final check:**

```bash
! grep -q 'Follow up on PR-4827 review' vigil-g2-plugin/dist/assets/*.js
# Expected: success (absent) — confirms demo data stripped from prod bundle
```

If this grep FAILS (demo data present), the pack is NOT safe to ship:
1. Check whether `.env.screenshot` leaked into the build env (`env | grep VITE_SCREENSHOT`).
2. Check whether `.env.production` was tampered.
3. Revert and rebuild. Do NOT commit.
  </action>
  <verify>
    <automated>
cd vigil-g2-plugin &&
test -f vigil.ehpk &&
test $(wc -c < vigil.ehpk) -gt 10000 &&
! grep -q 'VITE_SCREENSHOT_MODE' .env.production &&
! grep -q 'Follow up on PR-4827 review' dist/assets/*.js 2>/dev/null &&
test -f store-assets/01-work-orders.png &&
test -f store-assets/02-affirmation.png &&
test "$(sips -g pixelWidth store-assets/01-work-orders.png | grep pixelWidth | awk '{print $2}')" = "576" &&
test "$(sips -g pixelHeight store-assets/01-work-orders.png | grep pixelHeight | awk '{print $2}')" = "288" &&
test "$(sips -g pixelWidth store-assets/02-affirmation.png | grep pixelWidth | awk '{print $2}')" = "576" &&
test "$(sips -g pixelHeight store-assets/02-affirmation.png | grep pixelHeight | awk '{print $2}')" = "288" &&
grep -qE '^Verified:\s*20[0-9]{2}-[0-9]{2}-[0-9]{2}T' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
grep -c '\[x\] \*\*G2-0[123]\*\*' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md | grep -qE '^3$' &&
node scripts/check-verified.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `VERIFIED.md` has all three `- [x] **G2-01**`, `- [x] **G2-02**`, `- [x] **G2-03**` checkboxes ticked
    - `VERIFIED.md` has `Verified: <ISO 8601 timestamp>` line (matches regex `^Verified:\s*20[0-9]{2}-[0-9]{2}-[0-9]{2}T`)
    - `node vigil-g2-plugin/scripts/check-verified.mjs` exits 0 with "Proceeding." message (VALIDATION.md task 106-05-01)
    - `vigil-g2-plugin/vigil.ehpk` exists and is > 10000 bytes (VALIDATION.md task 106-05-02)
    - `vigil-g2-plugin/store-assets/01-work-orders.png` exists at exactly 576×288 (sips check)
    - `vigil-g2-plugin/store-assets/02-affirmation.png` exists at exactly 576×288 (sips check)
    - `.env.production` does NOT contain `VITE_SCREENSHOT_MODE` (final T8-leak-1 check)
    - Production bundle does NOT contain demo data: `! grep -q 'Follow up on PR-4827 review' dist/assets/*.js` succeeds
    - `.env.screenshot` removed/untracked (not committed) — `git status vigil-g2-plugin/.env.screenshot` shows no pending file
    - No `~/.config/evenhub/` artifacts in git status (T8-leak-2)
  </acceptance_criteria>
  <done>
    Atomic gate passed: VERIFIED.md fresh + all checkboxes ticked; vigil.ehpk produced and > 10KB; no demo-data leak in prod bundle; `.env.production` untouched; user can now manually upload vigil.ehpk + screenshots to Even Hub (D-14).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Stale-gate negative test (confirm gate refuses backdated timestamps)</name>
  <files>.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md</files>
  <read_first>
    - vigil-g2-plugin/scripts/check-verified.mjs (the 24h MAX_AGE_MS)
    - .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md (current Verified: line)
    - .planning/phases/106-g2-store-resubmit-atomic/106-CONTEXT.md §D-13 (hard failure, not warning)
  </read_first>
  <action>
**Purpose:** Prove the atomic gate can ALSO fail-closed (not just pass today). This catches regressions where someone might later edit `check-verified.mjs` to silently always-pass.

**(a) Save the current real timestamp:**

```bash
REAL_STAMP=$(grep -E '^Verified:\s*' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md | sed 's/Verified:\s*//')
echo "Saved: $REAL_STAMP"
```

**(b) Backdate the timestamp to ~48 hours ago and confirm the gate refuses:**

Replace `Verified: <REAL_STAMP>` with `Verified: 2020-01-01T00:00:00Z` (definitely > 24h old) using the Edit tool.

Run the gate:

```bash
node vigil-g2-plugin/scripts/check-verified.mjs
# Expected: stderr "[package:ehpk] VERIFIED.md is ~Nh old (max 24h). Re-run simulator verification."
# Expected: exit code 1
echo "Exit: $?"  # Expect: 1
```

Also confirm `npm run package:ehpk` refuses:

```bash
cd vigil-g2-plugin && npm run package:ehpk; echo "Exit: $?"
# Expected: Exit: 1 (gate rejects before build runs)
```

**(c) Restore the real timestamp:**

Replace `Verified: 2020-01-01T00:00:00Z` back to `Verified: <REAL_STAMP>` (the value saved in step a) using the Edit tool.

Re-run the gate to confirm we're back to pass:

```bash
node vigil-g2-plugin/scripts/check-verified.mjs; echo "Exit: $?"
# Expected: Exit: 0 with "Proceeding."
```

**(d) Record the negative test result in VERIFIED.md under "Observed Behavior Notes":**

Append a line like:
```markdown
- **Stale-gate negative test (D-13 enforcement):** Backdated `Verified:` to 2020-01-01 → `check-verified.mjs` exited 1 with "VERIFIED.md is Nh old (max 24h)" ✅. Restored real timestamp → exited 0.
```
  </action>
  <verify>
    <automated>
grep -qE '^Verified:\s*20[0-9]{2}-[0-9]{2}-[0-9]{2}T' .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
cd vigil-g2-plugin &&
node scripts/check-verified.mjs &&
grep -q 'Stale-gate negative test' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md
    </automated>
  </verify>
  <acceptance_criteria>
    - During backdate: `node scripts/check-verified.mjs` exited 1 (captured during Task 5 execution)
    - During backdate: `npm run package:ehpk` exited 1 (before entering build step)
    - After restore: `node scripts/check-verified.mjs` exited 0
    - VERIFIED.md retains the real (restored) `Verified:` timestamp — regex `^Verified:\s*20[0-9]{2}-[0-9]{2}-[0-9]{2}T` matches (not `2020-01-01`)
    - "Stale-gate negative test" note appended to VERIFIED.md — grep match
    - Final `node scripts/check-verified.mjs` exits 0 (gate passes again after restoration)
  </acceptance_criteria>
  <done>
    Atomic gate proven to fail-closed on backdated timestamps AND pass-open on fresh ones. Gate is working as designed per D-13.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final visual + artifact review before commit</name>
  <what-built>
    Full phase output in place: two PNGs in `store-assets/`, filled `VERIFIED.md`, packed `vigil.ehpk`, all gates passing. This checkpoint is the last opportunity to catch issues before everything is committed.
  </what-built>
  <how-to-verify>
    User review:

    1. **Open both PNGs again in Preview** — one more look to confirm they match store-submission quality (no blur, no crop artifacts, Vigil voice readable).

    2. **Scan `VERIFIED.md`** — open in editor, confirm:
       - [x] marks on all three G2 items and all four Resubmission Readiness items
       - `Verified: <ISO 8601>` line is a real timestamp (not placeholder, not 2020-01-01)
       - `## Observed Behavior Notes` has real observations (not template prose)
       - Stale-gate negative test note present
       - T8-leak-2 reminder still present (evenhub credentials warning)

    3. **Run `git status`** — confirm:
       - Tracked + modified:
         - `.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md`
         - `vigil-g2-plugin/store-assets/01-work-orders.png` (new)
         - `vigil-g2-plugin/store-assets/02-affirmation.png` (new)
       - Untracked (expected, do NOT commit): `vigil-g2-plugin/vigil.ehpk` (gitignored by `.gitignore` line `*.ehpk`), `vigil-g2-plugin/dist/` (gitignored)
       - Absent (must NOT be present): anything under `~/.config/evenhub/`, `vigil-g2-plugin/.env.screenshot`, any other `.env*.local`

    4. **Final confirmation:** user replies "approved" to proceed with git commit of the three files (PLAN 05's SUMMARY will cover next-steps like manual Even Hub upload).

    If any check fails: user identifies the gap and we either re-run a specific Wave 1 plan or re-capture screenshots. Nothing commits until this approval.
  </how-to-verify>
  <resume-signal>
    User replies "approved" (commit PNGs + VERIFIED.md; vigil.ehpk stays local-only per .gitignore and D-14) OR describes the remaining issue.
  </resume-signal>
  <files>
    vigil-g2-plugin/store-assets/01-work-orders.png,
    vigil-g2-plugin/store-assets/02-affirmation.png,
    .planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md,
    vigil-g2-plugin/vigil.ehpk
  </files>
  <action>
Final user-review checkpoint before any git commit. User reviews both PNGs in Preview, scans VERIFIED.md for ticked checkboxes + real timestamp + observed-behavior notes + stale-gate negative-test note, runs `git status` to confirm only the three intended files are staged (PNGs + VERIFIED.md) and that `vigil.ehpk` is UNTRACKED (per `.gitignore`'s `*.ehpk` rule and D-14), and that no `.env.screenshot` / `~/.config/evenhub/` / other credentials are staged. No file edits happen in this task — it's a pure gate before commit.
  </action>
  <verify>
    <automated>
cd vigil-g2-plugin &&
test -f store-assets/01-work-orders.png &&
test -f store-assets/02-affirmation.png &&
test -f vigil.ehpk &&
node scripts/check-verified.mjs &&
grep -c '\[x\] \*\*G2-0[123]\*\*' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md | grep -qE '^3$' &&
grep -qE '^Verified:\s*20[0-9]{2}-[0-9]{2}-[0-9]{2}T' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md &&
! grep -q 'VITE_SCREENSHOT_MODE' .env.production &&
! git status --porcelain | grep -qE '\.env\.screenshot$|\.config/evenhub'
    </automated>
  </verify>
  <acceptance_criteria>
    - Both PNGs present at 576×288 (re-confirmed at checkpoint)
    - `VERIFIED.md` has fresh real timestamp + 3 ticked G2 checkboxes + observation notes + stale-gate negative-test note
    - `node scripts/check-verified.mjs` exits 0 (gate in pass-state)
    - `vigil.ehpk` exists in `vigil-g2-plugin/` and is UNTRACKED per `.gitignore` (confirmed via `git status` — user reviews)
    - `.env.production` unchanged (final T8-leak-1 check)
    - No `.env.screenshot`, no `~/.config/evenhub/` artifacts in `git status --porcelain` output (T8-leak-2)
    - User verbally approved for commit
  </acceptance_criteria>
  <done>User approved; ready for `/gsd-commit` or manual commit of PNGs + VERIFIED.md. Plan 05 SUMMARY writes next.</done>
</task>

</tasks>

<verification>
```bash
cd vigil-g2-plugin

# All artifacts in place
test -f store-assets/01-work-orders.png
test -f store-assets/02-affirmation.png
test -f vigil.ehpk
test -f ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md

# Dimensions exact
test "$(sips -g pixelWidth -g pixelHeight store-assets/01-work-orders.png | grep -E 'pixelWidth|pixelHeight' | awk '{print $2}' | tr '\n' ' ')" = "576 288 "
test "$(sips -g pixelWidth -g pixelHeight store-assets/02-affirmation.png | grep -E 'pixelWidth|pixelHeight' | awk '{print $2}' | tr '\n' ' ')" = "576 288 "

# Gate passes
node scripts/check-verified.mjs

# All 3 G2 checkboxes ticked
test "$(grep -c '\[x\] \*\*G2-0[123]\*\*' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md)" = "3"

# Real ISO timestamp (not placeholder, not 2020)
grep -qE '^Verified:\s*202[6-9]-' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md

# Stale-gate negative test performed
grep -q 'Stale-gate negative test' ../.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md

# Production safety (T8-leak-1)
! grep -q 'VITE_SCREENSHOT_MODE' .env.production
! grep -q 'Follow up on PR-4827 review' dist/assets/*.js

# No leaked secrets (T8-leak-2)
! git status | grep -q '.env.screenshot$'
! find . -type d -name .config -path '*/evenhub/*' 2>/dev/null | grep -q evenhub

# Final atomic pack still works
npm run package:ehpk
test -f vigil.ehpk
```
</verification>

<success_criteria>
- [ ] `vigil-g2-plugin/store-assets/01-work-orders.png` at exactly 576×288, shows DEMO_BRIEF content + unified VIGIL header + greyscale border + exit-gesture footer
- [ ] `vigil-g2-plugin/store-assets/02-affirmation.png` at exactly 576×288, shows DEMO_AFFIRMATION text + same header/border/footer pattern
- [ ] `VERIFIED.md`: all G2-01/G2-02/G2-03 checkboxes ticked, real ISO 8601 `Verified:` timestamp, simulator observations recorded, stale-gate negative test documented
- [ ] `node scripts/check-verified.mjs` exits 0
- [ ] Backdated stale-gate test verified exits 1 (then restored)
- [ ] `npm run package:ehpk` exits 0 end-to-end
- [ ] `vigil-g2-plugin/vigil.ehpk` > 10KB, gitignored, ready for user's manual Even Hub upload
- [ ] `.env.production` still contains only VITE_API_URL + VITE_API_KEY (T8-leak-1)
- [ ] Production bundle does NOT contain `'Follow up on PR-4827 review'` (dead-code elimination)
- [ ] No `.env.screenshot` or `~/.config/evenhub/` artifacts in git status (T8-leak-2)
- [ ] User visually approved both PNGs and VERIFIED.md contents
</success_criteria>

<output>
After completion, create `.planning/phases/106-g2-store-resubmit-atomic/106-05-SUMMARY.md` per the summary template. Record:
- Simulator version + OS used
- Screenshot capture mechanism discovered (updates RESEARCH A2 / Q2)
- G2-02 host dialog observed behavior — CRITICAL for the next physical-hardware retest on ~2026-04-24 to have a baseline
- Stale-gate negative test outcome (proves D-13 enforcement works)
- `vigil.ehpk` byte size + next step: "User manually uploads `vigil.ehpk` + `store-assets/01-work-orders.png` + `store-assets/02-affirmation.png` + their own home + task-detail screenshots to Even Hub store listing per D-09 + D-14. Phase is closed on upload; resubmission response tracked via STATE.md blockers."
- Update STATE.md: resolve the Phase 106 research flags ("confirm exact G2 double-press event name" = resolved in CONTEXT D-03; "review Even Realities public Figma design spec" = resolved in VERIFIED.md Figma section or noted divergences).
</output>
