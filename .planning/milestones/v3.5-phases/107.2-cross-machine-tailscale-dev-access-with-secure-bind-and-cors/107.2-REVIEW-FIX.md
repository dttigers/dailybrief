---
phase: 107.2-cross-machine-tailscale-dev-access-with-secure-bind-and-cors
fixed_at: 2026-04-22T14:30:00Z
review_path: .planning/phases/107.2-cross-machine-tailscale-dev-access-with-secure-bind-and-cors/107.2-REVIEW.md
iteration: 1
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 107.2: Code Review Fix Report

**Fixed at:** 2026-04-22T14:30:00Z
**Source review:** .planning/phases/107.2-cross-machine-tailscale-dev-access-with-secure-bind-and-cors/107.2-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 3 (all 3 warnings; 6 info findings deferred per fix_scope=critical_warning)
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: VITE_DEV_API_TARGET empty-string fallthrough

**Files modified:** `vigil-pwa/vite.config.ts`
**Commit:** d515137
**Applied fix:** Swapped `??` for `||` on the `env.VITE_DEV_API_TARGET` fallback so empty-string (`VITE_DEV_API_TARGET=` with no value) also falls through to the `'http://localhost:3001'` default. Added a clarifying comment explaining the difference vs nullish coalescing and why empty string is treated as unset. Verification: `npx tsc --noEmit` reports zero errors in `vite.config.ts` (the only tsc errors are pre-existing TS6305 stale-declaration errors in unrelated source files).

### WR-02: preflight-check.sh strips spaces only, not tabs/CR

**Files modified:** `Scripts/preflight-check.sh`
**Commit:** f4d9542
**Applied fix:** Replaced `${BIND_VALUE// /}` (ASCII-space-only strip) with `${BIND_VALUE//[$' \t\r\n']/}` to cover tabs, CR, LF in addition to spaces. Added surrounding-quote strip (`#\"`/`%\"` and `#\'`/`%\'`) so dotenv-style `VIGIL_BIND_HOST="0.0.0.0"` also normalizes cleanly. Verification: `bash -n` passes, end-to-end `bash Scripts/preflight-check.sh` run still shows all 5 checks PASS on this iMac, and a targeted regression test on a synthesized CRLF + quoted `.env` confirmed raw `"0.0.0.0"\r` normalizes to literal `0.0.0.0` and matches the `== "0.0.0.0"` comparison.

### WR-03: .env.example ships VIGIL_BIND_HOST=0.0.0.0 uncommented

**Files modified:** `vigil-core/.env.example`
**Commit:** 6eda417
**Applied fix:** Commented out the active `VIGIL_BIND_HOST=0.0.0.0` line (now `#VIGIL_BIND_HOST=0.0.0.0`) so `dev-setup.sh` copying `.env.example` → `.env` lands on the documented safe default (unset → 127.0.0.1) rather than silently exposing :3001 on 0.0.0.0. Added a two-line "Uncomment to enable cross-machine access" preamble so opt-in is explicit. Verification: grep confirms no uncommented `^VIGIL_BIND_HOST=` line remains; the commented opt-in sits at line 68. The existing "Default (unset) = 127.0.0.1" doc comment on line 58 is now truthful instead of self-contradictory.

---

_Fixed: 2026-04-22T14:30:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
