---
phase: 107.2
slug: cross-machine-tailscale-dev-access-with-secure-bind-and-cors
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-21
---

# Phase 107.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> Validation Architecture details live in 107.2-RESEARCH.md § Validation Architecture. This file is the per-task verification map the planner + executor consume.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | shell grep + bash + curl (infra-hygiene phase — no runtime unit tests) |
| **Config file** | none — verifications are inline scripts |
| **Quick run command** | `bash scripts/preflight-check.sh` |
| **Full suite command** | `bash scripts/preflight-check.sh && bash scripts/dailybrief-doctor.sh` |
| **Estimated runtime** | ~3 seconds (preflight) / ~8 seconds (both) |

---

## Sampling Rate

- **After every task commit:** Run `bash scripts/preflight-check.sh` (post-task; fail-loud as designed)
- **After every plan wave:** Run both preflight + cross-machine curl smoke from RESEARCH.md § E2E Smoke Test
- **Before `/gsd-verify-work`:** Full suite green + one successful end-to-end request from MacBook Pro
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

Populated by the planner during step 8 — each task must declare:

- `<read_first>` files
- `<automated>` verification command (grep / curl / bash exit-code check)
- Requirement or threat ref

The gsd-planner agent fills this table during PLAN.md generation. Leave template rows so planner can replace.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 107.2-01-01 | 01 | TBD | TBD | TBD | TBD | TBD | TBD | ⬜ pending | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No framework installation needed — verifications are bash + grep + curl commands that already work on macOS.

Pre-existing infrastructure covers all phase requirements:
- `scripts/preflight-check.sh` (Phase 107.1 Plan 03) — extended with check 5 in this phase
- `scripts/dailybrief-doctor.sh` (Phase 107.1 Plan 07) — may gain Tailscale reachability row (Claude's Discretion)
- `curl` — already installed on macOS
- `/usr/libexec/ApplicationFirewall/socketfilterfw` — always present on macOS (not user-installable but no install needed)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| MacBook browser loads PWA from `http://jamesons-imac-2:5173` and completes a login | REQ-DEV-CROSS-MACHINE (new) | Requires second machine + browser; can't be scripted from iMac | 1) Ensure iMac `npm run dev` is running. 2) From MacBook Pro, open `http://jamesons-imac-2:5173` in Chrome/Safari. 3) Login with jamesonmorrill1@gmail.com / dev-password-1234. 4) Confirm thoughts tab + work orders tab render with seed data. 5) DevTools Network tab shows `/v1/*` requests returning 200 from same-origin (5173). |
| Ctrl+C on iMac kills both processes and frees :3001 + :5173 from MacBook's POV | D-E1 + D-B1 | Requires second machine | From MacBook: `curl http://jamesons-imac-2:3001/health` returns connection-refused after iMac Ctrl+C. |
| Prod FATAL boot-check (D-D2) actually fires on Railway | D-D2 | Requires Railway deploy | After Phase 107.2 ships: verify Railway has `CORS_ORIGINS` set. If unset, deploy should crash-loop with FATAL log. If accidentally unset in env dashboard, confirm the crash. |

*These 3 items go into `107.2-HUMAN-UAT.md` at verification time.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (none for this phase)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter after planner fills the verification map

**Approval:** pending
