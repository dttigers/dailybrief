---
status: complete
phase: 103-capture-repair-server-observability-foundations
source: [103-00-SUMMARY.md, 103-01-SUMMARY.md, 103-02-SUMMARY.md, 103-03-SUMMARY.md, 103-04-SUMMARY.md]
started: 2026-04-19T20:17:40Z
updated: 2026-04-19T20:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running vigil-core instance. Start vigil-core from scratch (npm run dev).
  Server boots without errors. No crash on startup, no missing-module errors.
  A GET /v1/health returns a live response. Specifically: the PostHog wiring (app.onError,
  shutdownPosthog imports) doesn't break startup — you should see the normal
  "[vigil-core] listening on port 3001" log with no stack traces.
result: blocked
blocked_by: other
reason: "FATAL: JWT_SECRET must be set — pre-existing local env requirement, not a Phase 103 regression. Railway cold start already passed: deployment succeeded and /v1/health polled healthy during Plan 04 Task 3."

### 2. HEIC Photo Upload (CAP-01 SC #1)
expected: |
  POST a real HEIC/HEIF file (e.g. an iPhone photo) to /v1/process-photo with
  Authorization: Bearer <your token> and Content-Type: application/json with
  {"image": "<base64-heic>", "mediaType": "image/heic"}.
  Response: HTTP 201. Body contains a "thoughts" array with at least one entry
  that has non-empty "content". No 400 ("unsupported media type") or 422 ("image
  conversion failed") — the HEIC is silently converted to JPEG before Claude
  processes it.
result: pass

### 3. Category Non-Null After Fix (CAP-02 SC #2)
expected: |
  POST any image to /v1/process-photo in commit mode (no ?preview=true).
  Response: HTTP 201. Every entry in the "thoughts" array has a non-null "category"
  field — one of: "task", "therapy", "idea", "reflection", "project".
  Before this phase all 5 category fields came back null; they should now be
  populated by the new per-thought parallel triage step.
result: pass

### 4. GET /v1/me with valid bearer (AUTH-08 SC #4)
expected: |
  GET https://api.vigilhub.io/v1/me with your bearer token.
  Response: HTTP 200, body = {"userId": "<integer as string>", "email": "<your email>"}.
  No extra fields (no passwordHash, no createdAt). Just those two.
result: pass

### 5. GET /v1/me without bearer (Pitfall 10 enforcement)
expected: |
  GET https://api.vigilhub.io/v1/me with NO Authorization header.
  Response: HTTP 401. The /v1/me endpoint is behind bearerAuth — it was
  intentionally NOT added to the exemption list that covers /v1/auth/login etc.
result: pass

### 6. PostHog production error event (ANLY-01 SC #3)
expected: |
  Open PostHog Cloud → your vigil-core-prod project → Errors (Error Tracking tab).
  You should see an exception event with message "phase-103-verification-throw"
  from today's session (around 20:05Z). It should have a stack trace pointing at
  the (now-reverted) debug-throw route, plus properties:
  - distinctId: "1" (your userId)
  - route: "/v1/debug-throw"
  - method: "GET"
  This confirms app.onError → captureException wiring is live on Railway.
result: pass

### 7. PostHog: local dev produces zero events (SC #5)
expected: |
  With POSTHOG_API_KEY NOT set in any local .env file, start vigil-core locally
  (npm run dev). Hit a few endpoints. Check PostHog Cloud live events view —
  zero events should appear from localhost. The D-10 key-absence gate means
  posthog === null locally, so all wrappers are no-ops.
result: skipped
reason: "Local dev blocked by JWT_SECRET env requirement; skipped for now"

## Summary

total: 7
passed: 5
issues: 0
pending: 0
skipped: 1
blocked: 1

## Gaps

[none]
