---
phase: 88
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-core/src/utils/date-window.ts
  - vigil-core/src/utils/date-window.test.ts
autonomous: true
requirements:
  - ROLLOVER-04
tags:
  - datetime
  - timezone
  - utility
  - backend
must_haves:
  truths:
    - "getCurrentWeekWindow(tz, now) returns the most recent Wednesday 00:00 in tz as start"
    - "getCurrentWeekWindow(tz, now) returns next Wednesday 00:00 in tz as end"
    - "When now IS Wednesday 00:00 in tz, start === now"
    - "getRollingDayWindow(tz, days, now) returns start at day-00:00 (today-days) in tz and end=now"
    - "Both functions are pure — no DB, no side effects, now is injectable"
    - "Helper correctly handles DST transitions (America/New_York spring-forward and fall-back weeks)"
    - "Helper correctly handles extreme offsets (Pacific/Kiritimati +14, Pacific/Pago_Pago -11)"
    - "Invalid tz strings throw a clear RangeError at helper entry (not swallowed)"
  artifacts:
    - path: "vigil-core/src/utils/date-window.ts"
      provides: "Pure date-window helpers for week and rolling-day windows"
      exports: ["getCurrentWeekWindow", "getRollingDayWindow"]
      min_lines: 60
    - path: "vigil-core/src/utils/date-window.test.ts"
      provides: "Unit tests for DW-01..DW-10 (Wed boundary, DST, tz offsets, now-injection)"
      min_lines: 100
  key_links:
    - from: "vigil-core/src/utils/date-window.ts"
      to: "Intl.DateTimeFormat.formatToParts"
      via: "wall-clock-in-tz resolution (no new dependency)"
      pattern: "Intl\\.DateTimeFormat"
    - from: "vigil-core/src/utils/date-window.test.ts"
      to: "vigil-core/src/utils/date-window.ts"
      via: "node:test + node:assert/strict import"
      pattern: "from \"\\./date-window\\.js\""
---

<objective>
Ship a pure, dependency-free date-window utility in `vigil-core/src/utils/date-window.ts` that exports two functions — `getCurrentWeekWindow(tz, now?)` and `getRollingDayWindow(tz, days, now?)` — each returning `{ start: Date; end: Date }`. Cover all Wed-boundary, DST, and tz-offset edges with `node:test` unit tests. This utility is the single source of truth for all v3.2 rollover/scope work and will be consumed by Phase 88 Plan 02 (GET /thoughts) and Phase 89 (Insights/Therapy).

Purpose: Establish correct, testable week-window math in user timezones without adding a date library (per D-05). Pure functions with injectable `now` (D-04) keep tests deterministic.

Output: Two new files in `vigil-core/src/utils/` — implementation + tests. No other code modified in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-date-window-helper-rollover/88-CONTEXT.md
@.planning/phases/88-date-window-helper-rollover/88-RESEARCH.md
@vigil-core/src/utils/token-crypto.ts
@vigil-core/src/utils/token-crypto.test.ts

<interfaces>
<!-- The two functions this plan must export. Downstream plans (88-02, Phase 89) import these directly. -->

From vigil-core/src/utils/date-window.ts (NEW — this plan creates):
```typescript
/** Most recent Wed 00:00 in tz through next Wed 00:00 in tz. */
export function getCurrentWeekWindow(
  tz: string,
  now?: Date,
): { start: Date; end: Date };

/** Last N days ending at now; start aligned to 00:00 in tz on (today - days). */
export function getRollingDayWindow(
  tz: string,
  days: number,
  now?: Date,
): { start: Date; end: Date };
```

Semantics (locked per CONTEXT.md D-01, D-02, D-04, D-05):
- `start` is INCLUSIVE, `end` is EXCLUSIVE (callers use `gte(createdAt, start)` + `lt(createdAt, end)`).
- When `now` is omitted, defaults to `new Date()` at call time.
- `tz` is any IANA timezone string (e.g. `"America/New_York"`, `"Pacific/Kiritimati"`).
- Invalid `tz` MUST throw a clear `RangeError` (do not catch; let `Intl.DateTimeFormat` throw).
- Zero new dependencies — native `Intl.DateTimeFormat` + `Date` only.
</interfaces>

<precedent>
`vigil-core/src/utils/token-crypto.ts` establishes the plain-function utility pattern used here (no classes, named exports, paired `.test.ts`).
`vigil-core/src/utils/token-crypto.test.ts` establishes the test pattern (`node:test` + `node:assert/strict`, `test("ID-NN: description", () => { ... })`).
</precedent>

<test_framework>
vigil-core uses `node:test` + `node:assert/strict` (NOT vitest — despite what 88-VALIDATION.md says, vigil-core/package.json test script is `tsx --test "src/**/*.test.ts"`). Run with `cd vigil-core && npm test`. Filter with `npm test -- --test-name-pattern "DW-"`.
</test_framework>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for date-window helpers</name>
  <files>vigil-core/src/utils/date-window.test.ts</files>
  <read_first>
    - vigil-core/src/utils/token-crypto.test.ts (exact test pattern — node:test, node:assert/strict, "test(\"ID-NN: …\", () => {})")
    - .planning/phases/88-date-window-helper-rollover/88-RESEARCH.md §"Validation Architecture" (DW-01..DW-10 cases)
    - .planning/phases/88-date-window-helper-rollover/88-RESEARCH.md §"DST Edge Cases"
    - vigil-core/package.json (confirm test script is `tsx --test "src/**/*.test.ts"`)
  </read_first>
  <behavior>
    Cover these named cases (use these exact test ID prefixes so they can be filtered with `--test-name-pattern "DW-"`):
    - DW-01: Thursday 2024-01-11T12:00:00Z in America/New_York → start === 2024-01-10T05:00:00.000Z (most recent Wed 00:00 ET = EST = UTC-5), end === 2024-01-17T05:00:00.000Z
    - DW-02: Wednesday 2024-01-10T05:00:00.000Z (Wed 00:00 ET) in America/New_York → start equals now (daysSinceWed === 0)
    - DW-03: Wednesday 2024-01-10T23:59:59Z in America/New_York (Wed 18:59:59 ET) → same start as DW-02 (still in current Wed)
    - DW-04: Tuesday 2024-01-16T23:59:59Z in America/New_York (Tue 18:59:59 ET) → start === 2024-01-10T05:00:00.000Z (6-day lookback max; still the same Wed as DW-02)
    - DW-05: Friday 2024-03-15T12:00:00Z in America/New_York (week AFTER spring-forward Sunday 2024-03-10) → start === 2024-03-13T04:00:00.000Z (Wed 00:00 EDT = UTC-4)
    - DW-06: Friday 2024-11-08T12:00:00Z in America/New_York (week AFTER fall-back Sunday 2024-11-03) → start === 2024-11-06T05:00:00.000Z (Wed 00:00 EST = UTC-5)
    - DW-07: Thursday 2024-01-11T12:00:00Z in Pacific/Kiritimati (+14, no DST) → start.getUTCDay() === 2 (Tuesday UTC) AND start.getUTCHours() === 10 (Wed 00:00 +14 = Tue 10:00 UTC)
    - DW-08: Thursday 2024-01-11T12:00:00Z in Pacific/Pago_Pago (-11, no DST) → start.getUTCDay() === 3 (Wednesday UTC) AND start.getUTCHours() === 11 (Wed 00:00 -11 = Wed 11:00 UTC)
    - DW-09: Same tz, two different `now` values → two different windows (proves `now` is injected, not captured)
    - DW-10: Same `now`, two different tz (America/New_York vs America/Los_Angeles) → start timestamps differ by exactly 3 hours (UTC-5 vs UTC-8, same calendar Wed)
    - DW-11: Invalid tz string `"Not/A_Zone"` → function throws RangeError (do not catch)
    - DW-12 (getRollingDayWindow): tz America/New_York, days=7, now=2024-01-11T12:00:00Z → end === now; start === 2024-01-04T05:00:00.000Z (Jan 4 00:00 ET = Jan 4 05:00 UTC, 7 days before Jan 11 ET)
    - DW-13 (getRollingDayWindow): tz UTC, days=7, now=2024-01-11T12:00:00Z → end === now; start === 2024-01-04T00:00:00.000Z
  </behavior>
  <action>
    Create `vigil-core/src/utils/date-window.test.ts`. Header imports mirror token-crypto.test.ts exactly:

    ```typescript
    import { test } from "node:test";
    import assert from "node:assert/strict";
    import { getCurrentWeekWindow, getRollingDayWindow } from "./date-window.js";
    ```

    Write one `test("DW-NN: …", () => { … })` block per case above (DW-01..DW-13). Use `assert.equal(window.start.toISOString(), "…")` for exact UTC comparisons, and `assert.equal(start.getUTCDay(), 2)` / `assert.equal(start.getUTCHours(), 10)` for the extreme-offset cases where the caller cares about UTC-side shape. For DW-11 use `assert.throws(() => getCurrentWeekWindow("Not/A_Zone", now), RangeError)`.

    For DW-09, pick any two `now` values one week apart in the same tz and assert `window1.start.getTime() !== window2.start.getTime()`.

    Do NOT write the implementation yet. Tests MUST fail on first `npm test` run (file does not yet export the functions → import resolution error). Commit is a RED commit.
  </action>
  <verify>
    <automated>cd vigil-core &amp;&amp; npm test -- --test-name-pattern "DW-" 2>&amp;1 | tail -20 || true  # MUST fail: "Cannot find module './date-window.js'" or equivalent</automated>
  </verify>
  <acceptance_criteria>
    - grep `"DW-01:"` vigil-core/src/utils/date-window.test.ts → match
    - grep `"DW-13:"` vigil-core/src/utils/date-window.test.ts → match
    - grep `import { getCurrentWeekWindow, getRollingDayWindow } from "./date-window.js"` vigil-core/src/utils/date-window.test.ts → match
    - `cd vigil-core && npm test -- --test-name-pattern "DW-"` → exits non-zero (tests fail because implementation does not exist yet) — this is the REQUIRED RED state
  </acceptance_criteria>
  <done>Test file exists with ≥13 DW-NN test blocks; tests currently fail because implementation file does not exist; file mirrors token-crypto.test.ts import style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getCurrentWeekWindow + getRollingDayWindow (tests go GREEN)</name>
  <files>vigil-core/src/utils/date-window.ts</files>
  <read_first>
    - vigil-core/src/utils/date-window.test.ts (from Task 1 — the tests are the spec)
    - vigil-core/src/utils/token-crypto.ts (plain-function utility style: named exports, no class, no default export)
    - .planning/phases/88-date-window-helper-rollover/88-RESEARCH.md §"Architecture Patterns" — Pattern 1 (Wed-anchored week via wall-clock parts), Pattern 2 (rolling day window)
    - .planning/phases/88-date-window-helper-rollover/88-RESEARCH.md §"Common Pitfalls" — Pitfall 1 (wallClockToUtc), Pitfall 4 (end is EXCLUSIVE)
    - vigil-core/src/routes/settings.ts (see `isValidTimezone` pattern — we rely on Intl throwing, we do not duplicate validation)
  </read_first>
  <action>
    Create `vigil-core/src/utils/date-window.ts` implementing exactly the interface in the plan's `<interfaces>` block. Use the "wall-clock parts in tz → binary-search to UTC" algorithm from RESEARCH.md Pattern 1. Required internal helpers (private, NOT exported):

    1. `function getWallClockParts(date: Date, tz: string): { year: number; month: number; day: number; hour: number; min: number; sec: number }` — uses `new Intl.DateTimeFormat('en-US', { timeZone: tz, year, month, day, hour, minute, second, hour12: false }).formatToParts(date)`.

    2. `function wallClockToUtc(year: number, month: number, day: number, hour: number, min: number, sec: number, tz: string): Date` — starts with `Date.UTC(year, month - 1, day, hour, min, sec)` and iterates up to 4 times, each iteration computing `diff = target_utc - actual_utc_from_wallclock_resolved_in_tz` and correcting `guess += diff` until it converges. For ambiguous fall-back times, natural convergence to the earlier occurrence is acceptable.

    3. `function dayOfWeekInTz(date: Date, tz: string): number` — returns 0=Sun..6=Sat using `Intl.DateTimeFormat('en-US', { timeZone: tz, weekday: 'short' })` mapped through `{ Sun:0, Mon:1, Tue:2, Wed:3, Thu:4, Fri:5, Sat:6 }`.

    Implement `getCurrentWeekWindow(tz, now = new Date())`:
      - Let `DOW_TO_DAYS_SINCE_WED = { 0:4, 1:5, 2:6, 3:0, 4:1, 5:2, 6:3 }` (Sun→4, Mon→5, Tue→6, Wed→0, Thu→1, Fri→2, Sat→3).
      - Get wall-clock parts of `now` in `tz`. Let `daysSinceWed = DOW_TO_DAYS_SINCE_WED[dayOfWeekInTz(now, tz)]`.
      - Compute a reference timestamp ≈ `now - daysSinceWed * 86_400_000`, get its wall-clock parts in `tz` → those are the `{year, month, day}` of the target Wed (the `day` field auto-handles month/year underflow because we re-resolve in tz).
      - `start = wallClockToUtc(wedYear, wedMonth, wedDay, 0, 0, 0, tz)`.
      - `end = wallClockToUtc(wedYear, wedMonth, wedDay + 7, 0, 0, 0, tz)` — `wallClockToUtc` must tolerate `day + 7` > month length; the internal `Date.UTC(year, month-1, day+7, …)` rolls over correctly.
      - Return `{ start, end }`.

    Implement `getRollingDayWindow(tz, days, now = new Date())`:
      - If `!Number.isFinite(days) || days < 0 || !Number.isInteger(days)`, throw `RangeError("days must be a non-negative integer")`.
      - `end = now` (NOT aligned to day boundary — per D-02 / RESEARCH Pattern 2).
      - `startApprox = new Date(now.getTime() - days * 86_400_000)`.
      - `parts = getWallClockParts(startApprox, tz)`.
      - `start = wallClockToUtc(parts.year, parts.month, parts.day, 0, 0, 0, tz)`.
      - Return `{ start, end }`.

    Invalid tz: do NOT catch. The first `new Intl.DateTimeFormat(..., { timeZone: tz })` call inside `getWallClockParts` will throw `RangeError`. That satisfies DW-11.

    Do not add any dependency imports. This file imports nothing.
  </action>
  <verify>
    <automated>cd vigil-core &amp;&amp; npm test -- --test-name-pattern "DW-"</automated>
  </verify>
  <acceptance_criteria>
    - grep `export function getCurrentWeekWindow` vigil-core/src/utils/date-window.ts → match
    - grep `export function getRollingDayWindow` vigil-core/src/utils/date-window.ts → match
    - grep `Intl\.DateTimeFormat` vigil-core/src/utils/date-window.ts → at least one match
    - grep `^import ` vigil-core/src/utils/date-window.ts → zero matches (no imports at all)
    - grep -E `(date-fns|dayjs|luxon|moment)` vigil-core/package.json → zero matches (no new dep added)
    - `cd vigil-core && npm test -- --test-name-pattern "DW-"` → exits 0, all DW-01..DW-13 pass
    - `cd vigil-core && npm run build` → exits 0 (TypeScript type-checks clean)
  </acceptance_criteria>
  <done>All DW-01..DW-13 tests pass. `npm run build` succeeds. No new dependencies added to package.json. File exports exactly `getCurrentWeekWindow` and `getRollingDayWindow`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → helper | `tz` string originates from `appSettings` DB row in route handlers. Single-user local DB. No end-user-supplied tz reaches here in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | `getCurrentWeekWindow` / `getRollingDayWindow` tz param | mitigate | Invalid tz string causes native `Intl.DateTimeFormat` to throw `RangeError` (verified by DW-11 test). Helper does not catch, so malformed tz surfaces immediately at the route boundary. No silent fallback to UTC. |
| T-88-02 | Denial of Service | `wallClockToUtc` binary search | mitigate | Loop is bounded to 4 iterations; converges in ≤3 for all real IANA zones (RESEARCH A2). No unbounded loop risk. |
| T-88-03 | Information Disclosure | Rollover scoping bypass (multi-user) | accept | Vigil is single-user local/Railway deployment (see PROJECT.md). No cross-user scoping model exists; week-window scoping is a view convenience, not an authorization boundary. Documented in CONTEXT.md. |
</threat_model>

<verification>
- `cd vigil-core && npm test` → full suite passes (all `DW-*` tests plus existing `token-crypto` tests)
- `cd vigil-core && npm run build` → clean TypeScript build
- `grep -r "getCurrentWeekWindow\|getRollingDayWindow" vigil-core/src` → only matches in date-window.ts and date-window.test.ts (no premature consumers leaked in)
</verification>

<success_criteria>
- vigil-core/src/utils/date-window.ts exports `getCurrentWeekWindow(tz, now?)` and `getRollingDayWindow(tz, days, now?)`.
- vigil-core/src/utils/date-window.test.ts includes DW-01..DW-13 (all pass under `node:test`).
- `now` is injectable with optional default `new Date()`.
- No new dependencies in vigil-core/package.json.
- Invalid tz surfaces as `RangeError` (not swallowed).
- Wed-boundary math is correct for DST weeks in America/New_York (DW-05, DW-06) and for extreme offsets Pacific/Kiritimati +14 (DW-07) and Pacific/Pago_Pago -11 (DW-08).
</success_criteria>

<output>
After completion, create `.planning/phases/88-date-window-helper-rollover/88-01-SUMMARY.md` documenting:
- Exact algorithm chosen for `wallClockToUtc` (binary-search iteration count observed)
- Whether `lt` vs `lte` semantics require any caller-side doc changes (note `end` is EXCLUSIVE so Plan 02 must use `lt`)
- Any test cases that surfaced unexpected behavior (and how resolved)
- Populate Per-Task Verification Map in `.planning/phases/88-date-window-helper-rollover/88-VALIDATION.md` for DW-01..DW-13.
</output>
