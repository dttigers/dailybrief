---
phase: 46-remove-dual-code-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Storage/DatabaseManager.swift
  - Sources/JarvisCore/Storage/ThoughtStore.swift
  - Sources/JarvisCore/Services/SyncService.swift
  - Sources/JarvisCore/Services/CloudKitManager.swift
  - Sources/JarvisCore/Services/FolderWatcherService.swift
  - Sources/JarvisCore/Models/Thought.swift
  - Sources/JarvisCore/Models/ThoughtLink.swift
  - Package.swift
autonomous: true
---

<objective>
Remove the local GRDB/SQLite storage layer, CloudKit sync, and folder watcher — all local-only infrastructure that is replaced by the Vigil Core API backend.

Purpose: Eliminate dead code paths that are no longer used now that the API backend is the primary data layer.
Output: Clean JarvisCore with no GRDB dependency, no CloudKit sync, no local-only storage files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Storage/DatabaseManager.swift
@Sources/JarvisCore/Storage/ThoughtStore.swift
@Sources/JarvisCore/Storage/ThoughtRepository.swift
@Sources/JarvisCore/Services/SyncService.swift
@Sources/JarvisCore/Services/CloudKitManager.swift
@Sources/JarvisCore/Services/FolderWatcherService.swift
@Sources/JarvisCore/Models/Thought.swift
@Sources/JarvisCore/Models/ThoughtLink.swift
@Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete local-only storage and sync files</name>
  <files>
    Sources/JarvisCore/Storage/DatabaseManager.swift,
    Sources/JarvisCore/Storage/ThoughtStore.swift,
    Sources/JarvisCore/Services/SyncService.swift,
    Sources/JarvisCore/Services/CloudKitManager.swift,
    Sources/JarvisCore/Services/FolderWatcherService.swift
  </files>
  <action>
    Delete these 5 files entirely:
    1. `DatabaseManager.swift` — SQLite/GRDB lifecycle, migrations, connection pooling. Replaced by PostgreSQL on Railway.
    2. `ThoughtStore.swift` — GRDB-backed ThoughtRepository implementation (~700 lines). Replaced by APIThoughtStore.
    3. `SyncService.swift` — CloudKit bidirectional sync. No longer needed with server-side storage.
    4. `CloudKitManager.swift` — CloudKit container/zone management. No longer needed.
    5. `FolderWatcherService.swift` — DispatchSource folder monitoring that uses concrete ThoughtStore. Cannot work with API backend and is not essential (users capture via dashboard/capture panel).

    These files are purely local-mode infrastructure with no references from API-backed code paths.
  </action>
  <verify>
    Confirm files are deleted:
    ```
    ! test -f Sources/JarvisCore/Storage/DatabaseManager.swift
    ! test -f Sources/JarvisCore/Storage/ThoughtStore.swift
    ! test -f Sources/JarvisCore/Services/SyncService.swift
    ! test -f Sources/JarvisCore/Services/CloudKitManager.swift
    ! test -f Sources/JarvisCore/Services/FolderWatcherService.swift
    ```
  </verify>
  <done>All 5 local-only files deleted</done>
</task>

<task type="auto">
  <name>Task 2: Remove GRDB from models and Package.swift</name>
  <files>
    Sources/JarvisCore/Models/Thought.swift,
    Sources/JarvisCore/Models/ThoughtLink.swift,
    Package.swift
  </files>
  <action>
    **Thought.swift:**
    - Remove `import GRDB`
    - Remove `FetchableRecord` and `MutablePersistableRecord` conformances from the Thought struct
    - Remove the GRDB `Columns` enum (used for GRDB column references)
    - Remove CloudKit-specific fields: `cloudKitRecordID`, `syncStatus`, `lastSyncedAt`
    - Remove the `SyncStatus` enum if it exists in this file
    - Remove any GRDB-specific `init(row:)` and `encode(to:)` methods
    - Keep all other fields (id, content, source, category, confidence, taskStatus, therapyClassification, tags, isFavorite, linkedThoughtIds, createdAt, updatedAt) — these are used by both API and protocol consumers
    - Keep Codable, Sendable, Identifiable, Equatable conformances
    - Keep CaptureSource, ThoughtCategory, TaskStatus, TherapyClassification enums (used everywhere)

    **ThoughtLink.swift:**
    - Remove `import GRDB`
    - Remove `FetchableRecord`, `PersistableRecord` conformances
    - Remove any GRDB-specific `Columns` enum, `init(row:)`, `encode(to:)` methods
    - Keep the struct with its fields and Codable/Sendable conformances

    **Package.swift:**
    - Remove the GRDB.swift dependency from the `dependencies` array (line 12)
    - Remove `.product(name: "GRDB", package: "GRDB.swift")` from JarvisCore target dependencies (line 18)
    - Remove `.linkedFramework("CloudKit")` from JarvisCore linkerSettings (line 22) — CloudKitManager is deleted, no more CloudKit usage in JarvisCore
  </action>
  <verify>
    Run `swift build 2>&1 | head -50` — expect compilation errors from AppDelegate.swift and DailyBrief.swift (they still reference deleted types like ThoughtStore, DatabaseManager, SyncService, etc.). These will be fixed in Plans 03 and 04. The key verification is that Thought.swift, ThoughtLink.swift, and Package.swift themselves are syntactically valid and the GRDB dependency is gone.

    Check: `grep -r "import GRDB" Sources/` should return NO results.
    Check: `grep "GRDB" Package.swift` should return NO results.
    Check: `grep "CloudKit" Package.swift` should return NO results.
  </verify>
  <done>
    GRDB completely removed from the project. Thought and ThoughtLink models are clean data structs without database-specific conformances. CloudKit linker setting removed. Build will have expected errors in consumer files (AppDelegate, DailyBrief) that are addressed in subsequent plans.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 local-only files deleted
- [ ] `grep -r "import GRDB" Sources/` returns nothing
- [ ] `grep "GRDB" Package.swift` returns nothing
- [ ] `grep "CloudKit" Package.swift` returns nothing
- [ ] Thought.swift compiles as a standalone struct (no GRDB/CloudKit references)
- [ ] ThoughtLink.swift compiles as a standalone struct
</verification>

<success_criteria>
- All local storage infrastructure removed
- GRDB dependency fully eliminated from project
- Models retain all API-relevant fields
- No compile errors in modified files themselves (consumer errors expected, fixed in Plans 03/04)
</success_criteria>

<output>
After completion, create `.planning/phases/46-remove-dual-code-paths/46-01-SUMMARY.md`
</output>
