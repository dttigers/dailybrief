---
phase: 32-g2-scaffold-home
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - vigil-g2-plugin/src/main.ts
  - vigil-g2-plugin/src/screens/home.ts
  - vigil-g2-plugin/src/types.ts
  - vigil-g2-plugin/src/constants.ts
autonomous: true
---

<objective>
Build the home screen with mock data showing key Vigil information on the G2 display.

Purpose: Create the first real screen for the glasses plugin, demonstrating the layout system with mock data matching Vigil API response shapes. This establishes the screen rendering pattern for Phase 33.
Output: A home screen showing task count, top priority item, and daily affirmation in the 576x288 greyscale display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-g2-scaffold-home/DISCOVERY.md
@.planning/phases/32-g2-scaffold-home/32-01-SUMMARY.md
@vigil-core/src/routes/summary.ts
@vigil-core/src/routes/affirmation.ts
@vigil-core/src/routes/prioritize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create types, constants, and home screen renderer</name>
  <files>vigil-g2-plugin/src/types.ts, vigil-g2-plugin/src/constants.ts, vigil-g2-plugin/src/screens/home.ts</files>
  <action>
1. Create `src/types.ts` with TypeScript types matching the Vigil API response shapes the plugin will consume:
   - `VigilSummary` — matches GET /v1/summary response: `{ total, byCategory, tasksByStatus, favorites, linkedThoughts, recent }`
   - `VigilAffirmation` — matches GET /v1/affirmation response: `{ affirmation: string }`
   - `VigilPrioritized` — matches POST /v1/prioritize response: `{ prioritized: Array<{ id, content, priority, reasoning }> }`
   - Keep types minimal — only what the home screen needs

2. Create `src/constants.ts` with display constants:
   - `DISPLAY_WIDTH = 576`, `DISPLAY_HEIGHT = 288`
   - `CHARS_PER_LINE = 32` (safe estimate for the greyscale display)
   - `DIVIDER = "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"` (full-width divider)
   - Container IDs enum: `HOME_HEADER = 1, HOME_BODY = 2, HOME_FOOTER = 3`

3. Create `src/screens/home.ts` with a `buildHomeScreen()` function that:
   - Takes no arguments (uses mock data internally for now)
   - Returns a container object suitable for `createStartUpPageContainer` or `rebuildPageContainer`
   - Layout (3 text containers, stacked vertically):

     **Header container** (ID: 1, y: 0, h: 40):
     ```
     VIGIL              04:32 PM
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     ```

     **Body container** (ID: 2, y: 40, h: 210, isEventCapture: 1):
     ```
     ▲ 3 tasks pending
     
     TOP PRIORITY:
     Fix HVAC unit at Store #142
     
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     You've been handling a lot of
     competing priorities today.
     That takes real skill.
     ```

     **Footer container** (ID: 3, y: 250, h: 38):
     ```
     ↓ swipe for work orders
     ```

   - Use mock data: 3 pending tasks, a sample work order as top priority, a sample affirmation
   - Export `MOCK_SUMMARY`, `MOCK_AFFIRMATION` as named exports so they can be swapped for real API data in Phase 33
   - The function should format content strings with `\n` line breaks and Unicode decorators

Do NOT use list containers yet — those are for the work orders screen in Phase 33. The home screen is all text containers.
  </action>
  <verify>
   - `cd vigil-g2-plugin && npx tsc --noEmit` passes (type check)
   - `src/screens/home.ts` exports `buildHomeScreen` function
   - Container structure has exactly 3 containers with valid IDs, positions within 576x288 bounds
  </verify>
  <done>Home screen renderer produces valid container layout with mock data, types match Vigil API shapes</done>
</task>

<task type="auto">
  <name>Task 2: Wire home screen into main.ts with lifecycle handling</name>
  <files>vigil-g2-plugin/src/main.ts</files>
  <action>
Update `src/main.ts` to:

1. Import `buildHomeScreen` from `./screens/home.ts`
2. Import `waitForEvenAppBridge` from the SDK
3. In the `init()` function:
   a. Get bridge via `waitForEvenAppBridge()`
   b. Build home screen container via `buildHomeScreen()`
   c. Call `bridge.createStartUpPageContainer()` with the home screen container
   d. Set up a basic event listener via `bridge.onEvenHubEvent()` that:
      - On `FOREGROUND_ENTER` (sysEvent type 4): log "Vigil foregrounded" (data refresh will be added in Phase 33)
      - On `FOREGROUND_EXIT` (sysEvent type 5): log "Vigil backgrounded"
      - Ignore all other events for now (navigation is Phase 33)
   e. Log "Vigil G2 plugin ready" to console

Keep it simple — no data fetching, no screen transitions, no timers. Just render the home screen and handle lifecycle.
  </action>
  <verify>
   - `cd vigil-g2-plugin && npm run build` succeeds
   - `main.ts` calls `buildHomeScreen()` and passes result to `createStartUpPageContainer`
   - Event listener registered for lifecycle events
  </verify>
  <done>Home screen renders on startup, lifecycle events logged, build passes clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd vigil-g2-plugin && npm run build` succeeds
- [ ] No TypeScript errors (`npx tsc --noEmit`)
- [ ] Home screen layout uses valid container positions (within 576x288)
- [ ] All containers have valid IDs and exactly one has isEventCapture=1
- [ ] Mock data types match Vigil API response shapes
- [ ] Footer shows navigation hint for Phase 33
</verification>

<success_criteria>

- All tasks completed
- Home screen renders 3 text containers with Vigil-branded content
- Mock data structure matches real API shapes for easy swap
- Lifecycle events handled (foreground enter/exit)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/32-g2-scaffold-home/32-02-SUMMARY.md`
</output>
