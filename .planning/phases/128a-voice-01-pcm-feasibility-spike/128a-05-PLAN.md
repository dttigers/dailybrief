---
phase: 128a
plan: 05
type: execute
wave: 3
depends_on: [128a-02, 128a-04]
files_modified: []
autonomous: false
requirements: [VOICE-01]
wallclock: true
must_haves:
  truths:
    - "vigil.ehpk plugin pack output excludes scripts/voice-spike-*.ts (CONTEXT D-A3 bundle exclusion verified)"
    - "OPENAI_API_KEY is set in Railway vigil-core env (verified via railway variables get, NOT full dump)"
    - "Even Hub developer portal allowlist accepts g2-microphone permission"
    - "If portal rejects g2-microphone, the spike returns BLOCK in the SPIKE-DECISION (D-G2 + D-BLOCK)"
  artifacts:
    - path: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md"
      provides: "Operator records: npm run pack output bundle inspection result; C-1 verification output; C-2 portal verdict"
      contains: "C-1"
  key_links:
    - from: "operator (wallclock)"
      to: "Railway vigil-core env"
      via: "railway variables get OPENAI_API_KEY (NOT full dump — railway variables leak memory)"
      pattern: "OPENAI_API_KEY"
    - from: "operator (wallclock)"
      to: "Even Hub developer portal"
      via: "portal UI confirms g2-microphone allowlist; if rejected, write BLOCK verdict immediately"
      pattern: "g2-microphone"
---

<objective>
Pre-hardware gates: verify spike code does NOT pollute the plugin pack bundle (D-A3); set OPENAI_API_KEY in Railway env (C-1); verify Even Hub developer portal allows the g2-microphone permission (C-2). Two of three steps are operator wallclock — Claude cannot perform them.

Purpose: Prevent two failure modes before Plan 06 burns operator time: (a) spike code shipping in the production .ehpk bundle (would leak tossable code to users), (b) hardware test fails 30 minutes in because OPENAI_API_KEY was never set in Railway OR because the portal silently rejected g2-microphone. This plan also documents the BLOCK-verdict short-circuit if the portal rejects the permission (CONTEXT D-G2: "first 30 minutes records that as the BLOCK signal and exits early").

Output: SUMMARY.md captures the npm run pack inspection (Task 1 — autonomous), the railway variables get output (Task 2 — operator, redacted), and the portal allowlist verdict (Task 3 — operator). If C-2 fails, Plan 06 short-circuits into BLOCK verdict authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-RESEARCH.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-VALIDATION.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify npm run pack excludes spike code (D-A3)</name>
  <files></files>
  <read_first>
    - vigil-g2-plugin/package.json (find the `pack` script and the file/glob inclusions/exclusions; verify what npm run pack actually produces)
    - vigil-g2-plugin/scripts/check-verified.mjs (existing precedent for scripts/ files staying out of bundle)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-A3" (spike code NOT to ship in plugin pack)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128A-PATTERNS.md §2 gotcha (scripts/ directory exclusion via the pack glob)
  </read_first>
  <action>
    Run `cd vigil-g2-plugin && npm run pack` (creates the `.ehpk` bundle in `dist/` or wherever the pack script puts it — verify path during read_first by inspecting package.json's pack script). After pack completes: identify the output bundle path (`dist/Vigil*.ehpk` or `Vigil*.zip` — check the pack script output for the actual filename). Inspect the bundle contents using `unzip -l <bundle-path>` (an .ehpk is a zip per Even SDK convention). Grep the listing for the spike file basenames: `unzip -l <bundle> | grep -E 'voice-spike-encoder|voice-spike\.ts'`. Expected result: BOTH greps return zero matches — `scripts/voice-spike-encoder.ts` should NOT be in the bundle (per D-A3 + check-verified.mjs precedent of scripts/ directory exclusion), and `src/screens/voice-spike.ts` may or may not appear because src/ IS bundled at build time, but after esbuild the `voice-spike.ts` source filename is unlikely to appear directly — what matters is whether the COMPILED screen module ships. Document both findings in SUMMARY.md. If `scripts/voice-spike-encoder.ts` appears in the bundle listing, the pack script's exclusion glob is wrong — STOP and document this as a Plan defect, do NOT proceed to Task 2. If only the compiled screen module ships (and the raw scripts/ file does not), that's the expected D-A3 outcome — the screen module is acceptable in the bundle because operator needs it for the spike-on-hardware run; only the encoder helper and any raw TS in scripts/ must stay out.

    Also verify no `voice-spike-*.html` is present in the bundle (no HTML file should exist anywhere per RESEARCH DRIFT-01 — the screen is a TS module, NOT an HTML page). Grep: `unzip -l <bundle> | grep -E 'voice-spike.*\.html'` → expect zero results.

    Document in SUMMARY.md: bundle path, bundle size in bytes (`stat -f%z <bundle>` on macOS), the grep outputs (verbatim, including the zero-match cases), and the DELTA in bundle size vs the prior build (if reproducible — capture by stashing the spike branch's pack first, then pre-spike pack from main, OR document baseline size as "not measured" with rationale).
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npm run pack 2>&1 | tee /tmp/pack-128a.log && BUNDLE=$(find . -maxdepth 3 -name 'Vigil*.ehpk' -o -name 'Vigil*.zip' 2>/dev/null | head -1) && [ -n "$BUNDLE" ] && unzip -l "$BUNDLE" > /tmp/bundle-listing-128a.txt && (! grep -E 'voice-spike-encoder|voice-spike\.html' /tmp/bundle-listing-128a.txt) && echo "BUNDLE EXCLUSION VERIFIED: $BUNDLE"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run pack` exits 0; bundle artifact (.ehpk or .zip) created
    - `unzip -l <bundle> | grep -E 'voice-spike-encoder'` returns ZERO matches (scripts/voice-spike-encoder.ts must not ship per D-A3)
    - `unzip -l <bundle> | grep -E 'voice-spike.*\.html'` returns ZERO matches (no HTML file should exist anywhere per RESEARCH DRIFT-01)
    - The full bundle listing is saved to `/tmp/bundle-listing-128a.txt` AND quoted in `128a-05-SUMMARY.md` (Tail the file or include it as a fenced code block in the SUMMARY)
    - SUMMARY.md records bundle size (`stat -f%z <bundle>` output) and pack-script stdout summary
  </acceptance_criteria>
  <done>
    Pack bundle inspected; CONTEXT D-A3 confirmed via two zero-match greps; bundle listing archived for Phase 130 reference (when the productionization re-checks the same exclusion).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: OPERATOR WALLCLOCK C-1 — Set OPENAI_API_KEY in Railway vigil-core env</name>
  <files>.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md</files>
  <action>OPERATOR-ONLY (Claude cannot execute — wallclock per `[feedback_wallclock_checkpoint_exempt]` memory). Operator (1) generates an OpenAI API key at platform.openai.com labeled `vigil-core spike 128a`; (2) sets it in Railway via Dashboard (Project: vigil-core → Variables → add `OPENAI_API_KEY`) OR via CLI `railway variables --set "OPENAI_API_KEY=<key>"`; (3) verifies via `railway variables get OPENAI_API_KEY` (subcommand form — NEVER bare `railway variables` which dumps all secrets per `[railway variables leak]` memory and has rotated Postgres twice); (4) waits ≤2min for redeploy; (5) hits `https://api.vigilhub.io/health` (or the actual health endpoint — operator confirms by grep in vigil-core/src/index.ts) expecting HTTP 200; (6) records timestamp + last-4-chars of the key + redeploy status in `128a-05-SUMMARY.md` "C-1: OPENAI_API_KEY" section. NEVER paste the full key into SUMMARY.md or any committed file.</action>
  <verify>Operator confirms via resume signal that `railway variables get OPENAI_API_KEY` (subcommand form) returned the expected key and `/health` returned 200. SUMMARY.md "C-1: OPENAI_API_KEY" section exists with last-4-chars only (regex check: `grep -c 'sk-proj-[A-Za-z0-9_-]\{20,\}' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md` returns 0 — no full keys committed).</verify>
  <done>OPENAI_API_KEY set in Railway vigil-core env; redeploy complete; /health green; SUMMARY.md records timestamp + last-4-chars; full key never committed.</done>
  <what-built>Plan 02 shipped a route that reads `process.env.OPENAI_API_KEY`. Without this env var, the route returns the lazy-init warning + throws "OPENAI_API_KEY not configured" → 502. Operator must set the key BEFORE the hardware spike (Plan 06) so the round-trip works.</what-built>
  <read_first>
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-W4" (operator action; use `railway variables get`, NOT full dump)
    - Auto-memory `[Anthropic key sprawl]` — drift pattern applies to OPENAI_API_KEY
    - Auto-memory `[Railway variables leak]` — full `railway variables` dump LEAKS ALL SECRETS; has rotated Postgres twice; never use it
    - Memory `project_railway_deploy` — vigil-core Root Directory is `vigil-core`; api.vigilhub.io custom domain live 2026-04-08
  </read_first>
  <how-to-verify>
    OPERATOR ACTION (Claude CANNOT execute — wallclock per `[feedback_wallclock_checkpoint_exempt]`):

    Step 1 — Obtain or generate the API key. Source: OpenAI dashboard (platform.openai.com → API keys → create new key labeled `vigil-core spike 128a`). Key shape: `sk-proj-...` (legacy `sk-...` also works). Operator records the key in the OS keychain (NOT in any repo file).

    Step 2 — Set the env var on Railway. Choose ONE of:
      - Option A (Dashboard): railway.com → Project: vigil-core → Variables tab → Add `OPENAI_API_KEY` = `<key>`. Click "Save". This triggers an automatic redeploy.
      - Option B (CLI): from a terminal where `railway login` is authenticated and the working directory is `vigil-core/`, run `railway variables --set "OPENAI_API_KEY=<key>"`. This redeploys automatically.

    Step 3 — Verify WITHOUT leaking. Run `railway variables get OPENAI_API_KEY` (note: subcommand `get`, NOT bare `railway variables` which dumps EVERYTHING per the `[railway variables leak]` memory and has caused two Postgres rotations). Expected output: `OPENAI_API_KEY=sk-...` (or similar; the value will be displayed but ONLY this one variable, no other secrets). DO NOT paste the full key into the SUMMARY — record only `Set: yes, sk-…<last-4-chars>` (last 4 chars only for forensic identification).

    Step 4 — Confirm Railway redeploy. Wait ≤2 minutes; visit `https://api.vigilhub.io/health` (or whatever the existing health endpoint is — verify by greping vigil-core/src/index.ts for a `/health` route) and confirm HTTP 200. Optionally trigger a one-time test against `/v1/voice/transcribe` with a known-valid base64 WAV using the operator's existing dev bearer token: expect HTTP 201 (success) OR HTTP 502 with "AI transcription failed" (if the key is invalid — re-check the value).

    Step 5 — Record in SUMMARY.md: timestamp of the env set, redeploy completion (yes/no), and the key suffix (last 4 chars only). DO NOT paste the full key. DO NOT run `railway variables` (bare command). DO NOT screenshot the dashboard with secrets visible.
  </how-to-verify>
  <acceptance_criteria>
    - `128a-05-SUMMARY.md` contains a section titled "C-1: OPENAI_API_KEY" with: timestamp of env set, last-4-chars of the key (e.g., `…1234`), redeploy completion (yes), and a `/health` verification line
    - SUMMARY.md does NOT contain the full key (grep `sk-` in SUMMARY.md should match only the redacted `…<last-4>` form, never a full `sk-proj-...` or `sk-...` string)
    - Operator confirms in the resume signal: command used was `railway variables get OPENAI_API_KEY` (subcommand form), NOT bare `railway variables`
  </acceptance_criteria>
  <resume-signal>
    Type one of:
      - `c1-done: <last-4-chars> redeploy-ok` — proceeds to Task 3
      - `c1-blocked: <reason>` — Plan 06 short-circuits to BLOCK verdict (no key, no spike)
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: OPERATOR WALLCLOCK C-2 — Verify g2-microphone permission in Even Hub developer portal</name>
  <files>.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md</files>
  <action>OPERATOR-ONLY (portal UI; Claude cannot execute). Operator (1) opens Even Realities developer portal (same portal used for prior v0.3.6 plugin submissions); (2) navigates to app `com.vigilapp.g2` → Permissions section; (3) confirms `g2-microphone` shows ALLOWED/APPROVED; (4) records portal URL, app_id, status (ALLOWED / REQUIRES REVIEW / BLOCKED / NOT_LISTED), and timestamp in `128a-05-SUMMARY.md` "C-2: g2-microphone portal verification" section. If status is anything OTHER than ALLOWED, ALSO write "Plan 06 disposition: short-circuit-block" in SUMMARY.md so Plan 06's executor skips C-3/C-4/C-5 and authors a BLOCK verdict directly per CONTEXT D-G2 + D-BLOCK.</action>
  <verify>Operator confirms via resume signal one of: c2-allowed | c2-rejected | c2-not-listed. SUMMARY.md "C-2: g2-microphone portal verification" section exists with the four required fields (portal URL, app_id, status, timestamp). If status ≠ ALLOWED, SUMMARY.md ALSO contains the literal string "Plan 06 disposition: short-circuit-block".</verify>
  <done>Portal verification recorded; Plan 06's executor has an unambiguous disposition (proceed-hardware OR short-circuit-block) to read from SUMMARY.md.</done>
  <what-built>Plan 03 manifested the g2-microphone permission in app.json. Operator must confirm Even Hub developer portal accepts the permission BEFORE plugin packs to hardware — per D-G2, portal rejection is a Plan 06 short-circuit straight to BLOCK verdict.</what-built>
  <read_first>
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-G2" + §"D-BLOCK" (portal rejection ⇒ BLOCK verdict in first 30 minutes; spike exits early)
    - vigil-g2-plugin/app.json (verify the g2-microphone entry from Plan 03 actually landed)
    - .planning/research/EVEN-SKILLS.md §"Audio capture" lines 94-118 (canonical g2-microphone permission name)
  </read_first>
  <how-to-verify>
    OPERATOR ACTION (Claude CANNOT execute — portal UI only):

    Step 1 — Open Even Realities developer portal (URL — operator confirms from existing plugin docs; the portal is the same UI used to submit prior plugin versions including v0.3.6).

    Step 2 — Navigate to the Vigil plugin (`com.vigilapp.g2`) → Permissions section.

    Step 3 — Confirm `g2-microphone` appears in the allowlist for this app_id. If the portal shows g2-microphone as ALLOWED/APPROVED for `com.vigilapp.g2`: C-2 PASSES → resume signal `c2-allowed`.

    Step 4 (alternative) — If the portal shows g2-microphone as REQUIRES REVIEW or BLOCKED: C-2 FAILS → resume signal `c2-rejected: <portal copy>`. Plan 06 immediately authors a BLOCK verdict in 128a-SPIKE-DECISION.md per D-G2 + D-BLOCK with the portal screenshot URL (or copy) as evidence.

    Step 5 (alternative) — If g2-microphone is not even listed in the portal's permissions catalog for v3.x apps: C-2 FAILS hard → BLOCK. The permission doesn't exist at the SDK level for Vigil's submission tier; spike cannot proceed. Document in SUMMARY.md as "permission unavailable" — re-eval in v3.10+ when Even ships the SDK surface.

    Step 6 — Record in SUMMARY.md under "C-2: g2-microphone portal verification": portal URL, app_id verified, permission status (ALLOWED / REQUIRES REVIEW / BLOCKED / NOT_LISTED), timestamp.
  </how-to-verify>
  <acceptance_criteria>
    - `128a-05-SUMMARY.md` contains a section titled "C-2: g2-microphone portal verification" with: portal URL, app_id (`com.vigilapp.g2`), status (ALLOWED / REQUIRES REVIEW / BLOCKED / NOT_LISTED), timestamp
    - Operator's resume signal explicitly states the portal status (one of: `c2-allowed`, `c2-rejected: <reason>`, `c2-not-listed: <reason>`)
    - If status is anything other than ALLOWED, SUMMARY.md ALSO marks "Plan 06 short-circuit: AUTHOR BLOCK VERDICT IMMEDIATELY" so Plan 06's executor reads the signal and skips the hardware-run tasks
  </acceptance_criteria>
  <resume-signal>
    Type one of:
      - `c2-allowed` — proceeds to Plan 06 hardware-run sequence
      - `c2-rejected: <portal copy>` — Plan 06 short-circuits to BLOCK verdict authoring (skip C-3/C-4/C-5)
      - `c2-not-listed: <reason>` — Plan 06 short-circuits to BLOCK verdict authoring
  </resume-signal>
</task>

</tasks>

<verification>
- Bundle exclusion verified (Task 1 autonomous): `unzip -l <bundle> | grep voice-spike-encoder` returns nothing
- Bundle listing archived at `/tmp/bundle-listing-128a.txt` AND echoed into `128a-05-SUMMARY.md`
- C-1 SUMMARY entry contains the last-4-chars of OPENAI_API_KEY (full key never written)
- C-2 SUMMARY entry contains explicit portal status
- If C-2 = anything but ALLOWED, SUMMARY contains "Plan 06 short-circuit: AUTHOR BLOCK VERDICT IMMEDIATELY"
</verification>

<success_criteria>
- npm run pack bundle confirmed to exclude scripts/voice-spike-encoder.ts (D-A3 satisfied)
- No HTML files for voice-spike anywhere in the bundle (RESEARCH DRIFT-01 confirmed)
- OPENAI_API_KEY set in Railway vigil-core env; redeploy completes; /health returns 200
- g2-microphone permission verified ALLOWED in Even Hub portal — OR a BLOCK signal is captured for Plan 06 short-circuit
- All three checkpoints documented in `128a-05-SUMMARY.md` with redaction discipline (no secrets in plaintext)
</success_criteria>

<output>
After completion, create `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md` with three sections:
- "Task 1 — Bundle exclusion (D-A3)": pack output, bundle path + size, grep results for `voice-spike-encoder` and `voice-spike.*\.html`
- "C-1 — OPENAI_API_KEY": timestamp, last-4-chars of key, redeploy status, /health check result
- "C-2 — g2-microphone portal verification": portal URL, app_id, status, timestamp
- "Plan 06 disposition": one of `proceed-hardware` (both C-1 + C-2 green) OR `short-circuit-block` (C-2 ≠ ALLOWED — Plan 06 authors BLOCK without burning operator wallclock on hardware)
</output>
