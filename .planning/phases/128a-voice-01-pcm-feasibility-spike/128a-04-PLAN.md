---
phase: 128a
plan: 04
type: execute
wave: 2
depends_on: [128a-02, 128a-03]
files_modified:
  - vigil-g2-plugin/src/navigation.ts
  - vigil-g2-plugin/src/main.ts
autonomous: true
requirements: [VOICE-01]
must_haves:
  truths:
    - "Screen.VOICE_SPIKE enum entry exists with value 'voice-spike'"
    - "SCREEN_ORDER includes VOICE_SPIKE positioned AFTER AFFIRMATION (carousel: HOME→COMPANION→WORK_ORDERS→AFFIRMATION→VOICE_SPIKE)"
    - "buildScreen switch routes Screen.VOICE_SPIKE → buildVoiceSpikeScreen(getRecording())"
    - "DOUBLE_CLICK on VOICE_SPIKE intercepted BEFORE the default NAV_EVENTS → HOME fallback — calls toggleVoiceSpikeRecording(bridge) instead"
    - "Carve-out mirrors the Companion D-08 pattern at navigation.ts lines 219-238"
    - "audioEvent collector wired in main.ts onEvenHubEvent dispatcher: calls appendPcmChunk(event.audioEvent.audioPcm) and logs chunk_n + bytes with GUARD-01-safe keys"
    - "Plugin still compiles; all existing tests stay green"
  artifacts:
    - path: "vigil-g2-plugin/src/navigation.ts"
      provides: "Screen.VOICE_SPIKE registration, SCREEN_ORDER entry, buildScreen case, DOUBLE_CLICK carve-out for VOICE_SPIKE"
      contains: "VOICE_SPIKE"
    - path: "vigil-g2-plugin/src/main.ts"
      provides: "audioEvent collector branch that calls appendPcmChunk"
      contains: "appendPcmChunk"
  key_links:
    - from: "vigil-g2-plugin/src/navigation.ts"
      to: "vigil-g2-plugin/src/screens/voice-spike.ts"
      via: "static import { buildVoiceSpikeScreen, getRecording, toggleVoiceSpikeRecording } from './screens/voice-spike.ts'"
      pattern: "from\\s+['\"]\\./screens/voice-spike"
    - from: "vigil-g2-plugin/src/main.ts"
      to: "vigil-g2-plugin/src/screens/voice-spike.ts"
      via: "import { appendPcmChunk } from './screens/voice-spike.ts'; called inside the audioEvent branch of onEvenHubEvent"
      pattern: "appendPcmChunk"
---

<objective>
Wire the Plan 03 assets into the carousel + event router. After this plan, an operator can swipe to the Voice Spike screen, DOUBLE_CLICK to start recording, see PCM chunks accumulate in the on-screen counter, DOUBLE_CLICK again to stop and POST the WAV to /v1/voice/transcribe.

Purpose: Keep the navigation/event-routing patches separate from the asset creation (Plan 03) so reviewers can audit the carve-out logic — which is the highest-risk change in the spike — in isolation. The Companion D-08 pattern at navigation.ts:219-238 is the template; this plan mirrors it for VOICE_SPIKE.

Output: navigation.ts gains the Screen.VOICE_SPIKE enum entry + SCREEN_ORDER entry + buildScreen case + DOUBLE_CLICK carve-out. main.ts gains the audioEvent collector branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-RESEARCH.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-PATTERNS.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-VALIDATION.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-UI-SPEC.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-03-SUMMARY.md

<interfaces>
<!-- Existing navigation/event-router contracts -->

From vigil-g2-plugin/src/navigation.ts (current state — verify line numbers during read_first):
  - Lines 12-23: static imports for screen builders (PATTERN: use static, NOT dynamic — Phase 125 Hermes fix)
  - Lines 27-33: `export const Screen = { HOME: 'home', COMPANION: 'companion', WORK_ORDERS: 'work-orders', AFFIRMATION: 'affirmation', TASK_DETAIL: 'task-detail' } as const`
  - Lines 37-42: `const SCREEN_ORDER: readonly ScreenName[] = [Screen.HOME, Screen.COMPANION, Screen.WORK_ORDERS, Screen.AFFIRMATION]`
  - Lines 58-122: `async function buildScreen(screen: ScreenName)` switch
  - Lines 184-257: `handleNavEvent(eventType, bridge)` — Companion DOUBLE_CLICK carve-out lives at lines 219-238 as the template

From vigil-g2-plugin/src/main.ts (current state — verify line numbers during read_first):
  - ~Lines 221-260: `bridge.onEvenHubEvent((event) => { ... })` dispatcher
  - Currently routes: listEvent CLICK_EVENT → navigateToTaskDetail; listEvent NAV_EVENTS → handleNavEvent; textEvent NAV_EVENTS → handleNavEvent
  - DOES NOT currently handle `event.audioEvent` — this plan adds the audioEvent branch

From vigil-g2-plugin/src/screens/voice-spike.ts (created in Plan 03):
```typescript
export function buildVoiceSpikeScreen(isRecording: boolean): RebuildPageContainer;
export function getRecording(): boolean;
export function appendPcmChunk(chunk: Uint8Array): void;
export async function toggleVoiceSpikeRecording(bridge: EvenAppBridge): Promise<void>;
```

From @evenrealities/even_hub_sdk:
  - `OsEventTypeList.DOUBLE_CLICK_EVENT` — is in NAV_EVENTS set (navigation.ts:69 per PATTERNS.md §5)
  - Without a carve-out, the default fallback maps DOUBLE_CLICK → navigate to HOME (navigation.ts:249-251 per PATTERNS.md)
  - `event.audioEvent?.audioPcm` — Uint8Array; fires ≥10× per second during active recording per STACK §1b
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register Screen.VOICE_SPIKE in navigation.ts (enum + SCREEN_ORDER + buildScreen)</name>
  <files>vigil-g2-plugin/src/navigation.ts</files>
  <read_first>
    - vigil-g2-plugin/src/navigation.ts (full file — verify current line numbers for Screen enum, SCREEN_ORDER, buildScreen, handleNavEvent and the Companion DOUBLE_CLICK carve-out at lines 219-238)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128A-PATTERNS.md §4 (exact diff deltas for the three edits — Screen enum, SCREEN_ORDER, buildScreen case)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128A-UI-SPEC.md §"Screen Registration" (placement: VOICE_SPIKE after AFFIRMATION; carousel order)
    - vigil-g2-plugin/src/screens/voice-spike.ts (verify exports after Plan 03 — `buildVoiceSpikeScreen`, `getRecording`, `toggleVoiceSpikeRecording`)
  </read_first>
  <action>
    Modify `vigil-g2-plugin/src/navigation.ts` with FOUR additive edits — NO existing lines removed.

    EDIT 1 (top of file, with the other screen-builder imports around lines 12-16): add a STATIC import (NOT dynamic — Phase 125 Hermes fix at navigation.ts:222-224 explicitly rejects dynamic imports): `import { buildVoiceSpikeScreen, getRecording, toggleVoiceSpikeRecording } from './screens/voice-spike.ts' // Phase 128a SPIKE — TOSSABLE`.

    EDIT 2 (Screen enum, lines 27-33): append `VOICE_SPIKE: 'voice-spike', // Phase 128a SPIKE — TOSSABLE` after the existing `TASK_DETAIL` entry.

    EDIT 3 (SCREEN_ORDER, lines 37-42): append `Screen.VOICE_SPIKE, // Phase 128a SPIKE — TOSSABLE` AFTER the existing `Screen.AFFIRMATION` entry. Resulting order: `[HOME, COMPANION, WORK_ORDERS, AFFIRMATION, VOICE_SPIKE]` — wraps back to HOME per modular arithmetic at lines 47-55.

    EDIT 4 (buildScreen switch around lines 58-122, after the AFFIRMATION case at lines 112-115): add the new case: `case Screen.VOICE_SPIKE: { return buildVoiceSpikeScreen(getRecording()) } // Phase 128a SPIKE — TOSSABLE`. No async API fetch — recording state lives in the screen module per Plan 03.

    Do NOT remove or reorder any existing lines. The Companion DOUBLE_CLICK carve-out at lines 219-238 stays untouched; Task 2 adds the VOICE_SPIKE carve-out immediately after it.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && grep -q "from './screens/voice-spike" src/navigation.ts && grep -E "VOICE_SPIKE:\s*'voice-spike'" src/navigation.ts && grep -E "Screen\.VOICE_SPIKE,?\s*(//.*)?$" src/navigation.ts && grep -E "case Screen\.VOICE_SPIKE" src/navigation.ts && grep -q "buildVoiceSpikeScreen(getRecording())" src/navigation.ts && (! grep -E "import\([^)]*['\"]\./screens/voice-spike" src/navigation.ts) && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - navigation.ts imports `buildVoiceSpikeScreen`, `getRecording`, `toggleVoiceSpikeRecording` from `./screens/voice-spike.ts` via a STATIC `import { ... } from` statement (NOT `await import(...)`)
    - Screen enum contains `VOICE_SPIKE: 'voice-spike'`
    - SCREEN_ORDER contains `Screen.VOICE_SPIKE` positioned IMMEDIATELY AFTER `Screen.AFFIRMATION` (use `awk` or line-number check to confirm AFFIRMATION line + 1 = VOICE_SPIKE line, ignoring comment-only lines)
    - `buildScreen` has a `case Screen.VOICE_SPIKE` returning `buildVoiceSpikeScreen(getRecording())`
    - No dynamic import (`await import('./screens/voice-spike')`) introduced — Hermes engine fix preserved
    - `cd vigil-g2-plugin && npx tsc --noEmit` exits 0
    - `cd vigil-g2-plugin && npm test` exits 0 (no regression in existing tests)
  </acceptance_criteria>
  <done>
    VOICE_SPIKE registered in the Screen enum, SCREEN_ORDER, and buildScreen switch via static imports. Carousel order is HOME → COMPANION → WORK_ORDERS → AFFIRMATION → VOICE_SPIKE → (wraps).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add DOUBLE_CLICK carve-out + audioEvent collector (navigation.ts + main.ts)</name>
  <files>vigil-g2-plugin/src/navigation.ts, vigil-g2-plugin/src/main.ts</files>
  <read_first>
    - vigil-g2-plugin/src/navigation.ts lines 184-257 — handleNavEvent body + Companion DOUBLE_CLICK carve-out at lines 219-238 (the carve-out template)
    - vigil-g2-plugin/src/main.ts ~lines 221-260 — the bridge.onEvenHubEvent dispatcher; identify where in the chain to add the audioEvent branch (place it BEFORE the listEvent CLICK_EVENT branch so audio fires don't get misrouted)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128A-PATTERNS.md §5 (Option B preferred — extend handleNavEvent with VOICE_SPIKE carve-out mirroring Companion lines 219-238) AND the PCM collector wiring snippet
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-G1" (DOUBLE_CLICK push-to-record; hardware-verified per project_g2_companion_doubletap_hardware_verified memory 2026-05-10)
    - vigil-g2-plugin/src/lib/audio-session-guard.ts (verify safeAudioControl signature so the carve-out passes the bridge correctly)
  </read_first>
  <action>
    PART A — Modify `vigil-g2-plugin/src/navigation.ts`. Inside `handleNavEvent`, IMMEDIATELY AFTER the existing Companion DOUBLE_CLICK carve-out (lines 219-238 per PATTERNS.md), add the VOICE_SPIKE carve-out — placement is load-bearing: this MUST be checked before the default `NAV_EVENTS.has(eventType)` dispatch that would otherwise navigate DOUBLE_CLICK → HOME. Block content: a guard `if (currentScreen === Screen.VOICE_SPIKE && eventType === OsEventTypeList.DOUBLE_CLICK_EVENT) { await toggleVoiceSpikeRecording(bridge); return; }` with the leading comment block: `// Phase 128a SPIKE — VOICE_SPIKE DOUBLE_CLICK toggles recording (TOSSABLE). Mirrors Companion D-08 carve-out at lines 219-238. Hardware-verified DOUBLE_CLICK per project_g2_companion_doubletap_hardware_verified 2026-05-10. Single-press REACTIVATE patch is Phase 133.`. The handler MUST call `toggleVoiceSpikeRecording(bridge)` from the screen module (imported in Task 1) and MUST `return` so the default NAV_EVENTS fallback doesn't also fire and navigate to HOME.

    PART B — Modify `vigil-g2-plugin/src/main.ts`. Add an import at the top alongside other screen module imports: `import { appendPcmChunk } from './screens/voice-spike.ts' // Phase 128a SPIKE — TOSSABLE`. Inside the existing `bridge.onEvenHubEvent((event) => { ... })` dispatcher (around lines 221-260), add a new BRANCH at the TOP of the function body (BEFORE the existing listEvent CLICK_EVENT branch) handling audioEvent fires. Branch content: an `if (event.audioEvent?.audioPcm)` guard whose body computes `const bytes = event.audioEvent.audioPcm.length` (variable name is `bytes`, NOT `pcm`, NOT `audioBuffer`, per GUARD-01 safe allowlist), calls `console.timeEnd('mic-on')` ONCE on the first chunk per recording session (use a closure-captured `firstChunk` flag, OR just always call — `console.timeEnd` no-ops after the first hit), then `console.log('[voice-spike] chunk bytes=' + bytes)` (NO `audio`/`pcm` substring in the log message), then `appendPcmChunk(event.audioEvent.audioPcm)`, then `return` so the audioEvent doesn't fall through to other handlers. The leading comment: `// Phase 128a SPIKE — VOICE_SPIKE audioEvent collector (TOSSABLE). Append PCM chunks while recording. Log strings use GUARD-01 safe keys (bytes, chunk_n) — never pcm/audio*.`. Do NOT register a separate `bridge.onEvenHubEvent` callback — use the existing dispatcher.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && grep -E "currentScreen === Screen\.VOICE_SPIKE.*DOUBLE_CLICK_EVENT" src/navigation.ts && grep -q "toggleVoiceSpikeRecording(bridge)" src/navigation.ts && grep -q "import { appendPcmChunk } from './screens/voice-spike" src/main.ts && grep -E "event\.audioEvent\?\.audioPcm" src/main.ts && grep -q "appendPcmChunk(event\.audioEvent" src/main.ts && (grep -E "console\.(log|warn|error|info)\(" src/main.ts | grep -E "['\"][^'\"]*\b(audioPcm|audio_pcm|audioBuffer|audio_buffer)\b" | grep -v '^$' | wc -l | grep -q '^0$') && (grep -E "console\.(log|warn|error|info)\(" src/screens/voice-spike.ts 2>/dev/null | grep -E "['\"][^'\"]*\b(audioPcm|audio_pcm|audioBuffer|audio_buffer)\b" | grep -v '^$' | wc -l | grep -q '^0$') && npx tsc --noEmit && npm test && cd ../vigil-core && npx tsx --test src/__tests__/audio-log-redaction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - navigation.ts contains a guard matching `currentScreen === Screen.VOICE_SPIKE` AND `DOUBLE_CLICK_EVENT` in the same conditional (grep both tokens within ~5 lines of each other)
    - The VOICE_SPIKE carve-out appears AFTER the Companion D-08 carve-out (line number of VOICE_SPIKE block > line number of `currentScreen === Screen.COMPANION` block)
    - The carve-out calls `toggleVoiceSpikeRecording(bridge)` and contains a `return` statement to short-circuit the NAV_EVENTS fallback
    - main.ts imports `appendPcmChunk` from `./screens/voice-spike.ts`
    - main.ts contains an `event.audioEvent?.audioPcm` check followed by an `appendPcmChunk(event.audioEvent...)` call
    - main.ts and voice-spike.ts (screen) console.* log strings contain NO `audioPcm`/`audio_pcm`/`audioBuffer`/`audio_buffer` substrings (regex filter — variable names like `audioB64` outside log strings are fine)
    - main.ts log strings use `bytes` (the safe-key allowlist) — grep `bytes=` confirms
    - `cd vigil-g2-plugin && npx tsc --noEmit` exits 0
    - `cd vigil-g2-plugin && npm test` exits 0 (audio-session-guard.test.ts regression rail green)
    - Cross-package regression rail: `cd vigil-core && npx tsx --test src/__tests__/audio-log-redaction.test.ts` exits 0
  </acceptance_criteria>
  <done>
    DOUBLE_CLICK on VOICE_SPIKE toggles recording via safeAudioControl (NOT navigates to HOME); audioEvent collector accumulates PCM chunks into the screen module's buffer; all log strings GUARD-01-safe; Phase 127 regression tests stay green.
  </done>
</task>

</tasks>

<verification>
- `cd vigil-g2-plugin && npx tsc --noEmit` passes
- `cd vigil-g2-plugin && npm test` passes (audio-session-guard regression rail green)
- `cd vigil-core && npx tsx --test src/__tests__/audio-log-redaction.test.ts` passes (plugin code logs respect GUARD-01)
- navigation.ts carve-out placed AFTER Companion D-08 block but BEFORE the default DOUBLE_CLICK → HOME fallback (visual diff inspection)
- All new lines carry `Phase 128a SPIKE — TOSSABLE` markers for Phase 130 cleanup grep
</verification>

<success_criteria>
- Screen.VOICE_SPIKE registered and reachable via swipe carousel (HOME→COMPANION→WORK_ORDERS→AFFIRMATION→VOICE_SPIKE→wraps)
- DOUBLE_CLICK on the VOICE_SPIKE screen toggles recording via safeAudioControl — does NOT navigate to HOME
- audioEvent collector accumulates PCM chunks for upload; logs only safe-key names
- Plugin compiles cleanly; all existing tests stay green; GUARD-01 redaction rail stays green
</success_criteria>

<output>
After completion, create `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-04-SUMMARY.md` recording:
- Files modified
- Tsc + npm test outputs (one line each)
- Confirmed line ordering: VOICE_SPIKE carve-out placed after Companion D-08 carve-out (cite line numbers)
- Plan 03 + Plan 04 together make the spike screen executable on hardware once C-1 (Plan 05) sets OPENAI_API_KEY
</output>
