---
phase: 128a
plan: 06
type: execute
wave: 4
depends_on: [128a-05]
files_modified:
  - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md
  - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md
  - .planning/phases/128a-voice-01-pcm-feasibility-spike/60s-demo.mp4
autonomous: false
requirements: [VOICE-01]
wallclock: true
must_haves:
  truths:
    - "Operator records raw mic_on_latency / inter_chunk_latency / e2e_latency / drop-out / battery-delta numbers in 128a-MEASUREMENTS.md"
    - "Operator runs five force-quit cycles (D-M4) and records each cycle's PASS/FAIL outcome"
    - "Operator runs the permission-revocation probe (D-G3) and records the failure mode"
    - "60s-demo.mp4 (or Loom URL) captured per UI-SPEC §60s Loom demonstration shape"
    - "128a-SPIKE-DECISION.md exists with verdict at TOP, threshold-matching evidence in body, and Phase 130 scope-lock implications"
    - "Verdict computed mechanically from D-PASS/DEGRADE/BLOCK thresholds — NOT subjective at write-up time"
    - "E2E latency measured from utterance-end → HTTP 200 of /v1/voice/transcribe (RESEARCH DRIFT-02), NOT to PWA dashboard render"
    - "If C-2 portal verification short-circuited to BLOCK in Plan 05, Tasks 1-2 skip; only Task 3 (BLOCK verdict authoring) runs"
  artifacts:
    - path: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md"
      provides: "Raw timing data, drop-out logs, battery-delta numbers, 5-cycle cleanup verification, permission-revocation probe"
      contains: "mic_on_latency"
    - path: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md"
      provides: "Verdict at TOP (PASS/DEGRADE/BLOCK) + threshold-matching evidence + Phase 130 scope implication"
      contains: "PASS\\|DEGRADE\\|BLOCK"
    - path: ".planning/phases/128a-voice-01-pcm-feasibility-spike/60s-demo.mp4"
      provides: "60s portfolio Loom demonstrating capture → transcription → PWA round-trip (PASS) or documenting the BLOCK/DEGRADE failure mode"
      contains: "(binary file — verify exists OR record Loom URL in SPIKE-DECISION header)"
  key_links:
    - from: "operator (G2 hardware)"
      to: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md"
      via: "console output, batteryLevel readings, force-quit cycles transcribed into the measurements file"
      pattern: "mic_on_latency|inter_chunk_latency|e2e_latency|drop_outs|battery_delta"
    - from: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md"
      to: ".planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md"
      via: "verdict logic applies D-PASS/DEGRADE/BLOCK thresholds to the measured numbers; verdict text at TOP of SPIKE-DECISION cites the specific measurement that drove the decision"
      pattern: "verdict.*PASS|verdict.*DEGRADE|verdict.*BLOCK"
---

<objective>
Execute the hardware spike: operator wears G2, runs the three recording modes (D-R1: 10× short clips + 1× near-cap clip + 1× battery-delta pair), executes the five force-quit cleanup cycles (D-M4), runs the permission-revocation probe (D-G3), records the 60s Loom (C-5), and then computes the PASS/DEGRADE/BLOCK verdict by mechanically applying the D-PASS/DEGRADE/BLOCK thresholds to the measured numbers.

Purpose: Produce the spike's two output artifacts (`128a-SPIKE-DECISION.md` + `128a-MEASUREMENTS.md`) and the portfolio Loom that together scope-lock Phase 130. The verdict logic is locked at the threshold level in CONTEXT D-PASS/DEGRADE/BLOCK — the author does NOT decide subjectively at write-up time; the verdict is a function of the measurements.

Output: Three artifacts: MEASUREMENTS.md (raw data), SPIKE-DECISION.md (verdict + scope-lock implication), 60s-demo.mp4 (or Loom URL captured in SPIKE-DECISION).

**Short-circuit gate:** If Plan 05's SUMMARY.md `"Plan 06 disposition"` reads `short-circuit-block`, Tasks 1 and 2 SKIP — operator only authors Task 3 (BLOCK verdict + Phase 130 termination rationale + three re-activation conditions per SEED-003 DMARC pattern).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-RESEARCH.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-PATTERNS.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-VALIDATION.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128A-UI-SPEC.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md

<interfaces>
<!-- Locked verdict thresholds — D-PASS/DEGRADE/BLOCK in CONTEXT.md §"Verdict thresholds" -->

PASS (ALL FOUR conditions): e2e_latency median ≤ 8s
  AND drop-out events ≤ 1 per 60s
  AND battery delta ≤ 15 percentage-points / hour
  AND audioControl(false) cleanup confirmed on all 5 force-quit cycles AND permission-revocation probe
  ⇒ Phase 130 scope = full VOICE-02..08

DEGRADE (ANY of, but NOT in BLOCK range):
  8s < e2e_latency median ≤ 15s
  OR drop-out events 2-5 per 60s
  OR battery delta 15-30 pp/hr
  OR cleanup fails on ≤2 of 5 cycles AND 3+ cycles succeed
  ⇒ Phase 130 scope = push-to-record short clips ONLY (≤5s; no ambient; explicit REC footer)

BLOCK (ANY of):
  e2e_latency median > 15s
  OR drop-out events > 5 per 60s
  OR battery delta > 30 pp/hr
  OR cleanup fails on ≥3 of 5 cycles
  OR g2-microphone permission rejected on Even Hub portal (D-G2)
  OR PCM not intelligible after WAV reconstruction (PASS GATE 1)
  ⇒ Phase 130 terminated from v3.9 scope; SPIKE-DECISION includes three re-activation conditions per SEED-003 DMARC pattern

LATENCY MEASUREMENT (RESEARCH DRIFT-02):
  e2e_latency = wall-clock from DOUBLE_CLICK_EVENT (stop record) on G2 → HTTP 200 of /v1/voice/transcribe (server-side response)
  NOT to PWA dashboard render (PWA polls every 30s; rendering floor is unreachable for the 8s target)
  Document this measurement-vs-dashboard gap in SPIKE-DECISION so Phase 130 plans SSE fan-out for VOICE-06 acceptance
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: OPERATOR WALLCLOCK C-3 + C-4 — Hardware spike runs (3 recording modes + 5 cleanup cycles + permission probe + battery delta)</name>
  <files>.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md, .planning/phases/128a-voice-01-pcm-feasibility-spike/60s-demo.mp4</files>
  <action>OPERATOR-ONLY (physical G2 hardware required per CONTEXT D-R3 + memory `[feedback_wallclock_checkpoint_exempt]`). SKIP-CHECK: if `128a-05-SUMMARY.md` "Plan 06 disposition" reads `short-circuit-block`, skip this task entirely — Task 2 authors a BLOCK verdict directly from the C-2 portal-rejection signal. Otherwise: operator executes the four runs documented in the `<how-to-verify>` block below (Run 1: 10× short clips per D-R1 mode 1 + D-M1; Run 2: 1× near-cap 55s clip per D-R1 mode 2 + D-M2; Run 3: 5× force-quit cleanup cycles per D-M4; Run 4: 1× permission-revocation probe per D-G3; Run 5: 2-wallclock-hour battery-delta pair per D-R1 mode 3 + D-M3) and records all raw numbers in `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md` using the template embedded in `<how-to-verify>` below. Operator ALSO captures the 60s portfolio Loom (C-5) per UI-SPEC §"60s Loom demonstration shape" — saved as `60s-demo.mp4` in the phase directory OR a Loom URL captured later in `128a-SPIKE-DECISION.md` frontmatter `**Loom:**` field. CRITICAL: e2e_latency is measured from `DOUBLE_CLICK_EVENT (stop)` wallclock → `console.log('[voice-spike] e2e_ms=…')` line that fires AFTER the HTTP 200 response is received — this is the to-HTTP-200 measurement per RESEARCH DRIFT-02, NOT to PWA dashboard render (PWA polls every 30s, dashboard-visibility floor is unreachable for the 8s target).</action>
  <verify>MEASUREMENTS.md exists with: (a) 10-row Run 1 table with all six columns filled (mic_on_ms, chunks, bytes, e2e_ms, HTTP, transcription); (b) Run 2 distribution + drop-out count; (c) 5-row Run 3 cleanup-cycle table with PASS/FAIL outcome per cycle; (d) Run 4 permission-probe error shape + UI behavior; (e) Run 5 battery deltas for both halves + incremental cost; (f) "Verdict inputs (mechanical)" block with bucket assignments per D-PASS/DEGRADE/BLOCK thresholds. 60s-demo.mp4 exists in phase dir OR a Loom URL is staged for Task 2 to embed into SPIKE-DECISION.md frontmatter.</verify>
  <done>All five hardware runs complete; MEASUREMENTS.md captures raw numbers + mechanical bucket assignments; 60s Loom captured; operator's resume signal includes the four headline numbers (e2e_median, drop-outs/60s, battery_delta, cleanup_pass_count) so Task 2's verdict computation can audit the bucket assignments against the raw data.</done>
  <what-built>The plugin scaffold from Plan 03 + 04 + the backend route from Plan 02 + the env from Plan 05's C-1 are live. Operator now runs the empirical spike on physical G2 hardware. SKIP this task if Plan 05's SUMMARY.md disposition is `short-circuit-block`.</what-built>
  <read_first>
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md (check the "Plan 06 disposition" line — if `short-circuit-block`, SKIP this task entirely and jump to Task 3)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-R1" (three recording modes: 10× short / 1× near-cap / 1× battery-delta pair)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-M1" / §"D-M2" / §"D-M3" / §"D-M4" (measurement methodology + battery protocol + cleanup verification)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-G3" (permission-revocation probe)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-RESEARCH.md DRIFT-02 paragraph (measure e2e_latency to HTTP 200 of /v1/voice/transcribe, NOT to PWA dashboard render — PWA polls every 30s)
    - vigil-g2-plugin/src/lib/audio-session-guard.ts (cleanup-path semantics that D-M4 exercises)
  </read_first>
  <how-to-verify>
    OPERATOR ACTION (Claude CANNOT execute — physical G2 hardware required per D-R3 + auto-memory `[feedback_wallclock_checkpoint_exempt]`):

    PREP:
    - Plugin packed and side-loaded onto operator's Even Hub iPhone app (operator does this via the standard plugin pack + portal upload flow used for prior versions)
    - G2 worn, paired, BLE connected, Hub app foregrounded, screen visible
    - iPhone fully charged (100%) for the battery-delta pair (D-M3)
    - Console log capture set up (operator uses the existing console-tap pattern — e.g., Safari Inspector connected to the Hub WebView, or whatever existing tooling they use to read `console.log` output)
    - Stopwatch or timer-overlay app ready

    RUN 1 (D-R1 mode 1 — 10× short clips for mic_on_latency, inter_chunk_latency, e2e_latency, drop-out baseline):
    For each of 10 trials:
      1. Swipe to VOICE_SPIKE screen
      2. DOUBLE_CLICK to start. Speak: "one one-thousand, two one-thousand, three one-thousand, four one-thousand, five one-thousand" (~5s)
      3. DOUBLE_CLICK to stop
      4. Record in MEASUREMENTS.md:
         - mic_on_latency (from console.time/timeEnd `'mic-on'` output) — ms value
         - chunks count + total bytes (from the on-screen counter + console logs `bytes=`)
         - e2e_latency (from `DOUBLE_CLICK_EVENT (stop)` wallclock timestamp → first `console.log('[voice-spike] e2e_ms=…')` line that fires AFTER the HTTP 200 response is received — this is the to-HTTP-200 measurement per DRIFT-02, NOT to PWA dashboard)
         - HTTP status (expect 201)
         - Transcription text (verify PCM intelligible — PASS GATE 1 per CONTEXT D-BLOCK condition 6)
    After 10 trials, compute: min/median/p95 of mic_on_latency; min/median/p95 of e2e_latency; total drop-out count (gap > 2× median inter-arrival of first 5s baseline per D-M2).

    RUN 2 (D-R1 mode 2 — 1× near-cap 55s clip for drop-out + cap-headroom):
      1. Swipe to VOICE_SPIKE. DOUBLE_CLICK start. Speak continuously for ~55s (cap is 60s server-side; stay under)
      2. DOUBLE_CLICK stop. Record full distribution of inter_chunk_latency gaps. Tally events ≥2× the recording's own first-5s baseline. Verify the 60s cap was NOT tripped (no AUDIO_SESSION_TOO_LONG response) — this validates the cap is wired without ever tripping it.

    RUN 3 (D-M4 — 5× force-quit cleanup cycles):
    For each of 5 trials:
      1. Swipe to VOICE_SPIKE. DOUBLE_CLICK start. Record 3s.
      2. Swipe-kill the Even Hub iPhone app mid-recording (iOS app switcher → swipe up on Hub)
      3. Reopen Even Hub. Inspect console log for ABNORMAL_EXIT_EVENT / SYSTEM_EXIT_EVENT / beforeunload / onBackgroundRestore fire (one of these four cleanup paths from Phase 127 GUARD-02 must fire).
      4. Verify G2 is NOT draining at active-mic rate: poll `onDeviceStatusChanged.batteryLevel` over 30s after reopen. If delta > active-mic-equivalent (~5-10× idle), cleanup FAILED.
      5. Record cycle outcome: PASS (cleanup confirmed) or FAIL (mic still draining).
    After 5 cycles: count PASS/FAIL. (0 FAIL → cleanup green. 1-2 FAIL → DEGRADE-eligible per D-DEGRADE. 3+ FAIL → BLOCK per D-BLOCK.)

    RUN 4 (D-G3 — permission-revocation probe, 1 cycle):
      1. Revoke `g2-microphone` permission via Even Hub developer portal UI
      2. Reopen the plugin → swipe to VOICE_SPIKE → DOUBLE_CLICK start
      3. Observe what `safeAudioControl(true, bridge)` does: error thrown? Specific error shape? UI shows `[NO MIC]` per UI-SPEC?
      4. Record the error shape + UI behavior in MEASUREMENTS.md
      5. Re-enable the permission via portal; verify normal operation returns

    RUN 5 (D-R1 mode 3 + D-M3 — 2-wallclock-hour battery delta — C-4):
      Half A — baseline (mic OFF, G2 worn, Hub foreground, screen visible):
        - Charge iPhone to 100%. Record G2 batteryLevel at t=0 via `onDeviceStatusChanged.batteryLevel`.
        - DO NOT swipe to VOICE_SPIKE. Sit with G2 worn, Hub foregrounded for 1h.
        - At t=60min, record G2 batteryLevel again. Compute delta in percentage points.
        - If iPhone falls below 70% during the hour: STOP, recharge to 100%, restart (non-linear discharge curves invalidate the comparison).
      Half B — push-to-record (10× ~5s clips evenly spaced):
        - Re-verify iPhone at 100% AND G2 at same starting batteryLevel as Half A start (recharge BOTH to ensure parity). Record G2 batteryLevel at t=0.
        - Spread 10× 5s recordings evenly across the hour (every 6 minutes). Between recordings: idle, G2 worn, Hub foregrounded.
        - At t=60min, record G2 batteryLevel. Compute delta.
      Battery delta = (battery_t0 - battery_t60) percentage points per half. Compute the DIFFERENCE between Half B delta and Half A delta — that's the "incremental cost of push-to-record". Compare against D-PASS (≤15pp), D-DEGRADE (15-30pp), D-BLOCK (>30pp).

    Record ALL of the above in `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md`. Use the template structure below (operator copies and fills):

    ```
    # Phase 128a — MEASUREMENTS

    ## Run 1 — Short clips (D-R1 mode 1)
    | Trial | mic_on_ms | chunks | bytes | e2e_ms | HTTP | transcription |
    |-------|-----------|--------|-------|--------|------|---------------|
    | 1     | …         | …      | …     | …      | …    | …             |
    ...
    | 10    | …         | …      | …     | …      | …    | …             |

    Aggregates: mic_on min/median/p95 = …  ;  e2e min/median/p95 = …  ;  drop-outs total = …

    ## Run 2 — Near-cap 55s clip (D-R1 mode 2)
    Inter-arrival distribution: …
    Drop-out events (gap ≥ 2× first-5s baseline of THIS recording): …  per 60s
    AUDIO_SESSION_TOO_LONG tripped? …

    ## Run 3 — 5× force-quit cleanup cycles (D-M4)
    | Cycle | Cleanup path observed | mic-drain-check | Outcome |
    |-------|----------------------|-----------------|---------|
    | 1     | …                    | …               | PASS/FAIL |
    ...
    | 5     | …                    | …               | PASS/FAIL |

    Cleanup pass count: X / 5

    ## Run 4 — Permission-revocation probe (D-G3)
    Revoked at portal: …
    safeAudioControl error shape: …
    UI behavior: …

    ## Run 5 — Battery delta (D-M3)
    Half A (baseline): t0 = …%  t60 = …%  delta = …pp
    Half B (push-to-record 10× 5s): t0 = …%  t60 = …%  delta = …pp
    Incremental cost = Half B delta - Half A delta = …pp/hr

    ## Verdict inputs (mechanical)
    e2e_latency median: ……ms  → bucket: PASS(≤8000) / DEGRADE(8001-15000) / BLOCK(>15000)
    drop-outs per 60s:  ……    → bucket: PASS(≤1) / DEGRADE(2-5) / BLOCK(>5)
    battery delta:      ……pp  → bucket: PASS(≤15) / DEGRADE(15-30) / BLOCK(>30)
    cleanup pass count: …/5   → bucket: PASS(5/5 + permission probe) / DEGRADE(3-4/5) / BLOCK(≤2/5)
    PCM intelligible after WAV reconstruction (PASS GATE 1): YES / NO
    ```
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md` exists
    - Contains the 10-row Run 1 table with all six columns filled (mic_on_ms, chunks, bytes, e2e_ms, HTTP, transcription)
    - Contains the Run 2 near-cap distribution + drop-out count
    - Contains the 5-row Run 3 cleanup-cycle table with PASS/FAIL outcome per cycle
    - Contains Run 4 permission-revocation probe error shape + UI behavior
    - Contains Run 5 battery delta numbers for BOTH halves + the incremental-cost computation
    - Contains the "Verdict inputs (mechanical)" block with bucket assignments per D-PASS/DEGRADE/BLOCK thresholds
    - `60s-demo.mp4` exists in the phase directory (or a Loom URL is committed to `128a-SPIKE-DECISION.md` header per C-5)
  </acceptance_criteria>
  <resume-signal>
    Type one of:
      - `hardware-runs-complete: e2e=…ms drops=… battery=…pp cleanup=…/5` — proceeds to Task 2 (verdict authoring)
      - `hardware-runs-blocked: <reason>` — log the blocker (e.g., G2 hardware failure, battery exhaustion, BLE-pair failure that prevented the spike); Plan 06 cannot complete; operator schedules a re-run
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply D-PASS/DEGRADE/BLOCK threshold logic mechanically; author SPIKE-DECISION.md</name>
  <files>.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md</files>
  <read_first>
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-MEASUREMENTS.md (just authored in Task 1; the "Verdict inputs (mechanical)" block is the input to this task)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-CONTEXT.md §"D-PASS" / §"D-DEGRADE" / §"D-BLOCK" / §"D-V1" (verdict thresholds + non-editability rule)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-RESEARCH.md DRIFT-02 paragraph (latency-to-HTTP-200 vs latency-to-PWA gap — must be documented in SPIKE-DECISION for Phase 130 SSE work)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-05-SUMMARY.md (C-2 portal verdict; if `c2-rejected`, this task is overridden by Task 3's BLOCK path)
  </read_first>
  <action>
    Apply the verdict logic MECHANICALLY (no subjective interpretation per CONTEXT D-V1):

    STEP 1 — Read MEASUREMENTS.md "Verdict inputs (mechanical)" block. Extract: e2e_latency median (ms), drop-outs per 60s, battery delta (pp/hr), cleanup pass count (N/5), permission-revocation probe outcome, PCM intelligibility (PASS GATE 1).

    STEP 2 — Apply BLOCK gate FIRST (any single condition triggers BLOCK):
      - If e2e_latency median > 15000ms → BLOCK
      - OR drop-outs per 60s > 5 → BLOCK
      - OR battery delta > 30pp/hr → BLOCK
      - OR cleanup pass count ≤ 2 (i.e., ≥3 FAILs of 5) → BLOCK
      - OR permission rejected at portal (from Plan 05 C-2) → BLOCK
      - OR PCM not intelligible (PASS GATE 1 = NO) → BLOCK

    STEP 3 — If not BLOCK, apply PASS gate (ALL four conditions required):
      - e2e_latency median ≤ 8000ms AND
      - drop-outs ≤ 1 per 60s AND
      - battery delta ≤ 15pp/hr AND
      - cleanup pass count = 5/5 AND permission-revocation probe documented
      → PASS

    STEP 4 — If neither BLOCK nor PASS, verdict is DEGRADE.

    STEP 5 — Author `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md`:

      Line 1 (header): `# Phase 128a — VOICE-01 SPIKE DECISION: <VERDICT>` (VERDICT = PASS / DEGRADE / BLOCK)
      Line 2 (blank)
      Line 3 (frontmatter): `**Verdict:** <VERDICT> · **Authored:** <YYYY-MM-DD> · **Loom:** <60s-demo.mp4 path OR Loom URL>`
      Section "## Verdict Evidence" — cite the SPECIFIC measurement that drove the verdict. If BLOCK due to e2e > 15s: quote the median number. If PASS: cite all four measurements with bucket assignments.
      Section "## Phase 130 Scope-Lock Implication":
        - PASS → "Phase 130 ships full VOICE-02..08 spec (push-to-record short clips with DOUBLE_CLICK gesture per Phase 127.5 REACTIVATE verdict on track for Phase 133 plumbing patch)"
        - DEGRADE → "Phase 130 ships push-to-record short clips ONLY (≤5s per clip; no ambient continuous capture; explicit `REC 0:00` footer indicator on Companion HUD)"
        - BLOCK → "Phase 130 TERMINATED from v3.9 scope. Voice anchor deferred to v3.10+. Three re-activation conditions (per SEED-003 DMARC pattern): [list 3 explicit conditions tied to the failing measurement — e.g., 'when Even SDK ships chunked audio upload so 60s cap doesn't gate ambient mode', 'when battery curve characterization improves to ≤10pp/hr push-to-record', 'when cleanup robustness reaches 5/5 in repeat trials']"
      Section "## RESEARCH DRIFT-02 — Latency Measurement Note" (REQUIRED on every verdict): "E2E latency in this spike is measured from utterance-end (DOUBLE_CLICK stop) → HTTP 200 of `/v1/voice/transcribe`, NOT to PWA dashboard render. The PWA dashboard polls every 30s (vigil-pwa/src/hooks/useThoughts.ts:81-87), so the dashboard-visibility floor is `transcribe_ms + insert_ms + 15s average`. VOICE-06's 8s acceptance criterion requires Phase 130 to wire SSE fan-out from `/v1/voice/transcribe` → existing AgentEventBus → PWA dashboard, OR an in-tab `vigil:thought-created` window event from the G2-origin path. Document this as a Phase 130 task requirement."
      Section "## Tossable Code Cleanup Note (Phase 130)": Grep anchor `PHASE 128a SPIKE` across `vigil-g2-plugin/scripts/voice-spike-encoder.ts`, `vigil-g2-plugin/src/screens/voice-spike.ts`, `vigil-core/src/routes/voice-spike.ts`, `vigil-core/src/ai/transcribe-spike.ts`, `vigil-core/src/routes/__tests__/voice-spike.test.ts`, plus the navigation.ts/main.ts/constants.ts/app.json/index.ts edits — Phase 130 ships clean by deleting all six TS files plus reverting the four modifications.
      Section "## Author Signature": operator's name + timestamp + "Per CONTEXT D-V1: this verdict is NOT editable after commit. Contradictory re-tests require Phase 128a.1 with updated protocol."

    STEP 6 — Verify the verdict-logic match: write a verification block at the BOTTOM of SPIKE-DECISION.md titled "## Threshold Trace" that re-states each threshold + the measured number + the bucket assignment, in the exact format from MEASUREMENTS.md "Verdict inputs (mechanical)" — proves the verdict is mechanical not subjective.

    If `128a-05-SUMMARY.md` "Plan 06 disposition" = `short-circuit-block`, the Task 2 verdict is BLOCK and the evidence is the portal rejection — author the BLOCK path verdict directly without invoking the threshold logic on absent measurements.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md ] && head -1 .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md | grep -qE 'VOICE-01 SPIKE DECISION: (PASS|DEGRADE|BLOCK)' && grep -q '\*\*Verdict:\*\*' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md && grep -q 'Phase 130 Scope-Lock Implication' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md && grep -q 'DRIFT-02' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md && grep -q 'Threshold Trace' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md && grep -q 'D-V1' .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `128a-SPIKE-DECISION.md` exists at the phase directory
    - Line 1 matches the regex `^# Phase 128a — VOICE-01 SPIKE DECISION: (PASS|DEGRADE|BLOCK)$` — verdict at TOP per D-V1
    - File contains a `**Verdict:**` frontmatter line with the chosen verdict
    - File contains a section titled `## Verdict Evidence` quoting the specific measurement(s)
    - File contains a section titled `## Phase 130 Scope-Lock Implication` with the correct scope per the verdict (full VOICE-02..08 for PASS / push-to-record-only for DEGRADE / TERMINATED-with-three-reactivation-conditions for BLOCK)
    - File contains a section titled `## RESEARCH DRIFT-02 — Latency Measurement Note` documenting the to-HTTP-200 vs to-PWA-render gap
    - File contains a section titled `## Threshold Trace` showing the mechanical bucket assignment for each input measurement
    - File contains a `D-V1` reference confirming verdict-non-editability
    - If verdict is BLOCK, the file lists THREE explicit re-activation conditions (per SEED-003 DMARC pattern)
    - The verdict at line 1 is consistent with the buckets in MEASUREMENTS.md "Verdict inputs (mechanical)" block — if any bucket is BLOCK, line 1 must say BLOCK; if all four are PASS-bucket, line 1 must say PASS; otherwise DEGRADE
    - 60s-demo.mp4 OR a Loom URL is referenced in the frontmatter `**Loom:**` field
  </acceptance_criteria>
  <done>
    SPIKE-DECISION.md exists with verdict at TOP, threshold trace at BOTTOM, Phase 130 scope-lock implication documented, DRIFT-02 latency-measurement note included, D-V1 non-editability rule cited, Loom referenced. Verdict is mechanically derivable from MEASUREMENTS.md (re-reader can recompute and arrive at the same verdict).
  </done>
</task>

</tasks>

<verification>
- 128a-MEASUREMENTS.md exists with the 10-row Run 1 table, Run 2 distribution, 5-row Run 3 cleanup table, Run 4 probe, Run 5 battery deltas, and "Verdict inputs (mechanical)" block
- 128a-SPIKE-DECISION.md exists with verdict at top, threshold trace at bottom, Phase 130 scope implication, DRIFT-02 note, D-V1 citation
- 60s-demo.mp4 exists in phase directory OR Loom URL is captured in SPIKE-DECISION frontmatter
- Verdict logic is auditable: a third party can read MEASUREMENTS.md "Verdict inputs (mechanical)" block + CONTEXT D-PASS/DEGRADE/BLOCK thresholds and arrive at the same verdict
- If Plan 05 disposition was `short-circuit-block`, SPIKE-DECISION.md shows BLOCK verdict with portal-rejection evidence (Task 1 skipped is correctly documented)
</verification>

<success_criteria>
- Operator records every required hardware measurement per D-R1/D-M1/D-M2/D-M3/D-M4/D-G3 in MEASUREMENTS.md
- 60s portfolio Loom captured per UI-SPEC §"60s Loom demonstration shape" (success criterion #3)
- Verdict computed mechanically from D-PASS/DEGRADE/BLOCK thresholds applied to the measured numbers (no subjective interpretation per D-V1)
- E2E latency measured to HTTP 200 of /v1/voice/transcribe (not to PWA render) per RESEARCH DRIFT-02, with the gap documented in SPIKE-DECISION as a Phase 130 SSE-wiring requirement for VOICE-06
- Phase 130 scope-lock is unambiguous from the SPIKE-DECISION: full VOICE-02..08 (PASS) / push-to-record-only (DEGRADE) / terminated-with-three-reactivation-conditions (BLOCK)
</success_criteria>

<output>
After completion, create `.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-06-SUMMARY.md` recording:
- Files created (MEASUREMENTS.md, SPIKE-DECISION.md, 60s-demo.mp4 or Loom URL)
- Final verdict (PASS / DEGRADE / BLOCK)
- Phase 130 scope-lock implication (one sentence)
- DRIFT-02 reminder for Phase 130 planner: SSE fan-out wiring required for VOICE-06 8s acceptance
- Tossable-code-cleanup checklist for Phase 130 (six files to delete + four files to revert: TS files = scripts/voice-spike-encoder.ts, src/screens/voice-spike.ts, src/routes/voice-spike.ts, src/ai/transcribe-spike.ts, src/routes/__tests__/voice-spike.test.ts; modifications = navigation.ts/main.ts/constants.ts/app.json/index.ts)
</output>
