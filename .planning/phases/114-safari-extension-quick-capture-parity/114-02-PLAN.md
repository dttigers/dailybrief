---
phase: 114
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.html
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.css
autonomous: true
requirements: [EXT-02]
tags: [safari-extension, webextension, popup-html, popup-css, verbatim-port]
must_haves:
  truths:
    - "Safari popup.html has the `Include page URL` checkbox below the textarea, the `Cmd+Enter to capture` shortcut hint below the Capture button, and the static `Captured!` text replaced with a dynamic `<span id=\"success-text\"></span>` (SC#1 visual scaffolding + SC#2 + SC#3 hint + SC#4 dynamic span)"
    - "Safari popup.css has the four verbatim-Chrome rule blocks `.url-toggle`, `.category-badge`, `.shortcut-hint`, `.analyzing` appended (SC#4 badge styling per D-11)"
    - "DOCTYPE remains line 1 of popup.html; D-02 lockstep header comment is line 2 — no quirks-mode regression (RESEARCH Pitfall 6)"
    - "D-02 lockstep header comment is added to popup.html (line 2) and popup.css (line 1) on the Safari side, mirroring Plan 00's Chrome-side comments"
  artifacts:
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html"
      provides: "Updated capture-view markup with checkbox + shortcut hint + dynamic success-text span"
      contains: 'id="include-url"'
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.css"
      provides: "Verbatim-Chrome CSS rules for url-toggle, category-badge, shortcut-hint, analyzing"
      contains: ".category-badge"
  key_links:
    - from: "popup.html checkbox + dynamic span"
      to: "popup.js (Plan 03 handler)"
      via: "DOM IDs include-url + success-text"
      pattern: 'id="(include-url|success-text)"'
    - from: "popup.css .category-badge rule"
      to: "popup.js triage poll innerHTML render"
      via: "CSS class category-badge applied via innerHTML interpolation"
      pattern: "\\.category-badge"
---

<objective>
Verbatim Chrome → Safari port of popup.html structural deltas + popup.css rule appends. Establishes the DOM scaffolding and styling that Plan 03's popup.js handler depends on.

Purpose: Land SC#1 visual scaffolding (dynamic success-text span replacing the static "Captured!"), SC#2 markup (the include-url checkbox), SC#3 visual confirmation (the "Cmd+Enter to capture" shortcut hint footer), and SC#4 styling (the four CSS rule blocks for `.url-toggle`, `.category-badge`, `.shortcut-hint`, `.analyzing`). Per D-11, ALL CSS values are verbatim from Chrome `vigil-extension/popup.css` lines 160-195 — Vigil teal `#1D9E75`, no tuning. Per D-06 / phase_specific_constraints, this is a "git diff Chrome popup.X against Safari popup.X, apply the diff" port — not "inspired by" Chrome, byte-for-byte parity.

Plan 02 is parallel-safe with Plan 03 — Plan 02 owns popup.html + popup.css; Plan 03 owns popup.js. No file overlap. Both depend on Plan 01's probe attestation.

Output: Updated Safari popup.html (1 net line added: checkbox row, shortcut-hint, success-text span, D-02 header) and Safari popup.css (37 net lines added: 4 rule blocks at end + D-02 header at line 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-VALIDATION.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md
@scripts/verify-phase-114.sh
@vigil-extension/popup.html
@vigil-extension/popup.css
@vigil-safari-extension/Vigil Capture Extension/Resources/popup.html
@vigil-safari-extension/Vigil Capture Extension/Resources/popup.css

<interfaces>
<!-- Chrome → Safari diff specification -->

Chrome popup.html (source-of-truth, 47 lines including the D-02 header added in Plan 00):
- Line 1: `<!DOCTYPE html>`
- Line 2: `<!-- Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html — Phase 114 (D-02) -->` (added in Plan 00)
- Line 35: `<label class="url-toggle"><input type="checkbox" id="include-url"> Include page URL</label>` (BETWEEN closing form-group div and Capture button)
- Line 37: `<div class="shortcut-hint">Cmd+Enter to capture</div>` (BETWEEN Capture button and capture-error div)
- Line 40: `<span id="success-text"></span>` (REPLACES static `<span class="checkmark">&#10003;</span> Captured!`)

Safari popup.html current (45 lines, NO D-02 header yet):
- Line 1: `<!DOCTYPE html>`
- Line 35: `<button id="capture-btn" class="btn btn-primary">Capture</button>` (immediately after `</div>` closing form-group at line 34)
- Line 38: `<span class="checkmark">&#10003;</span> Captured!` (the static text to replace)

Chrome popup.css (source-of-truth, lines 160-195 — verbatim D-11 rules, ALREADY had Plan 00 D-02 comment at line 1):
```css
.url-toggle {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: #999;
  cursor: pointer;
  text-transform: none;
  letter-spacing: 0;
}

.url-toggle input[type="checkbox"] {
  accent-color: #1D9E75;
}

.category-badge {
  display: inline-block;
  background: rgba(29, 158, 117, 0.15);
  color: #1D9E75;
  border-radius: 12px;
  padding: 2px 10px;
  font-size: 12px;
  font-weight: 500;
}

.shortcut-hint {
  font-size: 11px;
  color: #666;
  text-align: center;
  margin-top: 4px;
}

.analyzing {
  color: #999;
  font-size: 13px;
}
```

Safari popup.css current (158 lines, NO D-02 header, NO url-toggle/category-badge/shortcut-hint/analyzing rules at end). Last existing rule block ends at line 158 (`.loading-msg[hidden] { display: none; }`).

D-02 lockstep header comments to add on Safari side (path is reversed from Chrome side):
- popup.html line 2 (after DOCTYPE): `<!-- Keep in lockstep with ../../../vigil-extension/popup.html — Phase 114 (D-02) -->`
- popup.css line 1: `/* Keep in lockstep with ../../../vigil-extension/popup.css — Phase 114 (D-02) */`
  (`../../../` because Safari files are at `vigil-safari-extension/Vigil Capture Extension/Resources/`, three directory levels deep from repo root.)

Plan 03 owns popup.js — including its D-02 header comment at line 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Edit Safari popup.html — add D-02 header, checkbox, shortcut hint, dynamic success span</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/popup.html</files>
  <read_first>
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.html (current state — 45 lines; understand the exact lines being modified)
    - vigil-extension/popup.html (Chrome source-of-truth — verbatim port target; line 35 checkbox, line 37 shortcut hint, line 40 success-text span)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-02 header comment, D-06 verbatim port, D-07 default-unchecked)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (Diff 1 popup.html change table — exact line numbers + insertion/replacement actions; Pitfall 6 — DOCTYPE must remain line 1)
  </read_first>
  <action>
Apply 4 edits to `vigil-safari-extension/Vigil Capture Extension/Resources/popup.html`. Edit in this exact order (top-to-bottom of file) to keep line numbers predictable:

**Edit 1: Insert D-02 lockstep header comment at line 2** (after `<!DOCTYPE html>`, before `<html lang="en">`).

The line MUST be:
```html
<!-- Keep in lockstep with ../../../vigil-extension/popup.html — Phase 114 (D-02) -->
```

After this edit:
- Line 1: `<!DOCTYPE html>` (preserved — Pitfall 6)
- Line 2: `<!-- Keep in lockstep with ../../../vigil-extension/popup.html — Phase 114 (D-02) -->` (new)
- Line 3: `<html lang="en">` (existing line 2 pushed to line 3)

**Edit 2: Insert the include-url checkbox label** between the closing `</div>` of the form-group (currently line 34) and the Capture button (`<button id="capture-btn"...>`).

The line MUST be exactly verbatim from Chrome popup.html line 35:
```html
    <label class="url-toggle"><input type="checkbox" id="include-url"> Include page URL</label>
```

(Note: the indentation is 4 spaces to match the surrounding HTML inside `<div id="capture-view">`. Per D-07, the checkbox has NO `checked` attribute — default unchecked. Per D-14, `chrome.storage.local` is NOT read for persistence — every popup open starts unchecked.)

**Edit 3: Insert the shortcut-hint div** between the Capture button (`<button id="capture-btn"...Capture</button>`) and the `<div id="capture-error"...>` div.

The line MUST be exactly verbatim from Chrome popup.html line 37:
```html
    <div class="shortcut-hint">Cmd+Enter to capture</div>
```

(4-space indentation matches surrounding HTML.)

**Edit 4: Replace the static success text** at the existing Safari popup.html line 38 (which currently reads `<span class="checkmark">&#10003;</span> Captured!` inside the `<div id="capture-success">`). Replace with the dynamic span verbatim from Chrome popup.html line 40:
```html
      <span id="success-text"></span>
```

(6-space indentation matches the existing inner-div nesting.)

After all 4 edits, the Safari popup.html capture-view block (originally lines 26-40, now shifted by the 3 new lines from Edits 1-3) must read:

```html
  <!-- Capture View (API key stored) -->
  <div id="capture-view" class="view" hidden>
    <div class="header">
      <h1>Capture Thought</h1>
      <button id="settings-btn" class="btn-icon" title="Change API key">&#9881;</button>
    </div>
    <div class="form-group">
      <label for="content-input">Content</label>
      <textarea id="content-input" rows="6" placeholder="What's on your mind?"></textarea>
    </div>
    <label class="url-toggle"><input type="checkbox" id="include-url"> Include page URL</label>
    <button id="capture-btn" class="btn btn-primary">Capture</button>
    <div class="shortcut-hint">Cmd+Enter to capture</div>
    <div id="capture-error" class="error-msg" hidden></div>
    <div id="capture-success" class="success-msg" hidden>
      <span id="success-text"></span>
    </div>
  </div>
```

This matches Chrome popup.html lines 25-41 byte-for-byte (modulo the comment text which matches the file's role: setup-view in capture-view comment).

**Do NOT modify** the setup-view block (lines 11-23 in current Safari file — completely untouched). **Do NOT modify** the `<head>` section beyond adding the D-02 comment at line 2.

Net file growth: +3 lines added (D-02 header + checkbox + shortcut hint), +0 lines net for the success-text replacement (one line in, one line out). Total: 45 → 48 lines.
  </action>
  <verify>
    <automated>head -1 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" | grep -qF '<!DOCTYPE html>' && head -2 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" | tail -1 | grep -qF 'Keep in lockstep with ../../../vigil-extension/popup.html' && grep -qF 'id="include-url"' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" && grep -qF 'class="shortcut-hint">Cmd+Enter to capture' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" && grep -qF 'id="success-text"' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" && ! grep -qF 'class="checkmark">&#10003;</span> Captured!' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.html"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 popup.html | grep -qF '<!DOCTYPE html>'` exits 0 (DOCTYPE preserved as line 1 — Pitfall 6 mitigation)
    - `head -2 popup.html | tail -1 | grep -qF 'Keep in lockstep with ../../../vigil-extension/popup.html'` exits 0 (D-02 header present at line 2)
    - `grep -qF 'id="include-url"' popup.html` exits 0 (SC#2 checkbox)
    - `grep -qF 'type="checkbox" id="include-url"' popup.html` exits 0 (input is a checkbox, not text/radio)
    - `grep -qF '<label class="url-toggle">' popup.html` exits 0 (label class matches Chrome)
    - `grep -qF 'Include page URL' popup.html` exits 0 (visible label text)
    - `grep -qF 'class="shortcut-hint">Cmd+Enter to capture' popup.html` exits 0 (SC#3 visual hint)
    - `grep -qF 'id="success-text"' popup.html` exits 0 (SC#4 dynamic span scaffold)
    - `! grep -qF 'class="checkmark">&#10003;</span> Captured!' popup.html` exits 0 (the OLD static text is removed — replaced by the dynamic span)
    - `grep -c 'class="view"' popup.html` returns 2 (both setup-view and capture-view divs preserved)
    - `grep -c 'id="setup-view"' popup.html` returns 1 (setup-view block intact)
    - `grep -c 'id="api-key-input"' popup.html` returns 1 (setup-view input intact — no accidental edits to setup view)
    - `wc -l < popup.html` returns 48 (45 original + 3 net additions: D-02 header + checkbox + shortcut hint; success-text is in-place replacement)
    - `grep -qE '<!--.*-->' popup.html | head -1` returns the line containing the D-02 header (HTML comment well-formed)
  </acceptance_criteria>
  <done>
    Safari popup.html has D-02 header at line 2 (DOCTYPE preserved at line 1), include-url checkbox below textarea, shortcut hint below Capture button, dynamic success-text span replacing the static "Captured!" text. setup-view block untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit Safari popup.css — add D-02 header + 4 verbatim Chrome rule blocks (.url-toggle, .category-badge, .shortcut-hint, .analyzing)</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/popup.css</files>
  <read_first>
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.css (current state — 158 lines; understand existing rule structure to verify the appends don't collide)
    - vigil-extension/popup.css (Chrome source-of-truth — exact lines 160-195 contain the four rule blocks to copy verbatim, plus the Plan 00 D-02 header at line 1)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-11 verbatim Chrome CSS, D-02 header comment)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (Diff 3 popup.css change table — Vigil teal #1D9E75, all values exact)
  </read_first>
  <action>
Apply 2 edits to `vigil-safari-extension/Vigil Capture Extension/Resources/popup.css`:

**Edit 1: Insert D-02 lockstep header comment at line 1** (the file currently starts with `* {` — push that to line 2).

The new line 1 MUST be:
```css
/* Keep in lockstep with ../../../vigil-extension/popup.css — Phase 114 (D-02) */
```

After this edit:
- Line 1: `/* Keep in lockstep with ../../../vigil-extension/popup.css — Phase 114 (D-02) */` (new)
- Line 2: `* {` (existing line 1 pushed to line 2)
- Line 3: `  margin: 0;` (existing line 2 pushed to line 3)
... etc.

**Edit 2: Append 4 verbatim Chrome rule blocks** to the end of the file. Append AFTER the existing last rule block which ends at the original line 158 (`.loading-msg[hidden] { display: none; }`).

The exact 36 lines to append (one blank separator line between the existing content and the new blocks, then the verbatim Chrome lines 160-195):

```css

.url-toggle {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: #999;
  cursor: pointer;
  text-transform: none;
  letter-spacing: 0;
}

.url-toggle input[type="checkbox"] {
  accent-color: #1D9E75;
}

.category-badge {
  display: inline-block;
  background: rgba(29, 158, 117, 0.15);
  color: #1D9E75;
  border-radius: 12px;
  padding: 2px 10px;
  font-size: 12px;
  font-weight: 500;
}

.shortcut-hint {
  font-size: 11px;
  color: #666;
  text-align: center;
  margin-top: 4px;
}

.analyzing {
  color: #999;
  font-size: 13px;
}
```

These rules MUST be byte-for-byte identical to Chrome popup.css lines 160-195. Per D-11: Vigil teal `#1D9E75` is brand-locked across PWA, Mac app, Chrome extension; no tuning. Per phase_specific_constraints: verbatim, NOT "inspired by," NOT "approximately."

**Do NOT modify** any of the existing 158 lines beyond pushing them down by 1 line via the D-02 header insertion at line 1. **Do NOT touch** the existing `.success-msg`, `.checkmark`, `.error-msg`, `.btn-primary`, etc. rules — they remain identical.

Net file growth: +1 line (D-02 header) + 36 lines (4 new rule blocks + 1 separator blank line) = 37 lines. Total: 158 → 195 lines (matches Chrome popup.css line count after Plan 00's D-02 header was added there).
  </action>
  <verify>
    <automated>head -1 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" | grep -qF 'Keep in lockstep with ../../../vigil-extension/popup.css' && grep -qF '.url-toggle {' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && grep -qF '.category-badge {' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && grep -qF '.shortcut-hint {' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && grep -qF '.analyzing {' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && grep -qF 'accent-color: #1D9E75;' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && grep -qF 'background: rgba(29, 158, 117, 0.15);' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.css"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 popup.css | grep -qF 'Keep in lockstep with ../../../vigil-extension/popup.css'` exits 0 (D-02 header at line 1)
    - `head -2 popup.css | tail -1 | grep -qF '* {'` exits 0 (existing universal selector preserved at line 2)
    - `grep -c '\.url-toggle' popup.css` returns at least 2 (rule + nested input rule both present)
    - `grep -qF '.url-toggle input[type="checkbox"]' popup.css` exits 0 (nested checkbox accent rule)
    - `grep -qF 'accent-color: #1D9E75;' popup.css` exits 0 (Vigil teal verbatim per D-11)
    - `grep -qF '.category-badge {' popup.css` exits 0 (badge rule present)
    - `grep -qF 'background: rgba(29, 158, 117, 0.15);' popup.css` exits 0 (verbatim Chrome teal-tint background)
    - `grep -qF 'border-radius: 12px;' popup.css` exits 0 (badge pill shape)
    - `grep -qF '.shortcut-hint {' popup.css` exits 0 (shortcut hint footer rule)
    - `grep -qF 'font-size: 11px;' popup.css` exits 0 (shortcut hint sizing)
    - `grep -qF '.analyzing {' popup.css` exits 0 (pre-poll-success text rule)
    - `grep -qF 'color: #999;' popup.css | head -1` exits 0 (analyzing text color)
    - `grep -c '#1D9E75' popup.css` returns at least 4 (existing focus + btn-primary + success-msg uses of teal, plus new url-toggle accent-color + category-badge color)
    - `wc -l < popup.css` returns 195 (158 original + 37 added: 1 D-02 header + 36 rule lines including separator blank)
    - `bash scripts/verify-phase-114.sh --static` SC#4 CSS portion now passes (the four `.url-toggle / .category-badge / .shortcut-hint / .analyzing` greps in the verify script)
  </acceptance_criteria>
  <done>
    Safari popup.css has D-02 header at line 1, 4 verbatim Chrome rule blocks appended (`.url-toggle`, `.category-badge`, `.shortcut-hint`, `.analyzing`) with byte-for-byte parity to Chrome's `#1D9E75` teal palette per D-11. Existing 158 lines untouched (just shifted down by 1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safari extension popup HTML | Static markup loaded into a sandboxed extension popup; no user-controlled data interpolated; no `<script>` injection surface |
| Safari extension popup CSS | Static stylesheet; no `url(...)` external references; no `@import`; no JS-exposed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02-01 | Tampering | popup.html DOCTYPE preservation | mitigate | Pitfall 6 mitigation: D-02 header comment is inserted at line 2, NOT before `<!DOCTYPE html>`. Acceptance criterion `head -1 popup.html` matches DOCTYPE asserts compliance. Quirks-mode regression would silently break CSS layout (margins, font rendering) — this is the only risk in Plan 02 and it is gate-checked. |
| T-114-02-02 | Information Disclosure | popup.html include-url checkbox | accept | Checkbox is unchecked by default (D-07). The actual URL append is Plan 03's popup.js handler responsibility; Plan 02 only ships the markup. No URL is read or transmitted by Plan 02 alone. |
| T1 (XSS via injected category) | Tampering | popup.css `.category-badge` rule | mitigate | The CSS rule is purely visual — no expression(...), no JS-exposed properties. The XSS concern is the popup.js innerHTML interpolation in Plan 03; the CSS class itself is inert. Server-side category enum at thoughts.ts:37-43 is the load-bearing guard. |
| T2 (URL exfiltration via auto-prefill) | Information Disclosure | popup.html (no auto-prefill in markup) | mitigate | Markup contains no auto-fill defaults (`<textarea>` has only `placeholder`, no `value`). Plan 03 owns removing the JS-side auto-prefill. |
| T3 (Token leakage in error messages) | Information Disclosure | popup.html `<div id="capture-error">` | accept | Error div is empty markup; popup.js writes generic `textContent` (Plan 03 preserves this). Same posture as Phase 84/94. |
| T4 (Cmd+Enter triggers invalid submit) | (UX) | popup.html (no input validation in markup) | accept | HTML has no `required` or `pattern` attributes; validation is JS-side in popup.js handler. |

All Plan 02 threats LOW; only T-114-02-01 (DOCTYPE preservation) requires explicit gate (covered by acceptance criteria).
</threat_model>

<verification>
After Plan 02 lands:
- `bash scripts/verify-phase-114.sh --static` partial pass: SC#2 (checkbox HTML check passes), SC#4 (CSS rule check passes), D-02 (4/6 popup files have header — Chrome 3 from Plan 00 + Safari html/css 2 from Plan 02; Safari popup.js still missing until Plan 03)
- SC#1 + SC#3 still fail at the popup.js level (their JS-side checks are Plan 03's territory)
- Safari popup.html is parseable HTML5 (DOCTYPE intact, all tags closed, comments well-formed)
- Safari popup.css is valid CSS (4 new rule blocks closed properly, no syntax errors — plug into a CSS validator if uncertain)
</verification>

<success_criteria>
- Safari popup.html has the include-url checkbox label, the shortcut hint div, and the dynamic success-text span (verbatim Chrome positions per Diff 1)
- Safari popup.css has the four verbatim Chrome rule blocks (`.url-toggle`, `.category-badge`, `.shortcut-hint`, `.analyzing`) with `#1D9E75` teal preserved (D-11)
- D-02 lockstep header comment is at line 2 of popup.html (DOCTYPE preserved at line 1) and line 1 of popup.css
- No setup-view markup or pre-existing CSS rules were modified
- verify-phase-114.sh `--static` SC#2 + SC#4 portions now pass (HTML/CSS gates closed)
- Plan 03 (popup.js) can now wire to `getElementById('include-url')` and `getElementById('success-text')` — both DOM nodes are present
</success_criteria>

<output>
After completion, create `.planning/phases/114-safari-extension-quick-capture-parity/114-02-SUMMARY.md` capturing: line counts before/after, the exact 4 edits applied (with line numbers in the post-edit file), confirmation that DOCTYPE is line 1 (Pitfall 6), and confirmation that the 4 CSS rules match Chrome popup.css byte-for-byte (D-11).
</output>
