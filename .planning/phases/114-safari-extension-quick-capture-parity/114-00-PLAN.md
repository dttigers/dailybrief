---
phase: 114
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify-phase-114.sh
  - .planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md
  - vigil-extension/popup.html
  - vigil-extension/popup.js
  - vigil-extension/popup.css
autonomous: true
requirements: [EXT-02]
tags: [safari-extension, webextension, popup-port, verify-script, human-uat]
must_haves:
  truths:
    - "Executor can run one command (`bash scripts/verify-phase-114.sh --static`) to verify every static SC#1–SC#4 grep + every D-02 lockstep header"
    - "SC#5 (xcodebuild clean build + codesign --verify --deep --strict on .app + .appex) is encoded as runnable script paths even if it can't fully run pre-implementation"
    - "SC#3 + SC#5 hardware-dependent verification is persisted in 114-HUMAN-UAT.md so /gsd-progress surfaces them until user confirms"
    - "Chrome-side popup.{html,js,css} carry the D-02 lockstep header comment so Safari side can mirror in Plans 02/03"
  artifacts:
    - path: "scripts/verify-phase-114.sh"
      provides: "One-shot verification script implementing SC#1–SC#5 automated checks"
      contains: "codesign --verify --deep --strict"
    - path: ".planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md"
      provides: "Pending UAT tracker for SC#3 probe attestation + SC#5 Safari restart smoke"
      contains: "status: ship-with-uat-pending"
    - path: "vigil-extension/popup.html"
      provides: "D-02 lockstep header comment (line 2, after DOCTYPE)"
      contains: "Keep in lockstep with"
    - path: "vigil-extension/popup.js"
      provides: "D-02 lockstep header comment (line 2, after 'use strict';)"
      contains: "Keep in lockstep with"
    - path: "vigil-extension/popup.css"
      provides: "D-02 lockstep header comment (line 1)"
      contains: "Keep in lockstep with"
  key_links:
    - from: "scripts/verify-phase-114.sh"
      to: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.{html,js,css}"
      via: "grep static checks for SC#1–SC#4 markers"
      pattern: "grep.*include-url|category-badge|metaKey"
    - from: "scripts/verify-phase-114.sh"
      to: "vigil-safari-extension/Vigil Capture.xcodeproj"
      via: "xcodebuild clean build (Plan 04 runtime path)"
      pattern: "xcodebuild clean build"
---

<objective>
Wave 0 scaffold: create the verify harness, HUMAN-UAT tracker, and the Chrome-side D-02 lockstep header comments — the cross-tree parity foundation that Plans 02/03 mirror to.

Purpose: Establish the Nyquist-compliant feedback harness BEFORE any Safari popup behavior changes. Per phase_specific_constraints, the verify script uses `xcodebuild clean build` (D-16) and `codesign --verify --deep --strict` (D-15, NOT `spctl --assess` which rejects Apple Development-signed builds by design — verified empirically in 114-RESEARCH.md). HUMAN-UAT.md scaffolds SC#3 (probe attestation row, filled by Plan 01 SUMMARY content) and SC#5 (Safari-restart-on-physical-Mac smoke), following 113-HUMAN-UAT.md shape. The Chrome-side lockstep header comments are added now so that Plans 02/03 can simply mirror the same comment shape into the Safari files.

Output: scripts/verify-phase-114.sh (executable), 114-HUMAN-UAT.md (ship-with-uat-pending), and 3 Chrome popup files with D-02 header comments. NO Safari popup file changes (those are Plans 02/03). NO Xcode project changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- IMPORTANT — EXECUTOR SUBSTITUTION RULE: Inside fenced code blocks below, the placeholder `<<YAML_DELIM>>` represents a literal `---` (three dashes on a line by themselves, the standard YAML frontmatter delimiter). When you write the actual file content, substitute `<<YAML_DELIM>>` with `---`. The placeholder exists ONLY because the gsd-tools frontmatter validator regex would otherwise mis-parse the embedded example as a second frontmatter block in this PLAN.md. -->

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-VALIDATION.md
@.planning/phases/113-verify-email-on-signup/113-HUMAN-UAT.md
@scripts/verify-phase-107.sh

<interfaces>
<!-- Scripts directory + script style convention -->

Scripts directory already exists at /Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/
Existing files: bootstrap.sh, build.sh, dailybrief-doctor.sh, install.sh, setup.sh, sync-anthropic-key.sh, verify-phase-107.sh
Convention: chmod +x; `#!/usr/bin/env bash` shebang; `set -euo pipefail`.

verify-phase-107.sh shape (canonical reference for verify-phase-114.sh):
  - SCRIPT_DIR / REPO_ROOT computation
  - MODE="${1:---static}" dispatch with --static / --runtime / --full
  - red/green/info ANSI helpers
  - Per-check functions
  - FAIL=0 accumulator → exit 1 if any check fails
  - case "$MODE" dispatcher

Phase 114 SCs map to verify modes:
  --static: SC#1, SC#2, SC#3 (handler grep — NOT the empirical probe), SC#4 (all four are pure grep on the 3 Safari popup files)
  --runtime: SC#5 (xcodebuild clean build + codesign --verify --deep --strict on .app and .appex)
  --full: both

Chrome popup file current state (NO header comments yet — this plan adds them):

vigil-extension/popup.html line 1: `<!DOCTYPE html>`
  → insert at line 2: `<!-- Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html — Phase 114 (D-02) -->`

vigil-extension/popup.js line 1: `'use strict';`
  → insert at line 2: `// Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.js — Phase 114 (D-02)`

vigil-extension/popup.css line 1: `* {`
  → insert at line 1: `/* Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.css — Phase 114 (D-02) */`

113-HUMAN-UAT.md frontmatter shape (verbatim target for 114-HUMAN-UAT.md):
```yaml
<<YAML_DELIM>>
phase: 113
requirement: AUTH-11
status: partial
source: [113-VALIDATION.md, 113-05-PLAN.md]
created: 2026-04-26
tested_by: jamesonmorrill1@gmail.com
deploy: <fill in>
deploy_time: <fill in>
tested_on: <fill in>
<<YAML_DELIM>>
```

Body uses H2 sections, H3 per-SC test entries, "### Steps", "### Assertions" with [ ] checkboxes, "### Observed" lab-notebook fields, and a "**Result:** [ ] PASS [ ] FAIL [ ] DEFERRED" line.

114 frontmatter target (deploy/deploy_time fields not relevant — extension ships locally; replace with rebuild SHA + rebuild_time):
```yaml
<<YAML_DELIM>>
phase: 114
requirement: EXT-02
status: ship-with-uat-pending
source: [114-VALIDATION.md, 114-CONTEXT.md, 114-RESEARCH.md]
created: 2026-04-26
tested_by: jamesonmorrill1@gmail.com
rebuild_sha: <commit SHA of Plan 04 final rebuild — fill in>
rebuild_time: <UTC timestamp of xcodebuild clean build success — fill in>
tested_on: <date — fill in when UAT is executed>
<<YAML_DELIM>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/verify-phase-114.sh with --static / --runtime / --full modes</name>
  <files>scripts/verify-phase-114.sh</files>
  <read_first>
    - scripts/verify-phase-107.sh (reference shell style — bash, set -euo pipefail, MODE dispatch, red/green/info helpers, FAIL accumulator)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (§"Validation Architecture" + §"Re-Sign + spctl --assess Mechanics" — the codesign command shapes are load-bearing per D-15)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-VALIDATION.md (§"Per-Task Verification Map" — authoritative grep commands lifted verbatim into checks)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-15 codesign-not-spctl, D-16 xcodebuild clean build)
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.html (current state — checkbox NOT yet present; --static will exit 1 today, this is expected)
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js (current state — URL prefill at lines 84-92, no keydown handler, no triage poll; --static will exit 1 today, expected)
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.css (current state — no .url-toggle / .category-badge / .shortcut-hint / .analyzing rules; --static will exit 1 today, expected)
  </read_first>
  <action>
Create `scripts/verify-phase-114.sh` executable (`chmod +x`). Use `/usr/bin/env bash` shebang with `set -euo pipefail`. Implement THREE modes selected by `$1`: `--static` (SC#1–SC#4 grep checks + D-02 lockstep grep), `--runtime` (SC#5 xcodebuild clean build + codesign verify), `--full` (both).

Use absolute paths computed from `SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"` and `REPO_ROOT="$( cd "$SCRIPT_DIR/.." && pwd )"`.

Exact script content (copy verbatim — do NOT paraphrase the codesign or xcodebuild invocations; D-15 + D-16 are load-bearing):

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
REPO_ROOT="$( cd "$SCRIPT_DIR/.." && pwd )"

# Safari popup file paths (Phase 114 edit targets)
SAFARI_DIR="$REPO_ROOT/vigil-safari-extension/Vigil Capture Extension/Resources"
SAFARI_HTML="$SAFARI_DIR/popup.html"
SAFARI_JS="$SAFARI_DIR/popup.js"
SAFARI_CSS="$SAFARI_DIR/popup.css"

# Chrome popup file paths (D-02 lockstep mirror)
CHROME_DIR="$REPO_ROOT/vigil-extension"
CHROME_HTML="$CHROME_DIR/popup.html"
CHROME_JS="$CHROME_DIR/popup.js"
CHROME_CSS="$CHROME_DIR/popup.css"

# Xcode build inputs (Plan 04 runtime path)
XCODEPROJ="$REPO_ROOT/vigil-safari-extension/Vigil Capture.xcodeproj"

MODE="${1:---static}"
FAIL=0

red()   { printf '\033[31m%s\033[0m\n' "$*"; }
green() { printf '\033[32m%s\033[0m\n' "$*"; }
info()  { printf '[verify-114] %s\n' "$*"; }

# ---------- SC#1: empty textarea + focus ----------
check_sc1_empty_init() {
  info "Check SC#1: popup.js empty-init + focus (no auto-prefill)"
  # POSITIVE: contentInput.value = ''; AND contentInput.focus();
  if ! grep -qE "contentInput\.value = '';" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing empty-string init: contentInput.value = '';"
    FAIL=1
    return
  fi
  if ! grep -qF "contentInput.focus()" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing contentInput.focus()"
    FAIL=1
    return
  fi
  # NEGATIVE: must NOT contain auto-prefill template literal `${title}`
  if grep -qE 'contentInput\.value = `\$\{title\}' "$SAFARI_JS"; then
    red "  FAIL — popup.js still has URL/title auto-prefill (must be removed for SC#1)"
    FAIL=1
    return
  fi
  green "  PASS — empty textarea + focus, no auto-prefill"
}

# ---------- SC#2: include-URL checkbox + verbatim Chrome append format ----------
check_sc2_include_url() {
  info "Check SC#2: include-url checkbox + verbatim append format"
  if ! grep -qF 'id="include-url"' "$SAFARI_HTML"; then
    red "  FAIL — popup.html missing checkbox with id=\"include-url\""
    FAIL=1
    return
  fi
  if ! grep -qF "getElementById('include-url')" "$SAFARI_JS" \
     && ! grep -qF 'getElementById("include-url")' "$SAFARI_JS"; then
    red "  FAIL — popup.js missing getElementById('include-url')"
    FAIL=1
    return
  fi
  # Verbatim Chrome format: \n\n${tab.title || 'Page'}: ${tab.url}
  if ! grep -qF "tab.title || 'Page'" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing verbatim Chrome URL-append format: tab.title || 'Page'"
    FAIL=1
    return
  fi
  green "  PASS — checkbox + verbatim append format present"
}

# ---------- SC#3: Cmd+Enter handler (static — empirical probe is Plan 01 HUMAN-UAT) ----------
check_sc3_cmd_enter_handler() {
  info "Check SC#3: Cmd+Enter keydown handler bound (empirical probe attested separately in 114-HUMAN-UAT.md)"
  if ! grep -qE "e\.metaKey \|\| e\.ctrlKey" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing Cmd+Enter check: e.metaKey || e.ctrlKey"
    FAIL=1
    return
  fi
  if ! grep -qF "captureBtn.click()" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing captureBtn.click() invocation"
    FAIL=1
    return
  fi
  # NEGATIVE: probe code must be reverted (no [probe] log line in popup.js after Plan 01 closes)
  if grep -qF '[probe]' "$SAFARI_JS"; then
    red "  FAIL — popup.js still contains [probe] code; revert before phase ships (Plan 01 D-03)"
    FAIL=1
    return
  fi
  green "  PASS — keydown handler bound, probe code reverted"
}

# ---------- SC#4: triage poll (800ms / 5s / category-badge) ----------
check_sc4_triage_poll() {
  info "Check SC#4: triage poll (800ms cadence, 5s timeout, category-badge render)"
  if ! grep -qF "setInterval" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing setInterval (triage poll loop)"
    FAIL=1
    return
  fi
  if ! grep -qE "Date\.now\(\) - startTime > 5000" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing 5000ms timeout: Date.now() - startTime > 5000"
    FAIL=1
    return
  fi
  if ! grep -qF "800" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing 800ms poll cadence"
    FAIL=1
    return
  fi
  if ! grep -qF "category-badge" "$SAFARI_JS"; then
    red "  FAIL — popup.js missing category-badge render"
    FAIL=1
    return
  fi
  # CSS rule must exist
  if ! grep -qF ".category-badge" "$SAFARI_CSS"; then
    red "  FAIL — popup.css missing .category-badge rule"
    FAIL=1
    return
  fi
  if ! grep -qF ".url-toggle" "$SAFARI_CSS"; then
    red "  FAIL — popup.css missing .url-toggle rule"
    FAIL=1
    return
  fi
  if ! grep -qF ".shortcut-hint" "$SAFARI_CSS"; then
    red "  FAIL — popup.css missing .shortcut-hint rule"
    FAIL=1
    return
  fi
  if ! grep -qF ".analyzing" "$SAFARI_CSS"; then
    red "  FAIL — popup.css missing .analyzing rule"
    FAIL=1
    return
  fi
  green "  PASS — 800ms / 5s / category-badge poll loop present"
}

# ---------- D-02 lockstep header comments on all 6 files ----------
check_d02_lockstep_headers() {
  info "Check D-02: lockstep header comment present in all 6 popup files"
  local files=(
    "$CHROME_HTML"
    "$CHROME_JS"
    "$CHROME_CSS"
    "$SAFARI_HTML"
    "$SAFARI_JS"
    "$SAFARI_CSS"
  )
  local missing=0
  for f in "${files[@]}"; do
    if ! grep -qF 'Keep in lockstep with' "$f"; then
      red "  MISSING D-02 header in: $f"
      missing=1
    fi
  done
  if [[ "$missing" -eq 1 ]]; then
    red "  FAIL — D-02 lockstep header comment absent from one or more popup files"
    FAIL=1
  else
    green "  PASS — D-02 lockstep header comments present in all 6 files"
  fi
}

# ---------- SC#5: xcodebuild clean build + codesign --verify --deep --strict ----------
check_sc5_xcodebuild() {
  info "Check SC#5a: xcodebuild clean build (D-16) succeeds"
  local build_log
  build_log="$(mktemp)"
  if xcodebuild clean build \
       -project "$XCODEPROJ" \
       -scheme "Vigil Capture" \
       -configuration Debug \
       -quiet > "$build_log" 2>&1; then
    green "  PASS — xcodebuild clean build succeeded"
    rm -f "$build_log"
  else
    red "  FAIL — xcodebuild clean build failed; log tail:"
    tail -30 "$build_log" >&2
    rm -f "$build_log"
    FAIL=1
  fi
}

check_sc5_codesign() {
  info "Check SC#5b: codesign --verify --deep --strict on .app and .appex (D-15)"
  local app_path
  app_path="$(find "$HOME/Library/Developer/Xcode/DerivedData" -maxdepth 6 -name 'Vigil Capture.app' -type d 2>/dev/null | head -1)"
  if [[ -z "$app_path" ]]; then
    red "  FAIL — no built 'Vigil Capture.app' found under DerivedData; run --runtime after Check SC#5a"
    FAIL=1
    return
  fi
  info "  found built app at: $app_path"

  # codesign --verify --deep --strict on container .app
  if codesign --verify --deep --strict --verbose=2 "$app_path" 2>&1; then
    green "  PASS — .app passes codesign --verify --deep --strict"
  else
    red "  FAIL — .app rejected by codesign --verify --deep --strict"
    FAIL=1
    return
  fi

  # codesign --verify --deep --strict on embedded .appex
  local appex_path="$app_path/Contents/PlugIns/Vigil Capture Extension.appex"
  if [[ ! -d "$appex_path" ]]; then
    red "  FAIL — embedded .appex not found at: $appex_path"
    FAIL=1
    return
  fi
  if codesign --verify --deep --strict --verbose=2 "$appex_path" 2>&1; then
    green "  PASS — .appex passes codesign --verify --deep --strict"
  else
    red "  FAIL — .appex rejected by codesign --verify --deep --strict"
    FAIL=1
  fi
}

run_static() {
  check_sc1_empty_init
  check_sc2_include_url
  check_sc3_cmd_enter_handler
  check_sc4_triage_poll
  check_d02_lockstep_headers
}

run_runtime() {
  check_sc5_xcodebuild
  check_sc5_codesign
}

case "$MODE" in
  --static)
    run_static
    ;;
  --runtime)
    run_runtime
    ;;
  --full|"")
    run_static
    run_runtime
    ;;
  *)
    echo "usage: $0 [--static|--runtime|--full]" >&2
    exit 2
    ;;
esac

if [[ "$FAIL" -ne 0 ]]; then
  red "verify-phase-114: FAILED"
  exit 1
fi
green "verify-phase-114: all checks passed"
exit 0
```

After writing, make it executable: `chmod +x "$REPO_ROOT/scripts/verify-phase-114.sh"`.

Per 114-RESEARCH.md Pitfall 5: `spctl --assess` is intentionally NOT in this script. D-15 codifies `codesign --verify --deep --strict` as the load-bearing automatable check. If the executor sees an instinct to add `spctl --assess` "for completeness," resist — it will reject Apple Development-signed builds by design (verified empirically 2026-04-25).
  </action>
  <verify>
    <automated>test -x "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-114.sh" && bash -n "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-114.sh" && grep -q 'codesign --verify --deep --strict' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-114.sh" && grep -q 'xcodebuild clean build' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-114.sh" && ! grep -q 'spctl --assess' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-114.sh"</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/verify-phase-114.sh` returns 0 (executable bit set)
    - `bash -n scripts/verify-phase-114.sh` exits 0 (syntax clean)
    - `grep -q 'codesign --verify --deep --strict' scripts/verify-phase-114.sh` returns 0 (D-15)
    - `grep -q 'xcodebuild clean build' scripts/verify-phase-114.sh` returns 0 (D-16)
    - `! grep -q 'spctl --assess' scripts/verify-phase-114.sh` returns 0 (D-15 prohibits spctl)
    - `grep -q 'Date\.now() - startTime > 5000' scripts/verify-phase-114.sh` returns 0 (SC#4 5s timeout grep)
    - `grep -qF 'category-badge' scripts/verify-phase-114.sh` returns 0 (SC#4 badge grep)
    - `grep -qF 'tab.title || .Page.' scripts/verify-phase-114.sh` returns 0 (SC#2 verbatim Chrome format grep)
    - `grep -qF 'e.metaKey || e.ctrlKey' scripts/verify-phase-114.sh` returns 0 (SC#3 handler grep)
    - `grep -qF 'Keep in lockstep with' scripts/verify-phase-114.sh` returns 0 (D-02 grep)
    - `grep -qF '[probe]' scripts/verify-phase-114.sh` returns 0 (negative grep guard for SC#3 revert)
    - `grep -qE 'case "\$MODE"' scripts/verify-phase-114.sh` returns 0 (mode dispatch present)
    - Running `bash scripts/verify-phase-114.sh --static` exits 1 today (popup files have not yet been edited; SC#1, SC#2, SC#3, SC#4, D-02 all expected to fail pre-implementation — this is correct)
  </acceptance_criteria>
  <done>
    Script exists, chmod +x, bash -n clean. `--static` exits 1 today (Plans 02/03 not yet landed; expected). `--full` is gated to post-Plan-04 final rebuild verification.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 114-HUMAN-UAT.md scaffold for SC#3 probe attestation + SC#5 hardware UAT</name>
  <files>.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/113-verify-email-on-signup/113-HUMAN-UAT.md (canonical frontmatter + lab-notebook body shape — copy verbatim, adapt fields)
    - .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md (Phase 107 hardware-UAT precedent — completed sample; 5-test structure, "expected/result/detail" body, "Summary" totals, "Gaps" YAML)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-VALIDATION.md (§"Manual-Only Verifications" — authoritative list of two human-eyeball items: SC#3 empirical probe + SC#5 Safari restart)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-12 ship-with-uat-pending; D-15 codesign-not-spctl wording for SC#5; D-03 probe attestation source)
  </read_first>
  <action>
Create `.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md`. Frontmatter mirrors 113-HUMAN-UAT.md shape with `phase: 114`, `requirement: EXT-02`, `status: ship-with-uat-pending` (D-12), and Phase 114-specific fields `rebuild_sha:` and `rebuild_time:` (replacing 113's `deploy:` / `deploy_time:` since this is a local extension rebuild, not a Railway deploy).

Body has two H2 SC sections (SC#3 + SC#5) plus a sign-off, following the 113 lab-notebook style (Steps / Assertions checkbox list / Observed values / Result PASS/FAIL/DEFERRED).

Exact content to write:

```markdown
<<YAML_DELIM>>
phase: 114
requirement: EXT-02
status: ship-with-uat-pending
source:
  - 114-VALIDATION.md
  - 114-CONTEXT.md
  - 114-RESEARCH.md
created: 2026-04-26
tested_by: jamesonmorrill1@gmail.com
rebuild_sha: <commit SHA after Plans 01-04 land — fill in>
rebuild_time: <UTC timestamp of xcodebuild clean build success — fill in>
tested_on: <date — fill in when UAT is executed>
<<YAML_DELIM>>

# Phase 114 — Human UAT (EXT-02)

End-to-end live verification of the Safari extension quick-capture parity port
on physical Mac hardware. Run AFTER Plan 04's `xcodebuild clean build` +
`codesign --verify --deep --strict` exits 0 and the rebuilt `.app` is launched
once to refresh the Safari extension binding.

This file is a lab notebook — fill in observed values during the run, tick each
checkbox, and update frontmatter `status: verified` when sign-off is complete.

<<YAML_DELIM>>

## Pre-flight

- [ ] Plans 01-04 merged to main.
- [ ] Plan 04 final `xcodebuild clean build` exited 0 (record SHA + UTC time in frontmatter).
- [ ] `codesign --verify --deep --strict --verbose=2 <APP>` exited 0 for both `.app` and `.appex`.
- [ ] Rebuilt `.app` was opened once via Finder or `open` to refresh Safari's extension binding.
- [ ] Safari fully quit (⌘Q, not just close window) and reopened — Pitfall 1 from 114-RESEARCH.md.
- [ ] Vigil Capture extension shows enabled in Safari → Settings → Extensions.

<<YAML_DELIM>>

## SC#3 — Cmd+Enter empirical probe attestation (FROM PLAN 01 SUMMARY)

Source: 114-VALIDATION.md Manual-Only row 1 / ROADMAP SC#3 / D-03.

> Verifies that: WebKit fires `metaKey: true` on a popup keydown when ⌘+Enter
> is pressed in the textarea — which is the empirical bar D-03/D-04 set
> BEFORE any Safari port code lands. Plan 01's throwaway probe captures the
> observed event shape verbatim from Web Inspector console; this row is
> filled in with the Plan 01 SUMMARY content as part of phase ship-out.

### Steps (already executed by Plan 01)

1. Apply Plan 01 probe (`console.log('[probe]', { key, metaKey, ctrlKey, code })` in keydown listener).
2. `xcodebuild clean build` (D-16).
3. `open <APP>` to refresh Safari's extension binding; quit Safari fully; reopen Safari.
4. Click Vigil Capture toolbar icon to open popup.
5. Right-click inside popup body → Inspect Element (Pitfall 2 — direct Develop menu closes the popup).
6. Click into textarea; press ⌘+Enter.
7. Read Web Inspector console output verbatim.
8. Revert probe code in same Plan 01 (commit-and-revert pair).
9. Record exact console line in Plan 01 SUMMARY.md.

### Assertions

- [ ] Plan 01 SUMMARY.md exists at `.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md`.
- [ ] Plan 01 SUMMARY.md contains the verbatim console log line from the probe run.
- [ ] The recorded log line shows `metaKey: true` when ⌘+Enter was pressed (D-04 success bar).
- [ ] No `[probe]` string remains in `vigil-safari-extension/Vigil Capture Extension/Resources/popup.js` after the revert (proves D-03 commit-and-revert closure).

### Observed (paste from Plan 01 SUMMARY)

- Date/time of probe run:
- Console log line (verbatim):
- metaKey value observed:
- ctrlKey value observed:
- key value observed:
- Probe revert commit SHA:

**Result:** [ ] PASS  [ ] FAIL  [ ] DEFERRED

<<YAML_DELIM>>

## SC#5 — Safari restart on physical Mac hardware: rebuilt extension still works

Source: 114-VALIDATION.md Manual-Only row 2 / ROADMAP SC#5 / D-12.

> Verifies that: after a Safari restart on physical Mac hardware (not just app
> relaunch), the rebuilt extension is still enabled and the new quick-capture
> popup behaviors (empty textarea, checkbox, Cmd+Enter, triage badge) all work
> end-to-end with the live vigil-core /v1/thoughts endpoint. Per D-15, the
> automatable codesign check (codesign --verify --deep --strict on .app +
> .appex) gates phase ship-out; this row gates UAT sign-off.

### Steps

1. Confirm pre-flight checks above are all ticked.
2. Quit Safari fully (⌘Q, not just close window).
3. Reopen Safari.
4. Open Safari → Settings → Extensions. Confirm "Vigil Capture" is enabled (toggle ON).
5. Click the Vigil Capture toolbar icon. Popup opens.
6. Observe: textarea is EMPTY (no auto-prefilled tab title or URL — SC#1).
7. Observe: focus is on the textarea (cursor blinks inside it — SC#1).
8. Observe: an "Include page URL" checkbox is visible, UNCHECKED by default (SC#2 + D-07).
9. Observe: a "Cmd+Enter to capture" hint is visible below the Capture button (SC#3 visual confirmation).
10. Type "phase 114 uat smoke test" in the textarea.
11. Press ⌘+Enter (do NOT click Capture).
12. Observe: button text briefly changes to "Capturing...", then success area appears with "Analyzing..." text.
13. Within 5 seconds, observe: success area updates to "✓ Captured!" + a category-badge pill (e.g. "Idea", "Task", "Reflection").
14. After ~1.5s, popup closes automatically.
15. In a second test, navigate to a real webpage (e.g. https://news.ycombinator.com), click the toolbar icon again, type "url append test", check the "Include page URL" checkbox, click Capture.
16. After capture, in PWA or curl: `GET /v1/thoughts` and confirm the most recent thought has BOTH the typed text AND the page URL appended in the format `\n\n${title}: ${url}` (verbatim Chrome format per D-06).

### Assertions

- [ ] Safari extension still enabled after Safari quit+reopen (Pitfall 1 — Safari aggressively caches; full quit is required).
- [ ] Popup opens with empty textarea, focus on textarea (SC#1).
- [ ] "Include page URL" checkbox visible, UNCHECKED by default (SC#2 + D-07).
- [ ] "Cmd+Enter to capture" shortcut hint visible (SC#3 visual).
- [ ] ⌘+Enter submits the capture without clicking the button (SC#3 functional).
- [ ] Success area renders "Analyzing..." then transitions to "✓ Captured!" + category-badge pill within 5 seconds (SC#4 success path D-09).
- [ ] If category never arrives in 5 seconds: success area renders "✓ Captured!" with NO badge, then closes (SC#4 timeout path D-10) — note this in observed if hit.
- [ ] Second test: checked-checkbox capture has `\n\n${title}: ${url}` appended in the resulting thought (SC#2 + D-06 verbatim format).

### Observed

- Safari version:
- macOS version:
- Popup opened: [ ] yes / [ ] no
- Textarea empty + focused: [ ] yes / [ ] no
- Checkbox default unchecked: [ ] yes / [ ] no
- "Cmd+Enter to capture" hint visible: [ ] yes / [ ] no
- ⌘+Enter submitted (no mouse click): [ ] yes / [ ] no
- Time from submit to category-badge appearing (seconds):
- Category-badge text rendered:
- Second test: URL appended in thought body: [ ] yes (paste excerpt) / [ ] no

**Result:** [ ] PASS  [ ] FAIL  [ ] DEFERRED

<<YAML_DELIM>>

## Sign-off

Complete after both SC sections are filled in.

- [ ] SC#3 PASS — Plan 01 SUMMARY captured `metaKey: true` empirically; probe code reverted from popup.js.
- [ ] SC#5 PASS — Safari restart preserved extension; all four quick-capture parity behaviors (empty textarea, checkbox, Cmd+Enter, triage badge) verified live on physical Mac hardware.
- [ ] No regressions in Phase 107 behaviors: extension still enabled across reboot, container app SMAppService.mainApp.register() still wired, persistence pill still renders.
- [ ] Status updated to `verified` in this file's frontmatter.
- [ ] Phase 114 closure committed.

Any failures: open a fix branch, document the failure mode below, and re-execute the affected SC after the fix lands. Per D-05, an SC#3 probe failure (metaKey: false or no event fires) requires a stop+replan, not a silent fallback.

Any deferred items: log to `.planning/phases/114-safari-extension-quick-capture-parity/deferred-items.md` with rationale.

## Gaps

# No gaps until user runs the tests. Format matches 113-HUMAN-UAT.md when entries land.
```
  </action>
  <verify>
    <automated>test -f "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md" && grep -q 'status: ship-with-uat-pending' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md" && grep -q 'phase: 114' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md" && grep -qE '^## SC#3' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md" && grep -qE '^## SC#5' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md`
    - `grep -q 'status: ship-with-uat-pending' 114-HUMAN-UAT.md` returns 0 (D-12)
    - `grep -q 'phase: 114' 114-HUMAN-UAT.md` returns 0
    - `grep -q 'requirement: EXT-02' 114-HUMAN-UAT.md` returns 0
    - `grep -qE '^## SC#3' 114-HUMAN-UAT.md` returns 0 (probe attestation section)
    - `grep -qE '^## SC#5' 114-HUMAN-UAT.md` returns 0 (Safari restart smoke section)
    - `grep -qF 'metaKey: true' 114-HUMAN-UAT.md` returns 0 (D-04 success bar referenced)
    - `grep -qF 'codesign --verify --deep --strict' 114-HUMAN-UAT.md` returns 0 (D-15 wording)
    - `! grep -q 'spctl --assess' 114-HUMAN-UAT.md` returns 0 (D-15 prohibits spctl)
    - `grep -qF '[probe]' 114-HUMAN-UAT.md` returns 0 (revert assertion references probe string)
    - `grep -qF 'rebuild_sha:' 114-HUMAN-UAT.md` returns 0 (frontmatter field for rebuild traceability)
    - File has lab-notebook checkbox structure: `grep -c '\[ \]' 114-HUMAN-UAT.md` returns at least 20 (~SC#3 + SC#5 assertions + sign-off)
  </acceptance_criteria>
  <done>
    114-HUMAN-UAT.md exists with ship-with-uat-pending frontmatter, SC#3 probe-attestation section, SC#5 Safari-restart smoke section, sign-off checklist, and matches 113-HUMAN-UAT.md lab-notebook structure.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add D-02 lockstep header comments to all 3 Chrome popup files</name>
  <files>vigil-extension/popup.html, vigil-extension/popup.js, vigil-extension/popup.css</files>
  <read_first>
    - vigil-extension/popup.html (line 1 must remain `<!DOCTYPE html>`; comment goes at line 2 per 114-RESEARCH.md Pitfall 6 — comment before DOCTYPE downgrades to quirks mode)
    - vigil-extension/popup.js (line 1 is `'use strict';`; comment goes at line 2)
    - vigil-extension/popup.css (no preamble; comment goes at line 1)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-02 wording — exact comment shape per file)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (Diff 4: Chrome side D-02 mirror table — line numbers + insert positions)
  </read_first>
  <action>
Insert the D-02 lockstep header comment into each Chrome popup file at the precise position. The path in the comment is the relative path from the Chrome file to the Safari counterpart.

EXACT INSERTIONS (do not paraphrase the comment text — D-02 fixes the wording):

**1. vigil-extension/popup.html** — Insert at line 2 (immediately after `<!DOCTYPE html>`, before `<html lang="en">`). The HTML5 spec requires DOCTYPE to be the very first thing in the document; a comment before DOCTYPE downgrades to quirks mode (Pitfall 6).

Resulting first 3 lines must read:
```
<!DOCTYPE html>
<!-- Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html — Phase 114 (D-02) -->
<html lang="en">
```

**2. vigil-extension/popup.js** — Insert at line 2 (immediately after `'use strict';`, before the blank line that precedes `const STORAGE_KEY`).

Resulting first 3 lines must read:
```
'use strict';
// Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.js — Phase 114 (D-02)

```
(line 3 is blank, preserving existing structure)

**3. vigil-extension/popup.css** — Insert at line 1 (the file currently starts with `* {` — push that to line 2; line 1 becomes the comment).

Resulting first 3 lines must read:
```
/* Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.css — Phase 114 (D-02) */
* {
  margin: 0;
```

Do NOT modify any other lines in any of the three files. Do NOT change the path separator (the relative path uses forward slashes `/` and contains a literal space in `Vigil Capture Extension`). The space in the path is fine inside an HTML/JS/CSS comment — it's not a shell argument.

Note for Plans 02/03: the Safari side mirrors these comments with a reversed relative path:
- Safari popup.html line 2: `<!-- Keep in lockstep with ../../../vigil-extension/popup.html — Phase 114 (D-02) -->`
- Safari popup.js line 2: `// Keep in lockstep with ../../../vigil-extension/popup.js — Phase 114 (D-02)`
- Safari popup.css line 1: `/* Keep in lockstep with ../../../vigil-extension/popup.css — Phase 114 (D-02) */`
(The `../../../` accounts for `Vigil Capture Extension/Resources/` being three levels deep from repo root.)

That mirroring is NOT this task's responsibility — Plans 02 (HTML/CSS) and 03 (JS) own it.
  </action>
  <verify>
    <automated>head -2 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-extension/popup.html" | tail -1 | grep -qF 'Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html' && head -2 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-extension/popup.js" | tail -1 | grep -qF 'Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.js' && head -1 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-extension/popup.css" | grep -qF 'Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.css' && head -1 "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-extension/popup.html" | grep -qF '<!DOCTYPE html>'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 vigil-extension/popup.html` is exactly `<!DOCTYPE html>` (DOCTYPE preserved as line 1 — Pitfall 6)
    - `head -2 vigil-extension/popup.html | tail -1` contains `Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html` (D-02 in line 2)
    - `head -3 vigil-extension/popup.html | tail -1` is exactly `<html lang="en">` (existing line 2 pushed to line 3)
    - `head -1 vigil-extension/popup.js` is exactly `'use strict';` (preserved)
    - `head -2 vigil-extension/popup.js | tail -1` contains `Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.js`
    - `head -1 vigil-extension/popup.css` contains `Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.css`
    - `head -2 vigil-extension/popup.css | tail -1` contains `* {` (existing line 1 pushed to line 2)
    - `git diff vigil-extension/popup.html | grep '^+' | grep -c 'Keep in lockstep'` returns 1 (exactly one new line)
    - `git diff vigil-extension/popup.js | grep '^+' | grep -c 'Keep in lockstep'` returns 1
    - `git diff vigil-extension/popup.css | grep '^+' | grep -c 'Keep in lockstep'` returns 1
    - No other lines modified in any of the 3 Chrome files (line counts: popup.html grows by 1, popup.js grows by 1, popup.css grows by 1)
  </acceptance_criteria>
  <done>
    All 3 Chrome popup files carry the D-02 lockstep header comment at the spec'd position. DOCTYPE remains line 1 of popup.html (no quirks-mode regression). Safari side will mirror in Plans 02/03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → verify script | Local shell script; runs as logged-in user; reads project files only; no network IO |
| Developer machine → 114-HUMAN-UAT.md | Local markdown file with hand-typed observations; no code execution; no secrets |
| Chrome extension popup files | Already-shipped extension; comment-only edits (no behavior change in Plan 00) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-00-01 | Tampering | scripts/verify-phase-114.sh | accept | Low — script lives in version control; same trust as verify-phase-107.sh. No new attack surface; reads project files, runs xcodebuild + codesign as logged-in user. |
| T-114-00-02 | Information Disclosure | 114-HUMAN-UAT.md | accept | Test descriptions only; no PII (test_by email already in 113-HUMAN-UAT.md frontmatter — same posture). No secrets, no tokens. |
| T-114-00-03 | Tampering | vigil-extension/popup.{html,js,css} D-02 header comments | accept | Comment-only edits to a shipped extension. Comments are inert in HTML/JS/CSS parsing. DOCTYPE preserved as line 1 of popup.html (Pitfall 6 mitigation = comment at line 2). |
| T1 (XSS via injected category) | Tampering | popup.js (referenced — actual change is Plan 03) | mitigate (downstream) | Server-side category enum at thoughts.ts:37-43 makes innerHTML interpolation safe. Plan 03 owns the actual badge render. Plan 00 only writes the verify-script grep that asserts `category-badge` rule is present. |
| T2 (URL exfiltration via auto-prefill) | Information Disclosure | popup.js (referenced — actual change is Plan 03) | mitigate (downstream) | Plan 03 removes the auto-prefill block. Plan 00 writes the negative-grep guard for `contentInput.value = \`${title}` in the verify script. |
| T3 (Token leakage in error messages) | Information Disclosure | popup.js error display (no change in this phase) | accept | Existing generic HTTP-status display; no change. |
| T4 (Cmd+Enter submits invalid content) | (UX, not security) | popup.js submit handler (no change in this phase) | accept | Existing `content.trim() === ''` guard preserved by verbatim port (Plan 03). |

All Phase 114 Wave 0 threats are LOW severity. No high-severity threats — phase is a comment-only addition + a shell script + a markdown scaffold. No ASVS L1 blocker.
</threat_model>

<verification>
Run `bash scripts/verify-phase-114.sh --static` — exits 1 today (SC#1, SC#2, SC#3, SC#4 all fail because Safari popup files have not yet been edited; D-02 header check passes for the 3 Chrome files but fails for the 3 Safari files until Plans 02/03 land). Script syntax clean: `bash -n scripts/verify-phase-114.sh` exits 0. `grep -F 'Keep in lockstep' vigil-extension/popup.{html,js,css}` returns 3 hits.

114-HUMAN-UAT.md parses as markdown with YAML frontmatter; SC#3 + SC#5 sections present; ~20+ checkboxes for assertion + sign-off rows.
</verification>

<success_criteria>
- `scripts/verify-phase-114.sh` exists, chmod +x, bash -n clean, contains `codesign --verify --deep --strict` and `xcodebuild clean build`, does NOT contain `spctl --assess` (D-15)
- `--static` mode runs all 5 static check groups (SC#1 + SC#2 + SC#3 + SC#4 + D-02)
- `--runtime` mode runs xcodebuild + codesign verify (Plan 04 will gate on this passing)
- `.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md` exists with ship-with-uat-pending frontmatter, SC#3 + SC#5 sections, lab-notebook checkbox structure matching 113-HUMAN-UAT.md
- All 3 Chrome popup files carry the D-02 lockstep header comment at correct line position; DOCTYPE preserved as line 1 of popup.html
- Plan 01 (Wave 1) can start against the harness immediately
</success_criteria>

<output>
After completion, create `.planning/phases/114-safari-extension-quick-capture-parity/114-00-SUMMARY.md` capturing: script path, HUMAN-UAT path, which static checks pass pre-implementation (only the D-02 Chrome-side portion), which fail (SC#1, SC#2, SC#3, SC#4, D-02 Safari-side — all expected pre-implementation), and Check `--runtime` is unreachable until Plan 04.
</output>
