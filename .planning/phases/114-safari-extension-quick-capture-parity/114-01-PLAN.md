---
phase: 114
plan: 01
type: execute
wave: 1
depends_on: ["114-00"]
files_modified:
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js
  - .planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md
autonomous: false
requirements: [EXT-02]
tags: [safari-extension, webextension, empirical-probe, cmd-enter, sc3-gate]
must_haves:
  truths:
    - "WebKit Safari popup keydown handler observes `metaKey: true` truthy when ⌘+Enter is pressed in the textarea (D-04 success bar)"
    - "Probe code is added in commit A and reverted in commit B WITHIN this plan — no `[probe]` string remains in popup.js after Plan 01 closes"
    - "114-01-SUMMARY.md captures the verbatim Web Inspector console line — frozen attestation that Plans 02/03 are unblocked"
    - "If probe FAILS (D-05), phase pauses with documented failure mode in 114-01-SUMMARY.md — NO silent autopilot fallback to `getModifierState('Meta')`"
  artifacts:
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.js"
      provides: "Probe-and-revert pair (commit A adds [probe] keydown logger, commit B reverts; final state matches pre-Plan-01 popup.js)"
      contains: "(post-revert) no '[probe]' string anywhere"
    - path: ".planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md"
      provides: "Frozen empirical attestation of WebKit metaKey behavior"
      contains: "metaKey: true"
  key_links:
    - from: "popup.js [probe] keydown listener (transient)"
      to: "Safari Web Inspector console"
      via: "console.log inside contentInput.addEventListener('keydown', ...)"
      pattern: "\\[probe\\] keydown"
    - from: "114-01-SUMMARY.md observation"
      to: "Plans 02/03 unblock decision"
      via: "D-04 metaKey:true gate / D-05 failure stop+replan"
      pattern: "metaKey:\\s*true"
---

<objective>
Throwaway empirical probe gate (D-03/D-04/D-05): prove WebKit Safari fires `metaKey: true` on a popup keydown for ⌘+Enter — BEFORE any quick-capture parity port code lands. SC#3's "verified empirically before any implementation code is written" is load-bearing per phase_specific_constraints.

Purpose: Plan 01 is two commits — (A) add a temporary `console.log('[probe]', { key, metaKey, ctrlKey, code })` inside a keydown listener on `contentInput`, then `xcodebuild clean build` (D-16) and human-observe in Safari Web Inspector; (B) revert the probe code so popup.js returns to its pre-Plan-01 state. Both commits live in this plan. The plan also produces 114-01-SUMMARY.md capturing the verbatim console line as a frozen attestation.

D-04 success bar: console line shows `metaKey: true` truthy when ⌘ is held during ⌘+Enter keydown. D-05 failure path: stop and replan. Per phase_specific_constraints, NO silent autopilot fallback to `getModifierState('Meta')` ships without explicit user buy-in.

Plan 01 is a hard dependency for Plans 02, 03, 04 — no implementation code lands until this plan's SUMMARY confirms the probe passed.

Output: popup.js back to its pre-Plan-01 byte-for-byte state (probe added then reverted), Plan 01 SUMMARY containing the verbatim Web Inspector console line, two commits in git history (probe-add + probe-revert) with `[probe(114)]` and `[revert(114)]` prefixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- IMPORTANT — EXECUTOR SUBSTITUTION RULE: Inside fenced code blocks below, the placeholder `<<YAML_DELIM>>` represents a literal `---` (three dashes on a line by themselves, the standard YAML frontmatter delimiter). When you write the actual file content, substitute `<<YAML_DELIM>>` with `---`. The placeholder exists ONLY because the gsd-tools frontmatter validator regex would otherwise mis-parse the embedded example as a second frontmatter block in this PLAN.md. -->

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-VALIDATION.md
@.planning/phases/114-safari-extension-quick-capture-parity/114-HUMAN-UAT.md
@scripts/verify-phase-114.sh

<interfaces>
<!-- Current Safari popup.js state (Plan 01 inserts probe INTO this) -->

vigil-safari-extension/Vigil Capture Extension/Resources/popup.js current shape:
- Line 1: `'use strict';`
- Line 2: blank
- Line 3: `const STORAGE_KEY = 'vigil_api_key';`
- Lines 79-146: `async function initCaptureView(apiKey) { ... }` (the function the probe attaches into)
- Line 80: `showView(captureView);`
- Lines 84-92: current URL pre-fill block (try/catch reads tab.title + tab.url, sets contentInput.value, calls focus + selection setters) — leave intact during probe; Plan 03 will replace this
- Line 90: `contentInput.focus();` (success branch)
- Line 95: `contentInput.focus();` (catch branch)

Probe insertion point: immediately after the catch-branch focus() call at line 95, BEFORE the blank line that precedes `captureBtn.onclick = async () => {`. This keeps the probe inside `initCaptureView` so it fires only when the popup is in capture mode (matches the Phase 02/03 port target — keydown listener on the textarea, not on document).

xcodebuild build output path (per 114-RESEARCH.md):
~/Library/Developer/Xcode/DerivedData/Vigil_Capture-*/Build/Products/Debug/Vigil Capture.app

Probe MUST use `xcodebuild clean build` (D-16) to eliminate Resources/* staleness — RESEARCH Open Q2 documented the staleness risk, D-16 closes it at ~30s additional cost per build.

Web Inspector access (Pitfall 2 — load-bearing):
- Direct "Develop → Show Web Inspector" closes the popup (focus shift dismisses extension popups)
- Workaround: right-click INSIDE the popup body → "Inspect Element"
- This is the ONLY reliable way to keep both popup and inspector alive simultaneously

First-launch NSAlert (Pitfall 3): A clean DerivedData wipes the `io.vigilhub.extension.firstLaunchAlertShown` UserDefaults flag → on first `open` of the rebuilt .app, an NSAlert "Vigil Capture is installed" fires once. Dismiss it before clicking the Safari toolbar icon. Subsequent launches are silent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add temporary [probe] keydown logger to Safari popup.js + xcodebuild clean build</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/popup.js</files>
  <read_first>
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js (current state — must understand existing initCaptureView body lines 79-146 to insert probe at the correct spot)
    - vigil-extension/popup.js (Chrome reference — note its keydown handler at lines 91-96 is what Plan 03 will port verbatim; the probe deliberately does NOT include the captureBtn.click() side-effect — pure observation only)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-03 probe shape, D-04 success bar, D-16 xcodebuild clean build)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (§"Cmd+Enter Empirical Probe Mechanics" Step 1 — exact probe block to insert; Pitfall 3 NSAlert handling)
  </read_first>
  <action>
Edit `vigil-safari-extension/Vigil Capture Extension/Resources/popup.js`. Inside the existing `initCaptureView` function, immediately AFTER the catch-branch `contentInput.focus();` call at line 95 (so the probe registers regardless of which try/catch branch executed), and BEFORE the blank line that precedes `captureBtn.onclick = async () => {`, insert exactly this block:

```javascript
  // PHASE 114 PROBE — REVERT BEFORE IMPLEMENTATION (D-03/D-04). DO NOT SHIP.
  contentInput.addEventListener('keydown', (e) => {
    console.log('[probe] keydown', { key: e.key, metaKey: e.metaKey, ctrlKey: e.ctrlKey, code: e.code });
  });
```

The probe MUST:
- Be 4 lines (sentinel comment + 3-line listener block)
- Use the literal string `[probe]` in the console.log first argument (the verify script greps this string as a negative guard for revert closure)
- Use the EXACT object key order `{ key, metaKey, ctrlKey, code }` (the SUMMARY attestation references this shape)
- NOT call `e.preventDefault()` (probe is observation-only — preventing default would mask whether normal Enter behavior is also subject to popup auto-close)
- NOT call `captureBtn.click()` (probe is pre-implementation; the actual handler ships in Plan 03)
- NOT replace the existing URL pre-fill block (lines 84-92) — Plan 03 owns that change. Probe coexists with the pre-Plan-01 behavior so the diff is purely additive.

After saving popup.js, commit with message `probe(114): temporary keydown logger for SC#3 Cmd+Enter empirical gate (D-03)`. Use `git add vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` (specific file, not -A).

Then run `xcodebuild clean build` (D-16, NOT just `xcodebuild build`):
```bash
xcodebuild clean build \
  -project "vigil-safari-extension/Vigil Capture.xcodeproj" \
  -scheme "Vigil Capture" \
  -configuration Debug \
  -quiet
```

The `clean` is load-bearing — RESEARCH Open Q2 documented the Resources/* staleness risk where `xcodebuild build` (without clean) can leave the popup files stale in the .appex while reporting build success. ~30s additional cost is acceptable for a load-bearing probe.

After xcodebuild succeeds, locate the rebuilt .app:
```bash
APP_PATH="$(find "$HOME/Library/Developer/Xcode/DerivedData" -maxdepth 6 -name 'Vigil Capture.app' -type d 2>/dev/null | head -1)"
echo "Built app: $APP_PATH"
open "$APP_PATH"
```

The `open` re-registers the extension with Safari (Pitfall 1). On first launch, the Phase 107 first-launch NSAlert "Vigil Capture is installed" may fire — that's expected; dismiss it.

Do NOT proceed to Task 2 until you have confirmed:
- xcodebuild exit code 0
- `find` returned a non-empty path
- `open` was called and the NSAlert (if shown) was dismissed

This task is autonomous (no human observation yet — that's Task 2).
  </action>
  <verify>
    <automated>grep -F '[probe] keydown' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.js" && grep -F 'PHASE 114 PROBE' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.js" && find "$HOME/Library/Developer/Xcode/DerivedData" -maxdepth 6 -name 'Vigil Capture.app' -type d 2>/dev/null | head -1 | grep -q 'Vigil Capture.app'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '[probe] keydown' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` returns 0 (probe block present)
    - `grep -F 'PHASE 114 PROBE' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` returns 0 (sentinel comment present)
    - `grep -F 'metaKey: e.metaKey' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` returns 0 (correct probe object shape)
    - `! grep -F 'e.preventDefault()' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` would NOT pass (file already may contain preventDefault elsewhere in Plan 03 territory) — instead use: `awk '/PHASE 114 PROBE/,/^  });$/' popup.js | grep -cF 'e.preventDefault()'` returns 0 (no preventDefault inside the probe block)
    - `git log -1 --oneline -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js | grep -qE 'probe\(114\)'` returns 0 (probe-add commit landed)
    - `find "$HOME/Library/Developer/Xcode/DerivedData" -maxdepth 6 -name 'Vigil Capture.app' -type d` returns a non-empty path (xcodebuild succeeded)
    - The built .app's embedded popup.js contains the probe block: `find ... 'Vigil Capture.app' ... -path '*PlugIns/Vigil Capture Extension.appex/Contents/Resources/popup.js' -exec grep -lF '[probe] keydown' {} \;` returns the file path (proves clean build copied changes — D-16)
  </acceptance_criteria>
  <done>
    Probe block inserted in correct position, committed with `probe(114):` prefix, `xcodebuild clean build` succeeded, rebuilt .app exists at DerivedData path, NSAlert dismissed if shown.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN — Open Safari popup, observe ⌘+Enter keydown event in Web Inspector console</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/popup.js</files>
  <read_first>
    - .planning/phases/114-safari-extension-quick-capture-parity/114-RESEARCH.md (§"Cmd+Enter Empirical Probe Mechanics" Steps 3-5 — Safari quit/relaunch sequence, right-click→Inspect Element workaround for Pitfall 2, exact expected console output)
    - .planning/phases/114-safari-extension-quick-capture-parity/114-CONTEXT.md (D-04 success bar = metaKey:true; D-05 failure stop+replan = NO silent fallback)
  </read_first>
  <action>
**This is a checkpoint:human-verify task.** Claude pauses for the human to perform the steps below; there is no Claude-side automation in this task. Wait for the resume-signal before proceeding to the next task.

**Context (what was built before this checkpoint):**

Task 1 added a temporary `[probe] keydown` logger inside `initCaptureView` and ran `xcodebuild clean build`. The rebuilt `Vigil Capture.app` is at `~/Library/Developer/Xcode/DerivedData/Vigil_Capture-*/Build/Products/Debug/Vigil Capture.app` and was already `open`'d to refresh Safari's extension binding. Now requires human-on-keyboard observation in Safari Web Inspector.

**HUMAN STEPS (perform in order):**

1. **Safari quit + reopen** (Pitfall 1 — load-bearing):
   - Quit Safari fully (⌘Q from Safari menu, NOT just close window)
   - Reopen Safari

2. **Confirm extension still enabled**:
   - Safari → Settings → Extensions
   - Confirm "Vigil Capture" toggle is ON
   - If grayed out / disabled: Develop menu → Allow Unsigned Extensions (Phase 107 already passed this; should not be needed, but documented as fallback in 114-RESEARCH.md)

3. **Open the popup**:
   - Click the Vigil Capture toolbar icon in the Safari toolbar
   - The popup should open showing either the setup view (if no API key) or the capture view (textarea + Capture button)
   - If setup view: paste your `vk_*` API key, click Save Key — popup will switch to capture view
   - If a "Vigil Capture is installed" NSAlert appears (Pitfall 3 first-launch): dismiss it; the alert is from the .app, not the popup

4. **Open Web Inspector via right-click → Inspect Element** (Pitfall 2 — DO NOT use Develop menu, that closes the popup):
   - Right-click ANYWHERE in the popup body that is NOT the textarea (e.g. the "Capture Thought" header, the gear icon, the empty space below the button)
   - From the context menu, choose "Inspect Element"
   - The Web Inspector docks to the popup; both stay open

5. **Click into the popup textarea** (so the keydown listener is in scope):
   - Click inside the textarea (the "What's on your mind?" placeholder area)
   - Cursor should now be in the textarea

6. **Observe the Console tab in Web Inspector**:
   - Switch to the "Console" tab in Web Inspector if not already there
   - Clear any existing log entries (trash can icon) for cleanliness

7. **Press ⌘+Enter** (the empirical probe):
   - Hold the Command (⌘) key, press Enter
   - Web Inspector console should print exactly one line
   - **Expected (D-04 PASS bar):** `[probe] keydown {key: "Enter", metaKey: true, ctrlKey: false, code: "Enter"}` (or similar — the metaKey: true is the only field that matters)
   - **Failure modes (D-05 STOP+REPLAN, not silent fallback):**
     - No log line appears at all → keydown event is suppressed; report this verbatim
     - `metaKey: false` → WebKit doesn't propagate the modifier on keydown; report this verbatim (RESEARCH says this is unlikely — the WebKit keyup quirk doesn't apply to keydown — but document if it does happen)
     - Log fires but for a different key → wrong listener target; report this verbatim
     - Anything else unexpected → report verbatim

8. **Capture the EXACT console output**:
   - Right-click the log line in Web Inspector → Copy (or transcribe by hand if Copy doesn't work)
   - Paste/write the verbatim line below in the resume signal

9. **Optional: also try plain Enter** (sanity, not gating):
   - Click into textarea again, press just Enter (no ⌘)
   - Should log `metaKey: false` — confirms the listener fires for any keydown, not just modifier-held cases
   - This is informational; not part of the success bar

10. **Close the popup** when done (click outside, or close Safari):
    - The probe code stays in popup.js; Task 3 reverts it.

**RESUME SIGNAL (paste in chat to unblock the next task):**

Reply with the VERBATIM console output line (or describe the failure mode in detail).

    PASS template (D-04 bar met):
      `[probe] keydown {key: "Enter", metaKey: true, ctrlKey: false, code: "Enter"}` — proceed to Task 3 (revert).

    FAIL template (D-05 stop+replan, NO silent fallback):
      Describe what happened: log line absent / metaKey false / different shape / etc. Plan halts. User decides next step (accept different shortcut, drop SC#3, or investigate WebKit-specific event handling). NO `getModifierState('Meta')` fallback ships without explicit go-ahead.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — Claude must wait for the human resume-signal. The durable attestation is captured in the SUMMARY/UAT file written by the next task."</automated>
  </verify>
  <done>
    Human has executed the steps above and reported the resume-signal. Claude has the verbatim observation needed to proceed to the next task (which writes the durable attestation to the SUMMARY/UAT file).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Revert probe code from popup.js + write 114-01-SUMMARY.md attestation</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/popup.js, .planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md</files>
  <read_first>
    - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js (current state — has the probe block from Task 1; Task 3 removes those exact 4 lines + the trailing blank line if a stray one was added)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY structure used by other Phase plans)
    - .planning/phases/107-safari-extension-persistence/107-01-SUMMARY.md (Phase 107 SUMMARY shape — reference)
    - Task 2's resume-signal (the verbatim Web Inspector console line — this is the load-bearing attestation content)
  </read_first>
  <action>
**Step A: Revert the probe block.**

Edit `vigil-safari-extension/Vigil Capture Extension/Resources/popup.js`. Remove the exact 4-line block inserted in Task 1:
```javascript
  // PHASE 114 PROBE — REVERT BEFORE IMPLEMENTATION (D-03/D-04). DO NOT SHIP.
  contentInput.addEventListener('keydown', (e) => {
    console.log('[probe] keydown', { key: e.key, metaKey: e.metaKey, ctrlKey: e.ctrlKey, code: e.code });
  });
```

Plus any trailing blank line that was added with the block. After removal, popup.js MUST be byte-for-byte identical to its pre-Plan-01 state. Verify with `git diff vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` — the diff vs. HEAD~1 (probe-add commit) should be exactly the inverse of Task 1's diff.

Commit message: `revert(114): remove keydown probe — observed metaKey:true confirmed (D-03 closure)`. Use `git add vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` (specific file).

If Task 2 reported FAILURE (D-05 path), the commit message instead reads `revert(114): remove keydown probe — D-05 failure path; phase paused for replan`. The plan still completes (probe is reverted regardless); the SUMMARY captures the failure mode and the user decides next steps.

**Step B: Write 114-01-SUMMARY.md.**

Create `.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md` with the exact content below. Substitute `{{VERBATIM_CONSOLE_LINE}}` with the line from Task 2's resume signal. Substitute `{{PROBE_RESULT}}` with `PASS` or `FAIL`. Substitute `{{PROBE_ADD_SHA}}` and `{{PROBE_REVERT_SHA}}` with the actual git commit SHAs (use `git log --oneline -2 -- popup.js | awk '{print $1}'` to get them — second line is probe-add, first line is revert).

```markdown
<<YAML_DELIM>>
phase: 114
plan: 01
type: execute
status: complete
completed: <UTC ISO-8601 timestamp at write time>
probe_result: {{PROBE_RESULT}}
unblocks: ["114-02", "114-03", "114-04"]
source:
  - 114-CONTEXT.md (D-03/D-04/D-05)
  - 114-RESEARCH.md §"Cmd+Enter Empirical Probe Mechanics"
  - 114-VALIDATION.md (Manual-Only row 1)
<<YAML_DELIM>>

# Phase 114 Plan 01 — Cmd+Enter Empirical Probe (SC#3)

## Outcome

**{{PROBE_RESULT}}** — `metaKey: true` was/was not observed in WebKit Safari popup
keydown event when ⌘+Enter was pressed in the textarea.

## Verbatim Web Inspector console output

```
{{VERBATIM_CONSOLE_LINE}}
```

## D-04 success bar

D-04 reads: "Probe success bar: `metaKey: true` fires when ⌘ is held during the keydown.
The Chrome handler keys off `e.metaKey || e.ctrlKey`; if Safari fires `metaKey` truthy
on ⌘+Enter, parity port is unblocked."

The observed line above {{matches | does not match}} this bar.

## Probe lifecycle

- Probe-add commit: `{{PROBE_ADD_SHA}}` — `probe(114): temporary keydown logger for SC#3 Cmd+Enter empirical gate (D-03)`
- Probe-revert commit: `{{PROBE_REVERT_SHA}}` — `revert(114): remove keydown probe — observed metaKey:true confirmed (D-03 closure)`
- Net effect on popup.js: zero lines changed (commit-and-revert pair within Plan 01 per D-03)

## What this attestation enables

If PASS: Plans 02/03/04 are unblocked. Chrome handler `e.metaKey || e.ctrlKey`
ports verbatim into Safari popup.js (Plan 03 owns the actual port).

If FAIL (D-05 path): Phase is paused. NO silent autopilot fallback to
`e.getModifierState('Meta')` ships without explicit user buy-in. User decides:
(a) accept a different shortcut, (b) drop SC#3, or (c) investigate
WebKit-specific event handling.

## Pitfalls observed (or not)

- Pitfall 1 (Safari aggressive caching): Safari was fully quit (⌘Q) and reopened
  before the probe ran. {{Confirmed: extension picked up rebuild | Did NOT confirm}}.
- Pitfall 2 (Web Inspector closes popup): right-click → Inspect Element used
  successfully {{or: Develop menu was used and popup closed; needed to retry}}.
- Pitfall 3 (first-launch NSAlert): {{NSAlert appeared and was dismissed | NSAlert did not appear because flag was already set}}.

## Verification at plan close

- `! grep -qF '[probe]' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` exits 0 (probe code reverted)
- `! grep -qF 'PHASE 114 PROBE' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` exits 0 (sentinel comment also reverted)
- `git log -2 --oneline -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` shows probe-add followed by revert
- `git diff HEAD~2 HEAD -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` is empty (net-zero diff over the pair)

## Plan 04 / 114-HUMAN-UAT.md handoff

Plan 04's HUMAN-UAT (SC#3 row) cites this SUMMARY as the source of the
empirical attestation. Paste the verbatim console line into the
`Observed → Console log line (verbatim)` field of `114-HUMAN-UAT.md`.
```

After writing, commit the SUMMARY: `docs(114-01): probe attestation — observed metaKey:true (D-03 closure)`. Use `git add .planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md`.

**Step C: Sanity-rebuild (optional but recommended).**

Run `xcodebuild clean build` once more to confirm the popup.js revert produces a clean build:
```bash
xcodebuild clean build \
  -project "vigil-safari-extension/Vigil Capture.xcodeproj" \
  -scheme "Vigil Capture" \
  -configuration Debug \
  -quiet
```

Exit 0 expected. This is not a verification gate (verify-phase-114.sh --runtime is the gate, run after Plan 04), but it catches any accidental syntax break introduced by the manual revert.
  </action>
  <verify>
    <automated>! grep -qF '[probe]' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.js" && ! grep -qF 'PHASE 114 PROBE' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-safari-extension/Vigil Capture Extension/Resources/popup.js" && test -f "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md" && grep -q 'probe_result:' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md" && grep -qE 'metaKey:\s*true|metaKey:\s*false' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -qF '[probe]' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` exits 0 (probe string fully removed)
    - `! grep -qF 'PHASE 114 PROBE' vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` exits 0 (sentinel comment removed)
    - `git log --oneline -2 -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js | grep -cE 'probe\(114\)|revert\(114\)'` returns 2 (probe-add + revert pair both present in history)
    - `git diff HEAD~2 HEAD -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` produces empty output (net-zero diff)
    - `test -f .planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md` returns 0
    - `grep -q 'probe_result:' 114-01-SUMMARY.md` returns 0 (frontmatter field set)
    - `grep -qE 'metaKey:\s*true|metaKey:\s*false' 114-01-SUMMARY.md` returns 0 (verbatim console output captured)
    - `grep -q 'unblocks:' 114-01-SUMMARY.md` returns 0 (downstream gate links recorded)
    - `grep -q 'Plan 04' 114-01-SUMMARY.md` returns 0 (handoff to HUMAN-UAT documented)
    - SUMMARY committed: `git log -1 --oneline -- .planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md | grep -qE 'docs\(114-01\)'` returns 0
    - Optional sanity rebuild (if executed): `xcodebuild clean build` exit 0
  </acceptance_criteria>
  <done>
    popup.js is byte-for-byte identical to its pre-Plan-01 state (probe-add + revert net to zero); 114-01-SUMMARY.md exists with verbatim console attestation, probe_result field set, downstream unblock list. Plans 02/03/04 are unblocked iff probe_result == PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Safari extension popup (transient probe) | Extension code transiently logs keydown event details to local Web Inspector console. Probe is reverted in same plan. |
| Console.log output → Web Inspector | Local browser devtools only; no network egress; observable only by logged-in user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01-01 | Information Disclosure | console.log of keydown events (transient) | accept | Probe logs key/metaKey/ctrlKey/code only — no API key, no thought content, no PII. Logs visible only in local Web Inspector. Reverted in same plan; no log persists in shipped extension. |
| T-114-01-02 | Tampering | popup.js (probe insertion + revert) | mitigate | Two-commit probe-and-revert pair gates every later plan on the verify-phase-114.sh `[probe]` negative-grep check (Plan 00 Task 1 step). If revert fails, --static fails on Plan 02/03 verification, blocking ship. |
| T-114-01-03 | DoS | Plan 01 keydown listener (transient) | accept | Listener attaches to a single textarea in a popup with finite lifetime. No accumulation across popup opens (popup destroys JS context on close per RESEARCH Assumption A3). |
| T1 (XSS via injected category) | Tampering | popup.js (no badge code in this plan) | n/a (downstream) | Plan 03 owns badge render. Plan 01 probe does not touch innerHTML. |
| T2 (URL exfiltration via auto-prefill) | Information Disclosure | popup.js URL-prefill block (untouched in Plan 01) | n/a (downstream) | Plan 03 removes the block. Plan 01 probe coexists with the block — only added a listener, did not change existing prefill. |
| T3 (Token leakage in error messages) | Information Disclosure | unaffected | accept | No error path touched. |
| T4 (Cmd+Enter triggers invalid submit) | (UX) | unaffected | accept | Probe uses pure observation (no preventDefault, no captureBtn.click), so even if ⌘+Enter were submitted in current pre-Plan-03 popup, the existing `content.trim() === ''` guard at popup.js:99 would still block invalid submits. |

All Plan 01 threats are LOW severity (transient probe; no user data captured; revert is gate-checked). No ASVS L1 blocker.
</threat_model>

<verification>
- After Task 1: probe block in popup.js, xcodebuild clean build exits 0, .app rebuilt under DerivedData
- After Task 2: human attestation captured (PASS or FAIL with verbatim console line)
- After Task 3: probe block removed (verify-phase-114.sh check_sc3_cmd_enter_handler's `[probe]` negative grep would PASS — but the positive grep for the keydown handler would still FAIL until Plan 03 adds the real handler; this is correct, Plan 01 only proves the empirical bar, not the implementation), 114-01-SUMMARY.md committed with verbatim console line + probe_result field

Plan 01 closure: `git log --oneline | head -3` shows (top to bottom): docs(114-01) SUMMARY commit → revert(114) probe-revert → probe(114) probe-add → (Plan 00 commits below).

`git diff HEAD~2 HEAD -- vigil-safari-extension/Vigil\ Capture\ Extension/Resources/popup.js` empty across the probe-add+revert pair.
</verification>

<success_criteria>
- Probe was added, observed in Web Inspector by human, and reverted — within this plan
- 114-01-SUMMARY.md captures verbatim Web Inspector console output line for SC#3 attestation
- `probe_result: PASS` in SUMMARY frontmatter unblocks Plans 02/03/04
- `probe_result: FAIL` (D-05) halts the phase — user decides path; no silent fallback
- popup.js is byte-for-byte identical to its pre-Plan-01 state (commit-and-revert net-zero)
- Plan 04's HUMAN-UAT.md SC#3 row can be filled in directly from this SUMMARY's verbatim console line
</success_criteria>

<output>
After completion, ensure `.planning/phases/114-safari-extension-quick-capture-parity/114-01-SUMMARY.md` exists and is committed. The SUMMARY itself is the deliverable for Plan 01 (no additional summary file needed beyond what Task 3 wrote).
</output>
