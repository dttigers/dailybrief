---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [Sources/JarvisCore/Models/WorkOrder.swift, Sources/JarvisCore/Models/GameScore.swift, Sources/JarvisCore/Models/ReminderItem.swift, Sources/JarvisCore/Models/StandingsEntry.swift, Sources/JarvisCore/Models/DailyBriefData.swift, Sources/JarvisCore/Config/AppConfig.swift, Sources/JarvisCore/Config/ConfigLoader.swift, Sources/DailyBrief/DailyBrief.swift, Sources/DailyBrief/Services/GmailService.swift, Sources/DailyBrief/Services/SportsService.swift, Sources/DailyBrief/Services/RemindersService.swift, Sources/DailyBrief/Services/AIService.swift, Sources/DailyBrief/Services/CompletionStore.swift, Sources/DailyBrief/PDF/PDFGenerator.swift, Sources/DailyBrief/PDF/PageOneRenderer.swift, Sources/DailyBrief/PDF/PageTwoRenderer.swift, Sources/DailyBrief/PDF/PDFStyles.swift, Sources/DailyBrief/Utilities/Logger.swift, Sources/DailyBrief/Utilities/PrintService.swift, Package.swift]
autonomous: true
---

<objective>
Move existing shared models and configuration into JarvisCore so CLI, menu bar, and future dashboard all import from one library.

Purpose: Eliminates code duplication across targets. After this, adding a new feature (dashboard, capture UI) only needs `import JarvisCore` — no copy-pasting models or config structs.
Output: Models and config live in JarvisCore. DailyBrief CLI and DailyBriefMonitor import from JarvisCore. All existing functionality unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@Package.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBrief/Models/WorkOrder.swift
@Sources/DailyBrief/Models/GameScore.swift
@Sources/DailyBrief/Models/ReminderItem.swift
@Sources/DailyBrief/Models/StandingsEntry.swift
@Sources/DailyBrief/Models/DailyBriefData.swift
@Sources/DailyBrief/Config/AppConfig.swift
@Sources/DailyBrief/Config/ConfigLoader.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move models and config into JarvisCore</name>
  <files>Sources/JarvisCore/Models/WorkOrder.swift, Sources/JarvisCore/Models/GameScore.swift, Sources/JarvisCore/Models/ReminderItem.swift, Sources/JarvisCore/Models/StandingsEntry.swift, Sources/JarvisCore/Models/DailyBriefData.swift, Sources/JarvisCore/Config/AppConfig.swift, Sources/JarvisCore/Config/ConfigLoader.swift</files>
  <action>
  Move these files from `Sources/DailyBrief/` to `Sources/JarvisCore/`:
  - `Models/WorkOrder.swift` → `Sources/JarvisCore/Models/WorkOrder.swift`
  - `Models/GameScore.swift` → `Sources/JarvisCore/Models/GameScore.swift`
  - `Models/ReminderItem.swift` → `Sources/JarvisCore/Models/ReminderItem.swift`
  - `Models/StandingsEntry.swift` → `Sources/JarvisCore/Models/StandingsEntry.swift`
  - `Models/DailyBriefData.swift` → `Sources/JarvisCore/Models/DailyBriefData.swift`
  - `Config/AppConfig.swift` → `Sources/JarvisCore/Config/AppConfig.swift`
  - `Config/ConfigLoader.swift` → `Sources/JarvisCore/Config/ConfigLoader.swift`

  After moving, update access control:
  - All struct/class/enum declarations: add `public` keyword
  - All stored properties used by DailyBrief: add `public` keyword
  - All initializers: add `public init(...)` (synthesized memberwise inits become internal when type is public — add explicit public inits where needed)
  - All enum cases are already public when the enum is public
  - Static methods on ConfigLoader: add `public`
  - Nested config structs in AppConfig: add `public` to each

  Do NOT move Services (GmailService, SportsService, etc.) — they have CLI-specific dependencies (Process, EventKit) that don't belong in the shared library yet.
  Do NOT move PDF layer or Utilities — they're CLI-specific.

  Remove the now-empty `Sources/DailyBrief/Models/` and `Sources/DailyBrief/Config/` directories.
  </action>
  <verify>`swift build` for JarvisCore target succeeds — all moved types compile in their new location</verify>
  <done>All model and config types live in Sources/JarvisCore/. Old locations removed. JarvisCore compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update DailyBrief and DailyBriefMonitor to import from JarvisCore</name>
  <files>Sources/DailyBrief/DailyBrief.swift, Sources/DailyBrief/Services/GmailService.swift, Sources/DailyBrief/Services/SportsService.swift, Sources/DailyBrief/Services/RemindersService.swift, Sources/DailyBrief/Services/AIService.swift, Sources/DailyBrief/Services/CompletionStore.swift, Sources/DailyBrief/PDF/PDFGenerator.swift, Sources/DailyBrief/PDF/PageOneRenderer.swift, Sources/DailyBrief/PDF/PageTwoRenderer.swift, Sources/DailyBrief/PDF/PDFStyles.swift, Sources/DailyBrief/Utilities/Logger.swift, Sources/DailyBrief/Utilities/PrintService.swift</files>
  <action>
  Add `import JarvisCore` to every file in Sources/DailyBrief/ that references types now in JarvisCore (WorkOrder, GameScore, ReminderItem, StandingsEntry, DailyBriefData, AppConfig, ConfigLoader, and any nested config types).

  Scan each file for references to moved types. Files that likely need the import:
  - `DailyBrief.swift` — uses AppConfig, ConfigLoader, DailyBriefData, all model types
  - `GmailService.swift` — uses WorkOrder, AppConfig (GmailConfig)
  - `SportsService.swift` — uses GameScore, StandingsEntry, AppConfig (SportsConfig)
  - `RemindersService.swift` — uses ReminderItem
  - `AIService.swift` — uses AppConfig (AIConfig)
  - `CompletionStore.swift` — uses WorkOrder
  - `PDFGenerator.swift` — uses DailyBriefData
  - `PageOneRenderer.swift` — uses WorkOrder, ReminderItem
  - `PageTwoRenderer.swift` — uses GameScore, StandingsEntry
  - `PDFStyles.swift` — likely no model references (just CoreGraphics types)
  - `Logger.swift` — likely no model references
  - `PrintService.swift` — likely no model references

  Also check `Sources/DailyBriefMonitor/` files — StatusChecker may reference types if it parses output.

  Place `import JarvisCore` after system framework imports, before any code, following existing import order convention (system → third-party → internal).

  Do NOT change any logic, just fix imports so existing functionality compiles identically.
  </action>
  <verify>`swift build` succeeds for ALL three targets (JarvisCore, DailyBrief, DailyBriefMonitor). Run `swift build 2>&1 | grep -c error` returns 0. Verify the DailyBrief CLI binary still runs: `.build/debug/DailyBrief --help` prints help text.</verify>
  <done>All targets compile. DailyBrief CLI produces identical output. DailyBriefMonitor builds. Models and config are imported from JarvisCore — shared codebase established.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for all three targets with zero errors
- [ ] `.build/debug/DailyBrief --help` shows help text (CLI still works)
- [ ] No duplicate type definitions (models only in JarvisCore, not DailyBrief)
- [ ] `Sources/DailyBrief/Models/` directory removed (empty)
- [ ] `Sources/DailyBrief/Config/` directory removed (empty)
- [ ] All JarvisCore types are `public`
</verification>

<success_criteria>

- All shared models and config live exclusively in Sources/JarvisCore/
- DailyBrief CLI compiles and runs identically to before migration
- DailyBriefMonitor compiles
- No duplicate type definitions across targets
- Foundation is complete: JarvisCore library with GRDB storage + migrated shared types
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
