---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Package.swift, Sources/JarvisCore/JarvisCore.swift]
autonomous: true
---

<objective>
Create JarvisCore SPM library target with GRDB dependency so CLI, menu bar, and future dashboard share one codebase.

Purpose: Foundation for all shared code — models, storage, config. Without this, every target duplicates types.
Output: Package.swift with 3 targets (JarvisCore library, DailyBrief CLI, DailyBriefMonitor), GRDB dependency declared, JarvisCore compiles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

@Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JarvisCore library target and GRDB dependency to Package.swift</name>
  <files>Package.swift</files>
  <action>
  Modify Package.swift to:
  1. Add GRDB dependency: `.package(url: "https://github.com/groue/GRDB.swift.git", from: "7.0.0")`
  2. Add new library target `JarvisCore` with:
     - Path: `Sources/JarvisCore`
     - Dependencies: `GRDB` (product name "GRDB" from package "GRDB.swift")
     - No linker settings needed (GRDB is pure Swift)
  3. Update `DailyBrief` executableTarget to depend on `JarvisCore`
  4. Update `DailyBriefMonitor` executableTarget to depend on `JarvisCore`

  Keep existing dependencies (swift-argument-parser) and linker settings unchanged.
  Use `.library(name: "JarvisCore", targets: ["JarvisCore"])` in products array.
  </action>
  <verify>`swift package resolve` succeeds and fetches GRDB</verify>
  <done>Package.swift has JarvisCore library target, GRDB dependency resolves, both executables depend on JarvisCore</done>
</task>

<task type="auto">
  <name>Task 2: Create JarvisCore directory structure with placeholder</name>
  <files>Sources/JarvisCore/JarvisCore.swift, Sources/JarvisCore/Models/.gitkeep, Sources/JarvisCore/Storage/.gitkeep, Sources/JarvisCore/Config/.gitkeep</files>
  <action>
  Create directory structure:
  ```
  Sources/JarvisCore/
  ├── JarvisCore.swift      # Public namespace marker (empty enum or version constant)
  ├── Models/               # Will hold Thought, existing models after migration
  ├── Storage/              # Will hold DatabaseManager, ThoughtStore
  └── Config/               # Will hold migrated config types
  ```

  JarvisCore.swift should contain a minimal public enum:
  ```swift
  /// Shared core library for Jarvis — Personal AI Life Assistant.
  /// Contains data models, storage (GRDB/SQLite), and configuration.
  public enum JarvisCore {
      public static let version = "0.1.0"
  }
  ```

  This ensures the target compiles and `import JarvisCore` works from other targets.
  </action>
  <verify>`swift build` succeeds for all three targets (JarvisCore, DailyBrief, DailyBriefMonitor)</verify>
  <done>JarvisCore target compiles, `import JarvisCore` works from DailyBrief and DailyBriefMonitor, directory structure ready for Phase 1 plans 02 and 03</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift package resolve` fetches GRDB without errors
- [ ] `swift build` compiles all three targets
- [ ] `import JarvisCore` is resolvable from DailyBrief and DailyBriefMonitor
- [ ] No changes to existing CLI or Monitor behavior
</verification>

<success_criteria>

- Package.swift declares JarvisCore library target with GRDB dependency
- Directory structure Sources/JarvisCore/ exists with Models/, Storage/, Config/ subdirs
- All three targets compile successfully
- Existing DailyBrief and DailyBriefMonitor functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
