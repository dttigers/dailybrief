---
status: partial
phase: 107.1-local-dev-environment-with-postgres-and-hot-reload-stack
source: [107.1-VERIFICATION.md]
started: 2026-04-21T22:45:00Z
updated: 2026-04-21T22:45:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Dual-stream boot via `npm run dev`
expected: After `cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && npm run dev`, preflight prints 4 PASS lines, then two labeled streams `[core]` (blue) and `[pwa]` (magenta) appear. No `DATABASE_URL not set` warning in `[core]`. Ctrl+C kills both processes; `lsof -iTCP:3001 -sTCP:LISTEN` and `lsof -iTCP:5173 -sTCP:LISTEN` both return empty after.
result: [pending]

### 2. Browser → local vigil-core routing
expected: With `npm run dev` running, open http://localhost:5173 in a browser. PWA loads and makes API calls to http://localhost:3001 (NOT api.vigilhub.io). DevTools Network tab confirms local target. Login with jamesonmorrill1@gmail.com (VIGIL_ALLOWED_EMAILS) succeeds.
result: [pending]

### 3. Backup cleanup follow-up (~24h after stable local dev)
expected: After ~24h of stable local dev with no need to roll back, run `rm vigil-core/.env.bak.20260421-184421` (or clean up via `rm vigil-core/.env.bak.*` if multiple accumulate). This closes the T-LOCAL-1 acceptance (prod creds on disk for rollback safety, then deleted).
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
