---
status: partial
phase: 107.1-local-dev-environment-with-postgres-and-hot-reload-stack
source: [107.1-VERIFICATION.md]
started: 2026-04-21T22:45:00Z
updated: 2026-04-21T19:40:00Z
---

## Current Test

[items 1 & 2 passed; item 3 is a ~24h follow-up]

## Tests

### 1. Dual-stream boot via `npm run dev`
expected: After `cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && npm run dev`, preflight prints 4 PASS lines, then two labeled streams `[core]` (blue) and `[pwa]` (magenta) appear. No `DATABASE_URL not set` warning in `[core]`. Ctrl+C kills both processes.
result: passed (after 4 gap-closure commits)

Gap-closure commits required to reach passing state:
- `6915908` fix(107.1-05): reorder tsx flags — watch subcommand must precede --env-file
- `bf7acdb` fix(107.1-gap): add missing Google OAuth env vars to .env.example
- `a2534de` fix(107.1-gap): add drizzle migration 0013 to repair work_orders schema drift
- `b36c3a9` fix(107.1-gap): seed-local upserts password hash, overwriting migrate-102 placeholder

### 2. Browser → local vigil-core routing + login
expected: With `npm run dev` running, open http://localhost:5173 in a browser. PWA loads and makes API calls to http://localhost:3001 (NOT api.vigilhub.io). Login with jamesonmorrill1@gmail.com (VIGIL_ALLOWED_EMAILS) + SEED_PASSWORD succeeds.
result: passed (login verified with seed credentials)

### 3. Backup cleanup follow-up (~24h after stable local dev)
expected: After ~24h of stable local dev with no need to roll back, run `rm vigil-core/.env.bak.20260421-184421 vigil-core/.env.bak.20260421-192642` (two backups exist now — pre-rewrite from Plan 06 + pre-gap-fix from dev-setup.sh re-run). This closes the T-LOCAL-1 acceptance (prod creds on disk for rollback safety, then deleted).
result: pending (intentionally deferred — surface via /gsd-audit-uat or /gsd-progress)

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
