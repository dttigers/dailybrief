---
phase: 27-therapy-prep-patterns
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - Sources/JarvisCore/Models/DailyBriefData.swift
  - Sources/DailyBrief/DailyBrief.swift
  - Sources/DailyBrief/PDF/PageThreeRenderer.swift
autonomous: true
---

<objective>
Wire therapy pattern and prep services into the daily brief generation pipeline and render a therapy prep section in the PDF.

Purpose: Surface therapy patterns and session prep in the printed daily brief so the user has a tangible therapy prep document each day.
Output: DailyBriefData extended with therapy fields, generate command fetches therapy data, PageThreeRenderer renders therapy prep section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-therapy-prep-patterns/27-01-SUMMARY.md

@Sources/JarvisCore/Models/DailyBriefData.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBrief/PDF/PageThreeRenderer.swift
@Sources/DailyBrief/PDF/PDFGenerator.swift
@Sources/JarvisCore/Models/TherapyPattern.swift
@Sources/JarvisCore/Models/TherapyPrep.swift
@Sources/JarvisCore/Services/TherapyPatternService.swift
@Sources/JarvisCore/Services/TherapyPrepService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DailyBriefData and wire generate command</name>
  <files>Sources/JarvisCore/Models/DailyBriefData.swift, Sources/DailyBrief/DailyBrief.swift</files>
  <action>
  **DailyBriefData.swift:**
  - Add two new fields:
    - `therapyPatterns: [TherapyPattern]` (default `[]`)
    - `therapyPrep: TherapyPrep?` (default `nil`)
  - Add corresponding init parameters with defaults

  **DailyBrief.swift (generate command):**
  - After existing thought fetching, add therapy data fetching:
    1. Create TherapyPatternService and TherapyPrepService actors (same apiKey/model as existing AI services)
    2. Use ThoughtStore.fetchRecentTherapyThoughts(days: 30) to get recent therapy thoughts
    3. Use ThoughtStore.fetchRecentTherapyThoughts(days: 30, classification: .bringToTherapist) to get therapist-worthy thoughts
    4. Call patternService.detectPatterns(thoughts: allTherapyThoughts, lookbackDays: 30) wrapped in tryFetch
    5. Call prepService.generatePrep(thoughts: bringToTherapistThoughts, patterns: therapyPatterns) wrapped in tryFetch
    6. Pass results to DailyBriefData init
  - Use `async let` or sequential calls (sequential is fine since these depend on thought fetch results)
  - Wrap both calls in existing `tryFetch` error-swallowing pattern so failures don't block the brief
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>DailyBriefData has therapyPatterns and therapyPrep fields. Generate command fetches therapy data and passes it through.</done>
</task>

<task type="auto">
  <name>Task 2: Render therapy prep section in PageThreeRenderer</name>
  <files>Sources/DailyBrief/PDF/PageThreeRenderer.swift</files>
  <action>
  Add a "Therapy Prep" section to PageThreeRenderer, rendered AFTER the AI Insights section (at bottom of page, or on a new page if needed).

  **Section layout:**
  - Only render if `data.therapyPrep != nil` and has at least 1 item
  - Section header: "Therapy Prep" in headerFont
  - If therapyPatterns is non-empty, render a compact "Patterns" sub-header:
    - Up to 3 patterns, each as: "• {theme} ({trend})" in bodyFont, darkGray
  - Render prep items:
    - Up to 5 items, each as:
      - Urgency indicator: filled circle (high=black, medium=darkGray, low=medGray)
      - Topic in bold bodyFont
      - Context in regular bodyFont, darkGray, indented, truncated to ~60 chars
  - If suggestedFocus exists, render at bottom:
    - "Focus: {suggestedFocus}" in italic bodyFont, darkGray
  - Respect page bounds — break if not enough space (same pattern as AI Insights section)

  **Important:** Check remaining page space before rendering. If the Therapy Prep section won't fit on page 3, that's OK — just render what fits. Do NOT add a 4th page (keep it simple for now). Use the same `pageBottom` guard pattern from the AI Insights section.

  For italic font, use: `CTFontCreateWithName("Helvetica-Oblique" as CFString, S.bodySize, nil)`
  </action>
  <verify>swift build 2>&1 | tail -5 — builds without errors</verify>
  <done>PageThreeRenderer renders therapy prep section with patterns and prep items. Section respects page bounds. Only renders when therapy prep data exists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] DailyBriefData has therapyPatterns and therapyPrep fields
- [ ] Generate command fetches therapy patterns and prep (with error swallowing)
- [ ] PageThreeRenderer renders therapy prep section when data present
- [ ] No regression on existing PDF sections
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Daily brief PDF includes therapy prep section when therapy thoughts exist
- Failures in therapy services don't block brief generation
</success_criteria>

<output>
After completion, create `.planning/phases/27-therapy-prep-patterns/27-02-SUMMARY.md`
</output>
