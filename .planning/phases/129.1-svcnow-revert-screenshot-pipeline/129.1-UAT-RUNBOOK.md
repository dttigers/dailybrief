# Phase 129.1 UAT Runbook — SVCNOW Revert + Screenshot Pipeline + PWA Manual-Create

**Phase:** 129.1-svcnow-revert-screenshot-pipeline
**Operator:** Jameson Morrill
**Authored:** 2026-05-17
**Date executed:** _______________________ (operator fills as scenarios are run)
**Vigil API base (prod):** https://api.vigilhub.io
**Worktree path used for development:** `/home/morrillboss/dev/dailybrief/`

This runbook covers the 6 end-to-end operator-runnable tests for Phase 129.1. Scenarios 1 (extension revert) and 5a (daemon install + status + happy-path) were executed inline during waves 1 and 3 (see "Deviations from original plan" below). Remaining sections are pending operator execution.

Each section ends with a disposition (PASS / FAIL / BLOCKED) and a notes block. Roll-up dispositions table at the bottom.

---

## Deviations from original plan

### Inline hot-fix (commit `5926037`) — Polaris field-mapping remap (post-UAT)

During Wave 3 operator UAT of the screenshot pipeline, the first real Polaris screenshot was ingested successfully (`Screenshot 2026-05-16 at 2.46.14 PM.png → CS0363817`) and the PWA "Review draft" modal rendered correctly. The operator surfaced that the Plan 03 EXTRACTION_PROMPT was mapping the wrong Polaris fields onto the work_orders columns. The hot-fix was applied inline (commit `5926037`) deployed via Railway autodeploy. Final mapping:

| Polaris field | work_orders column | Example |
|---------------|--------------------|---------|
| "Location" | `store` | "LINS (CEDAR)" |
| "Maintenance Location" | `location` | "Bakery" |
| (none — semantic moved onto `location`) | `department` | **deprecated** — queued for removal in Phase 129.2 |

**Implication for this runbook:** Scenario 1 step 8 ("modal pre-populated with location/department/maintenance_problem") was originally executed against the pre-remap prompt and rendered the wrong field labels. A re-test (Scenario 1, step 8 onward) against a fresh Polaris screenshot **after** the remap deployment is `[pending]` operator action.

### Wave 1 + Wave 3 inline UAT — pre-recorded outcomes

The orchestrator surfaced that the following sections were executed inline against operator hardware before this runbook was authored:

- **Scenario 4 (Extension revert verification — Chrome + Safari):** **PASS** — Wave 1 (2026-05-17). Operator verified popup loads on https://example.com (non-SN URL), capture works end-to-end, thoughts land in the PWA dashboard. Both browsers exhibit a generic capture popup with no SVCNOW UI surface.
- **Scenario 5a (Daemon install + status + smoke):** **PASS** — Wave 3 (2026-05-17). Operator ran `node dist/cli.js install` (LaunchAgent installed cleanly); `node dist/cli.js status` reported the daemon as `running`. Operator dropped 2 random non-Polaris PNGs into `~/vigil-captures/`; daemon POSTed; both received `HTTP 422 {"error":"Vision extraction returned no case_number"}` (expected behavior); files moved to `failed/` with `.err` sidecars containing the verbatim error string. Operator then dropped a real Polaris screenshot (`Screenshot 2026-05-16 at 2.46.14 PM.png`); daemon POSTed, vigil-core extracted `CS0363817`, file moved to `processed/<iso-ts>-CS0363817.png`, log line `[OK] Screenshot 2026-05-16 at 2.46.14 PM.png → CS0363817`, and the PWA "Review draft" modal rendered (which surfaced the field-mapping issue that prompted commit `5926037`).

These two are marked `[PASS — Wave N pre-recorded]` in their disposition slots below. Operator may re-run any of them at any time; if a re-run produces a different disposition, edit the slot accordingly and add a note.

---

## 1. Screenshot pipeline end-to-end (post-remap retest)

**What this verifies:** SCAP-01 + SCAP-02 + SCAP-03 + SCAP-04 + SCAP-05 — operator screenshots a Polaris case → daemon uploads → vigil-core extracts (with the post-remap field mapping from commit `5926037`) → PWA shows pending_review draft with the correct labels → operator commits.

**Status as of authoring:** `[pending]` post-remap retest. The pre-remap happy-path was witnessed during Wave 3 (see "Deviations" above).

**Setup:**
- Confirm Vigil API key is in Keychain: `security find-generic-password -s vigil-api-key -w` (operator-only command; do not paste the output anywhere).
- Confirm daemon is loaded: `node ~/dev/dailybrief/scripts/vigil-capture/dist/cli.js status`. Expected: `state = running` and a recent `LastExitStatus` of `0` (or absent — daemon has not crashed).
- Confirm extension reverted (Scenario 4 PASSED — already recorded under Deviations).
- Confirm migration 0022 applied to prod. Operator method: visit Railway logs for the most recent vigil-core deploy and look for the Drizzle `applied 0022_add_work_orders_maintenance_problem_department.sql` line, OR connect with `railway run --service Postgres psql -c "\\d work_orders"` and confirm `maintenance_problem` + `department` text columns are present.
- Confirm Railway autodeploy of commit `5926037` is live. Operator method: visit Railway → Deployments tab → most recent successful deploy shows commit hash starting with `5926037`. (If not deployed yet, this scenario is BLOCKED until the deploy completes.)

**Steps:**
1. Open a real Polaris case in the browser (use one that has NOT been previously processed; the daemon will dedup if the clientCaptureId reappears, but the simpler invariant is just to grab a fresh case).
2. Cmd+Shift+4 → screenshot the case page → save to `~/vigil-captures/`. (Setting Cmd+Shift+4 default save-to-folder = `~/vigil-captures/` is the README-documented daily-use pattern.)
3. Wait ~5–10 seconds for chokidar `awaitWriteFinish` + Anthropic vision round-trip.
4. Run: `tail -5 ~/Library/Logs/vigil-capture.log`. Expected: `[OK] <filename> → CS<7-digits>`.
5. Verify file moved: `ls -lt ~/vigil-captures/processed/ | head -3`. Expected: most recent file has shape `<iso-ts>-CS<7-digits>.png`.
6. Open PWA `/work-orders` page.
7. Confirm a new row with the extracted case_number appears with an amber **Review draft** button (replaces the status-cycle button for `pending_review` rows).
8. Click **Review draft**. Confirm the modal opens with the following fields pre-populated **per the post-remap mapping**:
   - `case_number` (read-only)
   - `short_description`
   - `store` ← from Polaris "Location" field (e.g. "LINS (CEDAR)")
   - `location` ← from Polaris "Maintenance Location" field (e.g. "Bakery")
   - `maintenance_problem` ← from Polaris "Maintenance Problem" field
   - `department` may be empty (post-remap deprecated; field is in DB but no longer populated by the prompt) — that's expected, not a bug. Phase 129.2 will collapse this field's modal slot.
   - `extras` collapsible `<details>` block shows the 13 optional fields as formatted JSON.
9. Make a minor edit to `short_description` (e.g. append " [verified]").
10. Click **Commit**. Confirm the row's state changes to `open` (status pill turns from amber `pending_review` to the standard `open` pill); the edit persisted (re-open Review modal NOT available — commit transitions out of pending_review — but the standard row should show the updated short_description).

**Expected:** Each step completes without error; final work order is in `open` state with operator's edit applied. The `store` value reflects the Polaris "Location" field, the `location` value reflects the Polaris "Maintenance Location" field, and `department` is empty (expected post-remap).

**Disposition:** `[pending]` — PASS / FAIL / BLOCKED

**Notes (on FAIL or BLOCKED):**
_________________________________________________

---

## 2. Manual-create flow (non-Polaris work order)

**What this verifies:** WO-MANUAL-01 + WO-MANUAL-02 + WO-MANUAL-03.

**Status as of authoring:** `[pending]` — has NOT been operator-tested end-to-end.

**Setup:** PWA logged in as any user (operator or non-operator). Confirm migration 0022 is applied so `maintenance_problem` + `department` columns exist server-side.

**Steps:**
1. Navigate to `/work-orders`.
2. Click **+ Create work order** button (right side of the filter row, teal styling).
3. Modal opens. Confirm all 11 editable fields are visible: case_number, store, short_description, trade, location, equipment, priority, contact, notes, **maintenance_problem**, **department** (labeled "Maintenance Location" in the form per the Phase 129.1-03 operator decision — this label will collapse in Phase 129.2 since the field semantic moved onto `location`).
4. Type a lowercase case_number (`cs0001`). Confirm inline error "Case number must contain only A-Z and 0-9" appears immediately, and the Submit button is disabled.
5. Change to `MANUAL-001`. Confirm the inline error clears and Submit is enabled. (Hyphen is allowed because the regex matches `A-Z0-9` — wait — the regex is `/^[A-Z0-9]+$/` which does NOT allow hyphens. Use `MANUAL001` instead.)
6. Fill `short_description` ("Test manual create"), `priority` ("Medium"), `department` / "Maintenance Location" ("Test-Loc"), `maintenance_problem` ("Other").
7. Click Submit.
8. Modal closes; a new row with case_number `MANUAL001` appears in the list with state `open` (NOT `pending_review` — manual-create uses `state: "open"` per Plan 05).
9. Refresh the page (Cmd+R). Confirm the row persists.

**Expected:** Form validation works; submission persists; row visible without manual refresh (step 8); persists across refresh (step 9).

**Disposition:** `[pending]` — PASS / FAIL / BLOCKED

**Notes:** _________________________________________________

---

## 3. Draft discard flow

**What this verifies:** SCAP-05 + WO-MANUAL-02 `discardDraft`.

**Status as of authoring:** `[pending]` — has NOT been operator-tested end-to-end.

**Setup:** At least one `pending_review` row in the list (run Scenario 1 first to create one, or drop another Polaris screenshot into `~/vigil-captures/`).

**Steps:**
1. Click **Review draft** on a pending_review row.
2. Click **Discard**. Confirm a browser confirm dialog appears: "Discard this draft? This cannot be undone."
3. Click **OK**.
4. Modal closes; row disappears from the list immediately (no manual refresh).
5. Try to re-fetch via curl to confirm hard-delete:
   ```bash
   curl -H "Authorization: Bearer <your-vk-key>" \
     https://api.vigilhub.io/v1/work-orders/<caseNumber>
   ```
   Expected: 404 (row was hard-deleted, not soft-archived).
6. Confirm the row does NOT appear under any filter tab — not "Active", not "Archived", not "All".

**Expected:** Row is hard-deleted; reappears NOWHERE.

**Disposition:** `[pending]` — PASS / FAIL / BLOCKED

**Notes:** _________________________________________________

---

## 4. Extension revert verification

**What this verifies:** SVCNOW-01/02/03/05 reverts (browser extension generic-capture baseline).

**Setup:** Chrome + Safari extensions installed (from Phase 129.1 Plan 02 Task 3).

**Steps (pre-recorded from Wave 1; included here for re-runnability):**
1. Navigate to `https://example.com` (NOT a service-now.com URL).
2. Click the Vigil Chrome extension icon.
3. Confirm: popup opens with a generic textarea + Send button (NOT a SVCNOW case# form with locked CS# header).
4. Type "test thought from reverted extension" → click Send.
5. Open PWA dashboard. Confirm a new thought row with that content appears.
6. Repeat steps 1–5 in Safari (use the Vigil toolbar icon).

**Expected:** Both browsers show a generic capture popup; thoughts are captured via the Phase 84-style endpoint; no SVCNOW UI anywhere; no auto-detection of service-now.com URL patterns; both popups byte-equal to each other (Phase 129.1-02 Task 1 lockstep parity).

**Disposition:** `[PASS — Wave 1 pre-recorded 2026-05-17]`

**Notes:** Chrome + Safari extension smoke tests both PASSED inline during Wave 1. Operator verified popup loads on https://example.com, capture works end-to-end, thoughts land in the PWA. No SVCNOW UI visible in either browser.

---

## 5. Daemon control (status / pause / resume)

**What this verifies:** SCAP-04 CLI subcommands + LaunchAgent KeepAlive + orphan-scan recovery.

### 5a. Install + status + happy-path smoke

**Status as of authoring:** `[PASS — Wave 3 pre-recorded 2026-05-17]` for install + status + 2-failed + 1-success path. The remaining pause/resume sub-scenario is `[pending]` (Section 5b below).

**Steps (pre-recorded):**
1. Operator ran `node ~/dev/dailybrief/scripts/vigil-capture/dist/cli.js install`. LaunchAgent installed cleanly via `launchctl bootout gui/$UID` (swallowed not-loaded) then `launchctl bootstrap gui/$UID`.
2. Operator ran `node dist/cli.js status`. Output showed `state = running`.
3. Operator dropped 2 random non-Polaris PNGs into `~/vigil-captures/`. Both POSTed → `HTTP 422 {"error":"Vision extraction returned no case_number"}` (expected — vision could not find a CS# in non-Polaris images). Both files moved to `~/vigil-captures/failed/` with `.err` sidecars containing the verbatim `HTTP 422 ...` text.
4. Operator dropped 1 real Polaris screenshot (`Screenshot 2026-05-16 at 2.46.14 PM.png`). Daemon POSTed → `HTTP 200`, vigil-core extracted `CS0363817`, file moved to `~/vigil-captures/processed/<iso-ts>-CS0363817.png`. Log line emitted: `[OK] Screenshot 2026-05-16 at 2.46.14 PM.png → CS0363817`.
5. Operator opened PWA `/work-orders`. New `pending_review` row visible; "Review draft" modal rendered. (This is the step that surfaced the field-mapping issue → hot-fix `5926037`.)

**Disposition:** `[PASS — Wave 3 pre-recorded 2026-05-17]`

**Notes:** Daemon installed cleanly; status reported running; 2 failures + 1 success exercised both error and happy paths. Hot-fix `5926037` followed; Section 1 above tracks the post-remap retest.

### 5b. Pause / resume + orphan-scan recovery

**Status as of authoring:** `[pending]` — Plan 04 Task 5 step 6 has NOT been operator-tested.

**Steps:**
1. Run `node ~/dev/dailybrief/scripts/vigil-capture/dist/cli.js status`. Expected: `state = running`.
2. Pause the daemon: `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
3. Confirm `node dist/cli.js status` now exits non-zero or reports not-loaded.
4. Drop a test image into `~/vigil-captures/test.png` (any small PNG — even a screenshot of a blank page).
5. Wait 10 seconds. Confirm the file stays in the root of `~/vigil-captures/` (daemon is paused → no processing).
6. Resume the daemon: `launchctl load ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
7. Wait 5–10 seconds. Confirm `test.png` is processed (moved to `processed/` or `failed/` depending on whether vision finds a case_number — for a blank-page PNG it will be `failed/` with 422). The orphan-scan ran at daemon startup and picked it up.
8. Tail the log: `node dist/cli.js tail` (Ctrl+C to exit). Confirm log shows the orphan-scan + process event.

**Expected:** Pause stops processing (step 5); resume picks up the orphaned file via the startup orphan-scan (step 7); logs are readable (step 8); no manual intervention needed.

**Disposition:** `[pending]` — PASS / FAIL / BLOCKED

**Notes:** _________________________________________________

---

## 6. Edge cases

**What this verifies:** SCAP-03 sequential processing + SCAP-04 key rotation procedure.

**Status as of authoring:** `[pending]` — neither sub-scenario has been operator-tested.

### 6a. Multi-file drop

1. Drop 3 PNG files into `~/vigil-captures/` within ~1 second:
   ```bash
   cp ~/vigil-captures/processed/<known-good>.png ~/vigil-captures/a.png && \
   cp ~/vigil-captures/processed/<known-good>.png ~/vigil-captures/b.png && \
   cp ~/vigil-captures/processed/<known-good>.png ~/vigil-captures/c.png
   ```
   (Use a known-good PNG from `processed/` as the source so all 3 will extract successfully — the multi-file test is about ORDERING + sequential POST, not extraction accuracy.)
2. Tail the log: `node ~/dev/dailybrief/scripts/vigil-capture/dist/cli.js tail`. Confirm: 3 sequential `[OK]` lines (not interleaved chaos; sequential `await processFile` ensures one-at-a-time ordering per Plan 04 D-05).
3. Confirm all 3 files are in `processed/` (none remaining in root, none in `failed/`).

**Note:** Because all 3 files are copies of the same source, they'll have identical content → identical `imageBase64` → identical hash, BUT the daemon issues a fresh `crypto.randomUUID()` per file, so the SVCNOW-04 dedup (which keys on `clientCaptureId`) does NOT short-circuit. All 3 should round-trip through vision and produce 3 separate work_orders rows (or one row updated 3 times via `onConflictDoUpdate` if the case_number is identical — that's the accepted-risk RESEARCH §1 behavior).

**Disposition (6a):** `[pending]` — PASS / FAIL / BLOCKED. **Notes:** ____________

### 6b. API key rotation

1. Pause the daemon: `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`. (Note: pause not strictly required because the daemon reads the Keychain per-call, but pausing reduces noise during the swap.)
2. Rotate the Keychain key to a deliberately bad value to verify rotation actually takes effect:
   ```bash
   security add-generic-password -s vigil-api-key -a "$USER" -w 'wrong-key-test' -U
   ```
3. Resume: `launchctl load ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
4. Drop a test image. Tail logs: confirm `[FAIL] ... → 401` (or 403 — both are Authorization-failed signals from vigil-core's bearerAuth dispatcher).
5. Rotate back to the real key:
   ```bash
   security add-generic-password -s vigil-api-key -a "$USER" -w '<real-key>' -U
   ```
6. Drop another test image. Tail logs: confirm `[OK]` (no daemon restart needed — `getApiKey()` reads from Keychain per-call per Plan 04 D-03).

**Expected:** Bad key surfaces as 401/403 with file → `failed/` + `.err` sidecar; real key restores happy path on the NEXT event (no restart).

**Disposition (6b):** `[pending]` — PASS / FAIL / BLOCKED. **Notes:** ____________

---

## Roll-up Dispositions

| Scenario | Disposition | Notes |
|----------|-------------|-------|
| 1. Screenshot e2e (post-remap retest) | `[pending]` | Pre-remap happy-path observed Wave 3; post-remap retest is the open item |
| 2. Manual-create | `[pending]` | Not yet operator-tested |
| 3. Draft discard | `[pending]` | Not yet operator-tested |
| 4. Extension revert | `[PASS — Wave 1]` | Chrome + Safari both PASSED inline 2026-05-17 |
| 5a. Daemon install + happy path | `[PASS — Wave 3]` | LaunchAgent installed, status running, 2-fail + 1-success exercised inline 2026-05-17 |
| 5b. Daemon pause/resume + orphan-scan | `[pending]` | Plan 04 Task 5 step 6 not yet exercised |
| 6a. Multi-file drop | `[pending]` | |
| 6b. API key rotation | `[pending]` | |

**Overall phase outcome:** `[PARTIAL — awaiting operator]` (pending sections complete asynchronously; revert this line to COMPLETE / PARTIAL / BLOCKED when all sections have dispositions).

---

## Phase 129.2 follow-up queue (surfaced by this UAT)

The post-UAT field-mapping remap (commit `5926037`) leaves `work_orders.department` as an unused column. Phase 129.2 should:

1. **Migration 0023** — drop the `work_orders.department` column. (Depends on first confirming no live PWA build still reads it; verifier should run a grep across `vigil-core/`, `vigil-pwa/`, and `scripts/vigil-capture/` to confirm safe removal.)
2. **CreateWorkOrderModal + ReviewWorkOrderModal cleanup** — remove the "Maintenance Location" form input that currently maps to `department` (the semantic now lives on `location`); the modal already has a separate "Location" input. Collapse the two inputs into one labeled "Location" (or rename per operator preference).
3. **REQUIREMENTS.md cleanup** — once 0023 lands, update the SCAP-02 / WO-MANUAL-01 / WO-MANUAL-03 descriptions to drop the `department` mentions and add an explanatory note pointing to Phase 129.2 commits.

Phase 129.2 plan-phase should triage these via `/gsd:plan-phase 129.2`.

---

*Operator records gap entries in `.planning/STATE.md` for any FAIL or BLOCKED dispositions; planner spawns Phase 129.2 with `/gsd:plan-phase 129.1 --gaps` (or the same `--gaps` flag against 129.2 if it absorbs the post-remap cleanup queue above) if needed.*
