---
phase: 129.1-svcnow-revert-screenshot-pipeline
verified: 2026-05-17T21:33:09Z
status: human_needed
score: 4/4 ROADMAP success criteria automated-verified; SC4 requires human re-test post-remap
re_verification: null
human_verification:
  - test: "End-to-end Polaris screenshot POST-REMAP retest"
    expected: "Operator drops a fresh Polaris CS-page screenshot into ~/vigil-captures/ AFTER Railway autodeploy of commit 5926037 lands. Daemon POSTs → vigil-core extracts → PWA Review modal shows correctly-labeled fields: store='LINS (CEDAR)' (Polaris Location), location='Bakery' (Polaris Maintenance Location), short_description, case_number=CS<7-digits>. Commit transitions state pending_review → open."
    why_human: "Requires (a) live Polaris page access, (b) operator's Mac with daemon installed, (c) confirmation that Railway has redeployed commit 5926037. UAT runbook Scenario 1 disposition is [pending] until this re-runs. Wave 3's pre-remap pass observed wrong labels — that's what triggered the hot-fix."
  - test: "Manual-create flow end-to-end (Plan 05 / WO-MANUAL-01/02/03)"
    expected: "Operator navigates to PWA /work-orders, clicks '+ Create work order', form opens with all 11 fields, lowercase case_number is rejected inline, MANUAL-001 is accepted, fields filled and submitted, new row with state='open' appears in the list without manual refresh."
    why_human: "Form interaction + visual verification + observation of fetchTick refresh requires a browser session. UAT runbook Scenario 2 disposition is [pending]."
  - test: "Draft discard flow with browser confirm prompt"
    expected: "Operator opens a pending_review row, clicks Review draft, clicks Discard, sees `Discard this draft? This cannot be undone.` prompt, confirms, modal closes, row disappears from the list. Subsequent curl GET on the caseNumber returns 404 (hard-delete confirmed)."
    why_human: "Browser window.confirm() interaction + post-delete curl require human action. UAT runbook Scenario 3 disposition is [pending]."
  - test: "Daemon pause/resume + orphan-scan e2e (Plan 04 Task 5 step 6 / SCAP-03/04)"
    expected: "Operator runs `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`, drops a PNG, confirms it stays in root (daemon not running), then `launchctl load ...`, and within 5-10s the orphan-scan picks up the leftover file and processes it (moves to processed/ or failed/)."
    why_human: "Requires Mac hardware + launchctl unload/load + observing real fs.watch behavior over wall-clock time. UAT runbook Scenario 5b disposition is [pending]."
  - test: "Multi-file drop sequential processing (Plan 04 / SCAP-03 backpressure)"
    expected: "Operator drops 3 PNG files into ~/vigil-captures/ within 1 second; tail log shows 3 sequential [OK] or [FAIL] lines (not interleaved); all 3 files end up in processed/ or failed/, none remaining in root."
    why_human: "Tests real chokidar event ordering + sequential processFile invocations under timing pressure on operator's Mac. UAT runbook Scenario 6a disposition is [pending]."
  - test: "API key rotation without daemon restart (Plan 04 / SCAP-04)"
    expected: "Operator rotates Keychain entry to a bad key, drops a test PNG, sees [FAIL] → 401 in log; rotates back to real key, drops another PNG, sees [OK] (no daemon restart needed because getApiKey is called per-event)."
    why_human: "Requires real Keychain mutation via `security add-generic-password` on operator's Mac. UAT runbook Scenario 6b disposition is [pending]."
  - test: "Safari Xcode 'Copy Bundle Resources' build phase update (Plan 02 Task 3)"
    expected: "Operator opens vigil-safari-extension.xcodeproj in Xcode, removes background.js + content-script.js + popup-helpers.js from Vigil Capture Extension target's Build Phases → Copy Bundle Resources, builds (⌘B), confirms produced .appex Resources/ does not contain those files."
    why_human: "Xcode GUI operation cannot be automated. Plan 02 Task 3 is a blocking checkpoint that is paused; the file-level delete is verified in the source tree but the Xcode project file's build-phase reference is operator-only. Source files are absent (verified), but the .xcodeproj reference status is unknown."
---

# Phase 129.1: SVCNOW Revert + Screenshot Pipeline + PWA Manual-Create Verification Report

**Phase Goal:** Replace the SVCNOW assisted-capture popup workflow with (a) an operator-specific screenshot pipeline (vigil-core endpoint receives Polaris screenshot, calls Claude API for structured field extraction, writes to work_orders) and (b) a PWA manual-create UI in the work-orders list for all non-operator users. Revert the Chrome + Safari extension changes to the Phase 84 generic thought-capture baseline.
**Verified:** 2026-05-17T21:33:09Z
**Status:** human_needed
**Re-verification:** No — initial verification.

---

## Goal Achievement

### ROADMAP Success Criteria (the authoritative contract)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Operator screenshot → drops in ~/vigil-captures/ → launchd watcher uploads → vigil-core extracts ≥12 fields via Claude Sonnet vision → writes to work_orders deduped by client_capture_id | VERIFIED | `scripts/vigil-capture/src/watcher.ts` chokidar.watch + POST to `/v1/captures/screenshot` + `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` KeepAlive + `vigil-core/src/routes/captures-screenshot.ts` ExtraExtracted (13 optional fields) + RequiredExtracted (5 fields) = 18 fields total; SVCNOW-04 dedup via SELECT-by-(userId, clientCaptureId) short-circuit confirmed at captures-screenshot.ts lines ~250-280; tests T7 (dedup) + T1 (happy path INSERT pending_review) green |
| SC2 | Non-operator users see "Create work order" button in PWA work-orders list; form submits to /v1/work-orders/sync; row appears | VERIFIED (artifacts wired) | `vigil-pwa/src/pages/WorkOrdersPage.tsx` line 101 "+ Create work order" CTA + line 151 `<CreateWorkOrderModal>`; `vigil-pwa/src/components/CreateWorkOrderModal.tsx` 11 editable fields with `/^[A-Z0-9]+$/` validation; `useWorkOrders.createWorkOrder` line 158 POSTs to `/v1/work-orders/sync` with state="open" + crypto.randomUUID() clientCaptureId; sanitizer extended to "12 known fields" (work-orders.ts line 83). 16 modal tests pass. **Visual user-flow confirmation requires human.** |
| SC3 | Chrome + Safari extensions revert to Phase 84 generic thought-capture; no SVCNOW UI in either extension | VERIFIED | `vigil-extension/manifest.json` v1.0.0, host_permissions=`["https://api.vigilhub.io/*"]` (no `service-now.com`), no `background`/`content_scripts` keys; popup.html has `id="setup-view"` + `id="capture-view"` (no `svcnow-view`); 3 SVCNOW-only files (background.js, content-script.js, popup-helpers.js) absent from BOTH `vigil-extension/` and `vigil-safari-extension/Vigil Capture Extension/Resources/`; popup.{html,css,js} byte-equal Chrome ↔ Safari (diff -q produces no output); parity.test.ts 6/6 passing |
| SC4 | End-to-end test: operator screenshots CS0363817 → ~5 sec later work order appears in PWA with correctly extracted fields (Number, Store Account, Location, Assigned to, Equipment, Priority, Short Description) | HUMAN_NEEDED | Wave 3 (2026-05-17) PASSED for the daemon happy path with real `Screenshot 2026-05-16 at 2.46.14 PM.png → CS0363817` (UAT runbook Section 5a `[PASS — Wave 3 pre-recorded]`). HOWEVER, that observation surfaced a field-mapping issue → hot-fix `5926037` remapped Polaris Location→store + Polaris Maintenance Location→location. **The post-remap re-test against a fresh Polaris screenshot is `[pending]` in UAT runbook Section 1.** Vision endpoint code DOES reflect the remap (captures-screenshot.ts EXTRACTION_PROMPT lines 90-91 explicitly distinguish the two Polaris fields), but live e2e confirmation requires operator action. |

**Score:** 4/4 ROADMAP success criteria. SC1, SC2, SC3 are VERIFIED by codebase + automated tests. SC4 requires a human re-test against a real Polaris page **after** Railway autodeploys commit `5926037` (which the user-prompt note flags as `pending Railway autodeploy + a fresh screenshot`).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql` | Drizzle migration: 2 ADD COLUMN IF NOT EXISTS for maintenance_problem + department, separated by `--> statement-breakpoint` | VERIFIED | Both `ADD COLUMN IF NOT EXISTS "maintenance_problem" text` + `ADD COLUMN IF NOT EXISTS "department" text` present; `--> statement-breakpoint` separator present; no CREATE INDEX |
| `vigil-core/drizzle/meta/0022_snapshot.json` | Drizzle snapshot for migration 0022 | VERIFIED | File exists (48KB) — schema vs migrations in sync (drizzle-kit generate reports zero diff) |
| `vigil-core/src/db/schema.ts` | workOrders block declares both new columns as nullable text | VERIFIED | Lines 329-330: `maintenanceProblem: text("maintenance_problem")` + `department: text("department")` — neither has `.notNull()` or `.default(...)` |
| `vigil-core/src/routes/work-orders.ts` | SanitizedWorkOrder + sanitizer + dbInsertOrGet + dbUpsertLegacy carry 2 new fields; "12 known fields" comment; commit + delete routes registered in factory | VERIFIED | Lines 34-35 interface fields, line 83 "12 known fields" comment, lines 109-110 sanitizer destructure (coerce-or-null), lines 493-494 dbUpsertLegacy SET, lines 270 POST `/work-orders/:caseNumber/commit`, line 390 DELETE `/work-orders/:caseNumber`, line 297 `pending_review` 409-guard, line 430 `c.body(null, 204)` |
| `vigil-core/src/routes/work-orders.test.ts` | 22 tests covering 12-field round-trip + commit + delete + drift detectors | VERIFIED | 22/22 tests pass (WO-MANUAL-03/T1-3 sanitizer round-trip; WO-MANUAL-05/T1-7 commit + delete; DRIFT/T1-2 + STRUCT/T1-2) |
| `vigil-core/src/routes/captures-screenshot.ts` | DI factory + singleton; full validation; SVCNOW-04 dedup; Anthropic vision; pending_review INSERT with onConflictDoUpdate | VERIFIED | Line 156 `createCapturesScreenshotRoute(deps)`; line 404 `export const capturesScreenshot`; UUID_V4_REGEX, VALID_MEDIA_TYPES, MAX_IMAGE_BASE64_LENGTH constants present; `c.get("userId")` (not body); `callClaudeMultimodal` invoked with `userId`; `parseAIJson`; `onConflictDoUpdate` on caseNumber PK; state="pending_review" hardcoded; placeholder `DEPARTMENT-FIELD-FROM-OPERATOR-DECISION` absent (replaced); post-UAT remap reflected (Polaris Location → store, Maintenance Location → location) at lines 52-53 + prompt lines 90-91 |
| `vigil-core/src/routes/captures-screenshot.test.ts` | 10 tests covering happy + 9 sad paths via DI mocks | VERIFIED | 10/10 tests pass; mock-db captures INSERT VALUES + onConflictDoUpdate SET; mock-Anthropic captures `userId + maxTokens`; covers 400/413/422/502/503 + dedup + collision |
| `vigil-core/src/index.ts` | Mount `app.route('/v1', capturesScreenshot)` after bearerAuth dispatcher | VERIFIED | Line 22 import + line 229 mount; mount comment explicitly notes "bearerAuth required; mount AFTER dispatcher" |
| `scripts/vigil-capture/src/watcher.ts` | chokidar daemon with awaitWriteFinish + ignoreInitial + orphan-scan + DI + Keychain | VERIFIED | All required surface present: awaitWriteFinish, ignoreInitial, depth:0, scanOrphans, /v1/captures/screenshot URL, Bearer auth, `security find-generic-password -s vigil-api-key -w` via execSync, processFile + shouldSkip + getApiKey exports for DI testing; no WatchPaths anti-pattern |
| `scripts/vigil-capture/src/watcher.test.ts` | 7 tests covering shouldSkip + processFile happy/sad paths | VERIFIED | 7/7 tests pass after `npm install` (dependency install was required — chokidar dep present in package.json); covers .png/.jpg/.tmp/.icloud/dotfile filtering + HTTP 422 failure + network error + UUID v4 clientCaptureId regex + .jpg mediaType |
| `scripts/vigil-capture/src/cli.ts` | install/uninstall/status/tail subcommands; idempotent bootout-then-bootstrap; chmod 700 | VERIFIED | Shebang `#!/usr/bin/env node` present; all 4 subcommand strings; `launchctl bootstrap gui/$UID`; `launchctl bootout gui/$UID`; `0o700` mode on captures dir |
| `scripts/vigil-capture/README.md` | Operator runbook covering install/pause/resume/tail/key-rotation/edge-cases | VERIFIED | 182 lines (≥50 required); all required sections present |
| `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` | LaunchAgent plist mirroring vigilcore template; no API key | VERIFIED | Label=com.jamesonmorrill.vigilcapture; ProgramArguments uses /usr/local/bin/node + dist/watcher.js; KeepAlive.SuccessfulExit=false; LimitLoadToSessionType=Aqua; ProcessType=Background; RunAtLoad=true; EnvironmentVariables has VIGIL_API_BASE + VIGIL_CAPTURE_DIR; NO WatchPaths; NO API key (grep for sk-ant/api_key returns 0) |
| `vigil-pwa/src/components/CreateWorkOrderModal.tsx` | Modal with 11 editable fields + case_number regex + bg-black/50 overlay + Escape close | VERIFIED | 11 useState slots (caseNumber, store, shortDescription, trade, location, equipment, priority, contact, notes, maintenanceProblem, department); CASE_NUMBER_REGEX = /^[A-Z0-9]+$/ literal present (5 occurrences); bg-black/50 overlay; Escape key handler; onCreate prop wired |
| `vigil-pwa/src/components/ReviewWorkOrderModal.tsx` | Pre-populated edit form; extras JSON in `<details>`; Commit + Discard with confirm | VERIFIED | useState initializers from workOrder prop; `<details>` for extras JSON parsing workOrder.notes; `window.confirm('Discard this draft? This cannot be undone.')` (line 125); onCommit + onDiscard prop usage; maintenanceProblem + department fields read via index-signature cast (forward-compat seam) |
| `vigil-pwa/src/components/WorkOrderRow.tsx` | pending_review STATUS_STYLES + onReview prop + Review draft button branch | VERIFIED | Line 27 `pending_review: 'bg-amber-500/20 text-amber-400 hover:bg-amber-500/30'`; line 12 onReview prop; line 33 isPendingReview branch; line 72 Review draft button invocation |
| `vigil-pwa/src/hooks/useWorkOrders.ts` | createWorkOrder + commitDraft + discardDraft useCallbacks; setFetchTick refresh | VERIFIED | Line 158 createWorkOrder using crypto.randomUUID() + POST /v1/work-orders/sync; line 185 commitDraft POST /v1/work-orders/:caseNumber/commit; line 204 discardDraft method:'DELETE'; all 3 call setFetchTick(n=>n+1); return-object lines 224-226 includes all 3 |
| `vigil-pwa/src/pages/WorkOrdersPage.tsx` | "+ Create work order" CTA + both modals hosted; onReview wiring | VERIFIED | Lines 16-17 showCreateModal + reviewingWorkOrder useState; line 101 CTA text; line 143 `onReview={setReviewingWorkOrder}` wired to row; lines 151/158 both modal conditional renders |
| `vigil-pwa/src/components/CreateWorkOrderModal.test.tsx` | 8 tests (Vitest+RTL): regex validation + onCreate + Escape + outside-click | VERIFIED | 8/8 tests pass; asserts onCreate called with maintenanceProblem + department keys |
| `vigil-pwa/src/components/ReviewWorkOrderModal.test.tsx` | 8 tests: pre-populate + extras JSON + commit + discard with confirm | VERIFIED | 8/8 tests pass; asserts onCommit + onDiscard callbacks |
| `vigil-extension/manifest.json` | Phase 84 baseline: v1.0.0, no service-now.com, no MV3 background, no content_scripts | VERIFIED | manifest_version 3, version "1.0.0", host_permissions only `["https://api.vigilhub.io/*"]`, no background/content_scripts keys |
| `vigil-extension/popup.html` | Phase 84 popup with setup-view + capture-view; no svcnow-view | VERIFIED | `id="setup-view"` + `id="capture-view"` present; `id="svcnow-view"` absent |
| `vigil-extension-tests/parity.test.ts` | 6 assertions (relaxed): 2 manifest set-equality + 3 popup byte-equality + 1 absence-test | VERIFIED | 6/6 tests pass; EXT-revert: prefix; existsSync absence-test for the 3 SVCNOW files on both sides |
| `.planning/REQUIREMENTS.md` | 8 new SCAP-* + WO-MANUAL-* IDs; SVCNOW-01/02/03/05 SUPERSEDED markers; SVCNOW-04 PRESERVED; coverage 53→61; phase distribution adds 129.1 (8) | VERIFIED | All 8 IDs in section + traceability table; SVCNOW-04 PRESERVED unchanged; SVCNOW-01/02/03/05 carry per-item supersession pointers; v3.9 total = 61; Phase distribution mentions `129.1 (8)`; footer date 2026-05-17 |
| `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` | Operator runbook with 6 scenarios, pre-recorded dispositions for Wave 1 + Wave 3, [pending] for the rest | VERIFIED | 273 lines (≥120 required); 6 numbered scenarios + 5a/5b + 6a/6b sub-scenarios; both `launchctl unload` + `launchctl load` present; Scenarios 4 + 5a marked `[PASS — Wave N pre-recorded]`; remaining sub-scenarios `[pending]`; Phase 129.2 follow-up queue captured |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `vigil-core/src/routes/work-orders.ts` (sanitizer destructure) | `vigil-core/src/db/schema.ts` (workOrders.maintenanceProblem + .department) | SanitizedWorkOrder type extension + sanitizer destructure | WIRED | Sanitizer destructure lines 109-110 carry both fields; SanitizedWorkOrder interface lines 34-35 declares both; dbUpsertLegacy SET lines 493-494 explicitly includes both |
| `scripts/vigil-capture/src/watcher.ts` | `POST {VIGIL_API_BASE}/v1/captures/screenshot` | fetch with Bearer auth from Keychain | WIRED | URL fragment `/v1/captures/screenshot` present; Bearer auth header constructed from `await getApiKeyFn()`; fetch invoked with JSON body |
| `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` | `scripts/vigil-capture/dist/watcher.js` | ProgramArguments array | WIRED | ProgramArguments points to `/usr/local/bin/node` + absolute path to dist/watcher.js; KeepAlive enabled; RunAtLoad true |
| `scripts/vigil-capture/src/cli.ts` | `launchctl bootstrap/bootout gui/$UID` | execSync calls | WIRED | Both bootstrap and bootout commands present (idempotent bootout-then-bootstrap pattern) |
| `vigil-core/src/index.ts` | `vigil-core/src/routes/captures-screenshot.ts` | `app.route('/v1', capturesScreenshot)` AFTER bearerAuth dispatcher | WIRED | Import line 22; mount line 229; bearerAuth dispatcher is at ~line 168 — mount is after |
| `vigil-pwa/src/components/CreateWorkOrderModal.tsx` | `vigil-pwa/src/hooks/useWorkOrders.ts (createWorkOrder)` | onCreate prop | WIRED | WorkOrdersPage line 153 passes `onCreate={createWorkOrder}` to CreateWorkOrderModal |
| `vigil-pwa/src/components/ReviewWorkOrderModal.tsx` | `vigil-pwa/src/hooks/useWorkOrders.ts (commitDraft + discardDraft)` | onCommit + onDiscard props | WIRED | WorkOrdersPage lines 161-162 pass `onCommit={commitDraft}` + `onDiscard={discardDraft}` |
| `vigil-pwa/src/hooks/useWorkOrders.ts (createWorkOrder)` | `POST /v1/work-orders/sync` (sanitizer extended in plan 01) | vigilFetch | WIRED | createWorkOrder line 163: `vigilFetch('/v1/work-orders/sync', { method: 'POST', body: ... state: 'open', clientCaptureId })` |
| `vigil-pwa/src/hooks/useWorkOrders.ts (commitDraft)` | `POST /v1/work-orders/:caseNumber/commit` (new route plan 05) | vigilFetch | WIRED | commitDraft line 188: `/v1/work-orders/${encodeURIComponent(caseNumber)}/commit` POST |
| `vigil-pwa/src/hooks/useWorkOrders.ts (discardDraft)` | `DELETE /v1/work-orders/:caseNumber` (new route plan 05) | vigilFetch | WIRED | discardDraft lines 205-209: `/v1/work-orders/${encodeURIComponent(caseNumber)}` with method:'DELETE' |
| `vigil-pwa/src/pages/WorkOrdersPage.tsx` | `WorkOrderRow.onReview` → `ReviewWorkOrderModal` | onReview prop + reviewingWorkOrder useState | WIRED | Line 143 passes `onReview={setReviewingWorkOrder}`; lines 157-164 conditionally renders ReviewWorkOrderModal with workOrder + commitDraft/discardDraft |
| `vigil-extension/popup.html` | `vigil-extension/popup.js` | id selectors (setup-view, capture-view, textarea, Send button) | WIRED | popup.html contains both `setup-view` + `capture-view` divs; popup.js references them (smoke test PASSED in Wave 1) |
| Chrome `popup.{html,css,js}` | Safari `Resources/popup.{html,css,js}` | byte-equality enforced by parity.test.ts | WIRED | diff -q produces no output for all 3 files; assertion 3-5 of parity.test.ts encode this |

### Data-Flow Trace (Level 4)

Verified that wired artifacts produce real data, not static stubs.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `captures-screenshot.ts` route | INSERT VALUES (workOrders row) | `extracted.required` from `parseAIJson(callClaudeMultimodal(...))` + extras from extracted.extras + clientCaptureId from request body | Yes — real Anthropic API call (no mock); real Postgres INSERT via Drizzle | FLOWING |
| `captures-screenshot.ts` route | response body extractedFields | `extracted.required` parsed JSON | Yes — same path as INSERT | FLOWING |
| `watcher.ts` daemon | POST request body | fs.readFile → base64 + crypto.randomUUID() + extension-derived mediaType | Yes — real fs read; UUID per file | FLOWING |
| `work-orders.ts` commit handler | UPDATE SET object | request body `edits` whitelist-coerced; state="open" + lastChangeAt | Yes — real operator-supplied edits | FLOWING |
| `work-orders.ts` delete handler | DELETE WHERE | (caseNumber path param, userId from c.get) | Yes — userId from bearerAuth dispatcher | FLOWING |
| `CreateWorkOrderModal` (PWA) | onCreate payload | form state from 11 useState slots | Yes — real form input | FLOWING |
| `ReviewWorkOrderModal` (PWA) | onCommit edits payload | initial values from workOrder prop + operator edits | Yes — real API row + edits | FLOWING |
| `useWorkOrders.createWorkOrder` | POST request body | input prop + crypto.randomUUID + state="open" | Yes — real input | FLOWING |
| `useWorkOrders.commitDraft` | POST request body | edits prop from review modal | Yes — real edits | FLOWING |
| `WorkOrdersPage` row rendering | wo.state branching | useWorkOrders.workOrders → fetched from /v1/work-orders endpoint | Yes — real fetch (existing data layer) | FLOWING |

No HOLLOW or STATIC data sources detected. The screenshot endpoint genuinely calls Anthropic + writes real INSERT; the daemon genuinely reads files + posts; the PWA mutations call real `vigilFetch`.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| vigil-core builds (TypeScript clean) | `cd vigil-core && npm run build` | exit 0 (tsc + tsc -p tsconfig.scripts.json both clean) | PASS |
| vigil-pwa builds (TypeScript + Vite) | `cd vigil-pwa && npm run build` | exit 0 (Vite output: 21 precache entries, sw.js + workbox emitted) | PASS |
| vigil-capture builds (TypeScript) | `cd scripts/vigil-capture && npm run build` | exit 0; `dist/cli.js` + `dist/watcher.js` both emitted | PASS |
| work-orders tests | `cd vigil-core && npx tsx --test src/routes/work-orders.test.ts` | 22 pass / 0 fail | PASS |
| captures-screenshot tests | `cd vigil-core && npx tsx --test src/routes/captures-screenshot.test.ts` | 10 pass / 0 fail | PASS |
| watcher tests | `cd scripts/vigil-capture && npx tsx --test src/watcher.test.ts` (after `npm install`) | 7 pass / 0 fail | PASS |
| PWA modal tests | `cd vigil-pwa && npx vitest run src/components/CreateWorkOrderModal.test.tsx src/components/ReviewWorkOrderModal.test.tsx` | 16 pass / 0 fail (2 files) | PASS |
| Extension parity tests | `npm run test:extension` | 6 pass / 0 fail | PASS |
| Chrome ↔ Safari popup byte-equality | `diff -q vigil-extension/popup.{html,css,js} vigil-safari-extension/.../popup.{...}` | no output (byte-identical for all 3 files) | PASS |
| SVCNOW-only files absent (Chrome) | `ls vigil-extension/{background.js,content-script.js,popup-helpers.js}` | "No such file" × 3 | PASS |
| SVCNOW-only files absent (Safari) | `ls "vigil-safari-extension/Vigil Capture Extension/Resources/{...}"` | "No such file" × 3 | PASS |
| popup-helpers.test.ts deleted | `ls vigil-extension-tests/popup-helpers.test.ts` | "No such file" | PASS |
| Migration 0022 idempotent shape | `grep ADD COLUMN IF NOT EXISTS` on the migration SQL | 2 matches (maintenance_problem + department) | PASS |

### Probe Execution

Not applicable to this phase. Phase 129.1 has no `scripts/*/tests/probe-*.sh` convention probes and the PLANs do not declare any. Behavioral spot-checks (above) cover the runnable-code verification.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SCAP-01 | 03 | POST /v1/captures/screenshot with bearerAuth + UUID v4 clientCaptureId + dedup + pending_review + onConflictDoUpdate + response shape | SATISFIED | captures-screenshot.ts implements full surface; 10/10 tests green; REQUIREMENTS.md item marked `[x]` Complete; mounted in index.ts after bearerAuth |
| SCAP-02 | 03 | Anthropic vision via callClaudeMultimodal(userId); JSON schema with required + extras; parseAIJson handles fences; 422 on empty case_number; post-UAT remap reflects final field mapping | SATISFIED | EXTRACTION_PROMPT post-remap (lines 90-91); parseAIJson invocation; 422 branch tested (T9); userId passed to callClaudeMultimodal (T1 assertion) |
| SCAP-03 | 04 | macOS LaunchAgent + chokidar awaitWriteFinish + atomic move to processed/failed + orphan-scan | SATISFIED | watcher.ts implements all 4 behaviors; 7/7 tests green; LaunchAgent plist KeepAlive + Aqua + Background; orphan-scan present (Wave 3 pre-tested for happy path) |
| SCAP-04 | 04 | vigil-capture CLI install/uninstall/status/tail; idempotent; chmod 700; README documents Keychain rotation | SATISFIED | cli.ts has all 4 subcommands; idempotent bootout-then-bootstrap; 0o700 mkdir; README 182 lines covers all required sections |
| SCAP-05 | 05 | PWA WorkOrderRow amber pending_review badge + Review draft button; ReviewWorkOrderModal with extras JSON + Commit/Discard | SATISFIED | WorkOrderRow.tsx STATUS_STYLES + isPendingReview branch; ReviewWorkOrderModal.tsx with `<details>` extras + window.confirm Discard + onCommit/onDiscard wiring; 8 tests pass |
| WO-MANUAL-01 | 05 | PWA WorkOrdersPage "+ Create work order" CTA + CreateWorkOrderModal with 11 editable fields + case_number regex + POSTs to /v1/work-orders/sync state=open + crypto.randomUUID | SATISFIED | All wired (Page CTA line 101, Modal with 11 fields, regex enforcement, hook POST verified); 8 tests pass. **Note:** REQUIREMENTS.md acknowledges department column queued for 129.2 removal but the column + sanitizer + UI exist now. |
| WO-MANUAL-02 | 05 | useWorkOrders adds createWorkOrder + commitDraft + discardDraft useCallbacks with setFetchTick refresh + vigilFetch | SATISFIED | All 3 useCallbacks present (lines 158, 185, 204); each calls setFetchTick(n=>n+1); uses vigilFetch wrapper |
| WO-MANUAL-03 | 01 | vigil-core SanitizedWorkOrder + sanitizer destructure + dbInsertOrGet + dbUpsertLegacy carry maintenanceProblem + department; "12 known fields" comment; migration 0022 nullable text | SATISFIED | All 4 sites carry both fields; "12 known fields" comment present; migration 0022 nullable; tests pass round-trip |
| SVCNOW-01 (SUPERSEDED) | 02 | Browser extension content-script on `*.service-now.com/*` — REVERTED | SATISFIED (revert) | manifest.json has no service-now.com; content-script.js deleted from both extension dirs; REQUIREMENTS.md marks `[~]` with supersession pointer |
| SVCNOW-02 (SUPERSEDED) | 02 | Popup state-machine with CS# drift detector — REVERTED | SATISFIED (revert) | popup.html has setup-view + capture-view (Phase 84 shape); no MutationObserver popup-state-machine; REQUIREMENTS.md `[~]` supersession |
| SVCNOW-03 (SUPERSEDED) | 02 | Popup form for SVCNOW capture — REVERTED | SATISFIED (revert) | popup.html no svcnow-view; popup.js back to Phase 84 generic capture; REQUIREMENTS.md `[~]` supersession |
| SVCNOW-04 (PRESERVED) | — | client_capture_id + composite partial unique index | PRESERVED | Migration 0021 still applied; captures-screenshot.ts and work-orders.ts both use clientCaptureId dedup; REQUIREMENTS.md `[x]` PRESERVED unchanged |
| SVCNOW-05 (SUPERSEDED) | 02 | Safari extension lock-step port — REVERTED | SATISFIED (revert) | Safari files reset to pre-SVCNOW state; popup-helpers.test.ts deleted; parity test relaxed to 6 assertions; byte-equality with Chrome enforced |

All requirement IDs from PLAN frontmatter accounted for in REQUIREMENTS.md (≥2 occurrences each: section + traceability). No orphaned requirements detected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `vigil-core/src/routes/work-orders.ts` | 175 | `TODO: When manual work order creation is added, skip auto-archive for manual orders (D-05)` | Info (pre-existing) | Introduced by commit `965ba038` on 2026-05-15, BEFORE Phase 129.1 started (2026-05-17). Not a phase-129.1 debt marker. Acknowledged as a known future-work pointer from an earlier phase's D-05 design decision. |
| `scripts/vigil-capture/src/cli.ts` | 86 | `// 2. Read source plist and substitute ${HOME} placeholder if present.` | Info (descriptive) | "Placeholder" is descriptive language referring to the literal `${HOME}` template token in the plist, not a code-stub marker. Surrounding comment explains the verbatim-copy fallback. Not a debt marker. |

No `FIXME`, `XXX`, or unreferenced `HACK` markers introduced by this phase. The single `TODO` in work-orders.ts predates Phase 129.1 and references its origin decision D-05. No `return null` / `return []` empty-implementation stubs detected in any phase-modified file. No hardcoded empty-data patterns detected.

### Human Verification Required

7 items require operator action on hardware. See frontmatter `human_verification:` array for the full list. The user-prompt explicitly notes these as pending operator action (`[pending]` in UAT runbook):

1. **Polaris screenshot POST-REMAP retest** — Wave 3 PASSED with pre-remap field mapping; post-remap retest is `[pending]` (UAT Scenario 1)
2. **Manual-create flow end-to-end** — Plan 05 user-flow has not been operator-tested (UAT Scenario 2)
3. **Draft discard flow** — Confirms hard-delete via window.confirm (UAT Scenario 3)
4. **Daemon pause/resume + orphan-scan** — Plan 04 Task 5 step 6 (UAT Scenario 5b)
5. **Multi-file drop sequential processing** — UAT Scenario 6a
6. **API key rotation without daemon restart** — UAT Scenario 6b
7. **Safari Xcode "Copy Bundle Resources" build phase update** — Plan 02 Task 3 paused checkpoint; source files verified absent but .xcodeproj reference state requires Xcode GUI

### Deferred Items

Per Step 9b, the SUMMARY for Plan 06 explicitly queues two items for Phase 129.2 ("Phase 129.2 follow-up queue" in 129.1-06-SUMMARY.md). However, Phase 129.2 does NOT yet exist as a roadmap entry. These items are NOT verifier-deferred gaps — they are deliberate scope decisions captured in the SUMMARY for future planning. They don't affect the current verification status because:

1. The `work_orders.department` column drop is a follow-up cleanup (the column exists and is null-safe — no harm in leaving it). Plan 05 modals expose it as "Maintenance Location" label which the operator confirmed is fine for now.
2. ROADMAP success criteria do NOT require `department` column removal; they only require the screenshot pipeline to extract structured fields (which it does, regardless of which DB column the semantic ends up on).

No actionable gaps from Phase 129.1 are formally deferred to a non-existent future phase. The "Phase 129.2 follow-up queue" is informational for future planning, not a verifier-flagged gap.

### Gaps Summary

**No verifier-detected gaps.** All ROADMAP success criteria SC1-SC3 are VERIFIED in the codebase with full key-link wiring, real data flow, and green automated tests. SC4 (end-to-end Polaris case → PWA verification) is `human_needed` because:
- The Wave 3 operator UAT PASSED for the happy-path with a real `CS0363817` screenshot, but observed wrong field labels in the PWA Review modal.
- The hot-fix `5926037` remapped the EXTRACTION_PROMPT (verified in code at captures-screenshot.ts lines 90-91).
- The post-remap retest against a fresh Polaris page is **pending Railway autodeploy + operator action** (explicitly noted in the user prompt).

The remaining 6 human-verification items are user-flow + visual + real-time behavioral tests that no grep can validate. They are documented in the UAT runbook as `[pending]` and tracked through the standard end-of-phase HUMAN-UAT path.

**Phase 129.1 implementation is goal-achieved on the codebase.** Operator UAT closure is the remaining work, which the user prompt explicitly acknowledges as the proper state.

---

_Verified: 2026-05-17T21:33:09Z_
_Verifier: Claude (gsd-verifier)_
