---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 129.1-01
files_modified:
  - vigil-core/src/routes/captures-screenshot.ts
  - vigil-core/src/routes/captures-screenshot.test.ts
  - vigil-core/src/index.ts
autonomous: false
requirements:
  - SCAP-01
  - SCAP-02
user_setup:
  - service: anthropic
    why: "Vision extraction of Polaris screenshot uses claude-sonnet-4-20250514 via existing callClaudeMultimodal()."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Already provisioned in Phase 126 — verify present in Railway env. No new setup."
    dashboard_config: []

must_haves:
  truths:
    - "POST /v1/captures/screenshot with valid base64 PNG + mediaType + clientCaptureId returns 200 with {workOrderId, duplicate, extractedFields, extraExtractedFields}."
    - "POST with invalid mediaType returns 400; with missing fields returns 400; with Anthropic upstream error returns 502; with no ANTHROPIC_API_KEY returns 503."
    - "Second POST with same (userId, clientCaptureId) short-circuits to the existing row and returns duplicate=true."
    - "A successful POST writes a work_orders row with state='pending_review' and the 2 new columns (maintenance_problem, department) populated from extraction; case_number PK collision triggers onConflictDoUpdate (latest wins)."
    - "callClaudeMultimodal is invoked with userId so per-user spend tracking in ai_usage_daily (Phase 127 GUARD-03) accumulates correctly."
  artifacts:
    - path: "vigil-core/src/routes/captures-screenshot.ts"
      provides: "POST /v1/captures/screenshot route with Anthropic vision extraction → work_orders pending_review write"
      exports: ["createCapturesScreenshotRoute", "capturesScreenshot"]
      min_lines: 120
    - path: "vigil-core/src/routes/captures-screenshot.test.ts"
      provides: "Unit + integration tests for SCAP-01 + SCAP-02 (mock-db + mock-Anthropic via DI)"
      contains: "describe"
    - path: "vigil-core/src/index.ts"
      provides: "Route mount: app.route('/v1', capturesScreenshot) after bearerAuth dispatcher"
      contains: "capturesScreenshot"
  key_links:
    - from: "vigil-core/src/routes/captures-screenshot.ts"
      to: "vigil-core/src/ai/client.ts (callClaudeMultimodal)"
      via: "import { callClaudeMultimodal, getAIClient, parseAIJson }"
      pattern: "callClaudeMultimodal\\("
    - from: "vigil-core/src/routes/captures-screenshot.ts"
      to: "vigil-core/src/db/schema.ts (workOrders)"
      via: "db.insert(workOrders).values({...}).onConflictDoUpdate(...).returning()"
      pattern: "workOrders.*pending_review"
    - from: "vigil-core/src/index.ts"
      to: "vigil-core/src/routes/captures-screenshot.ts"
      via: "app.route('/v1', capturesScreenshot) AFTER bearerAuth dispatcher (line ~135)"
      pattern: "app\\.route\\(.*captures"
---

<objective>
Build `POST /v1/captures/screenshot` on vigil-core. The endpoint accepts a base64 PNG/JPG screenshot + mediaType + clientCaptureId, calls Anthropic vision (via existing `callClaudeMultimodal()` with per-user spend tracking), parses the JSON response into required + extras fields, and writes a `work_orders` row in `state: 'pending_review'` with the 2 new columns (`maintenance_problem`, `department`) populated. SVCNOW-04 dedup is preserved via SELECT-by-(userId, clientCaptureId) short-circuit before extraction. `case_number` PK collision uses `onConflictDoUpdate` (latest screenshot wins — operator's review step is the safety net). Implements SCAP-01 + SCAP-02 (planner-authored requirement IDs landed in plan 129.1-06).

Purpose: Replace the SVCNOW assisted-capture popup (typing description for every case — too high-friction) with a "drop a screenshot in a folder" workflow. The endpoint is the server-side half; the macOS daemon (plan 129.1-04) is the client-side half.

Output: 1 new route file (modeled verbatim on `describe-image.ts` + `dbInsertOrGet` patterns), 1 new test file (mock-db + mock-Anthropic via DI factory), 1 mount line edit in `index.ts`, 1 blocking operator-decision checkpoint for the `department` Polaris field mapping (DECISION-A unresolved — RESEARCH Q1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md
@vigil-core/src/routes/describe-image.ts
@vigil-core/src/routes/work-orders.ts
@vigil-core/src/ai/client.ts
@vigil-core/src/db/schema.ts
@vigil-core/src/index.ts
@vigil-core/src/routes/brief-generate.test.ts

<interfaces>
<!-- Primary analog (vision + auth + spend): describe-image.ts entire file (105 LOC) -->
<!-- Secondary analog (db write + dedup): work-orders.ts dbInsertOrGet + dbUpsertLegacy ~lines 313-357 -->

callClaudeMultimodal signature (vigil-core/src/ai/client.ts):
  callClaudeMultimodal({
    system?: string,
    content: Array<TextBlock | ImageBlock>,
    maxTokens: number,
    userId: number,  // REQUIRED — Phase 127 GUARD-03 / T-127-03
  }): Promise<string>  // returns raw text; use parseAIJson<T>() to extract JSON

ImageBlock shape: { type: "image", source: { type: "base64", media_type: MediaType, data: string } }
TextBlock shape: { type: "text", text: string }

parseAIJson<T>(raw: string): T — strips ```json ... ``` fences; throws on non-JSON.

c.get("userId") — populated by bearerAuth dispatcher at vigil-core/src/index.ts ~line 135. NEVER read userId from body.

DI factory pattern (from work-orders.ts line 56 — createWorkOrdersRoute(deps)):
  deps interface: { dbAvailable: boolean; db: typeof db; aiClient?: AIClient; ... }
  Use this for the new route so unit tests can inject mock-db + mock-Anthropic.

dbInsertOrGet pattern (work-orders.ts ~lines 313-334):
  SELECT-by-(userId, clientCaptureId) → if exists return {row, isNew: false}; else INSERT → return {row, isNew: true}.

Risk 1 mitigation (case_number PK collision): use onConflictDoUpdate on workOrders.caseNumber with SET clause for the editable fields. Operator's mental model = latest screenshot wins.

Phase 84 endpoint pattern is consistent: bearerAuth → c.get("userId") → validate body → call AI → write DB → return JSON. Don't deviate.

Route mount goes in vigil-core/src/index.ts AFTER the bearerAuth dispatcher (~line 135) at the same indentation as `app.route("/v1", describeImage)`. Confirm by reading index.ts before editing.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator decides Polaris `department` field mapping (DECISION-A unresolved, RESEARCH Q1)</name>
  <decision>What does `work_orders.department` capture from a Polaris case page?</decision>
  <context>
    Per CONTEXT.md DECISION-A, the new `department` column needs a Polaris field mapping. This decision shapes the vision-extraction prompt (Task 2) and the manual-create form label (plan 129.1-05). RESEARCH Q1 (BLOCKING) preserved this as an operator decision because all three options are plausible.

    The vision prompt template in plan 129.1-03 Task 2 contains a placeholder `<DEPARTMENT-FIELD-FROM-OPERATOR-DECISION>` that gets replaced based on this answer.
  </context>
  <options>
    <option id="option-a">
      <name>Assignment Group (e.g., "MT-Maintenance")</name>
      <pros>Identifies the ServiceNow routing team — useful for "which team owns this".</pros>
      <cons>Cardinality is low (~5-10 values); may not capture per-store context.</cons>
    </option>
    <option id="option-b">
      <name>Maintenance Location (e.g., "Bakery")</name>
      <pros>Captures the store area where the issue physically is — useful for in-store filtering.</pros>
      <cons>Overlaps with existing `location` column; redundant unless operator wants both.</cons>
    </option>
    <option id="option-c">
      <name>Service (e.g., "MT-Trades")</name>
      <pros>ServiceNow service category — coarser-grained than Assignment Group.</pros>
      <cons>Less actionable than Assignment Group for routing.</cons>
    </option>
    <option id="option-d">
      <name>Free text — operator types any of the three depending on context</name>
      <pros>Maximum flexibility; one column covers all use cases.</pros>
      <cons>Less extraction-deterministic; vision prompt becomes "look for the most relevant of Assignment Group, Maintenance Location, or Service".</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, option-c, or option-d. Also confirm any secondary preferences (e.g., "option-b for now, may add assignment_group as a separate column later").</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /v1/captures/screenshot route + DI factory + mount in index.ts</name>
  <files>vigil-core/src/routes/captures-screenshot.ts, vigil-core/src/index.ts</files>
  <read_first>
    - vigil-core/src/routes/describe-image.ts (PRIMARY analog — entire 105-line file; copy the auth + body validation + multimodal + parseAIJson + 502 error shape verbatim)
    - vigil-core/src/routes/work-orders.ts (SECONDARY analog — dbInsertOrGet ~lines 313-334 for SELECT-by-(userId, clientCaptureId) dedup; createWorkOrdersRoute factory at line 56 for DI pattern)
    - vigil-core/src/ai/client.ts (callClaudeMultimodal signature ~lines 100-126; parseAIJson ~lines 137-145; getAIClient ~lines 30-50)
    - vigil-core/src/db/schema.ts (workOrders table block ~lines 308-331 — confirm maintenanceProblem + department exist from plan 01)
    - vigil-core/src/index.ts (find the bearerAuth dispatcher line ~135 and the existing `app.route("/v1", describeImage)` mount around line 213; identify exact line numbers from the current file)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Screenshot Endpoint" (lines 312-479) — full code sketch
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-core/src/routes/captures-screenshot.ts" (LANDMINES block — 5 critical items)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md (confirm schema.ts edits landed)
  </read_first>
  <behavior>
    - Module exports `createCapturesScreenshotRoute(deps)` DI factory that returns a Hono router instance; also exports `capturesScreenshot` as the pre-wired singleton (using real db + real aiClient) for the index.ts mount.
    - DI factory accepts deps: `{ dbAvailable: boolean; db: typeof db; getAIClientFn?: typeof getAIClient; callClaudeMultimodalFn?: typeof callClaudeMultimodal }`. Defaults to the real imports when not injected.
    - POST /captures/screenshot validates: imageBase64 (non-empty string), mediaType (one of "image/png", "image/jpeg"), clientCaptureId (non-empty string + UUID v4 shape `/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i` per RESEARCH Q6 recommendation).
    - On any validation failure: 400 with `{error: "<specific message>"}`.
    - If `getAIClientFn()` returns falsy: 503 `{error: "AI service unavailable. ANTHROPIC_API_KEY not configured."}`.
    - SVCNOW-04 dedup: SELECT by (userId, clientCaptureId) FIRST. If existing row found, return 200 with `{workOrderId: existing.caseNumber, duplicate: true, extractedFields: <echoed from row>, extraExtractedFields: <parsed from existing.notes if JSON, else {}>}`. NO Anthropic call on dedup hit.
    - On no dedup hit: call `callClaudeMultimodalFn({content: [imageBlock, textBlock], maxTokens: 1500, userId})` with the extraction prompt (see EXTRACTION_PROMPT constant below). On AI error: 502 `{error: <message>}`.
    - Use `parseAIJson<{required: ..., extras: ...}>(rawText)` to parse Claude response (handles ```json fences).
    - Validate `extracted.required.case_number` non-empty; if empty: 422 `{error: "Vision extraction returned no case_number", extras: extracted.extras}`.
    - INSERT into workOrders with: `caseNumber = extracted.required.case_number`, `userId`, `state = "pending_review"`, `shortDescription = extracted.required.short_description`, `location = extracted.required.location`, `maintenanceProblem = extracted.required.maintenance_problem`, `department = extracted.required.department`, `notes = JSON.stringify(extracted.extras)`, `clientCaptureId = body.clientCaptureId`, `syncedAt = new Date()`, other fields = "" or null. Use `.onConflictDoUpdate({target: workOrders.caseNumber, set: {...editable fields..., syncedAt: new Date()}})` to handle case_number PK collision (latest wins per RESEARCH Risk 1).
    - Return 200 with `{workOrderId: inserted[0].caseNumber, duplicate: false, extractedFields: extracted.required, extraExtractedFields: extracted.extras}`.
    - When `!deps.dbAvailable`: 503 `{error: "Database not available"}`.
  </behavior>
  <action>
Create `vigil-core/src/routes/captures-screenshot.ts`. Structure:

1. Imports: Hono from hono; eq + and from drizzle-orm; db from "../db/connection.js"; workOrders from "../db/schema.js"; callClaudeMultimodal + getAIClient + parseAIJson from "../ai/client.js".

2. Constants:
   - VALID_MEDIA_TYPES = ["image/png", "image/jpeg"] as const.
   - UUID_V4_REGEX = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.
   - MAX_IMAGE_BASE64_LENGTH = 7_000_000 (≈5.25MB — RESEARCH §479).
   - EXTRACTION_PROMPT (string literal) — copy verbatim from RESEARCH §491-527, EXCEPT replace `<DEPARTMENT-FIELD-FROM-OPERATOR-DECISION>` with the field name decided in Task 1. If Task 1 picked option-d (free text), use the wording: "the operator-relevant department label — look for the most relevant of Assignment Group, Maintenance Location, or Service".

3. Type definitions:
   - interface RequiredExtracted (5 string fields per RESEARCH §339-345).
   - interface ExtraExtracted (13 optional string fields per RESEARCH §348-362).
   - interface CapturesScreenshotDeps (factory deps with sensible defaults).

4. Function `createCapturesScreenshotRoute(deps: Partial<CapturesScreenshotDeps> = {})` returns a `new Hono()` with the POST /captures/screenshot handler implementing the behavior above. Use injected deps; fall back to real `db`, real `getAIClient`, real `callClaudeMultimodal`.

5. Pre-wired singleton: `export const capturesScreenshot = createCapturesScreenshotRoute()` at module bottom.

Handler implementation rules:
- `const userId = c.get("userId")` — NEVER read userId from body.
- Body parse via `await c.req.json()`; on parse failure return 400 with `{error: "Invalid JSON body"}`.
- Validate the 3 required body fields + UUID_V4_REGEX + MAX_IMAGE_BASE64_LENGTH (return 413 with `{error: "Image too large"}` if exceeded).
- SELECT-by-(userId, clientCaptureId) BEFORE calling Anthropic; short-circuit on dedup.
- Call `callClaudeMultimodal({content: [{type:"image", source:{type:"base64", media_type: body.mediaType, data: body.imageBase64}}, {type:"text", text: EXTRACTION_PROMPT}], maxTokens: 1500, userId})`.
- On AI throw: catch, return 502 `{error: err.message}`.
- parseAIJson → extracted; validate `extracted?.required?.case_number` non-empty (422 if not).
- INSERT with onConflictDoUpdate on caseNumber PK; .returning() to get the row.
- Return 200 with the response shape above.

Then edit `vigil-core/src/index.ts`:
- Add an import line: `import { capturesScreenshot } from "./routes/captures-screenshot.js";` next to the existing `describeImage` import.
- Add a mount line: `app.route("/v1", capturesScreenshot);` IMMEDIATELY AFTER the existing `app.route("/v1", describeImage);` line (around line ~213; verify by reading index.ts).
- Mount must be AFTER the bearerAuth dispatcher (~line 135) — the describeImage mount already is, so adjacent placement guarantees it.

Do NOT add any new locked-enum error codes to `vigil-pwa/src/lib/api-error-codes.ts` in this plan; those are PWA-side and the existing 4xx/5xx body shapes are sufficient for the daemon's needs.
  </action>
  <acceptance_criteria>
    - `vigil-core/src/routes/captures-screenshot.ts` exists with `export const capturesScreenshot` AND `export function createCapturesScreenshotRoute`.
    - Same file contains `c.get("userId")` (NOT `body.userId`) — verify with grep + count.
    - Same file contains the literal string `state: "pending_review"` (the state value being written).
    - Same file contains `callClaudeMultimodal` invocation with both `userId` and `maxTokens` named arguments.
    - Same file contains `parseAIJson` invocation.
    - Same file contains `onConflictDoUpdate` with `target: workOrders.caseNumber`.
    - Same file contains `UUID_V4_REGEX` constant.
    - Same file contains `VALID_MEDIA_TYPES = ["image/png", "image/jpeg"]`.
    - EXTRACTION_PROMPT does NOT contain the literal placeholder `<DEPARTMENT-FIELD-FROM-OPERATOR-DECISION>` (must be replaced per Task 1 decision).
    - `vigil-core/src/index.ts` contains the import `from "./routes/captures-screenshot.js"`.
    - `vigil-core/src/index.ts` contains the mount `app.route("/v1", capturesScreenshot)`.
    - `cd vigil-core && npm run build 2>&1` exits 0 (TypeScript compiles).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-core && grep -q 'export const capturesScreenshot' src/routes/captures-screenshot.ts && grep -q 'export function createCapturesScreenshotRoute' src/routes/captures-screenshot.ts && grep -q 'c.get("userId")' src/routes/captures-screenshot.ts && ! grep -q 'body.userId' src/routes/captures-screenshot.ts && grep -q '"pending_review"' src/routes/captures-screenshot.ts && grep -q 'callClaudeMultimodal' src/routes/captures-screenshot.ts && grep -q 'parseAIJson' src/routes/captures-screenshot.ts && grep -q 'onConflictDoUpdate' src/routes/captures-screenshot.ts && grep -q 'UUID_V4_REGEX' src/routes/captures-screenshot.ts && grep -q 'VALID_MEDIA_TYPES' src/routes/captures-screenshot.ts && ! grep -q 'DEPARTMENT-FIELD-FROM-OPERATOR-DECISION' src/routes/captures-screenshot.ts && grep -q 'from "./routes/captures-screenshot.js"' src/index.ts && grep -q 'app.route("/v1", capturesScreenshot)' src/index.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Route file exists with DI factory + singleton + all 5 validation branches + dedup short-circuit + onConflictDoUpdate; mounted in index.ts; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit + integration tests for the screenshot endpoint (mock-db + mock-Anthropic via DI)</name>
  <files>vigil-core/src/routes/captures-screenshot.test.ts</files>
  <read_first>
    - vigil-core/src/routes/brief-generate.test.ts (PRIMARY analog — node:test + mock-db factory pattern at lines 1-60)
    - vigil-core/src/routes/work-orders.test.ts (secondary — same node:test + DI mock pattern; check existing mock-db shape that matches the new db.insert(...).values(...).onConflictDoUpdate(...).returning() chain)
    - vigil-core/src/routes/captures-screenshot.ts (the route file from Task 2 — confirm the DI factory accepts injected deps for db + getAIClient + callClaudeMultimodal)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Phase Requirements → Test Map" (lines 902-907 — SCAP-01 + SCAP-02 test cases)
  </read_first>
  <behavior>
    Test cases (each as a separate `test(...)` in node:test):

    1. **Happy path** — POST with valid base64 + mediaType + UUID clientCaptureId. Mock-db has no existing row. Mock-Anthropic returns a valid JSON response with all required fields. Expected: 200 status; response body has `workOrderId`, `duplicate: false`, `extractedFields.case_number`, `extraExtractedFields`. Mock-db captures the INSERT VALUES; assert state="pending_review", maintenanceProblem and department populated.

    2. **Invalid mediaType** — POST with `mediaType: "image/gif"`. Expected: 400; body.error mentions "mediaType". Anthropic NOT called (assert mock call count = 0).

    3. **Invalid clientCaptureId** (not UUID v4 shape) — POST with `clientCaptureId: "not-a-uuid"`. Expected: 400; body.error mentions "clientCaptureId". Anthropic NOT called.

    4. **Missing imageBase64** — POST with body lacking imageBase64. Expected: 400; body.error mentions "imageBase64".

    5. **No AI client** — DI inject `getAIClientFn: () => null`. POST with otherwise-valid body. Expected: 503; body.error mentions "AI service unavailable".

    6. **Anthropic error** — DI inject `callClaudeMultimodalFn` that throws `new Error("anthropic timeout")`. POST with valid body. Expected: 502; body.error contains "anthropic timeout". Mock-db INSERT NOT called.

    7. **Dedup short-circuit** — Mock-db has existing row matching (userId, clientCaptureId). POST with same clientCaptureId. Expected: 200; body.duplicate === true; body.workOrderId === existing caseNumber. Mock-Anthropic NOT called (assert call count = 0).

    8. **case_number PK collision (onConflictDoUpdate path)** — Mock-db is keyed on caseNumber; first POST creates row "CS0000001"; second POST with DIFFERENT clientCaptureId but Anthropic returns the same case_number "CS0000001". Expected: 200; the second POST hits onConflictDoUpdate path; mock captures the SET object containing the updated shortDescription/location/maintenanceProblem/department + new clientCaptureId.

    9. **Vision returns empty case_number** — Mock-Anthropic returns JSON with `required.case_number: ""`. Expected: 422; body.error mentions "case_number"; mock-db INSERT NOT called.

    10. **Image too large** — POST with imageBase64.length > MAX_IMAGE_BASE64_LENGTH. Expected: 413; body.error mentions "too large".
  </behavior>
  <action>
Create `vigil-core/src/routes/captures-screenshot.test.ts`. Use `node:test` + `assert from "node:assert/strict"` + `tsx --test` runner (matches brief-generate.test.ts).

Mock-db factory (similar to brief-generate.test.ts lines 23-50): builds a fluent chainable object supporting `select().from().where().limit()`, `insert(table).values(input).onConflictDoUpdate({target, set}).returning()`. Capture the values passed at each chain step (selectArgs, insertValues, conflictSet) for assertion.

Mock-Anthropic: a `vi.fn()`-equivalent (use a `let lastCallArgs; const fn = (args) => { lastCallArgs = args; return Promise.resolve(JSON.stringify({required:{...}, extras:{...}})); }` pattern). Variants for happy path, throw, and configurable returns.

For each of the 10 cases above, build a Hono fetch-style test:
  const route = createCapturesScreenshotRoute({db: mockDb, getAIClientFn: () => mockClient, callClaudeMultimodalFn: mockMultimodal});
  // Need to set c.get("userId") — the route reads from c.get(). Wrap the app or use a middleware that c.set("userId", 1) before the route.
  const app = new Hono(); app.use("*", async (c, next) => { c.set("userId", 1); await next(); }); app.route("/v1", route);
  const res = await app.request("/v1/captures/screenshot", { method: "POST", headers: {"Content-Type":"application/json"}, body: JSON.stringify({...}) });
  assert.equal(res.status, expectedStatus);
  const body = await res.json();
  assert.equal(body.duplicate, expectedDuplicate);
  // etc.

Each test must include at least one assertion on the captured mock arguments (proves the route called the right thing with the right values).

Run `cd vigil-core && npm test 2>&1` and confirm all tests pass.
  </action>
  <acceptance_criteria>
    - `vigil-core/src/routes/captures-screenshot.test.ts` exists and contains at least 10 `test(...)` declarations (one per case above).
    - File imports `from "node:test"` (NOT vitest — vigil-core uses node:test).
    - File contains the string `createCapturesScreenshotRoute` (proves it uses the DI factory).
    - File contains assertions on each of: 200 status (happy path), 400 status (invalid input), 413 (too large), 422 (empty case_number), 502 (AI error), 503 (no AI client).
    - File contains the literal `duplicate: true` and `duplicate: false` assertions.
    - File contains an assertion on captured INSERT VALUES that includes `state: "pending_review"`.
    - File contains an assertion on captured onConflictDoUpdate SET object (proves collision path covered).
    - `cd vigil-core && npm test 2>&1 | tee /tmp/captures-test.log` exits 0.
    - `/tmp/captures-test.log` includes a passing line for at least one test name containing "screenshot" or "captures".
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-core && [ "$(grep -v '^#' src/routes/captures-screenshot.test.ts | grep -v '//' | grep -cE 'test\(')" -ge "10" ] && grep -q 'from "node:test"' src/routes/captures-screenshot.test.ts && grep -q 'createCapturesScreenshotRoute' src/routes/captures-screenshot.test.ts && grep -q '"pending_review"' src/routes/captures-screenshot.test.ts && grep -q 'onConflictDoUpdate' src/routes/captures-screenshot.test.ts && npm test 2>&1 | tee /tmp/vigil-core-test-03.log | tail -40 && grep -qiE 'screenshot|capture' /tmp/vigil-core-test-03.log</automated>
  </verify>
  <done>10+ tests cover the SCAP-01/02 happy + sad paths; node:test runner; all green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS daemon (operator Mac) → POST /v1/captures/screenshot | Bearer-auth POST with base64 image — bearerAuth validates token → c.set("userId") |
| vigil-core → Anthropic API | callClaudeMultimodal with per-user spend tracking (Phase 127 GUARD-03) |
| vigil-core → Postgres (workOrders) | Drizzle ORM parameter binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-09 | Spoofing | userId injection via body | mitigate | Handler uses `c.get("userId")` populated by bearerAuth dispatcher; NEVER reads userId from body (Phase 121 D-D2 lock). Verified in acceptance criteria via grep. |
| T-129.1-10 | Denial of Service | large base64 payload | mitigate | MAX_IMAGE_BASE64_LENGTH = 7_000_000 bytes (≈5.25MB raw); request body checked BEFORE Anthropic call; 413 response on exceed. |
| T-129.1-11 | Denial of Service | Anthropic spend exhaustion | mitigate | callClaudeMultimodal carries `userId` → per-user daily AI-cost watermark (Phase 127 GUARD-03 `DAILY_AI_BUDGET_EXCEEDED`) blocks the call when watermark hit. No new logic needed. |
| T-129.1-12 | Information Disclosure | PII in vision logs | accept-via-existing-control | describe-image.ts (analog) does not log image content; this endpoint inherits same pattern. Operator-only single-user scope further reduces exposure (DECISION-F). |
| T-129.1-13 | Tampering | replay attack via clientCaptureId | mitigate | UUID v4 shape validation prevents trivial collisions; SELECT-by-(userId, clientCaptureId) dedup short-circuits replays (returns the existing row idempotently). |
| T-129.1-14 | Tampering | case_number PK collision corruption | accept | RESEARCH Risk 1: when two screenshots of the same Polaris case land, latest-wins via onConflictDoUpdate. Operator's review step (plan 05) is the safety net. Acceptable for v1. |
| T-129.1-15 | Information Disclosure | extras JSON in notes column | accept | The 13 optional extracted fields are persisted to `notes` as JSON for operator review. Same trust boundary as the rest of work_orders (per-userId scoped via FK to users.id). |
| T-129.1-SC | Tampering | npm/pip/cargo installs | mitigate | NO new package installs in this plan. All deps (@anthropic-ai/sdk, drizzle-orm, hono) already verified in vigil-core/package.json. |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "POST returns 200 with extractedFields" → Task 3 case 1 (happy path).
2. Truth: "400 on invalid input; 502 on Anthropic error; 503 on no AI client" → Task 3 cases 2/3/4/5/6.
3. Truth: "dedup short-circuit returns duplicate=true" → Task 3 case 7.
4. Truth: "writes pending_review row with 2 new columns" → Task 3 case 1 assertion on captured INSERT VALUES.
5. Truth: "callClaudeMultimodal called with userId" → Task 3 happy-path assertion on captured Anthropic args.

**Phase-level checks:**

- `cd vigil-core && npm test` exits 0.
- `cd vigil-core && npm run build` exits 0.
- Manual smoke test (deferred to plan 06 UAT runbook): POST a real screenshot from curl → confirm 200 + DB row.

</verification>

<success_criteria>

- New route file `vigil-core/src/routes/captures-screenshot.ts` exports both `createCapturesScreenshotRoute` (DI factory) and `capturesScreenshot` (singleton).
- Route handler reads userId from `c.get("userId")` (NEVER body).
- Body validation covers imageBase64, mediaType (PNG/JPEG only), clientCaptureId (UUID v4), image size cap (5.25MB base64).
- SVCNOW-04 dedup via SELECT-by-(userId, clientCaptureId) short-circuit.
- Anthropic call via callClaudeMultimodal with `userId` (Phase 127 GUARD-03 compliance).
- parseAIJson handles fence-wrapped JSON.
- 422 when extracted case_number empty.
- onConflictDoUpdate for case_number PK collisions (latest wins).
- Response shape: {workOrderId, duplicate, extractedFields, extraExtractedFields}.
- Mounted in index.ts after bearerAuth dispatcher.
- 10+ unit tests via node:test, all green.
- TypeScript compiles.
- Operator decision recorded for `department` Polaris field mapping; EXTRACTION_PROMPT placeholder replaced.

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md` when done. Include: which option (a/b/c/d) operator selected for `department` mapping (so plan 05 form label + plan 06 requirement docs stay in sync).
</output>
