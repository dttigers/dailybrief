---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 05
type: execute
wave: 2
depends_on:
  - 129.1-01
files_modified:
  - vigil-core/src/routes/work-orders.ts
  - vigil-core/src/routes/work-orders.test.ts
  - vigil-pwa/src/pages/WorkOrdersPage.tsx
  - vigil-pwa/src/components/WorkOrderRow.tsx
  - vigil-pwa/src/components/CreateWorkOrderModal.tsx
  - vigil-pwa/src/components/CreateWorkOrderModal.test.tsx
  - vigil-pwa/src/components/ReviewWorkOrderModal.tsx
  - vigil-pwa/src/components/ReviewWorkOrderModal.test.tsx
  - vigil-pwa/src/hooks/useWorkOrders.ts
  - vigil-pwa/src/api/client.ts
autonomous: true
requirements:
  - SCAP-05
  - WO-MANUAL-01
  - WO-MANUAL-02
  - WO-MANUAL-03

must_haves:
  truths:
    - "Operator (or any user) sees a '+ Create work order' button on the PWA /work-orders page; clicking opens a modal form with every editable work_orders column (case_number, store, short_description, trade, location, equipment, priority, contact, notes, maintenance_problem, department); submitting POSTs to /v1/work-orders/sync with state='open' + a generated UUID clientCaptureId, and the row appears in the list after refresh."
    - "Manual-create form rejects case_number not matching /^[A-Z0-9]+$/ at form-level (server has no regex enforcement)."
    - "A row with state='pending_review' renders an amber 'Review draft' button (NOT the status-cycle button); clicking opens the ReviewWorkOrderModal pre-populated with extracted fields + extras JSON visible in a collapsible block."
    - "Review modal Commit button calls POST /v1/work-orders/:caseNumber/commit with any operator edits + state='open' transition; Discard button calls DELETE /v1/work-orders/:caseNumber (hard-delete)."
    - "useWorkOrders hook returns createWorkOrder, commitDraft, discardDraft callbacks; each invokes setFetchTick(n+1) on success to trigger re-fetch."
    - "Both new vigil-core routes (POST /:caseNumber/commit + DELETE /:caseNumber) mount inside createWorkOrdersRoute() factory; scoped by userId; respect the SanitizedWorkOrder 12-field whitelist from plan 01."
  artifacts:
    - path: "vigil-pwa/src/components/CreateWorkOrderModal.tsx"
      provides: "Modal form with all 11 editable fields + case_number regex validation + UUID clientCaptureId generation"
      min_lines: 100
    - path: "vigil-pwa/src/components/ReviewWorkOrderModal.tsx"
      provides: "Pre-populated edit form + extras JSON collapsible block + Commit/Discard buttons"
      min_lines: 80
    - path: "vigil-pwa/src/hooks/useWorkOrders.ts"
      provides: "Extended with createWorkOrder + commitDraft + discardDraft mutations"
      contains: "createWorkOrder"
    - path: "vigil-pwa/src/pages/WorkOrdersPage.tsx"
      provides: "+ Create work order CTA + pending_review filter awareness + modal hosting"
      contains: "Create work order"
    - path: "vigil-pwa/src/components/WorkOrderRow.tsx"
      provides: "pending_review badge style + Review draft button branch"
      contains: "pending_review"
    - path: "vigil-core/src/routes/work-orders.ts"
      provides: "Extended with POST /:caseNumber/commit + DELETE /:caseNumber routes"
      contains: "/commit"
  key_links:
    - from: "vigil-pwa/src/components/CreateWorkOrderModal.tsx"
      to: "vigil-pwa/src/hooks/useWorkOrders.ts (createWorkOrder)"
      via: "onCreate prop"
      pattern: "onCreate"
    - from: "vigil-pwa/src/components/ReviewWorkOrderModal.tsx"
      to: "vigil-pwa/src/hooks/useWorkOrders.ts (commitDraft + discardDraft)"
      via: "onCommit + onDiscard props"
      pattern: "onCommit|onDiscard"
    - from: "vigil-pwa/src/hooks/useWorkOrders.ts (createWorkOrder)"
      to: "POST /v1/work-orders/sync (existing endpoint, sanitizer extended in plan 01)"
      via: "vigilFetch"
      pattern: "/v1/work-orders/sync"
    - from: "vigil-pwa/src/hooks/useWorkOrders.ts (commitDraft)"
      to: "POST /v1/work-orders/:caseNumber/commit (NEW route in this plan)"
      via: "vigilFetch"
      pattern: "/commit"
    - from: "vigil-pwa/src/hooks/useWorkOrders.ts (discardDraft)"
      to: "DELETE /v1/work-orders/:caseNumber (NEW route in this plan)"
      via: "vigilFetch"
      pattern: "DELETE"
---

<objective>
Build the PWA manual-create + draft-review surface AND the two new vigil-core routes that back it. PWA side: a "+ Create work order" CTA on `WorkOrdersPage.tsx` opens `CreateWorkOrderModal` with all 11 editable `work_orders` columns including the 2 new ones (`maintenance_problem`, `department`); submitting POSTs to existing `/v1/work-orders/sync` with `state: "open"` + a UUID `clientCaptureId`. Rows with `state: "pending_review"` (created by the screenshot endpoint in plan 03) render an amber "Review draft" button that opens `ReviewWorkOrderModal` pre-populated with extracted fields + a collapsible "extras JSON" block; Commit applies operator edits + state transition via `POST /v1/work-orders/:caseNumber/commit`; Discard calls `DELETE /v1/work-orders/:caseNumber`. Hook side: `useWorkOrders` gains 3 new useCallback mutations following the existing setFetchTick refresh pattern. Server side: 2 new routes added to `createWorkOrdersRoute()` factory + their tests.

Purpose: WO-MANUAL-01/02/03 + SCAP-05 (planner-authored IDs landed in plan 06). The PWA is where non-operator users create work orders without a popup, and where the operator reviews screenshot-extracted drafts before committing them.

Output: 5 new/modified PWA files (page + row + 2 modals + hook), 2 new modal test files, work-orders.ts gets 2 new routes + their tests, api/client.ts gets fetch helpers if needed. No new external deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md
@vigil-pwa/src/pages/WorkOrdersPage.tsx
@vigil-pwa/src/components/WorkOrderRow.tsx
@vigil-pwa/src/components/ContextMenu.tsx
@vigil-pwa/src/components/ThoughtRow.test.tsx
@vigil-pwa/src/hooks/useWorkOrders.ts
@vigil-pwa/src/api/client.ts
@vigil-core/src/routes/work-orders.ts
@vigil-core/src/routes/work-orders.test.ts

<interfaces>
<!-- Existing useWorkOrders return shape (line ~136 of useWorkOrders.ts): -->
return { workOrders, isLoading, error, updateLocalStatus, unarchive, deleteAllArchived };
// Plan 05 extends to:
return { workOrders, isLoading, error, updateLocalStatus, unarchive, deleteAllArchived, createWorkOrder, commitDraft, discardDraft };

<!-- vigilFetch wrapper (vigil-pwa/src/api/client.ts ~line 254): -->
// vigilFetch is the existing fetch wrapper that handles 401-Session-expired uniformly.
// All new hook mutations use vigilFetch — do NOT use raw fetch.

<!-- Existing /v1/work-orders/sync request shape: -->
// POST body: { workOrders: SanitizedWorkOrder[] } — sanitizer extended in plan 01 covers the 12 fields.
// createWorkOrder sends a 1-element array with state="open" + crypto.randomUUID() clientCaptureId.

<!-- NEW vigil-core routes: -->
// POST /v1/work-orders/:caseNumber/commit
//   body: Partial<EditableFields> — applies edits + transitions state from pending_review → open
//   userId-scoped via c.get("userId"); 404 if row not found or not owned by user; 409 if state already !== 'pending_review'
//
// DELETE /v1/work-orders/:caseNumber
//   no body; hard-deletes the row (scoped by userId); 404 if not found

<!-- Existing pattern for unarchive at lines 257-302 of work-orders.ts is the closest analog for DELETE -->

<!-- Modal pattern (no existing modal primitive): -->
// Use inline overlay div: <div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center" onClick={onClose}>
//   <div className="bg-gray-900 rounded-lg p-6 max-w-lg w-full max-h-[90vh] overflow-auto" onClick={(e)=>e.stopPropagation()}>...</div>
// </div>
// Close on Escape (useEffect + window.addEventListener) AND outside-click (overlay onClick) — match ContextMenu D-07 semantics.

<!-- STATUS_STYLES at WorkOrderRow.tsx lines 17-21: -->
const STATUS_STYLES: Record<string, string> = {
  open: 'bg-yellow-500/20 text-yellow-400 hover:bg-yellow-500/30',
  inProgress: 'bg-info-50 text-info-400 hover:bg-info-50/80',
  done: 'bg-green-500/20 text-green-400 hover:bg-green-500/30',
};
// ADD: pending_review: 'bg-amber-500/20 text-amber-400 hover:bg-amber-500/30'

<!-- Form fields list (DECISION-D) — 11 editable fields for the create form: -->
case_number (text, required, /^[A-Z0-9]+$/), store, short_description (textarea, required), trade, location, equipment,
priority (select: Low/Medium/High/Critical), contact, notes (textarea), maintenance_problem (input — free text per Q2 fallback,
or select if operator decided otherwise in plan 03 Task 1), department (input — text or select per Task 1 decision).
state is set automatically (open for create, pending_review→open for commit); userId/clientCaptureId/syncedAt are auto-managed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add POST /v1/work-orders/:caseNumber/commit + DELETE /v1/work-orders/:caseNumber routes + tests</name>
  <files>vigil-core/src/routes/work-orders.ts, vigil-core/src/routes/work-orders.test.ts</files>
  <read_first>
    - vigil-core/src/routes/work-orders.ts (FULL FILE — confirm plan 01's edits landed: SanitizedWorkOrder has maintenanceProblem + department; sanitizer destructure includes both; dbUpsertLegacy SET includes both; comment says "12 known fields")
    - vigil-core/src/routes/work-orders.test.ts (existing test fixture + mock-db shape; the existing unarchive test is the closest analog for DELETE)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"New vigil-core endpoints needed" (lines 823-830)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-core/src/routes/work-orders.ts MODIFY" + Shared Patterns §"Auth / userId resolution"
  </read_first>
  <behavior>
    Two new routes mounted inside the `createWorkOrdersRoute()` factory:

    **POST /:caseNumber/commit:**
    - userId from c.get("userId").
    - Body is `Partial<EditableWorkOrderFields>` — at minimum may be `{}`, at most all 11 editable columns.
    - Parse body via c.req.json(); on parse failure 400.
    - SELECT row by (caseNumber from path param, userId). If not found: 404 `{error: "Work order not found"}`.
    - If existing row's state !== "pending_review": 409 `{error: "Work order is not a pending_review draft"}`.
    - Build sanitized update object (reuse the 12-field whitelist pattern from the existing sanitizer — extract a `sanitizeEditableFields(body)` helper if it doesn't already exist).
    - UPDATE the row: SET state="open", lastChangeAt=now, lastChangeSummary="Committed from review", PLUS any operator edits from the body whitelist.
    - Return 200 with the updated row.

    **DELETE /:caseNumber:**
    - userId from c.get("userId").
    - SELECT row by (caseNumber, userId). If not found: 404.
    - DELETE the row (hard-delete; no archive).
    - Also DELETE any matching row in `work_order_statuses` (if that table exists — check schema.ts).
    - Return 204 (no body) on success.

    Tests (extend existing work-orders.test.ts):
    - commit: happy path (pending_review → open with edits applied)
    - commit: 404 when not owned by user
    - commit: 409 when state !== "pending_review"
    - commit: edits to maintenanceProblem + department propagate
    - delete: happy path returns 204
    - delete: 404 when not owned
    - delete: hard-deletes (not soft) — mock captures DELETE call
  </behavior>
  <action>
Open `vigil-core/src/routes/work-orders.ts`. Locate the `createWorkOrdersRoute(deps)` factory (line 56 area). Inside the returned Hono router, add two new route handlers immediately after the existing `unarchive` route (which is around lines 257-302):

1. `router.post("/work-orders/:caseNumber/commit", async (c) => { ... })`:
   - Parse caseNumber from `c.req.param("caseNumber")`.
   - Parse body: `let edits: Record<string, unknown> = {}; try { edits = await c.req.json(); } catch {}` (allow empty body — operator may commit with no edits).
   - Use `deps.db` to SELECT by (workOrders.caseNumber === caseNumber, workOrders.userId === userId).
   - If 0 rows: return c.json({error: "Work order not found"}, 404).
   - If row.state !== "pending_review": return c.json({error: "Work order is not a pending_review draft"}, 409).
   - Build the SET object: start with `{state: "open", lastChangeAt: new Date(), lastChangeSummary: "Committed from review"}`; then for each of the 11 editable fields, if present in `edits` (using the same `wo.X != null` coerce-or-skip pattern as the sanitizer), add `field: String(edits.X)` (or null for nullable fields).
   - UPDATE the row; .returning(); return c.json(updated[0], 200).

2. `router.delete("/work-orders/:caseNumber", async (c) => { ... })`:
   - caseNumber from path param.
   - SELECT row by (caseNumber, userId). If 0 rows: 404.
   - DELETE from workOrders. Also DELETE from workOrderStatuses if schema.ts has that table (check via grep — if it has `workOrderStatuses` export, also delete; if not, skip).
   - Return c.body(null, 204).

If extracting a `sanitizeEditableFields` helper, place it at the top of the file near `SanitizedWorkOrder` so both the sync handler and the commit handler can share it. Update the comment to mention both call sites.

Then extend `vigil-core/src/routes/work-orders.test.ts` with 7 new tests covering the cases above. Use the existing mock-db pattern; add `select().from().where().limit()` chain that returns the canned existing row; add `update(table).set(...).where(...).returning()` chain capture; add `delete(table).where(...)` chain capture.

Run `cd vigil-core && npm test` and confirm green.
  </action>
  <acceptance_criteria>
    - `vigil-core/src/routes/work-orders.ts` contains the literal route path string `"/work-orders/:caseNumber/commit"`.
    - Same file contains the literal route path string `"/work-orders/:caseNumber"` for the DELETE handler.
    - Both handlers read userId via `c.get("userId")` (grep count of `c.get("userId")` increases by at least 2).
    - Commit handler contains the state-guard `'pending_review'` check returning 409.
    - DELETE handler returns 204 (grep for `204` near the delete route).
    - `vigil-core/src/routes/work-orders.test.ts` contains at least 7 new `test(...)` declarations (count BEFORE this plan + 7 ≤ count AFTER).
    - Test names reference "commit" and "delete" (grep both).
    - At least one commit test asserts a 409 response code; at least one asserts edits to `maintenanceProblem` and `department` propagate.
    - `cd vigil-core && npm test` exits 0.
    - `cd vigil-core && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-core && grep -q '"/work-orders/:caseNumber/commit"' src/routes/work-orders.ts && grep -qE '"/work-orders/:caseNumber"' src/routes/work-orders.ts && grep -qE "['\"]pending_review['\"]" src/routes/work-orders.ts && grep -qE 'c\.body\(null,\s*204\)|return c\..*204' src/routes/work-orders.ts && grep -qi 'commit' src/routes/work-orders.test.ts && grep -qi 'delete' src/routes/work-orders.test.ts && grep -q '409' src/routes/work-orders.test.ts && grep -qE 'maintenanceProblem|maintenance_problem' src/routes/work-orders.test.ts && npm test 2>&1 | tee /tmp/vigil-core-test-05.log | tail -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both new routes mounted in factory; 7+ new tests pass; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CreateWorkOrderModal + extend useWorkOrders.createWorkOrder + WorkOrdersPage CTA</name>
  <files>vigil-pwa/src/components/CreateWorkOrderModal.tsx, vigil-pwa/src/components/CreateWorkOrderModal.test.tsx, vigil-pwa/src/hooks/useWorkOrders.ts, vigil-pwa/src/pages/WorkOrdersPage.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/WorkOrdersPage.tsx (FULL FILE — find the filter-tab row at lines 63-80 and the "Clear Archived" conditional CTA at lines 87-99)
    - vigil-pwa/src/hooks/useWorkOrders.ts (FULL FILE — locate unarchive + deleteAllArchived useCallback pattern at lines 118-134; the return object at line ~136)
    - vigil-pwa/src/components/ContextMenu.tsx (portaled overlay + outside-click + Escape close pattern — the closest existing modal-ish thing)
    - vigil-pwa/src/components/ThoughtRow.test.tsx (Vitest + RTL + base-fixture pattern)
    - vigil-pwa/src/api/client.ts (vigilFetch wrapper)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md (confirm which option a/b/c/d operator selected for `department` so the form label matches)
  </read_first>
  <behavior>
    **CreateWorkOrderModal** (new component):
    - Props: `{onClose: () => void; onCreate: (input: ManualCreateInput) => Promise<void>}`.
    - Renders an overlay-modal (Tailwind: `fixed inset-0 z-50 bg-black/50 flex items-center justify-center` + inner `bg-gray-900 rounded-lg p-6 max-w-lg w-full max-h-[90vh] overflow-auto`).
    - Closes on Escape, on outside-click (overlay onClick), and on a Cancel button click.
    - Form has 11 inputs covering: case_number (text, required, validation `/^[A-Z0-9]+$/`), store, short_description (textarea required), trade, location, equipment, priority (select: "", Low, Medium, High, Critical), contact, notes (textarea), maintenance_problem (text), department (text — or select per plan 03 Task 1 decision).
    - Live form-level validation on case_number; shows inline error "Case number must contain only A-Z and 0-9" if invalid; disables Submit button until valid.
    - On Submit: calls `await onCreate({caseNumber, store, shortDescription, trade, location, equipment, priority, contact, notes, maintenanceProblem, department})` then onClose; catches errors and surfaces inline (e.g. red text below the form).
    - Tailwind palette consistent with existing dark theme.

    **useWorkOrders.createWorkOrder** (new useCallback):
    - Signature: `(input: ManualCreateInput) => Promise<void>`.
    - Generates `clientCaptureId = crypto.randomUUID()`.
    - Calls `await vigilFetch('/v1/work-orders/sync', { method: 'POST', body: JSON.stringify({workOrders: [{...input, state: 'open', clientCaptureId}]}) })`.
    - On non-2xx: throws `new Error('Failed to create work order: ' + res.status)`.
    - On success: `setFetchTick(n => n + 1)`.
    - Add to return object (line ~136).

    **WorkOrdersPage CTA**:
    - Replace the existing filter-tab `<div>` wrapper with a `flex items-center justify-between` row: filter tabs on the left, "+ Create work order" button on the right.
    - Button style: `bg-teal-600 text-white hover:bg-teal-700 rounded-full px-3 py-1.5 text-sm font-medium`.
    - State: `const [showCreateModal, setShowCreateModal] = useState(false)`.
    - Conditional render: `{showCreateModal && <CreateWorkOrderModal onClose={() => setShowCreateModal(false)} onCreate={createWorkOrder} />}`.
    - `createWorkOrder` destructured from `useWorkOrders()`.

    **Tests** (CreateWorkOrderModal.test.tsx — Vitest + RTL):
    - Renders all 11 inputs.
    - case_number lowercase rejected: shows inline error, Submit disabled.
    - case_number uppercase + digits accepted: error gone, Submit enabled.
    - Submit calls onCreate with the right shape (capture via vi.fn()).
    - Submit captured payload includes maintenanceProblem + department fields.
    - Outside-click (overlay) calls onClose.
    - Escape key calls onClose.
  </behavior>
  <action>
1. Create `vigil-pwa/src/components/CreateWorkOrderModal.tsx`. Use the modal shell from RESEARCH §786 (interfaces section above). Implement form with all 11 inputs. Add a top-level `useEffect` to register `keydown` listener for Escape close. Use raw `<input>`, `<select>`, `<textarea>` with consistent Tailwind classes from WorkOrderRow.tsx palette (`bg-gray-900/80 border border-gray-400/30 rounded` and `text-gray-100`). Add a `validateCaseNumber` helper at the top of the file that returns `true | "Case number must contain only A-Z and 0-9"`. Use `useState` for each field (or a single state object — operator's discretion).

2. Open `vigil-pwa/src/hooks/useWorkOrders.ts`. Add the `createWorkOrder` useCallback near the existing `unarchive`/`deleteAllArchived` definitions. Use `crypto.randomUUID()` for clientCaptureId. Use `vigilFetch` (the existing wrapper). Add `createWorkOrder` to the return object on line ~136.

3. Open `vigil-pwa/src/pages/WorkOrdersPage.tsx`. Make 3 edits:
   - Destructure `createWorkOrder` from `useWorkOrders()`.
   - Add `const [showCreateModal, setShowCreateModal] = useState(false)` near other state declarations.
   - Replace the filter-tab block (lines 63-80) so it has a `flex items-center justify-between` parent: tabs on left, new `+ Create work order` button on right. Existing "Clear Archived" conditional CTA stays where it is (it's outside this row).
   - Import `CreateWorkOrderModal` and render it conditionally below the list:
     `{showCreateModal && <CreateWorkOrderModal onClose={() => setShowCreateModal(false)} onCreate={createWorkOrder} />}`

4. Create `vigil-pwa/src/components/CreateWorkOrderModal.test.tsx` (Vitest + RTL). Follow the ThoughtRow.test.tsx pattern. Cover the 7 test cases above.

Run `cd vigil-pwa && npm test` and confirm green. Run `cd vigil-pwa && npm run build` to ensure TypeScript + Vite build succeeds.
  </action>
  <acceptance_criteria>
    - `vigil-pwa/src/components/CreateWorkOrderModal.tsx` exists with `export default function CreateWorkOrderModal`.
    - File contains all 11 field-name strings: caseNumber, store, shortDescription, trade, location, equipment, priority, contact, notes, maintenanceProblem, department.
    - File contains the literal regex `/^[A-Z0-9]+$/`.
    - File contains `bg-black/50` (modal overlay style).
    - File handles Escape close (grep for `'Escape'`).
    - `vigil-pwa/src/hooks/useWorkOrders.ts` contains `createWorkOrder` as a useCallback.
    - Same file contains `crypto.randomUUID()`.
    - Same file's return object contains `createWorkOrder`.
    - `vigil-pwa/src/pages/WorkOrdersPage.tsx` contains the literal string `Create work order`.
    - Same file imports `CreateWorkOrderModal`.
    - `vigil-pwa/src/components/CreateWorkOrderModal.test.tsx` exists with at least 7 `it(` or `test(` declarations.
    - Test file contains assertions on the `onCreate` callback being called with `maintenanceProblem` and `department` keys.
    - `cd vigil-pwa && npm test -- --run CreateWorkOrderModal 2>&1 | tee /tmp/create-modal-test.log` exits 0.
    - `cd vigil-pwa && npm run build 2>&1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-pwa && grep -q 'export default function CreateWorkOrderModal' src/components/CreateWorkOrderModal.tsx && grep -q 'caseNumber' src/components/CreateWorkOrderModal.tsx && grep -q 'maintenanceProblem' src/components/CreateWorkOrderModal.tsx && grep -q 'department' src/components/CreateWorkOrderModal.tsx && grep -q '/\^\[A-Z0-9\]+\$/' src/components/CreateWorkOrderModal.tsx && grep -q 'bg-black/50' src/components/CreateWorkOrderModal.tsx && grep -q "'Escape'" src/components/CreateWorkOrderModal.tsx && grep -q 'createWorkOrder' src/hooks/useWorkOrders.ts && grep -q 'crypto.randomUUID()' src/hooks/useWorkOrders.ts && grep -q 'Create work order' src/pages/WorkOrdersPage.tsx && grep -q 'CreateWorkOrderModal' src/pages/WorkOrdersPage.tsx && [ "$(grep -v '^#' src/components/CreateWorkOrderModal.test.tsx | grep -v '//' | grep -cE '(it|test)\(')" -ge "7" ] && grep -q 'maintenanceProblem' src/components/CreateWorkOrderModal.test.tsx && grep -q 'department' src/components/CreateWorkOrderModal.test.tsx && npm test 2>&1 | tee /tmp/pwa-test-task2.log | tail -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CreateWorkOrderModal renders all 11 fields with case_number validation; useWorkOrders.createWorkOrder works via vigilFetch + clientCaptureId; WorkOrdersPage shows the CTA; 7+ tests pass; PWA builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build ReviewWorkOrderModal + WorkOrderRow pending_review branch + useWorkOrders commitDraft/discardDraft</name>
  <files>vigil-pwa/src/components/ReviewWorkOrderModal.tsx, vigil-pwa/src/components/ReviewWorkOrderModal.test.tsx, vigil-pwa/src/components/WorkOrderRow.tsx, vigil-pwa/src/hooks/useWorkOrders.ts</files>
  <read_first>
    - vigil-pwa/src/components/WorkOrderRow.tsx (FULL FILE — STATUS_STYLES at lines 17-21; status-cycle button at lines 62-68; "Updated" amber badge at lines 78-87)
    - vigil-pwa/src/components/CreateWorkOrderModal.tsx (just-built in Task 2 — extract the form layout to share, or sister-implement)
    - vigil-pwa/src/hooks/useWorkOrders.ts (createWorkOrder addition from Task 2 — extend with commitDraft + discardDraft following the same pattern)
    - vigil-pwa/src/api/client.ts (vigilFetch + WorkOrderApiResponse interface)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Pending-review row indicator" + §"useWorkOrders hook extension"
  </read_first>
  <behavior>
    **ReviewWorkOrderModal** (new component):
    - Props: `{workOrder: WorkOrderApiResponse; onClose: () => void; onCommit: (caseNumber: string, edits: Partial<ManualCreateInput>) => Promise<void>; onDiscard: (caseNumber: string) => Promise<void>}`.
    - Renders the same modal shell as CreateWorkOrderModal (`fixed inset-0 z-50 bg-black/50 ...`).
    - Form pre-populates each field from `workOrder` props.
    - Includes a collapsible `<details>` block showing the `extras` JSON (parse from `workOrder.notes` if it's JSON — fall back to empty if not parseable; surface in `<pre>`).
    - Two action buttons:
      - "Commit" → calls `onCommit(workOrder.caseNumber, currentFormState)` then onClose. Style: teal-600 (positive action).
      - "Discard" → shows confirm prompt ("Discard this draft? This cannot be undone."), then calls `onDiscard(workOrder.caseNumber)` then onClose. Style: red-500 (destructive).
    - Cancel button: just closes without saving.
    - Same Escape + outside-click close semantics as CreateWorkOrderModal.

    **WorkOrderRow pending_review branch**:
    - Extend STATUS_STYLES with `pending_review: 'bg-amber-500/20 text-amber-400 hover:bg-amber-500/30'`.
    - When `workOrder.state === 'pending_review'`, render the Review draft button INSTEAD of the status-cycle button. The status-cycle button at lines 62-68 must NOT be cyclable on pending_review rows (RESEARCH §568 LANDMINE).
    - Add a prop `onReview?: (workOrder: WorkOrderApiResponse) => void` so the parent (WorkOrdersPage) can host the modal.
    - The Review button calls `props.onReview?.(workOrder)`.

    **useWorkOrders.commitDraft + discardDraft** (new useCallbacks):
    - `commitDraft(caseNumber: string, edits: Partial<ManualCreateInput>) => Promise<void>`: POST to `/v1/work-orders/${caseNumber}/commit`; throw on non-2xx; setFetchTick on success.
    - `discardDraft(caseNumber: string) => Promise<void>`: DELETE `/v1/work-orders/${caseNumber}`; throw on non-2xx; setFetchTick on success.
    - Add both to the return object.

    **WorkOrdersPage hosting** (small edit):
    - Add `const [reviewingWorkOrder, setReviewingWorkOrder] = useState<WorkOrderApiResponse | null>(null)`.
    - Pass `onReview={setReviewingWorkOrder}` to WorkOrderRow.
    - Conditionally render: `{reviewingWorkOrder && <ReviewWorkOrderModal workOrder={reviewingWorkOrder} onClose={() => setReviewingWorkOrder(null)} onCommit={commitDraft} onDiscard={discardDraft} />}`.
    - Destructure `commitDraft`, `discardDraft` from `useWorkOrders()`.

    **Tests** (ReviewWorkOrderModal.test.tsx):
    - Form pre-populates fields from workOrder prop.
    - Extras JSON collapsible renders parsed JSON when workOrder.notes is valid JSON.
    - Extras JSON gracefully handles non-JSON notes (no crash; empty state).
    - Commit button calls onCommit with the right caseNumber + edits diff.
    - Discard button calls onDiscard after confirm (use vi.spyOn(window, 'confirm') returning true).
    - Discard does NOT call onDiscard if confirm returns false.
    - Escape / outside-click close.
  </behavior>
  <action>
1. Create `vigil-pwa/src/components/ReviewWorkOrderModal.tsx`. Mirror CreateWorkOrderModal structure; pre-populate each field from `workOrder` props via useState initializers; add `<details>` for the extras JSON; add Commit + Discard buttons with the styling above. Use `window.confirm("Discard this draft? This cannot be undone.")` before calling onDiscard.

2. Edit `vigil-pwa/src/components/WorkOrderRow.tsx`:
   - Extend STATUS_STYLES with the `pending_review` key.
   - Add `onReview?: (wo: WorkOrderApiResponse) => void` to props.
   - When `workOrder.state === 'pending_review'`:
     - Do NOT render the status-cycle button (early-return or conditional swap).
     - Render the Review draft button: `<button onClick={() => onReview?.(workOrder)} className="bg-amber-500/20 text-amber-400 hover:bg-amber-500/30 rounded-full px-3 py-1 text-xs font-medium">Review draft</button>`

3. Edit `vigil-pwa/src/hooks/useWorkOrders.ts`:
   - Add `commitDraft` useCallback (POST `/v1/work-orders/${caseNumber}/commit`).
   - Add `discardDraft` useCallback (DELETE `/v1/work-orders/${caseNumber}`).
   - Add both to the return object alongside createWorkOrder from Task 2.

4. Edit `vigil-pwa/src/pages/WorkOrdersPage.tsx`:
   - Destructure `commitDraft, discardDraft` from useWorkOrders().
   - Add `const [reviewingWorkOrder, setReviewingWorkOrder] = useState<WorkOrderApiResponse | null>(null)`.
   - Pass `onReview={setReviewingWorkOrder}` to each `<WorkOrderRow>`.
   - Conditionally render ReviewWorkOrderModal.

5. Create `vigil-pwa/src/components/ReviewWorkOrderModal.test.tsx` with the 7 test cases above.

Run `cd vigil-pwa && npm test` and `cd vigil-pwa && npm run build`. Both must be green.
  </action>
  <acceptance_criteria>
    - `vigil-pwa/src/components/ReviewWorkOrderModal.tsx` exists with `export default function ReviewWorkOrderModal`.
    - File contains `<details>` (for extras JSON collapsible).
    - File contains `window.confirm` (Discard confirmation).
    - File contains both `onCommit` and `onDiscard` prop usage.
    - `vigil-pwa/src/components/WorkOrderRow.tsx` contains the string `pending_review` in STATUS_STYLES.
    - Same file contains `Review draft` (button text).
    - Same file contains `onReview` (prop name).
    - `vigil-pwa/src/hooks/useWorkOrders.ts` contains `commitDraft` AND `discardDraft` useCallbacks.
    - Same file's return object contains both.
    - Same file contains the literal path fragment `/commit`.
    - Same file contains `method: 'DELETE'` or `method: "DELETE"`.
    - `vigil-pwa/src/pages/WorkOrdersPage.tsx` contains `ReviewWorkOrderModal` and `reviewingWorkOrder` state.
    - `vigil-pwa/src/components/ReviewWorkOrderModal.test.tsx` contains at least 7 `it(` or `test(` declarations.
    - Test file contains assertions on `onCommit` and `onDiscard` callbacks.
    - `cd vigil-pwa && npm test 2>&1 | tee /tmp/pwa-test-task3.log` exits 0 (full PWA suite).
    - `cd vigil-pwa && npm run build 2>&1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-pwa && grep -q 'export default function ReviewWorkOrderModal' src/components/ReviewWorkOrderModal.tsx && grep -q '<details' src/components/ReviewWorkOrderModal.tsx && grep -q 'window.confirm' src/components/ReviewWorkOrderModal.tsx && grep -q 'onCommit' src/components/ReviewWorkOrderModal.tsx && grep -q 'onDiscard' src/components/ReviewWorkOrderModal.tsx && grep -q 'pending_review' src/components/WorkOrderRow.tsx && grep -q 'Review draft' src/components/WorkOrderRow.tsx && grep -q 'onReview' src/components/WorkOrderRow.tsx && grep -q 'commitDraft' src/hooks/useWorkOrders.ts && grep -q 'discardDraft' src/hooks/useWorkOrders.ts && grep -q '/commit' src/hooks/useWorkOrders.ts && grep -qE "method: ['\"]DELETE['\"]" src/hooks/useWorkOrders.ts && grep -q 'ReviewWorkOrderModal' src/pages/WorkOrdersPage.tsx && grep -q 'reviewingWorkOrder' src/pages/WorkOrdersPage.tsx && [ "$(grep -v '^#' src/components/ReviewWorkOrderModal.test.tsx | grep -v '//' | grep -cE '(it|test)\(')" -ge "7" ] && grep -q 'onCommit' src/components/ReviewWorkOrderModal.test.tsx && grep -q 'onDiscard' src/components/ReviewWorkOrderModal.test.tsx && npm test 2>&1 | tee /tmp/pwa-test-task3.log | tail -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ReviewWorkOrderModal + WorkOrderRow pending_review branch + useWorkOrders commit/discard mutations + WorkOrdersPage hosting all wired; 7+ tests pass; PWA builds.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA client → vigil-core /v1/work-orders/* | bearerAuth via existing session cookie; sanitizer destructure is the mass-assignment defense |
| PWA modal form → useWorkOrders hook | client-side state; no network until submit |
| operator-typed input → SanitizedWorkOrder pipeline | sanitizer accepts via the 12-field whitelist from plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-23 | Spoofing | userId injection via path param | mitigate | Commit + Delete routes both read userId via `c.get("userId")` and scope the SELECT/UPDATE/DELETE by `(caseNumber, userId)`. Path param `caseNumber` is the WHAT, not the WHO. |
| T-129.1-24 | Tampering | mass-assignment via commit body | mitigate | Commit handler uses the 12-field editable whitelist (same `String(wo.X ?? null)` pattern as the sync sanitizer); never spreads `...edits`. Plan 01's "12 known fields" comment is the auditable invariant. |
| T-129.1-25 | Tampering | state transition bypass | mitigate | Commit handler enforces source state === "pending_review"; 409 if not. Prevents reopening committed/closed work orders via the commit endpoint. |
| T-129.1-26 | Information Disclosure | extras JSON in review modal | accept | Same data already persisted to `notes` column by plan 03 endpoint; rendered to the row's owner only (userId-scoped fetch); no cross-user leak. |
| T-129.1-27 | Repudiation | hard-delete via Discard | mitigate-via-UX | window.confirm prompt forces operator to acknowledge; no soft-delete because draft rows are by-definition pre-committed. Risk accepted: discarded drafts cannot be recovered. |
| T-129.1-28 | Tampering | case_number format-bypass in manual-create | mitigate | Form-level regex `/^[A-Z0-9]+$/` enforced before POST; server has no regex check (RESEARCH §949) so client validation is the only gate. Acceptable because server still scopes by userId and sanitizer coerces the field to String. |
| T-129.1-SC | Tampering | npm installs | n/a | NO new npm deps in this plan (PWA already has @testing-library/react + vitest; vigil-core already has hono + drizzle). |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "Create button opens modal, submits, row appears" → Task 2 acceptance + test assertions on onCreate callback + UAT runbook (plan 06).
2. Truth: "case_number regex rejected at form-level" → Task 2 test case (lowercase rejected, uppercase+digits accepted).
3. Truth: "pending_review row shows Review button, not status-cycle" → Task 3 acceptance (WorkOrderRow conditional render) + Task 3 test.
4. Truth: "Commit calls /:caseNumber/commit; Discard calls DELETE /:caseNumber" → Task 1 server route + Task 3 hook + test assertions.
5. Truth: "useWorkOrders returns 3 new mutations with setFetchTick" → Task 2 + Task 3 grep + return-object assertions.
6. Truth: "New routes mounted in factory, userId-scoped" → Task 1 acceptance.

**Phase-level checks:**

- `cd vigil-core && npm test && npm run build` green.
- `cd vigil-pwa && npm test && npm run build` green.
- Manual UAT (plan 06): create row via PWA → confirm in list. Screenshot via daemon → see pending_review badge → click Review → edit → Commit → confirm state="open". Screenshot another → click Discard → confirm row gone.

</verification>

<success_criteria>

- 2 new vigil-core routes (POST /:caseNumber/commit + DELETE /:caseNumber) in `createWorkOrdersRoute()` factory; userId-scoped; 404/409 enforced; tested.
- CreateWorkOrderModal exposes all 11 editable fields; case_number form-validated.
- ReviewWorkOrderModal pre-populates from workOrder prop; extras JSON collapsible; Commit + Discard with confirm.
- WorkOrderRow renders amber pending_review style + Review button instead of status-cycle when state=pending_review.
- useWorkOrders gains createWorkOrder + commitDraft + discardDraft (all setFetchTick).
- WorkOrdersPage hosts both modals; "+ Create work order" CTA visible.
- vigil-core test suite green (work-orders.test.ts +7 tests).
- vigil-pwa test suite green (2 new modal test files, 7+ tests each).
- TypeScript + Vite build green for both packages.
- No new npm deps.

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-05-SUMMARY.md` when done.
</output>
