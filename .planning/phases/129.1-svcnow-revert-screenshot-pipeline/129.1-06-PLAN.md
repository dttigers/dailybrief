---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 06
type: execute
wave: 4
depends_on:
  - 129.1-01
  - 129.1-02
  - 129.1-03
  - 129.1-04
  - 129.1-05
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md
autonomous: false
requirements:
  - SCAP-01
  - SCAP-02
  - SCAP-03
  - SCAP-04
  - SCAP-05
  - WO-MANUAL-01
  - WO-MANUAL-02
  - WO-MANUAL-03

must_haves:
  truths:
    - "REQUIREMENTS.md contains new sections for SCAP-01..05 + WO-MANUAL-01..03 with one-line behavioral descriptions per ID."
    - "REQUIREMENTS.md SVCNOW-01/02/03/05 SUPERSEDED markers point to Phase 129.1; SVCNOW-04 PRESERVED marker is unchanged."
    - "REQUIREMENTS.md Traceability table updated: new IDs mapped to 129.1; SVCNOW-* entries reference 129 → 129.1 supersession; coverage count incremented."
    - "129.1-UAT-RUNBOOK.md exists with operator-runnable end-to-end test sections covering: (a) screenshot pipeline e2e, (b) manual-create flow, (c) draft review commit+discard, (d) extension revert verification (Chrome + Safari on non-SN URL), (e) daemon control (status/tail/pause/resume), (f) edge cases (multi-file drop, daemon crash recovery, API key rotation)."
    - "Operator runs UAT runbook and records PASS/FAIL/BLOCKED per section in the runbook file."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated with SCAP-* + WO-MANUAL-* sections + SUPERSEDED markers + Traceability table"
      contains: "SCAP-01"
    - path: ".planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md"
      provides: "Operator hardware UAT runbook with checkbox sections"
      min_lines: 120
  key_links:
    - from: ".planning/REQUIREMENTS.md (new SCAP-* + WO-MANUAL-* sections)"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "ID → phase mapping rows"
      pattern: "SCAP-01.*129.1"
    - from: ".planning/REQUIREMENTS.md (SVCNOW SUPERSEDED markers)"
      to: ".planning/phases/129.1-svcnow-revert-screenshot-pipeline/"
      via: "phase-name reference in the supersession pointer"
      pattern: "129.1"
---

<objective>
Close the phase: (1) author SCAP-01..05 + WO-MANUAL-01..03 requirement IDs in `.planning/REQUIREMENTS.md` (with one-line behavioral descriptions sourced from RESEARCH §"Phase Requirements" and plans 01-05), (2) update the Traceability table mapping the new IDs to Phase 129.1 (and bump the coverage count), (3) finalize the SVCNOW-01/02/03/05 SUPERSEDED markers with the Phase 129.1 supersession pointer (SVCNOW-04 remains PRESERVED unchanged), and (4) author `129.1-UAT-RUNBOOK.md` covering end-to-end operator-runnable tests, then have the operator execute it and record PASS/FAIL/BLOCKED per section.

Purpose: Requirements traceability is part of GSD's contract — every requirement ID lives in REQUIREMENTS.md, gets mapped in the Traceability table, and gets verified by an operator. This plan is the closing tie-off that proves all of Phase 129.1's plans landed and the phase's stated success criteria are met.

Output: 1 REQUIREMENTS.md edit (additive — 8 new IDs + table rows + updated coverage count + SVCNOW supersession finalize), 1 new UAT runbook file, 1 operator UAT execution checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-02-SUMMARY.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-04-SUMMARY.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-05-SUMMARY.md

<interfaces>
<!-- Existing REQUIREMENTS.md structure (the section to add SCAP-* + WO-MANUAL-* into): -->
// Top: header + v3.9 Requirements heading.
// Body: requirement groups (Phase 0 Guardrails, Voice — G2 PCM capture, G2-ACTION, G2-REPLY, WATCH-ENRICH, SVCNOW, G2-LIFECYCLE, INSIGHTS-FRESH, CHAT-CTX, QUIET-AUTO, HUD-CLARITY).
// SVCNOW group at lines ~57-68 — already has SVCNOW-01..05 with SUPERSEDED markers pointing to 129.1.
//
// Insert a NEW group AFTER the SVCNOW group titled:
//   ### SCAP / WO-MANUAL — Screenshot pipeline + PWA manual-create (Phase 129.1 replacement for SVCNOW)
// With the 8 new IDs.

<!-- Traceability table at lines ~149-203 — append 8 rows for the new IDs (Phase = 129.1, Status = Pending or Complete depending on plan-execution state at the time of authoring): -->
| SCAP-01 | 129.1 | Pending |
| SCAP-02 | 129.1 | Pending |
... etc.

<!-- Coverage block at line ~205-209: -->
// Bump v3.9 total from 53 to 61 (8 new IDs); update Phase distribution: 129.1 (8).
// Keep SVCNOW-04 mapped to Phase 129 Complete; SVCNOW-01/02/03/05 stay mapped to "129 → 129.1 Superseded".

<!-- New requirement descriptions (sourced from RESEARCH §88-99 + plans 01-05): -->
SCAP-01: POST /v1/captures/screenshot accepts base64 PNG/JPG + mediaType + clientCaptureId; bearerAuth; per-user spend tracking; SVCNOW-04 dedup; writes pending_review row; returns {workOrderId, duplicate, extractedFields, extraExtractedFields}.
SCAP-02: Anthropic vision extraction returns JSON with required {case_number, short_description, location, department, maintenance_problem} + optional extras (13 fields); validated via parseAIJson; 422 on empty case_number.
SCAP-03: macOS LaunchAgent watches ~/vigil-captures/ via chokidar awaitWriteFinish; on new PNG/JPG, POSTs to endpoint with Keychain-sourced API key; moves to processed/<ts>-<workOrderId>.<ext> or failed/<ts>-<filename> + .err sidecar.
SCAP-04: vigil-capture CLI provides install (idempotent bootout-then-bootstrap, chmod 700 ~/vigil-captures/{processed,failed}/), uninstall, status, tail subcommands. README documents Keychain key rotation.
SCAP-05: PWA WorkOrderRow renders amber pending_review badge + Review draft button (replaces status-cycle); ReviewWorkOrderModal shows pre-populated form + extras JSON collapsible + Commit/Discard buttons.
WO-MANUAL-01: PWA WorkOrdersPage "+ Create work order" CTA opens CreateWorkOrderModal with all 11 editable columns; case_number form-validated /^[A-Z0-9]+$/; submit POSTs to /v1/work-orders/sync state="open" + UUID clientCaptureId.
WO-MANUAL-02: useWorkOrders hook gains createWorkOrder + commitDraft + discardDraft useCallback mutations; each does setFetchTick(n+1) on success; commitDraft → POST /:caseNumber/commit; discardDraft → DELETE /:caseNumber.
WO-MANUAL-03: vigil-core sanitizer (work-orders.ts SanitizedWorkOrder interface, destructure, dbInsertOrGet, dbUpsertLegacy) extended with maintenance_problem + department; migration 0022 adds nullable text columns; "12 known fields" comment auditability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SCAP-* + WO-MANUAL-* IDs + finalize SVCNOW supersession markers + update Traceability in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (FULL FILE — confirm exact line numbers for SVCNOW group ~57-68, Traceability table ~149-203, and Coverage block ~205-209)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md (confirm what landed for WO-MANUAL-03 specifically)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md (confirm department mapping decision so SCAP-02 description is precise)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-04-SUMMARY.md (confirm daemon details for SCAP-03/04)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-05-SUMMARY.md (confirm PWA wiring for SCAP-05 + WO-MANUAL-01/02)
  </read_first>
  <behavior>
    - REQUIREMENTS.md gains a new H3 section between the SVCNOW section (~line 57-68) and the G2-LIFECYCLE section (~line 70+). Section title: `### SCAP / WO-MANUAL — Screenshot pipeline + PWA manual-create (Phase 129.1 replacement for SVCNOW)`.
    - The new section has 8 list items (one per ID) following the existing format `- [ ] **<ID>** (Phase 129.1): <one-line description>` or `- [x]` if confirmed Complete via plan SUMMARYs.
    - The SVCNOW group's existing SUPERSEDED markers stay; refresh the supersession-pointer wording to read "→ replaced by SCAP-* + WO-MANUAL-* in Phase 129.1" rather than the existing generic "SUPERSEDED by Phase 129.1's screenshot-pipeline" preamble. SVCNOW-04 PRESERVED note unchanged.
    - The Traceability table at the bottom adds 8 new rows (one per SCAP-* + WO-MANUAL-*) mapping to Phase 129.1, with Status set to "Complete" if the corresponding plan's SUMMARY exists, else "Pending".
    - The Coverage block updates: v3.9 total 53 → 61; Phase distribution adds `129.1 (8)`.
    - The header "Last updated" date bumps to today (2026-05-17).
  </behavior>
  <action>
Open `.planning/REQUIREMENTS.md`. Make four edits:

1. **Insert new section** between the SVCNOW group (ends ~line 68) and the G2-LIFECYCLE group (~line 70):

   ### SCAP / WO-MANUAL — Screenshot pipeline + PWA manual-create (Phase 129.1 replacement for SVCNOW)

   > **Origin:** Phase 129 PARTIAL-COMPLETE 2026-05-16; mid-Session-2 UAT pivoted from the SVCNOW assisted-capture popup to a screenshot-pipeline (operator-only) + PWA manual-create (all users) direction. SVCNOW-01/02/03/05 reverted in Phase 129.1; SVCNOW-04 dedup primitive preserved as mechanism-agnostic.

   - [ ] **SCAP-01** (Phase 129.1): `POST /v1/captures/screenshot` endpoint with bearerAuth + per-user spend tracking; accepts base64 PNG/JPG + mediaType + UUID v4 clientCaptureId; SVCNOW-04 dedup via SELECT-by-(userId, clientCaptureId) short-circuit; writes `work_orders` row in `state: 'pending_review'`; case_number PK collision via onConflictDoUpdate (latest wins); returns `{workOrderId, duplicate, extractedFields, extraExtractedFields}`.
   - [ ] **SCAP-02**: Anthropic Claude Sonnet vision extraction via `callClaudeMultimodal(userId)`; prompt produces JSON `{required: {case_number, short_description, location, department, maintenance_problem}, extras: {service?, assignment_group?, assigned_to?, priority?, trade?, equipment?, contact?, opened?, time_worked?, resolution_code?, resolution_notes?, after_hours?, maintenance_vendor?}}`; `parseAIJson` handles ```json fences; 422 on empty case_number.
   - [ ] **SCAP-03**: macOS LaunchAgent watches `~/vigil-captures/` via `chokidar` with `awaitWriteFinish: {stabilityThreshold: 500, pollInterval: 100}`; on new PNG/JPG, base64-encodes, POSTs to endpoint with Keychain-sourced API key, moves to `processed/<iso-ts>-<workOrderId>.<ext>` on success or `failed/<ts>-<filename>` + `.err` sidecar on failure; startup orphan-scan recovers from prior-run crashes.
   - [ ] **SCAP-04**: `vigil-capture` CLI provides `install` (idempotent bootout-then-bootstrap, chmod 700 captures dir), `uninstall`, `status` (launchctl print), `tail` subcommands; README documents Keychain key rotation + pause/resume + edge cases.
   - [ ] **SCAP-05**: PWA `WorkOrderRow` renders amber `pending_review` badge + "Review draft" button (replaces status-cycle for pending_review rows); `ReviewWorkOrderModal` pre-populates from workOrder, shows `extras` JSON in collapsible `<details>`, Commit transitions state to `open` via `POST /v1/work-orders/:caseNumber/commit`, Discard hard-deletes via `DELETE /v1/work-orders/:caseNumber` after window.confirm.
   - [ ] **WO-MANUAL-01**: PWA `WorkOrdersPage` shows "+ Create work order" CTA next to filter tabs; `CreateWorkOrderModal` exposes all 11 editable `work_orders` columns (case_number, store, short_description, trade, location, equipment, priority, contact, notes, **maintenance_problem**, **department**); case_number form-validated `/^[A-Z0-9]+$/`; submit POSTs to `/v1/work-orders/sync` with `state: "open"` + `crypto.randomUUID()` clientCaptureId.
   - [ ] **WO-MANUAL-02**: `useWorkOrders` hook gains `createWorkOrder(input)` / `commitDraft(caseNumber, edits)` / `discardDraft(caseNumber)` useCallback mutations; each refreshes the list via `setFetchTick(n+1)` on success; uses existing `vigilFetch` wrapper (no Tanstack Query).
   - [ ] **WO-MANUAL-03**: vigil-core `SanitizedWorkOrder` interface, sanitizer destructure, `dbInsertOrGet`, and `dbUpsertLegacy` all carry `maintenanceProblem` + `department` fields (the "12 known fields" auditability comment maintained); Drizzle migration 0022 adds both as nullable `text` columns; schema.ts workOrders block declares both consistently.

   Mark `- [x]` instead of `- [ ]` for any ID whose plan SUMMARY confirms landed. (Plan 01 → WO-MANUAL-03; Plan 03 → SCAP-01/02; Plan 04 → SCAP-03/04; Plan 05 → SCAP-05 + WO-MANUAL-01/02.)

2. **Refresh SVCNOW supersession-pointer wording** in the existing SVCNOW group (~lines 57-68):
   - Update the blockquote at ~lines 59-62 to add a final sentence: "Replacement requirement IDs: SCAP-01..05 (screenshot pipeline) + WO-MANUAL-01..03 (PWA manual-create). See section below."
   - SVCNOW-01/02/03/05 entries remain marked `[~]` with `(SUPERSEDED)`; SVCNOW-04 entry stays `[x] **SVCNOW-04** (PRESERVED)`.

3. **Append 8 rows to the Traceability table** (~lines 149-203). After the SVCNOW-05 row, insert:

   | SCAP-01 | 129.1 | Complete-or-Pending |
   | SCAP-02 | 129.1 | Complete-or-Pending |
   | SCAP-03 | 129.1 | Complete-or-Pending |
   | SCAP-04 | 129.1 | Complete-or-Pending |
   | SCAP-05 | 129.1 | Complete-or-Pending |
   | WO-MANUAL-01 | 129.1 | Complete-or-Pending |
   | WO-MANUAL-02 | 129.1 | Complete-or-Pending |
   | WO-MANUAL-03 | 129.1 | Complete-or-Pending |

   Use "Complete" for IDs whose plan SUMMARY exists, "Pending" otherwise.

4. **Update Coverage block** (~lines 205-209):
   - `v3.9 requirements: 53 total` → `v3.9 requirements: 61 total` (+8).
   - `Mapped to phases: 53 ✓` → `Mapped to phases: 61 ✓`.
   - `Phase distribution: 127 (4) · ... · 129 (8) · ...` → add `· 129.1 (8)` after `129 (8)`.

5. **Bump the footer line** at the very bottom from `Last updated: 2026-05-12` to `Last updated: 2026-05-17 — Phase 129.1 SCAP-* + WO-MANUAL-* IDs authored; SVCNOW supersession finalized`.

After edits, re-read the file and verify it parses (no broken markdown tables, no missing closing markers).
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the literal string `### SCAP / WO-MANUAL` (new section header).
    - Same file contains 8 ID strings: SCAP-01, SCAP-02, SCAP-03, SCAP-04, SCAP-05, WO-MANUAL-01, WO-MANUAL-02, WO-MANUAL-03 (grep each).
    - Each ID appears at least TWICE: once in the requirements section (with description), once in the Traceability table (mapped to 129.1).
    - Same file Coverage block contains `v3.9 requirements: 61 total`.
    - Same file Coverage block Phase distribution mentions `129.1 (8)`.
    - SVCNOW-04 entry still contains `(PRESERVED)`.
    - SVCNOW-01/02/03/05 entries still contain `(SUPERSEDED)`.
    - SVCNOW group's blockquote mentions "Replacement requirement IDs: SCAP-01..05 + WO-MANUAL-01..03".
    - Footer "Last updated" date is `2026-05-17`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && grep -q '### SCAP / WO-MANUAL' .planning/REQUIREMENTS.md && for id in SCAP-01 SCAP-02 SCAP-03 SCAP-04 SCAP-05 WO-MANUAL-01 WO-MANUAL-02 WO-MANUAL-03; do [ "$(grep -c "$id" .planning/REQUIREMENTS.md)" -ge "2" ] || { echo "MISSING duplicate: $id"; exit 1; }; done && grep -q 'v3.9 requirements: 61 total' .planning/REQUIREMENTS.md && grep -q '129.1 (8)' .planning/REQUIREMENTS.md && grep -q 'SVCNOW-04.*PRESERVED' .planning/REQUIREMENTS.md && grep -qE 'SVCNOW-0[1235].*SUPERSEDED' .planning/REQUIREMENTS.md && grep -q 'Replacement requirement IDs' .planning/REQUIREMENTS.md && grep -q 'Last updated: 2026-05-17' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>8 new IDs authored with descriptions + traceability rows; SVCNOW supersession finalized; coverage count 53→61; footer date bumped.</done>
</task>

<task type="auto">
  <name>Task 2: Author 129.1-UAT-RUNBOOK.md covering 6 e2e operator scenarios</name>
  <files>.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md if exists (analog for runbook structure)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-04-PLAN.md Task 5 (the operator install + smoke test instructions — runbook should re-use these verbatim)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-02-PLAN.md Task 3 (extension revert smoke test)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Edge-case stress tests" (lines 689-695)
  </read_first>
  <behavior>
    - File contains 6 numbered scenario sections, each with: Setup / Steps / Expected / PASS-FAIL-BLOCKED checkbox / Notes-on-failure.
    - All curl/launchctl commands are copy-pasteable (absolute paths or HOME-relative).
    - Header includes phase + date + operator name placeholder.
    - Last section is a roll-up dispositions table summarizing the 6 scenarios.
  </behavior>
  <action>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` with this structure (write the file completely; do NOT use heredoc):

  # Phase 129.1 UAT Runbook — SVCNOW Revert + Screenshot Pipeline + PWA Manual-Create

  **Phase:** 129.1-svcnow-revert-screenshot-pipeline
  **Operator:** _______________________
  **Date executed:** _______________________
  **Vigil API base (prod):** https://api.vigilhub.io

  This runbook covers the 6 end-to-end operator-runnable tests for Phase 129.1. Each section ends with a disposition (PASS / FAIL / BLOCKED) and a notes block.

  ---

  ## 1. Screenshot pipeline end-to-end

  **What this verifies:** SCAP-01 + SCAP-02 + SCAP-03 + SCAP-04 + SCAP-05 — operator screenshots a Polaris case → daemon uploads → vigil-core extracts → PWA shows pending_review draft → operator commits.

  **Setup:**
  - Confirm Vigil API key is in Keychain: `security find-generic-password -s vigil-api-key -w`
  - Confirm daemon is loaded: `node /home/morrillboss/dev/dailybrief/scripts/vigil-capture/dist/cli.js status`
  - Confirm extension reverted (smoke test from plan 02 Task 3 passed).
  - Confirm migration 0022 applied to prod (via Railway logs or psql).

  **Steps:**
  1. Open a real Polaris case (e.g. CS0363817) in the browser.
  2. Cmd+Shift+4 → screenshot the case page → save to `~/vigil-captures/`.
  3. Wait ~5-10 seconds.
  4. Run: `tail -5 ~/Library/Logs/vigil-capture.log`. Expected: `[OK] <filename> → CS<7-digits>`.
  5. Verify file moved: `ls ~/vigil-captures/processed/ | tail -1`. Expected: file with name `<iso-ts>-CS<7-digits>.png`.
  6. Open PWA `/work-orders` page.
  7. Confirm a new row with the extracted case_number appears with an amber `Review draft` button.
  8. Click `Review draft`. Confirm modal opens with: case_number, short_description, location, department, maintenance_problem all pre-populated; collapsible `extras` JSON shows the 13 optional fields.
  9. Make a minor edit to short_description (e.g. add " [verified]").
  10. Click `Commit`. Confirm row's state changes to `open` (status pill turns yellow); the edit persisted.

  **Expected:** Each step completes without error; final work order is in `open` state with operator's edit applied.

  **Disposition:**  PASS / FAIL / BLOCKED

  **Notes (on FAIL or BLOCKED):**
  _________________________________________________

  ---

  ## 2. Manual-create flow (non-Polaris work order)

  **What this verifies:** WO-MANUAL-01 + WO-MANUAL-02 + WO-MANUAL-03.

  **Setup:** PWA logged in as any user (operator or non-operator).

  **Steps:**
  1. Navigate to `/work-orders`.
  2. Click `+ Create work order` button (top-right of filter row).
  3. Modal opens. Confirm all 11 fields are visible.
  4. Type a lowercase case_number (`cs0001`). Confirm inline error "Case number must contain only A-Z and 0-9" appears and Submit is disabled.
  5. Change to `MANUAL-001`. Confirm error clears and Submit is enabled.
  6. Fill short_description ("Test manual create"), priority ("Medium"), department ("Test-Dept"), maintenance_problem ("Other").
  7. Click Submit.
  8. Modal closes; new row `MANUAL-001` appears in the list with state `open`.

  **Expected:** Form validation works; submission persists; row visible without manual refresh.

  **Disposition:** PASS / FAIL / BLOCKED

  **Notes:** _________________________________________________

  ---

  ## 3. Draft discard flow

  **What this verifies:** SCAP-05 + WO-MANUAL-02 discardDraft.

  **Setup:** at least one pending_review row in the list (from scenario 1, or screenshot another case).

  **Steps:**
  1. Click `Review draft` on a pending_review row.
  2. Click `Discard`. Confirm browser confirm dialog: "Discard this draft? This cannot be undone."
  3. Click OK.
  4. Modal closes; row disappears from the list.
  5. Try to re-fetch via curl to confirm hard-delete: `curl -H "Authorization: Bearer <key>" https://api.vigilhub.io/v1/work-orders/<caseNumber>` → expect 404.

  **Expected:** Row is hard-deleted; reappears NOWHERE (not even in archived).

  **Disposition:** PASS / FAIL / BLOCKED

  **Notes:** _________________________________________________

  ---

  ## 4. Extension revert verification

  **What this verifies:** SVCNOW-01/02/03/05 reverts.

  **Setup:** Chrome + Safari extensions installed (from plan 02 Task 3).

  **Steps:**
  1. Navigate to `https://example.com` (NOT a service-now.com URL).
  2. Click the Vigil Chrome extension icon.
  3. Confirm: popup opens with textarea + Send button (NOT a SVCNOW case# form).
  4. Type "test thought from reverted extension" → click Send.
  5. Open PWA dashboard. Confirm a new thought row with that content appears.
  6. Repeat steps 1-5 in Safari (use the Vigil toolbar icon).

  **Expected:** Both browsers show a generic capture popup; thoughts are captured via the Phase 84 endpoint; no SVCNOW UI anywhere.

  **Disposition:** PASS / FAIL / BLOCKED

  **Notes:** _________________________________________________

  ---

  ## 5. Daemon control (status / pause / resume)

  **What this verifies:** SCAP-04 CLI subcommands + LaunchAgent KeepAlive + orphan-scan.

  **Steps:**
  1. Run `node scripts/vigil-capture/dist/cli.js status`. Expected: launchctl print output showing `running` or `waiting`.
  2. Pause: `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
  3. Drop a test image into `~/vigil-captures/test.png`.
  4. Wait 10 seconds. Confirm file stays in root (daemon not running).
  5. Resume: `launchctl load ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
  6. Wait 5-10 seconds. Confirm `test.png` is processed (moved to processed/ or failed/) — orphan-scan picked it up.
  7. Tail the log: `node scripts/vigil-capture/dist/cli.js tail` (Ctrl+C to exit). Confirm log shows the orphan-scan + process event.

  **Expected:** Pause stops processing; resume picks up the orphaned file; logs are readable.

  **Disposition:** PASS / FAIL / BLOCKED

  **Notes:** _________________________________________________

  ---

  ## 6. Edge cases (multi-file drop, API key rotation)

  **What this verifies:** SCAP-03 sequential processing + SCAP-04 key rotation procedure.

  **Steps:**

  **6a. Multi-file drop:**
  1. Drop 3 PNG files into `~/vigil-captures/` within 1 second (e.g. `cp existing-png a.png; cp existing-png b.png; cp existing-png c.png`).
  2. Tail the log. Confirm: 3 sequential `[OK]` or `[FAIL]` lines (not interleaved chaos).
  3. Confirm all 3 files are in either processed/ or failed/ (none remaining in root).

  **6b. API key rotation:**
  1. Pause the daemon: `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`.
  2. Rotate Keychain key: `security add-generic-password -s vigil-api-key -a $USER -w <new-key> -U` (set to a deliberately bad key like `wrong-key-test` to verify it actually rotates).
  3. Resume: `launchctl load ...`.
  4. Drop a test image. Confirm log shows `[FAIL] ... → 401` (Authorization failed).
  5. Rotate back to the real key: `security add-generic-password -s vigil-api-key -a $USER -w <real-key> -U`.
  6. Drop another test image. Confirm `[OK]` (no daemon restart needed — key is read per-call).

  **Disposition (6a):** PASS / FAIL / BLOCKED. **Notes:** ____________

  **Disposition (6b):** PASS / FAIL / BLOCKED. **Notes:** ____________

  ---

  ## Roll-up Dispositions

  | Scenario | Disposition | Notes |
  |----------|-------------|-------|
  | 1. Screenshot e2e | ___ | |
  | 2. Manual-create | ___ | |
  | 3. Draft discard | ___ | |
  | 4. Extension revert | ___ | |
  | 5. Daemon control | ___ | |
  | 6a. Multi-file drop | ___ | |
  | 6b. API key rotation | ___ | |

  **Overall phase outcome:** COMPLETE / PARTIAL / BLOCKED.

  ---

  *Operator records gap entries in `.planning/STATE.md` for any FAIL or BLOCKED dispositions; planner spawns Phase 129.2 with `/gsd:plan-phase 129.1 --gaps` if needed.*
  </action>
  <acceptance_criteria>
    - `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` exists.
    - File contains all 6 scenario numbers (`## 1.`, `## 2.`, `## 3.`, `## 4.`, `## 5.`, `## 6.`).
    - File contains the Roll-up Dispositions table.
    - File contains at least one mention of each requirement ID: SCAP-01, SCAP-02, SCAP-03, SCAP-04, SCAP-05, WO-MANUAL-01, WO-MANUAL-02, WO-MANUAL-03.
    - File contains `PASS / FAIL / BLOCKED` disposition markers.
    - File contains both `launchctl unload` and `launchctl load` (pause/resume commands).
    - File line count >= 120.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && [ -f .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md ] && for marker in '## 1\.' '## 2\.' '## 3\.' '## 4\.' '## 5\.' '## 6\.' 'Roll-up Dispositions' 'SCAP-01' 'SCAP-02' 'SCAP-03' 'SCAP-04' 'SCAP-05' 'WO-MANUAL-01' 'WO-MANUAL-02' 'WO-MANUAL-03' 'PASS / FAIL / BLOCKED' 'launchctl unload' 'launchctl load'; do grep -qE "$marker" .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md || { echo "MISSING: $marker"; exit 1; }; done && [ "$(wc -l < .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md)" -ge "120" ]</automated>
  </verify>
  <done>UAT runbook with 6 scenarios + dispositions table + all 8 requirement-ID references + pause/resume commands; ≥120 lines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator executes UAT runbook end-to-end and records dispositions</name>
  <what-built>
    Task 2 authored the runbook. This checkpoint is the operator actually doing the 6 scenarios on their hardware, filling in PASS/FAIL/BLOCKED per scenario in the runbook file itself, and committing the resulting marked-up runbook back to git.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` in your editor.
    2. Execute scenarios 1-6 in order on your Mac.
    3. After each scenario, fill in the disposition (PASS / FAIL / BLOCKED) and any notes.
    4. Fill in the Roll-up Dispositions table at the bottom.
    5. Set the "Overall phase outcome" line to COMPLETE / PARTIAL / BLOCKED based on the roll-up.
    6. If any scenario is FAIL or BLOCKED:
       - Note the failure in STATE.md's "Open Gaps" or equivalent section.
       - Consider running `/gsd:plan-phase 129.1 --gaps` to spawn gap-closure plans.
    7. Save and commit the runbook.
  </how-to-verify>
  <resume-signal>Type "approved — UAT runbook executed, dispositions filled" if all 6 scenarios are PASS or DEFERRED-NOT-BLOCKING. If any FAIL or BLOCKED, describe which and whether a gap-closure phase is needed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → REQUIREMENTS.md (docs edit) | trusted; operator owns the requirements doc |
| operator → UAT runbook execution | trusted; the runbook IS the operator's verification surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-29 | Tampering | REQUIREMENTS.md coverage count drift | mitigate | Task 1 acceptance criteria grep-validates the coverage count update (53→61); Traceability table is verified to contain all 8 new IDs. |
| T-129.1-30 | Repudiation | UAT runbook executed but dispositions not recorded | mitigate-via-checkpoint | Task 3 is a blocking-human checkpoint that requires the operator to fill in dispositions AND commit the marked-up runbook back to git. |
| T-129.1-31 | Information Disclosure | UAT runbook test screenshots | accept | Runbook instructs operator to use real Polaris cases; screenshots stay in operator's `~/vigil-captures/processed/` (chmod 700) and are not pushed to git (gitignored or in operator's HOME, not the repo). |
| T-129.1-SC | Tampering | npm installs | n/a | NO new package installs in this plan. |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "REQUIREMENTS.md has SCAP-* + WO-MANUAL-* sections" → Task 1 acceptance grep.
2. Truth: "SVCNOW SUPERSEDED markers point to 129.1" → Task 1 acceptance grep + visual review.
3. Truth: "Traceability table updated; coverage 53→61" → Task 1 acceptance grep.
4. Truth: "UAT runbook with 6 scenarios + edge cases" → Task 2 acceptance grep.
5. Truth: "Operator executes runbook + records dispositions" → Task 3 checkpoint.

**Phase-level checks:**

- After Task 1: `grep -c 'SCAP-\|WO-MANUAL-' .planning/REQUIREMENTS.md` ≥ 16 (each ID appears at least twice — section + traceability).
- After Task 2: `wc -l .planning/phases/.../129.1-UAT-RUNBOOK.md` ≥ 120.
- After Task 3: Operator-recorded dispositions in the runbook file; commit message references Phase 129.1 outcome.

</verification>

<success_criteria>

- REQUIREMENTS.md gains 8 new requirement IDs (SCAP-01..05 + WO-MANUAL-01..03) with one-line behavioral descriptions.
- SVCNOW-04 PRESERVED marker unchanged.
- SVCNOW-01/02/03/05 SUPERSEDED markers finalized with the "→ replaced by SCAP-* + WO-MANUAL-*" supersession-pointer.
- Traceability table contains 8 new rows mapping each ID to Phase 129.1.
- Coverage count bumped 53 → 61; Phase distribution mentions 129.1 (8).
- 129.1-UAT-RUNBOOK.md exists with 6 scenarios + edge cases + roll-up table.
- Operator executes the runbook on hardware and records PASS/FAIL/BLOCKED per scenario.
- Marked-up runbook committed back to git; phase outcome (COMPLETE / PARTIAL / BLOCKED) recorded.

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-06-SUMMARY.md` when done. SUMMARY must include the final UAT roll-up dispositions table copied from the runbook + the overall phase outcome decision.
</output>
