---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-extension/manifest.json
  - vigil-extension/popup.html
  - vigil-extension/popup.css
  - vigil-extension/popup.js
  - vigil-extension/background.js
  - vigil-extension/content-script.js
  - vigil-extension/popup-helpers.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.html
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.css
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/background.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js
  - vigil-extension-tests/parity.test.ts
  - vigil-extension-tests/popup-helpers.test.ts
  - package.json
autonomous: false
requirements:
  # Informal revert-action labels; formal SUPERSEDED markers for
  # SVCNOW-01/02/03/05 are authored in plan 129.1-06 (REQUIREMENTS.md).
  - SVCNOW-01
  - SVCNOW-02
  - SVCNOW-03
  - SVCNOW-05

must_haves:
  truths:
    - "Chrome extension popup is generic thought-capture (textarea + Send) and works on any URL (no service-now.com host restriction)."
    - "Safari extension popup is byte-identical to Chrome popup.html/css/js (Phase 84-equivalent state, lock-step preserved)."
    - "`npm run test:extension` passes with the relaxed parity test (6 assertions: 2 manifest set-equality + 3 popup byte-equality + 1 sanity-check that SVCNOW-specific files are absent)."
    - "popup-helpers.test.ts is deleted (function under test no longer exists)."
    - "The 3 SVCNOW-only files (background.js, content-script.js, popup-helpers.js) are absent from BOTH extension directories AND from the Safari Xcode 'Copy Bundle Resources' build phase."
  artifacts:
    - path: "vigil-extension/manifest.json"
      provides: "Phase 84 baseline manifest (v1.0.0, no SN host, no MV3 background, no content_scripts, no tabs perm)"
      contains: "\"version\": \"1.0.0\""
    - path: "vigil-extension/popup.html"
      provides: "Phase 84 popup with setup-view + capture-view markup"
      contains: "id=\"setup-view\""
    - path: "vigil-extension-tests/parity.test.ts"
      provides: "Relaxed parity test — 6 assertions matching Phase 84 surface"
      min_lines: 40
  key_links:
    - from: "vigil-extension/popup.html"
      to: "vigil-extension/popup.js"
      via: "id selectors (setup-view, capture-view, textarea, Send button)"
      pattern: "id=\"capture-view\""
    - from: "Chrome popup.{html,css,js}"
      to: "Safari Resources/popup.{html,css,js}"
      via: "byte-equality enforced by parity.test.ts"
      pattern: "assert.equal\\(chromePopup"
---

<objective>
Revert Chrome + Safari extensions from the Phase 129 SVCNOW assisted-capture popup state to the Phase 84 generic thought-capture baseline. Three SVCNOW-only files (background.js, content-script.js, popup-helpers.js) get deleted on both sides. The four shared files (manifest.json, popup.html, popup.css, popup.js) get reset via `git checkout` to their pre-SVCNOW state. Lock-step parity is preserved (Chrome ↔ Safari byte-equal). The 11-assertion `parity.test.ts` is rewritten to 6 assertions matching the new (smaller) surface; `popup-helpers.test.ts` is deleted; root `package.json` `test:extension` script is updated. A blocking operator checkpoint is included for the Safari Xcode "Copy Bundle Resources" step that cannot be automated.

Purpose: SVCNOW-01/02/03/05 are SUPERSEDED by Phase 129.1 (per CONTEXT.md DECISION-B). Extensions revert to a generic popup that works on any URL; the new screenshot pipeline (plan 03) + PWA manual-create (plan 05) replace the SVCNOW capture path.

Output: file-level reset of 4 files per side via `git checkout`, deletion of 3 files per side, parity test rewrite, popup-helpers.test.ts deletion, package.json script update, one blocking checkpoint for the Xcode build phase update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md
@vigil-extension/manifest.json
@vigil-extension/popup.html
@vigil-extension/popup.css
@vigil-extension/popup.js
@vigil-extension-tests/parity.test.ts
@vigil-extension-tests/popup-helpers.test.ts
@package.json

<interfaces>
<!-- Phase 84 baseline commit: 1788d97 (Apr 14 2026) -->
<!-- Safari extension was created in Phase 114 (commit f201ed9, May 6 2026) — Safari files were not in Phase 84. -->
<!-- Use 1788d97 for Chrome file reset; use the commit immediately before ab99acd for Safari (that's the Phase 114-EXT-02 state — the verbatim Chrome→Safari port of Phase 84 popup). -->

Files to delete (BOTH sides):
  - background.js (created by commit 0d41d1e, plan 129-03)
  - content-script.js (created by commit 0d41d1e, plan 129-03; modified by 397fcfe, plan 129-07)
  - popup-helpers.js (created by commit cb52108, plan 129-03)

Files to reset via `git checkout <baseline-commit> --`:
  - Chrome side (baseline = 1788d97): manifest.json, popup.html, popup.css, popup.js
  - Safari side (baseline = the commit immediately BEFORE ab99acd that has the Safari files in their post-114-EXT-02, pre-129-05 state): popup.html, popup.css, popup.js, manifest.json

Phase 84 manifest target shape (verified by RESEARCH §272-286):
  "manifest_version": 3,
  "name": "Vigil Capture",
  "version": "1.0.0",
  "description": "Capture the current page to Vigil",
  "permissions": ["activeTab", "storage"],   // NO "tabs"
  "host_permissions": ["https://api.vigilhub.io/*"],   // NO service-now.com
  "action": { "default_popup": "popup.html", "default_icon": {...} },
  "icons": {...}
  // NO "background" key
  // NO "content_scripts" key

Current `vigil-extension-tests/parity.test.ts` has 11 assertions. Read it to confirm before editing.
Current `package.json` `test:extension` script reads:
  "test:extension": "tsx --test \"vigil-extension-tests/popup-helpers.test.ts\" \"vigil-extension-tests/parity.test.ts\""
Target after revert:
  "test:extension": "tsx --test \"vigil-extension-tests/parity.test.ts\""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify exact baseline commits for Chrome + Safari, then reset files + delete SVCNOW-only files</name>
  <files>vigil-extension/manifest.json, vigil-extension/popup.html, vigil-extension/popup.css, vigil-extension/popup.js, vigil-extension/background.js (DELETE), vigil-extension/content-script.js (DELETE), vigil-extension/popup-helpers.js (DELETE), vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json, vigil-safari-extension/Vigil Capture Extension/Resources/popup.html, vigil-safari-extension/Vigil Capture Extension/Resources/popup.css, vigil-safari-extension/Vigil Capture Extension/Resources/popup.js, vigil-safari-extension/Vigil Capture Extension/Resources/background.js (DELETE), vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js (DELETE), vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js (DELETE)</files>
  <read_first>
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Extension Revert" — lines 224-310 (commit graph + revert strategy + manifest target)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"REVERT" table (lines 46-63)
    - vigil-extension/manifest.json (current SVCNOW state — confirm what gets removed)
    - vigil-extension/popup.html (current SVCNOW state — confirm svcnow-view + setup-view markup is present, then verify Phase 84 baseline has setup-view + capture-view)
    - Run `git log --oneline -- vigil-safari-extension/"Vigil Capture Extension"/Resources/popup.html` to find the commit immediately before `ab99acd` (the SVCNOW Safari lock-step port) — that's the Safari baseline for reset.
  </read_first>
  <action>
First, resolve the Safari baseline commit:

  cd /home/morrillboss/dev/dailybrief
  git log --oneline --all -- "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" | head -20

Find the commit hash IMMEDIATELY BEFORE `ab99acd` (the SVCNOW Safari lock-step port from plan 129-05). RESEARCH §262-266 names this as commit `f201ed9` (Phase 114 EXT-02 verbatim Chrome→Safari port of Phase 84 popup). Verify by reading the commit's popup.html — it must have `id="setup-view"` and `id="capture-view"` (NOT `id="svcnow-view"`). Call this `SAFARI_BASELINE`.

The Chrome baseline is the literal hash `1788d97` (Phase 84 baseline).

Then execute the reset (use absolute paths; quote the Safari path because it contains a space):

  # Chrome side — reset 4 files
  git checkout 1788d97 -- \
    vigil-extension/manifest.json \
    vigil-extension/popup.html \
    vigil-extension/popup.css \
    vigil-extension/popup.js

  # Chrome side — delete 3 SVCNOW-only files
  rm vigil-extension/background.js vigil-extension/content-script.js vigil-extension/popup-helpers.js

  # Safari side — reset 4 files (use SAFARI_BASELINE = f201ed9 if confirmed)
  git checkout f201ed9 -- \
    "vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json" \
    "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" \
    "vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" \
    "vigil-safari-extension/Vigil Capture Extension/Resources/popup.js"

  # Safari side — delete 3 SVCNOW-only files
  rm "vigil-safari-extension/Vigil Capture Extension/Resources/background.js" \
     "vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js" \
     "vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js"

If the `git log` confirms a different commit hash than `f201ed9` is the correct Safari baseline (e.g. if there are commits between Phase 114 and Phase 129-05 that touched Safari popup.html for non-SVCNOW reasons), use that hash instead and note it in the task SUMMARY.

After reset, confirm the produced files match Phase 84 shape:
  - `vigil-extension/manifest.json`: must contain `"version": "1.0.0"`, must NOT contain `service-now.com`, must NOT contain `"background"` or `"content_scripts"` keys.
  - `vigil-extension/popup.html`: must contain `id="setup-view"` and `id="capture-view"`, must NOT contain `id="svcnow-view"`.
  - Safari popup.{html,css,js} must be byte-equal to Chrome counterparts (verify with `diff`).

Do NOT delete the `vigil-extension-tests/` directory itself — that's preserved per DECISION-B (GAP-129-A).
  </action>
  <acceptance_criteria>
    - `vigil-extension/manifest.json` contains the literal string `"version": "1.0.0"`.
    - `vigil-extension/manifest.json` does NOT contain the literal string `service-now.com` (grep returns 0).
    - `vigil-extension/manifest.json` does NOT contain the keys `"background"` or `"content_scripts"` (jq query `.background, .content_scripts` returns `null` for both).
    - `vigil-extension/popup.html` contains `id="setup-view"` and `id="capture-view"`.
    - `vigil-extension/popup.html` does NOT contain `id="svcnow-view"`.
    - Files `vigil-extension/background.js`, `vigil-extension/content-script.js`, `vigil-extension/popup-helpers.js` are ABSENT (ls returns "No such file").
    - Files `vigil-safari-extension/Vigil Capture Extension/Resources/background.js`, `.../content-script.js`, `.../popup-helpers.js` are ABSENT.
    - `diff vigil-extension/popup.html "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html"` produces NO output (byte-equal).
    - Same byte-equal for popup.css and popup.js.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && grep -q '"version": "1.0.0"' vigil-extension/manifest.json && ! grep -q 'service-now.com' vigil-extension/manifest.json && jq -e '.background == null and .content_scripts == null' vigil-extension/manifest.json && grep -q 'id="setup-view"' vigil-extension/popup.html && grep -q 'id="capture-view"' vigil-extension/popup.html && ! grep -q 'id="svcnow-view"' vigil-extension/popup.html && [ ! -f vigil-extension/background.js ] && [ ! -f vigil-extension/content-script.js ] && [ ! -f vigil-extension/popup-helpers.js ] && [ ! -f "vigil-safari-extension/Vigil Capture Extension/Resources/background.js" ] && [ ! -f "vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js" ] && [ ! -f "vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js" ] && diff -q vigil-extension/popup.html "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html" && diff -q vigil-extension/popup.css "vigil-safari-extension/Vigil Capture Extension/Resources/popup.css" && diff -q vigil-extension/popup.js "vigil-safari-extension/Vigil Capture Extension/Resources/popup.js"</automated>
  </verify>
  <done>All 4 shared files reset to Phase 84-equivalent state; all 3 SVCNOW-only files deleted from both sides; Chrome ↔ Safari byte-equal for popup.{html,css,js}.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite parity.test.ts (relaxed), delete popup-helpers.test.ts, update package.json test:extension script</name>
  <files>vigil-extension-tests/parity.test.ts, vigil-extension-tests/popup-helpers.test.ts (DELETE), package.json</files>
  <read_first>
    - vigil-extension-tests/parity.test.ts (current 11-assertion shape — confirm exact line ranges to keep vs delete)
    - vigil-extension-tests/popup-helpers.test.ts (8 tests of extractCaseNumber — to be DELETED entirely)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"parity.test.ts updates" (lines 296-310)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-extension-tests/parity.test.ts REWRITE" (lines 619-675)
    - package.json (current `test:extension` script reference)
  </read_first>
  <behavior>
    - `parity.test.ts` has exactly 6 assertions after rewrite: 2 manifest set-equality (permissions + host_permissions) + 3 byte-equality (popup.html, popup.css, popup.js) + 1 sanity-check that the 3 SVCNOW-only files are ABSENT on BOTH sides.
    - No assertion references `popup-helpers.js`, `background.js`, `content-script.js`, `background.service_worker`, or `content_scripts[0]`.
    - Header comment names Phase 129.1 + the revert context.
    - `popup-helpers.test.ts` is deleted (function under test removed).
    - `package.json` `test:extension` script no longer references `popup-helpers.test.ts`.
    - `npm run test:extension` exits 0 with 6 passing tests.
  </behavior>
  <action>
Rewrite `vigil-extension-tests/parity.test.ts` from scratch. Keep ONLY:

1. The imports + dirname/resolve setup (lines 30-44 of current file).
2. The two manifest read+parse lines (lines 53-54).
3. The two manifest set-equality tests: permissions (sorted-array deepEqual) and host_permissions (sorted-array deepEqual).
4. The three popup byte-equality tests: popup.html, popup.css, popup.js.
5. NEW: one absence-test that asserts the 3 SVCNOW-only files do NOT exist on either side (use `existsSync` from `node:fs`; assert all 6 paths are false).

DELETE every assertion referencing: `popup-helpers.js`, `background.js`, `content-script.js`, `background.service_worker`, `content_scripts[0]`.

Update the header comment block to read (verbatim — operator can edit later):
  // Phase 129.1 revert (file 129.1-02-PLAN.md). Manifest + popup-only surface; SVCNOW-specific files removed.
  // 6 assertions: 2 manifest set-equality (permissions, host_permissions), 3 byte-equality (popup.html/css/js),
  // 1 absence-test (background.js, content-script.js, popup-helpers.js absent on both sides).

Test name prefix changes from "SVCNOW-05:" to "EXT-revert:".

Delete `vigil-extension-tests/popup-helpers.test.ts` entirely (`rm` the file).

In root `package.json`, update the `test:extension` script. Current value (approximately):
  "test:extension": "tsx --test \"vigil-extension-tests/popup-helpers.test.ts\" \"vigil-extension-tests/parity.test.ts\""
New value (drop the popup-helpers.test.ts path):
  "test:extension": "tsx --test \"vigil-extension-tests/parity.test.ts\""

Run `npm run test:extension` and confirm 6 passing tests, 0 failures.
  </action>
  <acceptance_criteria>
    - `vigil-extension-tests/parity.test.ts` exists and grep finds EXACTLY 6 occurrences of `test(` (one per assertion).
    - Same file grep returns 0 for `popup-helpers`, `background.service_worker`, `content_scripts`, `content-script.js`, `background.js`.
    - Same file contains the literal string `EXT-revert:` (test name prefix).
    - Same file contains the literal string `existsSync` (absence-test uses it).
    - `vigil-extension-tests/popup-helpers.test.ts` does NOT exist (ls returns "No such file").
    - `package.json` `test:extension` script does NOT reference `popup-helpers.test.ts`.
    - `npm run test:extension 2>&1 | tee /tmp/ext-test.log` exits 0.
    - `/tmp/ext-test.log` contains `# pass 6` (node:test reporter) OR equivalent "6 passing" output proving exactly 6 tests ran.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && [ "$(grep -v '^#' vigil-extension-tests/parity.test.ts | grep -v '//' | grep -cE 'test\(')" = "6" ] && ! grep -qE 'popup-helpers|background\.service_worker|content_scripts|background\.js|content-script\.js' vigil-extension-tests/parity.test.ts && grep -q 'EXT-revert:' vigil-extension-tests/parity.test.ts && grep -q 'existsSync' vigil-extension-tests/parity.test.ts && [ ! -f vigil-extension-tests/popup-helpers.test.ts ] && ! grep -q 'popup-helpers' package.json && npm run test:extension 2>&1 | tee /tmp/ext-test.log | tail -30 && grep -qE 'pass[: ]+6|# pass 6' /tmp/ext-test.log</automated>
  </verify>
  <done>parity.test.ts is exactly 6 assertions matching the new surface; popup-helpers.test.ts deleted; package.json script updated; npm run test:extension passes with 6 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator updates Safari Xcode "Copy Bundle Resources" + manual install + smoke test</name>
  <what-built>
    Tasks 1 + 2 have reset Chrome + Safari extension files to Phase 84-equivalent state, deleted 3 SVCNOW-only files from each side, rewritten parity.test.ts to 6 assertions, deleted popup-helpers.test.ts, and updated package.json. `npm run test:extension` is green. The Safari extension's Xcode project file still REFERENCES the 3 deleted files in its "Copy Bundle Resources" build phase — this MUST be updated manually in the Xcode GUI before the Safari .app bundle is rebuilt, or the rebuild will either FAIL (missing resource) or SILENTLY ship the stale files from the prior build.
  </what-built>
  <how-to-verify>
    1. **Safari Xcode build-phase update (REQUIRED — cannot be automated):**
       a. Open `vigil-safari-extension/vigil-safari-extension.xcodeproj` in Xcode.
       b. Select the "Vigil Capture Extension" target → Build Phases → Copy Bundle Resources.
       c. REMOVE these three entries: `background.js`, `content-script.js`, `popup-helpers.js`.
       d. Build the project (⌘B). Expected: succeeds.
       e. Locate the produced `.app` bundle (Xcode's "Show in Finder" on the Products group, or `find vigil-safari-extension/build -name '*.app' -type d`) and confirm its `Contents/Resources/` directory does NOT contain background.js, content-script.js, or popup-helpers.js. Run:
          `ls "vigil-safari-extension/build/Build/Products/Debug/Vigil Capture Extension.appex/Contents/Resources/" | grep -E 'background|content-script|popup-helpers'`
          Expected: no output.

    2. **Chrome extension smoke test (5 min):**
       a. In Chrome → chrome://extensions → toggle Developer Mode → "Load unpacked" → select `vigil-extension/`.
       b. Confirm extension loads with NO errors (the "Errors" button should be absent).
       c. Navigate to `https://example.com` (a non-ServiceNow URL — Phase 84 popup should work everywhere).
       d. Click the Vigil icon → popup opens.
       e. If api-key not yet set, the setup-view appears (per DECISION-B preservation) — enter a test key.
       f. Otherwise, capture-view appears with a textarea + Send button.
       g. Type "hello from reverted extension" → click Send. Expected: a confirmation message and a new thought row visible in the PWA dashboard within a few seconds.

    3. **Safari extension smoke test (5 min):**
       a. From the Xcode build, run the parent Safari app (the host app that installs the extension).
       b. In Safari → Preferences → Extensions → enable "Vigil Capture Extension".
       c. Navigate to `https://example.com` → click the Vigil toolbar icon → popup opens.
       d. Same capture flow as Chrome — type a test thought → Send → confirm it appears in PWA.

    4. **Verify the popup is byte-identical visual behavior across the two browsers** (the lock-step convention is now enforced by the relaxed parity test in code; this is a final visual sanity check).
  </how-to-verify>
  <resume-signal>Type "approved" if all three smoke tests passed AND the Safari Xcode build phase no longer references the 3 deleted files. Type a description of any failures if not.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → extension popup | popup script reads user input + api key from chrome.storage; posts to vigil-core |
| extension → vigil-core | bearer-auth POST to /v1/thoughts (Phase 84 endpoint) — unchanged by this revert |
| Safari host app bundle | Resources/ directory shipped to end-users via .app bundle — Xcode "Copy Bundle Resources" is the build-phase that controls what's included |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-05 | Tampering | Safari .app bundle | mitigate | Task 3 checkpoint requires operator to update Xcode "Copy Bundle Resources" build phase to remove the 3 deleted files, AND verify the produced bundle's Resources/ does not contain them. Risk 4 in RESEARCH §979-983. |
| T-129.1-06 | Information Disclosure | extension host_permissions reduction | mitigate-via-reduction | Reverted manifest removes `*://*.service-now.com/*` host permission. Phase 84 baseline uses only `https://api.vigilhub.io/*`. Smaller attack surface than the SVCNOW state. No content-script means no DOM access on any browsed page. |
| T-129.1-07 | Repudiation | extension version downgrade (1.1.0 → 1.0.0) | accept | Existing Chrome installs will see this as a "downgrade" but extension auto-updaters generally honor it. If operator prefers, bump to 1.2.0 to signal "post-revert" — Claude's discretion per RESEARCH §289. Default: stay at 1.0.0 (matches Phase 84 exactly). |
| T-129.1-08 | Tampering | parity test surface reduction | mitigate | Relaxed test still enforces Chrome ↔ Safari byte-equality for popup.{html,css,js} + manifest set-equality. Absence-test prevents accidental re-introduction of the 3 deleted files. Header comment names the Phase 129.1 revert context for auditability. |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "Chrome popup is generic, works on any URL" → covered by Task 3 smoke test on example.com.
2. Truth: "Safari popup is byte-identical to Chrome" → Task 1 diff command + Task 2 parity.test.ts byte-equality assertions.
3. Truth: "npm run test:extension passes with 6 assertions" → Task 2 automated verify.
4. Truth: "popup-helpers.test.ts deleted" → Task 2 automated `[ ! -f ]` check.
5. Truth: "3 SVCNOW files absent from BOTH dirs AND from Xcode build phase" → Task 1 file-absence asserts + Task 3 Xcode operator checkpoint with ls grep verification.

**Phase-level checks:**

- `npm run test:extension` exits 0 with exactly 6 passing tests.
- Chrome extension loads in Developer Mode with no errors.
- Safari .app bundle's Resources/ directory does not contain background.js, content-script.js, or popup-helpers.js.

</verification>

<success_criteria>

- 4 shared files (manifest.json, popup.html, popup.css, popup.js) reset to Phase 84-equivalent state on BOTH Chrome + Safari.
- 3 SVCNOW-only files (background.js, content-script.js, popup-helpers.js) DELETED from BOTH Chrome + Safari.
- Chrome popup.{html,css,js} byte-equal to Safari popup.{html,css,js}.
- parity.test.ts is exactly 6 assertions (2 manifest set-equality + 3 popup byte-equality + 1 absence-test).
- popup-helpers.test.ts deleted.
- package.json `test:extension` script updated.
- `npm run test:extension` passes (6/6).
- Operator confirms Safari Xcode "Copy Bundle Resources" updated; .app bundle's Resources/ verified clean.
- Chrome + Safari smoke tests pass on example.com (popup loads, capture works).

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-02-SUMMARY.md` when done.
</output>
