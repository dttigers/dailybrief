---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 129.1-03
files_modified:
  - scripts/vigil-capture/package.json
  - scripts/vigil-capture/tsconfig.json
  - scripts/vigil-capture/src/watcher.ts
  - scripts/vigil-capture/src/watcher.test.ts
  - scripts/vigil-capture/src/cli.ts
  - scripts/vigil-capture/README.md
  - LaunchAgent/com.jamesonmorrill.vigilcapture.plist
autonomous: false
requirements:
  - SCAP-03
  - SCAP-04
user_setup:
  - service: macos-keychain
    why: "Daemon reads operator's Vigil API key from macOS Keychain — never stored in plist (matches Phase 126 secret-hygiene)."
    env_vars: []
    dashboard_config:
      - task: "Store operator's Vigil API key in macOS Keychain via: security add-generic-password -s vigil-api-key -a $USER -w <api-key> -U"
        location: "macOS Keychain Access (CLI: security command)"

must_haves:
  truths:
    - "TypeScript daemon under scripts/vigil-capture/ watches ~/vigil-captures/ via chokidar with awaitWriteFinish; on new PNG/JPG it base64-encodes, POSTs to POST /v1/captures/screenshot, and moves the file to processed/<ts>-<workOrderId>.<ext> on success or failed/ with a .err sidecar on failure."
    - "Daemon filters dotfiles, .tmp/.download, and .icloud files (junk + iCloud edge cases)."
    - "Daemon reads VIGIL_API_KEY from macOS Keychain via `security find-generic-password -s vigil-api-key -w` (never from env or plist)."
    - "LaunchAgent plist with KeepAlive + LimitLoadToSessionType=Aqua + ProcessType=Background + StandardOut/ErrorPath under ~/Library/Logs/vigil-capture.{log,error.log}."
    - "`vigil-capture install` is idempotent (bootout-then-bootstrap), creates ~/vigil-captures/{processed,failed}/ with mode 0700, copies the plist into ~/Library/LaunchAgents/, calls launchctl bootstrap gui/$UID, and is safe to re-run."
    - "On daemon startup, a one-shot orphan scan picks up any leftover PNG/JPG in the root directory from a previous-run crash (RESEARCH §694)."
  artifacts:
    - path: "scripts/vigil-capture/src/watcher.ts"
      provides: "Long-running chokidar watcher + HTTP POST + atomic file-move handler"
      min_lines: 80
    - path: "scripts/vigil-capture/src/cli.ts"
      provides: "install/uninstall/status/tail subcommands wrapping launchctl"
      min_lines: 60
    - path: "scripts/vigil-capture/src/watcher.test.ts"
      provides: "Unit tests for processFile() with mock fetch + mock fs"
      min_lines: 60
    - path: "LaunchAgent/com.jamesonmorrill.vigilcapture.plist"
      provides: "LaunchAgent plist modeled on com.jamesonmorrill.vigilcore.plist"
      contains: "com.jamesonmorrill.vigilcapture"
    - path: "scripts/vigil-capture/README.md"
      provides: "Operator runbook covering install/pause/resume/tail/key-rotation/edge-cases"
      min_lines: 50
  key_links:
    - from: "scripts/vigil-capture/src/watcher.ts"
      to: "POST {VIGIL_API_BASE}/v1/captures/screenshot"
      via: "fetch with Bearer auth from Keychain"
      pattern: "/v1/captures/screenshot"
    - from: "LaunchAgent/com.jamesonmorrill.vigilcapture.plist"
      to: "scripts/vigil-capture/dist/watcher.js (or tsx + watcher.ts)"
      via: "ProgramArguments array"
      pattern: "ProgramArguments"
    - from: "scripts/vigil-capture/src/cli.ts"
      to: "launchctl bootstrap/bootout gui/$UID"
      via: "execSync calls"
      pattern: "launchctl (bootstrap|bootout)"
---

<objective>
Build the macOS-side half of the screenshot pipeline: a TypeScript daemon (`scripts/vigil-capture/src/watcher.ts`) that uses `chokidar` with `awaitWriteFinish` to watch `~/vigil-captures/`, base64-encodes new PNG/JPG files, POSTs to the `/v1/captures/screenshot` endpoint built in plan 129.1-03 using a bearer token sourced from macOS Keychain, and moves the file to `processed/<ts>-<workOrderId>.<ext>` on success or `failed/<ts>-<filename>` with a `.err` sidecar on failure. A LaunchAgent plist (modeled on `com.jamesonmorrill.vigilcore.plist`) keeps the daemon running with `KeepAlive`. An `install/uninstall/status/tail` CLI manages the LaunchAgent. Operator runbook in `README.md`. Implements SCAP-03 + SCAP-04 (planner-authored IDs landed in plan 06).

Purpose: Operator-only ambient screenshot capture — no hotkey, no popup, just drop a file in a folder. Single-user scope per DECISION-F.

Output: New monorepo subdirectory `scripts/vigil-capture/` with package.json, tsconfig, daemon source, CLI source, tests, README; new LaunchAgent plist; blocking operator checkpoint for actual install + hardware UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-03-SUMMARY.md
@LaunchAgent/com.jamesonmorrill.vigilcore.plist

<interfaces>
<!-- chokidar config (RESEARCH §663-669 + §381): -->
chokidar.watch(CAPTURE_DIR, {
  ignored: /(^|[\/\\])\../,     // ignore dotfiles
  persistent: true,
  ignoreInitial: true,          // don't fire on existing files at startup (orphan scan handles those)
  depth: 0,                     // don't recurse into processed/failed/
  awaitWriteFinish: { stabilityThreshold: 500, pollInterval: 100 },
})
.on("add", processFile);

<!-- POST request shape (matches plan 03 endpoint contract): -->
fetch(`${API_BASE}/v1/captures/screenshot`, {
  method: "POST",
  headers: { "Authorization": `Bearer ${apiKey}`, "Content-Type": "application/json" },
  body: JSON.stringify({ imageBase64, mediaType, clientCaptureId }),
})
// On success: { workOrderId: string, duplicate: boolean, extractedFields, extraExtractedFields }
// On failure: { error: string } with 400/413/422/502/503 status

<!-- Keychain access (RESEARCH §608-611): -->
execSync('security find-generic-password -s vigil-api-key -w', { encoding: "utf8" }).trim();

<!-- LaunchAgent template (LaunchAgent/com.jamesonmorrill.vigilcore.plist) — reuse keys: -->
// Label, ProgramArguments, WorkingDirectory, RunAtLoad, KeepAlive.SuccessfulExit=false,
// LimitLoadToSessionType=Aqua, ProcessType=Background, StandardOut/ErrorPath, EnvironmentVariables.PATH.
// DO NOT add WatchPaths (RESEARCH §591 — KeepAlive long-running is the right pattern with in-process chokidar).

<!-- launchctl commands (idempotent install pattern, PATTERNS landmine): -->
launchctl bootout gui/$UID ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist 2>/dev/null || true
launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold scripts/vigil-capture/ npm package with chokidar (gated by package legitimacy check)</name>
  <files>scripts/vigil-capture/package.json, scripts/vigil-capture/tsconfig.json</files>
  <read_first>
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Package Legitimacy Audit" (lines 205-218) — chokidar marked [ASSUMED]
    - vigil-core/package.json (to verify tsx + node version conventions)
    - vigil-core/tsconfig.json (to copy compiler options that match the repo's TS standards)
  </read_first>
  <action>
Before creating any package files, verify chokidar legitimacy:

  npm view chokidar version 2>&1
  npm view chokidar maintainers 2>&1

Confirm chokidar is the well-known package (used by Vite, jest, nodemon). Latest stable version should be 3.6.0 or 4.x. Verify the registry response shows >100M downloads/week class package.

Then create `scripts/vigil-capture/` directory and write:

`scripts/vigil-capture/package.json`:
  - name: "@dailybrief/vigil-capture" (scoped to project — not published)
  - version: "0.1.0"
  - private: true
  - type: "module"
  - bin: { "vigil-capture": "./dist/cli.js" }
  - scripts: { "build": "tsc", "watch": "tsc --watch", "start": "tsx src/watcher.ts", "test": "tsx --test src/**/*.test.ts" }
  - dependencies: { "chokidar": "^3.6.0" } (or whichever stable version `npm view` reports)
  - devDependencies: { "tsx": "^4.19.0", "typescript": "^5.5.0", "@types/node": "^22.0.0" }

`scripts/vigil-capture/tsconfig.json`:
  - target: ES2022, module: ESNext, moduleResolution: bundler, strict: true, outDir: ./dist, rootDir: ./src, esModuleInterop: true, skipLibCheck: true, declaration: false.

After files exist, run:

  cd scripts/vigil-capture && npm install

Confirm install succeeds and `node_modules/chokidar/package.json` exists. NOTE: per CONTEXT/RESEARCH the install is gated by Task 2 below (human-verify checkpoint for `[ASSUMED]` package).
  </action>
  <acceptance_criteria>
    - `scripts/vigil-capture/package.json` exists with `"chokidar"` in dependencies.
    - `scripts/vigil-capture/tsconfig.json` exists with strict: true and outDir: "./dist".
    - `npm view chokidar version` returns a version string (proves package exists on npm registry).
    - `scripts/vigil-capture/node_modules/chokidar/package.json` exists after install.
    - `scripts/vigil-capture/node_modules/chokidar/package.json` lists `paulmillr` (or a known chokidar maintainer) in the maintainers array — sanity check against slop.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && npm view chokidar version 2>&1 | head -1 && [ -f scripts/vigil-capture/package.json ] && grep -q '"chokidar"' scripts/vigil-capture/package.json && [ -f scripts/vigil-capture/tsconfig.json ] && grep -q '"strict": true' scripts/vigil-capture/tsconfig.json && cd scripts/vigil-capture && npm install 2>&1 | tail -5 && [ -f node_modules/chokidar/package.json ]</automated>
  </verify>
  <done>scripts/vigil-capture/ scaffolded with package.json + tsconfig; chokidar installed; legitimacy verified via npm view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator verifies chokidar package legitimacy ([ASSUMED] gate per RESEARCH §205-218)</name>
  <what-built>
    Task 1 ran `npm view chokidar version` + installed chokidar into `scripts/vigil-capture/node_modules/`. Per RESEARCH Package Legitimacy Audit, chokidar is marked `[ASSUMED]` — well-known package (powers Vite, jest, nodemon, webpack), but the planner did not run slopcheck. This checkpoint is the explicit human gate required before any code depends on the install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/chokidar in a browser.
    2. Confirm: maintainer is `paulmillr` (Paul Miller); repository points to `github.com/paulmillr/chokidar`; weekly downloads >50M; latest release in the last 6 months.
    3. Confirm: package is NOT a typo-squat (compare to https://www.npmjs.com/package/chokidor or similar — chokidar is the real one).
    4. Verify `scripts/vigil-capture/node_modules/chokidar/package.json` shows the same maintainer name.
    5. Optionally: run `npm audit --prefix scripts/vigil-capture` and confirm no critical/high vulnerabilities for chokidar itself.
  </how-to-verify>
  <resume-signal>Type "approved — chokidar verified legitimate" or describe any concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement watcher.ts + watcher.test.ts (chokidar daemon + processFile + orphan scan)</name>
  <files>scripts/vigil-capture/src/watcher.ts, scripts/vigil-capture/src/watcher.test.ts</files>
  <read_first>
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"macOS launchd Watcher" + "Daemon implementation sketch" (lines 539-695) — verbatim sketch
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-capture/src/watcher.ts" (LANDMINES — awaitWriteFinish REQUIRED + startup orphan scan + sequential not concurrent + iCloud filter)
    - vigil-core/src/routes/captures-screenshot.ts (the endpoint built in plan 03 — confirm request/response shapes match)
    - scripts/vigil-capture/package.json (from Task 1)
  </read_first>
  <behavior>
    - `processFile(filePath: string)` is exported (testable in isolation).
    - processFile validates the file: rejects dotfiles (filename starts with "."), rejects `.tmp` / `.download` / `.icloud` files, accepts only `.png` / `.jpg` / `.jpeg` extensions (case-insensitive).
    - On valid file: reads with `node:fs/promises.readFile`, base64-encodes, generates `clientCaptureId = randomUUID()`, fetches API key from Keychain via `getApiKey()`, POSTs to `${API_BASE}/v1/captures/screenshot`.
    - On 2xx response: extracts `workOrderId` from response, ensures `processed/` dir exists, moves file to `processed/<ISO-ts>-<workOrderId>.<ext>`, logs `[OK]`.
    - On non-2xx response: ensures `failed/` dir exists, moves file to `failed/<ts>-<filename>`, writes `failed/<ts>-<filename>.err` sidecar with `HTTP <status>\n<body>\n`, logs `[FAIL]`.
    - On thrown error (network, fs, keychain): moves file to `failed/<ts>-<filename>` with error message in sidecar, logs `[ERR]`.
    - On startup: one-shot orphan scan reads root of CAPTURE_DIR, processes any leftover PNG/JPG (this catches files left from a previous-run crash; RESEARCH §694).
    - chokidar configured with `{ignored: dotfile-regex, persistent: true, ignoreInitial: true, depth: 0, awaitWriteFinish: {stabilityThreshold: 500, pollInterval: 100}}` and `.on("add", processFile)`.
    - Constants: API_BASE = process.env.VIGIL_API_BASE ?? "https://api.vigilhub.io"; CAPTURE_DIR = process.env.VIGIL_CAPTURE_DIR ?? `${process.env.HOME}/vigil-captures`; PROCESSED = path.join(CAPTURE_DIR, "processed"); FAILED = path.join(CAPTURE_DIR, "failed").
    - getApiKey() exported separately (testable; can be mocked).
    - Top-level startup logs `[vigil-capture] watching ${CAPTURE_DIR}` on chokidar ready.
  </behavior>
  <action>
**TDD ordering — write `watcher.test.ts` FIRST (RED), then `watcher.ts` (GREEN).** The DI seams (`processFileDeps`, `shouldSkip` export, `getApiKey` export) are designed to be exercised from tests, so the test file dictates the production surface.

### Step 1 — Create `scripts/vigil-capture/src/watcher.test.ts` (RED)

Author the test file (node:test runner, `tsx --test`) before any production code exists. Imports from `./watcher.js` will fail to resolve initially — that is the expected RED state.

Covering:

Test 1 — shouldSkip filters dotfiles + .tmp + .download + .icloud, accepts normal .png/.jpg.

Test 2 — processFile rejects unsupported extension (e.g. .txt): returns without calling fetch.

Test 3 — processFile happy path: write a fake PNG to a tmpdir, inject mock fetchFn that returns `{ok: true, status: 200, json: () => ({workOrderId: "CS0000001"})}` and mock getApiKeyFn returning "test-key"; call processFile; assert the file moved to `processed/<ts>-CS0000001.png`.

Test 4 — processFile HTTP failure: mock fetchFn returns `{ok: false, status: 422, text: () => "validation error"}`; assert file moved to `failed/` with a `.err` sidecar containing "HTTP 422".

Test 5 — processFile network error: mock fetchFn throws `new Error("ECONNREFUSED")`; assert file moved to `failed/` with sidecar containing "ECONNREFUSED".

Test 6 — processFile correctly sets mediaType for .jpg files (assert captured request body has `mediaType: "image/jpeg"`).

Test 7 — processFile sends valid UUID v4 clientCaptureId (capture request body, assert against the UUID regex used in plan 03).

Use `node:test` runner; `tsx --test`; tmpdir via `os.tmpdir()` + `mkdtempSync`; cleanup tmpdirs via `afterEach`. Mock fetch via the dep-injection (`getApiKeyFn`, `fetchFn`).

Confirm RED: `cd scripts/vigil-capture && npm test` fails (cannot resolve `./watcher.js`). This proves the tests actually exercise the production surface and aren't accidentally green.

### Step 2 — Create `scripts/vigil-capture/src/watcher.ts` (GREEN)

Per the RESEARCH sketch (§596-674) with the additions:

1. Export `processFile` as a named export (not just used internally — needs to be testable).
2. Export `getApiKey` as a named export (so tests can mock the Keychain read).
3. Add the startup orphan scan BEFORE the chokidar watcher starts:
     async function scanOrphans() {
       try {
         const entries = await readdir(CAPTURE_DIR);
         for (const name of entries) {
           const full = path.join(CAPTURE_DIR, name);
           const st = await stat(full);
           if (st.isFile() && /\.(png|jpe?g)$/i.test(name)) {
             await processFile(full);
           }
         }
       } catch (err) { console.error(`[orphan-scan] failed: ${err}`); }
     }
4. Make the dotfile + tmp + download + icloud filter explicit and unit-testable:
     export const SKIP_PATTERNS = [
       (n: string) => n.startsWith("."),
       (n: string) => n.includes(".tmp."),
       (n: string) => n.endsWith(".download"),
       (n: string) => n.endsWith(".icloud"),
     ];
     export function shouldSkip(filename: string): boolean {
       return SKIP_PATTERNS.some(p => p(filename));
     }
   processFile uses `if (shouldSkip(filename)) return;` early.
5. Allow dependency injection for testing: factor the network + keychain + fs calls behind a `processFileDeps` argument with sensible defaults:
     export async function processFile(filePath: string, deps: {
       getApiKeyFn?: () => Promise<string>;
       fetchFn?: typeof fetch;
     } = {}) { ... uses deps.getApiKeyFn ?? getApiKey, deps.fetchFn ?? fetch ... }
6. Main entry-point (only runs when invoked directly, not when imported):
     if (import.meta.url === `file://${process.argv[1]}`) {
       await scanOrphans();
       const watcher = chokidar.watch(...);
       watcher.on("add", processFile);
       console.log(`[vigil-capture] watching ${CAPTURE_DIR}`);
     }

### Step 3 — Run tests (GREEN)

`cd scripts/vigil-capture && npm test` — confirm all 7 tests pass. If any fail, iterate on `watcher.ts` (not the tests). `npm run build` confirms TypeScript compiles to `dist/`.
  </action>
  <acceptance_criteria>
    - `scripts/vigil-capture/src/watcher.ts` exists; exports `processFile`, `getApiKey`, `shouldSkip`, `SKIP_PATTERNS`.
    - File imports `chokidar`, uses `awaitWriteFinish: { stabilityThreshold: 500, pollInterval: 100 }`, calls chokidar.watch on CAPTURE_DIR with `ignoreInitial: true` and `depth: 0`.
    - File does NOT contain `WatchPaths` (that's a launchd key, not a chokidar key — anti-pattern check).
    - File contains an orphan-scan function (grep for `scanOrphans`) executed before chokidar.watch.
    - File contains the literal POST URL fragment `/v1/captures/screenshot`.
    - File contains `Authorization`: `Bearer ${apiKey}` header construction.
    - File contains `security find-generic-password -s vigil-api-key -w` (Keychain read).
    - `scripts/vigil-capture/src/watcher.test.ts` exists; contains at least 7 `test(` declarations.
    - Test file injects deps (greps for `getApiKeyFn` and `fetchFn` in the test).
    - `cd scripts/vigil-capture && npm test 2>&1 | tee /tmp/watcher-test.log` exits 0.
    - `cd scripts/vigil-capture && npm run build 2>&1` exits 0 (TypeScript compiles to dist/).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/scripts/vigil-capture && grep -q 'export.*processFile' src/watcher.ts && grep -q 'export.*getApiKey' src/watcher.ts && grep -q 'export.*shouldSkip' src/watcher.ts && grep -q 'awaitWriteFinish' src/watcher.ts && grep -q 'ignoreInitial: true' src/watcher.ts && ! grep -q 'WatchPaths' src/watcher.ts && grep -q 'scanOrphans' src/watcher.ts && grep -q '/v1/captures/screenshot' src/watcher.ts && grep -q 'Bearer' src/watcher.ts && grep -q 'security find-generic-password -s vigil-api-key' src/watcher.ts && [ "$(grep -v '^#' src/watcher.test.ts | grep -v '//' | grep -cE 'test\(')" -ge "7" ] && grep -q 'getApiKeyFn' src/watcher.test.ts && grep -q 'fetchFn' src/watcher.test.ts && npm test 2>&1 | tee /tmp/watcher-test.log | tail -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>watcher.ts implements chokidar + processFile + orphan scan + DI for tests; 7+ tests pass; TypeScript builds.</done>
</task>

<task type="auto">
  <name>Task 4: Implement cli.ts (install/uninstall/status/tail) + LaunchAgent plist + README runbook</name>
  <files>scripts/vigil-capture/src/cli.ts, LaunchAgent/com.jamesonmorrill.vigilcapture.plist, scripts/vigil-capture/README.md</files>
  <read_first>
    - LaunchAgent/com.jamesonmorrill.vigilcore.plist (template — copy all keys; only change Label/ProgramArguments/WorkingDirectory/StandardOut+ErrorPath/EnvironmentVariables.VIGIL_API_BASE+VIGIL_CAPTURE_DIR)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"plist template" (lines 547-587) + §"Install / uninstall CLI" (lines 678-685)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"LaunchAgent/com.jamesonmorrill.vigilcapture.plist" + §"vigil-capture/src/cli.ts"
    - Scripts/install.sh (root-level — operator's mental model reference for install pattern)
  </read_first>
  <behavior>
    - `cli.ts` exposes subcommands: `install`, `uninstall`, `status`, `tail`. Invoked as `vigil-capture <subcommand>`.
    - `install` (idempotent): copies LaunchAgent plist from repo to `~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`; creates `~/vigil-captures/{processed,failed}/` with `chmod 700`; runs `launchctl bootout gui/$UID ... 2>/dev/null || true` (ignore "not loaded" errors); runs `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`; checks `launchctl print gui/$UID/com.jamesonmorrill.vigilcapture` to confirm loaded.
    - `uninstall`: runs `launchctl bootout gui/$UID ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`; removes the plist file; does NOT delete `~/vigil-captures/` (operator may still have data there).
    - `status`: runs `launchctl print gui/$UID/com.jamesonmorrill.vigilcapture | head -20` and prints output.
    - `tail`: execs `tail -f ~/Library/Logs/vigil-capture.log` (replaces process).
    - All subcommands print clear `[install] ...`, `[uninstall] ...`, etc. log prefixes and exit 0 on success, non-zero on failure.
    - Plist mirrors `com.jamesonmorrill.vigilcore.plist` keys but with: Label=`com.jamesonmorrill.vigilcapture`, ProgramArguments using `/usr/local/bin/node` + the compiled `dist/watcher.js` absolute path, WorkingDirectory=`~/vigil-captures` (expanded to absolute), StandardOutPath=`~/Library/Logs/vigil-capture.log`, StandardErrorPath=`~/Library/Logs/vigil-capture.error.log`, EnvironmentVariables.PATH (same as template), EnvironmentVariables.VIGIL_API_BASE (`https://api.vigilhub.io`), EnvironmentVariables.VIGIL_CAPTURE_DIR (`~/vigil-captures` absolute), KeepAlive.SuccessfulExit=false, RunAtLoad=true, LimitLoadToSessionType=Aqua, ProcessType=Background. NO WatchPaths, NO API key.
    - README covers: prerequisites (Node 20+, macOS, Vigil API key in Keychain), install steps, daily usage (just screenshot to `~/vigil-captures/`), pause/resume (`launchctl unload`/`load`), tail (`vigil-capture tail`), Keychain key rotation, edge cases (multi-file drops, daemon crash recovery), troubleshooting (`launchctl print` + logs).
  </behavior>
  <action>
Create `scripts/vigil-capture/src/cli.ts`:
- Top of file: shebang `#!/usr/bin/env node`.
- Read `process.argv[2]` for subcommand; switch on it.
- For each subcommand, use `child_process.execSync` with `{stdio: "inherit"}` so output streams to the operator's terminal.
- Build absolute paths via `path.join(homedir(), "Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist")` etc.
- The plist that gets copied into `~/Library/LaunchAgents/` comes from `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` in the repo. Use `path.resolve` against `import.meta.url` to locate the repo-relative path.
- `install` resolves the repo-relative paths to absolute paths at install time, then uses `fs.readFile` on the template plist, applies any substitution needed (e.g. `${HOME}` → actual homedir), then `fs.writeFile` to `~/Library/LaunchAgents/...`.
- Print a clear error if `/usr/local/bin/node` doesn't exist (operator may have node in a different path); suggest `which node` to debug.

Create `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` per the template above. Use the literal `/Users/jamesonmorrill/...` paths from the existing `com.jamesonmorrill.vigilcore.plist` for WorkingDirectory and StandardOut/ErrorPath (matches the operator's mental model that all LaunchAgent plists in this repo are pre-baked for the operator's home directory). The install CLI's responsibility is to verify the operator IS `jamesonmorrill` (or substitute homedir if not) at install time.

Create `scripts/vigil-capture/README.md` (50+ lines) with sections:
1. **Overview** — one-paragraph description; refers to plan 129.1-04 + the endpoint in plan 03.
2. **Prerequisites** — Node 20+, macOS, Vigil API key (instructions to store via `security add-generic-password -s vigil-api-key -a $USER -w <key> -U`).
3. **Install** — `cd scripts/vigil-capture && npm install && npm run build && node dist/cli.js install` OR `npm install -g .` then `vigil-capture install`.
4. **Daily usage** — set macOS screenshot save-to-location to `~/vigil-captures/`; just press Cmd+Shift+4 and select an area.
5. **Pause / resume** — `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist` / `launchctl load ...`.
6. **Tail logs** — `vigil-capture tail` or `tail -f ~/Library/Logs/vigil-capture.log`.
7. **Key rotation** — re-run `security add-generic-password -s vigil-api-key -a $USER -w <new-key> -U`; daemon auto-picks up new key on next file event (it reads keychain per-call).
8. **Edge cases & troubleshooting** — multi-file drops are sequential; daemon crash leaves files in root (orphan scan picks them up); `failed/<ts>-<filename>.err` sidecar shows what went wrong.

Then run `cd scripts/vigil-capture && npm run build` to ensure cli.ts compiles. Verify `dist/cli.js` exists.
  </action>
  <acceptance_criteria>
    - `scripts/vigil-capture/src/cli.ts` exists with `#!/usr/bin/env node` shebang.
    - File handles all 4 subcommands (grep `install`, `uninstall`, `status`, `tail`).
    - File contains `launchctl bootstrap gui/` (proves user-scope LaunchAgent install).
    - File contains `launchctl bootout gui/` with error-suppression (`|| true` or try/catch) (proves idempotent install).
    - File contains `chmod 700` (or equivalent fs.chmod call with 0o700) for the captures directory.
    - `LaunchAgent/com.jamesonmorrill.vigilcapture.plist` exists with Label `com.jamesonmorrill.vigilcapture`.
    - Plist contains KeepAlive with SuccessfulExit=false, LimitLoadToSessionType=Aqua, ProcessType=Background, RunAtLoad=true.
    - Plist contains EnvironmentVariables.VIGIL_API_BASE and VIGIL_CAPTURE_DIR.
    - Plist does NOT contain WatchPaths or any literal API key (grep for API key patterns returns 0).
    - `scripts/vigil-capture/README.md` exists with sections covering install / pause-resume / tail / key-rotation / troubleshooting.
    - `cd scripts/vigil-capture && npm run build` exits 0; `dist/cli.js` exists.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && [ -f scripts/vigil-capture/src/cli.ts ] && head -1 scripts/vigil-capture/src/cli.ts | grep -q '#!/usr/bin/env node' && grep -qE "['\"]install['\"]" scripts/vigil-capture/src/cli.ts && grep -qE "['\"]uninstall['\"]" scripts/vigil-capture/src/cli.ts && grep -qE "['\"]status['\"]" scripts/vigil-capture/src/cli.ts && grep -qE "['\"]tail['\"]" scripts/vigil-capture/src/cli.ts && grep -q 'launchctl bootstrap gui/' scripts/vigil-capture/src/cli.ts && grep -q 'launchctl bootout gui/' scripts/vigil-capture/src/cli.ts && grep -qE '0o700|chmod.*700' scripts/vigil-capture/src/cli.ts && [ -f LaunchAgent/com.jamesonmorrill.vigilcapture.plist ] && grep -q 'com.jamesonmorrill.vigilcapture' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && grep -q 'KeepAlive' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && grep -q 'Aqua' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && grep -q 'Background' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && grep -q 'VIGIL_API_BASE' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && grep -q 'VIGIL_CAPTURE_DIR' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && ! grep -qE 'WatchPaths|sk-ant|api[_-]?key' LaunchAgent/com.jamesonmorrill.vigilcapture.plist && [ -f scripts/vigil-capture/README.md ] && [ "$(wc -l < scripts/vigil-capture/README.md)" -ge "50" ] && cd scripts/vigil-capture && npm run build 2>&1 | tail -5 && [ -f dist/cli.js ]</automated>
  </verify>
  <done>cli.ts has all 4 subcommands + idempotent launchctl pattern; plist mirrors vigilcore template; README covers install/pause/tail/rotation/edge cases; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator installs daemon + runs end-to-end smoke test on a real Polaris screenshot</name>
  <what-built>
    Tasks 1-4 built the daemon (chokidar watcher + processFile + orphan scan), the install/uninstall/status/tail CLI, the LaunchAgent plist, and the operator runbook. `npm run build` succeeds and `dist/cli.js` exists. The daemon has NOT yet been installed on the operator's Mac — that's this checkpoint.
  </what-built>
  <how-to-verify>
    1. **Prerequisite — Vigil API key in Keychain:**
       Run: `security find-generic-password -s vigil-api-key -w 2>&1 | head -1`
       If output says "could not be found", add it: `security add-generic-password -s vigil-api-key -a $USER -s vigil-api-key -w <your-vigil-api-key> -U`
       Confirm: `security find-generic-password -s vigil-api-key -w` outputs your key (no "could not be found" error).

    2. **Install the daemon:**
       From `/home/morrillboss/dev/dailybrief/scripts/vigil-capture/` (on the operator's Mac — this is a checkpoint operator runs on their hardware):
         npm install && npm run build && node dist/cli.js install
       Expected: `[install] ...` log lines; no errors; ends with the daemon loaded.

    3. **Verify daemon is running:**
       Run: `node dist/cli.js status`
       Expected: output from `launchctl print` showing the agent with state `running` (or `waiting` if no recent files).

    4. **Smoke test — manual file drop:**
       Take any PNG (e.g. a small test image) → copy to `~/vigil-captures/test1.png`.
       Within ~10 seconds, run: `node dist/cli.js tail` (Ctrl+C to exit after seeing the log line).
       Expected: log line `[OK] test1.png → <workOrderId>` OR `[FAIL] test1.png → 422` (expected because test1.png isn't a Polaris page — vision returns empty case_number).
       In either case, the file should be moved from `~/vigil-captures/` to either `processed/` or `failed/` (NOT still in root):
         ls ~/vigil-captures/test1.png 2>&1 | head -1  # should say "No such file"
         ls ~/vigil-captures/{processed,failed}/ | grep test1   # should find it in one or the other.

    5. **End-to-end Polaris test:**
       Take a real Polaris case-page screenshot (e.g. of CS0363817 or any active case) using Cmd+Shift+4, save to `~/vigil-captures/`. Wait ~5-10 seconds.
       Run: `node dist/cli.js tail` and look for `[OK] <filename> → CS<7-digits>`.
       Open the PWA `/work-orders` page. Expected: a new row with state `pending_review` and the extracted case_number, with the extracted department, maintenance_problem, location fields visible (plan 05 makes them visible via the Review modal — for now, peek at the row data via dev tools or by visiting the row endpoint).
       NOTE: if plan 129.1-05 has not yet completed, the row may not display nicely — that's expected; verify the row EXISTS in the DB instead via:
         `curl -H "Authorization: Bearer <your-key>" https://api.vigilhub.io/v1/work-orders | jq '.[] | select(.state=="pending_review")'`

    6. **Pause / resume:**
       `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`
       Drop another image to `~/vigil-captures/`. Expected: file stays in root (daemon not running).
       `launchctl load ~/Library/LaunchAgents/com.jamesonmorrill.vigilcapture.plist`
       Expected: orphan-scan picks up the leftover file within ~5 seconds and processes it.

    7. **Tail logs:** confirm `~/Library/Logs/vigil-capture.log` exists and contains the daemon's output.
  </how-to-verify>
  <resume-signal>Type "approved" if install + smoke test + Polaris e2e + pause/resume all worked. Type a description of any failures if not (esp. the orphan-scan resume case — that's the most fragile path).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's filesystem (~/vigil-captures/) → daemon | trusts the user (no other user has access to homedir; chmod 700 enforces) |
| daemon → vigil-core API | Bearer-auth POST with Keychain-sourced API key |
| macOS Keychain → daemon | macOS-mediated access; same user-scope as daemon process |
| daemon → ~/Library/LaunchAgents/ + ~/Library/Logs/ | user-scope writes; LimitLoadToSessionType=Aqua isolates to the operator's login session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-16 | Information Disclosure | API key in plist | mitigate | Key stored in macOS Keychain ONLY; daemon reads via `security find-generic-password`; plist contains zero secrets. Verified in acceptance criteria via grep for `sk-ant`/`api[_-]?key` returning 0. |
| T-129.1-17 | Information Disclosure | ~/vigil-captures/ readable by other local users | mitigate | install CLI chmods the directory 0700 (operator-only). DECISION-F single-user scope. |
| T-129.1-18 | Tampering | malicious file in ~/vigil-captures/ | accept | Daemon trusts the directory (operator's homedir). Only valid PNG/JPG processed (extension whitelist). Worst case: vision returns garbage → 422 → file moves to failed/. No filesystem traversal because path.basename strips any path components. |
| T-129.1-19 | Tampering | chokidar [ASSUMED] package | mitigate-via-checkpoint | Task 2 is a blocking-human checkpoint per Package Legitimacy Audit (RESEARCH §205-218). Operator verifies chokidar npm page + maintainer + downloads. |
| T-129.1-20 | Denial of Service | daemon crash leaves files unprocessed | mitigate | Orphan-scan on startup picks up leftover files; KeepAlive=true restarts daemon on crash; log-on-failure surfaces issues to the operator. |
| T-129.1-21 | Denial of Service | rapid multi-file drop overwhelms vigil-core | accept | Sequential `await processFile` in chokidar event handler; single-user volume ≤50/day (RESEARCH §691). Per-user GUARD-03 watermark catches Anthropic spend exhaustion. |
| T-129.1-22 | Repudiation | API key rotation breaks daemon silently | mitigate-via-runbook | README documents key rotation procedure; daemon reads keychain per-call (no caching), so rotation takes effect on next file event. operator-discoverable via tail log. |
| T-129.1-SC | Tampering | chokidar npm install | mitigate | Task 2 blocking-human checkpoint. The new dep added in this plan IS chokidar, and the checkpoint is the required gate. |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "Daemon watches ~/vigil-captures/ via chokidar with awaitWriteFinish + POST to endpoint + atomic move" → Task 3 tests + Task 5 operator smoke test.
2. Truth: "Daemon filters dotfiles, .tmp, .download, .icloud" → Task 3 Test 1 (shouldSkip).
3. Truth: "API key from Keychain, not env/plist" → Task 3 grep for `security find-generic-password` + Task 4 plist grep for absence of `sk-ant`/`api_key`.
4. Truth: "Plist has KeepAlive + Aqua + Background + log paths" → Task 4 acceptance criteria grep.
5. Truth: "`vigil-capture install` is idempotent" → Task 4 grep for `bootout ... || true` pattern + Task 5 operator re-run smoke test (re-running should succeed without errors).
6. Truth: "Orphan-scan on startup" → Task 3 grep for `scanOrphans` + Task 5 pause-resume e2e step.

**Phase-level checks:**

- `cd scripts/vigil-capture && npm test` exits 0 (7+ unit tests).
- `cd scripts/vigil-capture && npm run build` exits 0; `dist/cli.js` exists.
- Operator smoke test passes (Task 5 checkpoint).

</verification>

<success_criteria>

- `scripts/vigil-capture/` exists as a self-contained npm package with chokidar dep.
- `watcher.ts` implements chokidar.watch + awaitWriteFinish + ignoreInitial + depth=0 + processFile + orphan-scan + DI for tests.
- 7+ unit tests cover shouldSkip, processFile happy path, HTTP failure, network error, mediaType derivation, UUID v4 clientCaptureId.
- `cli.ts` implements install/uninstall/status/tail with idempotent launchctl bootout-then-bootstrap.
- LaunchAgent plist mirrors `com.jamesonmorrill.vigilcore.plist` keys with vigilcapture-specific Label + paths + env.
- Plist contains NO API key (Keychain-only).
- README covers prerequisites + install + daily usage + pause/resume + tail + key rotation + edge cases.
- Operator install + smoke test (e2e Polaris screenshot → pending_review row in DB) passes via Task 5 checkpoint.
- chokidar `[ASSUMED]` checkpoint passed (Task 2).

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-04-SUMMARY.md` when done.
</output>
