# Phase 129.1 — Discussion Log

**Date:** 2026-05-16
**Operator:** Jameson Morrill
**Mode:** standard discuss (batched questions where decisions cluster)

---

## Round 1 — Four primary gray areas

### A. Schema scope — Polaris field-set mismatch with work_orders

**Question:** Polaris has 18+ form fields; work_orders currently stores 10 sanitized fields. How do we handle the field-set mismatch?

**Options presented:**
1. Extend work_orders with the new fields you care about (Recommended)
2. Add a single jsonb `extracted_fields` column for the long tail
3. Extract everything but only save the existing 10 fields

**Selection:** Option 1 — Extend work_orders with the new fields you care about.

**Why this matters:** Determines schema migration scope + extraction prompt + manual-create UI fields.

---

### B. Post-revert extension UX

**Question:** What happens to the Chrome + Safari extensions after the SVCNOW revert?

**Options presented:**
1. Full revert to Phase 84 generic thought-capture (Recommended)
2. Remove the extension entirely (no popup)
3. Keep the popup shell but rewire it to /v1/captures/screenshot

**Selection:** Option 1 — Full revert to Phase 84 generic thought-capture.

**Why this matters:** Locks the revert target. Operator retains generic-capture path for any web page; ServiceNow specificity goes away.

---

### C. Feedback loop — operator knows extraction worked + edits before commit

**Question:** How should the operator know an extraction worked / get to edit before commit?

**Options presented:**
1. Preview-before-commit in PWA (Recommended)
2. Silent auto-commit + macOS notification on success
3. Silent auto-commit, no notification (just check PWA)

**Selection:** Option 1 — Preview-before-commit in PWA.

**Why this matters:** Adds a draft state to work_orders. Operator reviews extraction before it goes live.

---

### D. PWA manual-create UI scope

**Question:** PWA manual-create UI — who sees it + which fields are exposed?

**Options presented:**
1. Everyone, with full Polaris field set including any new schema columns (Recommended)
2. Everyone, with minimal field set (case# + store + description + priority)
3. Operator-only (gated by user role / email)

**Selection:** Option 1 — Everyone, full field set.

**Why this matters:** Locks the manual-create UI scope. Replaces the popup's role for non-operator users (most of whom don't use ServiceNow).

---

## Round 2 — Follow-up clarifications

### E. Which specific Polaris fields to add to work_orders?

**Question:** (multiSelect) Which Polaris fields should we add to work_orders? (Currently has: case_number, store, short_description, trade, location, equipment, priority, contact, state — 10 columns.)

**Options presented:**
- maintenance_problem
- service
- assignment_group + assigned_to
- opened_at + time_worked

**Selection:** maintenance_problem + free-text note: "all i really need is CS#, description, location, and department."

**Interpretation:**
- CS# → existing `case_number` column. No change.
- description → existing `short_description` column. No change.
- location → existing `location` column. No change.
- **department → NEW column.** Operator's term doesn't directly map to one Polaris field. Three plausible mappings (Assignment Group, Maintenance Location, Service) — planner asks operator at plan-phase time.
- **maintenance_problem → NEW column.** Maps directly to Polaris "Maintenance Problem" field.

Net result: 2 new columns. Scope tighter than I initially expected.

---

### F. Draft storage location

**Question:** Where does the preview-before-commit draft live?

**Options presented:**
1. Same `work_orders` table with a new `state: 'pending_review'` value (Recommended)
2. Separate `work_order_drafts` table, promote on commit
3. Draft in chrome.storage / localStorage on operator's Mac, never hits server until commit

**Selection:** Option 1 — Same work_orders table with state: 'pending_review'.

**Why this matters:** No new table needed. `work_orders.state` is already a free-text column. Simple promotion (state → 'open' on commit, row delete on discard).

---

### G. macOS trigger mechanism specifics

**Question:** macOS trigger mechanism — specifically?

**Options presented:**
1. launchd watcher on ~/vigil-captures/ (Recommended)
2. macOS Shortcut with global hotkey
3. Both — launchd watcher AND a manual Shortcut

**Selection:** Option 1 — launchd watcher on ~/vigil-captures/.

**Why this matters:** Locks the trigger mechanism. Ambient capture (drop file into folder) vs. explicit hotkey. launchd agent + watch folder is the simplest reliable path.

---

## Deferred Ideas

(none surfaced this discussion — scope was already tight from the pivot decision)

## Claude's Discretion (planner / researcher handles these)

- Exact extraction prompt for Claude vision (planner authors based on Polaris screenshot structure).
- launchd plist schema + install/uninstall scripts (planner designs based on best-practice patterns).
- PWA review modal layout + form-level validation rules (planner designs based on existing PWA component patterns).
- Migration 0022 specific SQL shape (planner mirrors migration 0021's IF NOT EXISTS pattern).
- Revert commit dependency order (planner determines via `git log --oneline` of affected files).
- Confidence-scoring on extraction (out of scope — flagged in plan_outline; operator can ask later if extraction reliability becomes an issue).

## Operator Clarification Needed at Plan-Phase Time

- **`department` field semantic mapping** — does it map to Polaris's Assignment Group (e.g., "MT-Maintenance"), Maintenance Location (e.g., "Bakery"), or Service (e.g., "MT-Trades")? Planner asks before writing migration 0022.

---

*Discussion captured: 2026-05-16T20:55Z. CONTEXT.md is the canonical decisions record; this log is for audit/retrospective only.*
