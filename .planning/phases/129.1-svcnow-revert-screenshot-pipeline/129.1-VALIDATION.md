---
phase: 129.1
slug: svcnow-revert-screenshot-pipeline
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-17
---

# Phase 129.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Full strategy lives in `129.1-RESEARCH.md` → `## Validation Architecture`.
> This file is the per-task verification map; planner fills the table during plan-phase.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (vigil-core + vigil-pwa + vigil-extension-tests) |
| **Config file** | `vigil-core/vitest.config.ts`, `vigil-pwa/vitest.config.ts`, `vigil-extension-tests/vitest.config.ts` |
| **Quick run command** | `npx vitest run --reporter=dot <changed-file>` |
| **Full suite command** | `npx vitest run` (per-package) |
| **Estimated runtime** | ~5-30s per package |

---

## Sampling Rate

- **After every task commit:** Run quick command for the package touched
- **After every plan wave:** Run full suite for every package touched in that wave
- **Before `/gsd:verify-work`:** All package suites green; manual UAT runbook (129.1-06) executed
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*Planner fills this table during plan-phase. Each `<task>` in every PLAN.md must produce one row here, with the automated verify command copied from the task's `<automated>` block, and a Wave 0 dependency reference if test infra needs to be installed first.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Planner fills this list during plan-phase. Likely entries:*

- [ ] `vigil-extension-tests/parity.test.ts` rewritten for Phase-84 manifest assertions (replaces SVCNOW-specific assertions)
- [ ] `vigil-core/tests/captures-screenshot.test.ts` — stubs for SCAP-* requirements
- [ ] `vigil-pwa/tests/work-orders-create.test.tsx` — stubs for WO-MANUAL-* requirements
- [ ] `vigil-pwa/tests/work-orders-review.test.tsx` — stubs for pending_review state handling

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end screenshot → draft → commit → PWA list | SCAP-* end-to-end | Requires operator's Mac + Polaris session + real network round-trip | See 129.1-06-UAT-RUNBOOK.md (planner-authored) |
| Safari extension Xcode "Copy Bundle Resources" after file deletions | SVCNOW revert | Cannot be automated in CI — Xcode IDE step | Runbook in 129.1-02 plan |
| launchd watcher install / pause / resume | SCAP-* operator runbook | Requires user-scope launchctl invocation on operator's Mac | Runbook in 129.1-04 plan |
| Anthropic vision extraction quality on real Polaris screenshots | SCAP-* extraction accuracy | Cannot be deterministically asserted in unit tests | UAT runbook + ai_usage_daily watermark check |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
