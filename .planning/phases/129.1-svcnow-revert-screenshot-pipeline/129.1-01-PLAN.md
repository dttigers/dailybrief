---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql
  - vigil-core/src/db/schema.ts
  - vigil-core/src/routes/work-orders.ts
  - vigil-core/src/routes/work-orders.test.ts
autonomous: true
requirements:
  - WO-MANUAL-03

must_haves:
  truths:
    - "POST /v1/work-orders/sync accepting maintenanceProblem + department in the workOrders payload persists both columns to the work_orders row."
    - "drizzle-kit generate --dry reports no divergence between schema.ts workOrders block and applied migrations after 0022 applies."
    - "Existing work_orders rows survive migration 0022 unchanged (nullable columns; no data backfill)."
  artifacts:
    - path: "vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql"
      provides: "Drizzle migration adding maintenance_problem + department nullable text columns"
      contains: "ADD COLUMN IF NOT EXISTS \"maintenance_problem\""
    - path: "vigil-core/src/db/schema.ts"
      provides: "TypeScript schema for workOrders with maintenanceProblem + department"
      contains: "maintenanceProblem: text(\"maintenance_problem\")"
    - path: "vigil-core/src/routes/work-orders.ts"
      provides: "Extended SanitizedWorkOrder + sanitizer destructure + dbInsertOrGet + dbUpsertLegacy carry 2 new fields"
      contains: "maintenanceProblem: string | null"
    - path: "vigil-core/src/routes/work-orders.test.ts"
      provides: "Tests asserting 2-new-fields survive the sync pipeline end-to-end (drift-detector)"
      contains: "maintenance_problem"
  key_links:
    - from: "vigil-core/src/routes/work-orders.ts (sanitizer destructure ~lines 85-104)"
      to: "vigil-core/src/db/schema.ts (workOrders.maintenanceProblem + .department)"
      via: "SanitizedWorkOrder type + insert/update payload field names"
      pattern: "maintenanceProblem"
    - from: "vigil-core/src/routes/work-orders.ts (dbInsertOrGet + dbUpsertLegacy)"
      to: "workOrders table"
      via: "INSERT VALUES + ON CONFLICT DO UPDATE SET include maintenanceProblem + department"
      pattern: "(dbInsertOrGet|dbUpsertLegacy).*maintenanceProblem"
---

<objective>
Add `maintenance_problem` and `department` nullable text columns to `work_orders` via Drizzle migration 0022 and propagate both fields end-to-end through the sync write path: schema.ts declaration → `SanitizedWorkOrder` interface → sanitizer destructure → `dbInsertOrGet` INSERT VALUES → `dbUpsertLegacy` ON CONFLICT DO UPDATE SET. This unblocks plans 129.1-03 (screenshot endpoint writes these) and 129.1-05 (PWA manual-create posts these).

Purpose: WO-MANUAL-03 requires the new columns to flow through the existing `/v1/work-orders/sync` route without being silently dropped by the 10-field whitelist sanitizer at lines 85-104. Migration 0022 is a strict additive change — existing rows unaffected (nullable), re-run safe (IF NOT EXISTS).

Output: 1 migration SQL file, 1 schema.ts edit (additive), 1 work-orders.ts edit touching three sites (SanitizedWorkOrder interface ~lines 22-35, sanitizer destructure ~lines 85-104, dbInsertOrGet INSERT VALUES + dbUpsertLegacy SET clause ~lines 313-357), and extended tests asserting the round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-CONTEXT.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md
@.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md
@vigil-core/drizzle/0021_add_work_orders_client_capture_id.sql
@vigil-core/src/db/schema.ts
@vigil-core/src/routes/work-orders.ts
@vigil-core/src/routes/work-orders.test.ts

<interfaces>
<!-- Key contracts the executor must extend, extracted from work-orders.ts. -->
<!-- Match line-range references in PATTERNS.md §"vigil-core/src/routes/work-orders.ts MODIFY" -->

SanitizedWorkOrder interface lives at lines 22-35 of vigil-core/src/routes/work-orders.ts.
Current fields: userId, caseNumber, store, shortDescription, trade, location, equipment, priority, contact, state, clientCaptureId, syncedAt.
ADD: maintenanceProblem: string | null; department: string | null;

The sanitizer destructure at lines 85-104 is a STRICT WHITELIST. Header comment line 81 reads:
"Mass-assignment defense: destructure exactly the 10 known fields (T-129-15 / T-66-02)".
ADD destructure entries (after clientCaptureId, before syncedAt):
  maintenanceProblem: wo.maintenanceProblem != null ? String(wo.maintenanceProblem) : null,
  department: wo.department != null ? String(wo.department) : null,
After this change, update the comment to read "12 known fields" so future maintainers know the count.

dbInsertOrGet lives ~lines 313-334. Pattern: SELECT-by-(userId, clientCaptureId) short-circuit, then db.insert(workOrders).values(input).returning().
dbUpsertLegacy lives ~lines 336-357. Pattern: db.insert(workOrders).values(input).onConflictDoUpdate({target: workOrders.caseNumber, set: {...}}).returning().
Both routes pass `input: SanitizedWorkOrder`. The INSERT VALUES will automatically pick up new sanitizer fields because the type now includes them. BUT the dbUpsertLegacy SET clause is an explicit field list — must add:
  maintenanceProblem: input.maintenanceProblem,
  department: input.department,

Migration 0021 template (vigil-core/drizzle/0021_add_work_orders_client_capture_id.sql) header pattern:
- `-- ── Phase 129: SVCNOW-04 client_capture_id dedup ──`
- ALTER TABLE "work_orders" ADD COLUMN IF NOT EXISTS "client_capture_id" text;
- `--> statement-breakpoint` between statements (REQUIRED — drizzle-kit collapses without it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author migration 0022 + extend schema.ts workOrders block</name>
  <files>vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql, vigil-core/src/db/schema.ts</files>
  <read_first>
    - vigil-core/drizzle/0021_add_work_orders_client_capture_id.sql (header + IF NOT EXISTS pattern; reproduce verbatim)
    - vigil-core/src/db/schema.ts (workOrders table block ~lines 308-331; clientCaptureId is the last column added)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"Schema Migration 0022" (lines 696-747)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-core/drizzle/0022_..." (header pattern landmine: `--> statement-breakpoint` REQUIRED)
  </read_first>
  <behavior>
    - Migration 0022 contains TWO `ALTER TABLE "work_orders" ADD COLUMN IF NOT EXISTS` statements (one per column), separated by `--> statement-breakpoint`.
    - Header comment block names Phase 129.1 + requirement WO-MANUAL-03 + SCAP-* (forward reference to plan 03).
    - Both columns are nullable `text` (no `.notNull()`, no `default`).
    - schema.ts adds `maintenanceProblem: text("maintenance_problem")` and `department: text("department")` immediately after `clientCaptureId` in the workOrders block.
    - Running `npx drizzle-kit generate --dry` from `vigil-core/` reports zero diff (schema and migrations are in sync).
  </behavior>
  <action>
Create `vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql` mirroring 0021's header (date, phase, requirement). Use TWO statements separated by the literal line `--> statement-breakpoint`:

  ALTER TABLE "work_orders" ADD COLUMN IF NOT EXISTS "maintenance_problem" text;
  --> statement-breakpoint
  ALTER TABLE "work_orders" ADD COLUMN IF NOT EXISTS "department" text;

Header references: Requirement: WO-MANUAL-03 + SCAP-* (downstream in 129.1-03/05); Phase: 129.1-svcnow-revert-screenshot-pipeline.

In `vigil-core/src/db/schema.ts`, locate the `workOrders` table block (approximately lines 308-331). Immediately after the `clientCaptureId: text("client_capture_id"),` line (and before the closing `}, (table) => [`), add two lines:

  maintenanceProblem: text("maintenance_problem"),
  department: text("department"),

Both nullable (no `.notNull()`, no `.default(...)`) — matches migration nullability.

After both edits, run `cd vigil-core && npx drizzle-kit generate --dry` and confirm output is "No schema changes, nothing to migrate". If divergence reported, fix the schema or migration to match.

Do NOT add an index on either column (per RESEARCH §5 — not query keys).
  </action>
  <acceptance_criteria>
    - File `vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql` exists and contains the substring `ADD COLUMN IF NOT EXISTS "maintenance_problem" text`.
    - Same file contains the substring `ADD COLUMN IF NOT EXISTS "department" text`.
    - Same file contains the literal line `--> statement-breakpoint` between the two ALTER statements (grep `-c -- '--> statement-breakpoint'` returns >= 1).
    - `vigil-core/src/db/schema.ts` contains the substring `maintenanceProblem: text("maintenance_problem")` and `department: text("department")` (both must appear inside the `workOrders = pgTable(...)` block — verify with `awk '/workOrders = pgTable/,/^\}, \(table\)/'` includes both).
    - Neither column declaration in schema.ts contains `.notNull()` or `.default(`.
    - Running `cd vigil-core && npx drizzle-kit generate --dry --config drizzle.config.ts 2>&1` outputs no migration-pending text (no new migration file proposed).
    - Migration file does NOT create any index (grep `-i 'create.*index' vigil-core/drizzle/0022_...sql` returns 0 lines).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-core && npx drizzle-kit generate --dry 2>&1 | tee /tmp/drizzle-dry.log && grep -q 'maintenance_problem' drizzle/0022_add_work_orders_maintenance_problem_department.sql && grep -q 'department' drizzle/0022_add_work_orders_maintenance_problem_department.sql && grep -q 'maintenanceProblem: text("maintenance_problem")' src/db/schema.ts && grep -q 'department: text("department")' src/db/schema.ts && grep -c -- '--> statement-breakpoint' drizzle/0022_add_work_orders_maintenance_problem_department.sql</automated>
  </verify>
  <done>Migration 0022 + schema.ts both contain the two new columns; drizzle-kit dry-generate is clean; no index added; both columns nullable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SanitizedWorkOrder + sanitizer destructure + dbInsertOrGet + dbUpsertLegacy</name>
  <files>vigil-core/src/routes/work-orders.ts, vigil-core/src/routes/work-orders.test.ts</files>
  <read_first>
    - vigil-core/src/routes/work-orders.ts (read full file; pay attention to SanitizedWorkOrder ~lines 22-35, the sanitizer comment at ~line 81 "Mass-assignment defense: destructure exactly the 10 known fields", the destructure ~lines 85-104, and both dbInsertOrGet and dbUpsertLegacy ~lines 313-357)
    - vigil-core/src/routes/work-orders.test.ts (read full file; understand existing test fixture shape and mock-db pattern; identify the most analogous existing test to extend)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-PATTERNS.md §"vigil-core/src/routes/work-orders.ts MODIFY" — THE CRITICAL LANDMINE block (silently-drop-2-fields failure mode)
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-RESEARCH.md §"work-orders.ts sanitizer update" (lines 749-779)
  </read_first>
  <behavior>
    - `SanitizedWorkOrder` interface declares `maintenanceProblem: string | null` and `department: string | null`.
    - Sanitizer destructure includes both fields, coerced via `String(...)` when present else `null`.
    - `dbInsertOrGet` INSERT VALUES carries both fields through `input` (typed correctly).
    - `dbUpsertLegacy` ON CONFLICT DO UPDATE SET clause explicitly includes `maintenanceProblem: input.maintenanceProblem` and `department: input.department`.
    - The "10 known fields" comment at ~line 81 updated to "12 known fields".
    - New unit test asserts: when POST /v1/work-orders/sync receives `{workOrders: [{caseNumber: "TEST001", maintenanceProblem: "HVAC", department: "Bakery"}]}`, the captured INSERT payload includes both fields with those exact values (not undefined, not null).
    - New unit test asserts: when POST receives the same caseNumber a second time with different values, the dbUpsertLegacy SET clause updates BOTH new fields (capturing the SET-args mock proves this).
  </behavior>
  <action>
Open `vigil-core/src/routes/work-orders.ts`. Make four atomic edits:

1. In the `SanitizedWorkOrder` interface (~lines 22-35), after `clientCaptureId: string | null;` add:
     maintenanceProblem: string | null;
     department: string | null;

2. Update the comment at ~line 81 from "10 known fields" to "12 known fields" so the count stays accurate.

3. In the sanitizer destructure (~lines 85-104), after the `clientCaptureId: ...` line and before `syncedAt: new Date()`, add:
     maintenanceProblem: wo.maintenanceProblem != null ? String(wo.maintenanceProblem) : null,
     department: wo.department != null ? String(wo.department) : null,

4. In `dbUpsertLegacy` (~lines 336-357), inside the `.onConflictDoUpdate({target: workOrders.caseNumber, set: { ... }})` SET object, add:
     maintenanceProblem: input.maintenanceProblem,
     department: input.department,

`dbInsertOrGet` passes `input` whole to `db.insert(workOrders).values(input)` and does NOT need a per-field edit — typed correctly via the extended `SanitizedWorkOrder`.

Then open `vigil-core/src/routes/work-orders.test.ts`. Add two new tests (extend an existing `describe` block; do not introduce a new file):

Test A — "sanitizer carries maintenanceProblem + department through to dbInsertOrGet":
  - Build a mock-db that captures the values passed to `db.insert(workOrders).values(...)`.
  - POST `{workOrders: [{caseNumber: "TEST-CS001", clientCaptureId: "uuid-a", maintenanceProblem: "HVAC", department: "Bakery", state: "open", shortDescription: "x"}]}`.
  - Assert: captured insert payload has `maintenanceProblem === "HVAC"` and `department === "Bakery"` (not undefined, not null).

Test B — "dbUpsertLegacy SET clause updates maintenanceProblem + department on case_number conflict":
  - Build a mock-db that captures the `set` object passed to `.onConflictDoUpdate({target, set})`.
  - POST the same caseNumber WITHOUT clientCaptureId (forces legacy path) twice with different values for `maintenanceProblem` and `department`.
  - Assert: captured SET object has both fields with the second-POST's values.

Follow existing test file conventions: `node:test` runner, `assert.equal`/`assert.deepEqual`, the same DI-factory entry point already used by other tests in this file. Do NOT introduce vitest into vigil-core (it uses `node:test`).
  </action>
  <acceptance_criteria>
    - `vigil-core/src/routes/work-orders.ts` contains the substring `maintenanceProblem: string | null;` inside the `interface SanitizedWorkOrder` block (verify: file contains `interface SanitizedWorkOrder` and within ~30 lines after also contains `maintenanceProblem`).
    - Same file contains the substring `department: string | null;` inside the same interface.
    - Sanitizer destructure includes the literal `maintenanceProblem: wo.maintenanceProblem != null ? String(wo.maintenanceProblem) : null,`.
    - Sanitizer destructure includes the literal `department: wo.department != null ? String(wo.department) : null,`.
    - dbUpsertLegacy SET clause contains the literal `maintenanceProblem: input.maintenanceProblem,` and `department: input.department,`.
    - Comment at the sanitizer block reads "12 known fields" (not "10 known fields").
    - `vigil-core/src/routes/work-orders.test.ts` contains at least one test name string referencing `maintenance_problem` or `maintenanceProblem`, AND at least one assertion of `"Bakery"` or `"HVAC"` to prove the round-trip.
    - `cd vigil-core && npm test -- --test-name-pattern 'work-orders' 2>&1 | tee /tmp/wo-test.log` exits 0 (all work-orders tests pass).
    - `cd vigil-core && npm run build 2>&1` exits 0 (TypeScript compiles — proves the SanitizedWorkOrder type extension is consistent with all callers).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief/vigil-core && grep -q 'maintenanceProblem: string | null' src/routes/work-orders.ts && grep -q 'department: string | null' src/routes/work-orders.ts && grep -q 'maintenanceProblem: wo.maintenanceProblem' src/routes/work-orders.ts && grep -q 'department: wo.department' src/routes/work-orders.ts && grep -q 'maintenanceProblem: input.maintenanceProblem' src/routes/work-orders.ts && grep -q '12 known fields' src/routes/work-orders.ts && grep -qE 'maintenance_problem|maintenanceProblem' src/routes/work-orders.test.ts && npm test 2>&1 | tee /tmp/vigil-core-test.log | tail -50 && grep -q 'pass' /tmp/vigil-core-test.log</automated>
  </verify>
  <done>SanitizedWorkOrder interface + sanitizer destructure + dbUpsertLegacy SET clause all carry the 2 new fields; comment updated to "12 known fields"; tests pass; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → vigil-core /v1/work-orders/sync | Untrusted JSON body crosses bearerAuth dispatcher; sanitizer destructure is the mass-assignment defense |
| vigil-core → Postgres | Drizzle ORM parameter binding; column-name allowlist via schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129.1-01 | Tampering | sanitizer destructure | mitigate | The 10→12 field whitelist remains a strict allowlist; never use `...wo` spread; the new field-add comment updated to "12 known fields" so the invariant stays auditable. |
| T-129.1-02 | Information Disclosure | new text columns | accept | `maintenance_problem` and `department` are operator-typed business-context strings, not PII or secrets; no encryption-at-rest tier needed. |
| T-129.1-03 | Tampering | migration 0022 | mitigate | `ADD COLUMN IF NOT EXISTS` makes the migration idempotent; nullable text means no rewrite of existing rows; Railway deploy applies migration on boot before route code references the new columns (Risk 8 in RESEARCH). |
| T-129.1-04 | Denial of Service | column-add lock | accept | `ADD COLUMN ... text` (nullable, no default) in Postgres is a metadata-only operation (no table rewrite); zero downtime risk at single-user prod volume. |

</threat_model>

<verification>

**Goal-backward verification (must_haves):**

1. Truth: "POST /v1/work-orders/sync persists both columns" → covered by Task 2 Test A (sanitizer destructure + dbInsertOrGet INSERT VALUES capture).
2. Truth: "drizzle-kit generate --dry reports no divergence" → covered by Task 1 automated verify.
3. Truth: "existing rows survive migration" → covered by `ADD COLUMN IF NOT EXISTS ... text` (nullable, no default = no table rewrite).
4. Artifact: migration 0022 SQL file exists → Task 1 grep verify.
5. Artifact: schema.ts contains both new columns → Task 1 grep verify.
6. Artifact: work-orders.ts SanitizedWorkOrder + sanitizer + dbUpsertLegacy all carry both fields → Task 2 grep + test verify.
7. Artifact: work-orders.test.ts asserts round-trip → Task 2 grep + npm test verify.
8. Key link: sanitizer → dbInsertOrGet/dbUpsertLegacy → workOrders columns → Task 2 verifies via the captured-insert-payload assertion.

**Phase-level checks:**

- `cd vigil-core && npm test` exits 0.
- `cd vigil-core && npm run build` exits 0 (TypeScript proves SanitizedWorkOrder extension propagated correctly).
- `cd vigil-core && npx drizzle-kit generate --dry` reports zero diff.

</verification>

<success_criteria>

- Migration 0022 SQL exists; both columns declared with `IF NOT EXISTS` + nullable text + `--> statement-breakpoint` separator.
- schema.ts workOrders block declares both columns as plain `text(...)` (nullable).
- SanitizedWorkOrder interface declares both fields as `string | null`.
- Sanitizer destructure includes both with the coerce-or-null pattern; comment updated to "12 known fields".
- dbUpsertLegacy SET clause explicitly includes both fields (dbInsertOrGet inherits via the typed `input`).
- Tests in work-orders.test.ts assert both fields survive the sync round-trip (insert AND upsert).
- `npm test` and `npm run build` both green in vigil-core.
- drizzle-kit dry-generate reports clean.

</success_criteria>

<output>
Create `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-01-SUMMARY.md` when done.
</output>
