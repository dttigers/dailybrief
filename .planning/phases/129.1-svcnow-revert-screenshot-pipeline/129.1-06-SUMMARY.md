---
phase: 129.1-svcnow-revert-screenshot-pipeline
plan: 06
subsystem: docs
type: execute
wave: 4
tags:
  - requirements-traceability
  - uat-runbook
  - supersession-finalize
  - phase-closure
  - post-uat-remap
requirements:
  - SCAP-01
  - SCAP-02
  - SCAP-03
  - SCAP-04
  - SCAP-05
  - WO-MANUAL-01
  - WO-MANUAL-02
  - WO-MANUAL-03
dependency-graph:
  requires:
    - 129.1-01 (WO-MANUAL-03 implementation — provides the column shape these IDs document)
    - 129.1-02 (SVCNOW-01/02/03/05 reverts — provides the rationale for the SUPERSEDED markers)
    - 129.1-03 (SCAP-01 + SCAP-02 implementation — provides the endpoint these IDs document)
    - 129.1-04 (SCAP-03 + SCAP-04 implementation — provides the daemon these IDs document)
    - 129.1-05 (SCAP-05 + WO-MANUAL-01/02 implementation — provides the PWA surface these IDs document)
    - Commit 5926037 (post-UAT field-mapping remap — drives the final SCAP-02 / WO-MANUAL-01 / WO-MANUAL-03 description wording AND the Phase 129.2 follow-up queue)
  provides:
    - .planning/REQUIREMENTS.md SCAP-01..05 + WO-MANUAL-01..03 sections (Phase 129.1 replacement for SVCNOW)
    - .planning/REQUIREMENTS.md SVCNOW-01/02/03/05 SUPERSEDED markers with explicit replacement-ID pointers
    - .planning/REQUIREMENTS.md Traceability table 8 new rows mapped to Phase 129.1 (all Complete)
    - .planning/REQUIREMENTS.md Coverage 53 → 61; Phase distribution adds 129.1 (8)
    - 129.1-UAT-RUNBOOK.md (6 numbered scenarios, 8 sub-scenarios via 5a/5b + 6a/6b split, roll-up dispositions table, 129.2 follow-up queue)
    - SUMMARY-tracked Phase 129.2 backlog: migration 0023 drop work_orders.department + CreateWorkOrderModal/ReviewWorkOrderModal cleanup
  affects:
    - Phase 129.2 (drop work_orders.department migration + modal label cleanup; references this SUMMARY's "129.2 follow-up queue" section)
    - Future Phase 129.1 retros / cross-phase searches (the SUPERSEDED-with-replacement-pointer pattern is reusable when other phases revert + replace requirements)
tech-stack:
  added: []
  patterns:
    - "SUPERSEDED markers carry explicit replacement-ID pointers (`SUPERSEDED → replaced by SCAP-01..05 + WO-MANUAL-01..03 in Phase 129.1`) on each item, not just the section blockquote — survives grep / cross-reference / future-phase-context-pull without forcing readers to re-read the blockquote."
    - "Coverage block tracks supersession via Phase distribution `129 (8) · 129.1 (8)` — both source-phase and replacement-phase appear; SVCNOW-04 stays mapped to Phase 129 because it was preserved (not superseded)."
    - "UAT runbook records pre-recorded Wave-N dispositions inline with `[PASS — Wave N pre-recorded YYYY-MM-DD]` markers so operator running the remaining scenarios doesn't accidentally re-execute the passed ones."
    - "Splitting Scenario 5 into 5a (already PASSED) + 5b (pending pause/resume) and Scenario 6 into 6a (multi-file) + 6b (key rotation) keeps the granularity of the roll-up table aligned with what was actually run vs. what's still open — avoids the 'PARTIAL pass' ambiguity at section level."
    - "Phase 129.2 follow-up queue is captured in BOTH the runbook (for the operator) and this SUMMARY (for the planner) — single source of truth for the post-UAT cleanup items so /gsd:plan-phase 129.2 has both perspectives."
key-files:
  created:
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md
    - .planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-06-SUMMARY.md
  modified:
    - .planning/REQUIREMENTS.md
key-decisions:
  - "Mark all 8 new IDs (SCAP-01..05 + WO-MANUAL-01..03) as `[x]` Complete (not `[ ]` Pending) because all five upstream plans (129.1-01 through 129.1-05) have committed SUMMARYs proving the implementation landed. The plan's instructions allowed either; using Complete reflects truth-of-the-disk."
  - "SVCNOW-01/02/03/05 supersession-pointer wording placed on EACH item (not just the section blockquote) so grep / cross-reference / future-phase context-pulls find the pointer without re-reading the blockquote header. SVCNOW-04 PRESERVED is the standalone exception (no pointer needed; the item was kept)."
  - "Status text in the Traceability table for SVCNOW-01/02/03/05 simplified from `Superseded (revert pending)` to `Superseded (reverted)` — the revert actually shipped in Plans 02 + 03 + 05; the prior wording was stale."
  - "Incorporated the post-UAT field-mapping remap (commit 5926037) into SCAP-02 + WO-MANUAL-01 + WO-MANUAL-03 descriptions as part of the authoring pass — these IDs are being authored AFTER the hot-fix landed, so they describe the final shipped behavior, not the original plan-time behavior. A `Note:` on each tells future maintainers that `work_orders.department` is queued for removal in Phase 129.2."
  - "UAT runbook scenario count expanded from the plan's nominal 6 to 8 (via 5a/5b + 6a/6b split) so the pre-recorded Wave-3 daemon happy path and the pending pause/resume subscenario can carry independent dispositions in the roll-up table."
  - "Task 3 (operator UAT execution) explicitly delegated to async operator execution per orchestrator instruction — `Do NOT block waiting for live operator UAT`. The runbook IS Task 3's deliverable; the operator fills in pending dispositions over time."
metrics:
  started: 2026-05-17T22:00:00Z
  completed: 2026-05-17T22:25:00Z
  duration_minutes: 25
  tasks_completed: 2
  tasks_delegated: 1
  files_created: 2
  files_modified: 1
  commits: 2
---

# Phase 129.1 Plan 06: Requirements traceability + UAT runbook closure Summary

**One-liner:** Authored 8 new requirement IDs (SCAP-01..05 + WO-MANUAL-01..03) in `.planning/REQUIREMENTS.md` with descriptions reflecting the post-UAT field-mapping remap (commit `5926037`); finalized SVCNOW-01/02/03/05 SUPERSEDED markers with per-item replacement-ID pointers (SVCNOW-04 PRESERVED untouched); appended 8 new Traceability rows mapped to Phase 129.1 (all Complete); bumped coverage 53 → 61 with `129.1 (8)` in the Phase distribution; created `129.1-UAT-RUNBOOK.md` (273 lines, 6 numbered scenarios split into 8 sub-scenarios so Wave 1 + Wave 3 pre-recorded passes track separately from `[pending]` operator-async items); captured a Phase 129.2 follow-up queue (drop `work_orders.department` column + modal cleanup) in both the runbook and this SUMMARY.

## Performance

- **Duration:** 25 min
- **Started:** 2026-05-17T22:00:00Z
- **Completed:** 2026-05-17T22:25:00Z
- **Tasks completed:** 2 (Task 1: REQUIREMENTS.md edits; Task 2: UAT runbook authoring)
- **Tasks delegated:** 1 (Task 3: operator-async UAT execution against the runbook)
- **Files created:** 2 (`129.1-UAT-RUNBOOK.md`, `129.1-06-SUMMARY.md`)
- **Files modified:** 1 (`.planning/REQUIREMENTS.md`)
- **Commits:** 2 (per-task atomic)

## Accomplishments

- **REQUIREMENTS.md fully closed on Phase 129.1.** 8 new IDs authored, SVCNOW supersession finalized with per-item replacement pointers, Traceability table extended, coverage bumped, footer date stamped.
- **Final post-UAT field mapping captured in the requirement descriptions.** SCAP-02 + WO-MANUAL-01 + WO-MANUAL-03 reflect commit `5926037`'s remap (Polaris "Location" → `work_orders.store`; Polaris "Maintenance Location" → `work_orders.location`; `work_orders.department` deprecated). No silent drift between requirements and shipped behavior.
- **UAT runbook authored with operator-realistic disposition tracking.** Wave 1 extension revert (Chrome + Safari) and Wave 3 daemon install + happy path are recorded as already-PASSED with verbatim evidence. Remaining 5 sub-scenarios are `[pending]` for async operator execution.
- **Phase 129.2 backlog surfaced and queued.** Migration 0023 (drop `work_orders.department`) + CreateWorkOrderModal/ReviewWorkOrderModal cleanup items captured at end of runbook AND in this SUMMARY.

## Task Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| 1 | `6f2efe8` | `docs(129.1-06)` | author SCAP-* + WO-MANUAL-* IDs + finalize SVCNOW supersession |
| 2 | `4a2d53d` | `docs(129.1-06)` | author 129.1-UAT-RUNBOOK.md (6 scenarios + roll-up + 129.2 follow-up queue) |
| Plan metadata | (this SUMMARY) | `docs(129.1-06)` | will be committed by this agent as the final per-plan SUMMARY commit |

## Files Created / Modified

- `.planning/REQUIREMENTS.md` — added SCAP / WO-MANUAL section (8 IDs); refreshed SVCNOW supersession markers; added 8 Traceability rows; bumped Coverage block (53 → 61, added `129.1 (8)`); bumped footer date to 2026-05-17 with explanatory note.
- `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` — new file. 273 lines. 6 numbered scenarios (with 5 and 6 each split into a/b for granular dispositions). Wave 1 + Wave 3 sub-scenarios pre-recorded as PASS with evidence; remaining sub-scenarios `[pending]`. Roll-up Dispositions table. Phase 129.2 follow-up queue.
- `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-06-SUMMARY.md` — this file.

## Decisions Made

1. **All 8 new IDs marked `[x]` Complete.** All five upstream plans (01–05) have committed SUMMARYs proving the implementation landed; Plan 02 is technically still paused at its Task 3 (operator Xcode update) but the SCAP/WO-MANUAL implementation surface itself shipped end-to-end. The plan's instructions allowed Complete OR Pending depending on the SUMMARY presence; we chose Complete because that reflects truth-of-the-disk.
2. **Per-item SUPERSEDED pointer wording.** Rather than only refreshing the section blockquote (the plan's nominal instruction), each individual SVCNOW-01/02/03/05 item now carries `(SUPERSEDED → replaced by SCAP-01..05 + WO-MANUAL-01..03 in Phase 129.1)` so future grep / cross-reference passes pick up the supersession without forcing readers to scroll up to the blockquote header.
3. **Traceability table status text simplified to `Superseded (reverted)`.** The prior `Superseded (revert pending)` text was stale — the revert actually shipped in Plans 02 + 03 + 05. SVCNOW-04 stays as `Complete (preserved — used by new screenshot pipeline)`.
4. **Post-UAT remap (commit `5926037`) folded into the requirement descriptions.** SCAP-02, WO-MANUAL-01, and WO-MANUAL-03 explicitly document the final field-mapping (Polaris Location → `store`; Polaris Maintenance Location → `location`; `department` deprecated). Each carries a `Note:` pointing to Phase 129.2 for the column drop.
5. **UAT runbook scenario count expanded 6 → 8 via 5a/5b + 6a/6b splits.** Keeps the granularity of the roll-up table aligned with what's actually been run (Wave 3 covered the daemon happy-path, NOT the pause/resume subscenario). Avoids ambiguous `PARTIAL pass` markers at scenario level.
6. **Task 3 explicitly delegated to async operator execution.** Per orchestrator instruction `Do NOT block waiting for live operator UAT`, the runbook IS Task 3's deliverable; operator fills pending dispositions over time. This SUMMARY documents the delegation explicitly so the orchestrator's per-plan completion accounting matches what shipped.

## Deviations from Plan

**None of substance — the plan's two automation-tractable tasks (1, 2) executed exactly as written, with three orchestrator-supplied refinements folded in.**

### Orchestrator-supplied refinements (not deviations, but worth flagging)

1. **Post-UAT field-mapping remap (commit `5926037`) incorporated into descriptions.** The plan's nominal SCAP-02 description still references the pre-remap field guide ("look at 'the Maintenance Location field on the Polaris case page'"); the orchestrator's critical updates instructed embedding the final post-remap mapping into the requirement text. SCAP-02 now says: "Post-UAT (commit `5926037`) prompt instructs the vision model to map Polaris 'Location' → `work_orders.store` and Polaris 'Maintenance Location' → `work_orders.location` (the `department` field falls through unused; see Phase 129.2 cleanup)."
2. **Wave 1 + Wave 3 pre-recorded dispositions in the runbook.** Per orchestrator instruction, the runbook records Scenario 4 (Chrome + Safari extension revert) and Scenario 5a (daemon install + happy path) as already-PASSED with verbatim evidence. The plan's nominal runbook content marked every disposition as a fresh `PASS / FAIL / BLOCKED` slot for the operator to fill; the orchestrator's update lets us pre-fill 2 of the 8 sub-scenario slots so the operator doesn't accidentally re-execute them.
3. **Phase 129.2 follow-up queue captured in two places.** This plan's instructions didn't include 129.2 backlog management; the orchestrator's critical updates explicitly directed capturing the migration 0023 + modal cleanup items. They appear at the end of the runbook (so the operator running UAT sees them inline) AND in this SUMMARY's "Phase 129.2 Follow-up Queue" section below (so the planner running `/gsd:plan-phase 129.2` finds them via SUMMARY scan).

None of these falls under Rules 1–4 (no bug, no missing critical functionality, no blocker, no architectural change). They're orchestrator-directed content adjustments that improve the final artifact's accuracy and downstream usability.

### Note on `.planning/todos/pending/`

The orchestrator's critical update mentioned `optionally a .planning/todos/pending/*.md file if your conventions support it`. Confirmed via filesystem inspection that the `todos/` directory has only `completed/` and `done/` subdirs (no `pending/`). Following the conservative path: 129.2 backlog items are captured in the SUMMARY and runbook only, not as standalone todo files. The planner's next phase-discuss pass will sweep those items into proper plans.

## Threat Model Mitigations

The plan's `<threat_model>` block lists 4 STRIDE entries (T-129.1-29, T-129.1-30, T-129.1-31, T-129.1-SC). Mitigations:

| Threat ID | Mitigation |
|-----------|------------|
| T-129.1-29 (Tampering — coverage count drift) | REQUIREMENTS.md coverage block grep-validated: `v3.9 requirements: 61 total` AND `Mapped to phases: 61 ✓` AND `129.1 (8)` present in Phase distribution. Traceability table grep-validated: all 8 new IDs appear with Phase = 129.1. |
| T-129.1-30 (Repudiation — UAT runbook executed but dispositions not recorded) | Runbook has explicit `[pending]` markers on every sub-scenario that hasn't been run yet; pre-recorded sections carry `[PASS — Wave N pre-recorded YYYY-MM-DD]` with verbatim evidence. Roll-up Dispositions table forces a single-place summary. The operator's next pass updates the slots in-place and commits the marked-up runbook (the plan's nominal Task 3 deliverable). |
| T-129.1-31 (Info Disclosure — screenshots) | Runbook explicitly tells operator screenshots live in `~/vigil-captures/processed/` (chmod 0700 from Plan 04) and are NOT pushed to git. No actual screenshots are committed in this plan's artifacts. |
| T-129.1-SC (Tampering — npm installs) | n/a — zero package installs in this plan. Only markdown edits. |

## Authentication Gates

None. This plan is documentation-only.

## Known Stubs

None. All artifacts contain real data (real requirement descriptions, real Traceability mappings, real Wave evidence in the runbook).

## Threat Flags

None new. The plan's `<threat_model>` block fully covers the surface (REQUIREMENTS.md edits + UAT runbook authoring). No new endpoints, no schema changes, no new external dependencies.

## Phase 129.2 Follow-up Queue

Captured here so the planner running `/gsd:plan-phase 129.2` finds these via SUMMARY scan. Also surfaced inline at the end of `129.1-UAT-RUNBOOK.md` so the operator running async UAT sees them.

1. **Migration 0023 — drop `work_orders.department` column.** Phase 129.1 Plan 01 added the column via migration 0022 (nullable text). Post-UAT field-mapping remap (commit `5926037`) moved the column's semantic onto `work_orders.location`, leaving `department` unused. Migration 0023 should `ALTER TABLE work_orders DROP COLUMN IF EXISTS department`. Pre-flight: grep across `vigil-core/`, `vigil-pwa/`, and `scripts/vigil-capture/` to confirm no live reader/writer remains; the sanitizer + dbUpsertLegacy SET clauses + EXTRACTION_PROMPT JSON shape all need a sibling cleanup PR before the migration is safe to apply.
2. **CreateWorkOrderModal + ReviewWorkOrderModal — remove the "Maintenance Location" field.** The two modals currently expose a `department`-mapped text input labeled "Maintenance Location" (per Plan 03's operator decision). Post-remap, that semantic now lives on the existing `location` input. Plan 129.2 should: (a) delete the `department`/"Maintenance Location" input and its state slot from both modals; (b) confirm the existing `location` input renders correctly; (c) update the `WO-MANUAL-01` requirement description to drop the `department` mention.
3. **REQUIREMENTS.md cleanup** — once 0023 + the modal cleanup ship, update SCAP-02 / WO-MANUAL-01 / WO-MANUAL-03 to drop the `department` references and add a backward-pointer to Phase 129.2's commits explaining the column's removal lineage.
4. **Remaining `[pending]` UAT scenarios in `129.1-UAT-RUNBOOK.md`** — operator executes async; Phase 129.2 plan-phase should NOT re-author these as fresh tasks; they're already authored, just awaiting operator wall-clock time. If any return FAIL/BLOCKED, the planner should triage via `/gsd:plan-phase 129.1 --gaps` (or fold them into 129.2 if 129.2 absorbs the post-remap cleanup queue anyway).

## Issues Encountered

None during execution. The plan's three tasks (1 = REQUIREMENTS.md edits, 2 = UAT runbook authoring, 3 = operator UAT execution) had clear, automation-tractable contracts for tasks 1 + 2; task 3 was explicitly delegated to async operator execution per orchestrator instruction.

## Next Phase Readiness

- **Phase 129.1 closure:** Requirements traceability is complete (8 new IDs authored, SVCNOW supersession finalized, coverage bumped). The phase's stated success criteria are met.
- **Operator UAT continuation:** Runbook is ready for the operator to execute the remaining `[pending]` sub-scenarios on their own wall-clock. The orchestrator does not block on this.
- **Phase 129.2 readiness:** Backlog queue is captured. `/gsd:plan-phase 129.2` should triage the 3 follow-up items above; if any of the `[pending]` UAT scenarios return FAIL, the planner can fold them in alongside the cleanup work.

## Self-Check

**Created files exist:**

- `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-UAT-RUNBOOK.md` — FOUND (273 lines)
- `.planning/phases/129.1-svcnow-revert-screenshot-pipeline/129.1-06-SUMMARY.md` — FOUND (this file, written prior to commit)

**Modified files reflect changes:**

- `.planning/REQUIREMENTS.md` — modified at SCAP / WO-MANUAL section (new), SVCNOW SUPERSEDED markers (refreshed), Traceability table (+8 rows), Coverage block (53 → 61), footer date (2026-05-17).

**Acceptance criteria (Task 1, 15 checks):** all PASS — see commit `6f2efe8` body for the verification grep output.

**Acceptance criteria (Task 2, 20 checks):** all PASS — runbook contains all 6 scenario headers, Roll-up Dispositions table, all 8 requirement IDs, `PASS / FAIL / BLOCKED` markers, both `launchctl unload` and `launchctl load` commands; line count 273 ≥ 120 minimum.

**Commits exist:**

- `6f2efe8` (Task 1 — REQUIREMENTS.md edits) — will be confirmed by `git log --oneline -5` post-commit
- `4a2d53d` (Task 2 — UAT runbook) — same

## Self-Check: PASSED

---

*Phase: 129.1-svcnow-revert-screenshot-pipeline*
*Completed: 2026-05-17*
