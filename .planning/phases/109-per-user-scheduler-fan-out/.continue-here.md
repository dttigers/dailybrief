---
context: phase
phase: 109-per-user-scheduler-fan-out
plan: 3
task: 4
total_tasks: 4
status: paused_at_checkpoint
checkpoint_type: human-verify
last_updated: 2026-04-23T19:28:25.705Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `npm test` hang in vigil-core | `src/integration/cross-user-isolation.test.ts` imports `../index.js`, which spawns `setInterval` loops at module load and prevents tsx child-process exit. Surfaced while plan 109-02 executor tried to run the full suite. | advisory | Until fixed: run tests per-file via `npx tsx --test <file>`. Fix path: gate scheduler startup in `index.ts` on `NODE_ENV !== "test"` or split into `buildApp()` factory + bootstrap entrypoint. Not this phase's job. |

<current_state>
Phase 109 (Per-User Scheduler Fan-Out) — mid-plan 3, at a human-verify checkpoint.

- Plans 109-01 and 109-02 are fully complete and committed.
- Plan 109-03 has tasks 1, 2, 3 complete (5 commits: `4ab2d04 → 9054a5d`). Task 4 is a blocking human-verify gate that requires a human to start vigil-core, POST to `/v1/brief/generate`, and confirm the calendar section renders in the resulting PDF (Path A) OR that the brief still generates without a stack trace when no Google OAuth is connected (Path B).
- Build passes (`npm run build` exit 0). 276/276 tests pass per-file across touched + adjacent test files.
- Nothing committed for plan 03 close-out yet — no 109-03-SUMMARY.md, no final metadata commit, SCHED-01 not re-marked (already marked by Plan 01).
</current_state>

<completed_work>

Phase 109 plans:

- **Plan 109-01** — Scheduler fan-out + error isolation. DONE. Commits: `07b6eef`, `40411a9`, `afbbaf4`, `b7f87d7`. Summary: `.planning/phases/109-per-user-scheduler-fan-out/109-01-SUMMARY.md`.
- **Plan 109-02** — Prioritize cache-key scoped by userId + CACHE-01/02 tests. DONE. Commits: `639d37d`, `59b13c5`, `2556d01`. Summary: `.planning/phases/109-per-user-scheduler-fan-out/109-02-SUMMARY.md`.
- **Plan 109-03 tasks 1–3** —
  - Task 1 (thread userId into calendar-service, delete getSeedUserId, rewrite TODO in place): `4ab2d04` (RED) → `4fbf4a9` (GREEN).
  - Task 2 (thread userId through brief-assembly CalendarServiceDeps + assembleAndRender): `df70c7f` (RED) → `327a17d` (GREEN).
  - Task 3 (atomically wire `calendarService: createCalendarService()` into both index.ts:206-213 + routes/brief-generate.ts:40-47, route userId plumbing in routes/calendar.ts, gmail-workorder-service.ts TODO update): `9054a5d` (single commit per D-12 atomicity).
</completed_work>

<remaining_work>

- **Plan 109-03 Task 4 — Human-verify checkpoint.** Choose Path A or Path B below. No commits until human approves.
- **Plan 109-03 close-out** (runs after approval): continuation agent writes `109-03-SUMMARY.md`, updates STATE.md (`state advance-plan`, `state update-progress`, `state record-metric`, `state add-decision`, `state record-session`), updates ROADMAP.md (`roadmap update-plan-progress 109`), makes final metadata commit. SCHED-01 `requirements mark-complete` is idempotent (Plan 01 already marked it).
- **Phase 109 gates and verification** (runs after Plan 03 close-out):
  1. `gsd:code-review` skill on phase 109 (advisory).
  2. Regression gate — run prior-phase test files.
  3. Schema-drift gate — `verify schema-drift 109`.
  4. Spawn `gsd-verifier` with verifier_model=`sonnet` → writes VERIFICATION.md.
  5. If PASS: `phase complete 109`, evolve PROJECT.md, auto-copy learnings.
  6. Present next-up options — do NOT auto-advance (this run was not --auto).
</remaining_work>

<decisions_made>

- **SCHED-01 marker stays on Plan 01.** Plan 01's executor marked SCHED-01 complete in REQUIREMENTS.md. Plans 02 and 03 also contribute to SCHED-01's success criteria, but `requirements mark-complete` is idempotent and the phase verifier will reconcile at phase close.
- **D-12 atomicity** — Plan 03 Task 3 commits both `index.ts` and `routes/brief-generate.ts` calendarService wiring in a single commit because D-12 requires both call sites to change together. Previously neither path passed calendarService, so briefs have never actually contained Google Calendar events.
- **gmail-workorder-service.ts TODO rewritten in place** (D-15/D-16 hygiene pattern from Phase 108) to note AUTH-06+ deferral — avoids stranding stale seed-user comments after the scaffolding comes out.
</decisions_made>

<blockers>

- **Plan 03 Task 4** — human verification required before Plan 03 can be closed out. This is a checkpoint-type blocker (`autonomous: false` on the plan), not a technical failure. No workaround — D-12 made this the first time the calendar section goes live, so grep + build + tests cannot substitute for eyeballing the rendered PDF.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state for `/gsd-resume-work`.
2. `.planning/phases/109-per-user-scheduler-fan-out/109-03-PLAN.md` — the plan definition, including D-11/D-12/D-13/D-15/D-16 and Task 4 acceptance criteria.
3. `.planning/phases/109-per-user-scheduler-fan-out/109-CONTEXT.md` — phase context, especially §specifics on the latent calendar wiring bug.
4. `.planning/STATE.md` — current progress table.

## Infrastructure State

- **vigil-core** — not running. To verify, start with `cd vigil-core && npm run dev`. Listens on `:3001`.
- **iMac launchd daemon** — the persistent `com.jamesonmorrill.vigilcore` daemon should already be bootout'd from earlier work; double-check with `launchctl print gui/$UID/com.jamesonmorrill.vigilcore 2>/dev/null` before starting `npm run dev` to avoid port 3001 contention.
- **Database** — expected to be live local Postgres (Phase 107.1 setup). No migration pending for Phase 109.
- **Pre-existing uncommitted files** (unrelated to phase 109, do not stage): `vigil-pwa/src/index.css`, `vigil-pwa/tsconfig.tsbuildinfo`.

## How to Verify (Plan 03 Task 4)

Pick ONE path based on whether the seed user has Google OAuth connected locally.

**Path A — Google OAuth connected (proves the functional addition):**

1. Confirm the seed user has an `oauth_tokens` row:
   ```bash
   cd vigil-core && npx tsx -e "import { db } from './src/db/connection.js'; import { oauthTokens, users } from './src/db/schema.js'; import { eq, and } from 'drizzle-orm'; const u = (await db.select().from(users).where(eq(users.email, 'jamesonmorrill1@gmail.com')).limit(1))[0]; const t = await db.select().from(oauthTokens).where(and(eq(oauthTokens.userId, u.id), eq(oauthTokens.provider, 'google'))).limit(1); console.log(t.length > 0 ? 'OAuth row EXISTS' : 'NO OAuth row — use Path B'); process.exit(0);"
   ```
2. `cd vigil-core && npm run dev`
3. `curl -X POST http://localhost:3001/v1/brief/generate -H "Authorization: Bearer <seed_jwt>" --output /tmp/brief-109-manual.pdf`
4. Open `/tmp/brief-109-manual.pdf` — confirm Calendar section is present (empty-state "No events today" is acceptable; fully missing section is not).
5. Tail logs — no "No calendar service" rejection, no stack trace.

**Path B — No Google OAuth (proves graceful degradation):**

1. Start vigil-core, hit `POST /v1/brief/generate` as in Path A step 2-3.
2. PDF still generates successfully. Calendar section renders empty state or is gracefully omitted. At most one `needs_reauth` debug line, no stack trace.

**Acceptance:**
- Path A: PDF renders with visible Calendar section.
- Path B: PDF renders, no crash, no "No calendar service" log line.
- Scheduler spot-check: a `[generate-scheduler] generated brief for YYYY-MM-DD user N (email)` line appears per user with a schedule.

<context>
Phase 109 closes out the multi-user-foundation scaffolding that Phase 102 intentionally hard-scoped to a single seed user. Three plans, tight scope per plan: (01) scheduler fan-out, (02) prioritize cache-key isolation, (03) calendar fold-in + the first-ever calendarService wiring. Plans 01 and 02 landed cleanly. Plan 03 Task 3 was the riskiest commit — single atomic wiring change across scheduler + on-demand paths — and it's done. Task 4 is the intentional checkpoint because grep can confirm the wiring but only a PDF eyeball can confirm the calendar section actually renders.

Resuming should be straightforward: user runs Path A or B, replies "approved" or describes an anomaly, and the continuation agent writes Plan 03's SUMMARY.md and runs the phase-level gates (code-review / regression / schema-drift / gsd-verifier).
</context>

<next_action>
When resuming: pick Path A or Path B above and execute. Reply with either `approved` (proceed to plan close-out + phase verification) or a description of the anomaly (route to gap-closure plan). Continuation agent should resume at Plan 03 Task 4 — do NOT re-run Task 1/2/3 edits; they are committed at `4ab2d04..9054a5d`.
</next_action>
