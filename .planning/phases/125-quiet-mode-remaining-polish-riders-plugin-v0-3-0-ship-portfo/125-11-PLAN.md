---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
plan: 11
type: execute
wave: 4
depends_on: [09]
files_modified:
  - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md
autonomous: false
requirements: [AGENT-DEMO-01]
tags: [phase-125, wallclock, operator, demo-recording, portfolio]
# Wallclock checkpoint per memory feedback_wallclock_checkpoint_exempt:
# Real-hardware single-shot recording. Phone screen capture + worn G2.
# Operator wallclock ONLY.

must_haves:
  truths:
    - "Operator records 60-second portfolio demo clip on real G2 hardware (no sim, no composite)"
    - "Clip follows D-10 shot list: 0:00-0:10 VS Code start, 0:10-0:25 walk away, 0:25-0:35 needs_input banner, 0:35-0:45 double-tap ack, 0:45-0:60 task_complete toast"
    - "Clip uses double-tap to acknowledge (D-08; matches REQUIREMENTS.md AGENT-DEMO-01 amended wording)"
    - "Clip saved to ~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4 (per CONTEXT Claude's Discretion + reference_brand_guidelines)"
    - "Clip path noted in phase artifacts manifest .planning/phases/125-.../artifacts/demo-clip-manifest.md (NOT committed in repo — too large; iCloud is canonical home)"
    - "Clip is single-shot; minor post-production trim only (no composites; brand-honest portfolio piece)"
  artifacts:
    - path: ".planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md"
      provides: "Manifest pointing to the demo clip path in iCloud — clip itself is not committed (binary churn)"
      contains: "2026-05-vigil-v3.8-demo.mp4"
  key_links:
    - from: "AGENT-DEMO-01 amended wording (Plan 08)"
      to: "Demo recording shot list (D-10)"
      via: "double-tap acknowledge"
      pattern: "double-tap"
---

<objective>
Operator wallclock checkpoint per memory `feedback_wallclock_checkpoint_exempt`: record the 60-second portfolio demo clip on real G2 hardware per CONTEXT D-09 + D-10. Single-shot, single-take, real prompt that triggers `needs_input`, double-tap to ack (matches D-08 amendment landed in Plan 08).

Per memory `feedback_g2_tap_expand_broken` / `project_g2_tap_expand_broken`: NO sim screen-record. The portfolio piece must be authentic — recording it on the simulator violates the entire premise (the value prop is "ambient AI on real hardware"). Phone screen-recording of operator wearing G2, with VS Code visible in frame on the laptop.

Per CONTEXT Claude's Discretion + memory `reference_brand_guidelines`: clip lives in `~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4`. NOT committed to repo (size + binary churn). Phase artifacts reference the path; manifest .md is the in-repo handle.

This plan is `autonomous: false`. yolo mode does NOT bypass.

Output:
- 60-second video clip saved to iCloud Drive portfolio path
- 1 NEW file: `.planning/phases/125-.../artifacts/demo-clip-manifest.md` referencing the clip path + recording metadata
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-stage prompt that reliably triggers needs_input + create manifest skeleton</name>
  <read_first>
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md (D-09 lines 246-252; D-10 lines 254-266 — shot list)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 600-604 — Pitfall 7 demo single-shot fragility; pre-stage prompt recommendation)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md (Plan 09 output — Scenario 5 confirmed needs_input flow works on hardware)
  </read_first>
  <files>
    .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md
  </files>
  <action>
**Step 1 — Verify prerequisites.** Confirm Plan 09 Scenario 5 disposition was GREEN:

```bash
awk '/Scenario 5/{found=1} found && /Status:/{print; exit}' .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
```

Expected output contains "green" or "✅" or "GREEN". If yellow/red, surface to user — the demo will likely have the same issue Scenario 5 surfaced.

**Step 2 — Pre-stage prompt (Pitfall 7).** The 60s window is tight. The prompt MUST reliably trigger `needs_input` within ~25 seconds. Per RESEARCH §Pitfall 7: "Pre-stage a prompt known to reliably fire `needs_input` (e.g., a prompt that asks Claude to confirm a dangerous git operation). Test 2x before recording."

Examples of high-yield prompts (Claude Code stops to ask user):
- "Run `rm -rf node_modules` and reinstall — confirm before deleting"
- "Force-push origin/main from a local branch — confirm before push"
- "Drop the production database table users — confirm"

The operator picks a prompt and tests it 2 times BEFORE recording. If the first 3 takes don't get `needs_input` to fire, pause and investigate the prompt.

**Step 3 — Create manifest skeleton.** Create `.planning/phases/125-.../artifacts/demo-clip-manifest.md`:

```markdown
---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
status: pending
recorded_date: <fill on recording>
operator: <fill>
clip_path: ~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4
duration_seconds: <fill>
prompt_used: <fill — short paraphrase of prompt that triggered needs_input>
---

# AGENT-DEMO-01 — 60-second portfolio demo clip manifest

> Per CONTEXT D-09: real hardware single-shot recording (no sim, no composite).
> Per CONTEXT D-10: shot list 0:00-0:10 VS Code start, 0:10-0:25 walk away,
> 0:25-0:35 needs_input banner, 0:35-0:45 double-tap ack, 0:45-0:60 task_complete toast.
> Per memory `reference_brand_guidelines` + Claude's Discretion in CONTEXT.md: clip stored in
> iCloud Drive (NOT committed to repo).

## Recording metadata

- **Date:** (fill)
- **Operator:** (fill)
- **Hardware:** Even Realities G2, firmware 2.2.0.28
- **Plugin version:** 0.3.0 (sideloaded local OR latest dev portal upload)
- **iPhone:** (fill model + iOS version)
- **Mac:** (fill — running vigil-watch + VS Code with Claude Code)
- **Clip duration:** (fill, seconds)
- **Number of takes:** (fill — first-take success rate per Pitfall 7)
- **Post-production:** (fill — trim only? color? cut points?)

## Shot list verification (D-10)

- [ ] 0:00–0:10: VS Code window visible, Claude Code session starts running
- [ ] 0:10–0:25: User stands up, walks away from keyboard. Glasses idle.
- [ ] 0:25–0:35: Temple tap fires. HUD shows banner [NEEDS INPUT] + state line `waiting for input` + last event message
- [ ] 0:35–0:45: User double-taps temple to ack. Banner clears. State returns to `running`. User walks back.
- [ ] 0:45–0:60: User answers in VS Code. Claude Code finishes. HUD shows `task_complete` toast. Clip ends.

## Clip path

Saved to: `~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4`

(NOT committed to git per CONTEXT.md Claude's Discretion — clip is binary, lives in iCloud.)

## Notes / caveats

(fill — anything that would inform future demo recordings)
```

**Step 4 (OPERATOR wallclock — manual).** With prompt staged, real G2 worn, iPhone screen-recording armed, VS Code session prepared:

1. Start screen recording on iPhone (long-press control center → screen record button)
2. Press play on the staged prompt in VS Code (timer starts)
3. Stand up, walk away from keyboard (10s walk away)
4. Wait for temple tap; banner shows on HUD (~15s)
5. Double-tap to ack
6. Walk back, answer Claude Code's prompt
7. Wait for task_complete toast (~15s more)
8. Stop screen recording

Trim to ≤ 60s. Save to `~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4`. Trim only — no composites.

If first take has bugs (missed banner, missed ack, etc.), retake. Tight 60s window per CONTEXT D-10 wording — multi-take okay but each take is single-shot real-hardware.

**Step 5 — Update manifest.** After successful recording, fill in the manifest's metadata fields. Commit the manifest to git.

**Step 6 — Verify clip exists.** From the Mac:

```bash
ls -la "$HOME/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4"
```

Expected: file exists, size > 1MB (60s of 1080p phone video should be tens of MB).

If iCloud sync is in progress, the file may show with a `.icloud` extension — that's also acceptable; the file IS in iCloud, just not yet downloaded to local Mac. Wait for sync OR verify on iPhone Files app.
  </action>
  <verify>
    <automated>test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md && grep -c "2026-05-vigil-v3.8-demo.mp4" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md` exists
    - That file contains the substring `2026-05-vigil-v3.8-demo.mp4`
    - That file contains a citation to memory `feedback_wallclock_checkpoint_exempt`
    - That file's `Shot list verification (D-10)` section has all 5 boxes ticked (`- [x]`) — fillable post-recording by operator
    - **Operator-provided post-recording:** Recording metadata fields are non-empty (date, operator, hardware confirmed, clip duration, takes count, post-production note)
    - **Operator-provided post-recording:** The clip file at `~/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4` exists (verifiable via `test -f` from operator's Mac)
    - The clip is single-shot real-hardware (no sim, no composite — operator attests via the manifest's `Post-production:` line)
    - Clip duration ≤ 60 seconds (per AGENT-DEMO-01 wording; per CONTEXT D-10 strict timing)
  </acceptance_criteria>
  <done>60-second portfolio demo recorded on real G2 hardware; double-tap ack matches v3.8 ship reality; clip saved to iCloud Drive; manifest committed to repo; AGENT-DEMO-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo authenticity ↔ portfolio claims | Per memory project_g2_tap_expand_broken: "do not ship sim-only tap behaviors" — demo MUST be on real hardware to honor brand-honest portfolio framing |
| Local clip ↔ iCloud sync | Clip lives in iCloud; phase artifacts reference path, not the binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-W11-01 | Repudiation | Sim-recorded clip claimed as real-hardware demo | mitigate | Manifest's `Post-production:` line attests real-hardware single-shot; user reviews; falsifying is outside threat model |
| T-125-W11-02 | Information Disclosure | Demo clip in repo bloats git history | mitigate | Clip stored in iCloud per CONTEXT Claude's Discretion; only manifest .md is committed |
| T-125-04 | Denial of Service | needs_input fails to fire reliably during recording (Pitfall 7) | mitigate | Pre-stage prompt + 2x test runs before recording; if first 3 takes fail, pause and re-pick prompt |
</threat_model>

<verification>
```bash
# Manifest exists with correct path reference
test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md
grep "2026-05-vigil-v3.8-demo.mp4" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md

# Clip exists in iCloud (operator side)
test -f "$HOME/Library/CloudStorage/iCloud Drive/Vigil/portfolio/2026-05-vigil-v3.8-demo.mp4"

# Manifest metadata filled (post-recording)
grep -E "Date:|Operator:|Clip duration:" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/demo-clip-manifest.md
```
</verification>

<success_criteria>
- 60-second portfolio demo recorded on real G2 hardware
- Single-shot, single-take (or minimal trim post-production)
- Double-tap ack matches v3.8 ship reality (D-08)
- Clip saved to iCloud Drive portfolio path with naming convention `2026-05-vigil-v3.8-demo.mp4`
- Manifest committed to repo (clip itself NOT committed)
- AGENT-DEMO-01 requirement satisfied
- Operator wallclock checkpoint observed (yolo mode bypass not allowed)
</success_criteria>

<output>
Create `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-11-SUMMARY.md` recording: recording date, takes count, prompt used (paraphrased), clip duration, post-production verdict (trim only / color / cuts), final clip file size, and AGENT-DEMO-01 closure verdict.
</output>
