---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
plan: 08
type: execute
wave: 3
depends_on: [04, 06]
files_modified:
  - vigil-g2-plugin/scripts/check-verified.mjs
  - .planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md
  - vigil-g2-plugin/app.json
  - vigil-g2-plugin/vigil.ehpk
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
  - .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md
autonomous: true
requirements: [G2-PLUGIN-01, G2-POLISH-05, AGENT-DEMO-01]
tags: [phase-125, ship-prep, version-bump, docs-amendments, seed-005, demo-cascade]

must_haves:
  truths:
    - "vigil-g2-plugin/scripts/check-verified.mjs points at the actual VERIFIED.md location (file archived to .planning/milestones/v3.5-phases/106-... after v3.5 close — original .planning/phases/106-... no longer exists)"
    - "VERIFIED.md timestamp refreshed to within last 24h so check-verified.mjs exits 0 (Pitfall 9)"
    - "vigil-g2-plugin/app.json version bumped 0.2.0 -> 0.3.0 (D-11 step 1)"
    - "vigil-g2-plugin/app.json min_sdk_version bumped 0.0.7 -> 0.0.8 IF AND ONLY IF Phase 124's bridge.onLaunchSource (added in SDK 0.0.8) is in use (Pitfall 6 + RESEARCH A1)"
    - "npm run package:ehpk produces vigil-g2-plugin/vigil.ehpk artifact"
    - "REQUIREMENTS.md AGENT-DEMO-01 wording amended: single-tap -> double-tap (D-08)"
    - "ROADMAP.md Phase 125 SC #5 wording amended: single-tap -> double-tap"
    - "PROJECT.md v3.8 milestone target features wording amended: single-tap -> double-tap (or recorded as no-op if PROJECT.md has no demo-tap reference)"
    - "v3.8 spec lines 229-238 amended: single-tap deferred via SEED-011, quiet mode pipeline note replaces Honor iOS Focus state via Even SDK"
    - "REQUIREMENTS.md G2-POLISH-05 wording amended per D-06 fallback: swipe -> documented exit gesture"
    - "SEED-005 file gets a follow-up note documenting the SDK-source-dive negative finding (RESEARCH §SEED-005 SDK Source-Dive Spike)"
  artifacts:
    - path: "vigil-g2-plugin/vigil.ehpk"
      provides: "Submission-ready package for Even Hub developer portal upload (Wave 4)"
      contains: ""
    - path: "vigil-g2-plugin/app.json"
      provides: "Plugin manifest with v0.3.0 + (conditional) min_sdk_version 0.0.8"
      contains: "0.3.0"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AGENT-DEMO-01 + G2-POLISH-05 wordings now match shipped reality"
      contains: "double-tap to acknowledge"
  key_links:
    - from: "scripts/check-verified.mjs VERIFIED_PATH"
      to: ".planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md"
      via: "path constant in mjs file"
      pattern: "milestones.*106-g2-store-resubmit-atomic.*VERIFIED.md"
    - from: "vigil.ehpk artifact"
      to: "Plan 09 hardware retest + Plan 10 Even Hub upload"
      via: "file artifact passed forward"
      pattern: "vigil\\.ehpk"
---

<objective>
Wave 3 ship-prep + docs amendments cascade. Three threads, all complete before Wave 4 starts:

**Thread 1 - Pre-pack gate fixes (RESEARCH Pitfall 9 + A1).** Two real blockers identified by research:
1. `vigil-g2-plugin/scripts/check-verified.mjs` hard-codes `.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md`. After v3.5 close, the file moved to `.planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md`. Pack will fail with "VERIFIED.md not found". Fix: update the path constant.
2. The `Verified:` ISO line must be < 24h old. Refresh.

**Thread 2 - Version + min_sdk_version bumps (D-11).** Phase 124's `bridge.onLaunchSource` was added in SDK 0.0.8 per README changelog. If invoked in `vigil-g2-plugin/src/`, `min_sdk_version` MUST bump 0.0.7 -> 0.0.8. Verify by grep; bump only if confirmed in use.

**Thread 3 - Docs amendments cascade (D-08, D-06 fallback).** Wording cascades:
- REQUIREMENTS.md AGENT-DEMO-01: single-tap -> double-tap (D-08)
- REQUIREMENTS.md G2-POLISH-05: swipe -> documented exit gesture (D-06 fallback)
- ROADMAP.md Phase 125 SC #5: single-tap -> double-tap
- PROJECT.md v3.8 milestone target features: single-tap -> double-tap (or no-op if no reference)
- v3.8 spec §"Interaction" lines 229-233: single-tap deferred via SEED-011
- v3.8 spec §"Quiet mode" line 235: PWA->Core->SSE pipeline note (RESEARCH §State of the Art)
- SEED-005: append SDK-source-dive negative finding follow-up note

After all three threads complete, run `npm run package:ehpk` to produce `vigil.ehpk`.

Output:
- 1 MODIFIED file: `vigil-g2-plugin/scripts/check-verified.mjs` (path fix)
- 1 MODIFIED file: `.planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md` (timestamp refresh)
- 1 MODIFIED file: `vigil-g2-plugin/app.json` (version bump + conditional min_sdk_version bump)
- 1 NEW artifact: `vigil-g2-plugin/vigil.ehpk` (binary, npm run package:ehpk output)
- 5 MODIFIED docs files: REQUIREMENTS.md, ROADMAP.md, PROJECT.md, v3.8 spec, SEED-005
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
@.planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md
@vigil-g2-plugin/scripts/check-verified.mjs
@vigil-g2-plugin/app.json
@vigil-g2-plugin/package.json
@vigil-g2-plugin/src/main.ts
@.planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix check-verified.mjs path + refresh VERIFIED.md timestamp + bump app.json version</name>
  <read_first>
    - vigil-g2-plugin/scripts/check-verified.mjs (FULL FILE — confirm const VERIFIED_PATH layout at lines 11-19)
    - .planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md (FULL FILE — read current Verified line; preserve all other content verbatim)
    - vigil-g2-plugin/app.json (FULL FILE — confirm version + min_sdk_version field positions; preserve all other fields verbatim)
    - vigil-g2-plugin/src/main.ts (lines 73-90 — confirm whether bridge.onLaunchSource is invoked, per RESEARCH A1)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 612-618 Pitfall 9; line 1009 A1 onLaunchSource verification recipe)
  </read_first>
  <files>
    vigil-g2-plugin/scripts/check-verified.mjs,
    .planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md,
    vigil-g2-plugin/app.json
  </files>
  <action>
**Step 1 - Fix check-verified.mjs path.** Current path resolves to `.planning/phases/106-g2-store-resubmit-atomic/VERIFIED.md`. Update the `VERIFIED_PATH` constant to include the `'milestones'` and `'v3.5-phases'` segments:

```javascript
const VERIFIED_PATH = resolve(
  __dirname,
  '..',
  '..',
  '.planning',
  'milestones',
  'v3.5-phases',
  '106-g2-store-resubmit-atomic',
  'VERIFIED.md',
)
```

**Step 2 - Refresh VERIFIED.md timestamp.** Read VERIFIED.md FULL FILE. Find the `Verified:` line near the top. Replace its ISO timestamp with the current local-Mountain time (memory `user_timezone`). Use shell:

```bash
NEW_TS=$(date +%FT%T%z | sed 's/\([+-][0-9][0-9]\)\([0-9][0-9]\)$/\1:\2/')
```

Replace ONLY the timestamp portion of `^Verified: ...` line. Preserve all other content verbatim. Use Edit tool on the file (NOT sed/awk through Bash — Edit gives auditable diff).

**Step 3 - Bump app.json version.** Edit `vigil-g2-plugin/app.json` line 4:
```
"version": "0.3.0",
```
(Was `0.2.0`.)

**Step 4 - Conditionally bump min_sdk_version.** Run:
```bash
grep -rn "bridge\.onLaunchSource\|onLaunchSource" vigil-g2-plugin/src/ | grep -v "\.test\.ts"
```

If matches >= 1 (expected — main.ts:81 has Phase 124 D-07 registration): bump `min_sdk_version` to `"0.0.8"` per RESEARCH A1.

If matches == 0: leave `"0.0.7"` and document the verdict in the plan summary.

**Step 5 - Confirm pre-pack gate now passes.** Run:
```bash
cd vigil-g2-plugin && node scripts/check-verified.mjs
```
Expected: exit 0 with output containing "Proceeding" or "fresh".

**Step 6 - Run plugin tests.** Confirm no regression:
```bash
cd vigil-g2-plugin && npm test
```
Must exit 0.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && node scripts/check-verified.mjs && grep '"version": "0.3.0"' vigil-g2-plugin/app.json && cd vigil-g2-plugin && npm test</automated>
  </verify>
  <acceptance_criteria>
    - vigil-g2-plugin/scripts/check-verified.mjs contains the substring 'milestones' AND the substring 'v3.5-phases'
    - .planning/milestones/v3.5-phases/106-g2-store-resubmit-atomic/VERIFIED.md Verified line ISO timestamp parses to within last 24h
    - vigil-g2-plugin/app.json contains the substring "version": "0.3.0"
    - vigil-g2-plugin/app.json contains EITHER "min_sdk_version": "0.0.8" (if onLaunchSource invoked in src/) OR "min_sdk_version": "0.0.7" (if not — document verdict in summary)
    - cd vigil-g2-plugin and node scripts/check-verified.mjs exits 0 with output containing "Proceeding" or "fresh"
    - cd vigil-g2-plugin and npm test exits 0
  </acceptance_criteria>
  <done>Pre-pack gate now passes (path corrected + timestamp fresh); app.json version 0.3.0; min_sdk_version is honest about SDK API surface usage; plugin tests still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cascade docs amendments — REQUIREMENTS.md, ROADMAP.md, PROJECT.md, v3.8 spec, SEED-005</name>
  <read_first>
    - .planning/REQUIREMENTS.md (FULL FILE — locate AGENT-DEMO-01 line 44 and G2-POLISH-05 line 48)
    - .planning/ROADMAP.md (lines 463-474 — Phase 125 SC #5 wording is at line 472)
    - .planning/PROJECT.md (FULL FILE — search "single-tap" / "single tap" instances; v3.8 milestone target-features block — verify nothing in v3.8 block needs amendment)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md (lines 229-238 — Interaction + Quiet mode sections)
    - .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md (FULL FILE — append at end)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md (D-08 lines 218-238 — exact verbatim AGENT-DEMO-01 wording)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 934-991 — SEED-005 spike output verbatim)
  </read_first>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md,
    .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md,
    .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md
  </files>
  <action>
**Amendment A — REQUIREMENTS.md AGENT-DEMO-01 (line 44).** Use Edit tool to replace `single-tap to acknowledge` with `double-tap to acknowledge` in that line. Final line should read:

`- [ ] **AGENT-DEMO-01**: Full demo flow (start a Claude Code session in VS Code, walk away from keyboard, receive a needs_input tap on the temple, double-tap to acknowledge) is recordable in under 60 seconds for portfolio use.`

(Keep the backticks around `needs_input` — preserve all other markdown.)

**Amendment B — REQUIREMENTS.md G2-POLISH-05 (line 48).** Use Edit tool to replace the existing line with:

`- [ ] **G2-POLISH-05**: List screens have a documented exit gesture (DOUBLE_CLICK → home) with on-screen hint; SDK SCROLL bubble limit acknowledged in SEED-005 follow-up. Resolves SEED-005, hardware regression first observed in Phase 45 hardware UAT.`

**Amendment C — ROADMAP.md SC #5 (line 472).** Use Edit tool to replace `single-tap to acknowledge` with `double-tap to acknowledge` in that line. Final wording mirrors REQUIREMENTS.md AGENT-DEMO-01.

**Amendment D — PROJECT.md v3.8 milestone target features.** Read PROJECT.md FULL FILE. Search for any line in the v3.8 milestone block (look for "v3.8" or "Claude Code Companion" heading + bullet list) containing "single-tap" or "single tap" related to demo/ack. If found, update to "double-tap". If NOT found, record in summary: "PROJECT.md v3.8 block has no demo-tap reference; no amendment needed."

(The earlier grep found line 166 — `G2 resubmit: double-tap exit dialogue per lifecycle docs (G2-02 — code side done)` — that is unrelated to demo cascade and already says double-tap. Skip it.)

**Amendment E — v3.8 spec lines 229-238.** Use Edit tool. Replace §"Interaction" lines 229-233 with:

```
### Interaction

- Double tap on temple: context-sensitive — ack banner if shown, else cycle through active sessions if ≥2, else navigate Home (Phase 124 D-08).
- Single tap (CLICK_EVENT) and long press (LONG_PRESS_EVENT): DEFERRED via SEED-011 — sim-only on G2 hardware in v0.0.9 SDK; original three-tap-variant intent preserved for re-activation when SDK exposes reliable events.
```

Then replace §"Quiet mode" lines 235-238 with:

```
### Quiet mode

Quiet mode is honored on the HUD via a PWA Settings toggle that pipes through Vigil Core to a quiet_mode_changed SSE event consumed by the plugin (Phase 125 / D-01..D-04 — SDK exposes no Focus API as of @evenrealities/even_hub_sdk@0.0.9). When Quiet mode is enabled, only needs_input and task_failed reach the HUD; other events queue silently in a server-side suppression queue and replay in chronological order on toggle-off.
```

**Amendment F — SEED-005 follow-up note.** Read SEED-005 FULL FILE. APPEND at end (preserve all existing content verbatim):

```markdown
## 2026-05-10 SDK source-dive spike (Phase 125 / D-06)

Spike result: NOT FOUND. The Even Hub SDK v0.0.9 (@evenrealities/even_hub_sdk@0.0.9) exposes no list-container bubble flag. ListContainerProperty.isEventCapture is a SINGLE-OWNER routing flag (one container handles events at a time), NOT a bubble flag. Type defs at dist/index.d.ts:280-355 confirm. README isEventCapture semantics (lines 161, 399, 877) confirm.

Resolution: REQUIREMENTS.md G2-POLISH-05 wording amended from "swipe" to "documented exit gesture (DOUBLE_CLICK → home) with on-screen hint". Plugin's existing "() double-tap to exit" footer text (vigil-g2-plugin/src/screens/work-orders.ts:53) is the user-facing affordance.

Re-activation triggers: A future SDK release exposes a bubble flag (bubbleEvents / propagateEvents / equivalent) on ListContainerProperty. At that point: set the flag on wo-list (work-orders.ts:87-98), wire SCROLL_TOP / SCROLL_BOTTOM in handleNavEvent, hardware retest, revert REQUIREMENTS.md amendment.
```

After all amendments, run all 6 grep verification checks listed in the verify block.
  </action>
  <verify>
    <automated>grep -c "double-tap to acknowledge" .planning/REQUIREMENTS.md && grep -c "double-tap to acknowledge" .planning/ROADMAP.md && grep -c "documented exit gesture" .planning/REQUIREMENTS.md && grep -c "PWA Settings toggle" .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md && grep -c "DEFERRED via SEED-011" .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md && grep -c "SDK source-dive spike (Phase 125 / D-06)" .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains the substring "double-tap to acknowledge"
    - .planning/REQUIREMENTS.md contains the substring "documented exit gesture"
    - .planning/REQUIREMENTS.md does NOT contain the substring "single-tap to acknowledge" (line 44 fully replaced)
    - .planning/ROADMAP.md contains the substring "double-tap to acknowledge"
    - .planning/ROADMAP.md does NOT contain the substring "single-tap to acknowledge" (line 472 fully replaced)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md contains the substring "PWA Settings toggle"
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md does NOT contain the substring "Honor iOS Focus state via Even SDK" (line 235 fully replaced)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md contains the substring "DEFERRED via SEED-011"
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md does NOT contain the substring "Single tap on temple: acknowledge" (line 231 fully replaced)
    - .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md contains the substring "SDK source-dive spike (Phase 125 / D-06)"
    - PROJECT.md amendment is either applied (if reference found) OR documented as no-op in plan summary (if no reference found)
  </acceptance_criteria>
  <done>All wording amendments cascaded across REQUIREMENTS.md, ROADMAP.md, PROJECT.md (or no-op recorded), v3.8 spec, SEED-005; source artifacts now match shipped reality (double-tap on G2 hardware; PWA-pipeline quiet mode; documented-exit gesture).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run npm run package:ehpk to produce vigil.ehpk artifact</name>
  <read_first>
    - vigil-g2-plugin/package.json (line "package:ehpk" script — confirm full command chain: check-verified -> build -> pack)
    - vigil-g2-plugin/scripts/check-verified.mjs (verify the path fix from Task 1 is in place)
    - vigil-g2-plugin/app.json (verify version 0.3.0 from Task 1 is in place)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 1042-1058 — Environment Availability; evenhub CLI availability is unverified)
  </read_first>
  <files>
    vigil-g2-plugin/vigil.ehpk
  </files>
  <action>
**Step 1 - Verify environment.** Confirm `evenhub` CLI is available (RESEARCH §Environment Availability flags this as unverified):

```bash
which evenhub || echo "MISSING_EVENHUB_CLI"
```

If MISSING, the operator must install before this task can succeed. Likely paths: `npm install -g @evenrealities/cli` or homebrew. Surface the gap to the operator with the failing command output; do NOT fabricate a fix without the user.

**Step 2 - Clean build artifacts.** Per RESEARCH §Runtime State Inventory:

```bash
cd vigil-g2-plugin && rm -rf dist vigil.ehpk
```

(This ensures stale artifacts from prior packs don't pollute the new build.)

**Step 3 - Run package:ehpk.** This runs check-verified.mjs (Task 1 fix gate) -> build -> evenhub pack:

```bash
cd vigil-g2-plugin && npm run package:ehpk
```

Expected output:
- "[package:ehpk] VERIFIED.md fresh (Nm old). Proceeding."
- TypeScript build success
- evenhub pack success producing `vigil.ehpk`

If the pack fails:
- "VERIFIED.md not found" -> Task 1 path fix incomplete; redo
- "VERIFIED.md is Nh old" -> Task 1 timestamp refresh stale; redo
- "containerName ≤ 16 chars" -> a containerID/Name was added that exceeds the SDK constraint; this phase didn't add new containers, so investigate (Pitfall 5)
- evenhub CLI errors -> escalate to operator

**Step 4 - Confirm artifact.** Verify the ehpk file exists:

```bash
test -f vigil-g2-plugin/vigil.ehpk && stat -f "%z bytes" vigil-g2-plugin/vigil.ehpk
```

Record the file size in the plan summary. Pack file is binary; do NOT commit it to git (gitignored typically).
  </action>
  <verify>
    <automated>test -f vigil-g2-plugin/vigil.ehpk && echo "EHPK_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-g2-plugin/vigil.ehpk` exists after the pack command
    - The file is non-empty (size > 1KB — sanity check that it's a real archive, not a 0-byte placeholder)
    - The pack command exited 0
    - check-verified.mjs ran (visible in pack output) and returned "Proceeding"
    - vigil-g2-plugin/.gitignore includes vigil.ehpk (or it is otherwise excluded from version control — pack artifacts are not committed)
  </acceptance_criteria>
  <done>vigil.ehpk artifact ready for Wave 4 hardware retest (Plan 09) and Even Hub upload (Plan 10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin build artifact ↔ Even Hub portal | The ehpk that lands on Even Hub IS the user-facing software; pack-time validation gates protect production users |
| Source artifact ↔ shipped reality | Wording in REQUIREMENTS / ROADMAP / PROJECT must match what ships, else audit trail breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-W8-01 | Tampering | Stale VERIFIED.md slips through staleness gate | mitigate | check-verified.mjs reads ISO timestamp + compares to now; refresh enforces freshness; Task 1 ensures BOTH path correctness AND timestamp freshness |
| T-125-W8-02 | Repudiation | min_sdk_version contract drift breaks old-SDK users | mitigate | Conditional bump only after grep confirms onLaunchSource is invoked; if not, leave 0.0.7 with documented verdict |
| T-125-W8-03 | Information Disclosure | vigil.ehpk artifact committed to public repo | mitigate | gitignore check is part of acceptance criteria (Task 3); ehpk is the build output, not source |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Path fix landed
grep "milestones" vigil-g2-plugin/scripts/check-verified.mjs
grep "v3.5-phases" vigil-g2-plugin/scripts/check-verified.mjs

# Version bumps
grep '"version": "0.3.0"' vigil-g2-plugin/app.json
grep '"min_sdk_version"' vigil-g2-plugin/app.json

# Pre-pack gate clean
cd vigil-g2-plugin && node scripts/check-verified.mjs

# All amendments
grep -c "double-tap to acknowledge" .planning/REQUIREMENTS.md .planning/ROADMAP.md
grep -c "documented exit gesture" .planning/REQUIREMENTS.md
grep -c "PWA Settings toggle" .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
grep -c "DEFERRED via SEED-011" .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
grep -c "SDK source-dive spike (Phase 125 / D-06)" .planning/seeds/SEED-005-g2-swipe-out-of-list-broken-on-hardware.md

# Artifact exists
test -f vigil-g2-plugin/vigil.ehpk && echo "EHPK_OK"

# All test suites still green
(cd vigil-core && npm test) && (cd vigil-pwa && npm test) && (cd vigil-g2-plugin && npm test)
```
</verification>

<success_criteria>
- check-verified.mjs path corrected to .planning/milestones/v3.5-phases/106-... AND VERIFIED.md timestamp fresh (< 24h)
- vigil-g2-plugin/app.json version === 0.3.0
- vigil-g2-plugin/app.json min_sdk_version is correct given the bridge.onLaunchSource grep verdict (0.0.8 if invoked, 0.0.7 if not)
- All 5 docs amendments landed: REQUIREMENTS.md (2 changes), ROADMAP.md (1 change), PROJECT.md (1 change or no-op), v3.8 spec (2 changes), SEED-005 (append)
- vigil.ehpk artifact exists, > 1KB, gitignored
- Three repo test suites all green (no regression to Plan 02-07 work)
</success_criteria>

<output>
Create `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-08-SUMMARY.md` recording: check-verified.mjs old/new path, VERIFIED.md old/new timestamp, app.json version + min_sdk_version verdict (with grep evidence), pack exit code + ehpk file size, PROJECT.md amendment verdict (applied vs no-op), 6-grep matrix result.
</output>
