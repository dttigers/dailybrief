---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
plan: 09
type: execute
wave: 4
depends_on: [07, 08]
files_modified:
  - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
  - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/.gitkeep
autonomous: false
requirements: [AGENT-HUD-03, G2-PLUGIN-01, G2-POLISH-05, G2-POLISH-08]
tags: [phase-125, wallclock, operator, hardware-retest, e2e]
# Wallclock checkpoint per memory feedback_wallclock_checkpoint_exempt:
# yolo mode does NOT bypass physical-host actions. Operator wallclock ONLY.

must_haves:
  truths:
    - "Operator runs hardware retest checklist on real G2 firmware 2.2.0.28 with iPhone Even App + PWA + vigil-watch installed Mac"
    - "All 5 retest scenarios pass (Quiet mode E2E, Companion HUD ack, work-orders exit, home D-14 byte-identity, needs_input flow)"
    - "Evidence captured to .planning/phases/125-.../artifacts/ — screenshots / photos / video clips per scenario"
    - "VERIFICATION.md filed with operator findings + green/yellow/red disposition per scenario"
    - "If any scenario reds, plan is BLOCKED — operator must surface to user; cannot proceed to Plan 10 (Even Hub upload) until either fix lands OR carve-out is agreed"
  artifacts:
    - path: ".planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md"
      provides: "Operator findings document — green/yellow/red disposition per scenario + carve-out justifications"
      contains: "Scenario 1"
    - path: ".planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/"
      provides: "Hardware retest evidence directory (screenshots, photos, video)"
      contains: "" # directory existence is the contract; .gitkeep ensures it's committed
  key_links:
    - from: "vigil-g2-plugin/vigil.ehpk (Plan 08)"
      to: "Real G2 hardware via Even App sideload"
      via: "operator manual install"
      pattern: "vigil\\.ehpk"
---

<objective>
Operator wallclock checkpoint per memory `feedback_wallclock_checkpoint_exempt`: hardware retest on real G2 hardware. This is the ONLY way to verify AGENT-HUD-03 quiet-mode E2E, the Phase 124 D-14 home byte-identity invariant under v0.3.0, and the work-orders exit gesture (D-06 fallback path). Per memory `feedback_g2_tap_expand_broken` / `project_g2_tap_expand_broken`: NO sim-only ships — every tap or list-bubble change must hardware-retest before SDK pack moves to Even Hub.

This plan is `autonomous: false`. yolo mode does NOT bypass. Claude prepares the checklist + artifact directory + skeleton VERIFICATION.md; the operator runs the retest physically and back-fills findings.

Dependencies:
- vigil.ehpk built at v0.3.0 (Plan 08)
- Plan 07 PWA Quiet-mode toggle live in production (or running locally on Vite dev server with the operator's bearer)
- Plan 05 vigil-core /v1/quiet-mode endpoint live on Railway (auto-deploys after Plan 02 + Plan 05 commit & push)

Output:
- 1 NEW file: `.planning/phases/125-.../125-VERIFICATION.md` — skeleton with 5 scenario sections; operator fills findings + dispositions
- 1 NEW directory: `.planning/phases/125-.../artifacts/` (committed via .gitkeep) — operator drops evidence here
- 5 retest scenarios documented with exact step-by-step instructions + acceptance per UI-SPEC §"Verification Gates"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VALIDATION.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-UI-SPEC.md
@.planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create artifacts/ directory and skeleton VERIFICATION.md with 5 retest scenarios</name>
  <read_first>
    - .planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-VERIFICATION.md (FULL FILE — pattern reference for VERIFICATION.md structure; Phase 124's E2E findings format)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VALIDATION.md (lines 84-89 — Manual-Only Verifications table with Test Instructions for AGENT-HUD-03 + G2-PLUGIN-01)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-UI-SPEC.md (lines 502-513 — Surface 2 G2 Verification Gates table)
  </read_first>
  <files>
    .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/.gitkeep,
    .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
  </files>
  <action>
**Step 1.** Create artifacts directory + .gitkeep:

```bash
mkdir -p .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts
touch .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/.gitkeep
```

**Step 2.** Create skeleton `125-VERIFICATION.md` with 5 retest scenarios. Use the format below — operator fills `Status:` + `Evidence:` + `Notes:` after running each scenario:

```markdown
---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
status: pending
operator: <fill on retest>
retest_date: <fill on retest, ISO 8601>
firmware: 2.2.0.28
plugin_version: 0.3.0
---

# Phase 125 — Hardware Verification

> Operator wallclock checkpoint per memory `feedback_wallclock_checkpoint_exempt`.
> yolo mode does NOT bypass. Five scenarios; each requires real G2 worn,
> iPhone Even App connected, PWA bearer in localStorage, and vigil-watch
> running on the Mac.

## Pre-flight

- [ ] G2 paired and connected (battery > 30%)
- [ ] iPhone Even App version current; vigil sideloaded from local pack OR latest dev portal upload
- [ ] PWA accessible via Settings page; current bearer signed in
- [ ] vigil-watch installed via launchd, status `running`
- [ ] Test Claude Code session ready in VS Code (a prompt that reliably triggers `needs_input` — see Plan 11 §"Demo prompt staging")

---

## Scenario 1 — AGENT-HUD-03 Quiet mode E2E (PWA toggle → SSE → HUD filter → replay)

**Steps:**
1. With G2 worn and HUD on Companion, confirm header `rightSide` shows current state (offline indicator if SSE down, N/M if multi-session, else clock).
2. Open PWA Settings → G2 Plugin section. Confirm Quiet mode toggle is OFF.
3. Toggle Quiet mode ON. Within ~100ms, observe G2 Companion HUD `rightSide` now shows `Q` glyph (UI-SPEC priority order).
4. Trigger a non-allowlist event (e.g., trigger a `task_complete` from Claude Code session — let a session finish; or use vigil-watch test mode to inject one). Confirm HUD body does NOT show the [DONE] toast or update banner overlay.
5. Trigger an allowlist event: cause Claude Code to fire `needs_input` (e.g., a confirmation prompt). Confirm HUD shows the [NEEDS INPUT] banner (allowlist passes through).
6. Toggle Quiet mode OFF in PWA. Confirm `Q` glyph disappears from HUD.
7. Confirm any held events from step 4 replay in chronological order — HUD body briefly cycles through them.

**Acceptance:**
- Q glyph appears within 100ms of toggle-on
- Q glyph disappears within 100ms of toggle-off
- Non-allowlist events do NOT surface during quiet window
- Allowlist events DO surface during quiet window
- Held events replay in chronological order on toggle-off

**Status:** ⬜ pending
**Evidence:** (fill — paths to screenshots / photos / video in `artifacts/`)
**Notes:** (fill)

---

## Scenario 2 — Companion HUD ack (Phase 124 D-08 carry-forward + v0.3.0 regression check)

**Steps:**
1. With G2 worn, navigate to Companion (via plugin tile in Even App).
2. Trigger a `needs_input` event. Confirm banner [NEEDS INPUT] appears on HUD.
3. Double-tap the temple. Banner clears, state line returns to `running`.
4. Trigger a second `needs_input` on a different session. Confirm banner appears for that session.

**Acceptance:**
- Double-tap acks the banner (Phase 124 D-08 carry-forward)
- Multi-session banner re-appears for new sessions

**Status:** ⬜ pending
**Evidence:** (fill)
**Notes:** (fill)

---

## Scenario 3 — Work-orders exit gesture (D-06 fallback verification)

**Steps:**
1. With G2 worn, navigate via SCROLL_DOWN from Companion to Work Orders screen.
2. Confirm footer text reads `() double-tap to exit` (verbatim — use the bottom-line of the wo-list container).
3. Double-tap temple. Confirm screen exits to Home (via DOUBLE_CLICK → home pattern).

**Acceptance:**
- Footer reads exactly `() double-tap to exit`
- Double-tap exits to Home

**Status:** ⬜ pending
**Evidence:** (fill — photo of HUD showing footer text)
**Notes:** (fill)

---

## Scenario 4 — Home body D-14 byte-identity (Phase 124 invariant carried into v0.3.0)

**Steps:**
1. With G2 worn, capture screenshot of Home body via Even App's screenshot feature (or sim equivalent if hardware capture unavailable).
2. Wait 5 seconds. Capture again.
3. Compare the two captures pixel-for-pixel (Phase 124 D-14 invariant).

**Acceptance:**
- Two captures byte-identical (no auto-scroll/overflow regression introduced by v0.3.0 changes)

**Status:** ⬜ pending
**Evidence:** (fill — pair of PNG captures + diff verdict)
**Notes:** (fill — if not byte-identical: investigate which change caused regression; v0.3.0 should not touch Home rendering at all)

---

## Scenario 5 — needs_input → ack flow (AGENT-DEMO-01 dry run)

**Steps:**
1. Start a real Claude Code session in VS Code (the test prompt staged for Plan 11).
2. Walk away from keyboard for ~15 seconds.
3. Wait for `needs_input` to fire on G2 (banner [NEEDS INPUT] appears).
4. Double-tap to ack.
5. Walk back, answer Claude Code, let it complete.
6. Confirm HUD shows `task_complete` toast briefly.

**Acceptance:**
- Full flow operates end-to-end on real hardware
- Latency from prompt-fire to G2 banner < 5 seconds
- Double-tap acks reliably (no missed taps — if missed, surface SEED-011 to log)

**Status:** ⬜ pending
**Evidence:** (fill — phone video clip, < 60s)
**Notes:** (fill)

---

## Disposition

After all 5 scenarios are complete:

- **All green:** Plan 10 (Even Hub upload) is unblocked.
- **Any yellow** (passed with caveats): Document caveat in §Notes; surface to user; user decides whether to proceed to Plan 10 or fix-and-retest.
- **Any red:** Plan 10 BLOCKED. Operator surfaces failure mode to user; user decides whether to:
  - Fix issue in a follow-up commit, retest from scratch
  - Carve-out: ship v0.3.0 with the regression documented in REQUIREMENTS.md amendment + ride-along to Phase 126
  - Defer phase close until fix lands

**Operator sign-off:** _____________
**Disposition:** ⬜ green / ⬜ yellow / ⬜ red
```

**Step 3.** Confirm both files were created:

```bash
test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/.gitkeep
test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
```

**Step 4 (OPERATOR — wallclock).** The operator now physically runs the 5 scenarios. This task is autonomous: false; yolo mode does not bypass.

After running the retest, the operator returns to this plan and:
1. Saves evidence files to `artifacts/`
2. Updates VERIFICATION.md with `Status:`, `Evidence:`, `Notes:` per scenario
3. Sets the operator sign-off line + disposition
4. Commits the VERIFICATION.md + artifacts to git

If a scenario reds, the operator MUST surface the failure to the user before any further plans run.
  </action>
  <verify>
    <automated>test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md && test -d .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts && grep -c "Scenario [1-5]" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md` exists
    - That file contains the substring `Scenario 1` AND `Scenario 2` AND `Scenario 3` AND `Scenario 4` AND `Scenario 5`
    - That file contains the substring `feedback_wallclock_checkpoint_exempt` (memory citation)
    - That file contains operator sign-off + disposition fields
    - Directory `.planning/phases/125-.../artifacts/` exists (with `.gitkeep`)
    - **Operator-provided post-retest:** VERIFICATION.md `Status:` lines are NOT all `⬜ pending` — at least one disposition is filled (green / yellow / red)
    - **Operator-provided post-retest:** Disposition line is set to one of green / yellow / red (not blank)
  </acceptance_criteria>
  <done>VERIFICATION.md skeleton + artifacts/ directory committed; operator wallclock retest run against real G2; findings backfilled to VERIFICATION.md; if all green, Plan 10 unblocked; if red, user notified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sim → Hardware | Phase 124 D-14 byte-identity gate verified in sim; this plan re-verifies on hardware to catch any sim/hardware divergence |
| Pre-pack VERIFIED ↔ Post-pack hardware retest | Pre-pack gate (Plan 08) is sim-side; this plan is the hardware-side gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-W9-01 | Repudiation | Hardware retest skipped or fudged in yolo mode | mitigate | autonomous: false enforces operator wallclock per memory feedback_wallclock_checkpoint_exempt; verify gate requires operator-provided dispositions |
| T-125-W9-02 | Tampering | sim/hardware divergence ships v0.3.0 with broken Quiet mode | mitigate | Scenario 1 is the load-bearing E2E gate; if it fails, Plan 10 is blocked until either fix lands or carve-out is agreed |
| T-125-04 | Information Disclosure | Stale held events replay incorrectly during retest (Pitfall 4) | mitigate | Scenario 1 step 7 explicitly checks chronological order — if events arrive out-of-order, suppression queue's flush sort is broken |
</threat_model>

<verification>
```bash
# Skeleton + directory exist
test -f .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
test -d .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts

# Operator filled at least one disposition
grep -E "Status: (✅|⚠|❌|green|yellow|red|GREEN|YELLOW|RED)" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md | wc -l
# Expected >= 5 after retest

# Disposition line filled
grep "Disposition:" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
```
</verification>

<success_criteria>
- VERIFICATION.md skeleton with 5 scenarios committed
- artifacts/ directory committed (with .gitkeep)
- Operator runs the 5 scenarios on real G2 hardware (wallclock checkpoint)
- Each scenario has Status + Evidence + Notes filled
- Disposition field is set to green / yellow / red
- If green: Plan 10 unblocked
- If yellow / red: user notified, decision logged in plan summary
</success_criteria>

<output>
Create `.planning/phases/125-.../125-09-SUMMARY.md` recording: skeleton creation timestamp, operator name + retest start/end times, disposition per scenario, list of evidence files in artifacts/, and Plan 10 unblock verdict.
</output>
