---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
plan: 10
type: execute
wave: 4
depends_on: [09]
files_modified:
  - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-2026-MM-DD.png
autonomous: false
requirements: [G2-PLUGIN-01]
tags: [phase-125, wallclock, operator, even-hub-upload, store-submission]
# Wallclock checkpoint per memory feedback_wallclock_checkpoint_exempt:
# Even Hub developer portal upload is a manual web-dashboard step (no API).
# Operator wallclock ONLY.

must_haves:
  truths:
    - "Operator logs into Even Hub developer portal store dashboard"
    - "Operator uploads vigil-g2-plugin/vigil.ehpk (Plan 08 output) to v0.3.0 store submission slot"
    - "Operator confirms submission acknowledged by dashboard (e.g., status 'submitted' / 'in review' / queue position visible)"
    - "Operator captures dashboard acknowledgment screenshot saved to artifacts/even-hub-submission-2026-MM-DD.png"
    - "Per memory project_even_hub_preview_field — Preview field uses LIVE CAPTURES ONLY (no composited mockups; v0.2.0 was rejected for this; if dashboard demands composites, surface to user before submit)"
  artifacts:
    - path: ".planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-2026-MM-DD.png"
      provides: "Proof of submission — dashboard acknowledgment screenshot"
      contains: "" # binary PNG; existence is the contract
  key_links:
    - from: "vigil-g2-plugin/vigil.ehpk"
      to: "Even Hub store dashboard"
      via: "manual web upload by operator"
      pattern: "vigil\\.ehpk"
---

<objective>
Operator wallclock checkpoint per memory `feedback_wallclock_checkpoint_exempt`: upload `vigil-g2-plugin/vigil.ehpk` (Plan 08 build) to the Even Hub developer portal store dashboard. There is NO API for this step (per RESEARCH §Architectural Responsibility Map: "Manual upload via store dashboard" + memory `project_even_hub_preview_field`). Operator wallclock ONLY.

Per CONTEXT D-11 strict ship sequence: this plan runs ONLY AFTER Plan 09 hardware retest disposition is GREEN (or yellow with documented carve-out). If Plan 09 is RED, this plan is blocked until either fix-and-retest OR carve-out agreement.

Per memory `project_even_hub_preview_field`: the dashboard has 3 separate Preview slots distinct from listing screenshots. v0.2.0 was rejected for composited mockups in this field. v0.3.0 must use LIVE CAPTURES ONLY. If the dashboard demands content beyond what live captures provide, treat it as a submission blocker and surface to the user BEFORE submitting.

This plan is `autonomous: false`. yolo mode does not bypass.

Output:
- Even Hub dashboard now shows v0.3.0 submission in queue / under review
- Screenshot of dashboard acknowledgment saved to `artifacts/even-hub-submission-YYYY-MM-DD.png`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Upload vigil.ehpk to Even Hub developer portal + capture acknowledgment screenshot</name>
  <read_first>
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md (Plan 09 output — confirm disposition is GREEN before proceeding; if YELLOW/RED, surface to user before continuing)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md (D-11 strict ship sequence; D-12 + Claude's Discretion §"Even Hub Preview field")
  </read_first>
  <what-built>
    - vigil.ehpk artifact at v0.3.0 (Plan 08 output)
    - Hardware retest disposition GREEN (or YELLOW with documented carve-out) per Plan 09 VERIFICATION.md
    - Docs amendments cascaded (REQUIREMENTS.md, ROADMAP.md, PROJECT.md, v3.8 spec, SEED-005 — Plan 08)
  </what-built>
  <how-to-verify>
**Pre-flight (Claude — autonomous gate):**

```bash
# Confirm Plan 09 disposition is GREEN
grep "Disposition:.*green" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md
# If this returns 0 matches, halt and surface to user:
# "Plan 09 disposition is not GREEN. Plan 10 (Even Hub upload) requires GREEN per CONTEXT D-11. Current disposition: <value>. Proceed anyway? (requires explicit user approval)"

# Confirm vigil.ehpk artifact exists
test -f vigil-g2-plugin/vigil.ehpk
```

If either gate fails, do NOT proceed; surface to user.

**Operator wallclock — manual steps:**

1. Open Even Hub developer portal (browser; URL is in operator's bookmarks — RESEARCH §Sources line 1051 confirms "Even Realities G2 hardware" + "iPhone with Even App installed" but does not include the portal URL; operator knows it from prior v0.2.0 submissions).

2. Navigate to the Vigil plugin store listing.

3. Upload `vigil-g2-plugin/vigil.ehpk` (full path — operator should `cd` to repo root first or use file picker to navigate there).

4. Verify the version detected by the dashboard reads `0.3.0` (not 0.2.0 — if it reads 0.2.0, the wrong artifact was uploaded; redo Plan 08 pack).

5. **Preview field check (memory `project_even_hub_preview_field`):** Before submitting, verify the 3 Preview field slots. If they currently show v0.2.0's composited mockups (rejection cause), REPLACE with LIVE CAPTURES ONLY. If the dashboard demands composites despite the policy, STOP — surface to user via:

   "Even Hub Preview field is demanding content beyond live captures. Per memory project_even_hub_preview_field, v0.2.0 was rejected for this. Options: (a) revert to live-only and accept whatever it gives; (b) operator decides to ride composites this round; (c) escalate to Even Hub support. Which?"

6. Submit. Wait for dashboard acknowledgment (status changes to "submitted" / "in review" / similar).

7. Take a full-window screenshot of the dashboard showing the acknowledgment. Save to:

   `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-YYYY-MM-DD.png`

   (Replace YYYY-MM-DD with today's local date.)

8. Commit the screenshot to git.

**Verification (Claude — autonomous):**

```bash
# Screenshot exists in expected location
ls .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-*.png
# Expected: at least one matching file

# Screenshot is non-empty PNG (sanity check)
PNG=$(ls .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-*.png | head -1)
test -n "$PNG" && [ $(stat -f '%z' "$PNG" 2>/dev/null || stat -c '%s' "$PNG") -gt 10000 ]
# Expected: file exists AND > 10KB (PNG header + actual content)
```
  </how-to-verify>
  <resume-signal>
Type "submitted" once the upload is acknowledged AND screenshot is saved to artifacts/.

If a snag arose (preview field rejection, version mismatch, dashboard auth issue), describe the issue instead — the user will decide next steps.
  </resume-signal>
  <acceptance_criteria>
    - File matching `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-*.png` exists
    - That PNG file is > 10KB (sanity check — not a 0-byte placeholder)
    - The submission filename includes the local-date stamp
    - Even Hub dashboard reflects v0.3.0 in the submission slot (verifiable by operator visually; the screenshot is the artifact)
    - Plan 09 disposition was GREEN (or YELLOW with documented carve-out user-approved) BEFORE this task ran
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local artifact ↔ Even Hub portal | Manual upload — no automation possible (per RESEARCH §Architectural Responsibility Map) |
| Operator decision ↔ Preview field policy | Per memory project_even_hub_preview_field — composites have rejected v0.2.0; live captures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-W10-01 | Tampering | Wrong vigil.ehpk uploaded (e.g., a 0.2.x build from local dist/) | mitigate | Step 4 of how-to-verify confirms dashboard-detected version === 0.3.0; if mismatch, halt and rebuild |
| T-125-W10-02 | Repudiation | Submission acknowledgment screenshot fabricated or absent | mitigate | Acceptance criterion checks file > 10KB; manual review by user during verify-work; falsifying a screenshot is outside threat model |
| T-125-W10-03 | Repudiation | Preview field policy violation re-triggers v0.2.0-style rejection | mitigate | Step 5 explicit policy check before submit; operator surfaces to user if dashboard demands composites |
</threat_model>

<verification>
```bash
# Plan 09 was GREEN (or user-approved YELLOW carve-out)
grep "Disposition:.*green" .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VERIFICATION.md

# Submission screenshot in artifacts/
ls .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-*.png

# Screenshot is real PNG (sanity)
file .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/artifacts/even-hub-submission-*.png | grep -i "PNG image"
```
</verification>

<success_criteria>
- vigil.ehpk v0.3.0 uploaded to Even Hub developer portal store dashboard
- Submission acknowledged by dashboard
- Acknowledgment screenshot saved to artifacts/ with date-stamped filename
- Preview field policy honored (live captures only OR escalation to user)
- Operator wallclock checkpoint observed (yolo mode bypass not allowed)
</success_criteria>

<output>
Create `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-10-SUMMARY.md` recording: upload timestamp, dashboard version detected, Preview field disposition (live captures only / composites carve-out / blocked), screenshot file path, dashboard queue status (submitted / in review / etc).
</output>
