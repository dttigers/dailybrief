---
phase: 125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vigil-core/src/routes/quiet-mode.test.ts
  - vigil-core/src/lib/quiet-mode-suppression.test.ts
  - vigil-core/src/lib/__tests__/agent-events-bus.test.ts
  - vigil-core/src/routes/__tests__/agent-stream.test.ts
  - vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts
  - vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts
  - vigil-g2-plugin/src/screens/__tests__/companion.test.ts
autonomous: true
requirements: [AGENT-HUD-03, G2-POLISH-08]
tags: [phase-125, wave-0, test-infra, nyquist]

must_haves:
  truths:
    - "All 8 Wave-0 test files referenced by VALIDATION.md exist on disk"
    - "Each NEW test file fails red (TODO/skip placeholders that will be replaced by Wave 1+ implementations)"
    - "Each EXTENDED test file still has its existing tests passing — only new red placeholders are added"
    - "`cd vigil-core && npm test` exits 0 (existing green + new placeholders skip cleanly via t.skip OR todo: true)"
    - "`cd vigil-g2-plugin && npm test` exits 0 (existing green + new placeholders skip cleanly)"
  artifacts:
    - path: "vigil-core/src/routes/quiet-mode.test.ts"
      provides: "RED placeholder for AGENT-HUD-03 endpoint contract — pinned by Plan 05"
      contains: "import { test } from \"node:test\""
    - path: "vigil-core/src/lib/quiet-mode-suppression.test.ts"
      provides: "RED placeholder for AGENT-HUD-03 suppression queue — pinned by Plan 03"
      contains: "import { test } from \"node:test\""
    - path: "vigil-core/src/lib/__tests__/agent-events-bus.test.ts"
      provides: "EXTENDED with skip-tests for emitQuiet/onQuiet/offQuiet isolation — Plan 03 turns green"
      contains: "emitQuiet"
    - path: "vigil-core/src/routes/__tests__/agent-stream.test.ts"
      provides: "EXTENDED with skip-tests for Phase 0 synthetic frame ordering + suppression filter on replay — Plan 05 turns green"
      contains: "synthetic"
    - path: "vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts"
      provides: "RED placeholder for G2-POLISH-08 helper — Plan 04 turns green"
      contains: "createDedupedDeviceStatusListener"
    - path: "vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts"
      provides: "EXTENDED with skip-tests for quiet_mode_changed dispatch — Plan 06 turns green"
      contains: "quiet_mode_changed"
    - path: "vigil-g2-plugin/src/screens/__tests__/companion.test.ts"
      provides: "EXTENDED with skip-tests for setQuietMode mutator + Q glyph — Plan 06 turns green"
      contains: "setQuietMode"
  key_links:
    - from: "Wave 0 placeholders"
      to: "Wave 1+ implementations"
      via: "RED → GREEN cycle per plan"
      pattern: "skip|todo|TODO\\(125-\\d{2}\\)"
---

<objective>
Bootstrap the 8 Wave-0 test files enumerated in 125-VALIDATION.md so subsequent plans can turn them GREEN as they implement features. This phase requires test-first execution per the Nyquist contract (every `<verify>` block must include an `<automated>` command). Three of the eight files are NEW; five are EXTENDED. RED-state markers (`{ skip: true }` or `{ todo: 'TODO(125-XX): pending implementation' }`) document the contract before code exists.

Purpose: Unblock all of Wave 1+ — every Plan 02-07 task that lands implementation code points its `<verify>` block at one of these test files. No plan should encounter `MISSING — Wave 0 must create...` during execution.

Output:
- 3 NEW test files (quiet-mode.test.ts, quiet-mode-suppression.test.ts, deduped-device-status.test.ts) with RED test bodies that name the expected behaviors and `{ skip: true }` until Wave 1+ provides the SUT
- 4 EXTENDED test files with new RED tests appended (agent-events-bus.test.ts, agent-stream.test.ts, sse-client.test.ts, companion.test.ts)
- Zero existing tests modified — only new tests appended
- Both `npm test` invocations exit 0 (skipped tests count as pass)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-CONTEXT.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md
@.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VALIDATION.md
@vigil-core/src/lib/__tests__/agent-events-bus.test.ts
@vigil-core/src/routes/__tests__/agent-stream.test.ts
@vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts
@vigil-g2-plugin/src/screens/__tests__/companion.test.ts
@vigil-g2-plugin/package.json
@vigil-core/package.json

<interfaces>
<!-- node:test runner — both vigil-core and vigil-g2-plugin use `tsx --test "src/**/*.test.ts"` -->
<!-- vigil-core/package.json:9: "test": "tsx --test \"src/**/*.test.ts\"" -->
<!-- vigil-g2-plugin/package.json:14: "test": "tsx --test \"src/**/*.test.ts\"" -->

<!-- RED placeholder pattern for node:test — three options: -->
<!--   1. test('name', { skip: 'TODO(125-XX): impl pending' }, () => { ... }) -->
<!--   2. test('name', { todo: 'TODO(125-XX): impl pending' }, () => { ... }) -->
<!--   3. test.skip('name', () => { ... }) -->
<!-- USE OPTION 1 (skip with reason string) — appears in test output explicitly per contract D-04 readability -->

<!-- Existing extended files — read FULL FILE first to preserve existing tests verbatim. -->
<!-- DO NOT modify any existing test body; only APPEND new test() calls below the last existing test. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 3 NEW Wave-0 RED test files (server: 2; plugin: 1)</name>
  <read_first>
    - vigil-core/src/lib/__tests__/agent-events-bus.test.ts (full file — pattern reference for node:test imports + EventEmitter test shape)
    - vigil-core/src/routes/__tests__/agent-stream.test.ts (full file — pattern reference for streamSSE deps injection)
    - vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts (full file — pattern reference for plugin tsx --test imports)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-VALIDATION.md (lines 43-65 — Per-Task Verification Map, lines 71-78 — Wave 0 Requirements)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 280-440 — Pattern 2 GET/PUT route + Pattern 3 suppression queue + Example D helper test)
  </read_first>
  <files>
    vigil-core/src/routes/quiet-mode.test.ts,
    vigil-core/src/lib/quiet-mode-suppression.test.ts,
    vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts
  </files>
  <action>
Create three NEW test files with RED placeholders. Each file imports `test` from `node:test` and `assert` from `node:assert/strict`. Each test uses `{ skip: 'TODO(125-XX): pending implementation in Plan XX' }` so the runner counts them as skipped (NOT failing). Tests document the contract; bodies stay empty or use `assert.fail()` guarded by skip.

**File 1: `vigil-core/src/routes/quiet-mode.test.ts` (NEW)** — pins `GET/PUT /v1/quiet-mode` contract per RESEARCH §Pattern 2 (lines 295-374). Mirror the dependency-injection style of `vigil-core/src/routes/calendar.test.ts` (factory function `createQuietModeRouter(deps)` with `dbAvailable / dbGet / dbSet` + `bus.emitQuiet` + `suppressionQueue.flush`). Add these tests as `skip` placeholders (Plan 05 turns green):

```typescript
import { test } from "node:test";
import assert from "node:assert/strict";

const PLAN_05 = "TODO(125-05): pending implementation — vigil-core/src/routes/quiet-mode.ts";

test("GET /v1/quiet-mode returns {enabled, since} for authenticated user", { skip: PLAN_05 }, () => {
  // Plan 05: createQuietModeRouter with stub dbGet → c.get('userId') → returns {enabled, since: ISO|null}
  assert.fail("placeholder");
});

test("GET /v1/quiet-mode returns 503 when dbAvailable=false", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});

test("PUT /v1/quiet-mode {enabled: true} writes column + emits bus.emitQuiet + sets quietModeSince", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});

test("PUT /v1/quiet-mode {enabled: false} flushes suppressionQueue + emits each held row via bus.emit", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});

test("PUT /v1/quiet-mode rejects non-boolean enabled with 400 invalid_payload", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});

test("PUT /v1/quiet-mode rejects malformed JSON with 400 invalid_json", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});

test("Cross-user isolation: userA cannot read or write userB's quiet_mode (T-125-01)", { skip: PLAN_05 }, () => {
  assert.fail("placeholder");
});
```

**File 2: `vigil-core/src/lib/quiet-mode-suppression.test.ts` (NEW)** — pins suppression queue per RESEARCH §Pattern 3 (lines 384-443). Tests (Plan 03 turns green):

```typescript
import { test } from "node:test";
import assert from "node:assert/strict";

const PLAN_03 = "TODO(125-03): pending implementation — vigil-core/src/lib/quiet-mode-suppression.ts";

test("shouldSuppress returns false when isQuiet=false (passthrough)", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("shouldSuppress returns false for allowlist event types (needs_input, task_failed) even when isQuiet=true", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("shouldSuppress returns true and stores row for non-allowlist event when isQuiet=true", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("Map keyed (userId, sessionId, eventType) — last-of-each-kind via overwrite", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("flush(userId) returns held rows in ascending event_timestamp order (Pitfall 4)", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("flush(userId) clears the user's bucket; subsequent shouldSuppress on different event creates fresh entry", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("Cross-user isolation: flush(userA) does NOT affect userB's held rows (T-125-01)", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
test("Replay-storm DoS bounded: same (sessionId, eventType) emitted 100x stores exactly 1 row (T-125-03)", { skip: PLAN_03 }, () => { assert.fail("placeholder"); });
```

**File 3: `vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts` (NEW)** — pins G2-POLISH-08 helper per RESEARCH §Example D (lines 758-828). Tests (Plan 04 turns green):

```typescript
import { test } from 'node:test'
import assert from 'node:assert/strict'

const PLAN_04 = 'TODO(125-04): pending implementation — vigil-g2-plugin/src/lib/deduped-device-status.ts'

test('createDedupedDeviceStatusListener — 5×"none" fires once', { skip: PLAN_04 }, () => { assert.fail('placeholder') })
test('createDedupedDeviceStatusListener — change fires; consecutive same is deduped', { skip: PLAN_04 }, () => { assert.fail('placeholder') })
test('createDedupedDeviceStatusListener — first call always fires (lastSeen starts null)', { skip: PLAN_04 }, () => { assert.fail('placeholder') })
```

After writing all three files, verify each is syntactically valid TypeScript by running each test runner. Both runs should exit 0 with skipped count >= 8 (server) and >= 3 (plugin).
  </action>
  <verify>
    <automated>cd vigil-core && npm test 2>&1 | tail -30 | grep -E "tests [0-9]+|skipped [0-9]+|pass [0-9]+|fail 0" && cd ../vigil-g2-plugin && npm test 2>&1 | tail -30 | grep -E "tests [0-9]+|skipped [0-9]+|pass [0-9]+|fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-core/src/routes/quiet-mode.test.ts` exists and contains the substring `import { test } from "node:test"`
    - File `vigil-core/src/lib/quiet-mode-suppression.test.ts` exists and contains the substring `shouldSuppress`
    - File `vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts` exists and contains the substring `createDedupedDeviceStatusListener`
    - `cd vigil-core && npm test` exits 0 (no `fail >= 1` in output)
    - `cd vigil-g2-plugin && npm test` exits 0 (no `fail >= 1` in output)
    - `grep -c "TODO(125-03)" vigil-core/src/lib/quiet-mode-suppression.test.ts` returns >= 8
    - `grep -c "TODO(125-05)" vigil-core/src/routes/quiet-mode.test.ts` returns >= 7
    - `grep -c "TODO(125-04)" vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts` returns >= 3
  </acceptance_criteria>
  <done>Three NEW test files committed with RED placeholders; both repo test suites exit 0 with skipped tests counted; subsequent Wave 1+ plans replace skip:reason with green implementations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EXTEND 4 existing test files with Wave-0 RED placeholders (server: 2; plugin: 2)</name>
  <read_first>
    - vigil-core/src/lib/__tests__/agent-events-bus.test.ts (FULL FILE — preserve verbatim, append only)
    - vigil-core/src/routes/__tests__/agent-stream.test.ts (FULL FILE — preserve verbatim, append only)
    - vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts (FULL FILE — preserve verbatim, append only)
    - vigil-g2-plugin/src/screens/__tests__/companion.test.ts (FULL FILE — preserve verbatim, append only)
    - .planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-RESEARCH.md (lines 545-575 Pitfall 1 synthetic frame ordering; lines 656-685 Example B bus extension)
  </read_first>
  <files>
    vigil-core/src/lib/__tests__/agent-events-bus.test.ts,
    vigil-core/src/routes/__tests__/agent-stream.test.ts,
    vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts,
    vigil-g2-plugin/src/screens/__tests__/companion.test.ts
  </files>
  <action>
APPEND only — do NOT modify any existing test body. Read each full file first; identify the last `test(...)` or last `})` of file scope; insert new tests below.

**File 4: `vigil-core/src/lib/__tests__/agent-events-bus.test.ts` (EXTEND)** — append (Plan 03 turns green):

```typescript
// Phase 125 (AGENT-HUD-03 / D-02 / T-125-01) — quiet_mode_changed fan-out additions
import { test } from "node:test"; // already imported above; IDE may flag duplicate but tsx is OK with it
import assert from "node:assert/strict";

const PLAN_03_BUS = "TODO(125-03): pending implementation — bus.emitQuiet/onQuiet/offQuiet";

test("bus.emitQuiet(userId, payload) fires onQuiet listeners for that userId only (T-125-01)", { skip: PLAN_03_BUS }, () => { assert.fail("placeholder"); });
test("bus.emitQuiet(userId) does NOT fire onQuiet listeners for other userIds (cross-user isolation)", { skip: PLAN_03_BUS }, () => { assert.fail("placeholder"); });
test("bus.offQuiet removes the listener; subsequent emitQuiet does not fire", { skip: PLAN_03_BUS }, () => { assert.fail("placeholder"); });
test("emitter Map entry is deleted when both EVENT_NAME and QUIET_NAME listenerCount=0", { skip: PLAN_03_BUS }, () => { assert.fail("placeholder"); });
```

(If the existing file already has `import { test }` at the top, do NOT re-import — drop the duplicate import line on append. Same for assert.)

**File 5: `vigil-core/src/routes/__tests__/agent-stream.test.ts` (EXTEND)** — append (Plan 05 turns green):

```typescript
const PLAN_05_STREAM = "TODO(125-05): pending implementation — agent-stream Phase 0 synthetic frame + suppression filter on replay";

test("Phase 0 synthetic quiet_mode_changed frame is emitted FIRST after auth, BEFORE Phase 1 replay (Pitfall 1)", { skip: PLAN_05_STREAM }, () => { assert.fail("placeholder"); });
test("Phase 1 Last-Event-ID replay loop filters through suppressionQueue.shouldSuppress when isQuiet=true (T-125-04)", { skip: PLAN_05_STREAM }, () => { assert.fail("placeholder"); });
test("Phase 2 live attach: bus.on listener invokes suppressionQueue.shouldSuppress before stream.writeSSE", { skip: PLAN_05_STREAM }, () => { assert.fail("placeholder"); });
test("bus.onQuiet listener writes a quiet_mode_changed SSE frame and updates local isQuiet ref", { skip: PLAN_05_STREAM }, () => { assert.fail("placeholder"); });
test("stream.onAbort cleanup calls bus.off AND bus.offQuiet (no listener leak)", { skip: PLAN_05_STREAM }, () => { assert.fail("placeholder"); });
```

**File 6: `vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts` (EXTEND)** — append (Plan 06 turns green):

```typescript
const PLAN_06_SSE = "TODO(125-06): pending implementation — sse-client.ts quiet_mode_changed dispatch"

test("parsed.event === 'quiet_mode_changed' invokes opts.onQuietMode with parsed.data string", { skip: PLAN_06_SSE }, () => { assert.fail('placeholder') })
test("opts.onQuietMode is optional — missing callback does not throw on quiet_mode_changed frame", { skip: PLAN_06_SSE }, () => { assert.fail('placeholder') })
test("agent-event dispatch path UNCHANGED — quiet_mode_changed branch does not steal agent-event frames", { skip: PLAN_06_SSE }, () => { assert.fail('placeholder') })
```

**File 7: `vigil-g2-plugin/src/screens/__tests__/companion.test.ts` (EXTEND)** — append (Plan 06 turns green):

```typescript
const PLAN_06_COMP = "TODO(125-06): pending implementation — companion.ts setQuietMode + Q glyph + ALLOWLIST filter"

test("setQuietMode(true) toggles module-level quietMode ref to true", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("isQuietMode() reflects setQuietMode value", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("computeRightSide() prepends 'Q' when isQuietMode()=true (UI-SPEC priority order)", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("computeRightSide() with quiet=true + offline + 2/3 sessions returns 'Q ! 2/3' (UI-SPEC table row 1)", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("computeRightSide() with quiet=false matches Phase 124 baseline (Q glyph absent)", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("buildContainers() with quietMode=true and non-allowlist banner suppresses banner overlay (defense-in-depth)", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
test("buildContainers() with quietMode=true and needs_input banner STILL renders (allowlist passes through)", { skip: PLAN_06_COMP }, () => { assert.fail('placeholder') })
```

After all four extensions, both repo test suites must still exit 0.
  </action>
  <verify>
    <automated>cd vigil-core && npm test 2>&1 | tail -20 | grep -E "fail 0|tests" && cd ../vigil-g2-plugin && npm test 2>&1 | tail -20 | grep -E "fail 0|tests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO(125-03_BUS)" vigil-core/src/lib/__tests__/agent-events-bus.test.ts` returns 0 (constant ref'd) AND `grep -c "PLAN_03_BUS" vigil-core/src/lib/__tests__/agent-events-bus.test.ts` returns >= 4
    - `grep -c "PLAN_05_STREAM" vigil-core/src/routes/__tests__/agent-stream.test.ts` returns >= 5
    - `grep -c "PLAN_06_SSE" vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts` returns >= 3
    - `grep -c "PLAN_06_COMP" vigil-g2-plugin/src/screens/__tests__/companion.test.ts` returns >= 7
    - All 4 EXTENDED files preserved their pre-existing tests verbatim (verify via `git diff --stat` shows additions only, zero deletions)
    - `cd vigil-core && npm test` exits 0
    - `cd vigil-g2-plugin && npm test` exits 0
  </acceptance_criteria>
  <done>Four EXTENDED test files now have Wave-0 RED placeholders for each Wave 1+ implementation; both test suites exit 0; existing tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 → Wave 1+ | Test files specify the contracts implementations must satisfy; if Wave 0 is sloppy or missing tests, Wave 1+ implementations land without coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-W0-01 | Tampering | Existing test files (4 files extended) | mitigate | Append-only — no edits to existing test bodies; git diff shows additions only (acceptance criterion) |
| T-125-W0-02 | Information Disclosure | Test placeholder strings | accept | Test placeholders document expected behavior in plain text; no secrets, no PII |
</threat_model>

<verification>
After both tasks complete, run the full Wave-0 verification command set:

```bash
# Confirm all 8 W0 files exist
for f in \
  vigil-core/src/routes/quiet-mode.test.ts \
  vigil-core/src/lib/quiet-mode-suppression.test.ts \
  vigil-core/src/lib/__tests__/agent-events-bus.test.ts \
  vigil-core/src/routes/__tests__/agent-stream.test.ts \
  vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts \
  vigil-g2-plugin/src/lib/__tests__/sse-client.test.ts \
  vigil-g2-plugin/src/screens/__tests__/companion.test.ts; do
  test -f "$f" || { echo "MISSING: $f"; exit 1; }
done
echo "All 7 explicit W0 files present" # 7 because agent-events-bus extended counts once

# Confirm both suites exit 0 (skip counts as pass)
(cd vigil-core && npm test) && (cd vigil-g2-plugin && npm test)
```

The 8th W0 item — VERIFIED.md timestamp refresh — is intentionally NOT in this plan; it's a Wave 3 ship-prep responsibility (Plan 08) because the timestamp must be < 24h fresh AT pack time, not at Wave-0 time.
</verification>

<success_criteria>
- All 7 test files (3 NEW + 4 EXTENDED) committed
- Both `npm test` runs exit 0
- Wave 1+ plans can reference these files in their `<verify><automated>` blocks without `MISSING — Wave 0 must create...` warnings
- Zero modifications to pre-existing test bodies (append-only)
</success_criteria>

<output>
After completion, create `.planning/phases/125-quiet-mode-remaining-polish-riders-plugin-v0-3-0-ship-portfo/125-01-SUMMARY.md` recording: which 3 NEW files created, which 4 files extended (with append byte counts), test-skip count per file, and exit codes for both repo `npm test` runs.
</output>
