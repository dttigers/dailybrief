---
phase: 47-brief-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [vigil-core/src/db/schema.ts, vigil-core/src/db/types.ts, vigil-core/src/routes/brief-history.ts, vigil-core/src/index.ts, vigil-core/drizzle/]
autonomous: true
---

<objective>
Add brief history storage to Vigil Core API — table schema, migration, and CRUD endpoints.

Purpose: Enable persisting daily brief snapshots so users can browse and reprint past briefs. Currently brief data is ephemeral (computed on-demand, never saved). This plan creates the backend infrastructure.
Output: `briefs` table in PostgreSQL, POST endpoint to save snapshots, GET endpoints to list/retrieve history.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Vigil Core API patterns (follow existing conventions exactly):
@vigil-core/src/db/schema.ts
@vigil-core/src/db/types.ts
@vigil-core/src/routes/thoughts.ts
@vigil-core/src/routes/brief.ts
@vigil-core/src/index.ts
@vigil-core/drizzle/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add briefs table schema and generate migration</name>
  <files>vigil-core/src/db/schema.ts, vigil-core/src/db/types.ts</files>
  <action>
Add a `briefs` table to schema.ts following the existing Drizzle ORM pattern (pgTable with typed columns):

Table: `briefs`
- `id`: serial primary key
- `date`: date, not null, unique (one brief per day)
- `summary`: jsonb, not null — stores the full brief snapshot (thought counts, categories, open tasks, therapy data, sports, affirmation, calendar events summary)
- `pdfFilename`: text, nullable — the local PDF filename (e.g., "daily_sheet_2026-04-05.pdf") for reprint reference
- `thoughtCount`: integer, not null — total thoughts at time of generation
- `taskCount`: integer, not null — open tasks at time of generation
- `createdAt`: timestamp with timezone, default now, not null

Add indexes: unique index on `date`, index on `createdAt` (for pagination).

In types.ts, add:
- `DrizzleBrief` type (infer from table)
- `NewBrief` type (insert type)

Then generate the Drizzle migration:
```bash
cd vigil-core && npx drizzle-kit generate
```

Verify the generated SQL migration file looks correct (new table with columns, indexes, unique constraint on date).
  </action>
  <verify>
1. `cd vigil-core && npx drizzle-kit generate` completes without errors
2. New migration SQL file exists in vigil-core/drizzle/ with CREATE TABLE briefs
3. TypeScript compiles: `cd vigil-core && npx tsc --noEmit`
  </verify>
  <done>briefs table defined in schema.ts, types exported in types.ts, migration SQL generated</done>
</task>

<task type="auto">
  <name>Task 2: Add brief history API endpoints</name>
  <files>vigil-core/src/routes/brief-history.ts, vigil-core/src/index.ts</files>
  <action>
Create `vigil-core/src/routes/brief-history.ts` following the pattern in thoughts.ts and brief.ts:

1. **POST /briefs** — Save a brief snapshot
   - Body: `{ date: string (YYYY-MM-DD), summary: object, pdfFilename?: string, thoughtCount: number, taskCount: number }`
   - Validate required fields, parse date
   - Upsert (on conflict on `date`, update summary/counts/filename) — so regenerating a brief for the same day updates the record
   - Return 201 with the saved brief record

2. **GET /briefs** — List brief history
   - Query params: `limit` (default 30, max 200), `offset` (default 0), `from` (date), `to` (date)
   - Order by date DESC (most recent first)
   - Return paginated response: `{ data: Brief[], total: number, limit: number, offset: number }`

3. **GET /briefs/:date** — Get specific brief by date
   - Param: date as YYYY-MM-DD string
   - Return brief record or 404

Mount the router in index.ts: `app.route("/v1", briefHistory)` alongside other protected routes.
  </action>
  <verify>
1. `cd vigil-core && npx tsc --noEmit` passes
2. `cd vigil-core && npm run build` succeeds (or equivalent build command)
3. Start server locally and test:
   - POST /v1/briefs with test data returns 201
   - GET /v1/briefs returns paginated list
   - GET /v1/briefs/2026-04-05 returns specific brief or 404
  </verify>
  <done>Three endpoints working: POST /v1/briefs (upsert), GET /v1/briefs (list with pagination), GET /v1/briefs/:date (single). All follow existing API conventions (auth, error handling, response format).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd vigil-core && npx tsc --noEmit` passes
- [ ] Migration file generated in vigil-core/drizzle/
- [ ] All three endpoints respond correctly
- [ ] Upsert works (POST same date twice updates record)
</verification>

<success_criteria>
- All tasks completed
- briefs table schema defined with proper types and indexes
- Migration generated and ready to apply
- POST, GET list, GET single endpoints working
- Follows existing Vigil Core conventions (Hono router, Drizzle queries, auth middleware)
</success_criteria>

<output>
After completion, create `.planning/phases/47-brief-history/47-01-SUMMARY.md`
</output>
