---
phase: 127
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-core/src/db/schema.ts
  - vigil-core/drizzle/0020_add_ai_usage_daily.sql
  - vigil-core/src/lib/ai-budget.ts
  - vigil-core/src/lib/ai-budget.test.ts
autonomous: false
requirements: [GUARD-03]
threat_refs: [T-127-03]
user_setup:
  - service: postgres-local-dev
    why: "0020_add_ai_usage_daily.sql migration must apply against the developer's local Postgres before ai-budget.test.ts can run integration-style assertions"
    env_vars:
      - name: DATABASE_URL
        source: "Already set in vigil-core/.env from Phase 107.1 local-dev setup"
    dashboard_config:
      - task: "Run pnpm db:migrate against local Postgres after 0020 SQL file is committed"
        location: "Shell — `cd vigil-core && pnpm db:migrate` (uses drizzle-orm/postgres-js/migrator via src/db/migrate.ts)"
must_haves:
  truths:
    - "ai_usage_daily table exists in vigil-core schema with composite PK (user_id, usage_date), usd_estimate numeric(12,6) NOT NULL DEFAULT 0, idx_ai_usage_daily_date index"
    - "drizzle/0020_add_ai_usage_daily.sql is hand-crafted with CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS (Railway partial-fail-on-restart safety)"
    - "requireAiBudget(userId) reads today's ai_usage_daily row scoped by eq(table.userId, userId) and throws DailyBudgetExceededError when usd_estimate >= cap"
    - "withBudgetTracking(userId, fn) accumulates response.usage.input_tokens × $3/1M + output_tokens × $15/1M into ai_usage_daily via INSERT ... ON CONFLICT (user_id, usage_date) DO UPDATE"
    - "Token-counting failures are non-fatal (try/catch + console.error, function still returns fn() response)"
    - "DailyBudgetExceededError.code === 'DAILY_AI_BUDGET_EXCEEDED' (literal lock matching ERROR_CODE_MAP key)"
    - "VIGIL_DAILY_AI_BUDGET_USD env var overrides the 0.50 USD default"
    - "ai-budget.ts exports `__computeUsdForTest(input_tokens, output_tokens, model)` and `__readCapUsdForTest()` as test-only pure helpers so accumulator math + cap-env-read are unit-testable without a live DB"
  artifacts:
    - path: "vigil-core/src/db/schema.ts"
      provides: "aiUsageDaily Drizzle table export with composite PK + index"
      contains: "aiUsageDaily"
    - path: "vigil-core/drizzle/0020_add_ai_usage_daily.sql"
      provides: "Hand-crafted migration for ai_usage_daily table"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "vigil-core/src/lib/ai-budget.ts"
      provides: "requireAiBudget + withBudgetTracking + DailyBudgetExceededError + price constants + __computeUsdForTest + __readCapUsdForTest test helpers"
      exports: ["requireAiBudget", "withBudgetTracking", "DailyBudgetExceededError", "__computeUsdForTest", "__readCapUsdForTest"]
    - path: "vigil-core/src/lib/ai-budget.test.ts"
      provides: "Unit tests: env override, no-DB no-op, watermark throw, accumulator math, accumulator failure non-fatal — ALL 7 enumerated cases pass (no skip allowance)"
      min_lines: 80
  key_links:
    - from: "vigil-core/src/lib/ai-budget.ts requireAiBudget"
      to: "vigil-core/src/db/schema.ts aiUsageDaily.userId"
      via: "eq(aiUsageDaily.userId, userId) — W-01 cross-user-isolation pattern"
      pattern: 'eq\(aiUsageDaily\.userId,\s*userId\)'
    - from: "vigil-core/src/lib/ai-budget.ts withBudgetTracking"
      to: "ai_usage_daily table"
      via: "INSERT ... ON CONFLICT (user_id, usage_date) DO UPDATE — atomic accumulator"
      pattern: 'ON CONFLICT.*user_id.*usage_date.*DO UPDATE'
---

<objective>
Land GUARD-03 storage + library: the `ai_usage_daily` Drizzle table, its hand-crafted `0020_add_ai_usage_daily.sql` migration (with the migration application BLOCKING checkpoint), and the new `vigil-core/src/lib/ai-budget.ts` library that exports `requireAiBudget`, `withBudgetTracking`, `DailyBudgetExceededError`, plus the test-only pure helpers `__computeUsdForTest` and `__readCapUsdForTest`.

Purpose: GUARD-03 closes T-127-03 — single-user AI-cost DoS where chat + voice + insight regen could burn the $500/mo workspace cap in one day. Plan 05 lands the storage + helper library; Plans 05.1a/05.1b wire the helpers into routes, app.onError, and the AI client; Plan 06 lands the PWA-side locked-enum extension.

**Critical re-scopes vs CONTEXT.md** (per RESEARCH.md):
- `usd_estimate` type is `numeric(12,6)` (NOT `numeric(10,4)`) — Pitfall 9 sub-cent precision.
- Migration filename is `0020_add_ai_usage_daily.sql` (NOT `0020_reconcile_work_orders_107_1.sql` — that drift was already migrated in 0013 per Pitfall 2; GUARD-04 / Plan 07 ships only the drift detector).
- Migration apply path is `pnpm db:migrate` (drizzle-orm/postgres-js/migrator via src/db/migrate.ts), NOT `drizzle-kit push` (RESEARCH §Anti-pattern 4).
- **Two test-only pure helpers are now REQUIRED exports** (`__computeUsdForTest`, `__readCapUsdForTest`) — they let the math-only tests (Test 6 accumulator non-fatal path / Test 7 USD math) run as trivial pure-function assertions without DB or mock.module gymnastics. All 7 enumerated tests must pass; the prior "at least 5 of 7" escape hatch is removed.

Output: New table + migration committed, library with type-safe pre-flight + accumulator wrappers, unit tests covering env override / no-DB shape / watermark throw / accumulator math / non-fatal-failure shape. Plan 05.1a follows to wire the helpers into the AI client; Plan 05.1b adds the chat/index/integration glue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@.planning/phases/127-pre-spike-guardrails/127-RESEARCH.md
@.planning/phases/127-pre-spike-guardrails/127-PATTERNS.md

<interfaces>
Composite-PK + index pattern — vigil-core/src/db/schema.ts:244-258 (workOrderStatuses, the closest analog):
```typescript
export const workOrderStatuses = pgTable(
  "work_order_statuses",
  {
    caseNumber: text("case_number").notNull(),
    userId: integer("user_id")
      .notNull()
      .references(() => users.id, { onDelete: "restrict" }),
    status: text("status").notNull().default("open"),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    primaryKey({ columns: [table.userId, table.caseNumber] }),
    index("idx_work_order_statuses_user_id").on(table.userId),
  ],
);
```

Most recent migration — vigil-core/drizzle/0019_add_users_quiet_mode.sql (full body for header + `--> statement-breakpoint` convention).

Env-gate + null-singleton pattern — vigil-core/src/lib/sentry.ts:78-89 (the `if (!dsn) return;` shape for `if (!db) return;` in requireAiBudget).

Anthropic SDK response shape (verified live in RESEARCH):
```typescript
response.usage = { input_tokens: number, output_tokens: number }
```

Claude Sonnet 4 pricing (locked per D-03.3; RESEARCH A1 flagged for re-verification):
- input: $3 per 1M tokens
- output: $15 per 1M tokens
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aiUsageDaily table to schema.ts and write hand-crafted 0020 migration SQL</name>
  <files>vigil-core/src/db/schema.ts, vigil-core/drizzle/0020_add_ai_usage_daily.sql</files>
  <read_first>
    - vigil-core/src/db/schema.ts (lines 1-16 — verify `numeric` and `date` are imported from `drizzle-orm/pg-core`; if not, add them; lines 244-258 — workOrderStatuses composite-PK analog)
    - vigil-core/drizzle/0019_add_users_quiet_mode.sql (entire file — hand-crafted SQL convention: phase header, IF NOT EXISTS everywhere, `--> statement-breakpoint` separator, quoted identifiers)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/drizzle/0020_add_ai_usage_daily.sql" — locked content + section "vigil-core/src/db/schema.ts (EDIT — add aiUsageDaily table)" — locked invariants block)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 9 — numeric(12,6) precision override + §Pitfall 2 — file name is `0020_add_ai_usage_daily.sql`, NOT `0020_reconcile_work_orders_107_1.sql`)
  </read_first>
  <behavior>
    - `vigil-core/src/db/schema.ts` exports `aiUsageDaily` pgTable with columns: userId integer (FK users.id ON DELETE CASCADE), usageDate date, usdEstimate numeric(precision: 12, scale: 6) default "0", updatedAt timestamptz default now()
    - Composite PK on (userId, usageDate); index `idx_ai_usage_daily_date` on usageDate alone
    - Migration file `vigil-core/drizzle/0020_add_ai_usage_daily.sql` is hand-crafted SQL with `CREATE TABLE IF NOT EXISTS "ai_usage_daily" ...` and `CREATE INDEX IF NOT EXISTS "idx_ai_usage_daily_date" ...` separated by `--> statement-breakpoint`
    - Column type `numeric(12,6)` in migration matches Drizzle declaration `numeric("usd_estimate", { precision: 12, scale: 6 })`
    - `drizzle-kit generate` against this schema produces "No schema changes, nothing to migrate" (Plan 07's drift detector relies on this — schema.ts + migration must be in sync)
  </behavior>
  <action>
    Edit `vigil-core/src/db/schema.ts` per D-03.1 + Pitfall 9. First verify the imports at lines 1-16 include `numeric` and `date` from `drizzle-orm/pg-core`. If `numeric` is missing, add it to the import list (do NOT introduce a new import line if it can extend the existing one).

    Append a new export block AFTER the existing `workOrderStatuses` definition (lines 244-258) — choose a stable position (suggest: immediately after workOrderStatuses for affinity with the W-01-style composite-PK table cluster):

    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `aiUsageDaily`. Match the analog body verbatim except for the specific edits called out in this action.]

    Locked invariants (do NOT change):
    - `onDelete: "cascade"` — different from workOrderStatuses' `"restrict"`. Matches CONTEXT D-03.1 SQL verbatim (`ON DELETE CASCADE`).
    - Composite PK `(userId, usageDate)` — userId FIRST (W-01 grep-friendly).
    - `idx_ai_usage_daily_date` on usageDate alone (NOT user_id — the PK already gives an implicit index covering the userId leading column).
    - `numeric(12,6)` precision — Pitfall 9 override of CONTEXT's `numeric(10,4)`.

    Create `vigil-core/drizzle/0020_add_ai_usage_daily.sql` per Pitfall 2 (filename) + PATTERNS locked content + Pitfall 9 (numeric precision). Full body (verbatim):

    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    DO NOT name the file `0020_reconcile_work_orders_107_1.sql` (Pitfall 2 — that drift is already in 0013).

    After writing the SQL file, run `drizzle-kit generate` once (without committing its output) to let drizzle auto-create the `meta/_journal.json` entry + `meta/0020_snapshot.json` snapshot file. Verify the generated metadata aligns with the hand-crafted SQL: column types, PK shape, index name. If `drizzle-kit generate` instead emits a NEW `.sql` migration file (because schema.ts and the journal disagree), STOP — there's a mismatch between the Drizzle TypeScript declaration and the hand-crafted SQL. Most likely cause: `numeric(12,6)` declared in schema doesn't match SQL DDL. Fix the mismatch before proceeding.

    Acceptance: after committing both files + the auto-generated meta, `drizzle-kit generate` prints `No schema changes, nothing to migrate 😴` (Plan 07's drift detector relies on this).
  </action>
  <verify>
    <automated>cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx drizzle-kit generate 2>&1 | grep -iE 'No schema changes|nothing to migrate'</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-core/src/db/schema.ts` contains `export const aiUsageDaily = pgTable(` block
    - `grep -c 'numeric("usd_estimate", { precision: 12, scale: 6 })' vigil-core/src/db/schema.ts` === 1 (Pitfall 9 lock)
    - `grep -c 'onDelete: "cascade"' vigil-core/src/db/schema.ts` >= 1 in the aiUsageDaily block (verify with grep + context: `grep -B1 -A1 'onDelete: "cascade"' vigil-core/src/db/schema.ts | grep -B2 'aiUsageDaily'`)
    - `vigil-core/drizzle/0020_add_ai_usage_daily.sql` exists with filename matching exactly
    - `grep -c 'CREATE TABLE IF NOT EXISTS "ai_usage_daily"' vigil-core/drizzle/0020_add_ai_usage_daily.sql` === 1
    - `grep -c 'CREATE INDEX IF NOT EXISTS "idx_ai_usage_daily_date"' vigil-core/drizzle/0020_add_ai_usage_daily.sql` === 1
    - `grep -c '--> statement-breakpoint' vigil-core/drizzle/0020_add_ai_usage_daily.sql` === 1
    - `grep -c 'numeric(12,6)' vigil-core/drizzle/0020_add_ai_usage_daily.sql` === 1
    - `cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx drizzle-kit generate` stdout matches `/No schema changes/i` (Plan 07's drift detector premise — this is the same command)
    - No file named `0020_reconcile_work_orders_107_1.sql` exists in `vigil-core/drizzle/`
  </acceptance_criteria>
  <done>Table + migration committed; drizzle-kit reports schema in sync; auto-generated metadata files (`drizzle/meta/0020_snapshot.json`, `_journal.json`) included in the commit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT): Apply 0020 migration to local Postgres before ai-budget.test.ts integration assertions can run</name>
  <what-built>
    Task 1 created `vigil-core/drizzle/0020_add_ai_usage_daily.sql` and the `aiUsageDaily` Drizzle table. The migration must be applied to the developer's local Postgres BEFORE Task 3's `ai-budget.test.ts` integration assertions can run — the test queries `ai_usage_daily` directly (via Drizzle) and needs the table to exist.

    This project does NOT use `drizzle-kit push` (RESEARCH §Anti-pattern 4 — push bypasses the drizzle/ folder; rejected). The canonical apply path is `pnpm db:migrate` which invokes `drizzle-orm/postgres-js/migrator` via `vigil-core/src/db/migrate.ts`.
  </what-built>
  <how-to-verify>
    1. Run the migration locally:
       ```
       cd vigil-core && pnpm db:migrate
       ```
       Expected output: a log line for `0020_add_ai_usage_daily.sql` being applied (or "no pending migrations" if the apply path already ran during Task 1).

    2. Verify the table exists with the right shape:
       ```
       psql "$DATABASE_URL" -c "\d ai_usage_daily"
       ```
       Expected: 4 columns (user_id, usage_date, usd_estimate, updated_at); composite PK; `idx_ai_usage_daily_date` index visible.

    3. Verify column types match the migration:
       ```
       psql "$DATABASE_URL" -c "SELECT column_name, data_type, numeric_precision, numeric_scale FROM information_schema.columns WHERE table_name = 'ai_usage_daily';"
       ```
       Expected: `usd_estimate` row shows `data_type=numeric, numeric_precision=12, numeric_scale=6` (Pitfall 9 lock applied in production schema).

    4. (Railway deploy note for the SUMMARY) — on Railway, the migration applies during the next deploy via the existing post-deploy hook. Flag this in 127-05-SUMMARY.md so Plan 05.1a / Plan 05.1b / Plan 06 know prod schema is one deploy behind until that ships.
  </how-to-verify>
  <resume-signal>
    Type "applied" if Step 1 succeeded and Step 3 confirms `numeric_precision=12, numeric_scale=6`. Type "blocked: <reason>" if `pnpm db:migrate` errors (likely DATABASE_URL pointing at a stale schema or missing FK target — Postgres errors are loud).
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ai-budget.ts library (requireAiBudget + withBudgetTracking + DailyBudgetExceededError + test helpers) + unit test (all 7 cases REQUIRED)</name>
  <files>vigil-core/src/lib/ai-budget.ts, vigil-core/src/lib/ai-budget.test.ts</files>
  <read_first>
    - vigil-core/src/lib/sentry.ts (entire file — module-shape analog: env-gate `if (!dsn) return;` shape, module-state flag, exported pure functions)
    - vigil-core/src/lib/sentry.test.ts (env-mutation + dynamic import pattern; capture/restore env vars; describe/it/beforeEach/afterEach)
    - vigil-core/src/db/schema.ts (Task 1 output — provides `aiUsageDaily` table type)
    - vigil-core/src/db/connection.ts (verify db nullable shape — `db` is `null` when DATABASE_URL absent; ai-budget needs the same `if (!db) return;` early return)
    - vigil-core/src/analytics/posthog.ts (lines 139-149 — `trackEvent` catch-and-log pattern; mirror for the accumulator's non-fatal failure shape)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/src/lib/ai-budget.ts" — full locked body for both functions and error class; section "vigil-core/src/lib/ai-budget.test.ts" — test case list)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pattern 2 + §Pattern 3 — full reference implementations; §Pitfall 9 — accumulator math vs precision)
    - .planning/phases/127-pre-spike-guardrails/127-CONTEXT.md (D-03.2 — VIGIL_DAILY_AI_BUDGET_USD env override; D-03.3 — Sonnet 4 pricing constants $3/$15 per 1M; D-03.6 — UTC 00:00 rollover via CURRENT_DATE)
  </read_first>
  <behavior>
    - `DailyBudgetExceededError` extends Error, has readonly code "DAILY_AI_BUDGET_EXCEEDED" literal, accepts userId + usdEstimate in constructor, sets this.name = "DailyBudgetExceededError"
    - `requireAiBudget(userId: number): Promise<void>` returns silently when `db` is null (local-dev shape)
    - `requireAiBudget(userId)` queries ai_usage_daily WHERE userId AND usage_date=CURRENT_DATE; if `usd_estimate >= cap`, throws `DailyBudgetExceededError(userId, current)`
    - Cap reads from `process.env.VIGIL_DAILY_AI_BUDGET_USD` (parsed as float; default 0.50 if missing or invalid)
    - `withBudgetTracking(userId, fn)` calls `fn()`, reads `response.usage?.input_tokens` + `response.usage?.output_tokens`, computes `usd = input × 3/1M + output × 15/1M`, executes `INSERT … ON CONFLICT (user_id, usage_date) DO UPDATE SET usd_estimate = ai_usage_daily.usd_estimate + EXCLUDED.usd_estimate, updated_at = NOW()`, then returns `response`
    - If accumulator INSERT throws, `withBudgetTracking` catches + console.errors + still returns `response` (non-fatal — D-03.3 lock; matches `trackEvent` catch-and-log)
    - `withBudgetTracking` does NOT throw on missing `usage` field — defaults tokens to 0, computes usd=0, skips INSERT when `usd > 0 && db` is false
    - **Two test-only pure helpers are EXPORTED**: `__computeUsdForTest(input_tokens, output_tokens, model)` returns the same USD figure withBudgetTracking computes (no DB, no side effects), and `__readCapUsdForTest()` returns the same cap value requireAiBudget consults (reads VIGIL_DAILY_AI_BUDGET_USD env or returns 0.50). Both delegate to the same internal pure functions used in production paths — they ARE the production math, just exposed for tests.
  </behavior>
  <action>
    Create `vigil-core/src/lib/ai-budget.ts` per D-03.3, D-03.4, RESEARCH §Pattern 2 + §Pattern 3. Imports:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Define and export the error class:
    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `DailyBudgetExceededError`. Match the analog body verbatim except for the specific edits called out in this action.]

    Define module-private constants:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Define pure helpers (the math the production paths use):
    - `function computeUsd(input_tokens: number, output_tokens: number, model: string = "claude-sonnet-4"): number` — pure: `input_tokens × INPUT_PRICE_PER_TOKEN + output_tokens × OUTPUT_PRICE_PER_TOKEN`. For Phase 127 the only model is Sonnet 4, but accept a model parameter to keep the signature forward-compatible (Phase 130 may add audio-billed branches).
    - `function readCapUsd(): number` — pure: parses `process.env.VIGIL_DAILY_AI_BUDGET_USD` as float; returns the parsed value if finite and ≥ 0; otherwise returns 0.50 (default).

    Define and export the test-only re-exports (REQUIRED — Plan 05 frontmatter `exports` includes them):
    - `export const __computeUsdForTest = computeUsd;`
    - `export const __readCapUsdForTest = readCapUsd;`

    Use the `__` (double-underscore) prefix per CONTEXT D-03 "Claude's Discretion" naming convention; the prefix marks these as test-only entry points the rest of the codebase MUST NOT import. (No `// @internal` JSDoc is enough at the linter level without TS-noEmit guards, but flag them in JSDoc as "test-only — do not import from production code paths.")

    Define and export `requireAiBudget`:
    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `requireAiBudget`. Match the analog body verbatim except for the specific edits called out in this action.]

    Internally `requireAiBudget` MUST call `readCapUsd()` (not inline-duplicate the env read) so the test-only `__readCapUsdForTest()` export is identity-equal to the production code path.

    Define and export `withBudgetTracking`:
    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `withBudgetTracking`. Match the analog body verbatim except for the specific edits called out in this action.]

    Internally `withBudgetTracking` MUST call `computeUsd(input_tokens, output_tokens, model)` (not inline-duplicate the multiplication) so the test-only `__computeUsdForTest()` export is identity-equal to the production code path.

    Locked invariants (do NOT change):
    - The literal string `"DAILY_AI_BUDGET_EXCEEDED"` (matches `ERROR_CODE_MAP` key in Plan 06 verbatim).
    - `this.name = "DailyBudgetExceededError"` (preserves `instanceof` across module boundaries — required by Plan 05.1b `app.onError` branch).
    - `eq(aiUsageDaily.userId, userId)` — W-01 lock. Plan 05.1b extends cross-user-isolation.test.ts to grep for this.
    - `CURRENT_DATE` Postgres function — driven by SQL, NOT JavaScript date math.
    - `console.error` (NOT `console.log` or `console.warn`) — pairs with the existing trackEvent catch-and-log pattern and the source-grep drift detector at audio-log-redaction.test.ts Rail 3 (this file is in the denylist safe-list per Plan 01 Task 3 because `console.error` does NOT match `audio|pcm` regex — safe).
    - Accumulator INSERT is AWAITED (per Plan 05 Claude's Discretion 2 + CONTEXT — missed accumulation defeats the cap).
    - **`__computeUsdForTest` and `__readCapUsdForTest` are REQUIRED exports** — both must be re-exported aliases of the same internal `computeUsd` / `readCapUsd` functions used by the production code paths. Plan 05's frontmatter `exports` list includes them; a missing export is a blocker.

    Create `vigil-core/src/lib/ai-budget.test.ts` mirroring `sentry.test.ts` (env-mutation + dynamic-import). Imports + env capture:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    All 7 test cases below are REQUIRED (no skip allowance — the test helpers above exist precisely so Tests 6 and 7 are trivial pure-function assertions without DB or mock.module gymnastics):

    1. **`DailyBudgetExceededError carries the locked code and name`** — instantiate `new DailyBudgetExceededError(42, 0.51)`; assert `err.code === "DAILY_AI_BUDGET_EXCEEDED"`, `err.name === "DailyBudgetExceededError"`, `err instanceof Error === true`, `err.userId === 42`, `err.usdEstimate === 0.51`.

    2. **`__readCapUsdForTest default is 0.50`** — delete `process.env.VIGIL_DAILY_AI_BUDGET_USD`; dynamic-import the module; call `__readCapUsdForTest()`; assert `=== 0.50`. Restore env after.

    3. **`__readCapUsdForTest respects VIGIL_DAILY_AI_BUDGET_USD env override`** — set `process.env.VIGIL_DAILY_AI_BUDGET_USD = "1.25"`; dynamic-import (fresh module load to re-read env if the module caches); `__readCapUsdForTest()` returns `1.25`. Restore env after. (If the helper reads env at every call, no re-import needed — verify the production behavior matches a single observable.)

    4. **`__readCapUsdForTest falls back to 0.50 on invalid env value`** — set `process.env.VIGIL_DAILY_AI_BUDGET_USD = "not-a-number"`; assert `__readCapUsdForTest() === 0.50`. Set to `""`; same expectation. Set to negative `"-1"`; same expectation (cap must be ≥ 0).

    5. **`withBudgetTracking returns fn() response unchanged when usage is missing`** — pass `fn = async () => ({ ok: true })` (no `usage` field); await `withBudgetTracking(42, fn)`; assert returned `{ok: true}`; assert no error thrown. (Skips INSERT because computed usd is 0.)

    6. **`withBudgetTracking accumulator failure is non-fatal — verified via __computeUsdForTest + spy on console.error`** — primary assertion uses `__computeUsdForTest`: assert `__computeUsdForTest(100, 50, "claude-sonnet-4") === 100 * 3/1_000_000 + 50 * 15/1_000_000`. Secondary assertion: capture `console.error` calls (use `node:test` `mock.method(console, "error", ...)` or wrap `console.error` in a fake), induce an accumulator failure (mock the DB execute path OR force the module to use a bad DATABASE_URL — pick whichever the executor finds least painful given the existing connection-module seam), pass `fn = async () => ({ usage: { input_tokens: 100, output_tokens: 50 }, ok: true })`; assert returned value still `{usage: ..., ok: true}` AND `console.error` was called at least once with a string containing `"withBudgetTracking accumulator failed"`. Restore the stub at test end. **No skip allowed** — if the DB seam is intractable, replace the secondary half with a direct unit test of the catch block via a `__withBudgetTrackingForTest` helper or by extracting the catch behavior into a named private function that can be re-imported. The fix is a refactor, not a skip.

    7. **`__computeUsdForTest math: 1M input tokens === $3.00 USD`** — assert `__computeUsdForTest(1_000_000, 0, "claude-sonnet-4") === 3.0` and `__computeUsdForTest(0, 1_000_000, "claude-sonnet-4") === 15.0`. Additional sanity: `__computeUsdForTest(0, 0) === 0`; `__computeUsdForTest(100, 50) === 100 * 3/1_000_000 + 50 * 15/1_000_000`.

    Recommendation if you encounter Node-version-specific mock seams: the production module ships the `__test` exports specifically to AVOID the mock.module dance — use them. Tests 5 and 6's withBudgetTracking-shaped assertions can stub `console.error` via simple `const original = console.error; console.error = mock; try { ... } finally { console.error = original; }` — no module mocking required for the `console.error` capture path.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/lib/ai-budget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-core/src/lib/ai-budget.ts` exists
    - `grep -c 'export class DailyBudgetExceededError' vigil-core/src/lib/ai-budget.ts` === 1
    - `grep -c '"DAILY_AI_BUDGET_EXCEEDED"' vigil-core/src/lib/ai-budget.ts` === 1 (literal code lock)
    - `grep -c 'export async function requireAiBudget' vigil-core/src/lib/ai-budget.ts` === 1
    - `grep -c 'export async function withBudgetTracking' vigil-core/src/lib/ai-budget.ts` === 1
    - `grep -c 'export const __computeUsdForTest' vigil-core/src/lib/ai-budget.ts` === 1 (REQUIRED test helper export)
    - `grep -c 'export const __readCapUsdForTest' vigil-core/src/lib/ai-budget.ts` === 1 (REQUIRED test helper export)
    - `grep -c 'eq(aiUsageDaily.userId, userId)' vigil-core/src/lib/ai-budget.ts` === 1 (W-01 lock; Plan 05.1b cross-user-isolation grep relies on this)
    - `grep -c 'CURRENT_DATE' vigil-core/src/lib/ai-budget.ts` >= 2 (in requireAiBudget query + withBudgetTracking INSERT)
    - `grep -c 'ON CONFLICT.*user_id.*usage_date.*DO UPDATE' vigil-core/src/lib/ai-budget.ts` === 1 (atomic accumulator pattern)
    - `grep -c 'INPUT_PRICE_PER_TOKEN = 3 / 1_000_000' vigil-core/src/lib/ai-budget.ts` === 1 (Sonnet 4 price lock)
    - `grep -c 'OUTPUT_PRICE_PER_TOKEN = 15 / 1_000_000' vigil-core/src/lib/ai-budget.ts` === 1
    - `cd vigil-core && npx tsx --test src/lib/ai-budget.test.ts` exits 0
    - **ALL 7 enumerated test cases pass** — no skips, no "integration deferred" escape hatch. The `__computeUsdForTest` and `__readCapUsdForTest` helpers exist specifically to remove the DB-seam excuse.
    - File is in audio-log-redaction.test.ts Rail 3 denylist safe-list (Plan 01 Task 3 already includes `lib/ai-budget.ts`)
  </acceptance_criteria>
  <done>ai-budget.ts library landed with full Pattern 2 + Pattern 3 shape; DailyBudgetExceededError ready for Plan 05.1b app.onError branch; __computeUsdForTest + __readCapUsdForTest test-only helpers exported; all 7 enumerated tests green; Plan 05.1a / 05.1b pick up wiring + integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vigil-core ai/client.ts → Anthropic API | Token spend bills to the workspace cap; per-user accumulation in ai_usage_daily is the in-app fair-use sub-cap |
| Route handler → ai-budget.ts library | requireAiBudget is in-handler (not middleware) because it needs `c.get("userId")` — Plan 05.1b wires this |
| Postgres ai_usage_daily table | Composite PK (user_id, usage_date) enforces W-01 per Phase 121 lock; reads filter `eq(table.userId, userId)` always |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | Denial of Service (cross-feature AI cost burn) | Single user combines voice transcription + chat context expansion + insight regen on a chatty day — could burn $5+ via Anthropic Sonnet 4 + audio billing, exhausting the $500/mo workspace cap | mitigate | `ai_usage_daily` table with `numeric(12,6)` precision; `requireAiBudget(userId)` reads today's row and throws `DailyBudgetExceededError` when usd_estimate >= cap (0.50 USD default). `withBudgetTracking(userId, fn)` accumulates Sonnet 4 input × $3/1M + output × $15/1M after every AI call via atomic INSERT ... ON CONFLICT DO UPDATE. Workspace cap stays as absolute backstop (Phase 126); per-user cap is fair-use guardrail |
| T-127-03-A | Tampering (silent budget under-counting) | Accumulator failure → INSERT silently dropped → user's row stays low → no rejection until much later | mitigate | INSERT is AWAITED (D-03.3 — performance impact accepted for correctness); on accumulator throw, `console.error` logs the failure, function still returns response (Plan 05.1a's drift detector pinning all `ai.messages.create` sites are wrapped covers the dual-amplification risk from Pitfall 4) |
| T-127-03-B | Elevation of Privilege (cross-user read of ai_usage_daily) | A future route handler reads ai_usage_daily without the userId scope filter | mitigate | Composite PK `(user_id, usage_date)` is the W-01 pattern; every read in `requireAiBudget` uses `eq(aiUsageDaily.userId, userId)`; Plan 05.1b extends `cross-user-isolation.test.ts` to assert userA's row is never returned to userB |
| T-127-03-C | Tampering (env-override abuse) | `VIGIL_DAILY_AI_BUDGET_USD` env var bypass — operator sets to large value to silence the cap | accept | Env var is operator-controlled at Railway dashboard level; sets the watermark, not the cap. Workspace cap (Phase 126 $500/mo prod) remains the absolute hard limit. Documented in 127-05-SUMMARY |
| T-127-03-D | Information Disclosure | DailyBudgetExceededError message includes `userId` and `usdEstimate` — leaks to whoever sees the error log | accept | The error is caught by `app.onError` (Plan 05.1b) BEFORE Sentry/PostHog sink (Pitfall 5); the JSON response to the client only includes `code` + generic error string. The internal log line is fine — operator-visible only |
</threat_model>

<verification>
- `cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx drizzle-kit generate 2>&1 | grep -iE 'No schema changes|nothing to migrate'` returns one line
- `cd vigil-core && npx tsx --test src/lib/ai-budget.test.ts` exits 0 with ALL 7 cases passing
- After `pnpm db:migrate` against local Postgres: `psql -c "\d ai_usage_daily"` shows the table with composite PK + index
- `grep -E 'eq\(aiUsageDaily\.userId' vigil-core/src/lib/ai-budget.ts` returns ≥ 1 line (W-01 lock)
- `grep -E 'export const __(compute|read)' vigil-core/src/lib/ai-budget.ts` returns 2 lines (test helpers exported)
</verification>

<success_criteria>
- aiUsageDaily Drizzle table exported from schema.ts with composite PK (userId, usageDate), numeric(12,6) usd_estimate, onDelete cascade
- `vigil-core/drizzle/0020_add_ai_usage_daily.sql` is hand-crafted with CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS + --> statement-breakpoint
- drizzle-kit generate confirms schema-vs-migration parity ("No schema changes, nothing to migrate")
- Local Postgres migration applied via `pnpm db:migrate` (CHECKPOINT confirmed by operator)
- `vigil-core/src/lib/ai-budget.ts` exports requireAiBudget, withBudgetTracking, DailyBudgetExceededError, __computeUsdForTest, __readCapUsdForTest with literal `code: "DAILY_AI_BUDGET_EXCEEDED"` matching Plan 06's ERROR_CODE_MAP key
- ai-budget.test.ts green for ALL 7 enumerated cases (error-class lock, default cap, env override, invalid-env fallback, missing-usage no-throw, accumulator non-fatal failure with console.error capture, USD math via __computeUsdForTest)
- Plan 05.1a inheritance: AI client wrappers + Pitfall 4 process-audio.ts wrap all queued
- Plan 05.1b inheritance: route wiring + app.onError branch + cross-user-isolation extension all queued
</success_criteria>

<output>
After completion, create `.planning/phases/127-pre-spike-guardrails/127-05-SUMMARY.md` capturing:
- Files modified (4 — schema.ts EDIT, 0020_add_ai_usage_daily.sql NEW, ai-budget.ts NEW, ai-budget.test.ts NEW; plus auto-generated drizzle/meta/0020_snapshot.json + _journal.json)
- Re-scopes applied: numeric(12,6) per Pitfall 9, filename `0020_add_ai_usage_daily.sql` per Pitfall 2, `__computeUsdForTest` + `__readCapUsdForTest` test-helpers required so all 7 unit tests pass without DB seam gymnastics
- Local-dev migration applied via `pnpm db:migrate` (operator confirmed checkpoint)
- Railway deploy note: prod schema applies on next deploy; document for Plan 06 deploy plan
- Plan 05.1a dependencies: requires this plan's library exports (withBudgetTracking signature + DailyBudgetExceededError class); will wrap ai/client.ts + process-audio.ts:82-115 direct beta.* path; Wave 2
- Plan 05.1b dependencies: requires Plan 05.1a's wrapped client + applied migration; will wire chat.ts requireAiBudget gate + index.ts app.onError branch + cross-user-isolation.test.ts; Wave 3
- Future risk surfaced: workspace AI cap ($500/mo) is the absolute backstop; per-user $0.50/day is fair-use guardrail. Per-user-overridable caps deferred per CONTEXT
</output>
