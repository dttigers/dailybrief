---
phase: 127
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-g2-plugin/src/lib/audio-session-guard.ts
  - vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts
autonomous: true
requirements: [GUARD-02]
threat_refs: [T-127-02]
must_haves:
  truths:
    - "safeAudioControl(true) registers cleanup hooks for ABNORMAL_EXIT_EVENT, SYSTEM_EXIT_EVENT, beforeunload, and onBackgroundRestore on the first true call"
    - "Subsequent safeAudioControl(true) calls do NOT re-register listeners (idempotency)"
    - "Each of the four exit paths fires audioControl(false) when audioActive is true"
    - "The cleanup wrapper imports OsEventTypeList as a named import (NOT hard-coded integer 6/7) — survives SDK enum renumber"
    - "setBackgroundState callback returns a JSON-serializable {audioActive: boolean} object"
    - "onBackgroundRestore handler reads the snapshot and calls audioControl(false) if audioActive was true at backgrounding"
  artifacts:
    - path: "vigil-g2-plugin/src/lib/audio-session-guard.ts"
      provides: "safeAudioControl(on) cleanup wrapper exported for Phase 130 VOICE-02 consumption"
      exports: ["safeAudioControl"]
    - path: "vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts"
      provides: "Four independent exit-event unit tests + idempotency case + onBackgroundRestore case"
      min_lines: 100
  key_links:
    - from: "Phase 130 VOICE-02 (push-to-record gesture handler)"
      to: "safeAudioControl from vigil-g2-plugin/src/lib/audio-session-guard"
      via: "import { safeAudioControl } and use instead of bridge.audioControl"
      pattern: 'import.*safeAudioControl'
    - from: "safeAudioControl internal listener registration"
      to: "OsEventTypeList enum from @evenrealities/even_hub_sdk"
      via: "named import — never integer literal"
      pattern: 'import.*\{[^}]*OsEventTypeList[^}]*\}.*even_hub_sdk'
---

<objective>
Ship the GUARD-02 G2-plugin-side cleanup wrapper: `safeAudioControl(on: boolean)` registers idempotent `audioControl(false)` cleanup hooks on all four documented exit paths (ABNORMAL_EXIT_EVENT, SYSTEM_EXIT_EVENT, `window.beforeunload`, `onBackgroundRestore` after `setBackgroundState` snapshot). The wrapper has zero callers in Phase 127; Phase 130 VOICE-02 hooks it.

Purpose: A runaway G2 mic open (e.g. crash, force-quit, headless replay) without `audioControl(false)` is the Information Disclosure + Privacy violation pathway flagged in T-127-02. The Even SDK has no automatic cleanup — the plugin author must wire ALL four exit paths manually (RESEARCH §Pitfall 7 + §A6 — use the named enum constants, never hard-coded `6`/`7` integers).

Output: A pure-module wrapper with closure-captured `audioActive` state, idempotent registration, and a six-case unit test (four exit paths + idempotency + onBackgroundRestore snapshot replay).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@.planning/phases/127-pre-spike-guardrails/127-RESEARCH.md
@.planning/phases/127-pre-spike-guardrails/127-PATTERNS.md
@.planning/research/EVEN-SKILLS.md

<interfaces>
Module-shape analog — vigil-g2-plugin/src/lib/deduped-device-status.ts (full file, 25 lines):
```typescript
import type { DeviceStatus, DeviceConnectType } from '@evenrealities/even_hub_sdk'

/**
 * Phase 125 (G2-POLISH-08 / D-12): dedupe consecutive device-status events ...
 */
export function createDedupedDeviceStatusListener(
  callback: (status: DeviceStatus) => void,
): (status: DeviceStatus) => void {
  let lastSeenConnectType: DeviceConnectType | null = null
  return (status: DeviceStatus) => {
    if (status.connectType === lastSeenConnectType) return
    lastSeenConnectType = status.connectType
    callback(status)
  }
}
```

Event-handler registration analog — vigil-g2-plugin/src/main.ts:221-260 (the only existing onEvenHubEvent registration site):
```typescript
bridge.onEvenHubEvent((event) => {
  if (event.listEvent?.eventType === OsEventTypeList.CLICK_EVENT && ...) {...}
  if (event.sysEvent && bridge) {
    const eventType = event.sysEvent.eventType
    if (eventType === OsEventTypeList.FOREGROUND_ENTER_EVENT) {...}
    else if (eventType === OsEventTypeList.FOREGROUND_EXIT_EVENT) {...}
  }
})
```

Even SDK exit-event enum (per @evenrealities/even_hub_sdk v0.0.10 + .planning/research/EVEN-SKILLS.md):
- `OsEventTypeList.ABNORMAL_EXIT_EVENT` (= 6)
- `OsEventTypeList.SYSTEM_EXIT_EVENT` (= 7)
- `bridge.setBackgroundState(key, snapshotFn)` + `bridge.onBackgroundRestore(key, restoreFn)` — Headless WebView replay path

Test-pattern analog — vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts:
- `import { test } from 'node:test'; import assert from 'node:assert/strict'`
- Real `OsEventTypeList` import from `@evenrealities/even_hub_sdk` (RESEARCH §A6)
- `.ts` extension in relative import (TS-NodeNext config — verify against the file)
- `function makeStatus(connectType): DeviceStatus { return { sn: 'TEST', connectType } as DeviceStatus }` synthesizer helper
- Bare `test(name, () => { ... })` calls (no surrounding describe)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create safeAudioControl wrapper with idempotent four-exit-event cleanup registration</name>
  <files>vigil-g2-plugin/src/lib/audio-session-guard.ts</files>
  <read_first>
    - vigil-g2-plugin/src/lib/deduped-device-status.ts (entire 25-line file — module-shape analog: import type, JSDoc header, closure-captured state, single exported function)
    - vigil-g2-plugin/src/main.ts (lines 221-260 — the only existing bridge.onEvenHubEvent registration site; mirror the `event.sysEvent?.eventType` access pattern and OsEventTypeList named-import usage)
    - .planning/research/EVEN-SKILLS.md (§"Background state" lines 68-90 — setBackgroundState / onBackgroundRestore semantics; §"Exit events" lines 46-66)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-g2-plugin/src/lib/audio-session-guard.ts" — full reference implementation in code-block; locked invariants list)
    - .planning/phases/127-pre-spike-guardrails/127-CONTEXT.md (D-02.3 — four exit hooks enumerated)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 7 — setBackgroundState requires BOTH snapshot AND restore handler registration; §A6 — enum-import requirement)
  </read_first>
  <behavior>
    - Module loads without side effects when not imported (no top-level bridge calls)
    - First `safeAudioControl(true)` call registers FOUR cleanup callbacks: onEvenHubEvent handler (for ABNORMAL_EXIT / SYSTEM_EXIT branches), window.addEventListener("beforeunload", ...), bridge.setBackgroundState("vigil-audio-guard", snapshotFn), bridge.onBackgroundRestore("vigil-audio-guard", restoreFn)
    - Module-scope `audioActive` boolean tracks current state (true after `safeAudioControl(true)`, false after `safeAudioControl(false)`)
    - Module-scope `cleanupRegistered` boolean prevents re-registration across multiple `safeAudioControl(true)` calls
    - When sysEvent.eventType === OsEventTypeList.ABNORMAL_EXIT_EVENT (or SYSTEM_EXIT_EVENT) fires AND audioActive is true: call `bridge.audioControl(false)` and set `audioActive = false`
    - When window dispatches "beforeunload" AND audioActive is true: same cleanup
    - When onBackgroundRestore handler runs with `{audioActive: true}` snapshot: same cleanup
    - All cleanup calls use `.catch(() => {})` — best-effort (mic-close SDK failure does not crash plugin)
    - `safeAudioControl(on)` always calls `await bridge.audioControl(on)` after registration step
  </behavior>
  <action>
    Create `vigil-g2-plugin/src/lib/audio-session-guard.ts` per D-02.3 + PATTERNS section "vigil-g2-plugin/src/lib/audio-session-guard.ts (utility, lifecycle wrapper)" + RESEARCH §Pitfall 7.

    Header JSDoc: cite Phase 127 GUARD-02 + D-02.3 + RESEARCH §Pitfall 7 (setBackgroundState requires both snapshot AND restore handler). State that the module has ZERO callers in Phase 127 — Phase 130 VOICE-02 will hook it (mirror the `deduped-device-status.ts` precedent for "helper-only ship" pattern).

    **JSDoc and inline comments in `audio-session-guard.ts` MUST NOT contain the bare integer literals `6` or `7`** — refer to the Even SDK exit events by their named enum members only (`OsEventTypeList.ABNORMAL_EXIT_EVENT` and `OsEventTypeList.SYSTEM_EXIT_EVENT`). Do NOT write parenthetical notes like `// ABNORMAL_EXIT_EVENT (6)` or `// fires on SDK eventType 7`. The named-enum lock (RESEARCH §A6) extends to documentation prose, not just runtime expressions. Rationale: a bare `6` or `7` in a comment makes a future grep for hard-coded enum integers ambiguous — the acceptance criterion `grep -c '\b6\b\|\b7\b' === 0` must apply to comments too.

    Imports (locked per RESEARCH §A6 + PATTERNS):
    - `import { OsEventTypeList } from '@evenrealities/even_hub_sdk'` — value import for the enum (NOT just `import type` — runtime access required for the `event.sysEvent.eventType === OsEventTypeList.ABNORMAL_EXIT_EVENT` comparison)
    - Whatever bridge type/instance import the existing plugin code uses (check `vigil-g2-plugin/src/main.ts` and `deduped-device-status.ts` for the convention — likely `import bridge from '...'` or `import type { Bridge } from '...'`; mirror exactly).

    Module-scope state (closure-captured per `deduped-device-status.ts` precedent):
    - `let audioActive = false;`
    - `let cleanupRegistered = false;`

    Export `async function safeAudioControl(on: boolean): Promise<void>` body (lifted from PATTERNS code block):

    1. If `!cleanupRegistered && on` (only register on the FIRST true call — registration is idempotent across the process lifetime):
       a. Set `cleanupRegistered = true;`.
       b. `bridge.onEvenHubEvent((ev) => { ... })` — inside, read `const t = ev.sysEvent?.eventType;` and if `t === OsEventTypeList.ABNORMAL_EXIT_EVENT || t === OsEventTypeList.SYSTEM_EXIT_EVENT`: if `audioActive` is true, set `audioActive = false` and call `bridge.audioControl(false).catch(() => {})`.
       c. If `typeof window !== "undefined"`: `window.addEventListener("beforeunload", () => { if (audioActive) { audioActive = false; bridge.audioControl(false).catch(() => {}); } });`.
       d. `bridge.setBackgroundState("vigil-audio-guard", () => ({ audioActive }))` — JSON-serializable snapshot (Pitfall 7 lock: plain object, no Map/Set/Date).
       e. `bridge.onBackgroundRestore("vigil-audio-guard", (saved: unknown) => { const s = saved as { audioActive?: boolean }; if (s.audioActive) { bridge.audioControl(false).catch(() => {}); audioActive = false; } });` — Pitfall 7 critical: the snapshot alone does not auto-fire cleanup; the restore handler must be registered alongside.

    2. After the registration block: `audioActive = on;` then `await bridge.audioControl(on);`.

    Locked invariants (do NOT change):
    - Named-enum import (NEVER hard-code 6/7 integer literals — A6 risk: silent SDK renumber break).
    - No bare `6` or `7` integer tokens anywhere in the file, including JSDoc and inline comments (see prose rule above).
    - `setBackgroundState` snapshot returns a JSON-serializable plain object only.
    - `onBackgroundRestore` handler IS REGISTERED (Pitfall 7: snapshot alone doesn't fire cleanup).
    - All `bridge.audioControl(false)` calls use `.catch(() => {})` (best-effort mic-close).
    - Module has no side effects at import time — all listener registration happens inside the first true call.

    Do NOT export anything except `safeAudioControl`. Do NOT add a `forceCleanup()` or `getAudioActive()` helper — out of scope.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'audio-session-guard'</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-g2-plugin/src/lib/audio-session-guard.ts` exists
    - `grep -c 'export async function safeAudioControl' vigil-g2-plugin/src/lib/audio-session-guard.ts` === 1
    - `grep -cE 'OsEventTypeList(\.ABNORMAL_EXIT_EVENT|\.SYSTEM_EXIT_EVENT)' vigil-g2-plugin/src/lib/audio-session-guard.ts` >= 2 (named-enum reference for both exit events)
    - `grep -c "OsEventTypeList.*even_hub_sdk" vigil-g2-plugin/src/lib/audio-session-guard.ts` === 1 (named-import landed)
    - `grep -c 'beforeunload' vigil-g2-plugin/src/lib/audio-session-guard.ts` === 1
    - `grep -c 'setBackgroundState.*"vigil-audio-guard"' vigil-g2-plugin/src/lib/audio-session-guard.ts` === 1
    - `grep -c 'onBackgroundRestore.*"vigil-audio-guard"' vigil-g2-plugin/src/lib/audio-session-guard.ts` === 1
    - `grep -cE 'audioControl\(false\)\.catch' vigil-g2-plugin/src/lib/audio-session-guard.ts` >= 2 (best-effort mic-close pattern)
    - No bare integer literal `6` or `7` appears anywhere in the file — including JSDoc/comments — because executors are explicitly instructed in this plan's <action> not to mention `6` or `7` in prose. The named-enum members are the only allowed reference. Manual eyeball check during execution: scan every JSDoc/comment block in audio-session-guard.ts; if you see `6` or `7` (as a digit), refactor to use the named enum.
    - `cd vigil-g2-plugin && npx tsc --noEmit -p tsconfig.json` exits 0 (or errors are unrelated to audio-session-guard.ts)
  </acceptance_criteria>
  <done>safeAudioControl wrapper landed with all four exit hooks; named-enum import locked; no bare `6`/`7` integers in code OR prose; idempotent registration; ready for Task 2 unit test and Phase 130 wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add six-case audio-session-guard unit test (four exit events + idempotency + restore)</name>
  <files>vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts</files>
  <read_first>
    - vigil-g2-plugin/src/lib/__tests__/deduped-device-status.test.ts (entire file — test-pattern analog: node:test, mocked SDK enum import, bare test() calls, `makeXxx()` synthesizer helpers, `.ts` relative import extension)
    - vigil-g2-plugin/src/lib/audio-session-guard.ts (Task 1 output — provides `safeAudioControl` for import + module-scope state behavior to assert)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts" — fakeBridge stub pattern locked; six test cases enumerated)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 7 — "four independent it-blocks" required)
  </read_first>
  <behavior>
    - Test 1 (ABNORMAL_EXIT_EVENT): after `safeAudioControl(true)`, fire fake sysEvent with eventType=ABNORMAL_EXIT_EVENT → assert `bridge.audioControl(false)` was called exactly once after the initial true call
    - Test 2 (SYSTEM_EXIT_EVENT): same, with eventType=SYSTEM_EXIT_EVENT
    - Test 3 (beforeunload): after `safeAudioControl(true)`, dispatch `window.dispatchEvent(new Event("beforeunload"))` → assert audioControl(false) was called
    - Test 4 (onBackgroundRestore): after `safeAudioControl(true)`, capture the snapshot via `bridge.snapshot()` (returns `{audioActive: true}`), then call `bridge.restore({audioActive: true})` → assert audioControl(false) was called
    - Test 5 (idempotency): `safeAudioControl(true)` called twice → onEvenHubEvent listener registered ONLY once (use `bridge.calls` count or a registration counter on the fake)
    - Test 6 (cleanup is no-op when audioActive is false): `safeAudioControl(false)` then fire ABNORMAL_EXIT_EVENT → audioControl(false) NOT called (already off)
  </behavior>
  <action>
    Create `vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts` per PATTERNS section. Imports (mirror deduped-device-status.test.ts):
    - `import { test } from 'node:test'`
    - `import assert from 'node:assert/strict'`
    - `import { OsEventTypeList } from '@evenrealities/even_hub_sdk'` (real enum — RESEARCH §A6)
    - `import { safeAudioControl } from '../audio-session-guard.ts'` (with `.ts` extension per TS-NodeNext config)

    Build a `fakeBridge()` factory function returning a stub object with the locked shape from PATTERNS:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Because `audio-session-guard.ts` imports a module-singleton `bridge`, the test must inject the fake. Options:
    1. **Dependency injection** — refactor `safeAudioControl` to accept an optional bridge argument (default the singleton). This is the cleanest test seam. Verify against the actual `bridge` import path in audio-session-guard.ts.
    2. **Module-mocking** — use node:test's `mock.module()` API (Node 20.6+) to replace the bridge import before dynamic-importing audio-session-guard.
    3. **Manual stub** — set `globalThis.window = { addEventListener: ..., dispatchEvent: ... }` before import, and find a way to stub `bridge.onEvenHubEvent`.

    Choose option 1 if Task 1 used a singleton import (most likely — match the existing main.ts convention). If the bridge is imported as a module, refactor `safeAudioControl` to take an optional second arg `bridge: typeof realBridge = realBridge` so tests can pass a fake. Update audio-session-guard.ts accordingly if needed (small refactor; document the seam in a JSDoc note).

    For each test case, instantiate a fresh fakeBridge and a polyfill for `window` if not already present:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Six test cases:
    1. `test('safeAudioControl(true) + ABNORMAL_EXIT_EVENT fires audioControl(false)', async () => { ... })` — call `safeAudioControl(true, fb)`; assert `fb.calls === [{on: true}]`; `fb.fire({sysEvent: {eventType: OsEventTypeList.ABNORMAL_EXIT_EVENT}})`; assert `fb.calls === [{on: true}, {on: false}]`.
    2. Same for `OsEventTypeList.SYSTEM_EXIT_EVENT`.
    3. `test('safeAudioControl(true) + window beforeunload fires audioControl(false)', async () => { ... })` — call `safeAudioControl(true, fb)`; `window.dispatchEvent(new Event("beforeunload"))`; await a microtask; assert audioControl(false) was called.
    4. `test('onBackgroundRestore with audioActive=true fires audioControl(false)', async () => { ... })` — call `safeAudioControl(true, fb)`; capture `fb.snapshot()` (verify returns `{audioActive: true}`); call `fb.restore({audioActive: true})`; assert audioControl(false) was called.
    5. `test('idempotent registration: safeAudioControl(true) twice registers onEvenHubEvent only once', async () => { ... })` — call safeAudioControl(true) twice; assert `fb.handlerCount === 1`.
    6. `test('cleanup is no-op when audioActive=false', async () => { ... })` — call `safeAudioControl(false, fb)`; then `fb.fire({sysEvent: {eventType: OsEventTypeList.ABNORMAL_EXIT_EVENT}})`; assert audioControl(false) was NOT called (audioActive was false at exit-event time).

    Note: Test 6's setup is subtle — calling `safeAudioControl(false)` first means `cleanupRegistered` is still false (registration only happens on first `true` call). So fire-event-then-no-cleanup is automatic. Document this in a test comment.

    Also: each test needs FRESH module state. node:test's `mock.module()` reset or a `--test-isolation=process` flag may be needed, OR refactor the module to expose a `__resetForTesting()` helper guarded by `process.env.NODE_ENV === 'test'`. Easiest: each test calls a `reset()` helper exported from audio-session-guard.ts that zeroes `audioActive` and `cleanupRegistered`. If Task 1 didn't export this, add it (named-export `__resetForTesting` with a `// @internal` JSDoc tag). Recommended over per-test module dynamic import which adds ~50ms each.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npx tsx --test src/lib/__tests__/audio-session-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts` exists
    - File contains at least six `test(` calls (one per exit path + idempotency + restore + no-op)
    - File imports real `OsEventTypeList` from `@evenrealities/even_hub_sdk` (NOT a stub enum — RESEARCH §A6)
    - `cd vigil-g2-plugin && npx tsx --test src/lib/__tests__/audio-session-guard.test.ts` exits 0
    - All six test cases pass on a fresh `audioActive=false, cleanupRegistered=false` state
    - Inverse check (manual): commenting out the `onBackgroundRestore` registration in audio-session-guard.ts MUST cause Test 4 to fail
    - Inverse check 2: changing the ABNORMAL_EXIT_EVENT branch to also fire when audioActive is false MUST cause Test 6 to fail
  </acceptance_criteria>
  <done>Six-case unit test green; four exit paths + idempotency + onBackgroundRestore replay + no-op-when-off all covered; ready for Phase 130 VOICE-02 to hook safeAudioControl in place of bare bridge.audioControl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Even SDK runtime → G2 hardware microphone | The mic is open from `audioControl(true)` until `audioControl(false)` — only the plugin can close it |
| G2 plugin process lifecycle → SDK | Crash, force-quit, headless replay, beforeunload are the four documented exit paths where state can be left dirty |
| Plugin code → server-side `/v1/voice/transcribe` | Server cannot enforce mic-close — only the byte cap (Plan 03) is the structural backstop if plugin cleanup fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-02 | Information Disclosure + Privacy violation | G2 microphone left open after plugin crash / force-quit / headless replay → audio captured without user awareness | mitigate | `safeAudioControl` registers idempotent `audioControl(false)` cleanup on all four documented Even SDK exit paths: ABNORMAL_EXIT_EVENT, SYSTEM_EXIT_EVENT, window.beforeunload, onBackgroundRestore (after setBackgroundState snapshot captured audioActive=true). Six-case unit test verifies wiring; physical hardware verification deferred to Phase 128a spike acceptance criterion (ROADMAP 388 — "no zombie microphone sessions after 5 force-quit cycles") |
| T-127-02-C | Tampering (silent SDK renumber) | A future @evenrealities/even_hub_sdk version renumbers the OsEventTypeList enum integers — hard-coded integer literals would silently break | mitigate | Named-enum import `import { OsEventTypeList } from '@evenrealities/even_hub_sdk'`; `<acceptance_criteria>` prohibits bare integer literals matching the current enum values in code AND prose (JSDoc / inline comments) so future greps don't trip on documentation noise |
| T-127-02-D | Denial of Service (memory leak via listener re-registration) | Repeated `safeAudioControl(true)` calls would re-register listeners each time, causing O(N) handler invocations on each exit event | mitigate | `cleanupRegistered` module-state flag; idempotency test (Test 5) asserts `onEvenHubEvent` registered exactly once across multiple `safeAudioControl(true)` calls |
| T-127-02-E | Residual hardware-only risk | The unit tests assert callback wiring on mocked SDK events; whether the SDK actually closes the mic in hardware is a Phase 128a spike concern (RESEARCH §Pitfall 8) | accept (with documented forward reference) | Phase 127 cannot verify hardware behavior. The byte cap in Plan 03 is the structural backstop for any plugin-side failure. Phase 128a VOICE-01 spike acceptance criterion 4 (ROADMAP line 388) explicitly verifies hardware cleanup |
</threat_model>

<verification>
- `cd vigil-g2-plugin && npx tsx --test src/lib/__tests__/audio-session-guard.test.ts` exits 0
- `cd vigil-g2-plugin && npx tsc --noEmit -p tsconfig.json` exits 0 (or only pre-existing errors)
- Manual eyeball: `audio-session-guard.ts` JSDoc and inline comments refer to exit events by their named enum members only (no bare `6`/`7` digits)
- Phase 130 VOICE-02 inheritance: future task in Phase 130 must import `safeAudioControl` and replace any direct `bridge.audioControl` call in the voice gesture handler
</verification>

<success_criteria>
- `vigil-g2-plugin/src/lib/audio-session-guard.ts` exports `safeAudioControl(on: boolean): Promise<void>`
- Named-enum import for OsEventTypeList; no integer literals 6/7 in source OR in prose (JSDoc / inline comments)
- All four exit paths registered exactly once on first `safeAudioControl(true)` call (idempotency lock)
- `setBackgroundState` AND `onBackgroundRestore` both registered (Pitfall 7 lock)
- Six-case unit test green: ABNORMAL_EXIT / SYSTEM_EXIT / beforeunload / onBackgroundRestore / idempotency / no-op-when-off
- Module has zero callers (Phase 127 ships the helper; Phase 130 wires it)
</success_criteria>

<output>
After completion, create `.planning/phases/127-pre-spike-guardrails/127-04-SUMMARY.md` capturing:
- Files modified (2 — audio-session-guard.ts + __tests__/audio-session-guard.test.ts both NEW)
- Four exit paths covered: ABNORMAL_EXIT_EVENT, SYSTEM_EXIT_EVENT, window.beforeunload, onBackgroundRestore (referenced by named-enum member; bare integer literals prohibited in code and prose)
- Named-enum import locks against future SDK renumber risk (RESEARCH §A6)
- Hardware verification is Phase 128a spike concern (RESEARCH §Pitfall 8 forward reference)
- Phase 130 wiring note: VOICE-02 push-to-record gesture handler MUST call `safeAudioControl(true)` instead of `bridge.audioControl(true)`
</output>
