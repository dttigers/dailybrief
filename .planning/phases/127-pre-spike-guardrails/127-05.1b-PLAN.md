---
phase: 127
plan: 05.1b
type: execute
wave: 3
depends_on: [05.1a]
files_modified:
  - vigil-core/src/routes/chat.ts
  - vigil-core/src/index.ts
  - vigil-core/src/__tests__/app-on-error.test.ts
  - vigil-core/src/integration/cross-user-isolation.test.ts
autonomous: true
requirements: [GUARD-03]
threat_refs: [T-127-03]
must_haves:
  truths:
    - "POST /v1/chat handler calls await requireAiBudget(userId) after c.get('userId') and before any AI work"
    - "app.onError handler branches on err instanceof DailyBudgetExceededError BEFORE captureException/captureToSentry — returns 429 with code DAILY_AI_BUDGET_EXCEEDED (Pitfall 5)"
    - "app-on-error.test.ts asserts the budget-error branch: 429 + locked code AND zero calls to captureException + captureToSentry"
    - "cross-user-isolation.test.ts asserts ai_usage_daily rows of userA are never returned to userB"
  artifacts:
    - path: "vigil-core/src/routes/chat.ts"
      provides: "requireAiBudget pre-flight gate after c.get('userId')"
      contains: 'await requireAiBudget(userId)'
    - path: "vigil-core/src/index.ts"
      provides: "app.onError DailyBudgetExceededError 429 branch BEFORE Sentry/PostHog sinks (Pitfall 5)"
      contains: 'instanceof DailyBudgetExceededError'
    - path: "vigil-core/src/__tests__/app-on-error.test.ts"
      provides: "Pitfall 5 lock: DailyBudgetExceededError returns 429 + locked code AND does NOT call Sentry/PostHog sinks"
      min_lines: 40
    - path: "vigil-core/src/integration/cross-user-isolation.test.ts"
      provides: "Extended W-01 lock block for ai_usage_daily isolation"
      contains: 'ai_usage_daily isolation'
  key_links:
    - from: "vigil-core/src/routes/chat.ts handler entry"
      to: "vigil-core/src/lib/ai-budget.ts requireAiBudget"
      via: "await requireAiBudget(userId) after c.get('userId')"
      pattern: 'await requireAiBudget\(userId\)'
    - from: "vigil-core/src/index.ts app.onError handler"
      to: "DailyBudgetExceededError → 429 response"
      via: "if (err instanceof DailyBudgetExceededError) return c.json(...) BEFORE Sentry sink"
      pattern: 'instanceof DailyBudgetExceededError'
---

<objective>
Close the GUARD-03 wiring loop. Plan 05.1a wrapped every AI client call site in `withBudgetTracking` and gated `/v1/process-audio` with `requireAiBudget`. Plan 05.1b lands the remaining three glue pieces: `/v1/chat` handler gating, `app.onError` 429 branch (Pitfall 5 closure — DailyBudgetExceededError must NOT burn Sentry quota), and the integration-side `cross-user-isolation.test.ts` extension covering `ai_usage_daily`.

Purpose: Plan 05 shipped the library. Plan 05.1a closed Pitfall 4 (every AI call wrapped). Plan 05.1b closes Pitfall 5 (the 429 branch in `app.onError` must precede the Sentry/PostHog sinks — deliberate 429s are not errors and must not burn the 5k/mo Sentry quota). It also extends the W-01 cross-user-isolation lock to the new `ai_usage_daily` table.

**Critical re-scopes vs CONTEXT.md** (per RESEARCH.md):
- `app.onError` branch ordering: `if (err instanceof DailyBudgetExceededError) return 429` BEFORE captureException + captureToSentry (Pitfall 5 — 429s are deliberate; do not burn Sentry quota).
- Cross-user-isolation test lives at `vigil-core/src/integration/cross-user-isolation.test.ts` (NOT `src/__tests__/` — CONTEXT D-03.7 path was stale per PATTERNS correction).

Output: chat.ts gate landed; app.onError 429 branch landed; app-on-error.test.ts spying assert no-Sentry-sink; cross-user-isolation.test.ts gains an `ai_usage_daily isolation` block + W-01 source-grep assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@.planning/phases/127-pre-spike-guardrails/127-RESEARCH.md
@.planning/phases/127-pre-spike-guardrails/127-PATTERNS.md
@.planning/phases/127-pre-spike-guardrails/127-05-PLAN.md (Wave 1 predecessor — exports requireAiBudget / withBudgetTracking / DailyBudgetExceededError)
@.planning/phases/127-pre-spike-guardrails/127-05.1a-PLAN.md (Wave 2 predecessor — wrapped AI client; widened callClaude* to require userId; closed Pitfall 4 at process-audio.ts)

<interfaces>
From Plan 05's vigil-core/src/lib/ai-budget.ts:
```typescript
export async function requireAiBudget(userId: number): Promise<void>;
export class DailyBudgetExceededError extends Error {
  readonly code: "DAILY_AI_BUDGET_EXCEEDED";
  constructor(userId: number, usdEstimate: number);
}
```

Current chat.ts entry pattern — line ~19:
```typescript
chat.post("/chat", async (c) => {
  const userId = c.get("userId");
  // ... getAIClient() 503 check; AI work follows
});
```

Current app.onError handler — vigil-core/src/index.ts:265-280 (verbatim):
```typescript
app.onError((err, c) => {
  console.error("[vigil-core] unhandled error:", err);
  const userId = (c.get("userId") as number | undefined) ?? null;
  captureException(userId, err, { route: c.req.path, method: c.req.method });
  captureToSentry(userId, err, { route: c.req.path, method: c.req.method });
  return c.json({ error: "Internal server error" }, 500);
});
```

Mocked-side-effect pattern — vigil-core/src/lib/sentry.test.ts:75-102 (the precedent for asserting Sentry/PostHog spy call counts).

cross-user-isolation.test.ts existing pattern — `vigil-core/src/integration/cross-user-isolation.test.ts` (DB_READY skip pattern + userA/userB seeding + existing work-orders isolation it-block around lines 421-455).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire requireAiBudget into chat.ts + add app.onError DailyBudgetExceededError 429 branch (Pitfall 5 closure)</name>
  <files>vigil-core/src/routes/chat.ts, vigil-core/src/index.ts</files>
  <read_first>
    - vigil-core/src/routes/chat.ts (lines 15-30 — handler entry, get userId, getAIClient 503 check; verify the wiring point for requireAiBudget)
    - vigil-core/src/index.ts (lines 263-281 — current app.onError body; verify captureException + captureToSentry call sites)
    - vigil-core/src/lib/ai-budget.ts (Plan 05 output — provides DailyBudgetExceededError class to import)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/src/routes/chat.ts" + "vigil-core/src/index.ts" — locked invariants for branch ordering + import)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 5 — branch order matters; A7 — Hono's app.onError is a single function, early return short-circuits naturally)
  </read_first>
  <behavior>
    - chat.ts handler calls `await requireAiBudget(userId)` AFTER `c.get("userId")` and AFTER the existing `getAIClient()` 503 check, BEFORE any AI invocation
    - chat.ts callers of `callClaude*` wrappers (from Plan 05.1a) pass userId; verify each existing call site in chat.ts
    - `app.onError` branches: `if (err instanceof DailyBudgetExceededError) return c.json({error, code}, 429);` — placed BEFORE the console.error / captureException / captureToSentry calls
    - Throwing `DailyBudgetExceededError` from a route → app.onError returns 429 with body `{error: "Daily AI budget exceeded", code: "DAILY_AI_BUDGET_EXCEEDED"}`
    - When DailyBudgetExceededError is thrown, captureException AND captureToSentry are NOT called (verified via spies/mocks in Task 2's app-on-error.test.ts)
    - When any other Error is thrown, captureException AND captureToSentry are still called (existing behavior preserved)
  </behavior>
  <action>
    Edit `vigil-core/src/routes/chat.ts` per D-03.4 + PATTERNS locked invariants. Find the existing `chat.post("/chat", async (c) => { ... })` handler. Inside the handler body, after the existing `getAIClient()` 503 check and `const userId = c.get("userId");`, add ONE new line: `await requireAiBudget(userId);`. Position it BEFORE any AI work but AFTER both prerequisite checks. Add the import at the top of the file: `import { requireAiBudget } from "../lib/ai-budget.js";`. If Plan 05.1a's callClaude* widening means existing chat.ts call sites need a userId option, verify those passed too (they should be widened automatically by Plan 05.1a's Task 3; double-check the chat.ts call sites and add userId if any were missed).

    Locked invariants:
    - `requireAiBudget(userId)` is called AFTER `c.get("userId")` (needs the value) and AFTER `getAIClient()` 503 check (no point checking budget if AI client is missing).
    - Throw → `app.onError` → 429 (the handler does NOT catch DailyBudgetExceededError locally; PATTERNS Anti-pattern: "Catching DailyBudgetExceededError inside the route handler...").

    Edit `vigil-core/src/index.ts` per D-03.4 + RESEARCH §Pitfall 5. Find the existing `app.onError((err, c) => { ... })` block at lines 265-280. Replace the body to add the early branch (preserving the existing behavior for non-budget errors):

    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action — add an `if (err instanceof DailyBudgetExceededError) { return c.json({error: "Daily AI budget exceeded", code: "DAILY_AI_BUDGET_EXCEEDED"}, 429); }` block BEFORE the existing console.error + captureException + captureToSentry + 500 fallback. The order matters: the new branch returns FIRST.]

    Add the import at the top of index.ts: `import { DailyBudgetExceededError } from "./lib/ai-budget.js";`.

    Locked invariants (Pitfall 5):
    - Branch order: DailyBudgetExceededError check FIRST, then unconditional sink for other errors.
    - HTTP 429 (standard rate-limit status).
    - Response body `{error, code}` shape — PWA's `resolveApiError` reads `body.code`.
    - Other errors UNCHANGED — existing console.error + captureException + captureToSentry + 500 fallback preserved.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'chat\.ts|index\.ts' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'await requireAiBudget(userId)' vigil-core/src/routes/chat.ts` === 1
    - `grep -c 'from.*"\.\./lib/ai-budget' vigil-core/src/routes/chat.ts` === 1
    - `grep -c 'instanceof DailyBudgetExceededError' vigil-core/src/index.ts` === 1
    - `grep -c 'DAILY_AI_BUDGET_EXCEEDED' vigil-core/src/index.ts` === 1 (the literal locked-enum code in the response body)
    - `grep -c 'from.*"\./lib/ai-budget' vigil-core/src/index.ts` === 1
    - The `app.onError` body in index.ts has the DailyBudgetExceededError branch BEFORE the captureException + captureToSentry calls (verify by reading the file and confirming line ordering — the `if (err instanceof DailyBudgetExceededError)` block appears between the `app.onError((err, c) => {` opening and the first `captureException` call)
    - `cd vigil-core && npx tsc --noEmit -p tsconfig.json` exits 0 across chat.ts + index.ts (or any remaining errors are unrelated to GUARD-03)
    - Existing mount-order.test.ts (Phase 126) still passes (no regression on the bearerAuth precede-Hono lock)
    - PWA `resolveApiError({code: "DAILY_AI_BUDGET_EXCEEDED"})` lookup will work once Plan 06 lands the EXTENSION key (cross-plan dependency)
  </acceptance_criteria>
  <done>chat.ts gated; app.onError branches on DailyBudgetExceededError BEFORE Sentry sink (Pitfall 5 closed at source); Task 2 lands the spy-based unit test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create app-on-error.test.ts — asserts 429 + locked code AND no Sentry/PostHog sink for DailyBudgetExceededError (Pitfall 5 unit pin)</name>
  <files>vigil-core/src/__tests__/app-on-error.test.ts</files>
  <read_first>
    - vigil-core/src/index.ts (Task 1 output — the app.onError handler this test asserts against)
    - vigil-core/src/lib/sentry.test.ts (lines 75-102 — mocked-side-effect pattern for asserting Sentry/PostHog calls did NOT fire)
    - vigil-core/src/lib/ai-budget.ts (Plan 05 output — provides DailyBudgetExceededError class to import)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/src/__tests__/app-on-error.test.ts" — test cases enumerated)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 5 — branch order matters; Validation row 16)
  </read_first>
  <behavior>
    - Test 1: route handler throws `new DailyBudgetExceededError(42, 0.51)` → response is HTTP 429 with body `{error: "Daily AI budget exceeded", code: "DAILY_AI_BUDGET_EXCEEDED"}`; captureException + captureToSentry spies have ZERO calls
    - Test 2: route handler throws `new Error("kaboom")` → response is HTTP 500 with body `{error: "Internal server error"}`; captureException + captureToSentry spies have ONE call each (existing behavior preserved)
    - Tests run against a fresh Hono instance with the same onError handler body (or import the real app — whichever is cleaner)
  </behavior>
  <action>
    Create `vigil-core/src/__tests__/app-on-error.test.ts` (NEW file per RESEARCH Wave 0 gaps). Use node:test + node:assert/strict. Mock `captureException` (from `../analytics/posthog.js`) and `captureToSentry` (from `../lib/sentry.js`) using `import { mock } from "node:test"` (Node ≥ 20.6).

    Test setup pattern (mirror `vigil-core/src/lib/sentry.test.ts:75-102`):
    - Import the real `DailyBudgetExceededError` from `../lib/ai-budget.js`.
    - Mock the two side-effect functions OR build a minimal Hono app with the same `app.onError` body inline for isolation (recommended — avoids the mock.module dance against the production app instance).
    - If importing the real app is impractical (it boots all middleware), replicate the handler in the test against a fresh Hono instance:
      ```
      const testApp = new Hono();
      testApp.get("/throw-budget", () => { throw new DailyBudgetExceededError(42, 0.51); });
      testApp.get("/throw-generic", () => { throw new Error("kaboom"); });
      testApp.onError((err, c) => { /* same body as production */ });
      ```
      and inject mocked `captureException` + `captureToSentry` via the same import path.

    Test cases (per RESEARCH §Pitfall 5 + Validation row 16):

    1. **`DailyBudgetExceededError → 429 with locked code; Sentry/PostHog NOT called`:**
       - Build the test app (above pattern).
       - Mock `captureException` and `captureToSentry` to track call count via `mock.method` or `mock.fn`.
       - Fire `await testApp.request("/throw-budget")`.
       - Assert `response.status === 429`.
       - Assert response body deep-equals `{error: "Daily AI budget exceeded", code: "DAILY_AI_BUDGET_EXCEEDED"}`.
       - Assert `captureException.mock.calls.length === 0` AND `captureToSentry.mock.calls.length === 0`.

    2. **Other Error → 500 + Sentry/PostHog called (existing behavior preserved):**
       - Same setup, different route.
       - Fire `await testApp.request("/throw-generic")`.
       - Assert `response.status === 500`.
       - Assert response body matches `{error: "Internal server error"}`.
       - Assert `captureException.mock.calls.length === 1` AND `captureToSentry.mock.calls.length === 1`.

    Locked invariants:
    - Both tests assert on the EXACT response shape (status + body) AND the EXACT spy call count (0 for budget errors, 1 for generic errors). The combination is the Pitfall 5 lock — branch order can't drift undetected.
    - The test does NOT depend on a live Postgres / live Sentry / live PostHog — fully unit-level via mocks.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/__tests__/app-on-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-core/src/__tests__/app-on-error.test.ts` exists
    - `cd vigil-core && npx tsx --test src/__tests__/app-on-error.test.ts` exits 0
    - File contains at least 2 `it(` or `test(` blocks (one per case)
    - Test 1 asserts: status === 429 AND body has `code: "DAILY_AI_BUDGET_EXCEEDED"` AND mock.calls.length === 0 for both spies
    - Test 2 asserts: status === 500 AND body matches `{error: "Internal server error"}` AND mock.calls.length === 1 for both spies
    - Inverse manual check 1: temporarily move the `instanceof DailyBudgetExceededError` branch AFTER the captureException calls in index.ts → Test 1 MUST fail (the spy will record a call)
    - Inverse manual check 2: temporarily remove the entire DailyBudgetExceededError branch → Test 1 MUST fail (response will be 500, not 429)
  </acceptance_criteria>
  <done>Pitfall 5 unit lock landed; app-on-error.test.ts pins both the 429+code path AND the no-Sentry-sink behavior; future regressions caught at CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend cross-user-isolation.test.ts with ai_usage_daily W-01 lock block + source-grep assertion</name>
  <files>vigil-core/src/integration/cross-user-isolation.test.ts</files>
  <read_first>
    - vigil-core/src/integration/cross-user-isolation.test.ts (entire file — read full to understand DB_READY skip pattern + userA/userB seeding + existing work-orders isolation block at lines ~421-455 for the analog)
    - vigil-core/src/lib/ai-budget.ts (Plan 05 output — provides `eq(aiUsageDaily.userId, userId)` reference for the W-01 grep assertion)
    - vigil-core/src/db/schema.ts (Plan 05 Task 1 — provides aiUsageDaily table for the test query)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/src/integration/cross-user-isolation.test.ts (EDIT — add ai_usage_daily W-01 lock)" — locked test-block code)
    - .planning/phases/127-pre-spike-guardrails/127-CONTEXT.md (D-03.7 — note that the stale path `src/__tests__/cross-user-isolation.test.ts` is incorrect; the real file is in `src/integration/`)
  </read_first>
  <behavior>
    - cross-user-isolation.test.ts gains a new it-block `ai-usage-daily isolation — userA's row never returned for userB's query`
    - Test seeds an ai_usage_daily row for userA (usageDate = today's ISO date slice, usdEstimate = "0.45"), then queries the table filtered by userB's userId, asserts the result is empty
    - Test cleans up by deleting the seeded row in a `finally` block
    - Test is skipped (`t.skip(...)`) when DATABASE_URL is not set (existing DB_READY pattern)
    - Test imports `aiUsageDaily` from `../db/schema.js`
    - File-level source-grep assertion: `vigil-core/src/lib/ai-budget.ts` contains literal `eq(aiUsageDaily.userId, userId)` (matches the W-01 grep convention from W-01 / Phase 121 D-D2 lock)
  </behavior>
  <action>
    Edit `vigil-core/src/integration/cross-user-isolation.test.ts` per D-03.7 + PATTERNS locked test-block. Locate the existing `work-orders isolation` it-block (around lines 421-455 per PATTERNS). After it, add a new it-block:

    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action. The test seeds an `ai_usage_daily` row for userA (using Drizzle insert via aiUsageDaily table), queries with `eq(aiUsageDaily.userId, userB.id)` and asserts result is `[]`, then cleans up in a finally block via Drizzle delete. Mirror the existing work-orders isolation block's seed/query/cleanup shape.]

    Match the existing import + helper conventions in the file (the test may already have `eq` imported globally; if so, drop the dynamic import for eq).

    Add a source-grep assertion either inline in this file or in a separate test block. Use the W-01 grep style from prior phases:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim — read `vigil-core/src/lib/ai-budget.ts` via `fs.readFileSync`, assert `src.includes("eq(aiUsageDaily.userId, userId)")` with an error message naming "W-01-AI-BUDGET". Match the existing W-01 grep test pattern in the file if one exists; if not, add a fresh it-block before or after the seed test.]

    Use the existing fs/path imports from the integration test file's header.

    Do NOT remove or modify any existing test in the file (Phase 121 D-D2 + Phase 124 D-D2 locks). The new block is purely additive.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/integration/cross-user-isolation.test.ts 2>&1 | grep -E 'ai-usage-daily isolation|W-01-AI-BUDGET|pass|fail'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ai-usage-daily isolation' vigil-core/src/integration/cross-user-isolation.test.ts` === 1
    - `grep -c 'W-01-AI-BUDGET' vigil-core/src/integration/cross-user-isolation.test.ts` === 1
    - `cd vigil-core && DATABASE_URL=$DATABASE_URL npx tsx --test src/integration/cross-user-isolation.test.ts` exits 0 (against a live local Postgres with 0020 applied per Plan 05 Task 2 checkpoint)
    - With DATABASE_URL absent, the test SKIPS the integration assertion cleanly (no fail) but the W-01-AI-BUDGET source-grep still runs and passes
    - Existing tests in the file still pass (Phase 121 D-D2 + Phase 124 D-D2 locks preserved)
    - Inverse manual check: temporarily comment out `eq(aiUsageDaily.userId, userId)` in ai-budget.ts → W-01-AI-BUDGET assertion fails
  </acceptance_criteria>
  <done>cross-user-isolation.test.ts extended with ai_usage_daily W-01 lock; source-grep assertion enforces the eq() filter in ai-budget.ts; T-127-03-B mitigation verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.onError funnel | Catches all unhandled errors from route handlers; pre-Sentry-sink branch on DailyBudgetExceededError prevents budget-quota burn (Pitfall 5) |
| Database query layer (ai_usage_daily reads) | Every query filters `eq(aiUsageDaily.userId, userId)` — W-01 cross-user-isolation lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | DoS (AI cost) — handler-gate half | chat.ts handler must call requireAiBudget BEFORE any AI work | mitigate | `await requireAiBudget(userId)` placed after `c.get("userId")` + getAIClient() 503 check, before any AI invocation. Plan 05.1a already gated process-audio.ts the same way; this plan completes the chat.ts side |
| T-127-03-F (Pitfall 5 closure) | DoS of monitoring budget | Deliberate 429s flooding Sentry quota (5k events/mo) | mitigate | app.onError branch on `instanceof DailyBudgetExceededError` returns 429 BEFORE captureException + captureToSentry; app-on-error.test.ts asserts both spies registered zero calls on budget-error path; existing behavior preserved for other Error types (test 2 verifies the regression baseline) |
| T-127-03-B (W-01 closure) | Elevation of Privilege (cross-user read) | A future query against ai_usage_daily omits the userId filter | mitigate | cross-user-isolation.test.ts integration test verifies userA's row is not returned for userB's query; source-grep W-01-AI-BUDGET assertion in the same file enforces `eq(aiUsageDaily.userId, userId)` is literally present in ai-budget.ts |
</threat_model>

<verification>
- `cd vigil-core && npx tsx --test src/__tests__/app-on-error.test.ts src/integration/cross-user-isolation.test.ts` exits 0 (assuming DATABASE_URL set for the integration test; SKIPs otherwise)
- `cd vigil-core && npx tsc --noEmit -p tsconfig.json` exits 0
- Inverse smoke: set `VIGIL_DAILY_AI_BUDGET_USD=0.001`, run two `/v1/chat` requests against local server — second returns 429 with body `{error, code: "DAILY_AI_BUDGET_EXCEEDED"}`
- No Sentry events fired for the 429 (verify Sentry dashboard in dev or assert via the unit test spy)
- Existing mount-order.test.ts + W-01 Phase 121 D-D2 locks all still green
</verification>

<success_criteria>
- chat.ts handler calls requireAiBudget after c.get("userId") + getAIClient 503 check
- app.onError returns 429 + locked code for DailyBudgetExceededError; does NOT call captureException/captureToSentry (Pitfall 5 closed)
- app-on-error.test.ts asserts the 429 + no-sink behavior with mocked spies (2 test cases — budget-error path AND generic-error path)
- cross-user-isolation.test.ts extended with ai_usage_daily W-01 lock block + source-grep enforcement
- TypeScript compile passes; all touched test files green
- End-to-end GUARD-03 wiring complete: Plan 05 storage + library, Plan 05.1a AI client wrap + process-audio gate, Plan 05.1b chat gate + onError branch + integration test
</success_criteria>

<output>
After completion, create `.planning/phases/127-pre-spike-guardrails/127-05.1b-SUMMARY.md` capturing:
- Files modified (4 — chat.ts EDIT, index.ts EDIT, app-on-error.test.ts NEW, cross-user-isolation.test.ts EDIT)
- Pitfall 5 closed: app.onError branch on DailyBudgetExceededError BEFORE Sentry/PostHog sink; spy-based unit test pins both paths
- W-01 lock extended: ai_usage_daily isolation test + source-grep assertion in cross-user-isolation.test.ts
- End-to-end GUARD-03 wiring complete: Plan 05 (library) → Plan 05.1a (AI client wrap + process-audio gate) → Plan 05.1b (chat gate + onError branch + integration test) → Plan 06 (PWA copy)
- Note for Plan 06: requires DAILY_AI_BUDGET_EXCEEDED in ERROR_CODE_MAP so PWA can render the friendly copy (cross-plan dependency closes the user-visible loop; Plan 06 already in Wave 2 alongside Plan 05.1a)
- Open follow-up: Phase 131 INSIGHTS-FRESH-01 / regenerateAiCache helper will inherit requireAiBudget at the consolidation site
</output>
