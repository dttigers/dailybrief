---
phase: 127
plan: 07
type: execute
wave: 2
depends_on: [05]
files_modified:
  - vigil-core/src/__tests__/migration-drift.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [GUARD-04]
threat_refs: [T-127-04]
must_haves:
  truths:
    - "vigil-core/src/__tests__/migration-drift.test.ts shells `drizzle-kit generate` and asserts stdout matches /No schema changes/i — fails CI if a future commit drifts schema.ts away from drizzle/"
    - "STATE.md no longer claims the Phase 107.1 work_orders drift is 'carried' — the drift was closed by 0013_work_orders_drift_repair.sql in 2026-04-22 (per RESEARCH §Pitfall 2)"
    - "Test uses a fake DATABASE_URL fallback so it runs in CI without a reachable Postgres (verified live in RESEARCH §A4)"
    - "The migration filename `0020_reconcile_work_orders_107_1.sql` is NEVER created by Phase 127 (RESEARCH §Pitfall 2 — that drift is already migrated; Plan 05 takes the 0020 slot for ai_usage_daily)"
  artifacts:
    - path: "vigil-core/src/__tests__/migration-drift.test.ts"
      provides: "Shell-out drift detector — fails CI on schema-vs-migration divergence"
      min_lines: 30
    - path: ".planning/STATE.md"
      provides: "Stale Phase 107.1 work_orders carry-over blocker cleaned up"
  key_links:
    - from: "CI test invocation"
      to: "drizzle-kit generate subcommand"
      via: "node:child_process.execSync with cwd=vigil-core root"
      pattern: 'execSync\(.*drizzle-kit generate'
---

<objective>
Ship the GUARD-04 drift detector + clean up the stale Phase 107.1 doc entry. This plan is the re-scoped GUARD-04 per RESEARCH.md §Pitfall 1 + §Pitfall 2:

1. **No new migration written** — the Phase 107.1 `work_orders` drift was already closed by `vigil-core/drizzle/0013_work_orders_drift_repair.sql` in 2026-04-22 (verified live: `drizzle-kit generate` against current HEAD prints `No schema changes, nothing to migrate 😴`). Writing `0020_reconcile_work_orders_107_1.sql` would no-op against current schema OR pollute the migration journal.
2. **No `--dry` flag used** — `drizzle-kit generate --dry` is NOT a supported flag in `drizzle-kit@^0.31.10` (RESEARCH §Pitfall 1; verified live; the flag is an open feature request, GH issue drizzle-team/drizzle-orm#5059).
3. **Drift detector mechanism** — shell `drizzle-kit generate` (no flag) and parse stdout for `/No schema changes/i` (regex form is emoji-tolerant per RESEARCH §A2).
4. **STATE.md cleanup** — delete or update line 389 which still says "Phase 107.1 work_orders schema drift — columns notes/archived_at/last_change_at/last_change_summary defined in schema.ts but never migrated".

**Wave 2 sequencing vs Plan 05 (depends_on: [05]):** Plan 05 ships `0020_add_ai_usage_daily.sql` + the `aiUsageDaily` schema.ts export. Plan 07's drift detector test asserts `drizzle-kit generate` shows no pending changes — which REQUIRES Plan 05's migration + schema.ts to be IN SYNC and committed before Plan 07's test can run cleanly. The test file itself has zero file dependency on Plan 05 (it just shells the CLI), but the test's PASS state depends on Plan 05's work having landed. Encoding `depends_on: [05]` + Wave 2 placement eliminates the false-fail window where Plan 05's commit lands but Plan 07's runs in CI before the schema/migration are in sync (or vice versa). Wave 2 placement is unambiguous: Plan 07 runs after Plan 05 completes.

Output: A 30-line drift-detector test + a 1-line STATE.md cleanup, run in Wave 2 after Plan 05 lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@.planning/phases/127-pre-spike-guardrails/127-RESEARCH.md
@.planning/phases/127-pre-spike-guardrails/127-PATTERNS.md
@.planning/phases/127-pre-spike-guardrails/127-05-PLAN.md (predecessor — ships 0020_add_ai_usage_daily.sql + aiUsageDaily schema.ts export; this plan's test PASSES because Plan 05's schema-vs-migration parity holds)

<interfaces>
Outer-shell analog — vigil-core/src/__tests__/mount-order.test.ts (the only existing drift-detector test in the project):
```typescript
import { describe, it, before } from "node:test";
import assert from "node:assert/strict";

let indexSrc: string = "";

before(async () => {
  const fs = await import("node:fs");
  const path = await import("node:path");
  const url = await import("node:url");
  const here = path.dirname(url.fileURLToPath(import.meta.url));
  indexSrc = fs.readFileSync(path.join(here, "..", "index.ts"), "utf8");
});
```

Verified-live empirical facts (RESEARCH §Pitfall 1 + §Pitfall 10 + §A4):
- `npx drizzle-kit generate` (no flag) succeeds and prints `No schema changes, nothing to migrate 😴` when schema.ts is in sync with drizzle/
- `DATABASE_URL=postgres://noop@localhost/noop` is sufficient for `generate` (does NOT require a reachable Postgres — the subcommand reads only schema.ts + drizzle/meta/*.json)
- Command takes 1-3s to boot Node + drizzle-kit; a `{ timeout: 10_000 }` per-test option is safe headroom
- The 😴 emoji is part of the verified sentinel; `/No schema changes/i` regex tolerates a potential future emoji removal (RESEARCH §A2)

Stale STATE.md entry — line 389 (verified by grep):
```
- Phase 107.1 work_orders schema drift — columns notes/archived_at/last_change_at/last_change_summary defined in schema.ts but never migrated
```

This line is incorrect per RESEARCH §Pitfall 2 verification trail: drift was closed by `vigil-core/drizzle/0013_work_orders_drift_repair.sql:6-12` (file header verbatim: "Phase 107.1 gap repair: add schema.ts columns missing from prior migrations").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create migration-drift.test.ts (shells drizzle-kit generate, asserts no schema changes)</name>
  <files>vigil-core/src/__tests__/migration-drift.test.ts</files>
  <read_first>
    - vigil-core/src/__tests__/mount-order.test.ts (entire file — outer drift-detector shell to mirror: describe/it/before with fs.readFileSync setup; here we shell-out instead of reading source, but the layout is identical)
    - vigil-core/drizzle.config.ts (verify drizzle-kit reads config from this file — needed to confirm `cwd: VIGIL_CORE_ROOT` is correct for the shell-out)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 1 — full rationale for why --dry is fictional; §Pitfall 10 — DATABASE_URL fallback + timeout; §A2 — emoji-tolerant regex)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-core/src/__tests__/migration-drift.test.ts" — locked implementation skeleton)
    - vigil-core/package.json (confirm `drizzle-kit` is a devDependency at version ^0.31.10; the test invokes `npx drizzle-kit` so the version is whatever is locked in the lockfile)
  </read_first>
  <behavior>
    - Test invokes `npx drizzle-kit generate` (NO `--dry` flag — Pitfall 1 lock) from `vigil-core/` root
    - DATABASE_URL fallback `postgres://noop@localhost/noop` is set in the env if the real DATABAS URL is absent (RESEARCH §A4 — generate works against a fake URL)
    - stdout is captured and asserted to match `/No schema changes/i` (regex form per RESEARCH §A2 — emoji-tolerant + version-resilient)
    - Test has `{ timeout: 10_000 }` per-test option (RESEARCH §Pitfall 10 — drizzle-kit boots in 1-3s)
    - Error message on failure names the offending stdout text + suggests "Hand-craft a new 0021+ migration before proceeding" so the developer knows what to fix
    - When schema.ts diverges from drizzle/ migrations, the test FAILS (e.g., temporarily add `newColumn: text("new_column")` to any table without writing the SQL — the test detects the drift)
  </behavior>
  <action>
    Create `vigil-core/src/__tests__/migration-drift.test.ts` per RESEARCH §Pitfall 1 + §Pitfall 10 + PATTERNS locked implementation.

    Imports:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Compute the working directory:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]
    (`here` is `vigil-core/src/__tests__/`; `..` → `src/`; `..` again → `vigil-core/`. Verify against actual file layout when running locally.)

    Single test block:
    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `env`. Match the analog body verbatim except for the specific edits called out in this action.]

    Locked invariants (do NOT change):
    - NO `--dry` flag (RESEARCH §Pitfall 1 — the flag does not exist).
    - DATABASE_URL fallback `postgres://noop@localhost/noop` (RESEARCH §A4 — fake URL is sufficient for `generate`).
    - `{ timeout: 10_000 }` per-test option (RESEARCH §Pitfall 10).
    - `cwd: VIGIL_CORE_ROOT` — drizzle-kit must find `drizzle.config.ts`.
    - Regex `/No schema changes/i` (NOT literal-string compare to the emoji sentinel — RESEARCH §A2).
    - Error message includes the full captured stdout (debuggable on CI without re-running locally).

    Do NOT use `drizzle-kit check` instead — RESEARCH §"Open Q5" left this open; the planner authored Plan 07 to use `generate` for compatibility with the verified-live empirical command. A future task may evaluate `check` once its semantics for schema-vs-migration drift (rather than journal consistency) are confirmed.

    Wave 2 / depends_on [05] sequencing: Plan 05's `0020_add_ai_usage_daily.sql` + `meta/0020_snapshot.json` + `aiUsageDaily` schema.ts export must already be present in the working tree (Wave 1 completed before Wave 2 starts). Plan 07's test PASSES because Plan 05's schema-vs-migration parity holds — this dependency is now explicit in frontmatter and wave ordering.
  </action>
  <verify>
    <automated>cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx tsx --test src/__tests__/migration-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-core/src/__tests__/migration-drift.test.ts` exists
    - `grep -c 'execSync' vigil-core/src/__tests__/migration-drift.test.ts` === 1
    - `grep -c 'drizzle-kit generate' vigil-core/src/__tests__/migration-drift.test.ts` >= 1 (in the execSync command string)
    - `grep -c '\-\-dry' vigil-core/src/__tests__/migration-drift.test.ts` === 0 (RESEARCH §Pitfall 1 — the flag must NEVER appear)
    - `grep -c 'No schema changes' vigil-core/src/__tests__/migration-drift.test.ts` === 1 (regex pattern)
    - `grep -c 'postgres://noop@localhost' vigil-core/src/__tests__/migration-drift.test.ts` === 1 (RESEARCH §A4 fake URL fallback)
    - `grep -c 'timeout: 10_000' vigil-core/src/__tests__/migration-drift.test.ts` === 1 (RESEARCH §Pitfall 10)
    - `cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx tsx --test src/__tests__/migration-drift.test.ts` exits 0 — assumes Plan 05's migration + schema.ts changes have ALSO landed (Wave 2 ordering enforces this)
    - Inverse manual check: temporarily add `someNewColumn: text("xxx")` to any table in schema.ts → test MUST fail with the stdout-captured error message naming the offending column
    - Inverse manual check 2: temporarily change the assert regex to `/Some impossible string/i` → test MUST fail (sanity check)
    - File does NOT create `vigil-core/drizzle/0020_reconcile_work_orders_107_1.sql` (this plan ships ONLY the test; no migration file)
  </acceptance_criteria>
  <done>Drift-detector test landed; shells drizzle-kit generate without --dry flag; uses fake DATABASE_URL fallback; regex-matches No schema changes sentinel; ready to catch future schema/migration divergence in CI.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md to remove the stale Phase 107.1 work_orders drift carry-over entry</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (line 389 specifically — verify the stale entry text; read surrounding 5-10 lines for the section header context before editing)
    - vigil-core/drizzle/0013_work_orders_drift_repair.sql (line 1-12 — proves the drift was closed; capture the 2026-04-22 date + file header verbatim for the replacement entry)
    - .planning/phases/127-pre-spike-guardrails/127-RESEARCH.md (§Pitfall 2 — full verification trail: 0013 snapshot + 0019 snapshot both have the four columns; drizzle-kit generate confirms parity)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section ".planning/STATE.md (EDIT — delete stale Phase 107.1 blocker line)" — recommended replacement text)
  </read_first>
  <behavior>
    - The stale `- Phase 107.1 work_orders schema drift — columns notes/archived_at/last_change_at/last_change_summary defined in schema.ts but never migrated` line at STATE.md:389 is either deleted OR replaced with a brief closing note citing 0013_work_orders_drift_repair.sql + the 2026-04-22 close date + RESEARCH §Pitfall 2 rediscovery reference
    - The "Carried into v3.8" (or similar section header above line 389) section's other entries (if any) remain unchanged
    - No new STATE.md sections are introduced
    - The fact that the drift is closed becomes verifiable from STATE.md alone (no need to dive into 127-RESEARCH.md to confirm)
  </behavior>
  <action>
    Edit `.planning/STATE.md` per RESEARCH §Pitfall 2 + PATTERNS section ".planning/STATE.md (EDIT — delete stale Phase 107.1 blocker line)".

    Locate the line at approximately line 389 reading:
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Replace it with a single closing-note line (PATTERNS recommended replacement):
    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Alternative: if the entire "carried blocker" section becomes empty after the deletion (i.e., the Phase 107.1 line was the only entry), delete the section header too. Read 10 lines of surrounding context before editing to avoid orphan section headers.

    Do NOT touch other STATE.md sections (current/active decisions, recent commits log, etc.). This is a single-line surgical edit closing a stale-doc loop, NOT a STATE.md refactor.
  </action>
  <verify>
    <automated>! grep -E 'Phase 107\.1.*never migrated' .planning/STATE.md && grep -c 'resolved by.*0013_work_orders_drift_repair' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The literal stale phrase `Phase 107.1 work_orders schema drift — columns notes/archived_at/last_change_at/last_change_summary defined in schema.ts but never migrated` is GONE from `.planning/STATE.md` — verify with `! grep 'never migrated' .planning/STATE.md` returns exit 0 (no match)
    - The replacement closing note IS present — verify `grep -c '0013_work_orders_drift_repair' .planning/STATE.md` === 1
    - No other STATE.md line was modified — diff the file before/after, only one line should change
    - The "carried blocker" section header (if it exists) either remains with the new replacement line OR is removed cleanly if the Phase 107.1 entry was the only carried blocker
    - This is the GUARD-04 "stale docs" half of the re-scoped success criterion per RESEARCH §Pitfall 2 — STATE.md no longer falsely claims unfinished drift work
  </acceptance_criteria>
  <done>STATE.md cleaned up; stale Phase 107.1 carry-over blocker entry replaced with a closing note citing 0013_work_orders_drift_repair.sql + 127-RESEARCH.md §Pitfall 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer schema.ts edit → drizzle/ migration files | Schema-only edit without a matching migration is the silent-drift failure mode (T-127-04) |
| CI test invocation → drizzle-kit CLI (child process) | The shell-out is the verification mechanism; CI environment must have drizzle-kit reachable via `npx` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-04 | Tampering | A future commit edits `vigil-core/src/db/schema.ts` (adds a column, changes a type, drops a table) WITHOUT writing the corresponding migration; production deploys diverge silently from schema.ts; subsequent migrations attempt to alter columns that exist in schema but were never created in DB — destructive data loss risk | mitigate | migration-drift.test.ts shells `drizzle-kit generate` (no `--dry` flag — RESEARCH §Pitfall 1), captures stdout, asserts `/No schema changes/i` regex match; CI fails on any divergence with a descriptive error pointing the developer at the missing-migration fix path |
| T-127-04-A | Tampering (silent — version skew) | Future drizzle-kit minor version changes the "no changes" sentinel string (e.g., drops the emoji, renames to "Schema in sync") — test silently passes when there IS drift | accept (with mitigation note) | Regex `/No schema changes/i` (not literal `No schema changes, nothing to migrate 😴`) tolerates emoji removal; if drizzle-kit changes the wording entirely, the test must be updated. drizzle-kit pinned at `^0.31.10` in package.json — semver minor bumps are infrequent, and a CI false-pass during a sentinel rewrite is recoverable (manual `drizzle-kit generate` reveals drift; STATE.md rediscovery loop catches it) |
| T-127-04-B | Tampering (test bypass) | A developer deletes or skips migration-drift.test.ts during a "fast merge" | accept | Sentinel covers our v3.9 process. v3.10+ may add a CI required-check to prevent test deletion. Out of scope for Phase 127 |
| T-127-04-C | Tampering (stale documentation) | STATE.md continues to claim Phase 107.1 drift is unmigrated → future planner copies the stale framing into a new phase (exactly what happened in 127-CONTEXT.md per RESEARCH §Pitfall 2) | mitigate | Task 2 updates STATE.md:389 with the correct closing note citing 0013_work_orders_drift_repair.sql; future planner reading STATE.md gets the right answer |
</threat_model>

<verification>
- `cd vigil-core && DATABASE_URL=postgres://noop@localhost/noop npx tsx --test src/__tests__/migration-drift.test.ts` exits 0 (Wave 2 ordering ensures Plan 05's migration + schema.ts changes have landed first)
- `grep -c 'never migrated' .planning/STATE.md` === 0 (stale entry removed)
- `grep -c '0013_work_orders_drift_repair' .planning/STATE.md` === 1 (closing note landed)
- No new SQL file `0020_reconcile_work_orders_107_1.sql` exists in `vigil-core/drizzle/` (Phase 127 takes the 0020 slot for ai_usage_daily per Plan 05)
- Inverse smoke: temporarily add a non-existent column to any table in schema.ts → `pnpm test src/__tests__/migration-drift.test.ts` fails with stdout naming the column (verifies the test actually detects drift)
</verification>

<success_criteria>
- migration-drift.test.ts ships with the locked implementation (execSync drizzle-kit generate without --dry, regex /No schema changes/i, 10s timeout, fake DATABASE_URL fallback)
- STATE.md no longer claims Phase 107.1 drift is unmigrated
- No `0020_reconcile_work_orders_107_1.sql` file created (the 0020 slot belongs to Plan 05's ai_usage_daily)
- Test passes when schema.ts and drizzle/ are in sync; fails when they diverge (verified via inverse manual check)
- End-to-end re-scoped GUARD-04 complete: drift detector + stale docs both addressed
- Wave 2 / depends_on: [05] sequencing eliminates the false-fail window before Plan 05's `0020_add_ai_usage_daily.sql` lands
</success_criteria>

<output>
After completion, create `.planning/phases/127-pre-spike-guardrails/127-07-SUMMARY.md` capturing:
- Files modified (2 — migration-drift.test.ts NEW, STATE.md EDIT)
- Re-scopes applied per RESEARCH §Pitfall 1 + §Pitfall 2: no --dry flag (fictional), no new 0020_reconcile migration (drift already closed by 0013)
- STATE.md cleanup closes the stale-docs loop that originally caused 127-CONTEXT.md D-04 to specify a no-op migration
- Drift detector mechanism: shell-out + regex parse; emoji-tolerant; CI-friendly (fake DATABASE_URL works)
- Wave 2 / depends_on: [05] — implicit Plan 05 dependency now encoded explicitly in frontmatter so wave ordering is unambiguous
- v3.10+ follow-up candidate: evaluate `drizzle-kit check` as a stronger alternative once its schema-vs-migration semantics are confirmed (RESEARCH Open Q5)
- This plan's drift detector PROTECTS Plan 05's `ai_usage_daily` migration — any future change to the `aiUsageDaily` table in schema.ts without a matching migration will fail CI
</output>
