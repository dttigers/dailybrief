---
phase: 127
plan: 06
type: execute
wave: 2
depends_on: [03]
files_modified:
  - vigil-pwa/src/lib/api-error-codes.ts
  - vigil-pwa/src/lib/api-error-codes.test.ts
autonomous: true
requirements: [GUARD-03]
threat_refs: [T-127-03]
must_haves:
  truths:
    - "PWA ERROR_CODE_MAP exposes DAILY_AI_BUDGET_EXCEEDED with locked copy 'You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC.' and no CTA"
    - "Phase 126 LOCKED-block 9-key test still passes (no regression on Phase 126 D-04 lock)"
    - "Phase 127 Plan 03's AUDIO_SESSION_TOO_LONG entry still present (Wave 2 ordering preserves it)"
  artifacts:
    - path: "vigil-pwa/src/lib/api-error-codes.ts"
      provides: "DAILY_AI_BUDGET_EXCEEDED locked-enum extension key"
      contains: 'DAILY_AI_BUDGET_EXCEEDED'
    - path: "vigil-pwa/src/lib/api-error-codes.test.ts"
      provides: "GUARD-127-CODE-MAP-BUDGET-EXTENSION pin test"
      contains: 'GUARD-127-CODE-MAP-BUDGET-EXTENSION'
  key_links:
    - from: "vigil-pwa resolveApiError lookup for 429 responses with code DAILY_AI_BUDGET_EXCEEDED"
      to: "ERROR_CODE_MAP.DAILY_AI_BUDGET_EXCEEDED entry"
      via: "object key lookup in api-error-codes.ts"
      pattern: 'DAILY_AI_BUDGET_EXCEEDED:\s*\{'
---

<objective>
Land the PWA half of GUARD-03: extend `vigil-pwa/src/lib/api-error-codes.ts` ERROR_CODE_MAP EXTENSION block with the `DAILY_AI_BUDGET_EXCEEDED` locked-enum key so the PWA renders the friendly retry-tomorrow copy when vigil-core's `app.onError` returns 429 with that code (per Plan 05.1).

Purpose: vigil-core (Plan 05.1) throws DailyBudgetExceededError → 429 with `{code: "DAILY_AI_BUDGET_EXCEEDED"}`. PWA's `resolveApiError(body, fallback)` looks up `body.code` in `ERROR_CODE_MAP`. Without this extension, the PWA falls back to generic copy — defeating the user-visible part of T-127-03 mitigation.

**Wave 2 sequencing rationale:** This plan touches the same file as Plan 03 (`vigil-pwa/src/lib/api-error-codes.ts` and `.test.ts`). To avoid Wave-1 file conflict, Plan 06 lands AFTER Plan 03 — both entries end up in the EXTENSION block but each plan owns its key.

Output: Single new entry + single new pin test; LOCKED 9-key block + Plan 03's AUDIO_SESSION_TOO_LONG entry both untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@.planning/phases/127-pre-spike-guardrails/127-RESEARCH.md
@.planning/phases/127-pre-spike-guardrails/127-PATTERNS.md
@.planning/phases/127-pre-spike-guardrails/127-03-PLAN.md (Wave 1 predecessor — AUDIO_SESSION_TOO_LONG already landed by Plan 03)

<interfaces>
Expected state of vigil-pwa/src/lib/api-error-codes.ts AFTER Plan 03 (verify before editing):
```typescript
  // ── EXTENSION (Phase 126; D-04 additivity grants this authority) ──
  INVALID_REQUEST: { ... },
  INVALID_JSON: { ... },
  SERVER_NOT_CONFIGURED: { ... },
  INVALID_TOKEN_SUBJECT: { ... },
  // ── EXTENSION (Phase 127 GUARD-02 — D-04 additivity) ──
  AUDIO_SESSION_TOO_LONG: {
    message: "Recording is too long. Voice clips must be 60 seconds or less.",
  },
}
```

Plan 06 appends ONE more entry inside the same closing `}`:
```typescript
  // ── EXTENSION (Phase 127 GUARD-03 — D-04 additivity) ──
  DAILY_AI_BUDGET_EXCEEDED: {
    message: "You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC.",
  },
```

Test pattern — vigil-pwa/src/lib/api-error-codes.test.ts:61-78 (AUTH-126-CODE-MAP-LOCKED-ENUM) is the mirror for the new pin test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append DAILY_AI_BUDGET_EXCEEDED to PWA ERROR_CODE_MAP EXTENSION block + add pin test</name>
  <files>vigil-pwa/src/lib/api-error-codes.ts, vigil-pwa/src/lib/api-error-codes.test.ts</files>
  <read_first>
    - vigil-pwa/src/lib/api-error-codes.ts (full file post-Plan-03 state — verify AUDIO_SESSION_TOO_LONG is already present and locate the precise insertion point for the new entry; LOCKED block at 84-117 must NOT be touched)
    - vigil-pwa/src/lib/api-error-codes.test.ts (lines 61-78 AUTH-126-CODE-MAP-LOCKED-ENUM pattern; Plan 03's GUARD-127-CODE-MAP-AUDIO-EXTENSION should be present — use as the analog mirror)
    - .planning/phases/127-pre-spike-guardrails/127-CONTEXT.md (D-03.5 — exact copy `"You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC."` and no CTA)
    - .planning/phases/127-pre-spike-guardrails/127-PATTERNS.md (section "vigil-pwa/src/lib/api-error-codes.ts (EDIT — add 2 EXTENSION keys)" — confirms both keys land in EXTENSION block; section "vigil-pwa/src/lib/api-error-codes.test.ts" — pin-test pattern for both keys; this plan adds the second one)
    - .planning/phases/127-pre-spike-guardrails/127-05.1-PLAN.md (verifies vigil-core sends `{code: "DAILY_AI_BUDGET_EXCEEDED"}` — confirms the literal string the PWA must look up)
  </read_first>
  <behavior>
    - `ERROR_CODE_MAP.DAILY_AI_BUDGET_EXCEEDED` is defined
    - `ERROR_CODE_MAP.DAILY_AI_BUDGET_EXCEEDED.message === "You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC."` (D-03.5 literal copy lock)
    - `ERROR_CODE_MAP.DAILY_AI_BUDGET_EXCEEDED.ctaLabel === undefined` (D-03.5: no CTA)
    - `ERROR_CODE_MAP.DAILY_AI_BUDGET_EXCEEDED.ctaHref === undefined` (D-03.5: no CTA)
    - Phase 126 LOCKED 9-key block remains untouched (AUTH-126-CODE-MAP-LOCKED-ENUM still passes)
    - Plan 03's AUDIO_SESSION_TOO_LONG entry remains untouched (GUARD-127-CODE-MAP-AUDIO-EXTENSION still passes)
    - New entry sits AFTER AUDIO_SESSION_TOO_LONG (Wave 2 ordering — preserves Plan 03's edit)
    - The new test block `GUARD-127-CODE-MAP-BUDGET-EXTENSION` lives near (immediately after) Plan 03's `GUARD-127-CODE-MAP-AUDIO-EXTENSION` block for code locality
  </behavior>
  <action>
    Edit `vigil-pwa/src/lib/api-error-codes.ts` per D-03.5. Find the tail of the EXTENSION block — by Wave 2, Plan 03 has already added `AUDIO_SESSION_TOO_LONG` AFTER the four Phase 126 entries. Append a new entry IMMEDIATELY AFTER `AUDIO_SESSION_TOO_LONG`'s closing `},` and BEFORE the final closing `}` of ERROR_CODE_MAP:

    [Code shape lifted from PATTERNS.md / read_first sources. Identifiers to land: `DAILY_AI_BUDGET_EXCEEDED`. Match the analog body verbatim except for the specific edits called out in this action.]

    Locked invariants (D-03.5 + Phase 126 D-04):
    - New entry lives in EXTENSION block (NOT LOCKED block — Phase 126 D-04 LOCKED 9 keys must not change).
    - No `ctaLabel` / `ctaHref` (D-03.5: no CTA).
    - Copy is verbatim from CONTEXT D-03.5. Note the curly apostrophe in `"You've"` and the em-dash `"—"` — match the exact Unicode characters used in CONTEXT (UTF-8 `’` and `—`, NOT straight apostrophe `'` or hyphen-minus `-`). If CONTEXT uses straight ASCII, match that instead. CONTEXT D-03.5 reads literally: `"You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC."` — use this exact text byte-for-byte.

    Do NOT touch the LOCKED block (api-error-codes.ts:84-117). Do NOT touch Plan 03's AUDIO_SESSION_TOO_LONG entry. Do NOT remove Phase 126's INVALID_REQUEST / INVALID_JSON / SERVER_NOT_CONFIGURED / INVALID_TOKEN_SUBJECT entries.

    Edit `vigil-pwa/src/lib/api-error-codes.test.ts` — add a new `it("GUARD-127-CODE-MAP-BUDGET-EXTENSION: ...")` block mirroring the AUTH-126-CODE-MAP-LOCKED-ENUM pattern at lines 61-78 AND Plan 03's GUARD-127-CODE-MAP-AUDIO-EXTENSION block:

    [Code shape lifted from PATTERNS.md / read_first sources. Match the analog body verbatim except for the specific edits called out in this action.]

    Place the new it-block immediately after Plan 03's GUARD-127-CODE-MAP-AUDIO-EXTENSION block (Phase 127 extensions adjacent for code locality). Verbatim copy assertion (`expect(...).toBe(literal)`) locks the copy against accidental rewrites — pairs with the source-side copy in api-error-codes.ts.

    Do NOT touch existing AUTH-126-CODE-MAP-LOCKED-ENUM or GUARD-127-CODE-MAP-AUDIO-EXTENSION tests (each owned by their respective plan).
  </action>
  <verify>
    <automated>cd vigil-pwa && pnpm test src/lib/api-error-codes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DAILY_AI_BUDGET_EXCEEDED:' vigil-pwa/src/lib/api-error-codes.ts` === 1 (object key only)
    - `grep -c "You've hit today's AI processing limit" vigil-pwa/src/lib/api-error-codes.ts` === 1 (literal copy present)
    - `grep -c 'AUDIO_SESSION_TOO_LONG:' vigil-pwa/src/lib/api-error-codes.ts` === 1 (Plan 03's entry still present — Wave 2 sequencing preserved)
    - `grep -c 'GUARD-127-CODE-MAP-BUDGET-EXTENSION' vigil-pwa/src/lib/api-error-codes.test.ts` === 1
    - `grep -c 'GUARD-127-CODE-MAP-AUDIO-EXTENSION' vigil-pwa/src/lib/api-error-codes.test.ts` === 1 (Plan 03's test still present)
    - `grep -c 'AUTH-126-CODE-MAP-LOCKED-ENUM' vigil-pwa/src/lib/api-error-codes.test.ts` === 1 (Phase 126 lock still present)
    - `cd vigil-pwa && pnpm test src/lib/api-error-codes.test.ts` exits 0 — all THREE test groups pass: AUTH-126-CODE-MAP-LOCKED-ENUM + GUARD-127-CODE-MAP-AUDIO-EXTENSION + GUARD-127-CODE-MAP-BUDGET-EXTENSION
    - The new entry sits in the EXTENSION block region of api-error-codes.ts — verify by reading and confirming it appears AFTER the `// ── EXTENSION (Phase 126; D-04 additivity ...) ──` comment marker
    - LOCKED block lines (84-117) byte-identical to pre-Plan-06 state — verify via `diff` against HEAD if possible
  </acceptance_criteria>
  <done>DAILY_AI_BUDGET_EXCEEDED locked-enum extension landed; new pin test green; LOCKED block + Plan 03's AUDIO_SESSION_TOO_LONG block both preserved; user-visible 429 friendly copy ready when vigil-core (Plan 05.1) emits the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vigil-core HTTP 429 response → vigil-pwa resolveApiError | PWA reads `body.code` and looks up ERROR_CODE_MAP for friendly copy; missing entry falls back to generic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-03 | DoS (AI cost) — user-visible UX half | Without the EXTENSION entry, PWA shows generic copy on 429; user gets no actionable feedback about when AI features return | mitigate | DAILY_AI_BUDGET_EXCEEDED entry with verbatim D-03.5 copy "You've hit today's AI processing limit. Capture still works — AI features resume at midnight UTC." — surfaces both the cause (AI limit) and the workaround (capture still works) + reset time (midnight UTC) |
| T-127-03-G | Tampering (copy rewrite) | A future PR rewrites the friendly copy in a way that no longer mentions capture-still-works or midnight UTC | mitigate | Verbatim `expect(entry.message).toBe(...)` assertion in the new pin test fails CI on any character change |
| T-127-03-H | Information Disclosure (CTA leak) | A future copy edit adds a CTA pointing to a sensitive admin path | mitigate | `expect(entry.ctaLabel).toBeUndefined()` + `expect(entry.ctaHref).toBeUndefined()` in the pin test asserts no CTA can be added without updating the test |
</threat_model>

<verification>
- `cd vigil-pwa && pnpm test src/lib/api-error-codes.test.ts` exits 0
- Manual smoke (post-deploy): set `VIGIL_DAILY_AI_BUDGET_USD=0.001` locally → trigger 2 chat requests → PWA renders the friendly retry-tomorrow copy verbatim (no generic fallback)
- LOCKED 9-key block in api-error-codes.ts byte-identical to pre-plan state: `git diff HEAD~1 vigil-pwa/src/lib/api-error-codes.ts | grep '^[-+]' | grep -v EXTENSION | head` — no LOCKED keys appear in the diff
</verification>

<success_criteria>
- DAILY_AI_BUDGET_EXCEEDED entry present in EXTENSION block with verbatim D-03.5 copy
- ctaLabel + ctaHref both undefined
- Phase 126 LOCKED block + Plan 03's AUDIO_SESSION_TOO_LONG entry both preserved
- Vitest passes for all three test groups (AUTH-126 LOCKED + two Phase 127 EXTENSION pins)
- End-to-end GUARD-03 user-visible loop closes: vigil-core 429 → PWA friendly copy → user understands the limit + reset time
</success_criteria>

<output>
After completion, create `.planning/phases/127-pre-spike-guardrails/127-06-SUMMARY.md` capturing:
- Files modified (2 — api-error-codes.ts EDIT, api-error-codes.test.ts EDIT)
- Both Phase 127 EXTENSION keys now in ERROR_CODE_MAP: AUDIO_SESSION_TOO_LONG (Plan 03) + DAILY_AI_BUDGET_EXCEEDED (this plan)
- Wave 2 ordering preserved Plan 03's edit cleanly (no merge conflict)
- End-to-end loop closed: vigil-core throws → 429 + code → PWA resolveApiError → friendly copy rendered
- Manual smoke step documented for post-deploy verification (also tracked in 127-VALIDATION.md row 3)
</output>
