---
phase: 13-polish-integration
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - Sources/DailyBriefMonitor/AppDelegate.swift
  - Sources/JarvisCore/Services/FolderWatcherService.swift
  - .planning/REQUIREMENTS.md
autonomous: true
---

<objective>
Add event-driven sync triggers after capture/triage and commit the pending FolderWatcherService HEIC/TIFF image conversion improvement.

Purpose: SyncService currently only syncs on a timer. Thoughts captured or triaged should trigger an immediate sync for faster cross-Mac propagation. Also finalizes the uncommitted FolderWatcherService image format support.
Output: Sync triggers wired to capture events, FolderWatcherService improvement committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/DailyBriefMonitor/AppDelegate.swift
@Sources/JarvisCore/Services/FolderWatcherService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-capture sync trigger in AppDelegate</name>
  <files>Sources/DailyBriefMonitor/AppDelegate.swift</files>
  <action>
  In AppDelegate.swift, add a sync trigger after successful capture and triage operations:

  - In the `onCapture` closure (inside the CapturePanel setup), after `service.captureText(text)` returns, trigger a non-blocking sync:
    ```swift
    let thought = try await service.captureText(text)
    // Trigger sync after capture (non-blocking)
    if let syncService = self?.syncService {
        Task { try? await syncService.sync() }
    }
    return thought
    ```
    Note: The closure needs weak self access. Check if it already captures self weakly — if not, add `[weak self]` to the closure capture list. The `syncService` property is on AppDelegate.

  - In the `onTriage` closure, after the thought is updated with triage result, trigger sync similarly:
    ```swift
    try await thoughtStore.update(thought)
    if let syncService = self?.syncService {
        Task { try? await syncService.sync() }
    }
    ```

  - Do NOT add sync triggers in the error paths — only sync after successful capture/triage.
  - The sync calls should be fire-and-forget (Task + try?) to avoid blocking the capture UI.
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>Capture and triage operations trigger immediate sync when SyncService is active</done>
</task>

<task type="auto">
  <name>Task 2: Commit FolderWatcher HEIC/TIFF support and update REQUIREMENTS.md</name>
  <files>Sources/JarvisCore/Services/FolderWatcherService.swift, .planning/REQUIREMENTS.md</files>
  <action>
  **FolderWatcherService.swift:**
  - The file already has uncommitted changes adding HEIC/TIFF/BMP image format conversion support
  - Review the changes are correct (import AppKit, nativeImageExtensions set, convertToJPEG method, ImageConversionError enum)
  - No code changes needed — just stage and commit this file

  **REQUIREMENTS.md:**
  - Update the traceability section: Mark CAPT-03 (voice capture) and CAPT-04 (image capture) as Complete — they work via dashboard toolbar import buttons and folder watching (Phase 5 decision moved them from capture panel to dashboard)
  - Mark INTEG-01 (Google Calendar) as Complete — implemented in Phase 7
  - Update STORE-01 and STORE-02 status to Complete if not already
  - All v1 requirements should now be Complete
  </action>
  <verify>swift build succeeds; all v1 requirements in REQUIREMENTS.md show Complete status</verify>
  <done>FolderWatcherService HEIC/TIFF support committed; REQUIREMENTS.md fully updated with all v1 requirements complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for all targets
- [ ] Post-capture sync trigger fires (check with NSLog or test manually)
- [ ] FolderWatcherService handles HEIC/TIFF/BMP image formats
- [ ] REQUIREMENTS.md shows all v1 requirements as Complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sync triggers wired to capture and triage events
- FolderWatcherService image format conversion committed
- All v1 requirements marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/13-polish-integration/13-03-SUMMARY.md`
</output>
