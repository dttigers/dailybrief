---
phase: 115
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-pwa/src/components/ThoughtRow.tsx
  - vigil-pwa/src/components/ThoughtRow.test.tsx
autonomous: true
requirements:
  - POLISH-01
tags:
  - pwa
  - polish
  - tailwind
  - thought

must_haves:
  truths:
    - "The non-edit-mode <p> in ThoughtRow.tsx (renders thought.content) has className containing 'whitespace-pre-line'"
    - "Existing classes 'text-gray-100', 'text-sm', 'leading-relaxed', 'line-clamp-3', 'break-words', 'cursor-text' are preserved on the same <p>"
    - "Edit-mode <textarea> (lines 387-395) is unchanged"
    - "ThoughtRow.test.tsx contains a regression test that renders a thought with embedded \\n and asserts the rendered <p>'s className contains 'whitespace-pre-line'"
  artifacts:
    - path: "vigil-pwa/src/components/ThoughtRow.tsx"
      provides: "Updated <p> className with whitespace-pre-line appended"
      contains: "whitespace-pre-line"
    - path: "vigil-pwa/src/components/ThoughtRow.test.tsx"
      provides: "Regression test locking POLISH-01 against future className refactors"
      contains: "POLISH-01"
  key_links:
    - from: "ThoughtRow.tsx <p> at line 397-402"
      to: "Tailwind utility 'whitespace-pre-line'"
      via: "className string append"
      pattern: "whitespace-pre-line"
    - from: "Multi-line thought content (with \\n)"
      to: "Visible line breaks in row view"
      via: "CSS white-space: pre-line; (Tailwind whitespace-pre-line)"
      pattern: "whitespace-pre-line"
---

<objective>
Apply POLISH-01: append the Tailwind utility class `whitespace-pre-line` to the non-edit-mode `<p>` className in `vigil-pwa/src/components/ThoughtRow.tsx` (line 399) so multi-line thought captures render with line breaks preserved instead of collapsing to a single line. Add a regression test in `ThoughtRow.test.tsx` that pins this behavior against future className refactors or Tailwind purging (D-17).

Single-line code change. Standalone plan per D-18 — independent of Plans 01/02, can land in parallel (Wave 1) or even ship if calendar work hits a snag.

Purpose: Multi-line thought captures (paste multi-paragraph notes, voice transcriptions with newlines) currently collapse to a single line in the row view. This restores the visual structure the user typed.
Output: One className change, one regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-calendar-source-picker-thoughtrow-polish/115-CONTEXT.md

@vigil-pwa/src/components/ThoughtRow.tsx
@vigil-pwa/src/components/ThoughtRow.test.tsx

<interfaces>
<!-- Current state of the <p> className (vigil-pwa/src/components/ThoughtRow.tsx:397-402): -->

```jsx
<p
  onClick={handleContentClick}
  className="text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text"
>
  {thought.content}
</p>
```

<!-- Target state per D-15: -->

```jsx
<p
  onClick={handleContentClick}
  className="text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text whitespace-pre-line"
>
  {thought.content}
</p>
```

<!-- Unchanged (D-16): edit-mode textarea at lines 387-395 — DO NOT modify. -->
```jsx
<textarea
  ref={textareaRef}
  value={draft}
  onChange={(e) => setDraft(e.target.value)}
  onBlur={handleSave}
  onKeyDown={handleKeyDown}
  className="w-full bg-gray-900/80 border border-teal-600 rounded-lg p-2 text-gray-50 focus:outline-none resize-y min-h-[4rem]"
  autoFocus
/>
```

<!-- Existing test pattern (vigil-pwa/src/components/ThoughtRow.test.tsx, lines 1-65) — model the new test on this. -->
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, fireEvent, screen } from '@testing-library/react'
import ThoughtRow from './ThoughtRow'
import type { ThoughtApiResponse } from '../api/client'

const baseThought: ThoughtApiResponse = {
  id: 42,
  content: 'hello',
  category: 'idea',
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append whitespace-pre-line to ThoughtRow's non-edit-mode &lt;p&gt; className</name>
  <files>vigil-pwa/src/components/ThoughtRow.tsx</files>
  <read_first>
    - vigil-pwa/src/components/ThoughtRow.tsx (full file — locate lines 386-403, specifically the ternary that renders &lt;textarea&gt; in edit mode and &lt;p&gt; in display mode. The &lt;p&gt; is the SECOND branch of the ternary at lines 396-403.)
  </read_first>
  <behavior>
    - The display-mode `<p>` element's className contains `whitespace-pre-line` AFTER the change.
    - The display-mode `<p>` element's className still contains: `text-gray-100`, `text-sm`, `leading-relaxed`, `line-clamp-3`, `break-words`, `cursor-text` (D-15: existing classes preserved).
    - The edit-mode `<textarea>` className is unchanged (D-16).
    - Final exact className string: `text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text whitespace-pre-line`.
  </behavior>
  <action>
    1. Open `vigil-pwa/src/components/ThoughtRow.tsx`. Locate the ternary block at lines 386-403:
       ```jsx
       {isEditing ? (
         <textarea
           ref={textareaRef}
           value={draft}
           onChange={(e) => setDraft(e.target.value)}
           onBlur={handleSave}
           onKeyDown={handleKeyDown}
           className="w-full bg-gray-900/80 border border-teal-600 rounded-lg p-2 text-gray-50 focus:outline-none resize-y min-h-[4rem]"
           autoFocus
         />
       ) : (
         <p
           onClick={handleContentClick}
           className="text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text"
         >
           {thought.content}
         </p>
       )}
       ```

    2. UPDATE ONLY the `<p>` element's className. Append ` whitespace-pre-line` (note the leading space inside the quoted string) so the final value is:
       ```
       className="text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text whitespace-pre-line"
       ```

    3. DO NOT modify:
       - The `<textarea>` className (D-16).
       - Any other line in the file.
       - The `onClick={handleContentClick}` binding.
       - The `{thought.content}` text node (whitespace-pre-line is a CSS-only fix; React still text-escapes the content for XSS safety).

    4. Confirm Tailwind purging includes `whitespace-pre-line`: search the existing codebase for any prior use of this class to confirm Tailwind's content-glob picks it up. If the class is not used elsewhere, the new occurrence in this file is enough — Tailwind v3 scans .tsx files in `vigil-pwa/src/**`. No tailwind.config.js change required.

    5. TypeScript / lint: `cd vigil-pwa && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd vigil-pwa &amp;&amp; grep -n "whitespace-pre-line" src/components/ThoughtRow.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text whitespace-pre-line" vigil-pwa/src/components/ThoughtRow.tsx` matches exactly once.
    - `grep -n "whitespace-pre-line" vigil-pwa/src/components/ThoughtRow.tsx` matches exactly once.
    - `grep -n "resize-y min-h-\[4rem\]" vigil-pwa/src/components/ThoughtRow.tsx` still matches once (textarea className unchanged — D-16).
    - `grep -c "className=" vigil-pwa/src/components/ThoughtRow.tsx` is unchanged from before this task (count of className attributes is the same; only one is modified).
    - `cd vigil-pwa &amp;&amp; npx tsc --noEmit` exits 0.
    - File diff against HEAD shows exactly one line changed (the &lt;p&gt; className), zero lines added beyond that single edit.
  </acceptance_criteria>
  <done>
    The display-mode &lt;p&gt; className has `whitespace-pre-line` appended; all other classes preserved; edit-mode textarea untouched; TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add POLISH-01 regression test in ThoughtRow.test.tsx</name>
  <files>vigil-pwa/src/components/ThoughtRow.test.tsx</files>
  <read_first>
    - vigil-pwa/src/components/ThoughtRow.test.tsx (lines 1-80 — see existing imports, baseThought fixture, and describe block patterns)
    - vigil-pwa/src/components/ThoughtRow.tsx (after Task 1) — confirm the className change is in place before writing the assertion
  </read_first>
  <behavior>
    - Test 1 (POLISH-01-whitespace-pre-line-class): Render ThoughtRow with a thought whose content contains `\n` (e.g., `"line one\nline two\nline three"`). Find the rendered &lt;p&gt; element (the one displaying the content). Assert its className string contains the substring `whitespace-pre-line`.
    - The test does NOT need to assert visual rendering of line breaks (jsdom doesn't compute CSS); it locks in the class presence, which is the contract per D-17.
  </behavior>
  <action>
    1. Open `vigil-pwa/src/components/ThoughtRow.test.tsx`. ADD a new describe block at the END of the file (after all existing describe blocks):
       ```typescript
       describe('ThoughtRow — POLISH-01 whitespace-pre-line', () => {
         it('POLISH-01-whitespace-pre-line-class: display-mode <p> className contains whitespace-pre-line so multi-line content renders with line breaks', () => {
           const multilineThought: ThoughtApiResponse = {
             ...baseThought,
             content: 'line one\nline two\nline three',
           }
           render(<ThoughtRow thought={multilineThought} onUpdate={vi.fn()} />)
           // The display-mode <p> renders the thought content as its only text node.
           // Find by the literal text — React's text node search is whitespace-tolerant
           // by default but matches because the <p> is the only element rendering it.
           const para = screen.getByText((_, el) => {
             return el?.tagName === 'P' && el.textContent === 'line one\nline two\nline three'
           })
           expect(para).toBeInTheDocument()
           expect(para.className).toContain('whitespace-pre-line')
           // Sanity: the load-bearing existing classes survive the change (D-15 guard).
           expect(para.className).toContain('text-gray-100')
           expect(para.className).toContain('line-clamp-3')
           expect(para.className).toContain('break-words')
         })
       })
       ```

    2. The `screen` import is already present at file lines 1-9. The `vi`, `render` imports are already present too. The `ThoughtApiResponse` type is imported from `'../api/client'` at line 11, and `baseThought` is defined at lines 13-26 — reuse both.

    3. Run: `cd vigil-pwa && npx vitest run src/components/ThoughtRow.test.tsx`. The new test must pass alongside all existing ThoughtRow tests.
  </action>
  <verify>
    <automated>cd vigil-pwa &amp;&amp; npx vitest run src/components/ThoughtRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "POLISH-01-whitespace-pre-line-class" vigil-pwa/src/components/ThoughtRow.test.tsx` matches exactly once.
    - `grep -n "expect(para.className).toContain('whitespace-pre-line')" vigil-pwa/src/components/ThoughtRow.test.tsx` matches exactly once.
    - `grep -n "'line one\\\\nline two\\\\nline three'" vigil-pwa/src/components/ThoughtRow.test.tsx` matches exactly once (the embedded \n test fixture).
    - `cd vigil-pwa &amp;&amp; npx vitest run src/components/ThoughtRow.test.tsx` exits 0.
    - All pre-existing ThoughtRow tests still pass (regression check).
  </acceptance_criteria>
  <done>
    POLISH-01 regression test locks the className contract; passes against the Task 1 change; pre-existing ThoughtRow tests untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-stored thought.content → React render | Already a render boundary; this change does NOT introduce a new one. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03-01 | Tampering / XSS | Adding `whitespace-pre-line` regresses XSS posture | accept | The class is a CSS-only directive (`white-space: pre-line`) that affects whitespace handling. It does NOT change how `{thought.content}` is rendered — React still escapes the content as a text node. NO `dangerouslySetInnerHTML` is added. NO HTML parsing of thought content is enabled. Verify: `grep -n "dangerouslySetInnerHTML" vigil-pwa/src/components/ThoughtRow.tsx` matches 0 occurrences (before AND after the change). The Tailwind class compiles to standard CSS and cannot execute JavaScript. Pre-existing T-93 (XSS guards on thought content) posture is preserved. |
| T-115-03-02 | Information Disclosure | Long multi-line content overflowing the row to leak adjacent thoughts | mitigate | `line-clamp-3` is preserved on the same className — multi-line content is still clamped to 3 visual lines, regardless of newline count. `break-words` handles long unbroken tokens. Visual overflow into adjacent rows is impossible. The new `whitespace-pre-line` interacts cleanly with `line-clamp-3` (line-clamp counts visual lines after whitespace processing). |
| T-115-03-03 | Denial of Service | Massive content with thousands of `\n` characters causing layout thrash | accept | `line-clamp-3` caps the visual lines at 3, so layout cost is bounded regardless of content length. CSS engine handles arbitrary newlines without measurable performance impact. NO new DOM nodes are created (it's still one &lt;p&gt; element). |

**Block on:** T-115-03-01 (XSS posture preserved). The grep check in acceptance criteria confirms no `dangerouslySetInnerHTML` was introduced.
</threat_model>

<verification>
## End-of-plan checks

1. `grep -n "whitespace-pre-line" vigil-pwa/src/components/ThoughtRow.tsx` — exactly 1 match.
2. `grep -n "text-gray-100 text-sm leading-relaxed line-clamp-3 break-words cursor-text whitespace-pre-line" vigil-pwa/src/components/ThoughtRow.tsx` — exactly 1 match (the full target className).
3. `grep -n "dangerouslySetInnerHTML" vigil-pwa/src/components/ThoughtRow.tsx` — 0 matches (XSS posture).
4. `grep -n "POLISH-01-whitespace-pre-line-class" vigil-pwa/src/components/ThoughtRow.test.tsx` — exactly 1 match.
5. `cd vigil-pwa && npx vitest run src/components/ThoughtRow.test.tsx` — exit 0.
6. `cd vigil-pwa && npx tsc --noEmit` — exit 0.
7. Diff vs HEAD on `ThoughtRow.tsx` shows exactly 1 line changed (the &lt;p&gt; className).
</verification>

<success_criteria>
- Multi-line thought content renders with preserved newlines in the row view (CSS contract; visible on the deployed PWA after this ships).
- Existing classes (`line-clamp-3`, `break-words`, color/typography) are preserved.
- Edit-mode textarea is untouched.
- Regression test pins the className against future refactors / Tailwind purging.
- All pre-existing ThoughtRow tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/115-calendar-source-picker-thoughtrow-polish/115-03-SUMMARY.md` documenting the className change, the regression test, and confirmation that XSS posture is unchanged.
</output>
