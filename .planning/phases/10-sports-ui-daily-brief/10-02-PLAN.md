---
phase: 10-sports-ui-daily-brief
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Sources/DailyBrief/PDF/PageTwoRenderer.swift
  - Sources/DailyBrief/DailyBrief.swift
  - Sources/DailyBrief/Config/ConfigLoader.swift
autonomous: false
---

<objective>
Make PDF sports section dynamic — use config-driven team and division names instead of hardcoded "Detroit Tigers" / "AL Central Standings". Make the standings abbreviation map work for any division.

Purpose: After Plan 01 adds team/division names to config, the PDF renderer must use them so switching teams in settings actually changes the brief output.
Output: PDF that dynamically renders the correct team name, division name, and team abbreviations for any MLB team.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sports-ui-daily-brief/10-01-SUMMARY.md

@Sources/DailyBrief/PDF/PageTwoRenderer.swift
@Sources/DailyBrief/PDF/PDFGenerator.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/JarvisCore/Config/AppConfig.swift
@Sources/JarvisCore/Models/MLBTeamData.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make PageTwoRenderer use config-driven names</name>
  <files>Sources/DailyBrief/PDF/PageTwoRenderer.swift, Sources/DailyBrief/DailyBrief.swift</files>
  <action>
  1. Update PageTwoRenderer.render() signature to accept `teamName: String` and `divisionName: String` parameters (from config).

  2. Replace hardcoded values:
     - Line ~16: Replace hardcoded "Detroit Tigers" title with the `teamName` parameter
     - Line ~92: Replace hardcoded "AL Central Standings" with "{divisionName} Standings"
     - Update the team highlighting logic: currently checks `entry.team.contains("Detroit")` — change to check if entry.team matches the configured team name (use `entry.team.contains(teamName)` or compare with the full name)

  3. Replace hardcoded `shortenTeamName()` function:
     - Currently maps only 5 AL Central teams with hardcoded abbreviations
     - Use MLBTeamData to build the abbreviation map dynamically: for each StandingsEntry team name, find it in MLBTeamData.allTeams and use its abbreviation
     - New approach: `MLBTeamData.allTeams.first(where: { entry.team.contains($0.name) || $0.name.contains(entry.team) })?.abbreviation`
     - Format: "{abbreviation} {lastName}" where lastName is the last word of the team name (e.g., "DET Tigers", "NYY Yankees")
     - Fallback: if no match found, truncate to first 12 chars (shouldn't happen with complete team data)

  4. Update the call site in DailyBrief.swift generate command:
     - Pass `config.sports.teamName` and `config.sports.divisionName` to PageTwoRenderer.render()
     - The config should already have these fields from Plan 01's SportsConfig update

  Note: The render method currently has parameters like `(context:, data:, contentRect:, ...)`. Add teamName and divisionName as additional parameters. Do NOT restructure the existing parameter list — just append.
  </action>
  <verify>swift build compiles without errors. Run `swift run dailybrief --dry-run` to verify the generate flow works end-to-end.</verify>
  <done>PageTwoRenderer uses config-driven team name as title, config-driven division name for standings header, and dynamically generates team abbreviations from MLBTeamData. No hardcoded team/division references remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dynamic PDF sports section that renders based on configured team selection instead of hardcoded Detroit Tigers / AL Central</what-built>
  <how-to-verify>
    1. Run: `swift run dailybrief` (generates PDF)
    2. Open the generated PDF (check ~/Documents/DailyBrief/ or configured output path)
    3. Verify Page 2:
       - Team name header matches configured team (default: "Detroit Tigers")
       - Standings header shows correct division (default: "AL Central Standings")
       - Team abbreviations render correctly in standings table
       - Configured team is still highlighted bold in standings
    4. Optional: Change team in Settings to a different team (e.g., New York Yankees), regenerate, verify PDF updates
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for both targets
- [ ] No hardcoded "Detroit Tigers" or "AL Central" in PageTwoRenderer
- [ ] PDF renders correctly with default config
- [ ] Team abbreviations work for any division
- [ ] Human verified PDF output
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hardcoded team/division references in PDF renderer
- PDF dynamically reflects configured team selection
- Human approved PDF visual output
</success_criteria>

<output>
After completion, create `.planning/phases/10-sports-ui-daily-brief/10-02-SUMMARY.md`
</output>
