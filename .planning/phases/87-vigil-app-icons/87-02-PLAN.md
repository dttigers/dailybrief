---
phase: 87-vigil-app-icons
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - vigil-pwa/vite.config.ts
  - vigil-pwa/public/pwa-192x192.png
  - vigil-pwa/public/pwa-256x256.png
  - vigil-pwa/public/pwa-384x384.png
  - vigil-pwa/public/pwa-512x512.png
  - vigil-pwa/public/pwa-512x512-maskable.png
  - vigil-pwa/public/apple-touch-icon.png
  - vigil-pwa/public/favicon.svg
  - vigil-pwa/public/favicon.ico
autonomous: false
requirements: []
must_haves:
  truths:
    - "vigil-pwa/public/ contains all 8 icon files matching brand/pwa/ byte-for-byte"
    - "vite.config.ts VitePWA manifest lists 192, 256, 384, 512 (any), and 512 (maskable) icon entries pointing to real files (SC-1)"
    - "vite.config.ts manifest theme_color is #1D9E75 (D-02)"
    - "vite.config.ts includeAssets includes favicon.svg and favicon.ico"
    - "favicon.svg in vigil-pwa/public/ is the brand mark, not the stopgap (SC-2)"
    - "`vite build` produces a dist/manifest.webmanifest whose icons array contains the 5 expected src values"
    - "PWA installed on iOS/macOS home screen shows the Vigil diamond+V mark (SC-3, human-verified)"
  artifacts:
    - path: "vigil-pwa/vite.config.ts"
      provides: "VitePWA manifest with full icon set + correct theme_color"
    - path: "vigil-pwa/public/pwa-256x256.png"
      provides: "New 256px icon (didn't exist before)"
    - path: "vigil-pwa/public/pwa-384x384.png"
      provides: "New 384px icon (didn't exist before)"
    - path: "vigil-pwa/public/pwa-512x512-maskable.png"
      provides: "New maskable 512 variant with 80% safe zone"
    - path: "vigil-pwa/public/favicon.ico"
      provides: "Legacy-browser favicon"
  key_links:
    - from: "vigil-pwa/vite.config.ts icons[]"
      to: "vigil-pwa/public/pwa-*.png"
      via: "VitePWA plugin copies public/ into build output"
      pattern: "manifest src references resolve to files in public/"
    - from: "dist/manifest.webmanifest"
      to: "installed PWA icon on iOS home screen"
      via: "browser install flow reads manifest.icons"
      pattern: "best-match size chosen by OS at install"
---

<objective>
Wire the PWA to the new brand assets from Plan 01: copy assets from `brand/pwa/` into `vigil-pwa/public/`, extend the VitePWA manifest to include all 5 sizes (D-05), update theme_color to brand teal (D-02), and human-verify installed PWA on both iOS and macOS.

Purpose: Close SC-1, SC-2, and SC-3. After this plan, every PWA surface — install prompt, home screen, browser tab — shows the Vigil brand mark.

Output: PWA rebuilds cleanly, manifest.webmanifest references correct files, installed PWA displays brand icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-vigil-app-icons/87-CONTEXT.md
@vigil-pwa/vite.config.ts
</context>

<interfaces>
Current VitePWA manifest (vigil-pwa/vite.config.ts lines 18-48) — to be extended:

```typescript
VitePWA({
  registerType: 'autoUpdate',
  includeAssets: ['favicon.svg'],              // ← must add 'favicon.ico', 'apple-touch-icon.png'
  manifest: {
    name: 'Vigil Dashboard',
    short_name: 'Vigil',
    description: 'Vigil ambient AI dashboard',
    theme_color: '#0F6E56',                     // ← change to '#1D9E75' (D-02)
    background_color: '#0f172a',                // ← KEEP (splash bg unrelated to icon)
    display: 'standalone',
    start_url: '/',
    scope: '/',
    icons: [                                    // ← extend to 5 entries (D-05)
      { src: 'pwa-192x192.png', sizes: '192x192', type: 'image/png' },
      { src: 'pwa-512x512.png', sizes: '512x512', type: 'image/png' },
      { src: 'pwa-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' }
    ],
  },
  // workbox config — leave unchanged
})
```

Target icons[] array (D-05):
```typescript
icons: [
  { src: 'pwa-192x192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
  { src: 'pwa-256x256.png', sizes: '256x256', type: 'image/png', purpose: 'any' },
  { src: 'pwa-384x384.png', sizes: '384x384', type: 'image/png', purpose: 'any' },
  { src: 'pwa-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'any' },
  { src: 'pwa-512x512-maskable.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
]
```

Note: D-05 allows either a combined `'any maskable'` entry or split entries. Split is preferred because the maskable PNG has a different safe-zone composition (D-04) so it shouldn't double as "any".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Copy brand assets into vigil-pwa/public/</name>
  <files>vigil-pwa/public/pwa-192x192.png, vigil-pwa/public/pwa-256x256.png, vigil-pwa/public/pwa-384x384.png, vigil-pwa/public/pwa-512x512.png, vigil-pwa/public/pwa-512x512-maskable.png, vigil-pwa/public/apple-touch-icon.png, vigil-pwa/public/favicon.svg, vigil-pwa/public/favicon.ico</files>
  <read_first>
    - brand/pwa/ directory (output of Plan 01 Task 2)
    - vigil-pwa/public/ (existing 4 files to be overwritten + 4 new ones)
  </read_first>
  <action>
    Direct copy per D-08 ("Direct copies are simpler — use those"):

    ```
    cp brand/pwa/pwa-192x192.png         vigil-pwa/public/pwa-192x192.png
    cp brand/pwa/pwa-256x256.png         vigil-pwa/public/pwa-256x256.png
    cp brand/pwa/pwa-384x384.png         vigil-pwa/public/pwa-384x384.png
    cp brand/pwa/pwa-512x512.png         vigil-pwa/public/pwa-512x512.png
    cp brand/pwa/pwa-512x512-maskable.png vigil-pwa/public/pwa-512x512-maskable.png
    cp brand/pwa/apple-touch-icon.png    vigil-pwa/public/apple-touch-icon.png
    cp brand/pwa/favicon.svg             vigil-pwa/public/favicon.svg
    cp brand/pwa/favicon.ico             vigil-pwa/public/favicon.ico
    ```

    Verify byte-for-byte match (defense against accidental editor rewrites):
    ```
    for f in pwa-192x192.png pwa-256x256.png pwa-384x384.png pwa-512x512.png \
             pwa-512x512-maskable.png apple-touch-icon.png favicon.svg favicon.ico; do
      diff -q brand/pwa/$f vigil-pwa/public/$f || { echo "DRIFT: $f"; exit 1; }
    done
    ```
  </action>
  <verify>
    <automated>
      test -s vigil-pwa/public/pwa-192x192.png && \
      test -s vigil-pwa/public/pwa-256x256.png && \
      test -s vigil-pwa/public/pwa-384x384.png && \
      test -s vigil-pwa/public/pwa-512x512.png && \
      test -s vigil-pwa/public/pwa-512x512-maskable.png && \
      test -s vigil-pwa/public/apple-touch-icon.png && \
      test -s vigil-pwa/public/favicon.svg && \
      test -s vigil-pwa/public/favicon.ico && \
      diff -q brand/pwa/favicon.svg vigil-pwa/public/favicon.svg && \
      diff -q brand/pwa/pwa-512x512-maskable.png vigil-pwa/public/pwa-512x512-maskable.png
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 files in vigil-pwa/public/ non-empty
    - `diff -q` reports zero differences from brand/pwa/ counterparts
    - `grep -q "#1D9E75" vigil-pwa/public/favicon.svg` (brand teal present in favicon)
  </acceptance_criteria>
  <done>public/ directory mirrors brand/pwa/; ready for manifest wiring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update vite.config.ts VitePWA manifest (icons + theme_color + includeAssets)</name>
  <files>vigil-pwa/vite.config.ts</files>
  <read_first>
    - vigil-pwa/vite.config.ts (current state — 58 lines, VitePWA block at lines 18-56)
    - .planning/phases/87-vigil-app-icons/87-CONTEXT.md (D-02 theme_color, D-05 icon list)
  </read_first>
  <action>
    Edit `vigil-pwa/vite.config.ts` with these three diffs:

    **Change 1 — theme_color (line 25):**
    ```
    -        theme_color: '#0F6E56',
    +        theme_color: '#1D9E75',
    ```

    **Change 2 — includeAssets (line 20):**
    ```
    -      includeAssets: ['favicon.svg'],
    +      includeAssets: ['favicon.svg', 'favicon.ico', 'apple-touch-icon.png'],
    ```

    **Change 3 — icons array (lines 30-47) — replace with 5-entry form:**
    ```
            icons: [
              {
                src: 'pwa-192x192.png',
                sizes: '192x192',
                type: 'image/png',
                purpose: 'any',
              },
              {
                src: 'pwa-256x256.png',
                sizes: '256x256',
                type: 'image/png',
                purpose: 'any',
              },
              {
                src: 'pwa-384x384.png',
                sizes: '384x384',
                type: 'image/png',
                purpose: 'any',
              },
              {
                src: 'pwa-512x512.png',
                sizes: '512x512',
                type: 'image/png',
                purpose: 'any',
              },
              {
                src: 'pwa-512x512-maskable.png',
                sizes: '512x512',
                type: 'image/png',
                purpose: 'maskable',
              },
            ],
    ```

    Do NOT modify:
    - `background_color: '#0f172a'` (D-02 says KEEP)
    - workbox block (unrelated)
    - name, short_name, description, display, start_url, scope

    After editing, run a production build to force VitePWA to validate the manifest:
    ```
    cd vigil-pwa && npm run build
    ```
    Confirm no warnings about missing icons or invalid manifest.

    Extract built manifest and grep-verify:
    ```
    cat vigil-pwa/dist/manifest.webmanifest | python3 -m json.tool
    ```
  </action>
  <verify>
    <automated>
      grep -q "#1D9E75" vigil-pwa/vite.config.ts && \
      ! grep -q "#0F6E56" vigil-pwa/vite.config.ts && \
      grep -q "pwa-256x256.png" vigil-pwa/vite.config.ts && \
      grep -q "pwa-384x384.png" vigil-pwa/vite.config.ts && \
      grep -q "pwa-512x512-maskable.png" vigil-pwa/vite.config.ts && \
      grep -q "favicon.ico" vigil-pwa/vite.config.ts && \
      grep -q "apple-touch-icon.png" vigil-pwa/vite.config.ts && \
      (cd vigil-pwa && npm run build) && \
      test -s vigil-pwa/dist/manifest.webmanifest && \
      grep -q "pwa-256x256.png" vigil-pwa/dist/manifest.webmanifest && \
      grep -q "pwa-384x384.png" vigil-pwa/dist/manifest.webmanifest && \
      grep -q "pwa-512x512-maskable.png" vigil-pwa/dist/manifest.webmanifest && \
      grep -q "#1D9E75" vigil-pwa/dist/manifest.webmanifest
    </automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains all 5 icon srcs and theme_color #1D9E75
    - `npm run build` exits 0 with no icon/manifest warnings
    - `dist/manifest.webmanifest` contains all 5 icon entries and theme_color #1D9E75
    - `dist/pwa-256x256.png`, `dist/pwa-384x384.png`, `dist/pwa-512x512-maskable.png` all present
    - `dist/favicon.ico` present (via includeAssets)
  </acceptance_criteria>
  <done>Manifest wired; SC-1 and SC-2 closed at the config level. SC-3 requires human-verify in Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify installed PWA shows Vigil brand mark (SC-3)</name>
  <files>vigil-pwa/dist/manifest.webmanifest (build artifact — read only)</files>
  <action>Run `cd vigil-pwa && npm run preview` to serve the built PWA locally, then perform the manual verification steps in `<how-to-verify>` on desktop Chrome and iOS Safari. Wait for user to type "approved" or report issues.</action>
  <verify>
    <automated>test -s vigil-pwa/dist/manifest.webmanifest && grep -q "pwa-512x512-maskable" vigil-pwa/dist/manifest.webmanifest</automated>
  </verify>
  <what-built>
    PWA with full brand icon set: 5 manifest sizes + favicon.ico + favicon.svg + apple-touch-icon. Theme color updated to brand teal #1D9E75.
  </what-built>
  <how-to-verify>
    **Desktop (Chrome/Brave on macOS):**
    1. `cd vigil-pwa && npm run preview` (serves dist/ on a local port, e.g. http://localhost:4173)
    2. Open the URL in Chrome
    3. Browser tab should show Vigil mark favicon (not the old stopgap)
    4. Click the install icon in the address bar → "Install Vigil"
    5. Confirm the install prompt shows the Vigil teal diamond+V (not a generic placeholder)
    6. Once installed, check macOS Dock — app icon should be Vigil brand mark
    7. Right-click Dock icon → Options → confirm icon rendering at small sizes is crisp

    **iOS (Safari on iPhone/iPad):**
    1. Serve on LAN (or deploy to preview URL). Open in Safari.
    2. Share sheet → "Add to Home Screen"
    3. Preview tile shows Vigil brand mark (uses apple-touch-icon.png)
    4. Tap Add, then go to home screen — icon should be the Vigil mark
    5. Long-press icon → "Edit Home Screen" — still shows mark

    **Android (optional — only if Android device available):**
    1. Open in Chrome
    2. "Install app" prompt → preview uses maskable 512 variant
    3. Home screen icon survives circular/squircle mask without losing the mark (D-04 safe zone validation)

    If the mark appears cropped, blurry, or shows the old stopgap mark → reject and investigate.
  </how-to-verify>
  <acceptance_criteria>
    - Desktop Chrome install prompt shows Vigil mark
    - Installed Dock icon on macOS shows Vigil mark
    - iOS home screen icon shows Vigil mark (apple-touch-icon rendering)
    - Browser tab favicon shows Vigil mark
    - User types "approved" after at least desktop + iOS checks
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which surface still shows wrong icon</resume-signal>
  <done>SC-1, SC-2, SC-3 all human-confirmed. PWA track complete.</done>
</task>

</tasks>

<verification>
- `grep -q "#1D9E75" vigil-pwa/vite.config.ts` — brand teal theme_color
- `grep -q "pwa-256x256\|pwa-384x384\|maskable" vigil-pwa/vite.config.ts` — all new sizes declared
- `npm run build` in vigil-pwa/ succeeds with no manifest warnings
- `dist/manifest.webmanifest` contains 5 icon entries
- Human-verified installed PWA shows brand mark on at least desktop Dock + iOS home screen
</verification>

<success_criteria>
- SC-1: ✅ PWA manifest has 192, 256, 384, 512, 512-maskable icon entries pointing to real files
- SC-2: ✅ favicon.svg replaced with brand mark + favicon.ico present
- SC-3: ✅ Installed PWA shows Vigil diamond+V on home screen / Dock
</success_criteria>

<output>
After completion, create `.planning/phases/87-vigil-app-icons/87-02-SUMMARY.md` documenting:
- Final `icons[]` shape shipped
- Any build warnings encountered
- Which surfaces were human-verified (desktop, iOS, Android) and results
</output>
