---
phase: 06-evolved-daily-brief
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [Sources/DailyBrief/Services/AIService.swift, Sources/DailyBrief/DailyBrief.swift]
autonomous: true
---

<objective>
Make AI affirmations contextual by feeding recent captured thoughts into the Claude prompt, so the daily affirmation references what's actually on the user's mind.

Purpose: BRIEF-02 — affirmations that acknowledge real captured entries feel personal and relevant, not generic. "I see you captured 3 therapy questions today — that takes courage" hits differently than a random ADHD platitude.
Output: ClaudeAIProvider accepts thought context, system prompt incorporates entries, affirmation references recent captures.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evolved-daily-brief/06-01-SUMMARY.md

@Sources/DailyBrief/Services/AIService.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/JarvisCore/Models/Thought.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AIService to accept thought context for contextual affirmation</name>
  <files>Sources/DailyBrief/Services/AIService.swift</files>
  <action>
  1. Update the AIProvider protocol:
     - Change `generateAffirmation()` to `generateAffirmation(recentThoughts: [String])` where each string is a thought summary.
     - Keep the protocol Sendable.

  2. Update ClaudeAIProvider.generateAffirmation(recentThoughts:):
     - Cache key: Keep daily cache (`affirmation-YYYY-MM-DD.txt`). The affirmation is generated once per day. If thoughts change during the day, the cached version from the morning run is fine (the brief is a morning ritual).
     - Pass recentThoughts to callClaude.

  3. Update callClaude to accept `recentThoughts: [String]`:
     - Update the system prompt to include thought context:
       ```
       Generate a brief, warm ADHD-specific affirmation (2-3 sentences).
       Address themes like focus, time management, self-worth, or embracing how your brain works.
       Be encouraging but not patronizing. Vary the theme each day.

       The user recently captured these thoughts (reference 1-2 naturally if relevant, don't force it):
       {thought summaries, truncated to ~50 chars each, max 5}

       Return only the affirmation text.
       ```
     - If recentThoughts is empty, use the original prompt without the thought context section (fallback to generic affirmation).
     - Truncate each thought summary to 50 characters to keep the prompt small.
     - Include at most 5 thought summaries to avoid excessive token usage.
     - Keep max_tokens at 200 — contextual affirmations should still be concise (2-3 sentences).

  4. Keep the fallback text unchanged: "You've got this. Your brain works differently, and that's your superpower."
  </action>
  <verify>swift build succeeds. AIService compiles with updated protocol signature.</verify>
  <done>AIProvider protocol accepts thought context. ClaudeAIProvider incorporates recent thoughts into affirmation prompt. Empty thoughts gracefully fall back to generic prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Wire thought context into affirmation call in Generate command</name>
  <files>Sources/DailyBrief/DailyBrief.swift</files>
  <action>
  1. In Generate.run(), after the thought arrays are fetched (from Plan 06-01), build the thought summary list for the affirmation:
     - Combine all three thought arrays (unprocessed + task + recent) into a single list.
     - Map to content strings: `thought.content` truncated to 50 characters.
     - Cap at 10 entries total.

  2. Update the affirmation fetch call:
     - Change from: `try await aiProvider.generateAffirmation()`
     - Change to: `try await aiProvider.generateAffirmation(recentThoughts: thoughtSummaries)`
     - The thoughtSummaries variable should be computed BEFORE the async let block if possible, or restructure the async let to use the result. Since thought fetching is also async let, the simplest approach: fetch thoughts first (they're fast, local DB), then use results in affirmation call.
     - Restructure: Move thought fetching BEFORE the main async let block (ThoughtStore queries are fast local DB reads, not network calls). Then pass summaries into the affirmation async let.

  3. Update the dry-run printSummary to note whether affirmation is contextual:
     - After printing affirmation text, add "(contextual)" if thought summaries were non-empty, "(generic)" if empty.
  </action>
  <verify>swift build succeeds. `swift run DailyBrief --dry-run` shows affirmation with contextual/generic indicator. If thoughts exist in DB and API key is valid, affirmation references captured entries.</verify>
  <done>Generate command passes recent thought summaries to affirmation generation. Affirmation is contextual when thoughts exist, generic otherwise.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors or warnings
- [ ] `swift run DailyBrief --dry-run` shows contextual affirmation indicator
- [ ] With no thoughts in DB, affirmation falls back to generic prompt (no crash)
- [ ] With thoughts in DB, affirmation prompt includes thought summaries
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Affirmation references recent captured entries when available
- Graceful fallback to generic affirmation when no thoughts exist
- BRIEF-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-evolved-daily-brief/06-02-SUMMARY.md`
</output>
