---
status: partial
phase: 116.1-sports-route-pwa-error-class-differentiation
source: [116.1-VERIFICATION.md]
started: 2026-04-30T02:00:00Z
updated: 2026-04-30T02:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. PWA countdown UI + disabled Retry button while counting
expected: Open PWA Settings, enable MLB, intercept `/v1/sports/teams/mlb` request via browser DevTools to return a 502 with body `{"error":"Upstream sports provider unavailable","retryAfter":30}`. Red error block appears with text "Sports data temporarily unavailable." and a live countdown "Try again in 0:29" (or similar). Retry button is disabled while countdown > 0 and re-enables at 0.
result: [pending]

### 2. Local-env-gap reproduction (missing BALLDONTLIE_API_KEY)
expected: Remove `BALLDONTLIE_API_KEY` from local `.env`. Trigger a brief generation OR open Sports Settings and enable a league. Server returns 502 with body `{"error":"Upstream sports provider unavailable"}` (NOT a generic 500 or raw BDL error string). PWA shows "Sports data temporarily unavailable." copy. No "balldontlie" substring appears anywhere in server response body or PWA UI.
result: [pending]

### 3. Brief PDF per-league placeholder rendering
expected: With at least one sports league configured but BDL unreachable (e.g. block `api.balldontlie.io` in `/etc/hosts` OR set `BALLDONTLIE_API_KEY` to an invalid value), generate a brief. The PDF sports section renders placeholder text "NFL data temporarily unavailable." (per-league failure) OR "Sports data temporarily unavailable. Try again on tomorrow's brief." (all-failed). No league is silently omitted — the user sees what failed.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
