---
phase: 134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05
plan: 03
type: execute
wave: 3
depends_on:
  - 134-01
  - 134-02
files_modified:
  - vigil-linux-hooks/redact.sh
  - vigil-linux-hooks/redaction-patterns.json
  - vigil-linux-hooks/vigil-agent-bridge.sh
  - vigil-linux-hooks/__tests__/redaction-corpus.test.ts
autonomous: true
requirements:
  - AGENT-LINUX-03
threat_model:
  - T-134-R1
  - T-134-R2
  - T-134-I3

must_haves:
  truths:
    - "`redaction-patterns.json` contains exactly the 6 WATCH-ENRICH-03 patterns (api[_-]?key, bearer, password, vk_, ey[A-Za-z0-9_-]{10,}, [A-Za-z0-9+/]{40,}={0,2}) with `max_length=80`"
    - "JWT pattern threshold is `{10,}` (NOT `{20,}` as in CONTEXT D-R1) per RESEARCH Pitfall 4 — a JWT-shaped substring starting at offset 70 of an 80-char input still matches"
    - "Running `redact_prompt` on a prompt that matches ANY pattern returns the literal string `[redacted: contains sensitive pattern]`"
    - "Running `redact_prompt` on a clean prompt returns the truncated content (≤80 chars), never the literal `[redacted: …]`"
    - "Running `vigil-agent-bridge.sh --event=UserPromptSubmit` with a clean prompt emits a body where `message` equals the truncated prompt (≤80 chars); with a secret-shaped prompt, `message` equals `[redacted: contains sensitive pattern]`"
    - "Truncate-FIRST then regex-match: a 4KB clean prompt that happens to contain `bearer` at offset 2000 is NOT redacted (truncation discards the offset-2000 byte first), matching CONTEXT D-R2 ordering"
  artifacts:
    - path: "vigil-linux-hooks/redaction-patterns.json"
      provides: "Canonical denylist JSON source-of-truth"
      contains: "ey[A-Za-z0-9_-]"
    - path: "vigil-linux-hooks/redact.sh"
      provides: "Sourceable bash function redact_prompt that truncates to <=80 chars then regex-matches against patterns from redaction-patterns.json"
      contains: "redact_prompt"
    - path: "vigil-linux-hooks/vigil-agent-bridge.sh"
      provides: "UserPromptSubmit case branch wired to call redact_prompt then emit_event heartbeat"
      contains: "redact_prompt"
    - path: "vigil-linux-hooks/__tests__/redaction-corpus.test.ts"
      provides: "Table-driven node:test covering 15 secret-shaped + 10 clean fixtures + the JWT-at-offset-70 edge case"
  key_links:
    - from: "vigil-linux-hooks/redact.sh"
      to: "vigil-linux-hooks/redaction-patterns.json"
      via: "load_patterns helper reading via node -e + fs.readFileSync"
      pattern: "redaction-patterns"
    - from: "vigil-linux-hooks/vigil-agent-bridge.sh UserPromptSubmit branch"
      to: "redact.sh redact_prompt + emit_event heartbeat"
      via: 'REDACTED=$(redact_prompt "$PROMPT"); emit_event "heartbeat" "$REDACTED"'
      pattern: "redact_prompt"
---

<objective>
Land the redaction subsystem: (a) the canonical `redaction-patterns.json` source-of-truth, (b) the sourceable `redact.sh` bash function implementing truncate-<=80-then-binary-redact, (c) the wiring in `vigil-agent-bridge.sh`'s `UserPromptSubmit` branch, and (d) the table-driven test corpus covering 15 secret-shaped + 10 clean prompts plus the RESEARCH-Pitfall-4 JWT-at-offset-70 edge case.

Purpose: Deliver AGENT-LINUX-03 — the privacy redaction layer that prevents secret material from reaching the wire. Phase 134 owns the canonical JSON file (current ROADMAP order puts 134 before 133 vigil-watch); Phase 133 will consume the same file cross-repo. JWT threshold MUST be `{10,}` (not the `{20,}` in CONTEXT D-R1) per RESEARCH Pitfall 4 — researcher's note is authoritative.

Output: A working `redact_prompt` that emits `[redacted: contains sensitive pattern]` on ANY match and the truncated content otherwise. UserPromptSubmit hook events go out the wire with redacted-or-truncated `message`. 25+ table-driven test cases pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-VALIDATION.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-01-SUMMARY.md
@vigil-linux-hooks/vigil-agent-bridge.sh

<interfaces>
emit_event signature (from Plan 01):
  emit_event <EVENT_NAME> [<MESSAGE>]
  When MESSAGE is non-empty, the JSON body includes "message" : <MESSAGE> (after JSON.stringify escapes it).

Canonical denylist (from CONTEXT D-R1 + RESEARCH Pitfall 4):
  patterns = [
    "api[_-]?key",
    "bearer",
    "password",
    "vk_",
    "ey[A-Za-z0-9_-]{10,}",
    "[A-Za-z0-9+/]{40,}={0,2}",
  ]
  max_length = 80

Binary redaction posture (D-R3):
  Any match -> entire message = "[redacted: contains sensitive pattern]"
  No match  -> return truncated content (<=max_length chars)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create redaction-patterns.json and redact.sh</name>
  <files>
    vigil-linux-hooks/redaction-patterns.json,
    vigil-linux-hooks/redact.sh
  </files>
  <read_first>
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 2 (redact.sh structure at lines 115-159) AND Pattern Assignment 3 (redaction-patterns.json content at lines 162-186)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md D-R1 (denylist source-of-truth + max_length=80), D-R2 (truncate-then-redact order), D-R3 (binary redaction)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md Pitfall 4 (JWT threshold MUST be {10,} not {20,}) AND Pitfall 6 (bash [[ =~ ]] no-match returns 1; wrap in if/then/fi)
  </read_first>
  <action>
Create `vigil-linux-hooks/redaction-patterns.json` containing:
  - Top-level key `patterns`: array of 6 strings in this exact order: `api[_-]?key`, `bearer`, `password`, `vk_`, `ey[A-Za-z0-9_-]{10,}`, `[A-Za-z0-9+/]{40,}={0,2}`.
  - Top-level key `max_length`: integer 80.
  - JWT pattern MUST use `{10,}` not `{20,}` per RESEARCH Pitfall 4.
  - File parses cleanly with `JSON.parse`.

Create `vigil-linux-hooks/redact.sh` (sourceable bash, not standalone executable). Composition per PATTERNS Pattern Assignment 2 lines 115-159:
  - Top-of-file comment containing the literal string `redaction-patterns.json` (drift detector Rail 2 pin).
  - Define `_VIGIL_PATTERNS_FILE="$(dirname "${BASH_SOURCE[0]}")/redaction-patterns.json"`.
  - Define `load_patterns()` using `node -e` with `process.env.PFILE` to read the JSON, extract `patterns`, write them joined by ASCII SOH (``). Silent on parse failure (empty string output).
  - Define `redact_prompt()` taking one positional arg: truncate FIRST to `${VIGIL_MAX_PROMPT_LEN:-80}` chars via bash slice `${input:0:$max_len}`. Then split loaded patterns on SOH (`local IFS=$'\x01'`). Loop each pattern with `if [[ "$truncated" =~ $pat ]]; then printf '%s' "[redacted: contains sensitive pattern]"; return 0; fi`. After the loop, `printf '%s' "$truncated"`.
  - Do NOT use `set -euo pipefail` (Pitfall 6).
  - The `if`-gated regex form is load-bearing — no-match returns 1 which would be fatal under set -e.

Do NOT inline the bash function bodies; PATTERNS.md Pattern Assignment 2 provides the canonical implementation to copy.
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('vigil-linux-hooks/redaction-patterns.json','utf8'));process.exit((j.patterns.length===6 && j.max_length===80 && j.patterns.includes('ey[A-Za-z0-9_-]{10,}') && !j.patterns.includes('ey[A-Za-z0-9_-]{20,}'))?0:1)" && bash -n vigil-linux-hooks/redact.sh</automated>
  </verify>
  <done>
    - `redaction-patterns.json` exists, parses as JSON, contains exactly 6 patterns and max_length=80.
    - The JWT pattern is `ey[A-Za-z0-9_-]{10,}` (NOT `{20,}`).
    - `redact.sh` syntax-checks clean via `bash -n`.
    - `redact.sh` source contains the literal string `redaction-patterns.json` (Rail 2 pin).
    - Sourcing redact.sh and calling `redact_prompt "my password is hunter2"` returns `[redacted: contains sensitive pattern]`.
    - Sourcing redact.sh and calling `redact_prompt "help me refactor this function"` returns the input unchanged (length <=80, no match).
  </done>
  <acceptance_criteria>
    - Source assertion: `node -e "const j=JSON.parse(require('fs').readFileSync('vigil-linux-hooks/redaction-patterns.json','utf8'));console.log(j.patterns.length)"` outputs `6`.
    - Source assertion: `grep -c 'ey\[A-Za-z0-9_-\]{10,}' vigil-linux-hooks/redaction-patterns.json` returns 1.
    - Source assertion: `grep -c 'ey\[A-Za-z0-9_-\]{20,}' vigil-linux-hooks/redaction-patterns.json` returns 0 (anti-regression for Pitfall 4).
    - Source assertion: `grep -c 'redaction-patterns\.json' vigil-linux-hooks/redact.sh` returns >= 1 (Rail 2 pin).
    - Source assertion: `grep -c 'redact_prompt' vigil-linux-hooks/redact.sh` returns >= 1.
    - Behavior assertion: `bash -c 'source vigil-linux-hooks/redact.sh; redact_prompt "my password is hunter2"'` outputs exactly `[redacted: contains sensitive pattern]`.
    - Behavior assertion: `bash -c 'source vigil-linux-hooks/redact.sh; redact_prompt "Bearer eyJhbGciOiJIUzI1NiJ9.abc.def"'` outputs `[redacted: contains sensitive pattern]`.
    - Behavior assertion: `bash -c 'source vigil-linux-hooks/redact.sh; redact_prompt "help me refactor this function"'` outputs `help me refactor this function` (no redaction).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire UserPromptSubmit branch in vigil-agent-bridge.sh + add redaction-corpus test</name>
  <files>
    vigil-linux-hooks/vigil-agent-bridge.sh,
    vigil-linux-hooks/__tests__/redaction-corpus.test.ts
  </files>
  <read_first>
    - vigil-linux-hooks/vigil-agent-bridge.sh (locate the `case "$EVENT_TYPE" in` block; find the UserPromptSubmit) stub)
    - vigil-linux-hooks/redact.sh (the sourceable function landed in Task 1)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 1 (adaptation note at line 110: "Source `redact.sh` near top: `source \"$(dirname \"$0\")/redact.sh\"`") AND Pattern Assignment 8 (redaction-corpus.test.ts table-driven structure at lines 399-446)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md D-I3 (UserPromptSubmit fires once per submitted prompt), D-R5 (synthetic corpus shape — 15 secret + 10 clean fixtures)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md Pitfall 4 (JWT-at-offset-70 edge case MUST be in the corpus)
    - vigil-core/src/__tests__/audio-log-redaction.test.ts:212-303 (table-driven describe/it pattern analog)
  </read_first>
  <action>
**Sub-step A — Modify `vigil-linux-hooks/vigil-agent-bridge.sh`:**

(1) Near the top of the file (after the auth-gate, before the case block), add `source "$(dirname "$0")/redact.sh"` so `redact_prompt` is available in scope. Place this AFTER the `[ -z "${VIGIL_API_KEY:-}" ] && exit 0` line (so the source doesn't run on the silent-no-op path) but BEFORE the case dispatch.

(2) Replace the `UserPromptSubmit)` stub branch with: build a local variable `REDACTED="$(redact_prompt "$PROMPT")"`, then call `emit_event "heartbeat" "$REDACTED"`. If `$PROMPT` is empty (envelope had no prompt field), set `REDACTED=""` and call `emit_event "heartbeat"` without the message arg (the emit_event helper drops the message key when arg is empty per Plan 01 spec).

(3) Do NOT touch the SessionStart) or Stop) branches landed by Plan 02.

(4) Do NOT modify the final `exit 0`, the auth-gate, or the STDIN parser.

**Sub-step B — Create `vigil-linux-hooks/__tests__/redaction-corpus.test.ts`:**

Imports per PATTERNS Pattern G + Pattern Assignment 8: `describe, it, before` from `node:test`, `assert` from `node:assert/strict`, `execSync` from `node:child_process`, `path`, `url`. Compute `ROOT = path.join(here, "..")` in a `before()` block.

Define two table arrays:
  - `CASES_REDACT: Array<[string, string]>` with 15 entries — each entry is `[prompt, label]`. Cover at minimum: `api_key` lowercase, `API_KEY` uppercase, `API-KEY` hyphen variant, `apikey` no-separator, `bearer` lowercase, `Bearer` mixed-case (regex is case-sensitive; document if `bearer` only fires lowercase — adjust corpus to lowercase forms only or extend the pattern; CONTEXT D-R1 has lowercase `bearer`), `password is hunter2`, `vk_xxxxxxxxxxxx`, `eyJhbGciOiJIUzI1NiJ9.abc.def` (JWT), 50-char base64 blob, 40-char base64 blob with trailing `==`, and the RESEARCH-Pitfall-4 edge case: a 70-char prefix of clean text followed by `eyJabc12345xyz` (so the JWT lands at offset 70 within the 80-char truncation window).
  - `CASES_CLEAN: Array<[string, string]>` with 10 entries: plain dev prompts like `help me refactor this function`, `what is the weather in Detroit`, `show me how Stripe webhooks work`, `explain rust borrow checker`, etc. None must match any of the 6 patterns.

For each `CASES_REDACT` entry, write `it("redacts: ${label}", () => { ... })` that runs `bash -c "source ${ROOT}/redact.sh; redact_prompt \"$1\"" -- "${prompt}"` via execSync and asserts `out.trim() === "[redacted: contains sensitive pattern]"`. Pass prompt via execSync's `input`/positional-arg path, NOT via shell heredoc interpolation (Anti-pattern from RESEARCH §Code Examples — shell-quote injection).

For each `CASES_CLEAN` entry, write `it("passes clean: ${label}", () => { ... })` that asserts the output `!== "[redacted: contains sensitive pattern]"` AND `output.length <= 80`.

Add ONE explicit edge-case `it("Pitfall 4: JWT at offset 70 of 80-char truncation still matches with {10,} threshold")` that constructs a prompt of exactly 70 clean chars + the substring `eyJabc12345` (11 chars total after the `ey` so 9 base64 chars — borderline matches `{10,}`? Recompute: pattern is `ey[A-Za-z0-9_-]{10,}` meaning `ey` + at least 10 trailing chars; so use `eyJabc1234567890` = `ey` + 14 chars). Run redact_prompt against this 80-char input; assert the output is `[redacted: contains sensitive pattern]`. This test FAILS if a future commit reverts the threshold to `{20,}`.

Add ONE explicit edge-case `it("D-R2: truncate-first ordering — a 4KB clean prompt with bearer at offset 2000 is NOT redacted")` that constructs a prompt = 70 clean chars + `bearer at offset 2000` placed at offset 2000 within a 4KB blob (10 chars of `a` * 200 then the secret then more padding). Assert the output is the truncated 80-char prefix (the clean part), NOT the redacted token. This pins the truncate-FIRST contract.
  </action>
  <verify>
    <automated>bash -n vigil-linux-hooks/vigil-agent-bridge.sh && cd vigil-linux-hooks && npm test 2>&1 | tee /tmp/134-03-task2.log; grep -E "ok |not ok" /tmp/134-03-task2.log | wc -l</automated>
  </verify>
  <done>
    - `vigil-agent-bridge.sh` sources `redact.sh` (grep `source.*redact.sh` returns >= 1).
    - The UserPromptSubmit) branch calls `redact_prompt` and `emit_event "heartbeat"`.
    - `redaction-corpus.test.ts` exists with >= 25 `it` blocks (15 redact + 10 clean) + 2 explicit edge-case it blocks (Pitfall 4 + D-R2 ordering) — 27 total.
    - `cd vigil-linux-hooks && npm test` exits 0 with all tests passing.
    - Behavior probe: `VIGIL_API_KEY=vk_test VIGIL_AGENT_BRIDGE_EMIT_ONLY=1 bash vigil-linux-hooks/vigil-agent-bridge.sh --event=UserPromptSubmit` against a fixture containing a JWT-shaped prompt emits a body where `message` is `[redacted: contains sensitive pattern]`.
    - Behavior probe: same invocation with a clean prompt fixture emits a body where `message` equals the truncated clean prompt.
  </done>
  <acceptance_criteria>
    - Source assertion: `grep -c 'source.*redact\.sh' vigil-linux-hooks/vigil-agent-bridge.sh` returns >= 1.
    - Source assertion: `grep -c 'redact_prompt[[:space:]]*"\$PROMPT"' vigil-linux-hooks/vigil-agent-bridge.sh` returns >= 1.
    - Source assertion: `grep -c 'emit_event[[:space:]]\+"heartbeat"[[:space:]]\+"\$REDACTED"' vigil-linux-hooks/vigil-agent-bridge.sh` returns >= 1 (or the equivalent emit_event call wired with REDACTED).
    - Source assertion: `grep -c "Pitfall 4" vigil-linux-hooks/__tests__/redaction-corpus.test.ts` returns >= 1 (named edge case present).
    - Source assertion: `grep -c "truncate-first" vigil-linux-hooks/__tests__/redaction-corpus.test.ts` returns >= 1 (or "D-R2" — name of the ordering invariant).
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/redaction-corpus.test.ts"` exits 0 with >= 27 passing `it` blocks reported.
    - Test command: `cd vigil-linux-hooks && npm test` exits 0 (full suite green — body-builder + fail-safe + redaction-corpus).
    - Behavior assertion: piping `printf '{"session_id":"abc","cwd":"/home/morrillboss/dev/dailybrief","prompt":"Bearer eyJabc12345xyz"}'` to `VIGIL_API_KEY=vk_test VIGIL_AGENT_BRIDGE_EMIT_ONLY=1 bash vigil-linux-hooks/vigil-agent-bridge.sh --event=UserPromptSubmit` produces a body where `JSON.parse(stdout).message === "[redacted: contains sensitive pattern]"`.
    - Behavior assertion: piping `printf '{"session_id":"abc","cwd":"/home/morrillboss/dev/dailybrief","prompt":"help me refactor this function"}'` to the same invocation produces a body where `JSON.parse(stdout).message === "help me refactor this function"` (no redaction).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator prompt -> hook redactor | The STDIN envelope's `prompt` field is operator-typed; may contain secret material the operator does NOT want shipped to the wire. |
| redactor output -> emit_event body | Whatever survives redaction becomes the public `message` field on the HUD. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-R1 | Information Disclosure (novel pattern bypass) | `redact_prompt` denylist | mitigate | 6-pattern denylist covers the WATCH-ENRICH-03 baseline; truncate-FIRST to <=80 chars bounds any unmatched residual leak to 80 bytes. Drift detector (Plan 04) pins parity across JSON + bash + future Swift. Operator can extend `redaction-patterns.json` without code changes; quarterly review of new credential formats noted in README (Plan 04). |
| T-134-R2 | Information Disclosure (truncation elides JWT) | `redact_prompt` ordering | mitigate | JWT threshold lowered from `{20,}` to `{10,}` per RESEARCH Pitfall 4 — even a JWT starting at offset 70 of an 80-char window still matches (`ey` + 8 chars survives truncation). Corpus test pins the offset-70 edge case as a regression guard. |
| T-134-I3 | Tampering (shell injection via prompt content) | `redact_prompt` invocation in `vigil-agent-bridge.sh` | mitigate | `redact_prompt "$PROMPT"` passes the prompt as a single quoted positional arg to a bash function (no eval, no heredoc, no `printf`-into-shell). The function consumes it as a `local input="$1"` — never re-interpreted. Body builder (Plan 01) passes `MESSAGE` via `process.env` to `node -e` for `JSON.stringify` — defeats injection at the JSON layer too. |
</threat_model>

<verification>
```
bash -n vigil-linux-hooks/vigil-agent-bridge.sh
bash -n vigil-linux-hooks/redact.sh
cd vigil-linux-hooks && npm test
```

Expected end-state: all tests pass (body-builder 5 + fail-safe 5 + redaction-corpus 27 = 37 total) in <2s.
</verification>

<success_criteria>
- `redaction-patterns.json` exists with the 6 canonical patterns and JWT threshold `{10,}`.
- `redact.sh` is sourceable and exports `redact_prompt` with truncate-first-then-binary-match semantics.
- `vigil-agent-bridge.sh` sources `redact.sh` and the UserPromptSubmit branch wires `redact_prompt` -> `emit_event "heartbeat"`.
- `redaction-corpus.test.ts` covers 15 secret-shaped + 10 clean fixtures + 2 named edge cases (Pitfall 4 JWT-at-offset-70 + D-R2 truncate-first ordering).
- All Plan 01 + Plan 02 invariants hold (fail-safe, SessionStart, Stop body shapes unchanged).
</success_criteria>

<output>
Create `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-03-SUMMARY.md` when done — capture: the final corpus size (target 27+), any patterns flagged during testing that needed adjustment, and a sample `[redacted: …]` body emitted by the wired UserPromptSubmit branch.
</output>
