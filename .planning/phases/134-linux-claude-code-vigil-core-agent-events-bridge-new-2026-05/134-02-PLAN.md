---
phase: 134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05
plan: 02
type: execute
wave: 2
depends_on:
  - 134-01
files_modified:
  - vigil-linux-hooks/vigil-agent-bridge.sh
  - vigil-linux-hooks/__tests__/body-builder.test.ts
autonomous: true
requirements:
  - AGENT-LINUX-01
  - AGENT-LINUX-02
threat_model:
  - T-134-I3

must_haves:
  truths:
    - "Running `vigil-agent-bridge.sh --event=SessionStart` against the probe envelope and with `VIGIL_AGENT_BRIDGE_EMIT_ONLY=1` emits a JSON body with `event=\"heartbeat\"`, `message=\"session started\"`, `label=\"dailybrief\"` (basename of cwd), and a non-empty `session_id` matching the envelope's session_id field"
    - "Running `vigil-agent-bridge.sh --event=Stop` emits a JSON body with `event=\"task_complete\"` and `message=\"turn complete\"`"
    - "Both event paths produce a body containing exactly the 7 Phase 121 KNOWN_FIELDS allowed keys: session_id, event, timestamp, label, host, client_event_id, message"
    - "Neither event path mutates the auth-gate (missing VIGIL_API_KEY still exits 0 silently for both events)"
  artifacts:
    - path: "vigil-linux-hooks/vigil-agent-bridge.sh"
      provides: "Wired SessionStart + Stop case branches calling emit_event with the correct event-type and static message strings"
      contains: '"task_complete"'
    - path: "vigil-linux-hooks/__tests__/body-builder.test.ts"
      provides: "Extended test coverage for SessionStart heartbeat shape AND Stop task_complete shape"
      contains: "task_complete"
  key_links:
    - from: "vigil-agent-bridge.sh case SessionStart"
      to: "emit_event helper (Plan 01)"
      via: 'emit_event SessionStart "session started"'
      pattern: 'emit_event[[:space:]]+SessionStart'
    - from: "vigil-agent-bridge.sh case Stop"
      to: "emit_event helper (Plan 01)"
      via: 'emit_event Stop "turn complete"'
      pattern: 'emit_event[[:space:]]+Stop'
---

<objective>
Wire the `SessionStart` and `Stop` dispatch branches in `vigil-agent-bridge.sh` so they invoke `emit_event` with the correct Phase 121 `event` literal (`heartbeat` / `task_complete`) and static message strings (`session started` / `turn complete`). Extend `body-builder.test.ts` to cover both event paths against the probe envelope.

Purpose: Deliver AGENT-LINUX-01 (SessionStart → heartbeat) and AGENT-LINUX-02 (Stop → task_complete). These two events form the session bookends visible to the G2 HUD operator.

Output: Two of three case branches in the runtime hook fully wired; body-builder.test.ts has 5 passing cases (the original 3 from Plan 01 + 2 added by this plan); hook continues to satisfy the Plan 01 fail-safe contract under unmodified envelopes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-VALIDATION.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-01-SUMMARY.md
@vigil-linux-hooks/vigil-agent-bridge.sh

<interfaces>
From vigil-core/src/routes/agent-events.ts:23-29 — VALID_EVENTS enum:
  ["needs_input", "task_complete", "task_failed", "milestone", "heartbeat"]
  This plan emits: SessionStart → "heartbeat", Stop → "task_complete".

Static messages (per RESEARCH §Open Question 1 recommendation + ROADMAP intent):
  SessionStart message = "session started"
  Stop message = "turn complete"
  (UserPromptSubmit message is set in Plan 03 from redacted truncated prompt)

emit_event signature (from Plan 01):
  emit_event <EVENT_NAME> [<MESSAGE>]
  Reads SESSION_ID, CWD, VIGIL_HOST_OVERRIDE, VIGIL_API_KEY, VIGIL_API_URL from environment.
  Builds 7-key body (or 6-key if MESSAGE empty), fire-and-forget POSTs.
  Honors VIGIL_AGENT_BRIDGE_EMIT_ONLY=1 → prints body to stdout, returns 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SessionStart and Stop case branches in vigil-agent-bridge.sh</name>
  <files>
    vigil-linux-hooks/vigil-agent-bridge.sh
  </files>
  <read_first>
    - vigil-linux-hooks/vigil-agent-bridge.sh (the scaffold from Plan 01 — locate the `case "$EVENT_TYPE" in` block)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md D-I2 (`Stop` hook fires per assistant turn → maps 1:1 to `task_complete`), D-P2 (static strings for SessionStart/Stop per ROADMAP)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md §Open Question 1 (SessionStart `message = "session started"`, Stop `message = "turn complete"`)
    - vigil-core/src/routes/agent-events.ts:23-29 (VALID_EVENTS enum — `heartbeat` and `task_complete` are the only two Phase 134 uses)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 1 (adaptation notes at lines 104-112 — `[ -n "$PROMPT" ]` guard for UserPromptSubmit; static strings for SessionStart/Stop)
  </read_first>
  <action>
Modify `vigil-linux-hooks/vigil-agent-bridge.sh` to replace the Plan-01 stub bodies in two of the three case branches.

Locate the `case "$EVENT_TYPE" in` block. Replace the `SessionStart)` branch body with a single call: `emit_event "heartbeat" "session started"` followed by `;;`. This sets `event: "heartbeat"` and `message: "session started"` in the body composed by emit_event. The `label` field is automatically derived from `basename "$CWD"` inside emit_event (Plan 01), and the STDIN envelope's `cwd` was parsed at the top of the script — no per-branch cwd handling needed.

Replace the `Stop)` branch body with a single call: `emit_event "task_complete" "turn complete"` followed by `;;`. The Stop hook fires once per finished assistant turn (D-I2); each invocation produces one task_complete event.

Leave the `UserPromptSubmit)` branch as the Plan-01 stub — Plan 03 wires that branch.

Do NOT modify the auth-gate (`[ -z "${VIGIL_API_KEY:-}" ] && exit 0`), the STDIN parser, the `emit_event` function body, the dispatch-fallback `*)`, or the final `exit 0`. Those are Plan 01's responsibility.

Verification convention: the executor should run `bash -n vigil-agent-bridge.sh` after editing to confirm no syntax regression.
  </action>
  <verify>
    <automated>bash -n vigil-linux-hooks/vigil-agent-bridge.sh && grep -E 'emit_event[[:space:]]+"heartbeat"[[:space:]]+"session started"|emit_event[[:space:]]+"task_complete"[[:space:]]+"turn complete"' vigil-linux-hooks/vigil-agent-bridge.sh | wc -l</automated>
  </verify>
  <done>
    - Hook source contains the literal `emit_event "heartbeat" "session started"` exactly once.
    - Hook source contains the literal `emit_event "task_complete" "turn complete"` exactly once.
    - `bash -n vigil-linux-hooks/vigil-agent-bridge.sh` exits 0.
    - The `UserPromptSubmit)` branch body is unchanged from Plan 01 (Plan 03 will wire it).
    - All Plan 01 invariants still hold: missing VIGIL_API_KEY → exit 0; unreachable URL → exit 0 within 3s; final `exit 0` line preserved.
  </done>
  <acceptance_criteria>
    - Source assertion: `grep -c 'emit_event[[:space:]]\+"heartbeat"[[:space:]]\+"session started"' vigil-linux-hooks/vigil-agent-bridge.sh` returns exactly 1.
    - Source assertion: `grep -c 'emit_event[[:space:]]\+"task_complete"[[:space:]]\+"turn complete"' vigil-linux-hooks/vigil-agent-bridge.sh` returns exactly 1.
    - Behavior assertion: `VIGIL_API_KEY=vk_test VIGIL_AGENT_BRIDGE_EMIT_ONLY=1 bash vigil-linux-hooks/vigil-agent-bridge.sh --event=SessionStart < vigil-linux-hooks/__tests__/fixtures/probe-envelope.json` writes a JSON body containing `"event":"heartbeat"`, `"message":"session started"`, and `"label":"dailybrief"` (basename of the fixture cwd).
    - Behavior assertion: same invocation with `--event=Stop` writes a body with `"event":"task_complete"` and `"message":"turn complete"`.
    - Fail-safe regression: `VIGIL_API_KEY="" bash vigil-linux-hooks/vigil-agent-bridge.sh --event=Stop < vigil-linux-hooks/__tests__/fixtures/probe-envelope.json 2>&1; echo "exit=$?"` still produces exactly `exit=0` (no terminal output).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend body-builder.test.ts with SessionStart and Stop shape assertions</name>
  <files>
    vigil-linux-hooks/__tests__/body-builder.test.ts
  </files>
  <read_first>
    - vigil-linux-hooks/__tests__/body-builder.test.ts (Plan 01's 3 existing `it` blocks — model the new tests on their `execSync` capture pattern)
    - vigil-linux-hooks/__tests__/fixtures/probe-envelope.json (the probe envelope shape the tests will pipe to the hook)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 7 (body-builder.test.ts structure at lines 338-396, particularly the "no unknown fields" loop at lines 380-384)
    - vigil-core/src/routes/agent-events.ts:34-43 (KNOWN_FIELDS Set — assertion target)
    - vigil-core/src/routes/agent-events.ts:23-29 (VALID_EVENTS — `heartbeat` and `task_complete` validity)
  </read_first>
  <action>
Extend `vigil-linux-hooks/__tests__/body-builder.test.ts` with TWO new `it` blocks inside the existing `describe("AGENT-LINUX-01/02 …", …)`.

Add `it("SessionStart probe — produces heartbeat body with static message")`:
  - Spawn `bash vigil-agent-bridge.sh --event=SessionStart` with `env: { VIGIL_API_KEY: "vk_test", VIGIL_AGENT_BRIDGE_EMIT_ONLY: "1" }` and pipe the SessionStart slice of `__tests__/fixtures/probe-envelope.json` as STDIN.
  - Capture stdout and `JSON.parse` it into `body`.
  - Assert: `body.event === "heartbeat"`, `body.message === "session started"`, `body.session_id` equals the fixture's session_id, `body.label === "dailybrief"` (basename of the fixture cwd `/home/morrillboss/dev/dailybrief`).
  - Assert KNOWN_FIELDS membership: for each `k in Object.keys(body)`, assert `["session_id","event","message","timestamp","label","host","exit_code","client_event_id"].includes(k)`.
  - Assert: `body` does NOT contain key `cwd` (regression guard for D-P3 — unknown field would 400-reject).

Add `it("Stop probe — produces task_complete body with static message")`:
  - Same spawn pattern with `--event=Stop` and the Stop slice of the fixture.
  - Assert: `body.event === "task_complete"`, `body.message === "turn complete"`, `body.session_id` is non-empty, `body.client_event_id` matches `/^[0-9a-f-]{36}$/`.
  - Assert the KNOWN_FIELDS membership loop (same as above).

Do NOT modify the original 3 `it` blocks from Plan 01 — they remain load-bearing for the AGENT-LINUX-04 emit-only escape hatch.

The fixture STDIN-slice extraction: read the JSON, extract the per-event sub-object, and pipe `JSON.stringify(slice)` to the spawned bash process via `input: ...` in the `execSync` options OR via `printf '%s' <slice>` shell composition.
  </action>
  <verify>
    <automated>cd vigil-linux-hooks && npm test 2>&1 | tee /tmp/134-02-task2.log; grep -E "ok|not ok" /tmp/134-02-task2.log | wc -l</automated>
  </verify>
  <done>
    - `body-builder.test.ts` contains 5 total `it` blocks (3 from Plan 01 + 2 added by this task).
    - The two new `it` blocks each assert the KNOWN_FIELDS membership loop AND assert `body` does NOT contain key `"cwd"`.
    - The SessionStart test asserts `body.label === "dailybrief"` (derived from the fixture's cwd).
    - The Stop test asserts `body.event === "task_complete"` and `body.message === "turn complete"`.
    - `cd vigil-linux-hooks && npm test` exits 0 with all 5 body-builder tests passing plus the 5 fail-safe tests from Plan 01 (10/10 total under these two test files).
  </done>
  <acceptance_criteria>
    - Source assertion: `grep -c 'SessionStart probe' vigil-linux-hooks/__tests__/body-builder.test.ts` returns >= 1.
    - Source assertion: `grep -c 'Stop probe' vigil-linux-hooks/__tests__/body-builder.test.ts` returns >= 1.
    - Source assertion: `grep -c '"task_complete"' vigil-linux-hooks/__tests__/body-builder.test.ts` returns >= 1.
    - Source assertion: the file contains the literal `"dailybrief"` (label assertion) at least once.
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/body-builder.test.ts" -t 'SessionStart probe'` exits 0.
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/body-builder.test.ts" -t 'Stop probe'` exits 0.
    - Full-suite regression: `cd vigil-linux-hooks && npm test` exits 0 with no failures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STDIN envelope → hook handler | Claude Code dispatches per event; `session_id` and `cwd` are envelope-controlled. |
| hook handler → emit_event helper | Static message strings for SessionStart/Stop; no operator input on these two paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-I3 | Tampering (command injection via crafted cwd) | `case` dispatch + emit_event | mitigate | SessionStart and Stop messages are static literals (`"session started"`, `"turn complete"`) — no operator input flows into the body for these paths. `$CWD` reaches emit_event but is consumed only by `basename "$CWD"` (which does not eval) and passed via `process.env` to `node -e` (never heredoc-interpolated). Plan 01 already established this discipline; Plan 02 inherits it unmodified. |
</threat_model>

<verification>
```
bash -n vigil-linux-hooks/vigil-agent-bridge.sh
cd vigil-linux-hooks && npm test
```

Expected end-state: all 10 tests under body-builder.test.ts (5) + fail-safe.test.ts (5) pass in <2s.
</verification>

<success_criteria>
- SessionStart branch in `vigil-agent-bridge.sh` calls `emit_event "heartbeat" "session started"`.
- Stop branch calls `emit_event "task_complete" "turn complete"`.
- UserPromptSubmit branch is untouched (Plan 03's responsibility).
- `body-builder.test.ts` has 5 passing `it` blocks; KNOWN_FIELDS membership and `cwd`-not-present regressions are guarded for both new event paths.
- All Plan 01 fail-safe invariants survive intact.
</success_criteria>

<output>
Create `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-02-SUMMARY.md` when done — capture the SessionStart/Stop body shapes observed in tests (paste a sample JSON body for each) and confirm `event` and `message` exactness against ROADMAP intent.
</output>
