---
phase: 134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05
plan: 05
type: execute
wave: 5
depends_on:
  - 134-04
files_modified: []
autonomous: false
requirements: []
threat_model:
  - T-134-A1
  - T-134-A2
  - T-134-I1
  - T-134-I2

must_haves:
  truths:
    - "Success Criterion 1: A fresh `claude` session started on the operator's Linux box (cwd `/home/morrillboss/dev/dailybrief`) appears on the G2 Companion HUD within 5 seconds of the first user prompt, with session label `dailybrief: running`"
    - "Success Criterion 4: Toggling iPhone airplane mode mid-session does NOT stall, hang, or crash Claude Code on the Linux box — the operator can continue typing prompts and getting responses normally"
    - "Success Criterion 5: `bash vigil-linux-hooks/install.sh --uninstall` removes all three hook entries from the real `~/.claude/settings.json` AND removes the three runtime files from `~/.claude/hooks/`, while leaving every GSD hook entry byte-for-byte unchanged (verified by diff against pre-install snapshot)"
    - "Code review (HIGH severity) findings on `vigil-linux-hooks/` source files are addressed BEFORE the operator UAT begins (per Phase 130 lesson `feedback_code_review_before_hardware_uat`)"
  artifacts:
    - path: ".planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-UAT-RESULTS.md"
      provides: "Operator-signed UAT log with PASS/FAIL per Success Criterion + pre-install settings.json snapshot diff + airplane-mode timing observations + uninstall diff"
    - path: ".planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-CODE-REVIEW.md"
      provides: "Code review findings from /gsd:code-review run on vigil-linux-hooks/ source files; HIGH severity items addressed before UAT"
  key_links:
    - from: "Operator's Linux Claude Code session (cwd ~/dev/dailybrief)"
      to: "G2 Companion HUD session list"
      via: "vigil-agent-bridge.sh SessionStart hook -> POST /v1/agent-events -> Phase 124 SSE -> G2 plugin subscriber"
      pattern: "dailybrief: running"
    - from: "iPhone airplane mode toggle"
      to: "Linux Claude Code session continuity"
      via: "curl --max-time 2 timeout + nohup/disown + async:true settings.json entry"
      pattern: "no stall, no error popup"
---

<objective>
Verify Phase 134 success criteria end-to-end via operator hardware UAT on the real Linux dev workstation. This plan is NOT autonomous — three of the four tasks pause for the operator. The first task is a `/gsd:code-review` checkpoint per the Phase 130 memory lesson (`feedback_code_review_before_hardware_uat.md`): the reviewer catches structural gaps that UAT behavioral observation can only confirm-by-accident, so reviewer-found HIGH severity issues MUST be resolved before the operator burns hardware-UAT cycles.

Purpose: Close Success Criteria 1 (HUD round-trip), 4 (airplane-mode fail-safe), and 5 (clean uninstall). These three criteria cannot be automated — they require the real Linux box, the real Railway production endpoint, the real G2 device, and the operator's iPhone for the airplane-mode toggle. Plans 01-04 prove the code is structurally correct; Plan 05 proves the system works in production.

Output: A signed `134-05-UAT-RESULTS.md` recording PASS/FAIL per Success Criterion, with timestamps, a pre-install/post-install/post-uninstall diff of `~/.claude/settings.json`, and the airplane-mode timing observation. If any criterion fails, the failure is captured as a gap-closure target for a subsequent `/gsd:plan-phase 134 --gaps` invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-VALIDATION.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-01-SUMMARY.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-02-SUMMARY.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-03-SUMMARY.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-04-SUMMARY.md
@vigil-linux-hooks/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run /gsd:code-review on Phase 134 source files; resolve HIGH severity findings before UAT</name>
  <read_first>
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-01-SUMMARY.md (Plan 01 outcomes — emit_event signature, EMIT_ONLY escape-hatch contract, any deviations from PATTERNS.md)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-02-SUMMARY.md (SessionStart + Stop body shapes observed in tests)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-03-SUMMARY.md (redaction corpus size, sample redacted body)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-04-SUMMARY.md (installer test outcomes, total mini-package test count)
    - Phase 130 memory: $HOME/.claude/projects/-home-morrillboss-dev-dailybrief/memory/feedback_code_review_before_hardware_uat.md (rationale for running /gsd:code-review BEFORE operator UAT)
  </read_first>
  <acceptance_criteria>
    - File assertion: `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-CODE-REVIEW.md` exists and lists every finding with severity (HIGH / MEDIUM / LOW / INFO).
    - Resolution assertion: every finding tagged HIGH has a corresponding "resolved in commit <SHA>" line in the CODE-REVIEW file, OR an explicit "deferred with rationale: <text>" line approved by the operator on the resume signal.
    - Test command: `cd vigil-linux-hooks && npm test` exits 0 AFTER all HIGH fixes land — the test suite must remain green across the fix commits.
    - Process assertion: each HIGH-severity fix is committed as its own atomic commit on the phase branch with message prefix `fix(134-05): `.
  </acceptance_criteria>
  <what-built>
    Plans 01-04 produced 13 new files under `vigil-linux-hooks/` (6 runtime + 6 test + 1 fixture, plus package.json/tsconfig.json/README.md). The full mini-package test suite is green (target 47+ tests in <2s). Code review is now warranted to surface structural gaps that automated tests can miss — auth-gate ordering, JSON escape edges in emit_event, settings.json splice idempotency on adversarial inputs, log-redaction blind spots, etc. Per the Phase 130 memory lesson (`feedback_code_review_before_hardware_uat.md`), reviewer-found HIGH severity issues MUST be resolved BEFORE the operator burns hardware-UAT cycles.
  </what-built>
  <how-to-verify>
    1. Run `/gsd:code-review` in the current Claude Code session with scope limited to the Phase 134 source files:
       - vigil-linux-hooks/vigil-agent-bridge.sh
       - vigil-linux-hooks/redact.sh
       - vigil-linux-hooks/redaction-patterns.json
       - vigil-linux-hooks/install.js
       - vigil-linux-hooks/install.sh
       - vigil-linux-hooks/__tests__/body-builder.test.ts
       - vigil-linux-hooks/__tests__/redaction-corpus.test.ts
       - vigil-linux-hooks/__tests__/redaction-drift.test.ts
       - vigil-linux-hooks/__tests__/fail-safe.test.ts
       - vigil-linux-hooks/__tests__/installer-idempotency.test.ts
       - vigil-linux-hooks/__tests__/fixtures/settings.json
       - vigil-linux-hooks/__tests__/fixtures/probe-envelope.json
       - vigil-linux-hooks/README.md
    2. Record all findings to `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-CODE-REVIEW.md` with severity labels (HIGH / MEDIUM / LOW / INFO).
    3. Address every HIGH severity finding in-place. Each fix MUST keep `cd vigil-linux-hooks && npm test` green; commit each fix as its own atomic commit on the phase branch with message `fix(134-05): <reviewer finding summary>`.
    4. After all HIGH items are resolved, re-run `cd vigil-linux-hooks && npm test` and confirm exit 0.
    5. Update `134-05-CODE-REVIEW.md` with the resolution status of each finding ("resolved in commit <SHA>" / "deferred — MEDIUM" / "wontfix — INFO with rationale").
  </how-to-verify>
  <resume-signal>
    Type "approved" once: (a) code review is complete, (b) all HIGH severity findings are resolved with commits, (c) full test suite is green, (d) `134-05-CODE-REVIEW.md` reflects the final state. If any HIGH finding cannot be fixed without redesign, type "blocked: <description>" and the orchestrator will re-route through `/gsd:plan-phase 134 --gaps`.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install + Success Criterion 1 (HUD round-trip within 5s)</name>
  <read_first>
    - vigil-linux-hooks/README.md (operator install instructions, env var setup — Plan 04 deliverable)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-CODE-REVIEW.md (the Task 1 deliverable — confirm all HIGH items are resolved before installing on the real Linux box)
    - .planning/ROADMAP.md Phase 134 Success Criterion 1 (HUD round-trip ≤5s + label = `dailybrief: running`)
  </read_first>
  <acceptance_criteria>
    - File assertion: `/tmp/134-settings-before.json` exists at the time the operator runs `bash vigil-linux-hooks/install.sh` (pre-install snapshot for the Task 4 uninstall diff).
    - CLI assertion: install stdout matches exactly `vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable.`
    - CLI assertion: `grep -c vigil-agent-bridge ~/.claude/settings.json` returns >= 3 after install.
    - Diff assertion: `diff /tmp/134-settings-before.json ~/.claude/settings.json` shows ONLY additions (no deletions or modifications to existing GSD entries).
    - Behavior assertion (logged in UAT-RESULTS): observed HUD latency from first prompt to label-visible <= 5.0 seconds.
    - Behavior assertion (logged in UAT-RESULTS): observed label string equals `dailybrief: running` exactly (basename, not full path; not session UUID).
  </acceptance_criteria>
  <what-built>
    The Phase 134 mini-package is code-complete and code-review-clean. The operator now installs it on the real Linux dev workstation and verifies that a fresh `claude` session surfaces on the G2 Companion HUD within 5 seconds with the correct label.
  </what-built>
  <how-to-verify>
    1. **Snapshot pre-install state**: `cp ~/.claude/settings.json /tmp/134-settings-before.json` for later diff comparison.
    2. **Install**: `cd ~/dev/dailybrief && bash vigil-linux-hooks/install.sh`. Confirm the exact stdout: `vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable.`
    3. **Confirm splice**: `grep -c vigil-agent-bridge ~/.claude/settings.json` returns at least 3. `diff /tmp/134-settings-before.json ~/.claude/settings.json` shows only ADDITIONS (3 new matcher groups in SessionStart / UserPromptSubmit / Stop) — no DELETIONS, no MODIFICATIONS to existing GSD entries.
    4. **Set env**: Confirm `$VIGIL_API_KEY` is exported in the operator's shell (either via `~/.bashrc` or `source ~/.config/vigil/env`). `echo ${VIGIL_API_KEY:0:6}` should print `vk_xxx` (first 6 chars only — DO NOT echo the full key).
    5. **Open the G2 Companion HUD** on the operator's Even G2 device. Note the current "active sessions" count (expected: 0 if no Mac/Linux session is running; may be 1 if a Mac session is open).
    6. **Start a Linux Claude Code session**: in a NEW terminal, `cd ~/dev/dailybrief && claude`. Submit the first prompt (anything benign — `hello`).
    7. **Time the HUD update**: start a wall clock when you press Enter on the first prompt. The G2 HUD MUST show:
       - Active sessions count incremented by 1 (`0 -> 1` or `1 -> 2`).
       - The new session labeled `dailybrief: running` (basename of the cwd).
       - Latency MUST be <= 5 seconds end-to-end (SessionStart hook -> POST -> Railway -> SSE -> G2).
    8. **Record the observation** in `134-05-UAT-RESULTS.md`: timestamp, latency (seconds, decimal), label string observed, host string observed (should match `hostname -s` output on the Linux box).
    9. **Optional sanity check** for AGENT-LINUX-03 redaction in production: submit a prompt containing the literal string `my password is hunter2`. Confirm via the G2 HUD body line (if it surfaces the prompt) that the displayed message is `[redacted: contains sensitive pattern]` and NOT the raw prompt. If the HUD doesn't surface prompts directly, inspect vigil-core production logs for the POST body of the corresponding UserPromptSubmit event.
  </how-to-verify>
  <resume-signal>
    Type "pass: <latency>s, label=<observed>, host=<observed>" if all four observations hold. Type "fail: <description>" if any criterion missed (e.g., "label was `morrillboss/dev/dailybrief` not `dailybrief`" — full-path vs basename bug). Failures will be captured in 134-05-UAT-RESULTS.md as gap-closure targets.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Success Criterion 4 (airplane-mode fail-safe)</name>
  <read_first>
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md Pitfall 1 (v2.1.87 stdio-inheritance silent stall — `Session timed out after 649s` failure signature) AND Pitfall 5 (cloud-VM hostname edge cases)
    - vigil-linux-hooks/vigil-agent-bridge.sh (confirm `nohup curl` + `</dev/null >/dev/null 2>&1 & disown` chain is present)
    - ~/.claude/settings.json (confirm spliced entries have `"async": true` + `"timeout": 5` from Plan 04)
    - .planning/ROADMAP.md Phase 134 Success Criterion 4 (fail-safe under airplane mode)
  </read_first>
  <acceptance_criteria>
    - Behavior assertion (logged in UAT-RESULTS): >= 5 prompts submitted during airplane-mode outage; each prompt receives a normal Claude response without stall, hang, or error popup.
    - Stream assertion (logged in UAT-RESULTS): zero stderr/stdout noise from the hook in the terminal during the outage window (operator inspection — T-134-A1 gate).
    - Timing assertion (logged in UAT-RESULTS): no single prompt-to-response cycle exceeds 30s (Claude Code's default hook timeout is 30s; we want zero hook-induced latency).
    - Behavior assertion (logged in UAT-RESULTS): after restoring network and submitting one more prompt, HUD timestamp updates within <= 5s.
    - Log assertion (post-test only): if `VIGIL_AGENT_BRIDGE_DEBUG=1` was set, `tail /tmp/vigil-agent-bridge.log` contains NO substring matching `vk_[a-f0-9]{6}` (T-134-A1 gate — no API key bytes leaked to the log).
  </acceptance_criteria>
  <what-built>
    With the hook installed and Success Criterion 1 verified, the operator now stress-tests the fail-safe contract by toggling iPhone airplane mode mid-session. The hook MUST NOT block the Claude Code session under any network failure mode.
  </what-built>
  <how-to-verify>
    1. **Start a fresh Linux Claude Code session**: `cd ~/dev/dailybrief && claude`. Submit the first prompt; confirm the session appears on the G2 HUD (per Task 2).
    2. **Turn iPhone airplane mode ON**. This severs the Linux box's effective path to the Railway production endpoint if the Linux box routes through iPhone-pair tethering OR if the operator's Wi-Fi depends on the iPhone hotspot. If the Linux box has its own LAN/Wi-Fi connection that is independent of the iPhone, alternatively: temporarily block `api.vigilhub.io` in `/etc/hosts` by adding `127.0.0.1 api.vigilhub.io`.
    3. **Submit 5 prompts in rapid succession** (one every 5-10 seconds). For each prompt, observe:
       - The prompt is accepted by Claude Code without any visible stall, freeze, or error message.
       - Claude responds within the normal turn cadence (no 30s/60s/649s timeout).
       - No stderr noise appears in the terminal from the hook (T-134-A1 gate).
       - The G2 HUD body line is now stale (last activity timestamp >30s old) — that's EXPECTED because events are being dropped silently. The session does NOT disappear from the HUD; it stays in `running` state because vigil-watch hasn't emitted a stale-marker yet (HUD-CLARITY-01 territory, not Phase 134).
    4. **Toggle iPhone airplane mode OFF** (or remove the `/etc/hosts` block). Wait 30 seconds.
    5. **Submit one more prompt**. Confirm the G2 HUD picks up the activity again within 5 seconds (new SessionStart heartbeat is NOT re-emitted, but UserPromptSubmit + Stop ARE — so the HUD timestamp updates).
    6. **Record observations** in `134-05-UAT-RESULTS.md`: number of prompts submitted during outage, observed Claude Code latency per prompt (`<turn elapsed>` from a wall clock), any stderr/stdout noise from the hook (target: zero bytes), and the recovery latency after network restoration (target: <=5s for HUD update).
    7. **Inspect debug log AFTER the test** (do NOT enable during the test — it would bias the observation): `tail /tmp/vigil-agent-bridge.log` if `VIGIL_AGENT_BRIDGE_DEBUG=1` was set. The log MUST NOT contain `$VIGIL_API_KEY` value bytes (verify by inspecting the first 6 chars of any logged token-shaped string).
  </how-to-verify>
  <resume-signal>
    Type "pass: <N prompts handled during outage, max turn latency Xs, zero hook noise>" if all observations hold. Type "fail: <description>" if Claude Code stalled, the terminal showed error noise, or the recovery latency exceeded 5s. Stall observations are the highest-severity failure mode — they trigger an emergency `/gsd:plan-phase 134 --gaps` to remediate the `async:true` / `nohup` chain.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Success Criterion 5 (clean uninstall + GSD coexistence)</name>
  <read_first>
    - /tmp/134-settings-before.json (the pre-install snapshot captured in Task 2 — the diff target for confirming GSD coexistence)
    - vigil-linux-hooks/install.js (confirm uninstall path filters by `vigil-agent-bridge.sh.*--event=` regex only — T-134-I2 mitigation)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md D-N2 (uninstall MUST preserve GSD entries byte-for-byte; do NOT delete empty hooks.<Event> keys)
    - .planning/ROADMAP.md Phase 134 Success Criterion 5 (clean uninstall)
  </read_first>
  <acceptance_criteria>
    - CLI assertion: uninstall stdout matches exactly `vigil-agent-bridge uninstalled.`
    - CLI assertion: `grep -c vigil-agent-bridge ~/.claude/settings.json` returns 0 after uninstall.
    - File-not-exists assertions: `~/.claude/hooks/vigil-agent-bridge.sh`, `~/.claude/hooks/redact.sh`, and `~/.claude/hooks/redaction-patterns.json` all return `No such file or directory` from `ls`.
    - Diff assertion: `diff <(jq -S . /tmp/134-settings-before.json) <(jq -S . ~/.claude/settings.json)` is empty (or only differs by empty `UserPromptSubmit: []` / `Stop: []` keys per CONTEXT D-N2 — log any extra keys as MEDIUM observation but NOT a failure).
    - Behavior assertion (logged in UAT-RESULTS): starting a fresh `claude` session still fires GSD's "Project State Reminder" injection (proves the GSD hook chain survived uninstall byte-for-byte — T-134-I2 mitigation).
    - File assertion: `134-05-UAT-RESULTS.md` exists, is signed by the operator with name + date 2026-05-19 (or current), and records PASS/FAIL per Success Criterion 1, 4, 5.
    - File assertion: `134-05-SUMMARY.md` records the overall phase close-out recommendation ("Phase 134 closed" or "open gaps → /gsd:plan-phase 134 --gaps").
  </acceptance_criteria>
  <what-built>
    Final UAT step. The operator uninstalls the hook and verifies that the real `~/.claude/settings.json` returns to its pre-install state byte-for-byte (modulo the JSON formatter's whitespace canonicalization), and that all three runtime files are removed from `~/.claude/hooks/`. GSD hook entries MUST be byte-for-byte unchanged from the pre-install snapshot.
  </what-built>
  <how-to-verify>
    1. **Uninstall**: `bash ~/dev/dailybrief/vigil-linux-hooks/install.sh --uninstall`. Confirm the exact stdout: `vigil-agent-bridge uninstalled.`
    2. **Verify settings.json**: `grep -c vigil-agent-bridge ~/.claude/settings.json` returns exactly 0 (no occurrences).
    3. **Verify runtime files removed**: `ls ~/.claude/hooks/vigil-agent-bridge.sh ~/.claude/hooks/redact.sh ~/.claude/hooks/redaction-patterns.json 2>&1` returns "No such file or directory" for all three.
    4. **Verify GSD coexistence (the critical assertion)**: `cp ~/.claude/settings.json /tmp/134-settings-after-uninstall.json`. Then `diff /tmp/134-settings-before.json /tmp/134-settings-after-uninstall.json`. The expected diff is EMPTY (or only whitespace differences from JSON.stringify's 2-space indent — if so, normalize both via `jq -S .` before diffing). Specifically:
       - `hooks.SessionStart` array length is back to the pre-install value (e.g., 2 entries).
       - `hooks.PostToolUse` is byte-for-byte unchanged (3 matchers).
       - `hooks.PreToolUse` is byte-for-byte unchanged (4 matchers).
       - `hooks.UserPromptSubmit` may or may not exist (it didn't on the pre-install box; uninstall does NOT delete the empty array per CONTEXT D-N2, but the array IS empty — if `jq` shows an extra empty `[]` key, document as MEDIUM in UAT results).
       - `hooks.Stop` same caveat as UserPromptSubmit.
    5. **Start a fresh Claude Code session**: `claude` (any cwd). Confirm GSD's "Project State Reminder" still fires from `gsd-session-state.sh` (proving the GSD chain is intact). Confirm no `vigil-agent-bridge` activity appears on the G2 HUD.
    6. **Record final state** in `134-05-UAT-RESULTS.md`: diff output (or "empty diff" PASS marker), confirmation that GSD hooks still fire, confirmation that the G2 HUD no longer surfaces Linux sessions.
    7. **(Optional sanity check)** Re-install to ensure idempotency under real conditions: `bash vigil-linux-hooks/install.sh`. Confirm same install-confirmation message. Confirm `grep -c vigil-agent-bridge` returns 3 again. This is the production-mirror of the `installer-idempotency.test.ts` round-trip and provides one more layer of confidence before phase close-out.
  </how-to-verify>
  <resume-signal>
    Type "pass: clean uninstall, GSD entries preserved, GSD chain still fires, HUD no longer shows Linux sessions" if all observations hold. Type "fail: <description>" if the diff shows GSD entries were modified, or if any of the three runtime files survived uninstall, or if the install-confirmation message did not match exactly. GSD coexistence failures are the highest-severity uninstall bug — they trigger an emergency `/gsd:plan-phase 134 --gaps`.

    After all three Success Criteria are PASS, the planner closes Phase 134. Record the final operator sign-off in `134-05-UAT-RESULTS.md` with the operator's name, the date (currently 2026-05-19), and a one-line summary of any non-blocking observations for future phases (e.g., "Hostname returns `morrillhouse` not `morrillboss-linux` — VIGIL_HOST_OVERRIDE setup may be cleaner for the operator").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's Linux box -> Railway production endpoint | Real network path; bearer auth crosses here under the operator's vk_ key. |
| operator's iPhone -> Linux box network path | The airplane-mode toggle severs this; tests the fail-safe contract under real conditions. |
| installer process -> real `~/.claude/settings.json` | Real file mutation; uninstall MUST round-trip cleanly without touching GSD entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-A1 | Information Disclosure | runtime hook stderr/stdout under real load | mitigate | Task 3 explicitly tests for zero hook noise during airplane-mode failure. If the operator observes ANY stderr output in the terminal, the hook is broken and the UAT FAILs. |
| T-134-A2 | Denial of Service (v2.1.87+ stall) | hook fire-and-forget chain under real network failure | mitigate | Task 3 is the load-bearing test — if Claude Code stalls during airplane-mode outage, the `async:true` + `nohup`/`disown` chain is broken. Failure here is highest severity and triggers immediate gap-closure. |
| T-134-I1 | Tampering (settings.json corruption) | real `~/.claude/settings.json` after install | mitigate | Task 2 step 3 explicitly diffs pre/post install state to confirm only ADDITIONS, no DELETIONS or MODIFICATIONS to existing GSD entries. Atomic write (tmpfile + rename) is the underlying mechanism. |
| T-134-I2 | Tampering (uninstall clobbers GSD entries) | real `~/.claude/settings.json` after uninstall | mitigate | Task 4 step 4 explicitly diffs pre-install vs post-uninstall state. Empty diff (or whitespace-only after `jq -S`) is the PASS criterion. Task 4 step 5 also confirms GSD hooks still functionally fire on a fresh Claude session. |
</threat_model>

<verification>
This plan has NO automated verification — it is operator hardware UAT only.

Quick-look summary for the planner/orchestrator after operator sign-off:
```
cat .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-UAT-RESULTS.md
```

Expected end-state: three PASS lines (Success Criterion 1, 4, 5) + a "Phase 134 closed" signature from the operator.
</verification>

<success_criteria>
- `/gsd:code-review` complete; HIGH severity findings resolved with atomic commits; test suite remains green.
- Success Criterion 1 PASS: Linux session appears on G2 HUD within 5s, label = `dailybrief: running`.
- Success Criterion 4 PASS: iPhone airplane-mode toggle does NOT stall/hang/error Claude Code; zero hook stderr noise; recovery <=5s after network restored.
- Success Criterion 5 PASS: Uninstall removes all 3 hook entries + 3 runtime files; GSD entries byte-for-byte unchanged (empty diff); GSD chain still fires on fresh session.
- `134-05-UAT-RESULTS.md` signed off by the operator with timestamp 2026-05-19 (or later if testing slips).
- `134-05-CODE-REVIEW.md` records all findings with resolution status.
</success_criteria>

<output>
Create `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-SUMMARY.md` when done — record the three Success Criterion outcomes (PASS/FAIL), the operator's name, the testing date, and a "Phase 134 closed" / "Phase 134 has open gaps -> /gsd:plan-phase 134 --gaps" recommendation for the orchestrator.

Also create:
- `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-UAT-RESULTS.md` (operator-signed UAT log)
- `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-05-CODE-REVIEW.md` (code review findings + resolution status)
</output>
