---
phase: 134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05
plan: 04
type: execute
wave: 4
depends_on:
  - 134-02
  - 134-03
files_modified:
  - vigil-linux-hooks/install.js
  - vigil-linux-hooks/install.sh
  - vigil-linux-hooks/README.md
  - vigil-linux-hooks/__tests__/installer-idempotency.test.ts
  - vigil-linux-hooks/__tests__/redaction-drift.test.ts
  - vigil-linux-hooks/__tests__/fixtures/settings.json
autonomous: true
requirements:
  - AGENT-LINUX-05
  - AGENT-LINUX-06
threat_model:
  - T-134-I1
  - T-134-I2
  - T-134-R3
  - T-134-A2

must_haves:
  truths:
    - "Running `node install.js` against a tempdir fake `$HOME` seeded with the fixture settings.json (2 GSD SessionStart entries) results in 3 SessionStart entries; the original 2 are preserved byte-for-byte"
    - "Re-running `node install.js` against the same fake HOME does NOT add a 4th SessionStart entry (idempotent)"
    - "Running `node install.js --uninstall` against the post-install fake HOME returns the SessionStart array to exactly 2 entries (round-trip to seed state)"
    - "After install, each spliced hook entry contains `\"async\": true` AND `\"timeout\": 5` per RESEARCH §Pattern 4"
    - "`install.js` writes settings.json via tmpfile + rename (atomic) — a crashed install leaves either the pre-state or the post-state on disk, never a zero-byte file"
    - "On settings.json parse failure, `install.js` exits 1 WITHOUT writing — never clobbers a malformed file"
    - "Drift detector `redaction-patterns.json` + `redact.sh` parity test passes: each of the 6 patterns appears verbatim in BOTH files"
    - "Rail 3 cross-repo test skips with `console.warn` when `vigil-watch/Sources/VigilWatch/Redactor.swift` is missing (Phase 133 not shipped yet) — does NOT fail the build"
    - "The hook command splice writes the absolute path `bash <os.homedir>/.claude/hooks/vigil-agent-bridge.sh --event=<Event>` for each of SessionStart, UserPromptSubmit, Stop"
    - "After install, `~/.claude/hooks/` contains the three runtime files (vigil-agent-bridge.sh, redact.sh, redaction-patterns.json) with executable perms on .sh files"
  artifacts:
    - path: "vigil-linux-hooks/install.js"
      provides: "ESM Node CLI: parses settings.json, splices 3 hook entries idempotently with async:true + timeout:5, copies runtime files to ~/.claude/hooks/, atomic write via tmpfile+rename. Supports --uninstall flag."
      contains: "renameSync"
    - path: "vigil-linux-hooks/install.sh"
      provides: "3-line bash wrapper that execs install.js, forwarding all args"
      contains: "exec"
    - path: "vigil-linux-hooks/README.md"
      provides: "Operator-facing install instructions, env var setup, troubleshooting, uninstall, Phase 133 drift detector note"
    - path: "vigil-linux-hooks/__tests__/installer-idempotency.test.ts"
      provides: "Integration test: tempdir fake HOME + fixture seed + install/install/uninstall sequence with byte-for-byte preservation assertions"
    - path: "vigil-linux-hooks/__tests__/redaction-drift.test.ts"
      provides: "Drift detector AGENT-LINUX-06: Rails 1+2 hard assertions, Rail 3 soft-skip"
    - path: "vigil-linux-hooks/__tests__/fixtures/settings.json"
      provides: "Seed fixture mirroring the real on-box settings.json shape (2 GSD SessionStart + 3 PostToolUse + 4 PreToolUse)"
  key_links:
    - from: "vigil-linux-hooks/install.js"
      to: "~/.claude/settings.json"
      via: "fs.writeFileSync(tmp) + fs.renameSync(tmp, real) — atomic write"
      pattern: "renameSync"
    - from: "vigil-linux-hooks/install.js splice"
      to: "Claude Code v2.1.23+ async hook behavior"
      via: 'spliced entry contains async:true and timeout:5'
      pattern: "async.*true"
    - from: "vigil-linux-hooks/__tests__/redaction-drift.test.ts Rail 1"
      to: "vigil-linux-hooks/redaction-patterns.json"
      via: "fs.readFileSync + JSON.parse + required[] membership assert"
      pattern: "redaction-patterns"
    - from: "vigil-linux-hooks/__tests__/redaction-drift.test.ts Rail 2"
      to: "vigil-linux-hooks/redact.sh"
      via: "fs.readFileSync + regex assert for the literal string redaction-patterns.json"
      pattern: "redact\\.sh"
---

<objective>
Land the installer, the drift detector, the cross-on-box settings.json fixture, the operator README, and the installer-idempotency integration test. This plan closes AGENT-LINUX-05 (one-command portable install + uninstall + idempotency + GSD coexistence) and AGENT-LINUX-06 (drift detector source-grep parity).

Purpose: Phase 134 deliverables become operator-installable from the Linux box with a single `bash vigil-linux-hooks/install.sh` invocation. The installer MUST coexist with the 2 existing GSD SessionStart entries on the real box — the test seeds the fixture mirroring that shape and asserts byte-for-byte preservation. The async:true + timeout:5 hook entry shape is non-negotiable per RESEARCH §Pitfall 1 (Claude Code v2.1.87+ stdio-stall bug).

Output: A working installer, 3 net-new test files (idempotency, drift, fixture), an operator-facing README, and full-suite green across all 6 test files in the mini-package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-VALIDATION.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-02-SUMMARY.md
@.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-03-SUMMARY.md
@vigil-linux-hooks/vigil-agent-bridge.sh
@vigil-linux-hooks/redact.sh
@vigil-linux-hooks/redaction-patterns.json

<interfaces>
From RESEARCH §Pattern 4 + §Pitfall 1 — the async hook entry shape:
  Each spliced hook entry MUST be:
    { "type": "command",
      "command": "bash <abs-path>/vigil-agent-bridge.sh --event=<EventName>",
      "async": true,
      "timeout": 5 }

From CONTEXT D-N1 — installer algorithm:
  1. Read ~/.claude/settings.json (init to {"hooks":{}} on ENOENT).
  2. JSON.parse; refuse to write on parse error (process.exit(1) to stderr, no clobber).
  3. For each of SessionStart, UserPromptSubmit, Stop: splice with idempotency check (regex /vigil-agent-bridge\.sh.*--event=<Event>/).
  4. Copy vigil-agent-bridge.sh, redact.sh, redaction-patterns.json into ~/.claude/hooks/. chmod 755 on *.sh files.
  5. Atomic write: fs.writeFileSync(tmp, JSON.stringify(settings, null, 2)); fs.renameSync(tmp, settingsPath).
  6. Print one-line confirmation: "vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable."

From CONTEXT D-N2 — uninstall:
  Filter each hooks.<Event> array to drop entries whose hooks[] contains a command matching the regex.
  Preserve everything else byte-for-byte (do NOT delete the hooks.<Event> key itself).
  Remove the three copied files from ~/.claude/hooks/ (try/catch — file-may-be-missing is fine).
  Print: "vigil-agent-bridge uninstalled."

EVENTS = ["SessionStart", "UserPromptSubmit", "Stop"]
COMMAND_REGEX = /vigil-agent-bridge\.sh.*--event=/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture settings.json + installer-idempotency.test.ts (Wave 0 stubs)</name>
  <files>
    vigil-linux-hooks/__tests__/fixtures/settings.json,
    vigil-linux-hooks/__tests__/installer-idempotency.test.ts
  </files>
  <read_first>
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 12 (fixture settings.json content at lines 661-682) AND Pattern Assignment 11 (installer-idempotency.test.ts structure at lines 583-651)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md §Code Examples (installer-idempotency.test.ts canonical version at lines 614-672)
    - vigil-core/src/services/brief-assembly-service.test.ts:1-99 (analog: mkdtempSync + before/after lifecycle)
    - vigil-core/src/__tests__/migration-drift.test.ts:37-75 (analog: execSync + env override pattern)
  </read_first>
  <action>
**Sub-step A — Fixture `vigil-linux-hooks/__tests__/fixtures/settings.json`:**

Create with the exact JSON shape from PATTERNS Pattern Assignment 12 lines 661-682:
  - `hooks.SessionStart`: array of 2 entries, each with `hooks: [{type:"command", command:"node gsd-check-update.js"}]` and `hooks: [{type:"command", command:"bash gsd-session-state.sh"}]` respectively.
  - `hooks.PostToolUse`: array of 3 entries with matchers `"Write|Edit"`, `"Write|Edit"`, `"Read"` and commands `node gsd-context-monitor.js`, `bash gsd-phase-boundary.sh`, `node gsd-read-injection-scanner.js`.
  - `hooks.PreToolUse`: array of 4 entries with matchers `Bash`, `Read`, `Write|Edit`, `Bash` and commands `node gsd-prompt-guard.js`, `node gsd-read-guard.js`, `node gsd-workflow-guard.js`, `bash gsd-validate-commit.sh`.
  - DO NOT include `UserPromptSubmit` or `Stop` keys (absent on the real box per RESEARCH §Runtime State Inventory) — the installer must CREATE these arrays.
  - DO NOT include any absolute paths like `/home/morrillboss/…` — the fixture must be operator-agnostic.

**Sub-step B — Test file `vigil-linux-hooks/__tests__/installer-idempotency.test.ts`:**

Imports per PATTERNS Pattern Assignment 11 lines 588-600: `describe, it, before, after` from node:test, `assert` from `node:assert/strict`, `execSync` from `node:child_process`, `mkdtempSync, readFileSync, writeFileSync, mkdirSync, copyFileSync, rmSync` from `node:fs`, `tmpdir` from `node:os`, `join, dirname` from `node:path`, `fileURLToPath` from `node:url`. Compute `HOOKS_ROOT = join(dirname(fileURLToPath(import.meta.url)), "..")`.

Write the test scaffold (will FAIL because `install.js` does not exist yet — that's Task 2):

`describe("AGENT-LINUX-05 — installer idempotency", () => { ... })` containing:
  - `let fakeHome: string;` declared at describe scope.
  - `before(() => { ... })`: mkdtemp under tmpdir, mkdir `${fakeHome}/.claude`, copyFileSync the fixture `__tests__/fixtures/settings.json` to `${fakeHome}/.claude/settings.json`.
  - `after(() => { rmSync(fakeHome, {recursive:true, force:true}) })`.

Add SIX `it` blocks (these prove must_haves listed in frontmatter):

  1. `"after install: 3 SessionStart entries + originals preserved byte-for-byte"` — execSync `node ${HOOKS_ROOT}/install.js` with `env: {...process.env, HOME: fakeHome}`. Parse the post-install settings.json. Assert `hooks.SessionStart.length === 3`. Assert `hooks.SessionStart[0].hooks[0].command === "node gsd-check-update.js"` AND `hooks.SessionStart[1].hooks[0].command === "bash gsd-session-state.sh"` (T-134-I2 byte-for-byte preservation). Assert `hooks.SessionStart[2].hooks[0].command` matches `/vigil-agent-bridge\.sh --event=SessionStart/`.

  2. `"after install: spliced entries have async:true and timeout:5"` — assert `hooks.SessionStart[2].hooks[0].async === true` AND `hooks.SessionStart[2].hooks[0].timeout === 5`. Repeat for UserPromptSubmit and Stop arrays (the installer creates these arrays per CONTEXT D-N1 step 3). Closes T-134-A2 mitigation gate.

  3. `"after install: UserPromptSubmit and Stop arrays are CREATED (were absent in fixture)"` — assert `hooks.UserPromptSubmit.length === 1` and `hooks.Stop.length === 1`. Each has a single matcher group containing a single hook with the appropriate `--event=` flag.

  4. `"after install: runtime files copied to ~/.claude/hooks/ with executable perms"` — assert `fs.existsSync(${fakeHome}/.claude/hooks/vigil-agent-bridge.sh)`, same for `redact.sh` and `redaction-patterns.json`. Assert `fs.statSync(${fakeHome}/.claude/hooks/vigil-agent-bridge.sh).mode & 0o111` is non-zero (executable bit set).

  5. `"re-run install: no 4th entry added (idempotent)"` — execSync `node install.js` a second time. Assert `hooks.SessionStart.length === 3` (unchanged). Assert `hooks.UserPromptSubmit.length === 1` and `hooks.Stop.length === 1` (unchanged).

  6. `"uninstall: returns to 2-entry seed state + runtime files removed"` — execSync `node install.js --uninstall`. Assert `hooks.SessionStart.length === 2`. Assert `hooks.SessionStart[0].hooks[0].command === "node gsd-check-update.js"` AND `hooks.SessionStart[1].hooks[0].command === "bash gsd-session-state.sh"` (T-134-I2 byte-for-byte). Assert `hooks.PostToolUse` and `hooks.PreToolUse` are byte-for-byte equal to the fixture (use a JSON.stringify compare against the original fixture text). Assert `hooks.UserPromptSubmit` and `hooks.Stop` keys still EXIST but are empty arrays (CONTEXT D-N2: do NOT delete the hooks.<Event> key even when empty). Assert `fs.existsSync(${fakeHome}/.claude/hooks/vigil-agent-bridge.sh) === false`.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('vigil-linux-hooks/__tests__/fixtures/settings.json','utf8'))" && cd vigil-linux-hooks && npx tsx --test "__tests__/installer-idempotency.test.ts" 2>&1 | head -50 | grep -E "ok |not ok" | wc -l</automated>
  </verify>
  <done>
    - `fixtures/settings.json` parses as valid JSON and contains exactly 2 SessionStart entries with the expected GSD commands.
    - `installer-idempotency.test.ts` exists with the 6 `it` blocks listed in the action, plus `before`/`after` lifecycle hooks.
    - All 6 `it` blocks currently FAIL (because install.js does not exist) — this is the expected Wave-0 RED state.
    - Each `it` block's assertion message references the human-readable failure mode (preservation / idempotency / uninstall round-trip / async-true / file copy).
  </done>
  <acceptance_criteria>
    - Source assertion: `node -e "const j=JSON.parse(require('fs').readFileSync('vigil-linux-hooks/__tests__/fixtures/settings.json','utf8'));process.exit(j.hooks.SessionStart.length===2 && j.hooks.PostToolUse.length===3 && j.hooks.PreToolUse.length===4 && !j.hooks.UserPromptSubmit && !j.hooks.Stop?0:1)"` exits 0.
    - Source assertion: `grep -cE 'it\("' vigil-linux-hooks/__tests__/installer-idempotency.test.ts` returns >= 6.
    - Source assertion: `grep -c 'async.*true' vigil-linux-hooks/__tests__/installer-idempotency.test.ts` returns >= 1 (async:true assertion present).
    - Source assertion: `grep -c 'timeout.*5' vigil-linux-hooks/__tests__/installer-idempotency.test.ts` returns >= 1.
    - Source assertion: `grep -c 'byte-for-byte\|preservation\|preserved' vigil-linux-hooks/__tests__/installer-idempotency.test.ts` returns >= 1 (T-134-I2 named assertion).
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/installer-idempotency.test.ts" 2>&1 | grep -c "not ok"` returns >= 6 (Wave-0 RED state — all 6 tests fail because install.js does not exist; this is the expected stub-first state).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create install.js + install.sh + redaction-drift.test.ts + README</name>
  <files>
    vigil-linux-hooks/install.js,
    vigil-linux-hooks/install.sh,
    vigil-linux-hooks/__tests__/redaction-drift.test.ts,
    vigil-linux-hooks/README.md
  </files>
  <read_first>
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-PATTERNS.md Pattern Assignment 4 (install.js algorithm at lines 195-283), Pattern Assignment 5 (install.sh 3-line wrapper at lines 286-302), Pattern Assignment 9 (redaction-drift.test.ts Rails 1/2/3 at lines 448-520), Pattern Assignment 13 (README.md content at lines 691-705)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-RESEARCH.md §Pattern 5 (JSON splice + atomic write at lines 356-407), §Pattern 4 (async:true + timeout:5 in spliced entry, lines 310-333), §Pitfall 1 (v2.1.87 stdio stall — RATIONALE for async:true), §Pitfall 2 (settings.json parse failure cascade — RATIONALE for tmpfile+rename atomic write), §Code Examples (drift detector canonical at lines 540-609)
    - .planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-CONTEXT.md D-N1 through D-N5 (installer algorithm), D-R4 (drift detector test — Phase 133 fs.existsSync guard with console.warn skip)
    - vigil-linux-hooks/__tests__/installer-idempotency.test.ts (the Task-1 contract — install.js must satisfy these 6 assertions)
    - vigil-core/src/__tests__/audio-log-redaction.test.ts:212-303 (drift detector source-grep analog)
    - ~/.claude/hooks/gsd-check-update.js (node-from-bash precedent — CommonJS form; install.js uses ESM since package.json is "type":"module")
  </read_first>
  <action>
**Sub-step A — `vigil-linux-hooks/install.js` (Node ESM):**

Implement per PATTERNS Pattern Assignment 4 lines 213-274 + RESEARCH §Pattern 5 lines 356-407.

Imports: `import fs from "node:fs"; import path from "node:path"; import os from "node:os"; import url from "node:url";`

Path resolution: `const settingsPath = path.join(os.homedir(), ".claude", "settings.json"); const hookDir = path.join(os.homedir(), ".claude", "hooks"); const hookPath = path.join(hookDir, "vigil-agent-bridge.sh"); const here = path.dirname(url.fileURLToPath(import.meta.url)); const SRC_FILES = ["vigil-agent-bridge.sh", "redact.sh", "redaction-patterns.json"];`

Argument parse: `const isUninstall = process.argv.includes("--uninstall");`

Constants: `const EVENTS = ["SessionStart", "UserPromptSubmit", "Stop"]; const COMMAND_REGEX = /vigil-agent-bridge\.sh.*--event=/;`

(1) Parse settings.json with ENOENT fallback to `{ hooks: {} }`. On any other parse error: write `settings.json parse failed: <message>\n` to stderr and `process.exit(1)` BEFORE writing anything (T-134-I1 + RESEARCH Pitfall 2 — refuse to clobber).

(2) Ensure `settings.hooks ??= {};`

(3) If `isUninstall`:
    - For each event in EVENTS: if `settings.hooks[event]` exists, filter the array to drop any group whose `hooks[]` contains a command matching `COMMAND_REGEX`. Do NOT delete the `hooks[event]` key even if it becomes empty (CONTEXT D-N2).
    - For each file in SRC_FILES: `try { fs.unlinkSync(path.join(hookDir, name)) } catch {}` (file-may-be-missing is fine).
    - Atomic write of settings.json via `const tmp = settingsPath + ".tmp"; fs.writeFileSync(tmp, JSON.stringify(settings, null, 2)); fs.renameSync(tmp, settingsPath);`.
    - `process.stdout.write("vigil-agent-bridge uninstalled.\n");`
    - `process.exit(0);`

(4) Otherwise (install path):
    - Ensure `hookDir` exists: `fs.mkdirSync(hookDir, { recursive: true });`
    - For each event in EVENTS: ensure `settings.hooks[event] ??= []`. Compute `alreadyInstalled` by scanning groups for a hook command matching `COMMAND_REGEX` AND including `--event=${event}` substring (idempotency check per CONTEXT D-N1 step 3 + T-134-I1). If NOT alreadyInstalled, push a new matcher group `{ hooks: [{ type: "command", command: \`bash ${hookPath} --event=${event}\`, async: true, timeout: 5 }] }`. The `async: true, timeout: 5` fields are NON-NEGOTIABLE per RESEARCH §Pitfall 1 (T-134-A2 mitigation).
    - Copy each file from `here` (the install.js dir) to `hookDir` via `fs.copyFileSync`. After copying `vigil-agent-bridge.sh` and `redact.sh`, run `fs.chmodSync(dest, 0o755)`.
    - Atomic write of settings.json via tmpfile + rename (RESEARCH Pitfall 2 mitigation for T-134-I1).
    - `process.stdout.write("vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable.\n");`

Do NOT print to stdout anywhere except the one final confirmation line (CONTEXT D-N1 step 6). Stderr is only used on parse-failure exit-1 path.

**Sub-step B — `vigil-linux-hooks/install.sh` (3-line wrapper):**

Create per PATTERNS Pattern Assignment 5 lines 292-297 exactly:
  - Line 1: `#!/usr/bin/env bash`
  - Line 2: `set -euo pipefail`
  - Line 3: `exec /usr/bin/node "$(dirname "$0")/install.js" "$@"`

`chmod 755 vigil-linux-hooks/install.sh`. The `set -euo pipefail` is appropriate here (no `[[ =~ ]]` regex; Pitfall 6 only applies to the runtime hook).

**Sub-step C — `vigil-linux-hooks/__tests__/redaction-drift.test.ts`:**

Implement per PATTERNS Pattern Assignment 9 lines 448-520 + RESEARCH §Code Examples lines 540-609.

Imports + `ROOT` boilerplate per PATTERNS Pattern G (line 783+).

`describe("AGENT-LINUX-06 — redaction pattern parity", () => {...})` containing three rails:

Rail 1 (`"redaction-patterns.json contains all six WATCH-ENRICH-03 patterns"`): readFileSync the JSON, JSON.parse, assert each of the 6 patterns (including `ey[A-Za-z0-9_-]{10,}` per RESEARCH Pitfall 4 — NOT `{20,}`) is in `json.patterns`. Assert `json.max_length === 80`. T-134-R3 mitigation.

Rail 2 (`"redact.sh references redaction-patterns.json (no hardcoded list)"`): readFileSync `redact.sh`, assert `/redaction-patterns\.json/.test(src)` — pins the canonical-source reference per CONTEXT D-R4.

Rail 3 (`"(cross-repo) vigil-watch source contains same pattern list — SKIP if not yet shipped"`): try readFileSync of `path.join(ROOT, "..", "..", "vigil-watch", "Sources", "VigilWatch", "Redactor.swift")`. On ENOENT (or any throw) — `console.warn("[skip] vigil-watch not present — Phase 133 not yet shipped"); return;`. When the file IS present, for each pattern in the JSON, assert `src` matches a regex constructed from the pattern with regex metacharacters escaped (the `p.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")` idiom from RESEARCH Code Example lines 605-607).

Add a Rail 0 anti-trivial-pass smoke test (`"required[] array contains 6 patterns — anti-trivial-pass guard"`): assert `required.length === 6` AND that the array contains the literal string `"ey[A-Za-z0-9_-]{10,}"` (NOT `"ey[A-Za-z0-9_-]{20,}"`). This is the GUARD against a future commit that empties `required[]` or downgrades the JWT threshold.

**Sub-step D — `vigil-linux-hooks/README.md`:**

Operator-facing documentation per PATTERNS Pattern Assignment 13 lines 697-705. Sections:

1. **Install**: `cd ~/dev/dailybrief && bash vigil-linux-hooks/install.sh` — one command, idempotent. Show the expected confirmation: `vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable.`

2. **Set environment variables** (CONTEXT D-A1):
   - `VIGIL_API_KEY=vk_...` — required. Source: vigil-core user dashboard. Add to `~/.bashrc` / `~/.zshrc` / `~/.config/vigil/env`.
   - `VIGIL_API_URL` — optional; default `https://api.vigilhub.io`. Override for staging.
   - `VIGIL_AGENT_BRIDGE_DEBUG=1` — optional; writes per-event log line to `/tmp/vigil-agent-bridge.log` (never stderr).
   - `VIGIL_HOST_OVERRIDE` — optional; overrides `hostname -s` output (useful on cloud VMs where the auto-detected host is `ip-10-0-1-42` — RESEARCH Pitfall 5).

3. **Verify install**: `grep -c vigil-agent-bridge ~/.claude/settings.json` returns 3. The 2 existing GSD SessionStart entries are preserved byte-for-byte.

4. **Troubleshooting**:
   - Airplane-mode test (Success Criterion 4): Toggle iPhone airplane mode mid-session and confirm Claude Code continues to function normally with no stall and no error popup. If the session hangs, check `~/.claude/settings.json` for `"async": true` on the spliced entries.
   - Debug log: enable `VIGIL_AGENT_BRIDGE_DEBUG=1` and inspect `/tmp/vigil-agent-bridge.log`. The log NEVER contains the `VIGIL_API_KEY` value.
   - 400 inspection: if events don't reach the HUD, run `curl -v $VIGIL_API_URL/v1/agent-events ...` manually with a sample body and check vigil-core access logs.

5. **Uninstall**: `bash vigil-linux-hooks/install.sh --uninstall`. Removes the 3 hook entries from settings.json AND the 3 runtime files from `~/.claude/hooks/`. GSD entries are preserved byte-for-byte.

6. **Phase 133 cross-repo coordination note**: The drift detector's Rail 3 (`redaction-drift.test.ts`) currently soft-skips when `vigil-watch/Sources/VigilWatch/Redactor.swift` is absent. When Phase 133 ships, that phase's plan will flip the skip to a hard-fail to enforce byte-for-byte pattern parity across the Linux hook and the macOS daemon.

Keep the README under ~150 lines. Use plain markdown — no images, no badges.
  </action>
  <verify>
    <automated>chmod +x vigil-linux-hooks/install.sh && cd vigil-linux-hooks && npm test 2>&1 | tee /tmp/134-04-task2.log; grep -cE "not ok" /tmp/134-04-task2.log</automated>
  </verify>
  <done>
    - `install.js` exists; `node --check vigil-linux-hooks/install.js` returns 0 (syntax valid).
    - `install.js` contains the literal strings `renameSync`, `async: true`, `timeout: 5`, `--uninstall`, and `vigil-agent-bridge installed (3 hook entries). Set VIGIL_API_KEY to enable.`
    - `install.sh` is exactly 3 substantive lines (shebang + set + exec); `chmod 755` applied; `bash -n install.sh` returns 0.
    - `redaction-drift.test.ts` contains Rails 0, 1, 2, 3 as separate `it` blocks; the JWT threshold is `{10,}` in the `required[]` array; Rail 3 uses `console.warn` skip-on-missing.
    - `README.md` exists; contains the literal `bash vigil-linux-hooks/install.sh`, `VIGIL_API_KEY`, `VIGIL_API_URL`, `VIGIL_AGENT_BRIDGE_DEBUG`, `VIGIL_HOST_OVERRIDE`, `airplane-mode`, and the Phase 133 cross-repo note.
    - `cd vigil-linux-hooks && npm test` exits 0 — full suite green (body-builder 5 + fail-safe 5 + redaction-corpus 27 + installer-idempotency 6 + redaction-drift 4 = 47 total tests passing).
    - All 6 `installer-idempotency.test.ts` `it` blocks now PASS (Wave-0 RED → GREEN transition).
  </done>
  <acceptance_criteria>
    - Source assertion: `node --check vigil-linux-hooks/install.js` exits 0.
    - Source assertion: `grep -c 'renameSync' vigil-linux-hooks/install.js` returns >= 1.
    - Source assertion: `grep -cE 'async:[[:space:]]*true' vigil-linux-hooks/install.js` returns >= 1.
    - Source assertion: `grep -cE 'timeout:[[:space:]]*5' vigil-linux-hooks/install.js` returns >= 1.
    - Source assertion: `grep -c "vigil-agent-bridge installed" vigil-linux-hooks/install.js` returns >= 1.
    - Source assertion: `grep -c "vigil-agent-bridge uninstalled" vigil-linux-hooks/install.js` returns >= 1.
    - Source assertion: `grep -c "ey\[A-Za-z0-9_-\]{10,}" vigil-linux-hooks/__tests__/redaction-drift.test.ts` returns >= 1.
    - Source assertion: `grep -c "ey\[A-Za-z0-9_-\]{20,}" vigil-linux-hooks/__tests__/redaction-drift.test.ts` returns 0 (anti-regression).
    - Source assertion: `grep -c "console.warn" vigil-linux-hooks/__tests__/redaction-drift.test.ts` returns >= 1 (Rail 3 soft-skip).
    - Source assertion: `wc -l vigil-linux-hooks/install.sh` returns 3 (or 4 with trailing newline).
    - Source assertion: `grep -c 'VIGIL_API_KEY\|VIGIL_API_URL\|VIGIL_AGENT_BRIDGE_DEBUG\|VIGIL_HOST_OVERRIDE' vigil-linux-hooks/README.md` returns >= 4.
    - Source assertion: `grep -c 'Phase 133' vigil-linux-hooks/README.md` returns >= 1 (cross-repo coordination note).
    - Test command: `cd vigil-linux-hooks && npm test` exits 0 with full suite passing.
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/installer-idempotency.test.ts"` exits 0 with 6/6 passing (the Task-1 RED-state tests now turn GREEN).
    - Test command: `cd vigil-linux-hooks && npx tsx --test "__tests__/redaction-drift.test.ts"` exits 0 with 4/4 passing (Rails 0+1+2+3-soft-skip).
    - Behavior assertion: against a tempdir fake HOME seeded with the fixture, running `HOME=$tmpdir node vigil-linux-hooks/install.js` produces a settings.json with `hooks.SessionStart.length === 3`, byte-for-byte preservation of the original 2 entries, and the 3rd entry containing `"async":true` and `"timeout":5`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer process -> ~/.claude/settings.json | Atomic write boundary; partial-write would brick Claude Code. |
| ~/.claude/settings.json -> Claude Code runtime | Claude Code parses this file at every session start; corrupt JSON = no hooks fire. |
| Phase 134 redactor JSON <-> Phase 133 vigil-watch Swift redactor | Cross-repo parity; drift between them = privacy leak in one direction. |
| operator -> `bash install.sh --uninstall` | Single command must remove only Phase 134 entries; must NOT touch GSD entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-I1 | Tampering (settings.json mid-write corruption) | `install.js` write path | mitigate | Atomic write via `fs.writeFileSync(tmp); fs.renameSync(tmp, settingsPath)`. POSIX rename is atomic on same FS. On parse-failure during read: `process.exit(1)` BEFORE writing anything (no clobber). RESEARCH §Pitfall 2 source. |
| T-134-I2 | Tampering (uninstall clobbers GSD entries) | `install.js` uninstall path | mitigate | `COMMAND_REGEX = /vigil-agent-bridge\.sh.*--event=/` anchored on the unique substring `vigil-agent-bridge.sh`; filter only drops entries whose `command` matches this regex. Test `"uninstall: returns to 2-entry seed state"` asserts JSON.stringify equality of `hooks.PostToolUse` and `hooks.PreToolUse` against the seed fixture — byte-for-byte guard. |
| T-134-R3 | Information Disclosure (drift between JSON + bash + Swift pattern source) | `redaction-drift.test.ts` Rails 1/2/3 | mitigate | Rail 1 asserts `redaction-patterns.json` contains the 6 canonical patterns; Rail 2 asserts `redact.sh` references the JSON file; Rail 3 cross-asserts vigil-watch Swift parity (currently soft-skip until Phase 133 ships, then Phase 133 plan flips it to hard-fail). Anti-trivial-pass guard pins `required[].length === 6` and the JWT `{10,}` threshold. |
| T-134-A2 | Denial of Service (Claude Code v2.1.87+ stdio stall) | Spliced hook entry in settings.json | mitigate | Each spliced entry includes `"async": true` (official Claude Code v2.1.23+ posture per `[CITED: code.claude.com/docs/en/hooks]`) AND `"timeout": 5` (defensive cap). Combined with the script-side `nohup … </dev/null >/dev/null 2>&1 & disown` from Plan 01, this is the belt-and-suspenders mitigation. Test `"spliced entries have async:true and timeout:5"` is the gate. |
</threat_model>

<verification>
```
node --check vigil-linux-hooks/install.js
bash -n vigil-linux-hooks/install.sh
cd vigil-linux-hooks && npm test
```

Expected end-state: full mini-package test suite green; 6/6 installer-idempotency tests pass; 4/4 redaction-drift tests pass (Rail 3 emits `[skip]` warning); README exists with 5 documented env vars and the Phase 133 coordination note.
</verification>

<success_criteria>
- `install.js` is ESM, splices 3 hook entries (with `async:true, timeout:5`), copies runtime files to `~/.claude/hooks/`, supports `--uninstall`, and writes settings.json atomically.
- `install.sh` is a 3-line bash wrapper that execs `install.js`.
- `installer-idempotency.test.ts` 6/6 pass: GSD coexistence + idempotency + uninstall round-trip + async-true + file copy + UserPromptSubmit/Stop array creation.
- `redaction-drift.test.ts` 4/4 pass: Rails 0 (anti-trivial), 1 (JSON contents), 2 (bash references JSON), 3 (cross-repo soft-skip).
- `README.md` documents install, 4 env vars, troubleshooting (airplane-mode), uninstall, and Phase 133 cross-repo note.
- Full mini-package `npm test` green.
</success_criteria>

<output>
Create `.planning/phases/134-linux-claude-code-vigil-core-agent-events-bridge-new-2026-05/134-04-SUMMARY.md` when done — record: total test count across the mini-package (target 47+), exact wall-clock duration of `npm test` (target <2s per VALIDATION.md), any deviations from the fixture shape that surfaced during testing, and a clear note that Plans 02/03/04 are now CODE-COMPLETE pending the Plan 05 hardware UAT.
</output>
