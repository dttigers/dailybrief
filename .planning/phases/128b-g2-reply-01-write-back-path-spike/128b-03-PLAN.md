---
phase: 128b
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/128b-write-back/pathD-mcp-probe.sh
  - .planning/spikes/128b-write-back/pathD-mcp-server.mjs
  - .planning/spikes/128b-write-back/evidence/pathD-fresh-out.txt
  - .planning/spikes/128b-write-back/evidence/pathD-mcp-config.json
  - .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, path-d, mcp, claude-code, inverted-model]
must_haves:
  truths:
    - "Path D (MCP server hook) is empirically probed via a minimal ~30 LOC stdio MCP server stub exposing one tool `vigil_external_reply` that returns a buffered string"
    - "The probe uses `claude -p --mcp-config <config> --allowedTools mcp__vigil-spike__vigil_external_reply` against a fresh session to test the Claude-pulls model (NOT Vigil-pushes — Vigil cannot force a tool call mid-turn from outside the process)"
    - "The MCP SDK (`@modelcontextprotocol/sdk`) is loaded ad-hoc via `npx` into the spike dir; NEVER added to a project package.json (D-A1 isolation)"
    - "Active-session attempt is documented as ANALYTICAL (no inject-tool-result-into-running-session surface exists; this is the inverted-model conclusion from RESEARCH)"
    - "Per-path predicted verdict per RESEARCH: DEGRADE (inverted model — fresh-session-only via prompted tool-call) — recorded for Plan 05 SPIKE-DECISION aggregation"
    - "Fallback: if `@modelcontextprotocol/sdk` npm fetch fails in ≤60 min, mark INCONCLUSIVE and lean on analytical-only treatment (RESEARCH §'Environment Availability' fallback)"
  artifacts:
    - path: ".planning/spikes/128b-write-back/pathD-mcp-server.mjs"
      provides: "Minimal ~30 LOC stdio MCP server stub exposing the `vigil_external_reply` tool"
      contains: "vigil_external_reply"
    - path: ".planning/spikes/128b-write-back/pathD-mcp-probe.sh"
      provides: "Empirical probe script that wires the MCP server to a fresh claude -p subprocess"
      contains: "PHASE 128b SPIKE — TOSSABLE"
    - path: ".planning/spikes/128b-write-back/evidence/pathD-fresh-out.txt"
      provides: "Raw stdout from `claude -p --mcp-config ...` after Claude calls the tool and prints the buffered reply"
      contains: "yes\\|no-reply\\|INCONCLUSIVE"
    - path: ".planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md"
      provides: "Mini-verdict per Pattern 2 (D-V1 four-step checklist for Path D); records inverted-model DEGRADE"
      contains: "DEGRADE\\|INCONCLUSIVE\\|FAIL\\|PASS"
  key_links:
    - from: ".planning/spikes/128b-write-back/pathD-mcp-probe.sh"
      to: ".planning/spikes/128b-write-back/pathD-mcp-server.mjs"
      via: "mcp-config.json `mcpServers.vigil-spike.command=node args=[path/to/server]`"
      pattern: "vigil-spike\\|pathD-mcp-server"
    - from: ".planning/spikes/128b-write-back/pathD-mcp-server.mjs"
      to: "@modelcontextprotocol/sdk"
      via: "npx --package @modelcontextprotocol/sdk -- node script (ad-hoc; NOT in project package.json)"
      pattern: "modelcontextprotocol"
---

<objective>
Empirically probe Path D (MCP server hook) — the inverted-direction "Claude pulls, Vigil pushes-buffered" candidate write-back path. The probe wires a tiny stdio MCP server (one tool: `vigil_external_reply` returning a hardcoded `"yes"`) to a fresh `claude -p --mcp-config` invocation and observes whether Claude calls the tool and surfaces the buffered reply when prompted. Active-session injection is structurally impossible (MCP servers attach at session start; tool calls are Claude's decision, not Vigil's push) — this is documented analytically per RESEARCH §"Path D" — not via unsafe-primitive escalation.

Purpose: Satisfy the third of the three enumerated empirical paths for G2-REPLY-01 success criterion 1 ("at least 3 of (a)/(b)/(c)/(d)"). Path D's structural conclusion ("MCP is the right pattern for Claude-pulls, but wrong for Vigil-pushes-mid-session") is load-bearing for the v3.10+ Deferred Idea per CONTEXT line 222 — the MCP-as-prompter variant where an MCP server buffers G2 replies and exposes `vigil_check_external_reply` that Claude proactively calls before every `needs_input`. Recording this empirically here avoids re-investigation later.

Output: One MCP server stub (`pathD-mcp-server.mjs` ~30 LOC) + one probe script + raw probe stdout + minimal mcp-config.json + per-path transcript with mechanical D-V1 mini-verdict (predicted DEGRADE per RESEARCH; INCONCLUSIVE fallback if SDK fetch fails).

Per RESEARCH §"Path D" + RESEARCH §"Open Questions" 1: the MCP SDK fetch is best-effort via `npx --package @modelcontextprotocol/sdk -- node ...`; if it fails in ≤60 min (RESEARCH wallclock cap for this path), the transcript records `INCONCLUSIVE — covered by analytical-only treatment` and Plan 05 cites the RESEARCH §"Path D" analytical conclusion verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/001-tmux-write-back-128b/README.md

<interfaces>
<!-- Locked from CONTEXT.md D-A1 + D-O1 path (d) + RESEARCH.md §"Path D" methodology -->

D-V1 four-step gate (same as Plans 01-02):
  step 1 — non-TTY writer originates the reply (server.mjs returns `process.env.VIGIL_BUFFERED_REPLY ?? 'no-reply'`)
  step 2 — string reaches input channel (the tool-result returned to Claude)
  step 3 — Claude processes as next user turn (Claude's output mentions the buffered string — verifiable via grep)
  step 4 — session continues healthy ≥60s (only relevant if active-session — which is structurally inapplicable)

D-V2 DEGRADE conditions matching Path D:
  - "Round-trip works ONLY on fresh `claude -p` sessions, NOT mid-interactive-session"  ← matches inverted-model
  - The "inverted-direction" framing: Claude decides when to call tools; Vigil cannot force a tool call mid-turn

MCP server stdio shape (verified from RESEARCH §"Path D" lines 423-435):
  - `Server` from `@modelcontextprotocol/sdk/server/index.js`
  - `StdioServerTransport` from `@modelcontextprotocol/sdk/server/stdio.js`
  - `server.setRequestHandler({ method: 'tools/list' }, ...)` → array of tool descriptors
  - `server.setRequestHandler({ method: 'tools/call' }, ...)` → `content: [{ type: 'text', text: ... }]`
  - `await server.connect(new StdioServerTransport())`

mcp-config.json shape (verified from RESEARCH §"Path D" lines 440-449):
  {
    "mcpServers": {
      "vigil-spike": {
        "command": "node",
        "args": ["<absolute-path-to-mcp-server.mjs>"],
        "env": {"VIGIL_BUFFERED_REPLY": "yes"}
      }
    }
  }

Claude invocation (verified from RESEARCH §"Path D" lines 453-457):
  claude -p --mcp-config "<config>" --strict-mcp-config \
    --allowedTools "mcp__vigil-spike__vigil_external_reply" \
    --model haiku \
    "Use the vigil_external_reply tool and print exactly what it returned."

Bail conditions (RESEARCH wallclock):
  - SDK install fails after ≤60 min → INCONCLUSIVE; cite analytical-only treatment
  - stdio handshake fails → INCONCLUSIVE; do NOT escalate to hand-rolled JSON-RPC stub unless wallclock budget allows

Per CONTEXT D-A1: server.mjs + probe.sh + config + evidence all live under `.planning/spikes/128b-write-back/` — NEVER add `@modelcontextprotocol/sdk` to any project package.json.

D-G1 redaction: tool name `vigil_external_reply` contains `external` not `secret`/`auth`/`token`; env var `VIGIL_BUFFERED_REPLY` is a content vehicle not an auth-name; OK.

Pitfall 7 (D-A2): the `"yes"` test string IS the test surface for Path D — but DO NOT add an `ALLOWED_REPLIES` constant or validation. The spike's question is "does the round-trip work?"; production allowlist is Phase 133 G2-REPLY-04 scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MCP server stub (pathD-mcp-server.mjs)</name>
  <files>.planning/spikes/128b-write-back/pathD-mcp-server.mjs</files>
  <read_first>
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Path D" lines 411-478 (the verbatim concrete probe script — the server stub is embedded as a heredoc at lines 423-435; extract that as the standalone `pathD-mcp-server.mjs` file)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-A1" (spike code lives under `.planning/spikes/128b-write-back/`; NOT in vigil-core or vigil-g2-plugin)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-A2" (the 5-string allowlist is markdown-sketch-only; DO NOT implement it here)
    - .planning/spikes/CONVENTIONS.md §"Tossable headers" (the .mjs file needs an equivalent header adapted for JS comment syntax)
  </read_first>
  <action>
    Create `.planning/spikes/128b-write-back/pathD-mcp-server.mjs` (mode 0644, NOT executable — this is consumed by `node`, not invoked directly) with EXACTLY the content below. The structure is the verbatim RESEARCH §"Path D" lines 423-435 server stub adapted with a JS-comment-syntax TOSSABLE header.

    File contents (line-by-line):

    ```javascript
    #!/usr/bin/env node
    // PHASE 128b SPIKE — TOSSABLE. Phase 133 owns G2-REPLY productionization;
    // this file is spike-only and SHOULD BE DELETED after the verdict is committed.
    // Path D: minimal stdio MCP server exposing `vigil_external_reply` tool
    //   - returns process.env.VIGIL_BUFFERED_REPLY (or 'no-reply' if unset)
    //   - tests the Claude-pulls model (NOT Vigil-pushes — see CONTEXT D-O1 path (d))
    import { Server } from '@modelcontextprotocol/sdk/server/index.js';
    import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';

    const server = new Server(
      { name: 'vigil-spike', version: '0.0.0' },
      { capabilities: { tools: {} } },
    );

    server.setRequestHandler({ method: 'tools/list' }, async () => ({
      tools: [
        {
          name: 'vigil_external_reply',
          description: 'Returns the buffered external reply',
          inputSchema: { type: 'object', properties: {} },
        },
      ],
    }));

    server.setRequestHandler({ method: 'tools/call' }, async (_req) => ({
      content: [{ type: 'text', text: process.env.VIGIL_BUFFERED_REPLY ?? 'no-reply' }],
    }));

    await server.connect(new StdioServerTransport());
    ```

    Notes on adaptations from the RESEARCH heredoc:
    - The RESEARCH heredoc embeds the server inline via `cat > "$SCRATCH/mcp-server.mjs" <<'EOF'`; here it's a committed file at the spike dir per D-A1.
    - The TOSSABLE header is expanded to four comment lines (vs the bash three-line form) because the .mjs file uses `//` syntax and there's no `#!/usr/bin/env bash` line to count against.
    - The `(_req)` arg gets an underscore prefix to suppress unused-var lint warnings (if a future spike adds lint to this dir).
    - DO NOT add an `ALLOWED_REPLIES` constant. DO NOT add allowlist validation. DO NOT add error logging that names the input. The tool returns the env var verbatim because the spike's question is "does the tool-call round-trip work?" — not "is the production-shape safe?" (Phase 133 G2-REPLY-04 owns the allowlist; D-A2).

    The file is NOT made executable — Node loads it via `args: ["<path>"]` in the mcp-config, not via shebang execution.
  </action>
  <verify>
    <automated>test -f .planning/spikes/128b-write-back/pathD-mcp-server.mjs && head -6 .planning/spikes/128b-write-back/pathD-mcp-server.mjs | grep -q "PHASE 128b SPIKE — TOSSABLE" && grep -q "vigil_external_reply" .planning/spikes/128b-write-back/pathD-mcp-server.mjs && grep -q "@modelcontextprotocol/sdk/server/index.js" .planning/spikes/128b-write-back/pathD-mcp-server.mjs && grep -q "@modelcontextprotocol/sdk/server/stdio.js" .planning/spikes/128b-write-back/pathD-mcp-server.mjs && grep -q "VIGIL_BUFFERED_REPLY" .planning/spikes/128b-write-back/pathD-mcp-server.mjs && ! grep -E "ALLOWED_REPLIES|allowlist" .planning/spikes/128b-write-back/pathD-mcp-server.mjs && echo "MCP server stub verified"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/pathD-mcp-server.mjs` exists
    - First 6 lines contain `PHASE 128b SPIKE — TOSSABLE`, `Phase 133 owns G2-REPLY productionization`, and `Path D: minimal stdio MCP server`
    - Contains the tool name `vigil_external_reply` exactly
    - Contains both imports: `@modelcontextprotocol/sdk/server/index.js` AND `@modelcontextprotocol/sdk/server/stdio.js`
    - Contains the env var reference `process.env.VIGIL_BUFFERED_REPLY` (the buffered-reply vehicle)
    - Does NOT contain `ALLOWED_REPLIES` or `allowlist` (D-A2 — Phase 133 owns the allowlist)
    - File mode is non-executable 0644 (Node loads via `args:`, not `#!/usr/bin/env node` shebang execution)
    - Total file size ≤80 lines / ≤2KB (the "~30 LOC stub" target per CONTEXT line 177 + RESEARCH §"Don't Hand-Roll" row 4)
  </acceptance_criteria>
  <done>
    Committed minimal MCP server stub ready for the probe script (Task 2) to consume. The stub is verbatim per RESEARCH §"Path D" lines 423-435 except for the standalone-file framing + JS comment header. No allowlist (D-A2). No project-package.json contamination (D-A1).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write + run pathD-mcp-probe.sh probe; capture evidence + transcript</name>
  <files>.planning/spikes/128b-write-back/pathD-mcp-probe.sh, .planning/spikes/128b-write-back/evidence/pathD-fresh-out.txt, .planning/spikes/128b-write-back/evidence/pathD-mcp-config.json, .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md</files>
  <read_first>
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Path D" lines 411-478 (verbatim concrete probe script — adapt for ad-hoc npx + bail condition + transcript heredoc)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-A1" (spike code isolated to `.planning/spikes/128b-write-back/`; ad-hoc npx must NOT mutate any project package.json)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Environment Availability" line 851 ("`@modelcontextprotocol/sdk` (npm) — Path D MCP stub — ✗ (not in project deps) — will install ad-hoc via `npx` — If npm install fails in spike scope, fall back to a hand-rolled 30-LOC stdio JSON-RPC stub")
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Pitfall 1" + §"Pitfall 5" + §"Pitfall 7" (sentinel; mechanical verdict; no allowlist in spike code)
    - .planning/spikes/CONVENTIONS.md (full)
    - vigil-core/package.json (READ-ONLY — confirm `@modelcontextprotocol/sdk` is NOT listed; if it IS in deps, that's a project-side install and the spike can use it directly via `node` without npx — record the choice in TRANSCRIPT)
  </read_first>
  <action>
    Create `.planning/spikes/128b-write-back/pathD-mcp-probe.sh` (mode 0755) following the structure below. The probe runs `claude -p` against a fresh session with the MCP server stub wired in via `--mcp-config`. Active-session injection is NOT attempted (structural N/A per RESEARCH); recorded analytically in the transcript.

    Script structure:

    1. Shebang + TOSSABLE header (THREE lines):
       `#!/usr/bin/env bash`
       `# PHASE 128b SPIKE — TOSSABLE. Phase 133 owns G2-REPLY productionization;`
       `# this file is spike-only and SHOULD BE DELETED after the verdict is committed.`
       `# Path D: MCP server hook — Claude-pulls model via fresh -p subprocess`
    2. `set -euo pipefail`
    3. Vars (D-G1 — descriptive non-blocked names):
       `EVIDENCE_DIR=".planning/spikes/128b-write-back/evidence"`
       `mkdir -p "$EVIDENCE_DIR"`
       `SCRATCH=$(mktemp -d -t spike-128b-D-XXXXXX)`
       `SERVER_PATH="$(pwd)/.planning/spikes/128b-write-back/pathD-mcp-server.mjs"`
       `trap 'rm -rf "$SCRATCH"' EXIT`
    4. SDK availability check (best-effort with fallback per RESEARCH §"Environment Availability"):
       ```
       echo "[Path D] checking @modelcontextprotocol/sdk availability"
       SDK_AVAILABLE=0
       SDK_MODE="unknown"
       if node -e "import('@modelcontextprotocol/sdk/server/index.js').then(()=>process.exit(0)).catch(()=>process.exit(1))" 2>/dev/null; then
         SDK_AVAILABLE=1
         SDK_MODE="globally-resolvable (no npx needed)"
         echo "[Path D] SDK already resolvable via node — no npx install needed"
       else
         echo "[Path D] SDK not globally resolvable; will attempt ad-hoc npx fetch"
         # Try ad-hoc fetch with a hard 5-minute cap to avoid 60-min wallclock blowout per RESEARCH bail
         if timeout 300 npm exec --yes --package=@modelcontextprotocol/sdk -- node -e "import('@modelcontextprotocol/sdk/server/index.js').then(()=>process.exit(0)).catch(()=>process.exit(1))" 2>"$EVIDENCE_DIR/pathD-sdk-install-err.txt"; then
           SDK_AVAILABLE=1
           SDK_MODE="npx-fetched (ad-hoc)"
         else
           SDK_AVAILABLE=0
           SDK_MODE="UNAVAILABLE — see pathD-sdk-install-err.txt"
         fi
       fi
       echo "[Path D] SDK_AVAILABLE=$SDK_AVAILABLE  SDK_MODE=$SDK_MODE"
       ```
    5. Build mcp-config.json (committed to evidence/ for reproducibility):
       ```
       cat > "$EVIDENCE_DIR/pathD-mcp-config.json" <<EOF
       {
         "mcpServers": {
           "vigil-spike": {
             "command": "node",
             "args": ["$SERVER_PATH"],
             "env": {"VIGIL_BUFFERED_REPLY": "yes"}
           }
         }
       }
       EOF
       echo "[Path D] mcp-config written to $EVIDENCE_DIR/pathD-mcp-config.json"
       ```
    6. Probe — conditional on SDK_AVAILABLE:
       ```
       FRESH_PASS=0
       if [ "$SDK_AVAILABLE" -eq 1 ]; then
         echo "[Path D] running claude -p with --mcp-config (fresh session)"
         # If SDK_MODE indicates npx-fetched, prepend the npx wrapper so node finds the package
         if [ "$SDK_MODE" = "npx-fetched (ad-hoc)" ]; then
           timeout 120 npm exec --yes --package=@modelcontextprotocol/sdk -- claude -p \
             --mcp-config "$EVIDENCE_DIR/pathD-mcp-config.json" \
             --strict-mcp-config \
             --allowedTools "mcp__vigil-spike__vigil_external_reply" \
             --model haiku \
             "Use the vigil_external_reply tool and print exactly what it returned." \
             > "$EVIDENCE_DIR/pathD-fresh-out.txt" 2>&1 || true
         else
           timeout 120 claude -p \
             --mcp-config "$EVIDENCE_DIR/pathD-mcp-config.json" \
             --strict-mcp-config \
             --allowedTools "mcp__vigil-spike__vigil_external_reply" \
             --model haiku \
             "Use the vigil_external_reply tool and print exactly what it returned." \
             > "$EVIDENCE_DIR/pathD-fresh-out.txt" 2>&1 || true
         fi

         # Sentinel: the buffered reply is "yes" (set via env in mcp-config); grep for it.
         # NOTE: "yes" is a common English word — for a stronger sentinel, the operator-facing
         # transcript should also note WHETHER the tool was actually called (look for
         # `mcp__vigil-spike__vigil_external_reply` invocation markers in stdout).
         if grep -qE '\byes\b' "$EVIDENCE_DIR/pathD-fresh-out.txt" && grep -qE 'vigil_external_reply|vigil-spike' "$EVIDENCE_DIR/pathD-fresh-out.txt"; then
           FRESH_PASS=1
           echo "[Path D] fresh-session: tool was invoked AND 'yes' appears in stdout ⇒ step 3 ✓"
         else
           FRESH_PASS=0
           echo "[Path D] fresh-session: tool-invocation OR 'yes' missing ⇒ step 3 ✗"
         fi
       else
         echo "[Path D] SKIPPING empirical probe — SDK unavailable; falling back to analytical-only"
       fi
       ```
    7. Mechanical TRANSCRIPT heredoc:
       ```
       MINI_VERDICT=""
       if [ "$SDK_AVAILABLE" -eq 1 ] && [ "$FRESH_PASS" -eq 1 ]; then
         MINI_VERDICT="DEGRADE (inverted model — fresh-session-only via prompted tool-call)"
       elif [ "$SDK_AVAILABLE" -eq 1 ] && [ "$FRESH_PASS" -eq 0 ]; then
         MINI_VERDICT="FAIL (fresh-session tool-call did not complete the round-trip)"
       else
         MINI_VERDICT="INCONCLUSIVE — SDK unavailable; covered by RESEARCH §'Path D' analytical-only treatment"
       fi

       cat > "$EVIDENCE_DIR/pathD-TRANSCRIPT.md" <<EOF
       # Path D — Per-Path D-V1 Mini-Verdict

       Probe run: $(date -Iseconds)
       Script: .planning/spikes/128b-write-back/pathD-mcp-probe.sh
       MCP server: .planning/spikes/128b-write-back/pathD-mcp-server.mjs
       claude version: $(claude --version 2>/dev/null || echo unknown)
       SDK availability: $SDK_AVAILABLE ($SDK_MODE)

       ## D-V1 Four-Step Gate (Fresh Session, Claude-pulls model)

       | Step | Description | Verdict |
       |------|-------------|---------|
       | 1    | Reply originates from non-TTY writer | $([ "$SDK_AVAILABLE" -eq 1 ] && echo "✓ (env VIGIL_BUFFERED_REPLY=yes set by probe; server reads from env)" || echo "N/A (SDK unavailable)") |
       | 2    | String reaches input channel | $([ "$SDK_AVAILABLE" -eq 1 ] && echo "✓ if step 3 ✓ (tool-result is the input channel from MCP server → Claude)" || echo "N/A") |
       | 3    | Claude processes as next user turn | $([ "$SDK_AVAILABLE" -eq 1 ] && [ "$FRESH_PASS" -eq 1 ] && echo "✓ (tool invoked AND 'yes' in stdout — see pathD-fresh-out.txt)" || echo "✗ (tool-invocation OR 'yes' missing; see pathD-fresh-out.txt)") |
       | 4    | Session continues healthy ≥60s | N/A (claude -p exits after single response — by design) |

       ## D-V1 Four-Step Gate (Active Session)

       | Step | Description | Verdict |
       |------|-------------|---------|
       | 1    | Reply originates from non-TTY writer | ✓ (server emits a tool result) |
       | 2    | String reaches input channel | ✗ STRUCTURAL — MCP tools are tools Claude CALLS, not channels that PUSH to Claude. Vigil cannot force a tool call mid-turn from outside the process. |
       | 3    | Claude processes as next user turn | ✗ vacuous (no input received) |
       | 4    | Session continues healthy ≥60s | ✗ vacuous |

       ## Mini-Verdict

       **$MINI_VERDICT**

       Per CONTEXT line 222 (Deferred Idea — "MCP server-as-prompter UX"):
       The interesting v3.10+ variant is "Claude pulls from a \`vigil_check_external_reply\` tool when it's about to ask the operator" — but that requires Claude to be prompt-conditioned to call the tool before every \`needs_input\`, which is NOT the round-trip the spike is testing.

       ## Evidence

       - \`pathD-mcp-config.json\` — the mcp-config wiring the server stub to claude -p
       - \`pathD-fresh-out.txt\` — raw stdout from \`claude -p --mcp-config ...\` (full transcript including tool invocations)
       - \`pathD-mcp-server.mjs\` — the ~30 LOC stdio MCP server stub
       - \`pathD-sdk-install-err.txt\` — stderr from npx attempt (only present if SDK fetch failed)

       ## Cited

       - CONTEXT D-O1 path (d) — "MCP tools are tools Claude CALLS, not channels that PUSH to Claude. The injection model is inverted."
       - CONTEXT line 222 — Deferred MCP-as-prompter UX for v3.10+
       - RESEARCH §"Path D" — predicted DEGRADE (inverted model)
       - RESEARCH §"Environment Availability" — INCONCLUSIVE fallback if SDK fetch fails
       EOF
       echo "[Path D] transcript written: $EVIDENCE_DIR/pathD-TRANSCRIPT.md"
       ```
    8. Final exit: `exit 0` regardless of SDK_AVAILABLE — the transcript records the correct INCONCLUSIVE branch if needed.

    Make script executable (`chmod 0755`). RUN it: `bash .planning/spikes/128b-write-back/pathD-mcp-probe.sh`. Total wallclock target ≤10 minutes (5 min SDK fetch + 2 min claude probe + transcript authoring); HARD CAP at 60 min per RESEARCH bail.

    Per CONTEXT D-G1: variables `EVIDENCE_DIR`, `SCRATCH`, `SERVER_PATH`, `SDK_AVAILABLE`, `SDK_MODE`, `FRESH_PASS`, `MINI_VERDICT` — none match BLOCKED_PROPERTY_NAMES.

    Per Pitfall 7 (D-A2): the script does NOT enforce an allowlist. The `"yes"` test string IS the test surface, not a production allowlist gate. Production allowlist enforcement is Phase 133 G2-REPLY-04 scope.

    Per Pitfall 3 (D-G3): script does NOT use `--bare`. Operator's Claude Max OAuth handles auth.

    Per RESEARCH bail: if `node -e "import(...)"` AND `npm exec --yes --package=@modelcontextprotocol/sdk` both fail in ≤5 min, the script proceeds to the INCONCLUSIVE branch and writes the analytical-only transcript. Do NOT fall back to a hand-rolled JSON-RPC stub (RESEARCH §"Environment Availability" allows it, but the analytical conclusion holds regardless and saves wallclock).
  </action>
  <verify>
    <automated>test -x .planning/spikes/128b-write-back/pathD-mcp-probe.sh && head -4 .planning/spikes/128b-write-back/pathD-mcp-probe.sh | grep -q "PHASE 128b SPIKE — TOSSABLE" && grep -q "set -euo pipefail" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && grep -q "vigil_external_reply" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && grep -q "VIGIL_BUFFERED_REPLY" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && grep -q "strict-mcp-config" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && ! grep -E "(TOKEN|AUTH|SECRET|BEARER|APIKEY)=" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && ! grep -q -- "--bare" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && ! grep -qE "ALLOWED_REPLIES" .planning/spikes/128b-write-back/pathD-mcp-probe.sh && bash .planning/spikes/128b-write-back/pathD-mcp-probe.sh && test -f .planning/spikes/128b-write-back/evidence/pathD-mcp-config.json && test -f .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md && grep -qE 'DEGRADE \(inverted model — fresh-session-only|INCONCLUSIVE — SDK unavailable|FAIL \(fresh-session tool-call' .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md && grep -qE 'inverted model|inverted-direction|Claude pulls' .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md && echo "Path D empirical record complete"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/pathD-mcp-probe.sh` exists, mode 0755, executable
    - First 4 lines (after shebang) contain `PHASE 128b SPIKE — TOSSABLE`, `Phase 133 owns G2-REPLY productionization`, and `Path D: MCP server hook`
    - Script contains `set -euo pipefail`
    - Script contains the tool name `vigil_external_reply` exactly (matches server stub)
    - Script contains `VIGIL_BUFFERED_REPLY` env var (matches server stub)
    - Script contains `--strict-mcp-config` (verified surface from RESEARCH §"Path D" + claude --help 2026-05-14)
    - Script does NOT contain blocked-property-name variable assignments
    - Script does NOT contain `--bare`
    - Script does NOT contain `ALLOWED_REPLIES` (D-A2)
    - Running the script via `bash .planning/spikes/128b-write-back/pathD-mcp-probe.sh` exits with status 0 in ≤600 seconds (10 min)
    - `.planning/spikes/128b-write-back/evidence/pathD-mcp-config.json` exists with `mcpServers.vigil-spike.command=node` and `env.VIGIL_BUFFERED_REPLY="yes"`
    - `.planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md` exists and contains: (a) TWO D-V1 four-step gate tables (one for "Fresh Session, Claude-pulls model" + one for "Active Session"); (b) the literal phrase `inverted model` OR `inverted-direction` OR `Claude pulls`; (c) a mini-verdict line resolving to one of: `DEGRADE (inverted model — fresh-session-only via prompted tool-call)`, `FAIL (fresh-session tool-call did not complete the round-trip)`, or `INCONCLUSIVE — SDK unavailable; covered by RESEARCH §'Path D' analytical-only treatment`
    - The TRANSCRIPT mini-verdict matches the empirical run: if SDK_AVAILABLE AND tool was invoked AND 'yes' in stdout ⇒ DEGRADE; if SDK_AVAILABLE AND step 3 failed ⇒ FAIL; if SDK unavailable ⇒ INCONCLUSIVE
    - No project package.json files were modified (grep `git diff --name-only` for `package.json` should return empty — the ad-hoc npx fetch must not leak into project deps)
    - Total Anthropic API spend ≤$0.05 (one fresh `claude -p` call with one tool invocation)
  </acceptance_criteria>
  <done>
    Path D empirically probed via a fresh `claude -p --mcp-config` invocation with the stub MCP server wired in. The Claude-pulls model is empirically tested; the active-session direction is documented analytically as structurally inapplicable. Transcript records the mechanical D-V1 mini-verdict (DEGRADE / FAIL / INCONCLUSIVE depending on SDK availability and tool-call success). The MCP SDK is loaded via `npx` ad-hoc — no project package.json contamination (D-A1 verified by absence of project package.json diff). Plan 06 MEASUREMENTS and Plan 05 SPIKE-DECISION can cite `pathD-TRANSCRIPT.md` and `pathD-fresh-out.txt` directly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-runner shell → npx fetch | Ad-hoc package fetch; cached under `~/.npm/_npx` (npm's standard cache); never written to project `package.json` or `node_modules` |
| MCP server stdio → claude subprocess | JSON-RPC over stdio; standard MCP transport; no privileged surface |
| Buffered reply env var → tool result | `process.env.VIGIL_BUFFERED_REPLY` carries the test string (`"yes"`); not auth/secret content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-03-01 | Tampering | project package.json (D-A1 isolation) | mitigate | The script uses `npm exec --yes --package=@modelcontextprotocol/sdk -- ...` and NEVER `npm install <pkg>` from a project working directory. Verified by acceptance criterion: `git diff --name-only` for `package.json` is empty after probe. |
| T-128b-03-02 | Spoofing | sentinel false-positive (`"yes"` is a common English word) | mitigate | The grep checks BOTH `\byes\b` AND `vigil_external_reply|vigil-spike` in stdout — the tool-invocation marker is much rarer than `"yes"` alone, so a false-positive requires Claude to mention `vigil-spike` AND say `"yes"` without actually invoking the tool. This is unlikely; reviewer can inspect `pathD-fresh-out.txt` to confirm tool was actually called. |
| T-128b-03-03 | Information Disclosure | SDK install stderr (`pathD-sdk-install-err.txt`) | mitigate | Stderr from `npm exec --yes` may contain registry URL + cache paths — no secrets (operator's npm auth is in `~/.npmrc`, not in the install command). The error file is committed only if non-empty; reviewer can inspect before Plan 06 MEASUREMENTS consolidation. |
| T-128b-03-04 | Repudiation | subjective mini-verdict override (Pitfall 5) | mitigate | `MINI_VERDICT` is computed from `$SDK_AVAILABLE` and `$FRESH_PASS` (both mechanically derived); the heredoc generates the transcript from the same vars. No hand-edit. |
| T-128b-03-05 | Denial of Service | npx fetch hangs / SDK install times out | accept | Hard `timeout 300` (5 min) on SDK fetch and `timeout 120` (2 min) on claude probe; both caps inside RESEARCH §"Path D" 60-min cap. INCONCLUSIVE fallback always reachable. |
| T-128b-03-06 | Elevation of Privilege | npx running with operator privileges | accept | Operator's `~/.npm/` cache is operator-owned; npx executes Node code under operator's uid. No setuid, no privilege escalation. Standard development surface. |
</threat_model>

<verification>
1. MCP server stub (`pathD-mcp-server.mjs`) exists with TOSSABLE header + tool name `vigil_external_reply` + env var `VIGIL_BUFFERED_REPLY` + no allowlist constants.
2. Probe script has TOSSABLE header, `set -euo pipefail`, references tool name + env var + `--strict-mcp-config`, no blocked-property-name vars, no `--bare`, no `ALLOWED_REPLIES`.
3. SDK availability is checked via `node -e "import(...)"` first; ad-hoc `npm exec --yes` fallback with `timeout 300`; INCONCLUSIVE branch reached if both fail.
4. Probe runs to completion writing `pathD-mcp-config.json`, `pathD-fresh-out.txt` (if SDK available), `pathD-TRANSCRIPT.md`.
5. TRANSCRIPT mini-verdict matches the empirical branch: DEGRADE / FAIL / INCONCLUSIVE. Mechanically computed.
6. No project package.json files modified.
</verification>

<success_criteria>
- Path D probe runs once, mechanically branching on SDK availability
- Transcript records DEGRADE (predicted per RESEARCH) OR INCONCLUSIVE (SDK fetch failure) OR FAIL (tool-call didn't round-trip) — all three are structurally valid outcomes
- `pathD-mcp-config.json` is committed for reproducibility
- `pathD-fresh-out.txt` captures the raw claude stdout (transcript including any tool invocations) — Plan 05 can quote it
- No project package.json mutated (verified by `git diff --name-only` showing only `.planning/` files)
- Active-session direction is documented analytically with the verbatim "inverted model" framing — no unsafe-primitive attempts
- Plan 05 SPIKE-DECISION can cite `pathD-TRANSCRIPT.md` as the empirical source for Path D's row in the per-path verdict table
- Plan 06 MEASUREMENTS consolidates SDK fetch wallclock + claude probe wallclock + total cost
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-03-SUMMARY.md` recording:
- The MCP server stub creation result (file exists, ~30 LOC actual count, TOSSABLE header present)
- The SDK availability outcome (`SDK_AVAILABLE`, `SDK_MODE`) — globally-resolvable OR npx-fetched OR UNAVAILABLE
- If SDK fetched ad-hoc: the npx fetch wallclock + whether any project package.json was touched (answer must be no per D-A1)
- The probe run wallclock, exit code, and the verbatim `pathD-TRANSCRIPT.md` mini-verdict
- Whether the tool was actually invoked (grep `vigil_external_reply|vigil-spike` in `pathD-fresh-out.txt`) — separate signal from `"yes"` appearing in stdout
- A `## D-A1 isolation audit` section: `git diff --name-only` (or equivalent) confirming NO project `package.json` was modified; only `.planning/` files touched
- A `## Handoff` section noting that Plans 05 (SPIKE-DECISION) + 06 (MEASUREMENTS) consume `pathD-TRANSCRIPT.md` + `pathD-fresh-out.txt`
- **A `## Anomaly check` section** comparing the empirical mini-verdict against the RESEARCH-predicted DEGRADE: if the empirical verdict is `FAIL (fresh-session tool-call did not complete the round-trip)`, flag it as **ANOMALOUS — predicted DEGRADE, observed FAIL** with a sentence on the most likely cause (e.g., MCP stdio handshake didn't return tool result correctly; check `pathD-fresh-out.txt` for handshake errors). Plan 05 SPIKE-DECISION reads this anomaly flag and reflects it in the per-path table's "Source" column. INCONCLUSIVE is NOT anomalous (covered by analytical fallback per RESEARCH).
</output>
