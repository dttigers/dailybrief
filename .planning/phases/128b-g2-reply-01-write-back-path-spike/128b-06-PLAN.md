---
phase: 128b
plan: 06
type: execute
wave: 2
depends_on: [128b-01, 128b-02, 128b-03, 128b-04]
files_modified:
  - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, measurements, evidence-aggregation]
must_haves:
  truths:
    - "128b-MEASUREMENTS.md consolidates per-path wallclock, exit code, evidence-file inventory, and Anthropic API cost into one document"
    - "Each per-path section quotes the verbatim mini-verdict line from the corresponding TRANSCRIPT.md (NOT a re-interpretation; mechanical extraction)"
    - "All per-path transcript outputs are redacted per CONTEXT D-G1 — no `secret`/`token`/`auth`/`bearer`/`apiKey` variable names or values appear in the consolidated log"
    - "Total wallclock + total cost row at the bottom enables Plan 05 SPIKE-DECISION's `## Cost summary` section to be populated by reference"
    - "Plan 04's regression-run.log + spike 001 historical evidence both inventoried (the phase artifact set is self-contained AND cross-links the spike 001 originals)"
  artifacts:
    - path: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md"
      provides: "Consolidated per-path wallclock + cost + evidence-file inventory; cross-links all transcripts and raw outputs"
      contains: "Path B\\|Path A\\|Path D\\|Path E"
  key_links:
    - from: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md §Per-path log"
      to: ".planning/spikes/128b-write-back/evidence/path{A,B,D,E}-TRANSCRIPT.md"
      via: "verbatim quote of each TRANSCRIPT's mini-verdict line"
      pattern: "pathA-TRANSCRIPT|pathB-TRANSCRIPT|pathD-TRANSCRIPT|pathE-TRANSCRIPT"
    - from: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md §Cost summary"
      to: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md §Cost summary"
      via: "Plan 05 reads this MEASUREMENTS file to populate the SPIKE-DECISION cost table"
      pattern: "Cost summary"
---

<objective>
Author `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md` — the consolidated per-path evidence log. This document aggregates wallclock, exit code, evidence-file inventory, and Anthropic API cost across all four empirical paths (A + B + D + E regression) into a single readable record. Plan 05 SPIKE-DECISION's `## Cost summary` section reads from this file. Plan 06 is the LAST plan in Wave 2 before Plan 05 lands in Wave 3.

Purpose: Two reasons MEASUREMENTS lives in its own artifact (per D-A4):
1. **Separation of concerns:** SPIKE-DECISION owns the verdict (mechanical, gated by the verdict-at-TOP invariant); MEASUREMENTS owns the supporting numbers (wallclock, cost, evidence inventory). The two have different review cadences — verdict is non-editable once written; measurements can be refined with re-runs without invalidating the verdict.
2. **Self-containment:** The phase's `.planning/phases/128b-g2-reply-01-write-back-path-spike/` artifact set should be readable in isolation (per D-A4). MEASUREMENTS provides the per-path summary so a future reader doesn't need to open each TRANSCRIPT.md individually.

Output: One markdown file. No code. No tests. The content is purely consolidating numbers + transcript excerpts from Plans 01-04 outputs.

Per CONTEXT D-G1: this file MUST be redaction-safe. The per-path transcripts already comply (Plan 01-04 acceptance criteria forbid blocked-property-name vars). This file MUST NOT introduce any new names matching the patterns; it copies from the source transcripts only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-01-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-02-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-03-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-04-SUMMARY.md
@.planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathE-regression-run.log
@.planning/spikes/001-tmux-write-back-128b/README.md

<interfaces>
<!-- Required document structure: SUMMARY → Per-path sections → Cost summary → Evidence inventory → GUARD-03 audit -->

Per-path section template (one per path):
  ### Path X — <one-line name>
  - **Mini-verdict (verbatim from TRANSCRIPT):** "<line>"
  - **Wallclock:** <seconds> (target: <RESEARCH estimate>)
  - **Exit code:** <int> (0 expected)
  - **Anthropic API cost:** <USD> (per RESEARCH §Cost estimate; haiku rate ≤$0.01-$0.02 per call)
  - **Evidence files:**
    - `.planning/spikes/128b-write-back/evidence/<file>` (size, brief description)
  - **Probe script:** `.planning/spikes/128b-write-back/<script>` (lines of code, last-modified)

Cost summary table (final section):
  | Path | Wallclock | API cost | Notes |
  |------|-----------|----------|-------|
  | B | <actual> | <actual> | <notes> |
  | A | <actual> | <actual> | <notes> |
  | D | <actual> | <actual> | <notes> |
  | E | <actual> | <actual> | regression re-run (spike 001 original wallclock 77s, cost <$0.02) |
  | **Total** | <sum> | <sum> | per RESEARCH §Cost ≤$0.20 expected |

GUARD-03 audit (CONTEXT D-G3 — budget watermark N/A but documented):
  - Total Anthropic API spend: <sum>
  - Auth path: operator's Claude Max OAuth (NOT a multi-user budget; not subject to GUARD-03 `DAILY_AI_BUDGET_EXCEEDED` enum)
  - For forensic reference only

D-G1 redaction audit:
  - This file MUST NOT contain variable assignments matching `^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=`
  - Quoted transcript content must come from source transcripts that already complied (Plan 01-04 acceptance criteria)

Plan ordering — why MEASUREMENTS is wave 2 (between Wave 1 path probes and Wave 3 SPIKE-DECISION):
  - Wave 1: Plans 01-04 produce per-path TRANSCRIPTs
  - Wave 2: Plan 06 consolidates the TRANSCRIPTs into MEASUREMENTS
  - Wave 3: Plan 05 reads MEASUREMENTS + TRANSCRIPTs to author SPIKE-DECISION
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 128b-MEASUREMENTS.md — per-path log + cost summary + evidence inventory</name>
  <files>.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md</files>
  <read_first>
    - .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md (extract: mini-verdict line, run timestamp, probe-script reference; also read the source `pathB-fresh-out.jsonl` if present for size/line count)
    - .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md (extract: mini-verdict line, target SID, SIGUSR1 delivery status, DETECTED state)
    - .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md (extract: mini-verdict line, SDK_AVAILABLE state, FRESH_PASS state, fall-through to INCONCLUSIVE if applicable)
    - .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md (extract: BOTH the Historical PASS and the Regression Re-Run verdict; the Plan 04 regression's wallclock should be in the regression log)
    - .planning/spikes/128b-write-back/evidence/pathE-regression-run.log (extract: total wallclock if measured; if not, note "wallclock not instrumented — refer to spike 001 §Iteration 4 ~77s")
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-01-SUMMARY.md AND 128b-02-SUMMARY.md AND 128b-03-SUMMARY.md AND 128b-04-SUMMARY.md (extract: wallclock numbers and any cost figures recorded by execute-plan SUMMARY authoring)
    - .planning/spikes/001-tmux-write-back-128b/README.md §Cost lines 234-239 (the historical Path E cost reference — included in the cost summary for completeness)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Per-Path Predicted-Verdict Summary" + §"Common Pitfalls" §"Pitfall 8" (D-G1 redaction)
  </read_first>
  <action>
    Author the file with EXACTLY the following structure. All `<placeholder>` markers must be replaced with values read from the source artifacts. Where a number is unavailable, use `(not measured — see <source>)` with a citation rather than a fabricated value.

    ```markdown
    # Phase 128b — MEASUREMENTS

    **Authored:** <YYYY-MM-DD>
    **Phase:** 128b — G2-REPLY-01 write-back path spike
    **Requirement:** G2-REPLY-01
    **Consolidates:** per-path empirical evidence from Plans 01-04 (paths B, A, D, E-regression)

    > Per CONTEXT D-A4: this file is one of the spike's seven artifacts. It owns the SUPPORTING NUMBERS (wallclock, cost, evidence-file inventory). The VERDICT and Phase 133 scope-lock live in `128b-SPIKE-DECISION.md` (authored in Plan 05). The two documents have different review cadences — verdict is non-editable once written; measurements can be refined without invalidating the verdict.

    ## Summary

    | Path | Mini-verdict (verbatim from TRANSCRIPT) | Wallclock | API cost | Plan |
    |------|------------------------------------------|-----------|----------|------|
    | B | <verbatim mini-verdict from pathB-TRANSCRIPT.md> | <s> | <USD> | 128b-01 |
    | A | <verbatim mini-verdict from pathA-TRANSCRIPT.md> | <s> | <USD> | 128b-02 |
    | D | <verbatim mini-verdict from pathD-TRANSCRIPT.md> | <s> | <USD> | 128b-03 |
    | E (regression) | <verbatim regression mini-verdict from pathE-TRANSCRIPT.md> | <s> | <USD> | 128b-04 |
    | E (spike 001 historical) | PASS (per spike 001 §Results) | 77s | <$0.02 | spike 001 (pre-128b) |

    **Total empirical wallclock (Plans 01-04):** <sum_s>
    **Total Anthropic API spend (Plans 01-04 + spike 001):** <sum_usd>

    Per RESEARCH §Cost and §"Per-Path Predicted-Verdict Summary": expected total ≤$0.20; expected total wallclock ≤4h (vs CONTEXT D-O4 per-path cap of 3h). Actual aggregate above.

    ---

    ## Per-path log

    ### Path B — claude -p --input-format stream-json (D-O1 ordering: cheapest first)

    - **Mini-verdict (verbatim from TRANSCRIPT):** "<copy the mini-verdict line from pathB-TRANSCRIPT.md>"
    - **Wallclock:** <seconds from Plan 01 SUMMARY> (RESEARCH estimate: 30 min cap; actual: <s>)
    - **Exit code:** <int — read from Plan 01 SUMMARY>
    - **Anthropic API cost:** <USD — derived from haiku rate × turn count; if uncertain, use $0.005 per spike 001 §Cost L3 reference>
    - **Sentinel:** "1337" (computed from prompt `Compute seven multiplied by one hundred ninety-one. Reply with only the resulting integer.`)
    - **Sentinel detection:** <yes/no — derived from `grep -E '1337' pathB-fresh-out.jsonl`; if yes, the fresh-session step 3 ✓; if no, fresh-session step 3 ✗>
    - **Active-session attempt:** SKIPPED BY DESIGN — `claude -p` is print-and-exit; no input-channel attach surface (RESEARCH §"Path B" + CONTEXT D-O1 path (b))
    - **Evidence files:**
      - `.planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl` — full stream-json transcript (raw)
      - `.planning/spikes/128b-write-back/evidence/pathB-fresh-err.txt` — stderr (auth, permission, network errors if any)
      - `.planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md` — mini-verdict (mechanical)
    - **Probe script:** `.planning/spikes/128b-write-back/pathB-stream-json.sh`

    ### Path A — JSONL append + IPC (D-O1 ordering: 2nd in plan)

    - **Mini-verdict (verbatim from TRANSCRIPT):** "<copy the mini-verdict line from pathA-TRANSCRIPT.md>"
    - **Wallclock:** <seconds from Plan 02 SUMMARY> (RESEARCH estimate: 1-2h; actual: <s>)
    - **Exit code:** <int — read from Plan 02 SUMMARY>
    - **Anthropic API cost:** <USD — depends on whether claude --resume produced any output before the failed-re-read pattern was observed; estimate $0.01-$0.05>
    - **D-T5 clobber-protect:** target SID was `<TARGET_SID from Plan 02 SUMMARY>` (NOT equal to `$CLAUDE_SESSION_ID` if set); operations confined to `$COPYDIR` under `mktemp -d`; live corpus unchanged
    - **SIGUSR1 delivery:** <yes (PID <pid>) / no — pgrep empty — from Plan 02 SUMMARY>
    - **Sentinel detection (30-iteration poll):** <DETECTED 0 expected (FAIL) / DETECTED 1 unexpected (PASS) — from Plan 02 SUMMARY>
    - **Unsafe primitives attempted:** NONE (CONTEXT D-V3 ban on ptrace + /proc/<pid>/fd/0 writes)
    - **Evidence files:**
      - `.planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt` — last 5 lines of the copied JSONL after append (step 2 ✓ proof)
      - `.planning/spikes/128b-write-back/evidence/pathA-final-pane.txt` — final tmux pane capture (forensic post-mortem)
      - `.planning/spikes/128b-write-back/evidence/pathA-at-detection.txt` — at-detection snapshot (only present if step 3 ✓ unexpectedly; absent on predicted FAIL)
      - `.planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md` — mini-verdict (mechanical)
    - **Probe script:** `.planning/spikes/128b-write-back/pathA-jsonl-append.sh`

    ### Path D — MCP server hook (D-O1 ordering: 3rd in plan)

    - **Mini-verdict (verbatim from TRANSCRIPT):** "<copy the mini-verdict line from pathD-TRANSCRIPT.md>"
    - **Wallclock:** <seconds from Plan 03 SUMMARY> (RESEARCH estimate: 30-60 min including SDK ad-hoc fetch; actual: <s>)
    - **Exit code:** <int — read from Plan 03 SUMMARY>
    - **Anthropic API cost:** <USD — typically ≤$0.05 for one fresh `claude -p` invocation with one tool call>
    - **SDK availability:** <SDK_AVAILABLE state + SDK_MODE from Plan 03 SUMMARY> (`globally-resolvable` / `npx-fetched (ad-hoc)` / `UNAVAILABLE`)
    - **D-A1 isolation audit:** <result from Plan 03 SUMMARY §"D-A1 isolation audit"> — `npx --package` ad-hoc fetch; project package.json files NOT modified
    - **Tool invocation:** <yes — `vigil_external_reply` was called by Claude / no — only `yes` appeared in stdout / N/A — SDK unavailable, INCONCLUSIVE branch — from Plan 03 SUMMARY>
    - **Active-session direction:** documented analytically as STRUCTURAL FAIL — MCP tools are tools Claude CALLS, not channels that PUSH to Claude; Vigil cannot force a tool call mid-turn (RESEARCH §"Path D" + CONTEXT line 222)
    - **Evidence files:**
      - `.planning/spikes/128b-write-back/evidence/pathD-mcp-config.json` — the mcp-config wiring the server stub
      - `.planning/spikes/128b-write-back/evidence/pathD-fresh-out.txt` — raw claude stdout including tool invocations
      - `.planning/spikes/128b-write-back/pathD-mcp-server.mjs` — the ~30 LOC stdio MCP server stub
      - `.planning/spikes/128b-write-back/evidence/pathD-sdk-install-err.txt` — (only present if SDK fetch failed)
      - `.planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md` — mini-verdict (mechanical, branches on SDK_AVAILABLE)
    - **Probe script:** `.planning/spikes/128b-write-back/pathD-mcp-probe.sh`

    ### Path E — tmux send-keys (★ — pre-validated in spike 001; regression re-run in Plan 04)

    - **Mini-verdict (verbatim from TRANSCRIPT, Regression row):** "<copy the Regression Re-Run mini-verdict line from pathE-TRANSCRIPT.md>"
    - **Mini-verdict (Historical, from spike 001 README §Results):** "PASS (all 4 D-V1 steps ✓)"
    - **Wallclock (regression):** <seconds from Plan 04 SUMMARY> (spike 001 §Iteration 4 reference: 77s end-to-end)
    - **Wallclock (historical):** 77s (spike 001 §Iteration 4)
    - **Exit code (regression):** <int — read from Plan 04 SUMMARY $REGRESSION_EXIT>
    - **Regression PASS detected:** <0 / 1 — Plan 04 SUMMARY $REGRESSION_PASS>
    - **Anthropic API cost (regression):** ~$0.01 (one fresh L4 round-trip at haiku rate)
    - **Anthropic API cost (historical):** <$0.02 (spike 001 §Cost — L1+L2+L3+L4 aggregate)
    - **Mechanism:** `tmux send-keys -t <session> Enter` from a non-TTY writer; tmux server (operator-uid, 0700 socket) writes to the pty fd it already owns. No ptrace, no /proc/<pid>/fd/0, no preflight FD redirect.
    - **Surfaced constraint (Phase 133 carry-forward):** Claude Code must be launched **inside a tmux pane** for Path E to work (spike 001 README §"Surfaced constraint" lines 175-178)
    - **Evidence files (phase-local copies):**
      - `.planning/spikes/128b-write-back/evidence/pathE-regression-run.log` — full stdout+stderr of the re-run
      - `.planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt` — copy of spike 001's `needs_input` dialog evidence
      - `.planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt` — copy of spike 001's 60s-health-probe transcript (`221`)
      - `.planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt` — copy of spike 001's unique-marker proof
      - `.planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md` — Historical PASS + Regression mini-verdicts
    - **Evidence files (spike 001 originals — UNCHANGED):**
      - `.planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt`
      - `.planning/spikes/001-tmux-write-back-128b/evidence/L4-health-check-snapshot.txt`
      - `.planning/spikes/001-tmux-write-back-128b/evidence/L4-tool-output-marker.txt`
      - `.planning/spikes/001-tmux-write-back-128b/evidence/L3-claude-response-snapshot.txt` (L3 ancestor; not load-bearing for L4 PASS)
    - **Regression script:** `.planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh` (re-run by Plan 04; spike 001 owns)

    ### Path C — named-pipe / FIFO (NOT empirically tested — analytical-only)

    - **Verdict source:** RESEARCH §"Path C" recommendation + spike 001 README §"Position vs. the 4 enumerated 128b paths"
    - **Analytical mini-verdict:** "INCONCLUSIVE — covered analytically by Path E (structural refinement)"
    - **Wallclock:** 0 (skipped per CONTEXT D-O4 wallclock cap + RESEARCH §"Open Questions §2" recommendation)
    - **API cost:** $0
    - **Rationale:** Path E (`tmux send-keys`) is structurally a clean refinement of Path C — the tmux server is a pre-existing, well-tested, user-uid launcher that owns the pty FD. Vigil doesn't have to invent the launcher (C1) or escalate privileges to ptrace (C2). The "3 of 4" success criterion is satisfied by A + B + D empirical + Path E historical, without needing Path C.

    ---

    ## Cost summary

    | Path | Wallclock | API cost | Notes |
    |------|-----------|----------|-------|
    | B | <s> | <USD> | Cheapest probe per D-O1; fresh-session-only PASS predicted ⇒ DEGRADE |
    | A | <s> | <USD> | Clobber-protected operation on COPY of corpus session per D-T5 |
    | D | <s> | <USD> | Ad-hoc MCP SDK via npx; D-A1 isolation (no project package.json mutation) |
    | E (regression) | <s> | ~$0.01 | spike 001 L4 re-run; copies of evidence preserved in phase artifact set |
    | E (spike 001) | 77s | <$0.02 | Historical reference; preserved record at sibling spike dir |
    | C | 0 | $0 | NOT tested — analytical-only per RESEARCH §"Open Questions §2" |
    | **Total (empirical)** | <sum_s> | <sum_usd> | per RESEARCH §Cost ≤$0.20 expected |

    ## Evidence file inventory

    Phase-local artifact set (per CONTEXT D-A4):
    - `.planning/phases/128b-g2-reply-01-write-back-path-spike/`
      - `128b-CONTEXT.md` (locked)
      - `128b-DISCUSSION-LOG.md` (locked)
      - `128b-RESEARCH.md` (locked)
      - `128b-VALIDATION.md` (locked)
      - `128b-NN-PLAN.md` × N (plans 01-08)
      - `128b-NN-SUMMARY.md` × N (plan execution records)
      - `128b-MEASUREMENTS.md` (this file)
      - `128b-SPIKE-DECISION.md` (Plan 05 — verdict at TOP)
      - `60s-demo.mp4` OR Loom URL recorded in SPIKE-DECISION header (Plan 08 C-2)
    - `.planning/spikes/128b-write-back/` (NEW per CONTEXT D-A1)
      - `README.md` + `pathA-jsonl-append.sh` + `pathB-stream-json.sh` + `pathD-mcp-probe.sh` + `pathD-mcp-server.mjs`
      - `evidence/` (per-path raw outputs + at-detection snapshots + TRANSCRIPT mini-verdicts + Path E regression artifacts)
    - `.planning/spikes/001-tmux-write-back-128b/` (PRE-EXISTING — referenced only)
      - `README.md` (verdict VALIDATED-2026-05-14; immutable)
      - `L1-...sh, L2-...sh, L3-...sh, L4-...sh` (canonical Path E probe scripts)
      - `evidence/L3-...txt + L4-*.txt` (canonical Path E evidence; phase-local copies under 128b-write-back/evidence/)
    - `.planning/spikes/MANIFEST.md` (spike landscape; verdict cells for 001 + 128b-write-back)

    ## GUARD-03 budget audit (CONTEXT D-G3)

    - **Total Anthropic API spend across all 128b paths:** <sum_usd>
    - **Auth path:** operator's Claude Max OAuth (NOT a multi-user budget; NOT subject to GUARD-03 `DAILY_AI_BUDGET_EXCEEDED` enum)
    - **GUARD-03 applicability:** N/A — documented for forensic reference per RESEARCH §Cost
    - **Budget headroom remaining:** N/A (single-user OAuth; no per-user daily cap applies)

    ## D-G1 redaction audit

    - This file's variable references + transcript quotes are inherited from per-path TRANSCRIPT.md files; each TRANSCRIPT was authored by Plans 01-04 scripts whose acceptance criteria forbid blocked-property-name variable assignments (CONTEXT D-G1 + `vigil-core/src/analytics/posthog.ts:32` BLOCKED_PROPERTY_NAMES).
    - Verified: no occurrences of `^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=` in this file or its source transcripts.

    ## Cited sources

    - per-path TRANSCRIPTs: `.planning/spikes/128b-write-back/evidence/path{A,B,D,E}-TRANSCRIPT.md`
    - per-plan SUMMARYs: `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-{01,02,03,04}-SUMMARY.md`
    - spike 001 README §Cost lines 234-239 + §Results lines 141-157
    - RESEARCH §"Per-Path Predicted-Verdict Summary" + §"Cost"
    - CONTEXT D-A4 (artifact list) + D-G1 (redaction) + D-G3 (budget) + D-O4 (wallclock cap)
    ```

    Substitution rules:
    - Read each Plan 01-04 SUMMARY to extract wallclock + cost; if a value is missing, write `(not measured — see <SUMMARY-link>)` instead of fabricating
    - Quote each per-path TRANSCRIPT's mini-verdict VERBATIM (use double quotes around the literal line); do NOT paraphrase
    - The Summary table at the TOP is a high-level scan view; the detailed Per-path log below is the canonical record
    - The Total row sums the Wallclock + API cost columns arithmetically

    Per CONTEXT D-G3: even though GUARD-03 budget watermark is N/A for this single-user spike, document the total spend for forensic reference (operator can audit Claude Max usage at the dashboard).
  </action>
  <verify>
    <automated>test -f .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## Summary" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## Per-path log" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "### Path B" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "### Path A" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "### Path D" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "### Path E" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "### Path C" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## Cost summary" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## Evidence file inventory" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## GUARD-03 budget audit" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -q "## D-G1 redaction audit" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && ! grep -E "(TOKEN|AUTH|SECRET|BEARER|APIKEY)=" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && grep -qE 'spike 001|001-tmux-write-back-128b' .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md && echo "MEASUREMENTS structure verified"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-MEASUREMENTS.md` exists
    - Contains the 5 H2 sections: `## Summary`, `## Per-path log`, `## Cost summary`, `## Evidence file inventory`, `## GUARD-03 budget audit`, `## D-G1 redaction audit`
    - `## Per-path log` contains 5 H3 path sections: `### Path B`, `### Path A`, `### Path D`, `### Path E`, `### Path C` — in that order
    - Each empirical path section (B, A, D, E) contains: Mini-verdict (verbatim from TRANSCRIPT), Wallclock, Exit code, Anthropic API cost, Evidence files list, Probe script reference
    - Path E section contains BOTH Regression and Historical sub-rows
    - Path C section is marked NOT empirically tested with rationale citing RESEARCH §"Open Questions §2"
    - `## Cost summary` table contains 6 rows (B, A, D, E-regression, E-spike-001, C) + Total row
    - `## Evidence file inventory` lists phase-local artifacts AND spike 001 cross-referenced files
    - File does NOT contain any blocked-property-name patterns (`^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=` returns 0 matches per D-G1)
    - File cites spike 001 by at least one of: `spike 001` OR `001-tmux-write-back-128b` (cross-link present)
    - All `<placeholder>` markers from the template are replaced with values OR with `(not measured — see <source>)` (no raw placeholder strings remain)
    - Total wallclock row arithmetic correct (sum of the per-path Wallclock cells)
    - Total cost row ≤$0.20 (per RESEARCH §Cost expected ceiling; if exceeded, write the actual value AND a one-line note explaining the deviation)
  </acceptance_criteria>
  <done>
    `128b-MEASUREMENTS.md` consolidates per-path empirical evidence into a single readable record. Each path's mini-verdict is quoted verbatim from the source TRANSCRIPT (no re-interpretation). Cost summary table enables Plan 05 SPIKE-DECISION's `## Cost summary` to be populated by reference (or by inline copy). Evidence inventory makes the phase artifact set self-contained per CONTEXT D-A4. GUARD-03 + D-G1 audits documented for forensic posterity even though both are N/A for this spike's scope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-path TRANSCRIPTs → MEASUREMENTS aggregation | Mini-verdict lines copied verbatim; reviewer can `diff` against TRANSCRIPT source |
| MEASUREMENTS → SPIKE-DECISION cost section (Plan 05) | One-way reference; Plan 05 reads this file at SPIKE-DECISION authoring time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-06-01 | Information Disclosure | per-path transcripts may contain command-line strings or environment hints | mitigate | Per-path TRANSCRIPTs (the sources for this aggregation) already comply with D-G1 — Plans 01-04 acceptance criteria forbid blocked-property-name variable assignments. This file is a pure aggregation; no new content is introduced. Reviewer can `grep -E '^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=' 128b-MEASUREMENTS.md` and confirm zero matches. |
| T-128b-06-02 | Repudiation | aggregated cost figures could be inflated or deflated relative to actual API spend | mitigate | Each cost cell is sourced from a Plan 01-04 SUMMARY (which in turn references the probe wallclock + token count). The total row is arithmetic; reviewer can sum the per-path cells and confirm. If a cell is uncertain, the template requires `(not measured — see <source>)` rather than a fabricated value. |
| T-128b-06-03 | Tampering | post-hoc edits to MEASUREMENTS that diverge from per-path TRANSCRIPTs | accept | Unlike SPIKE-DECISION's verdict-at-TOP invariant, MEASUREMENTS can be refined (re-runs may produce new numbers). The TRANSCRIPT mini-verdict is the authoritative per-path signal; MEASUREMENTS aggregates them. Plan 05's verdict computation reads the TRANSCRIPTs directly, not this file, so edits here cannot change the verdict. |
| T-128b-06-04 | Information Disclosure | spike 001 evidence cross-references could leak if sibling dir contains sensitive content | accept | Spike 001 evidence files were committed by the spike 001 verdict-validation process; their content is already public in the planning repo. MEASUREMENTS only references the file paths and quotes the canonical `221` (health probe) and `L4-TOOL-RAN-<id>` (unique marker) — no new disclosure. |
</threat_model>

<verification>
1. MEASUREMENTS.md exists with the 6 H2 sections + 5 H3 path sub-sections.
2. Each empirical path section contains the verbatim TRANSCRIPT mini-verdict, wallclock, exit code, cost, evidence list.
3. Path E section contains BOTH Regression + Historical rows; Path C section is analytical-only.
4. Cost summary table sums correctly to ≤$0.20 (or documents deviation).
5. Evidence file inventory cross-links spike 001 originals.
6. D-G1 redaction audit shows zero blocked-property-name matches.
</verification>

<success_criteria>
- 128b-MEASUREMENTS.md exists with consolidated per-path log
- Each path's mini-verdict is quoted verbatim from the corresponding TRANSCRIPT.md
- Total wallclock + cost are summed correctly; ≤$0.20 expected per RESEARCH
- Evidence file inventory enables future readers to find any artifact in one document
- Plan 05 SPIKE-DECISION's `## Cost summary` can read this file's table and copy verbatim
- D-G1 redaction is preserved (no blocked-property-name patterns introduced)
- GUARD-03 budget audit documented even though N/A (forensic reference)
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-06-SUMMARY.md` recording:
- The total wallclock and total API cost for the spike (sum of Plans 01-04 per-path numbers + spike 001 historical)
- Whether the actual total cost is ≤$0.20 (RESEARCH expectation) — if exceeded, the per-path attribution that drove the overage
- A `## Source-quote audit` section: confirmation that each per-path mini-verdict in MEASUREMENTS matches the literal line in the corresponding TRANSCRIPT (verified by `grep -F <line> evidence/path*-TRANSCRIPT.md`)
- A `## Handoff to Plan 05` section: which sections Plan 05 SPIKE-DECISION should read (the `## Cost summary` table + `## Evidence file inventory` cross-references)
</output>
