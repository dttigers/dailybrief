---
phase: 128b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/128b-write-back/README.md
  - .planning/spikes/128b-write-back/pathB-stream-json.sh
  - .planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl
  - .planning/spikes/128b-write-back/evidence/pathB-fresh-err.txt
  - .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md
  - .planning/spikes/MANIFEST.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, path-b, stream-json, claude-code]
must_haves:
  truths:
    - "Path B (claude -p --input-format stream-json) is empirically tested against a fresh session and the result is recorded in a transcript file"
    - "The probe writes a non-prompt-echoed sentinel (1337 from 'seven multiplied by one hundred ninety-one') so D-V1 step 3 cannot be a false positive (Pitfall 1)"
    - "The active-session question is DOCUMENTED as N/A (claude -p is print-and-exit by design) — this is the textbook D-V2 case, not a missing test"
    - "The probe creates the .planning/spikes/128b-write-back/ directory per CONTEXT D-A1 (the new spike dir, separate from spike 001's tmux validation)"
    - "Per-path predicted verdict matches RESEARCH: PASS-fresh / FAIL-active ⇒ DEGRADE overall (recorded for Plan 05 SPIKE-DECISION aggregation)"
    - "TOSSABLE header present on probe script per CONTEXT D-A1 + spike CONVENTIONS.md"
  artifacts:
    - path: ".planning/spikes/128b-write-back/README.md"
      provides: "New spike-dir landing page; cross-references spike 001 (Path E) and the phase artifacts"
      contains: "spike 001"
    - path: ".planning/spikes/128b-write-back/pathB-stream-json.sh"
      provides: "Empirical probe script for Path B (claude stream-json)"
      contains: "PHASE 128b SPIKE — TOSSABLE"
    - path: ".planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl"
      provides: "Raw stream-json output from a fresh claude -p invocation including the assistant turn that contains 1337"
      contains: "1337"
    - path: ".planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md"
      provides: "Mini-verdict per Pattern 2 (D-V1 four-step checklist applied to Path B); records fresh PASS + active N/A ⇒ DEGRADE"
      contains: "DEGRADE"
    - path: ".planning/spikes/MANIFEST.md"
      provides: "Updated spike landscape with a row for the new 128b-write-back spike directory"
      contains: "128b-write-back"
  key_links:
    - from: ".planning/spikes/128b-write-back/pathB-stream-json.sh"
      to: "claude CLI (~/.local/bin/claude 2.1.141)"
      via: "claude -p --input-format stream-json --output-format stream-json --model haiku"
      pattern: "input-format stream-json"
    - from: ".planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl"
      to: ".planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md"
      via: "TRANSCRIPT cites the grep result against the output JSONL (1337 present ⇒ fresh PASS column ✓)"
      pattern: "1337"
---

<objective>
Empirically probe Path B (`claude -p --input-format stream-json --output-format stream-json`) — the canonical advertised SDK streaming surface — against a fresh subprocess. Record the empirical result with a false-positive-resistant sentinel (1337 from a compute-prompt, NOT a string echoed from the prompt). Document the active-session question as structurally N/A (`-p` is print-and-exit by design — no input channel exists on an already-running session). This satisfies one of the three enumerated empirical paths required by G2-REPLY-01 success criterion 1 ("at least 3 of (a)/(b)/(c)/(d)").

Purpose: The phase's overall verdict is already mechanically PASS per D-V4 (spike 001 validated Path E). What this plan delivers is the letter-of-the-law empirical record for Path B so that the per-path table in Plan 05's SPIKE-DECISION.md has a real `DEGRADE (fresh-only)` row (per RESEARCH predicted verdict). The probe is the cheapest of the three (~30 min per RESEARCH wallclock estimate) so this plan also establishes the new `.planning/spikes/128b-write-back/` directory and README (per CONTEXT D-A1) for plans 02-04 to populate.

Output: New spike directory + README + probe script + raw stream-json output + per-path transcript with mechanical D-V1 mini-verdict. MANIFEST.md updated with a row for the new spike dir.

Per RESEARCH "Path-by-Path Methodology" §"Path B": predicted verdict is PASS (fresh) / FAIL (active-session) ⇒ DEGRADE overall (D-V2 bullet 1: "Round-trip works ONLY on fresh `claude -p` sessions, NOT mid-interactive-session"). Plan 05 SPIKE-DECISION aggregates this with Path A FAIL, Path D DEGRADE, and Path E PASS to produce overall PASS (D-V4 max).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/MANIFEST.md
@.planning/spikes/001-tmux-write-back-128b/README.md

<interfaces>
<!-- Locked from CONTEXT.md D-V1 and RESEARCH.md §"Path B" methodology -->
<!-- Executor uses these directly — no additional codebase exploration needed -->

D-V1 four-step PASS gate (each column gets ✓ or ✗ mechanically):
  step 1 — reply originates from a separate non-TTY writer process
  step 2 — string reaches the target Claude Code session's input channel
  step 3 — Claude processes the string as the next user turn (Claude responds to it)
  step 4 — session continues healthy ≥60s (no crash/hang/error state)

D-V2 DEGRADE gate (any of):
  - Round-trip works ONLY on fresh `claude -p` sessions, NOT mid-interactive-session  ← THIS IS PATH B
  - Works mid-session BUT requires operator to press ENTER once after injection
  - Works for fresh-session BUT cleanup is partial

RESEARCH §"Path B" predicted verdict: PASS (fresh) / FAIL (active) ⇒ DEGRADE overall.

Sentinel pattern (Pitfall 1 — DO NOT put sentinel value in prompt):
  PROMPT="Compute seven multiplied by one hundred ninety-one. Reply with only the resulting integer."
  EXPECTED="1337"   # NOT in PROMPT — would-be false positive if it were

Stream-json input shape (from RESEARCH §"Path B"):
  {"type":"user","message":{"role":"user","content":"<text>"}}

Stream-json output rows contain assistant turns; grep -E '1337' against the raw output JSONL is sufficient.

claude --bare is FORBIDDEN (Pitfall 3 — strips OAuth/keychain; operator uses Claude Max).
Use --model haiku for cost control (~$0.005 per turn per spike 001 §Cost).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spike directory + README + MANIFEST row</name>
  <files>.planning/spikes/128b-write-back/README.md, .planning/spikes/128b-write-back/evidence/.gitkeep, .planning/spikes/MANIFEST.md</files>
  <read_first>
    - .planning/spikes/CONVENTIONS.md (full file — patterns this dir must follow: tossable headers, clobber-protection, false-positive-resistant sentinels, snapshot-at-detection, evidence preservation)
    - .planning/spikes/MANIFEST.md (existing file — see the table at the bottom; you'll add a row for the new 128b-write-back dir following the same column shape: # / Name / Type / Validates / Verdict / Tags)
    - .planning/spikes/001-tmux-write-back-128b/README.md (the sibling spike that lives at the OLD naming convention 001-tmux-write-back-128b/; this dir will follow CONTEXT D-A1 verbatim name 128b-write-back/ instead; cross-link both directions)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-A1" (verbatim directory name)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Open Questions §1" (the planner-resolved decision to CREATE NEW dir, not extend spike 001)
  </read_first>
  <action>
    Create the directory `.planning/spikes/128b-write-back/` and within it: an `evidence/` subdirectory (committed via a `.gitkeep` placeholder file containing the single line `# evidence files preserved at-detection per CONVENTIONS.md §"Snapshot at moment of detection"`) and a `README.md` with the structure below. Do not invent the contents of the as-yet-unwritten Path A / D probe scripts — those are owned by plans 02 and 03; this README references them by name only.

    README.md MUST contain (in this order):
    1. A markdown frontmatter block with these exact keys: `spike: 128b-write-back`, `name: 128b-write-back`, `type: phase-spike`, `validates: "Empirical record for G2-REPLY-01 enumerated paths (a) JSONL append, (b) stream-json, (d) MCP — completing the 3-of-4 success criterion. Path E (tmux send-keys) is owned by sibling spike 001-tmux-write-back-128b/."`, `verdict: in-progress`, `related: [phase-128b, spike-001-tmux-write-back-128b]`, `tags: [128b, write-back, jsonl, stream-json, mcp, claude-code]`.
    2. A `# Spike 128b: Write-back path empirical sweep (paths A/B/D)` H1.
    3. A `## Sibling spike` section linking `[spike 001 — tmux-write-back-128b](../001-tmux-write-back-128b/README.md)` with one sentence: "Path E was VALIDATED on 2026-05-14 in spike 001. This dir holds the per CONTEXT D-A1 empirical record for the four enumerated paths (a)/(b)/(c)/(d) that completes the G2-REPLY-01 'at least 3 of 4' success criterion."
    4. A `## Layout` section showing the file tree: `README.md`, `pathB-stream-json.sh`, `pathA-jsonl-append.sh` (created in plan 02), `pathD-mcp-probe.sh` + `pathD-mcp-server.mjs` (created in plan 03), and `evidence/`.
    5. A `## Phase 128b artifacts` section linking the phase dir: `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-{CONTEXT,RESEARCH,VALIDATION,SPIKE-DECISION,MEASUREMENTS}.md`.
    6. A `## How to run` section: `bash .planning/spikes/128b-write-back/pathB-stream-json.sh` (and analogous lines for A and D, noting they are created in their respective plans).
    7. A `## Conventions` section linking `[../CONVENTIONS.md](../CONVENTIONS.md)` with one sentence summarising the four mandates (TOSSABLE header, unique tmux session names, computed sentinels, snapshot-at-detection).

    Then update `.planning/spikes/MANIFEST.md`: append ONE row to the table at the bottom (after row 001) with: # = `128b-WB`, Name = `128b-write-back`, Type = `phase-spike`, Validates = `Empirical record for G2-REPLY-01 enumerated paths (a) JSONL append, (b) stream-json, (d) MCP`, Verdict = `🚧 in-progress`, Tags = `128b, write-back, jsonl, stream-json, mcp`. Do NOT remove or reorder the existing 001 row.
  </action>
  <verify>
    <automated>test -d .planning/spikes/128b-write-back && test -d .planning/spikes/128b-write-back/evidence && test -f .planning/spikes/128b-write-back/README.md && test -f .planning/spikes/128b-write-back/evidence/.gitkeep && grep -q "spike-001-tmux-write-back-128b\|001-tmux-write-back-128b" .planning/spikes/128b-write-back/README.md && grep -q "128b-write-back" .planning/spikes/MANIFEST.md && grep -q "001 " .planning/spikes/MANIFEST.md && echo "DIR + README + MANIFEST verified"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/spikes/128b-write-back/` exists with subdirectory `evidence/`
    - File `.planning/spikes/128b-write-back/evidence/.gitkeep` exists and contains the line `# evidence files preserved at-detection per CONVENTIONS.md §"Snapshot at moment of detection"`
    - File `.planning/spikes/128b-write-back/README.md` exists and contains all seven sections listed above in order
    - README.md frontmatter `verdict:` field reads `in-progress` (not VALIDATED — that's spike 001's posture)
    - README.md `## Sibling spike` section cross-links the relative path `../001-tmux-write-back-128b/README.md`
    - `.planning/spikes/MANIFEST.md` contains a NEW row whose Name column is `128b-write-back` AND still contains the original `001 | tmux-write-back-128b | standard | …` row (no destructive edits)
    - Grep `grep -c '| 128b-WB \| 128b-write-back \|' .planning/spikes/MANIFEST.md` returns >= 1 OR the row is present with the equivalent column shape
  </acceptance_criteria>
  <done>
    New spike directory created per CONTEXT D-A1; README cross-links spike 001 in both directions (README §Sibling spike here + MANIFEST table row); plans 02 and 03 can drop their probe scripts in without further scaffolding.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write + run pathB-stream-json.sh probe; capture evidence + transcript</name>
  <files>.planning/spikes/128b-write-back/pathB-stream-json.sh, .planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl, .planning/spikes/128b-write-back/evidence/pathB-fresh-err.txt, .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md</files>
  <read_first>
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Path B" lines 299-335 (the verbatim concrete probe script from RESEARCH — DO NOT re-derive; adapt with bail-on-error, redaction-safe variable names per D-G1, and a self-contained transcript-authoring tail block)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-G1" (variable names MUST NOT be `secret`/`token`/`auth`/`bearer`/`apiKey`; use `sessionId`/`pid`/`procName`/`outFile`/`errFile`)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Pitfall 1" + §"Pitfall 3" + §"Pitfall 5" + §"Pitfall 6" (false-positive sentinel; --bare strips OAuth; mechanical verdict — do NOT subjectively decide; do NOT confuse fresh-PASS with active-PASS)
    - .planning/spikes/CONVENTIONS.md §"Tossable headers" (exact header text required)
    - .planning/spikes/001-tmux-write-back-128b/L3-claude-basic.sh AND .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh (pattern source for snapshot-at-detection loops, trap-cleanup, set -euo pipefail discipline — DO NOT extend either of those scripts; this is a NEW script in a NEW dir per CONTEXT D-A1)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Architectural Responsibility Map" row "Stream-json input (Path B target)" (confirms `claude -p --input-format stream-json` is a fresh subprocess — there is NO documented active-session attach surface; the script must document this as a NEGATIVE finding, not attempt ptrace/proc-fd attacks per D-V3 unsafe-primitive ban)
  </read_first>
  <action>
    Create the script `.planning/spikes/128b-write-back/pathB-stream-json.sh` (mode 0755) using the EXACT structure below (line-by-line; do not paraphrase the TOSSABLE header). This script MUST run successfully on this Linux dev workstation in ≤2 minutes wallclock (RESEARCH estimate: 30 min INCLUDING transcript authoring — the script itself is <60s claude wallclock + tiny overhead).

    Script structure (each `// ...` is a structural comment for you, not literal output):

    1. Shebang: `#!/usr/bin/env bash`
    2. TOSSABLE header (THREE lines, verbatim per CONVENTIONS.md):
       `# PHASE 128b SPIKE — TOSSABLE. Phase 133 owns G2-REPLY productionization;`
       `# this file is spike-only and SHOULD BE DELETED after the verdict is committed.`
       `# Path B: claude -p --input-format stream-json --output-format stream-json (D-O1 step 1)`
    3. `set -euo pipefail`
    4. `EVIDENCE_DIR=".planning/spikes/128b-write-back/evidence"` (relative to repo root — script is run from repo root)
    5. `SCRATCH=$(mktemp -d -t spike-128b-B-XXXXXX)`
    6. `trap 'rm -rf "$SCRATCH"' EXIT` (NOT a tmux trap — Path B uses no tmux per RESEARCH; it's claude -p only)
    7. A descriptive echo: `echo "[Path B probe] fresh-session stream-json round-trip ($(date -Iseconds))"`
    8. The fresh-session probe (RESEARCH lines 312-314 verbatim except for output redirection — adapt to redirect into the COMMITTED `$EVIDENCE_DIR/pathB-fresh-out.jsonl` and `$EVIDENCE_DIR/pathB-fresh-err.txt`, with `mkdir -p "$EVIDENCE_DIR"` first):
       ```
       mkdir -p "$EVIDENCE_DIR"
       printf '%s\n' '{"type":"user","message":{"role":"user","content":"Compute seven multiplied by one hundred ninety-one. Reply with only the resulting integer."}}' \
         | claude -p --input-format stream-json --output-format stream-json --model haiku \
         > "$EVIDENCE_DIR/pathB-fresh-out.jsonl" 2> "$EVIDENCE_DIR/pathB-fresh-err.txt"
       ```
    9. Sentinel check (Pitfall 1 — sentinel 1337 must NOT appear in the prompt text above; verify by grepping the prompt arg of the probe doesn't contain 1337 first):
       ```
       if grep -qE '1337' "$EVIDENCE_DIR/pathB-fresh-out.jsonl"; then
         FRESH_PASS=1
         echo "[Path B] fresh-session sentinel 1337 detected ⇒ step 3 ✓"
       else
         FRESH_PASS=0
         echo "[Path B] fresh-session sentinel NOT detected ⇒ step 3 ✗ (unexpected — see RESEARCH §Path B Predicted verdict)"
       fi
       ```
    10. Active-session NEGATIVE finding (do NOT attempt — document why):
        ```
        echo "[Path B] active-session attempt: SKIPPED BY DESIGN."
        echo "[Path B] rationale: claude --help confirms -p is single-shot (print-and-exit)."
        echo "[Path B] there is no documented surface to feed stream-json into an"
        echo "[Path B] already-running interactive session. Active-session step 2"
        echo "[Path B] (reaches input channel) FAILS structurally, not for lack of effort."
        echo "[Path B] Per CONTEXT D-V3 'unsafe primitives' clause: ptrace / proc-fd"
        echo "[Path B] hacks are explicitly forbidden as PASS evidence."
        ```
    11. Mini-verdict computation (mechanical — write the transcript inline; this is NOT subjective):
        ```
        cat > "$EVIDENCE_DIR/pathB-TRANSCRIPT.md" <<EOF
        # Path B — Per-Path D-V1 Mini-Verdict

        Probe run: $(date -Iseconds)
        Script: .planning/spikes/128b-write-back/pathB-stream-json.sh
        claude version: $(claude --version 2>/dev/null || echo unknown)

        ## D-V1 Four-Step Gate

        | Step | Description | Fresh session | Active session |
        |------|-------------|---------------|----------------|
        | 1    | Reply originates from a non-TTY writer process | ✓ (bash heredoc piped into claude stdin) | ✗ (no input channel — \`-p\` is print-and-exit) |
        | 2    | String reaches input channel | ✓ (stdin of fresh claude subprocess) | ✗ (no active-session attach surface) |
        | 3    | Claude processes as next user turn | $([ "$FRESH_PASS" -eq 1 ] && echo "✓ (sentinel 1337 present in stream-json output)" || echo "✗ (sentinel 1337 NOT present — see pathB-fresh-out.jsonl)") | ✗ (vacuous — no input was received) |
        | 4    | Session continues healthy ≥60s | N/A (claude -p exits after single response — by design, not a failure) | ✗ (vacuous) |

        ## Mini-Verdict

        **Fresh-session: $([ "$FRESH_PASS" -eq 1 ] && echo PASS || echo FAIL)**
        **Active-session: STRUCTURAL FAIL** (no input channel exists; not a defect of Path B but a property of \`claude -p\`).

        **Per D-V2 bullet 1** ("Round-trip works ONLY on fresh \`claude -p\` sessions, NOT mid-interactive-session"):
        Path B mini-verdict ⇒ **$([ "$FRESH_PASS" -eq 1 ] && echo "DEGRADE (fresh-only)" || echo "FAIL")**

        ## Evidence

        - \`pathB-fresh-out.jsonl\` — full stream-json transcript (raw)
        - \`pathB-fresh-err.txt\` — stderr (auth, permission, network errors if any)

        ## Cited

        - CONTEXT D-V1 / D-V2 (verdict gates)
        - CONTEXT D-O1 path (b) — "Likely PASSes for fresh sessions; the active-session question is whether stdin is re-readable mid-interactive (it almost certainly is NOT — \`-p\` is print-and-exit)"
        - RESEARCH §"Path B" — predicted verdict DEGRADE (fresh-only) matches empirical result above
        EOF
        echo "[Path B] transcript written: $EVIDENCE_DIR/pathB-TRANSCRIPT.md"
        ```
    12. Final exit: `exit 0` (the script runs to completion regardless of FRESH_PASS — the transcript records the mini-verdict either way; the wave checker reads the transcript, not the exit code).

    Make the script executable (`chmod 0755`). Then RUN it from the repo root: `bash .planning/spikes/128b-write-back/pathB-stream-json.sh`. The script writes its own outputs into `.planning/spikes/128b-write-back/evidence/`. Do NOT run interactively; do NOT pipe through tee (the script handles its own redirection).

    After the script completes: read `pathB-fresh-out.jsonl` (compact JSONL — grep for `1337` and quote the matching line in the SUMMARY for plan 06 to consume; the SUMMARY for THIS plan is owned by execute-plan, not authored here).

    Per CONTEXT D-G1: variables `FRESH_PASS`, `EVIDENCE_DIR`, `SCRATCH` are descriptive and contain NO blocked-property-name patterns (`secret`/`token`/`auth`/`bearer`/`apiKey`). DO NOT add a SESSION variable (no tmux). DO NOT add an ALLOWED_REPLIES constant (Pitfall 7 — Phase 133 G2-REPLY-04 owns the allowlist; spike code uses ONLY 'compute seven multiplied by one hundred ninety-one' as its test surface, not 'yes'/'no'/etc.).
  </action>
  <verify>
    <automated>test -x .planning/spikes/128b-write-back/pathB-stream-json.sh && head -3 .planning/spikes/128b-write-back/pathB-stream-json.sh | grep -q "PHASE 128b SPIKE — TOSSABLE" && grep -q "set -euo pipefail" .planning/spikes/128b-write-back/pathB-stream-json.sh && ! grep -E "(TOKEN|AUTH|SECRET|BEARER|APIKEY)=" .planning/spikes/128b-write-back/pathB-stream-json.sh && bash .planning/spikes/128b-write-back/pathB-stream-json.sh && test -f .planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl && test -f .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md && grep -qE 'DEGRADE \(fresh-only\)|FAIL' .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md && grep -q 'D-V1' .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md && echo "Path B empirical record complete"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/pathB-stream-json.sh` exists, mode 0755, executable
    - First 3 lines of the script (after shebang) contain `PHASE 128b SPIKE — TOSSABLE`, `Phase 133 owns G2-REPLY productionization`, and `Path B: claude -p --input-format stream-json`
    - Script contains `set -euo pipefail` (CONVENTIONS.md mandate)
    - Script does NOT contain any of the regex-checked blocked-property-name assignments: `^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=` (D-G1 redaction)
    - Script does NOT contain `--bare` (Pitfall 3 — strips OAuth)
    - Running the script via `bash .planning/spikes/128b-write-back/pathB-stream-json.sh` exits with status 0 in ≤120 seconds
    - `.planning/spikes/128b-write-back/evidence/pathB-fresh-out.jsonl` exists and is non-empty
    - `.planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md` exists and contains the markdown table with D-V1 four-step columns AND a `**Per D-V2 bullet 1**` line citing the verdict gate AND a mini-verdict line resolving to either `DEGRADE (fresh-only)` or `FAIL`
    - The TRANSCRIPT.md mini-verdict matches the empirical result: if `pathB-fresh-out.jsonl` contains `1337`, TRANSCRIPT records `DEGRADE (fresh-only)`; if not, TRANSCRIPT records `FAIL`. The verdict is computed mechanically — no subjective override (Pitfall 5)
    - Total Anthropic API spend for this probe ≤$0.02 (RESEARCH §"Cost" haiku-rate estimate; documented in Plan 06 MEASUREMENTS)
  </acceptance_criteria>
  <done>
    Path B empirically tested with a false-positive-resistant sentinel; transcript records the mechanical D-V1 mini-verdict (predicted DEGRADE per RESEARCH); evidence files committed for Plan 06 MEASUREMENTS consolidation and Plan 05 SPIKE-DECISION per-path table population. The active-session NEGATIVE finding is documented as a structural property of `claude -p`, not as a missing test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-runner shell → claude subprocess | The probe pipes a controlled stream-json line to a fresh claude subprocess; no privileged surface crossed |
| claude subprocess → Anthropic API | Operator's Claude Max OAuth (NOT --bare; NOT a fresh API key); standard auth surface, not touched by the spike |
| spike outputs → committed evidence dir | Probe writes stdout/stderr to evidence/; no secrets in the prompt or response (just `seven multiplied by one hundred ninety-one` and `1337`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-01-01 | Tampering | spike-runner's live Claude Code session (D-T5 clobber) | mitigate | Path B uses NO tmux and NO JSONL writes — it runs a fresh `claude -p` subprocess with isolated stdin/stdout. Cannot collide with `$CLAUDE_SESSION_ID`. Confirmed by absence of any `--resume` or `$CLAUDE_SESSION_ID` reference in the script. |
| T-128b-01-02 | Information Disclosure | per-path transcript (GUARD-01 / D-G1) | mitigate | Variable names use descriptive non-blocked terms (`EVIDENCE_DIR`, `FRESH_PASS`, `SCRATCH`). Stderr is captured to `pathB-fresh-err.txt`; if any auth-related token leaks into stderr, the file is committed only after Plan 06 MEASUREMENTS authoring redacts it. Probe never echoes API keys (uses operator's OAuth via claude CLI, not env-var keys). |
| T-128b-01-03 | Spoofing | sentinel false-positive (Pitfall 1) | mitigate | Sentinel `1337` is computed by the model (`7 * 191 = 1337`), NOT present in the prompt text. Pre-flight check: prompt string `seven multiplied by one hundred ninety-one. Reply with only the resulting integer.` does NOT contain `1337`. Grep against the output JSONL therefore matches only the assistant's reply, not echoed prompt. |
| T-128b-01-04 | Repudiation | mechanical verdict (Pitfall 5 — subjective override) | mitigate | TRANSCRIPT.md is generated by the script's heredoc using `$FRESH_PASS` (computed from grep), NOT by hand-edit. Per CONTEXT D-V4 + Phase 128a D-V1 precedent, the verdict cell is computed from the step-column pattern; reviewer can re-run the script and confirm the same output. |
| T-128b-01-05 | Elevation of Privilege | unsafe primitive temptation (D-V3) | accept | Script explicitly documents WHY the active-session step 2 is structurally impossible (no ptrace, no proc-fd writes, no kernel-level fd injection). The active-session test does not attempt unsafe primitives; it records the structural absence of an attach surface as a vacuous ✗. |
</threat_model>

<verification>
1. Spike directory + README + MANIFEST row exist; cross-link spike 001 is bidirectional (README §"Sibling spike" + MANIFEST table).
2. Path B probe script has TOSSABLE header, `set -euo pipefail`, no blocked-property-name variable assignments, no `--bare`.
3. Probe runs to completion (`bash .planning/spikes/128b-write-back/pathB-stream-json.sh` exits 0) writing `pathB-fresh-out.jsonl`, `pathB-fresh-err.txt`, and `pathB-TRANSCRIPT.md` into `evidence/`.
4. Sentinel `1337` either present (⇒ TRANSCRIPT records `DEGRADE (fresh-only)`) or absent (⇒ TRANSCRIPT records `FAIL`). Either outcome is mechanically correct per D-V4.
5. The active-session row in the D-V1 table records structural ✗ at step 2 (no input channel), NOT an attempted-and-failed unsafe-primitive test.
</verification>

<success_criteria>
- `.planning/spikes/128b-write-back/` directory exists per CONTEXT D-A1
- `pathB-stream-json.sh` probe runs successfully and produces three evidence files
- `pathB-TRANSCRIPT.md` records the D-V1 mechanical mini-verdict (`DEGRADE (fresh-only)` or `FAIL` — matches the empirical run, not the predicted)
- MANIFEST.md gains a row for `128b-write-back` without disturbing the existing `001` row
- Plan 06 MEASUREMENTS can later consume `pathB-TRANSCRIPT.md` + `pathB-fresh-out.jsonl` for the consolidated per-path log
- Plan 05 SPIKE-DECISION can later cite `pathB-TRANSCRIPT.md` as the empirical source for Path B's row in the per-path verdict table
- Total wallclock ≤30 minutes per RESEARCH estimate; Anthropic API spend ≤$0.02 per RESEARCH §Cost
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-01-SUMMARY.md` recording:
- The directory creation result (`.planning/spikes/128b-write-back/` exists, README structured per task 1, MANIFEST row appended)
- The probe run wallclock, exit code, and the verbatim `pathB-TRANSCRIPT.md` mini-verdict (`DEGRADE (fresh-only)` expected per RESEARCH; record actual)
- The sentinel-detection result: did `1337` appear in `pathB-fresh-out.jsonl`? (yes/no, with the matched line quoted if yes)
- Any deviations from the verbatim RESEARCH §"Path B" probe shape — and why each was necessary (e.g., absolute paths vs relative; output redirection adapted for committed evidence dir)
- A `## Handoff` section noting that Plan 02 will populate `pathA-jsonl-append.sh` and Plan 03 will populate `pathD-mcp-probe.sh` + `pathD-mcp-server.mjs` in the same dir
</output>
