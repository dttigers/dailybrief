---
phase: 128b
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/128b-write-back/evidence/pathE-regression-run.log
  - .planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt
  - .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt
  - .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt
  - .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, path-e, tmux, regression, claude-code]
must_haves:
  truths:
    - "Path E's validated PoC (spike 001 L4) is re-executed in the current Linux dev environment as a regression test; the script `.planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh` runs to completion and produces the four-step round-trip evidence"
    - "Spike 001's preserved evidence files (`evidence/L4-*.txt` under `001-tmux-write-back-128b/`) are COPIED — NOT moved or symlinked — to the phase's own evidence dir (`.planning/spikes/128b-write-back/evidence/pathE-L4-*.txt`) so the phase artifact set is self-contained"
    - "This plan does NOT re-derive the privilege/portability sketch — that's reused by reference in Plan 05 SPIKE-DECISION (per RESEARCH §'Criteria Mapping' criterion 4: the spike 001 README §'Privilege & portability sketch' lines 182-218 already contains a complete D-A2-compliant pseudo-code form)"
    - "The regression run confirms Path E is still the dominant per-path verdict per D-V4 max aggregation (PASS); if the re-run unexpectedly FAILS, the verdict downgrades and Plan 05 must adopt the empirical re-run result (per CONTEXT D-V4 'verdict is mechanically computed', not 'historical PASS sticks regardless')"
    - "Per CONTEXT §'specifics' line 207: spike runs on `main` branch directly (no feature branch); commits are documentation-only — this plan touches NO production code"
  artifacts:
    - path: ".planning/spikes/128b-write-back/evidence/pathE-regression-run.log"
      provides: "Full stdout+stderr of the re-run of `bash .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh` (the canonical Path E regression script)"
      contains: "L4\\|tool"
    - path: ".planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt"
      provides: "Copy of spike 001's preserved `needs_input` permission dialog evidence; identical to `.planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt`"
      contains: "Bash\\|permission\\|Yes"
    - path: ".planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt"
      provides: "Copy of spike 001's preserved 60s-health-probe transcript showing `13 * 17 = 221` after permission grant"
      contains: "221"
    - path: ".planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt"
      provides: "Copy of spike 001's preserved unique marker proving the Bash tool executed after permission grant"
      contains: "L4-TOOL-RAN-"
    - path: ".planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md"
      provides: "Mini-verdict per Pattern 2 (D-V1 four-step checklist for Path E); records PASS (re-confirmed) with cross-link to spike 001"
      contains: "PASS"
  key_links:
    - from: ".planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md"
      to: ".planning/spikes/001-tmux-write-back-128b/README.md §Results + evidence/"
      via: "TRANSCRIPT cites spike 001 verdict VALIDATED + cross-links the 4 evidence files (originals AT spike 001; copies AT this dir)"
      pattern: "001-tmux-write-back-128b\\|spike 001"
    - from: ".planning/spikes/128b-write-back/evidence/pathE-regression-run.log"
      to: ".planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh"
      via: "the log is the verbatim stdout/stderr of re-running that script; reviewer can re-run to confirm reproducibility"
      pattern: "L4-needs-input-pause"
---

<objective>
Re-execute spike 001's L4 script (`.planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh`) as a regression test in the current Linux dev environment. Confirm the four-step D-V1 round-trip still PASSes (permission dialog dismissed via `tmux send-keys Enter` → gated Bash tool runs → unique marker file present → 60s health probe returns 221). Copy spike 001's preserved evidence files into this phase's own evidence dir (`.planning/spikes/128b-write-back/evidence/pathE-L4-*.txt`) so the phase artifact set is self-contained for Plan 05 SPIKE-DECISION citation. Author the Path E mini-verdict transcript with the verbatim D-V1 four-step gate mapping (per RESEARCH §"Path E" lines 510-515).

Purpose: Satisfy G2-REPLY-01 success criterion 3 ("If PASS, the spike includes a working proof-of-concept round-trip — not just feasibility analysis") empirically in the phase artifact set. Spike 001 already PASSed on 2026-05-14; this plan confirms reproducibility AND preserves the evidence inside the phase boundary so Plan 05 doesn't need to reach into the sibling spike directory for its citations.

This plan does NOT re-derive the privilege/portability sketch — that's load-bearing for Plan 05 (SPIKE-DECISION) but already exists in spike 001 README lines 182-218 in a complete D-A2-compliant TypeScript pseudo-code form. Plan 05 reuses it by reference; this plan only re-runs the empirical L4 round-trip and copies the evidence.

Output: Five files in `.planning/spikes/128b-write-back/evidence/` — one regression-run log + three copied evidence files (L4-permission-pause-snapshot, L4-health-check-snapshot, L4-tool-output-marker) + one mini-verdict transcript.

Per CONTEXT §"specifics" line 207: this plan operates on `main` directly. Commits are markdown / evidence files only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/spikes/001-tmux-write-back-128b/README.md
@.planning/spikes/CONVENTIONS.md

<interfaces>
<!-- Locked from spike 001 README + CONTEXT D-V1 + D-T5 + RESEARCH §"Path E" -->

Spike 001 L4 script: `.planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh`
  - Spawns a uniquely-named tmux session (`spike-128b-L4-$$`) — does NOT collide with this plan's other paths because of $$-suffix
  - Runs interactive `claude` inside the pane, drives to Bash-tool permission dialog, sends Enter via `tmux send-keys`
  - Asserts the gated tool's marker file appears on disk
  - 60s idle → health probe `13 * 17` → asserts response `221`
  - Preserves evidence under `/tmp/spike-128b-L4-XXXXXX/`, then copies to `.planning/spikes/001-tmux-write-back-128b/evidence/`
  - Cost: ~$0.01 per spike 001 §Cost
  - Wallclock: 77s end-to-end per spike 001 §Iteration 4 "Observed timing"

Spike 001 preserved evidence (under `.planning/spikes/001-tmux-write-back-128b/evidence/`):
  - `L3-claude-response-snapshot.txt` (Path E L3 — not load-bearing for L4 regression; SKIP)
  - `L4-permission-pause-snapshot.txt` — pane state at the `needs_input` permission dialog (3-option Yes/Yes-allow/No)
  - `L4-health-check-snapshot.txt` — full transcript: prompt → tool invocation → tool ran → DONE → +60s health probe → 221
  - `L4-tool-output-marker.txt` — unique marker (`L4-TOOL-RAN-91284096`) written to disk by Bash tool after permission grant

D-V1 four-step PASS gate (mapped per spike 001 README §"Results"):
  step 1 — Reply originates from a separate non-TTY writer process ⇒ ✓ (bash script outside the tmux pane)
  step 2 — String reaches the target Claude Code session's input channel ⇒ ✓ (verified by `tmux capture-pane` showing dialog state change)
  step 3 — Claude processes the string as the next user turn ⇒ ✓ (verified by Bash tool producing its output file with unique marker — only possible after permission was granted via the injected Enter)
  step 4 — Session continues to completion for ≥60 seconds ⇒ ✓ (verified by health probe `13 * 17` returning `221` after 60s idle)

CONTEXT D-T5 clobber-protect: spike 001's L4 script ALREADY enforces this via the `spike-128b-L4-$$` unique session name; this plan does not need to re-implement clobber-protect.

CONTEXT D-V4 mechanical verdict: if the regression run UNEXPECTEDLY FAILS (one or more of the four steps' assertions fail), the TRANSCRIPT must record the failed step and resolve to FAIL — NOT to historical PASS. Per D-V4 "verdict is mechanically computed"; the plan author cannot override.

Evidence copy approach: use `cp` (NOT `mv`, NOT `ln -s`). Symlinks break when sibling directories are moved/archived; copies preserve self-containment of the phase artifact set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run spike 001 L4 + copy preserved evidence + author Path E mini-verdict</name>
  <files>.planning/spikes/128b-write-back/evidence/pathE-regression-run.log, .planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt, .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt, .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt, .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md</files>
  <read_first>
    - .planning/spikes/001-tmux-write-back-128b/README.md (FULL — load-bearing for Path E methodology; pay particular attention to §Results lines 141-157 (the D-V1 four-step mapping), §Investigation Trail §Iteration 4 lines 113-130 (observed timing), §Surfaced constraint lines 175-178 (the launcher-wrapper Phase 133 carry-forward))
    - .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh (FULL — the script you are about to re-run; understand its trap-cleanup, snapshot points, and assertions so you can correctly attribute any failure if the regression run errors)
    - .planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt (exists; will be copied via `cp` to the phase evidence dir)
    - .planning/spikes/001-tmux-write-back-128b/evidence/L4-health-check-snapshot.txt (exists; will be copied)
    - .planning/spikes/001-tmux-write-back-128b/evidence/L4-tool-output-marker.txt (exists; will be copied)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Path E" lines 500-523 (the D-V1 mapping verbatim — adapt for the TRANSCRIPT heredoc)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-T5" + §"D-V4" (clobber-protect + mechanical verdict)
    - .planning/spikes/CONVENTIONS.md §"Evidence preservation" (copy at-detection / preserve raw pane captures)
  </read_first>
  <action>
    Execute the following sub-steps in order. If ANY assertion fails, do NOT silently continue — record the failure in the TRANSCRIPT and resolve to FAIL per D-V4 (mechanical verdict).

    Step 1 — Verify preconditions:
    ```bash
    test -x .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh \
      || { echo "L4 script missing or not executable" >&2; exit 2; }
    test -f .planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt \
      || { echo "L4 permission-pause snapshot missing" >&2; exit 2; }
    test -f .planning/spikes/001-tmux-write-back-128b/evidence/L4-health-check-snapshot.txt \
      || { echo "L4 health-check snapshot missing" >&2; exit 2; }
    test -f .planning/spikes/001-tmux-write-back-128b/evidence/L4-tool-output-marker.txt \
      || { echo "L4 tool-output-marker missing" >&2; exit 2; }
    mkdir -p .planning/spikes/128b-write-back/evidence
    ```

    Step 2 — Re-run the L4 regression script, capturing full output:
    ```bash
    bash .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh \
      > .planning/spikes/128b-write-back/evidence/pathE-regression-run.log 2>&1
    REGRESSION_EXIT=$?
    echo "[Path E] regression run exit code: $REGRESSION_EXIT"
    ```
    The script uses `tmux send-keys` against a `spike-128b-L4-$$` disposable session; it does NOT touch the spike-runner's live session (D-T5 already enforced by the script). Expected wallclock: ~77s end-to-end per spike 001 §Iteration 4. Expected cost: ~$0.01.

    If the script exits non-zero, do NOT halt — capture the exit code in the TRANSCRIPT and proceed to author the FAIL branch. The regression-run.log captures any error output for forensic post-mortem.

    Step 3 — Verify the regression produced fresh evidence (the L4 script writes new evidence files into its own evidence/ on success — but the LOG capture above is the authoritative regression-run signal):
    ```bash
    # The regression-run.log itself is the primary signal. Additionally, grep it for the
    # D-V1 four-step assertion markers the script prints (the script's exact markers are
    # readable from L4-needs-input-pause.sh — adapt grep patterns to whatever the script
    # actually writes; do NOT invent marker patterns).
    REGRESSION_PASS=0
    if [ $REGRESSION_EXIT -eq 0 ]; then
      # Verify the regression-run.log shows successful completion patterns.
      # The exact success markers depend on the script's output shape — read the script
      # to discover them (e.g., "L4 PASS", "ALL FOUR STEPS PASS", "ROUND-TRIP COMPLETE",
      # or step-by-step "step 1 ✓" / "step 2 ✓" etc.). Capture whatever the script writes.
      if grep -qE 'PASS|complete|round-trip|L4-TOOL-RAN-' \
           .planning/spikes/128b-write-back/evidence/pathE-regression-run.log; then
        REGRESSION_PASS=1
        echo "[Path E] regression run PASSED — D-V1 four-step round-trip confirmed"
      else
        echo "[Path E] regression run exit=0 BUT success markers absent from log — see pathE-regression-run.log"
      fi
    else
      echo "[Path E] regression run FAILED (exit=$REGRESSION_EXIT) — see pathE-regression-run.log for forensic detail"
    fi
    ```

    Step 4 — Copy spike 001's preserved evidence into the phase evidence dir (always — regardless of regression result; the historical evidence is the SPIKE 001 record, and Plan 05 needs it for the per-path table even if the regression FAILed):
    ```bash
    cp .planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt \
       .planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt
    cp .planning/spikes/001-tmux-write-back-128b/evidence/L4-health-check-snapshot.txt \
       .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt
    cp .planning/spikes/001-tmux-write-back-128b/evidence/L4-tool-output-marker.txt \
       .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt
    ```
    NOT `mv` (would destroy spike 001's record). NOT `ln -s` (would break if sibling dir is later archived/moved). `cp` preserves both the spike 001 historical record AND the phase's self-contained artifact set.

    Step 5 — Author `pathE-TRANSCRIPT.md` mechanically. The transcript MUST contain TWO D-V1 four-step tables: one for the historical spike 001 L4 run (verdicts PASS per the README §Results), and one for THIS regression re-run (verdicts determined by `$REGRESSION_PASS` / `$REGRESSION_EXIT`).

    ```bash
    cat > .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md <<EOF
    # Path E — Per-Path D-V1 Mini-Verdict (★ — pre-validated in spike 001)

    Regression run: $(date -Iseconds)
    Regression script: .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh
    Regression log: .planning/spikes/128b-write-back/evidence/pathE-regression-run.log
    Regression exit code: $REGRESSION_EXIT
    Regression success markers detected: $REGRESSION_PASS

    Original spike: spike 001 (tmux-write-back-128b) — 2026-05-14
    Original verdict: VALIDATED (PASS — all 4 D-V1 steps)

    ## D-V1 Four-Step Gate — Historical (spike 001 L4)

    | Step | Description | Verdict | Evidence |
    |------|-------------|---------|----------|
    | 1    | Reply originates from non-TTY writer | ✓ | spike 001 README §Results step 1 — bash script outside the tmux pane |
    | 2    | String reaches input channel | ✓ | pathE-L4-permission-pause-snapshot.txt — dialog state change captured |
    | 3    | Claude processes as next user turn | ✓ | pathE-L4-tool-output-marker.txt — Bash tool produced its output file (L4-TOOL-RAN-91284096) only possible after permission was granted via injected Enter |
    | 4    | Session continues healthy ≥60s | ✓ | pathE-L4-health-check-snapshot.txt — health probe \`13 * 17\` returned \`221\` after 60s idle |

    **Historical mini-verdict: PASS (all 4 steps ✓ — fresh AND active session both ✓ per D-V1)**

    ## D-V1 Four-Step Gate — Regression Re-Run ($(date -I))

    | Step | Description | Verdict | Evidence |
    |------|-------------|---------|----------|
    | All  | (full round-trip — script asserts each step internally) | $([ "$REGRESSION_PASS" -eq 1 ] && echo "✓ (regression PASSED — exit 0 + success markers in log)" || echo "✗ (regression FAILED — exit $REGRESSION_EXIT — see pathE-regression-run.log)") | pathE-regression-run.log |

    **Regression mini-verdict: $([ "$REGRESSION_PASS" -eq 1 ] && echo "PASS (re-confirmed)" || echo "FAIL (regression diverged from spike 001 — see log)")**

    ## Overall Path E Mini-Verdict

    **$([ "$REGRESSION_PASS" -eq 1 ] && echo "PASS — re-confirmed in current env; D-V4 max aggregation: Path E dominates per-path table" || echo "DEGRADED FROM HISTORICAL — see Plan 05 SPIKE-DECISION for impact on overall verdict")**

    Per CONTEXT D-V4 (mechanical verdict): if the regression re-run FAILed, Plan 05's SPIKE-DECISION MUST adopt the regression result (FAIL) for Path E's row in the per-path table, NOT the historical PASS. The historical evidence remains preserved (pathE-L4-*.txt files) for forensic comparison, but the verdict cell follows the current empirical signal.

    ## Surfaced constraint (preserved from spike 001 README §"Surfaced constraint" lines 175-178)

    Claude Code must be launched **inside a tmux pane** for Path E to work. This is a launcher-UX surface that Phase 133 must address (NOT this spike's concern):
    - Operator launches \`claude\` via a \`vigil-claude\` wrapper (or aliases \`claude\` to wrap in tmux).
    - If launched directly (no tmux), \`vigil-tmux-bridge\` cannot reach the input channel.
    - Phase 133 productionization detects non-wrapped sessions (e.g., absence of \`VIGIL_TMUX_SESSION\` env in claude's process tree) and degrades to G2-REPLY-05 banner-ack-only for that session.

    ## Evidence (copies under this phase's evidence dir — originals preserved at spike 001)

    - \`pathE-regression-run.log\` — full stdout/stderr of the re-run
    - \`pathE-L4-permission-pause-snapshot.txt\` — copy of spike 001's preserved \`needs_input\` dialog evidence
    - \`pathE-L4-health-check-snapshot.txt\` — copy of spike 001's preserved 60s-health-probe transcript (\`221\`)
    - \`pathE-L4-tool-output-marker.txt\` — copy of spike 001's preserved unique-marker proof of Bash tool execution

    Originals at: \`.planning/spikes/001-tmux-write-back-128b/evidence/L4-*.txt\` (untouched by this plan)

    ## Privilege & portability sketch — REFERENCED, not re-derived

    Per RESEARCH §"Criteria Mapping" criterion 4 + CONTEXT D-A2: the spike 001 README §"Privilege & portability sketch (D-A2 — markdown only)" lines 182-218 ALREADY contains a complete D-A2-compliant TypeScript pseudo-code form of the 5-string allowlist + privilege-drop + send-keys injection. Plan 05 SPIKE-DECISION reuses that sketch by reference; THIS plan does not re-derive it.

    ## Cited

    - spike 001 README §Results — historical D-V1 four-step mapping (PASS)
    - spike 001 evidence/L4-*.txt — preserved 2026-05-14
    - CONTEXT D-V1 / D-V4 / D-T5
    - RESEARCH §"Path E" lines 500-523 + §"Criteria Mapping" criterion 3
    EOF
    echo "[Path E] transcript written: .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md"
    ```

    Step 6 — Final exit:
    - If `$REGRESSION_PASS -eq 1`: exit 0 (regression PASSed; Path E remains the dominant per-path verdict).
    - If `$REGRESSION_PASS -eq 0`: STILL exit 0 (the transcript records FAIL mechanically; Plan 05 SPIKE-DECISION reads the transcript to determine the overall verdict — exit code is informational only).

    The plan does NOT escalate to manual investigation if the regression FAILs — that's a SEPARATE follow-up (would open Phase 128b.1 per CONTEXT D-V4 "if a re-test contradicts, open Phase 128b.1"). This plan's job is to record the empirical result mechanically.

    Per CONTEXT §"specifics" line 207: this plan operates on `main` branch (no feature branch). Only `.planning/spikes/128b-write-back/evidence/` files are touched.

    DO NOT modify `.planning/spikes/001-tmux-write-back-128b/` — its README, scripts, frontmatter, and evidence are immutable historical record per spike-001 verdict-locked posture.

    DO NOT re-derive the privilege sketch — Plan 05 references spike 001 README lines 182-218 directly.

    DO NOT add the 60s Loom here — that's C-2 wallclock checkpoint (Plan 08 — operator-only).
  </action>
  <verify>
    <automated>test -s .planning/spikes/128b-write-back/evidence/pathE-regression-run.log && test -f .planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt && test -f .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt && test -f .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt && test -f .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md && cmp -s .planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt .planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt && cmp -s .planning/spikes/001-tmux-write-back-128b/evidence/L4-health-check-snapshot.txt .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt && cmp -s .planning/spikes/001-tmux-write-back-128b/evidence/L4-tool-output-marker.txt .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt && grep -q '## D-V1 Four-Step Gate — Historical' .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md && grep -qE '## D-V1 Four-Step Gate — Regression Re-Run' .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md && grep -qE 'PASS|FAIL|DEGRADED' .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md && grep -q '001-tmux-write-back-128b' .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md && grep -q '221' .planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt && grep -qE 'L4-TOOL-RAN-' .planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt && echo "Path E regression record complete"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/evidence/pathE-regression-run.log` exists and is non-empty (captures full stdout+stderr of the L4 re-run)
    - `.planning/spikes/128b-write-back/evidence/pathE-L4-permission-pause-snapshot.txt` exists AND its content matches `.planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt` byte-for-byte (`cmp` returns 0; verifies `cp`, not `mv`)
    - `.planning/spikes/128b-write-back/evidence/pathE-L4-health-check-snapshot.txt` exists AND `cmp` matches the spike 001 original AND contains the literal `221` (the health-probe response)
    - `.planning/spikes/128b-write-back/evidence/pathE-L4-tool-output-marker.txt` exists AND `cmp` matches the spike 001 original AND contains the prefix `L4-TOOL-RAN-`
    - `.planning/spikes/001-tmux-write-back-128b/evidence/L4-permission-pause-snapshot.txt` is UNCHANGED (spike 001's record is preserved — verified by re-reading + comparing to original mtime/content)
    - `.planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md` exists and contains BOTH `## D-V1 Four-Step Gate — Historical` AND `## D-V1 Four-Step Gate — Regression Re-Run` section headers
    - TRANSCRIPT contains the literal phrase `Privilege & portability sketch — REFERENCED, not re-derived` (encoding the D-A2 reuse-by-reference posture for Plan 05)
    - TRANSCRIPT mini-verdict for the Regression row resolves mechanically to either `PASS (re-confirmed)` or `FAIL (regression diverged from spike 001 — see log)` based on `$REGRESSION_PASS` (no subjective override)
    - Total wallclock ≤3 minutes (77s regression + ~30s for copies + transcript authoring; ≤180s ceiling)
    - Total Anthropic API spend ≤$0.02 (one L4 re-run at haiku rate per spike 001 §Cost)
  </acceptance_criteria>
  <done>
    Path E re-execution complete. Regression-run log captures fresh stdout/stderr. Spike 001's preserved evidence COPIED (not moved) into the phase evidence dir — phase artifact set is now self-contained for Plan 05 SPIKE-DECISION citation. Transcript records BOTH the historical PASS (verbatim mapping from spike 001 README §Results) AND the regression re-run verdict (mechanically computed). Privilege/portability sketch is REFERENCED (not re-derived) — Plan 05 reuses the existing spike 001 README §"Privilege & portability sketch" lines 182-218 form.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-runner → spike 001 dir | READ-ONLY access to spike 001's L4 script + evidence; no writes to spike 001's dir |
| L4 regression script → disposable tmux session | Same as spike 001's posture: unique session name (`spike-128b-L4-$$`), trap-cleanup, no live-session collision |
| Evidence copy | `cp` preserves original; `mv` would destroy spike 001's record (forbidden) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-04-01 | Tampering | spike 001's preserved evidence (immutable historical record) | mitigate | Use `cp`, NOT `mv` or `ln -s`. Acceptance criterion: after the plan completes, `cmp .planning/spikes/001-tmux-write-back-128b/evidence/L4-*.txt .planning/spikes/128b-write-back/evidence/pathE-L4-*.txt` returns 0 for each pair AND the spike 001 originals are unchanged from their pre-plan mtime/content. |
| T-128b-04-02 | Tampering | spike-runner's live Claude Code session (D-T5) | mitigate | The L4 script ALREADY enforces D-T5 via `spike-128b-L4-$$` unique tmux session name (spike 001 §"Observability" line 75). This plan inherits that protection by running the script unchanged. |
| T-128b-04-03 | Repudiation | regression failure subjectively overridden as PASS (Pitfall 5) | mitigate | TRANSCRIPT is generated by heredoc using `$REGRESSION_PASS` and `$REGRESSION_EXIT` (both computed from grep + script exit code); the mini-verdict is mechanically determined. Reviewer can re-run the script and confirm. |
| T-128b-04-04 | Information Disclosure | health-check snapshot may contain conversation content from the test prompt | mitigate | Copies are byte-for-byte from spike 001's already-public evidence; no new information disclosed. Reviewer can re-inspect before commit. |
| T-128b-04-05 | Denial of Service | regression script hangs (e.g., tmux server not responsive) | accept | The L4 script's own trap-cleanup handles tmux-session disposal; no top-level timeout because spike 001 measured 77s wallclock — adding a generous 5x cap (~6 min) would only matter if claude itself hung. If observed, this becomes a separate investigation; Plan 05 records the regression-FAIL outcome regardless. |
| T-128b-04-06 | Elevation of Privilege | unsafe-primitive temptation if regression FAILs | mitigate | The plan EXPLICITLY does not escalate. Per CONTEXT D-V4, the FAIL is recorded mechanically and Plan 05 propagates it. Phase 128b.1 (separate follow-up) would investigate; not this plan's scope. |
</threat_model>

<verification>
1. Spike 001 L4 script re-executes; log captures full stdout+stderr.
2. Three preserved evidence files COPIED (not moved/linked) into `.planning/spikes/128b-write-back/evidence/`; spike 001's originals untouched.
3. TRANSCRIPT contains TWO D-V1 tables (historical + regression) and resolves to PASS (re-confirmed) or FAIL mechanically.
4. Privilege/portability sketch is REFERENCED in the TRANSCRIPT — not re-derived (Plan 05 reuses spike 001 README directly).
5. Spike 001's evidence dir is unchanged after the plan (verified by `cmp` and by absence of any spike-001-touching writes in this plan's action).
</verification>

<success_criteria>
- Spike 001 L4 re-execution completed (regardless of PASS/FAIL outcome — the empirical result is what matters)
- Phase evidence dir contains the regression-run log + three copied L4 evidence files + the Path E TRANSCRIPT
- Spike 001's preserved evidence files are byte-identical to the copies (verified by `cmp`)
- TRANSCRIPT records BOTH historical PASS AND regression result mechanically (no subjective override)
- Plan 05 SPIKE-DECISION can use `.planning/spikes/128b-write-back/evidence/pathE-*.txt` as the canonical Path E evidence without reaching into the sibling spike dir
- Total wallclock ≤3 min; total cost ≤$0.02
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-04-SUMMARY.md` recording:
- The regression-run exit code (`$REGRESSION_EXIT`) and the `$REGRESSION_PASS` mechanical signal (0 or 1)
- Verbatim quote of the Regression Re-Run row of the TRANSCRIPT's D-V1 table (PASS re-confirmed OR FAIL with link to the log)
- Wallclock for the L4 re-run (target ~77s per spike 001 §Iteration 4; record actual)
- Confirmation that spike 001's preserved evidence is UNCHANGED (run `cmp` on each pair and record the result — all three must show 0/0 byte difference)
- Cost estimate for the regression run (~$0.01 per spike 001 §Cost; record actual via the model+token observation in regression-run.log if available)
- A `## Reuse-by-reference posture` section confirming: the TRANSCRIPT references spike 001 README lines 182-218 for the privilege & portability sketch; Plan 05 SPIKE-DECISION will cite the same lines directly (NOT re-derive)
- A `## Handoff` section: Plans 05 (SPIKE-DECISION) + 06 (MEASUREMENTS) consume `.planning/spikes/128b-write-back/evidence/pathE-*.txt` (4 files: log + 3 L4 copies + TRANSCRIPT)
</output>
