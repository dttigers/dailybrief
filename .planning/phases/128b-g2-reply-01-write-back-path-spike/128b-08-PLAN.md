---
phase: 128b
plan: 08
type: execute
wave: 5
depends_on: [128b-05, 128b-07]
files_modified:
  - .planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4
  - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md
  - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-08-SUMMARY.md
autonomous: false
wallclock: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, wallclock, operator, c-2, loom, portfolio]
must_haves:
  truths:
    - "C-2 operator wallclock checkpoint: operator records a 60s portfolio Loom demonstrating the write-back round-trip (PASS verdict — success demo; DEGRADE/BLOCK verdict — failure-mode documentation)"
    - "Per RESEARCH §'Open Questions §3' resolved recommendation: re-record live (NOT assemble from spike 001 transcripts) — Path E's round-trip is fast enough (77s wallclock per spike 001 §Iteration 4) that a real-time recording fits cleanly"
    - "Per `[feedback_wallclock_checkpoint_exempt]` memory: this task is EXEMPT from `--auto` execution; Claude CANNOT record the Loom"
    - "The Loom URL OR the local 60s-demo.mp4 path is recorded in `128b-SPIKE-DECISION.md` header section AND in `128b-08-SUMMARY.md`"
    - "Loom shape mirrors Phase 128a precedent (5s gesture close-up → 5s wide shot → 10s HUD reaction → 15s session reaction → 25s split-screen replay) adapted for the write-back demo (no G2 hardware in scope; the 'wide shot' is the operator's terminal panes)"
    - "If verdict is BLOCK: the Loom is a 30-second failure-mode documentation, NOT a portfolio piece (per CONTEXT §'specifics' line 204)"
  artifacts:
    - path: ".planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4"
      provides: "60s portfolio Loom demonstrating the write-back round-trip; success-criterion proxy artifact"
      contains: "(binary file — operator verifies playback OR records Loom URL in header)"
    - path: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md"
      provides: "Updated with `## C-2 Loom` section recording the Loom URL OR confirming the local 60s-demo.mp4 path"
      contains: "60s-demo\\|Loom"
    - path: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-08-SUMMARY.md"
      provides: "Operator wallclock log: recording date, Loom URL or file path, shape adherence notes"
      contains: "C-2"
  key_links:
    - from: "operator (wallclock)"
      to: ".planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4"
      via: "operator records and either commits the file OR posts to Loom and records the URL in SPIKE-DECISION"
      pattern: "60s-demo|loom"
---

<objective>
Operator wallclock checkpoint C-2 per CONTEXT §"Operator wallclock checkpoints" + CONTEXT §"specifics" §"60s Loom demonstration shape" + RESEARCH §"Manual-Only Verifications" row 2: the operator records a 60-second portfolio Loom (or local MP4) demonstrating the write-back round-trip. The Loom is the success-criterion proxy artifact — G2-REPLY-01 success criterion 3 says "if PASS, the spike includes a working proof-of-concept round-trip"; the empirical evidence is in `pathE-L4-*.txt` and `pathE-regression-run.log`, but a 60s visual artifact is the portfolio-readable form.

Purpose: The Loom serves two audiences:
1. **Phase 133 planner** — sees the round-trip behavior in motion and can scope vigil-tmux-bridge productionization confidently
2. **Portfolio / future reader** — sees an evidence-grade demonstration of a non-obvious technical solve (write-back into a live TUI session via tmux IPC)

Per RESEARCH §"Open Questions §3" resolved recommendation: re-record live (NOT assemble from spike 001 transcripts). Path E's 77s wallclock fits cleanly into a 60s Loom with editing.

Loom shape (per CONTEXT §"specifics" line 204 — adapted from Phase 128a precedent):
- 5s: terminal close-up (the writer-process command line about to be run)
- 5s: wide shot of operator's screen (both panes visible — the claude pane + the writer-shell pane)
- 10s: HUD/pane reaction (the claude pane's `needs_input` permission dialog surfaces; operator's hand visible NOT touching the keyboard yet)
- 15s: session reaction (the writer-process command runs; the dialog dismisses; the gated tool executes; claude prints completion)
- 25s: split-screen replay (slowed/zoomed; highlight the key transitions — writer-process command → dialog dismissed → tool ran → marker file appeared on disk → 60s health probe response `221`)

Total: 60s. For BLOCK verdict, the Loom is a 30s failure-mode documentation instead (CONTEXT §"specifics" line 204 inverse case).

CRITICAL: This task is EXEMPT from `--auto` execution per `[feedback_wallclock_checkpoint_exempt]` memory. Claude does NOT record the Loom; the operator does.

Output: One Loom URL OR one local 60s-demo.mp4 file + SPIKE-DECISION header update + SUMMARY recording the wallclock observations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-07-SUMMARY.md
@.planning/spikes/001-tmux-write-back-128b/README.md

<interfaces>
<!-- Locked from CONTEXT §"specifics" §"60s Loom demonstration shape" + RESEARCH §"Open Questions §3" -->

Loom shape (PASS verdict — success demo):
  5s — gesture/command close-up: terminal pane with writer-process command pre-typed (e.g., `tmux send-keys -t vigil-claude-c2-spike-<timestamp> Enter`) — DO NOT hit Enter yet
  5s — wide shot: both panes visible (claude pane on left, writer-shell pane on right); operator visible hands-free
  10s — HUD/pane reaction: prompt claude in the left pane ("Run the bash command: echo C2-MARKER > /tmp/c2-demo.out"); permission dialog surfaces; pause here visibly
  15s — session reaction: switch to writer-shell, hit Enter on the pre-typed `tmux send-keys` command; the claude pane's dialog dismisses; the Bash tool runs; the marker file `cat /tmp/c2-demo.out` shows `C2-MARKER`
  25s — split-screen replay (slowed; could be a slow-motion or zoomed replay of the 15s above with annotations highlighting: writer-process command → dialog dismissed → tool ran → marker file appeared → 60s health probe response `221`)

Loom shape (DEGRADE verdict — banner-ack-only fallback demo):
  Same 60s structure but the demo shows: dialog dismisses BUT operator must press Enter once more (D-V2 bullet 2 case), OR demo shows fresh-session-only behavior (D-V2 bullet 1), OR demo shows partial cleanup (D-V2 bullet 3). The Loom documents WHICH degradation pattern was observed and articulates the Phase 133 G2-REPLY-05 fallback (banner-ack-only).

Loom shape (BLOCK verdict — failure-mode documentation, 30s):
  Not a portfolio piece per CONTEXT §"specifics" line 204. Just: 30s showing the failure mode (e.g., permission dialog doesn't dismiss; tool never runs) + annotation of which D-V3 BLOCK gate clause was tripped + a reference to the three SEED-003-style re-activation conditions in SPIKE-DECISION.

Recording host: operator's workstation (Ubuntu per the 2026-05-14 SURFACE-MAP.md shift; macOS acceptable as fallback if Ubuntu setup pending).

Recording tool: Loom.com (operator's existing Loom account) OR a local screen recording tool exporting to MP4. The choice is operator preference; the SUMMARY records which was used.

Per RESEARCH §"Open Questions §3": "Operator-driven; cannot be `--auto`-executed (C-2 per CONTEXT)."

Recording location: the operator's workstation tmux session is sufficient; no G2 hardware involved (unlike Phase 128a which required physical glasses).

Disposable tmux session name (D-T5 clobber-protect):
  - `vigil-claude-c2-loom-<timestamp>` (distinct prefix from C-1's `vigil-claude-c1-spike-*`)

Resume signal: `c2-done` (Loom URL committed to SPIKE-DECISION + SUMMARY) OR `c2-blocked` (with rationale; e.g., recording-tool unavailable; operator schedule constraint).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: OPERATOR WALLCLOCK C-2 — Record 60s portfolio Loom (write-back round-trip demo)</name>
  <files>.planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4, .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md</files>
  <read_first>
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md (read the verdict at TOP — PASS / DEGRADE / BLOCK — to select the Loom shape per the CONTEXT §"specifics" + interfaces above)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"Operator wallclock checkpoints" + §"specifics" §"60s Loom demonstration shape"
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128A-UI-SPEC.md (the precedent Loom shape definition — even though 128a is voice + G2 hardware, the structural shape (5s/5s/10s/15s/25s) transfers to 128b's terminal-pane demo)
    - .planning/spikes/001-tmux-write-back-128b/README.md §"Iteration 4 Observed timing" lines 117-124 (the 77s wallclock timing; ensures the Loom fits the round-trip within the 60s envelope)
  </read_first>
  <action>OPERATOR-ONLY (Claude CANNOT execute — wallclock per `[feedback_wallclock_checkpoint_exempt]` memory). Operator records the Loom per the shape determined by the SPIKE-DECISION verdict at TOP (PASS / DEGRADE / BLOCK), then commits either the local 60s-demo.mp4 file OR records the Loom URL in SPIKE-DECISION's header.</action>
  <what-built>The phase's 7 markdown artifacts + the spike scripts under `.planning/spikes/128b-write-back/` are now complete. C-2 produces the 8th artifact (per CONTEXT D-A4 line 91): `60s-demo.mp4` — the success-criterion-3 proxy artifact. The Loom is the portfolio-readable form of the round-trip evidence; the raw evidence is in `evidence/pathE-L4-*.txt` and `evidence/pathE-regression-run.log`.</what-built>
  <how-to-verify>
    OPERATOR ACTION (Claude CANNOT execute — wallclock per `[feedback_wallclock_checkpoint_exempt]`):

    **Step 1 — Read the SPIKE-DECISION verdict at TOP** (first line of `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md`):
    - **PASS** ⇒ proceed with the 60s success demo (5s/5s/10s/15s/25s shape below)
    - **DEGRADE** ⇒ proceed with the 60s degrade demo (same shape but highlighting which D-V2 clause was tripped + the G2-REPLY-05 banner-ack-only fallback)
    - **BLOCK** ⇒ proceed with the 30s failure-mode documentation (shorter; NOT a portfolio piece)

    **Step 2 — Set up the recording environment.** Open the screen-recording tool (Loom desktop app OR macOS Screenshot.app/QuickTime Player; Linux: OBS Studio / `kazam` / `wf-recorder`). Frame the recording area to capture TWO terminal panes side-by-side:
    - Left pane: the live claude session (running inside a disposable tmux session — see step 3)
    - Right pane: the writer-process shell (where the operator runs `tmux send-keys`)

    Test the frame is correct (both panes visible) BEFORE starting the recording.

    **Step 3 — Set up the disposable tmux session for the demo (D-T5 clobber-protect):**
    ```
    SESSION="vigil-claude-c2-loom-$(date +%s)"
    tmux new-session -d -s "$SESSION" "claude --model haiku"
    echo "Demo session: $SESSION"
    sleep 5
    tmux send-keys -t "$SESSION" Enter   # dismiss workspace-trust if present
    tmux attach -t "$SESSION"   # in the left pane
    ```
    In a separate shell (the right pane), pre-type but DO NOT hit Enter on the writer-process command:
    ```
    SESSION=vigil-claude-c2-loom-<timestamp-from-above>
    # Pre-type, ready to run:
    tmux send-keys -t "$SESSION" Enter
    ```

    **Step 4 — Start recording.** Begin the screen capture.

    **Step 5 — Execute the 60s shape (PASS case):**

    **0:00-0:05 — Gesture/command close-up:**
    - Zoom or focus the recording on the right pane (writer-shell) showing the pre-typed `tmux send-keys -t "$SESSION" Enter` command. Do NOT press Enter yet. Caption: "Writer process: about to inject Enter into a separate claude session"

    **0:05-0:10 — Wide shot:**
    - Pull back to show BOTH panes. Caption: "Left: live claude session (Ubuntu, tmux pane). Right: writer-process shell (non-TTY; will inject via `tmux send-keys`)."

    **0:10-0:20 — HUD/pane reaction (priming the needs_input pause):**
    - In the left pane (claude), type and submit:
      ```
      Run the bash command: echo 'C2-MARKER' > /tmp/c2-demo.out
      ```
    - Wait for claude to surface the Bash permission dialog (~5-10s)
    - The dialog appears in the left pane; the right pane still has the pre-typed `tmux send-keys` command waiting. Caption: "claude pauses on permission dialog; operator's hands are not on the keyboard."

    **0:20-0:35 — Session reaction (the round-trip):**
    - Switch focus to the right pane; hit Enter on the pre-typed command
    - Watch the left pane: the dialog dismisses; the Bash tool runs (`● Bash(echo 'C2-MARKER' > /tmp/c2-demo.out)`); claude prints `● DONE` or equivalent
    - In the right pane, run: `cat /tmp/c2-demo.out` — output shows `C2-MARKER`. Caption: "Writer process injected Enter via tmux send-keys; tool ran; marker file on disk."

    **0:35-1:00 — Split-screen replay (slowed/zoomed):**
    - Replay the 0:20-0:35 segment slowed by 2x with annotations: "(1) writer presses Enter (2) tmux server delivers to pane pty (3) claude's permission dialog dismisses (4) Bash tool runs (5) marker file written"
    - Optional: skip the 60s health probe in the recording (it adds 60s of dead time); reference the spike 001 §Results §"step 4 ✓" as the empirical proof of step-4 of the D-V1 gate

    **Step 6 — Stop recording. Save as MP4** (or upload to Loom.com).

    **Step 7 — Cleanup:**
    ```
    tmux kill-session -t "$SESSION"
    rm /tmp/c2-demo.out
    ```

    **Step 8 — Commit the artifact.** Two paths:

    *Path A (local MP4 commit):* Save the file as `.planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4`. The file is binary; operator should verify it plays before committing. Note: binary file is ~5-30 MB; acceptable for the planning repo.

    *Path B (Loom URL only):* Upload to Loom; copy the URL; record it in `128b-SPIKE-DECISION.md` header section by adding a line like `**Loom recording:** <https://www.loom.com/share/...>` between the verdict line and the per-path table. Do NOT commit a local MP4 (the URL is the canonical artifact). Note: Loom URLs are public-shareable; ensure no sensitive prompts/responses are visible in the recording before sharing.

    For DEGRADE verdict: same shape but the captions and segment content emphasize the degradation pattern (fresh-only / requires-extra-Enter / partial-cleanup) AND the G2-REPLY-05 banner-ack-only fallback that Phase 133 would adopt.

    For BLOCK verdict: 30s only; no portfolio framing; just: "Path X attempted; failed at D-V1 step Y; see SPIKE-DECISION re-activation conditions." Not a Loom upload — local 30-second MP4 only.

    **Step 9 — Record observations.** Update `128b-SPIKE-DECISION.md` to add a `## C-2 Loom (success criterion 3 proxy)` section (between the per-path table and the Phase 133 scope-lock section) with:
    ```markdown
    ## C-2 Loom (success criterion 3 proxy)

    - **Recording date:** <YYYY-MM-DDTHH:MM:SSZ>
    - **Artifact form:** <local MP4 at .planning/phases/.../60s-demo.mp4 | Loom URL: https://www.loom.com/share/...>
    - **Recording duration:** <60s (PASS/DEGRADE) | 30s (BLOCK)>
    - **Demo shape:** <success demo / degrade demo / failure-mode documentation> per CONTEXT §"specifics" §"60s Loom demonstration shape"
    - **Disposable tmux session used:** vigil-claude-c2-loom-<timestamp>
    - **Verdict at TOP confirmation:** <PASS / DEGRADE / BLOCK — matches the verdict-at-TOP line>
    ```

    Resume signal: `c2-done` (Loom committed; SPIKE-DECISION updated) OR `c2-blocked` (rationale: recording-tool unavailable; defer recording to next dev day; the spike's verdict-at-TOP is unaffected — Plan 04 regression remains the empirical anchor).

    DO NOT record any operator-personal information visible on screen (e.g., other tmux session names containing project codes; chat windows; calendar events). The recording frame should be limited to the two terminal panes only.

    DO NOT post the Loom URL to social media or external forums before the spike-decision is committed; the URL goes in SPIKE-DECISION first.
  </how-to-verify>
  <acceptance_criteria>
    - EITHER `.planning/phases/128b-g2-reply-01-write-back-path-spike/60s-demo.mp4` exists locally AND plays back to show the round-trip; OR `128b-SPIKE-DECISION.md` contains a `Loom recording:` line with a `loom.com/share/` URL
    - `128b-SPIKE-DECISION.md` contains a `## C-2 Loom (success criterion 3 proxy)` section with 6 listed fields: Recording date, Artifact form, Recording duration, Demo shape, Disposable tmux session used, Verdict at TOP confirmation
    - Recording duration matches the verdict shape: 60s for PASS or DEGRADE; 30s for BLOCK
    - Disposable tmux session was killed before resume signal (operator confirms `tmux ls | grep vigil-claude-c2-loom-` returns empty)
    - The `/tmp/c2-demo.out` file was removed before resume signal
    - Verdict at TOP confirmation in the C-2 section matches the actual verdict line at the top of SPIKE-DECISION (no divergence)
    - The Loom URL (if Path B chosen) does NOT show operator-personal information beyond the two demo terminal panes (operator self-audit per step 8 caveat)
    - Operator's resume signal is `c2-done` (artifact committed) OR `c2-blocked` (rationale documented; SPIKE-DECISION's verdict-at-TOP is independent and unaffected)
  </acceptance_criteria>
  <resume-signal>Resume signal: `c2-done` (Loom URL or 60s-demo.mp4 committed; SPIKE-DECISION updated) OR `c2-blocked` (with rationale — recording-tool unavailable; operator can defer to next dev session without impacting the verdict)</resume-signal>
  <done>C-2 60s Loom recorded and committed (or URL posted in SPIKE-DECISION); SPIKE-DECISION has the C-2 Loom section; disposable tmux session and demo marker file cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen recording → MP4 / Loom URL | Operator-controlled; the recording frame is the operator's responsibility |
| Loom URL → public shareable | If Path B chosen, the URL is public; operator self-audits the recording for sensitive content before sharing |
| operator's primary work session | Protected by `vigil-claude-c2-loom-<timestamp>` unique prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-08-01 | Information Disclosure | Loom recording captures operator-personal info (other tmux sessions, chat windows, calendar) | mitigate | Step 8 caveat: recording frame limited to the two demo terminal panes; operator self-audit before sharing. Acceptance criterion: operator confirms in resume signal. |
| T-128b-08-02 | Information Disclosure | full API keys or tokens accidentally visible in the recording | mitigate | The demo prompt is canonical safe text (`echo 'C2-MARKER' > /tmp/c2-demo.out`; `13 * 17`); no API keys are typed or displayed. tmux session names are non-sensitive. |
| T-128b-08-03 | Tampering | operator's live work tmux session | mitigate | Unique prefix `vigil-claude-c2-loom-<timestamp>`; same pattern as C-1 (Plan 07); cannot collide with operator's sessions. |
| T-128b-08-04 | Repudiation | recorded outcome inflated relative to actual SPIKE-DECISION verdict | mitigate | Acceptance criterion: Verdict at TOP confirmation in the C-2 section must match the actual first-line verdict of SPIKE-DECISION (no divergence). |
| T-128b-08-05 | Denial of Service | Disposable tmux session left running after C-2 | mitigate | Step 7 cleanup explicit; acceptance criterion verifies `tmux ls | grep vigil-claude-c2-loom-` returns empty before resume signal. |
| T-128b-08-06 | Information Disclosure | Loom URL shared externally before SPIKE-DECISION is committed | mitigate | Step 8 caveat: URL goes in SPIKE-DECISION first; only after that does the operator share externally (if at all). Acceptance criterion: SPIKE-DECISION contains the C-2 section before resume signal. |
</threat_model>

<verification>
1. 60s-demo.mp4 file exists OR Loom URL recorded in SPIKE-DECISION.
2. SPIKE-DECISION has `## C-2 Loom` section with 6 fields populated.
3. Recording duration matches verdict shape (60s for PASS/DEGRADE; 30s for BLOCK).
4. Disposable tmux session cleaned up; /tmp/c2-demo.out removed.
5. Verdict at TOP confirmation matches the verdict-at-TOP line of SPIKE-DECISION.
6. No operator-personal information visible in the recording (self-audit).
</verification>

<success_criteria>
- C-2 60s Loom artifact committed (file OR URL)
- SPIKE-DECISION updated with the C-2 Loom section
- Disposable tmux session and demo marker cleaned up
- Resume signal confirms PASS (preferred) or documents the BLOCK rationale
- The phase's 8-artifact set per CONTEXT D-A4 is now complete (CONTEXT-locked artifacts + planner-authored artifacts + operator wallclock artifacts)
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-08-SUMMARY.md` recording:
- The operator's resume signal (`c2-done` / `c2-blocked`)
- Artifact form chosen (local MP4 / Loom URL) and the artifact location (file path OR URL)
- Recording duration (60s expected for PASS/DEGRADE; 30s for BLOCK)
- Demo shape adherence: which 5-segment timing was followed (5s/5s/10s/15s/25s for PASS/DEGRADE; or 30s flat for BLOCK)
- Verdict-at-TOP cross-check: confirm the verdict in the C-2 section matches `head -1 128b-SPIKE-DECISION.md`
- The disposable tmux session name used (the `vigil-claude-c2-loom-<timestamp>` value)
- Confirmation that `tmux ls | grep vigil-claude-c2-loom-` returns empty and `/tmp/c2-demo.out` is absent
- A `## Phase 133 carry-forward note` section: any operator-surfaced UX concerns about the launcher-wrapper (vigil-claude) that should be captured for Phase 133 planning (e.g., did the operator naturally adopt the launcher-wrapper pattern? was the launch-via-tmux flow ergonomic? — these inform G2-REPLY-04's onboarding-UX surface)
- A `## Phase 128b closeout` section: confirmation that all 8 artifacts per CONTEXT D-A4 lines 84-91 exist: 128b-CONTEXT.md, 128b-DISCUSSION-LOG.md, 128b-RESEARCH.md, 128b-NN-PLAN.md × 8, 128b-SPIKE-DECISION.md, 128b-MEASUREMENTS.md, 60s-demo.mp4 (or Loom URL in SPIKE-DECISION). With C-2 done, the phase is closeout-ready for `/gsd-verify-work`.
</output>
