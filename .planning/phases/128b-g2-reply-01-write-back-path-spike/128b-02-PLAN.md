---
phase: 128b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/128b-write-back/pathA-jsonl-append.sh
  - .planning/spikes/128b-write-back/evidence/pathA-at-detection.txt
  - .planning/spikes/128b-write-back/evidence/pathA-final-pane.txt
  - .planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt
  - .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, path-a, jsonl, claude-code, clobber-protect]
must_haves:
  truths:
    - "Path A (JSONL append + IPC) is empirically probed against a COPY of a real session JSONL from the corpus — never the live spike-runner session (D-T5 clobber-protect)"
    - "The probe verifies whether a running `claude --resume <sid>` re-reads the JSONL after a user-row append + SIGUSR1 — predicted FAIL active-session (Claude reads JSONL only at startup/resume per RESEARCH Predicted-Verdict Summary)"
    - "Snapshot-at-detection pattern preserves pane content the instant a sentinel is grep-matched (Pitfall 2: TUI alt-screen re-renders erase evidence)"
    - "The probe uses a unique tmux session name `spike-128b-pathA-$$` and a `mktemp -d` scratch projects dir — collides with neither the spike-runner's session NOR the live JSONL corpus"
    - "The probe DOES NOT attempt ptrace / `/proc/<pid>/fd/0` writes — those are CONTEXT D-V3 unsafe-primitive territory and would invalidate any PASS evidence"
    - "Mechanical D-V1 mini-verdict written by the script via heredoc; not subjectively edited (Pitfall 5)"
  artifacts:
    - path: ".planning/spikes/128b-write-back/pathA-jsonl-append.sh"
      provides: "Empirical probe script for Path A (JSONL append + IPC)"
      contains: "PHASE 128b SPIKE — TOSSABLE"
    - path: ".planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md"
      provides: "Mini-verdict per Pattern 2 (D-V1 four-step checklist for Path A); records active-session step 3 ✗ ⇒ FAIL"
      contains: "FAIL\\|PASS"
    - path: ".planning/spikes/128b-write-back/evidence/pathA-final-pane.txt"
      provides: "Final tmux pane capture of the `claude --resume` session — shows whether 1337 ever appeared (predicted: it did not)"
      contains: "claude"
    - path: ".planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt"
      provides: "Post-append state of the copied session JSONL — confirms the row was written to the file (step 2 ✓) even if Claude never read it (step 3 ✗)"
      contains: "user"
  key_links:
    - from: ".planning/spikes/128b-write-back/pathA-jsonl-append.sh"
      to: "~/.claude/projects/-home-morrillboss-dev-dailybrief/*.jsonl (corpus — READ-ONLY; copied to scratch)"
      via: "cp $PROJDIR/$TARGET_SID.jsonl $SCRATCH/proj/ — operates on a copy, never the live file"
      pattern: "PROJDIR.*morrillboss-dev-dailybrief"
    - from: ".planning/spikes/128b-write-back/pathA-jsonl-append.sh"
      to: "tmux session `spike-128b-pathA-$$` (disposable, killed on trap)"
      via: "tmux new-session -d -s spike-128b-pathA-$$"
      pattern: "spike-128b-pathA-\\$\\$|spike-128b-pathA-"
---

<objective>
Empirically probe Path A (JSONL append + IPC) — the unofficial OS-side workaround that asks "does the running interactive `claude` process re-read its session JSONL mid-session when a user row is appended (with SIGUSR1 or other signals)?" Predicted answer: NO. Per RESEARCH "Path A" methodology + CONTEXT D-O1 path (a): "Likely NO mid-session re-read, in which case Path A is BLOCK unless paired with a session-restart." This plan records the empirical confirmation.

Purpose: Satisfy the second of the three enumerated empirical paths for G2-REPLY-01 success criterion 1. The strongest support for "JSONL-watch is not a viable production path" comes from confirming empirically (not just analytically) that no available signal mechanism causes a mid-session re-read. This gives Phase 133 a documented reason to NOT pursue a JSONL-based writer architecture.

CRITICAL clobber-protection (D-T5): the probe operates on a COPY of a real `.jsonl` session under a `mktemp -d` scratch dir — never on the live `~/.claude/projects/-home-morrillboss-dev-dailybrief/` corpus, and never on the spike-runner's own session ID (`$CLAUDE_SESSION_ID` if set). Tmux session name uses the unique `spike-128b-pathA-$$` pattern; cleaned up on trap-EXIT.

Output: One probe script + four evidence files (at-detection snapshot if PASS, final pane if FAIL, post-append jsonl state, mini-verdict transcript). Predicted FAIL active-session ⇒ Plan 05 SPIKE-DECISION enters this as `FAIL` in the per-path table. Per D-V4 max aggregation, this does NOT change overall PASS (Path E dominates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh
@.planning/spikes/001-tmux-write-back-128b/README.md

<interfaces>
<!-- Locked from CONTEXT.md D-T5 and RESEARCH.md §"Path A" methodology -->
<!-- Executor uses these directly — no additional codebase exploration needed -->

D-T5 clobber-protection rules (BOTH must be enforced before any file write):
  1. SKIP the live session: pick a target session ID from the corpus that is NOT equal to `$CLAUDE_SESSION_ID` (if set)
  2. Operate on a COPY: `cp $PROJDIR/$TARGET_SID.jsonl $SCRATCH/proj/$TARGET_SID.jsonl` — append only to the copy

D-V1 four-step gate (same as Plan 01):
  step 1 — non-TTY writer originates the reply (script echo >> file)
  step 2 — string reaches input channel candidate (the JSONL file — verified by inspecting `$SCRATCH/proj/$TARGET_SID.jsonl` after append)
  step 3 — Claude processes as next user turn (verified by sentinel 1337 appearing in the tmux pane capture)
  step 4 — session continues healthy ≥60s (only meaningful if step 3 ✓)

D-V3 unsafe-primitive ban (these are FORBIDDEN as PASS evidence — would invalidate the verdict):
  - ptrace-based hot reload
  - `/proc/<pid>/fd/0` writes
  - kernel-level fd injection

Permitted signal attempts (per RESEARCH §"Path A"):
  - SIGUSR1 to the claude PID via `kill -USR1` (common Unix re-read convention)
  - inotify wait (no — Claude doesn't subscribe; just documents the absence)
  - `--resume <sid>` from a separate process to force restart-style re-read (this would be DEGRADE, NOT PASS, because it requires a session restart)

Sentinel pattern (Pitfall 1, identical to Plan 01):
  user-row content = "Compute seven multiplied by one hundred ninety-one. Reply with only the resulting integer."
  sentinel = 1337 (NOT in prompt text; computed by model)

JSONL user-row shape (verified from RESEARCH §"Path A" line 377):
  {"type":"user","message":{"role":"user","content":"<text>"}}

Tmux session name pattern (CONVENTIONS.md mandate):
  SESSION="spike-128b-pathA-$$"  ← $$ = shell PID, unique per-run

Bail condition (RESEARCH wallclock cap): 30 seconds of polling for sentinel; if not detected, declare step 3 FAIL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + run pathA-jsonl-append.sh probe with clobber-protection + snapshot-at-detection</name>
  <files>.planning/spikes/128b-write-back/pathA-jsonl-append.sh, .planning/spikes/128b-write-back/evidence/pathA-at-detection.txt, .planning/spikes/128b-write-back/evidence/pathA-final-pane.txt, .planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt, .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md</files>
  <read_first>
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Path A — JSONL append + IPC" lines 337-407 (the verbatim concrete probe script + predicted verdict + bail conditions — DO NOT re-derive; adapt with snapshot-at-detection writing both at-detection AND final-pane evidence files, and a self-contained TRANSCRIPT heredoc tail block)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-T5" (clobber-protect — stamp $CLAUDE_SESSION_ID; refuse to write if target SID equals it)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md §"D-G1" (variable names must be redaction-safe; do not use `secret`/`token`/`auth`/`bearer`/`apiKey`)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Pitfall 2" (snapshot-at-detection — TUI alt-screen erases evidence on re-render)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Pitfall 4" (touching the live spike-runner session — use unique tmux session name + scratch dir)
    - .planning/spikes/001-tmux-write-back-128b/L4-needs-input-pause.sh (CANONICAL pattern source for trap-cleanup, snapshot-at-detection loop with `tmux capture-pane -p -t "$SESSION"`, and the `for i in $(seq 1 N); do sleep 1; CAP=...; if grep -q ...; then echo "$CAP" > "$LOG.at-detection"; break; fi; done` shape — adapt the script structure but do NOT extend that file; this is a NEW script in `.planning/spikes/128b-write-back/`)
    - .planning/spikes/CONVENTIONS.md §"Clobber-protection" + §"Snapshot at moment of detection" + §"Tossable headers"
    - vigil-core/src/analytics/posthog.ts:32 (THE source-of-truth BLOCKED_PROPERTY_NAMES Set; the spike's variable names must avoid all of these patterns)
  </read_first>
  <action>
    Create the script `.planning/spikes/128b-write-back/pathA-jsonl-append.sh` (mode 0755) following the structure below. The script extends the verbatim RESEARCH §"Path A" methodology by adding: (a) explicit D-T5 clobber-protect refusal with a hard `exit 2` if target SID equals `$CLAUDE_SESSION_ID`; (b) snapshot-at-detection pattern writing `pathA-at-detection.txt` immediately on match AND `pathA-final-pane.txt` always at end-of-poll; (c) post-append JSONL state capture to `pathA-jsonl-after.txt`; (d) inline mechanical TRANSCRIPT heredoc.

    Script structure:

    1. Shebang + TOSSABLE header (THREE lines verbatim per CONVENTIONS.md, but adjust the third line for Path A):
       `#!/usr/bin/env bash`
       `# PHASE 128b SPIKE — TOSSABLE. Phase 133 owns G2-REPLY productionization;`
       `# this file is spike-only and SHOULD BE DELETED after the verdict is committed.`
       `# Path A: JSONL append + IPC against a copy of a corpus session (D-O1 step 3 — after Path B and Path D)`
    2. `set -euo pipefail`
    3. Vars (D-G1 — descriptive non-blocked names):
       `EVIDENCE_DIR=".planning/spikes/128b-write-back/evidence"`
       `mkdir -p "$EVIDENCE_DIR"`
       `SESSION="spike-128b-pathA-$$"`
       `SCRATCH=$(mktemp -d -t spike-128b-A-XXXXXX)`
       `PROJDIR="$HOME/.claude/projects/-home-morrillboss-dev-dailybrief"`
    4. Trap-cleanup (cleans BOTH tmux session AND scratch dir):
       `trap 'tmux kill-session -t "$SESSION" 2>/dev/null || true; rm -rf "$SCRATCH"' EXIT`
    5. D-T5 clobber-protect — pick a target SID from corpus that is NOT `$CLAUDE_SESSION_ID`:
       ```
       TARGET_SID=""
       for f in "$PROJDIR"/*.jsonl; do
         SID=$(basename "$f" .jsonl)
         [ "${CLAUDE_SESSION_ID:-}" = "$SID" ] && continue
         TARGET_SID="$SID"
         break
       done
       if [ -z "$TARGET_SID" ]; then
         echo "[Path A] ERROR: no safe session ID in $PROJDIR (only live session present)" >&2
         exit 2
       fi
       echo "[Path A] target session ID (copied): $TARGET_SID"
       ```
    6. Copy the target session to scratch:
       ```
       COPYDIR="$SCRATCH/proj"
       mkdir -p "$COPYDIR"
       cp "$PROJDIR/$TARGET_SID.jsonl" "$COPYDIR/"
       ```
    7. Start `claude --resume` in a fresh tmux pane against the COPY (DO NOT touch the live corpus):
       ```
       tmux new-session -d -s "$SESSION" -x 240 -y 60 \
         "cd '$COPYDIR' && claude --resume '$TARGET_SID' --model haiku"
       sleep 8
       ```
       Note: `claude --resume` reads from `~/.claude/projects/` by default. Since the COPY is under `$COPYDIR` and we `cd` into a different cwd path before launching, the resume may operate on a fresh-state context — record this in the TRANSCRIPT under "Methodology caveat" if observed. The empirical question is still valid: does a running `claude` re-read the JSONL when it's appended? If `claude --resume <sid>` instead reads from the user's `~/.claude/projects/...`, the test still works because the corpus copy is the SAME session ID — `claude` will treat it as a resume.

       ALTERNATIVE (preferred for cleanliness): start a NEW claude session in the scratch dir, then capture its newly-created session ID from the latest file under `~/.claude/projects/-home-morrillboss-dev-dailybrief/` or `~/.claude/projects/<scratch-cwd-encoded>/`, and append to THAT file. The choice between resume-corpus-copy and new-session-in-scratch should be made empirically: the script attempts resume-corpus-copy first; if `tmux capture-pane` shows claude has launched normally (waiting for user input), proceed with the append test; if claude errors on resume, fall back to the new-session pattern. Record the choice in the TRANSCRIPT.
    8. Capture initial pane state for forensics:
       `INITIAL_PANE=$(tmux capture-pane -p -t "$SESSION")`
       `echo "$INITIAL_PANE" > "$SCRATCH/initial-pane.txt"`
    9. Append a user row with the false-positive-resistant sentinel prompt:
       ```
       APPEND='{"type":"user","message":{"role":"user","content":"Compute seven multiplied by one hundred ninety-one. Reply with only the resulting integer."}}'
       echo "$APPEND" >> "$COPYDIR/$TARGET_SID.jsonl"
       # Capture post-append JSONL state (tail only — full file may be large from the corpus)
       tail -5 "$COPYDIR/$TARGET_SID.jsonl" > "$EVIDENCE_DIR/pathA-jsonl-after.txt"
       ```
    10. Try SIGUSR1 IPC (common Unix re-read signal):
        ```
        CLAUDE_PID=$(pgrep -f "claude --resume $TARGET_SID" 2>/dev/null | head -1 || true)
        if [ -n "$CLAUDE_PID" ]; then
          echo "[Path A] sending SIGUSR1 to claude PID $CLAUDE_PID"
          kill -USR1 "$CLAUDE_PID" 2>/dev/null || true
        else
          echo "[Path A] WARN: no claude PID found via pgrep — SIGUSR1 skipped; relying on inotify (which claude is not subscribed to)"
        fi
        ```
    11. Snapshot-at-detection polling loop (Pitfall 2 — preserve evidence at the instant of match):
        ```
        DETECTED=0
        for i in $(seq 1 30); do
          sleep 1
          CAP=$(tmux capture-pane -p -t "$SESSION" 2>/dev/null || echo "")
          if echo "$CAP" | grep -q "1337"; then
            echo "$CAP" > "$EVIDENCE_DIR/pathA-at-detection.txt"
            DETECTED=1
            echo "[Path A] UNEXPECTED PASS — sentinel 1337 detected at iteration $i; pane preserved"
            break
          fi
        done
        # Final pane capture regardless of detection — for forensic post-mortem
        tmux capture-pane -p -S -200 -t "$SESSION" 2>/dev/null > "$EVIDENCE_DIR/pathA-final-pane.txt" || true
        ```
    12. Mechanical TRANSCRIPT heredoc (writes `pathA-TRANSCRIPT.md`):
        ```
        cat > "$EVIDENCE_DIR/pathA-TRANSCRIPT.md" <<EOF
        # Path A — Per-Path D-V1 Mini-Verdict

        Probe run: $(date -Iseconds)
        Script: .planning/spikes/128b-write-back/pathA-jsonl-append.sh
        claude version: $(claude --version 2>/dev/null || echo unknown)
        Target session (COPY — D-T5 clobber-protect): $TARGET_SID
        Live session ID (\$CLAUDE_SESSION_ID, if set): ${CLAUDE_SESSION_ID:-unset}
        Tmux session (disposable): $SESSION
        Scratch dir: $SCRATCH (cleaned on EXIT)

        ## D-V1 Four-Step Gate (Active-Session)

        | Step | Description | Verdict |
        |------|-------------|---------|
        | 1    | Reply originates from non-TTY writer | ✓ (bash \`echo "\$APPEND" >> ...\`) |
        | 2    | String reaches input channel candidate (the JSONL file) | ✓ (verified by \`tail -5\` of \$COPYDIR/\$TARGET_SID.jsonl showing the appended row — see pathA-jsonl-after.txt) |
        | 3    | Claude processes as next user turn (sentinel 1337 in pane) | $([ "$DETECTED" -eq 1 ] && echo "✓ UNEXPECTED — see pathA-at-detection.txt" || echo "✗ (no mid-session JSONL re-read confirmed — 30s poll exhausted)") |
        | 4    | Session continues healthy ≥60s | $([ "$DETECTED" -eq 1 ] && echo "needs follow-up probe — UNEXPECTED PASS path" || echo "N/A (step 3 ✗, step 4 vacuous)") |

        ## Mini-Verdict

        **Active-session: $([ "$DETECTED" -eq 1 ] && echo "PASS (unexpected — see RESEARCH §Assumption A1 Risk note)" || echo "FAIL")**

        Per CONTEXT D-V3 candidate first bullet ("All 3+ tested paths produce no observable round-trip"):
        Path A mini-verdict ⇒ **$([ "$DETECTED" -eq 1 ] && echo "PASS (overall verdict unchanged — Path E already PASS)" || echo "FAIL")**

        ## Signal attempts

        - SIGUSR1 sent to claude PID: $([ -n "${CLAUDE_PID:-}" ] && echo "yes (PID $CLAUDE_PID)" || echo "no — pgrep returned empty")
        - inotify: NOT attempted (claude is not subscribed to filesystem watch per RESEARCH §"Path A")
        - ptrace / proc-fd writes: FORBIDDEN per CONTEXT D-V3 (unsafe primitive)

        ## Evidence

        - \`pathA-jsonl-after.txt\` — last 5 lines of the copied session JSONL after append (proves step 2 ✓)
        - \`pathA-at-detection.txt\` — pane snapshot at moment of sentinel match (only present if step 3 ✓; absent on predicted FAIL)
        - \`pathA-final-pane.txt\` — final pane capture (forensic post-mortem regardless of outcome)

        ## D-T5 Clobber-Protect Verification

        - Live session ID (CLAUDE_SESSION_ID): ${CLAUDE_SESSION_ID:-unset}
        - Target SID (selected from corpus, EXCLUDED if equal to live): $TARGET_SID
        - JSONL append landed on COPY at: $COPYDIR/$TARGET_SID.jsonl
        - Live JSONL at $PROJDIR/$TARGET_SID.jsonl: NOT modified (cp is one-way; the live corpus is read-only in this probe)

        ## Cited

        - CONTEXT D-V1 / D-V3 / D-T5 (verdict gates + clobber-protect)
        - CONTEXT D-O1 path (a) — predicted BLOCK
        - RESEARCH §"Path A" — predicted FAIL active-session
        - RESEARCH §"Assumption A1" — risk LOW (verdict robust even if A1 wrong)
        EOF
        echo "[Path A] transcript written: $EVIDENCE_DIR/pathA-TRANSCRIPT.md"
        ```
    13. Final exit: `exit 0` (the trap cleans tmux + scratch; evidence files in `$EVIDENCE_DIR` persist).

    Make script executable. RUN it: `bash .planning/spikes/128b-write-back/pathA-jsonl-append.sh`. The 30-iteration poll bounds wallclock at ~40 seconds; total script wallclock ≤90s including 8s claude startup.

    If `pgrep` returns empty (CLAUDE_PID unset), the script continues — the SIGUSR1 branch is best-effort; the FAIL signal is the absence of mid-session re-read regardless of whether SIGUSR1 was delivered. Per CONTEXT D-V3, do NOT escalate to ptrace if SIGUSR1 has no effect.

    Per CONTEXT D-G1: variable names `EVIDENCE_DIR`, `SESSION`, `SCRATCH`, `PROJDIR`, `TARGET_SID`, `SID`, `COPYDIR`, `INITIAL_PANE`, `APPEND`, `CLAUDE_PID`, `CAP`, `DETECTED` — none match the BLOCKED_PROPERTY_NAMES patterns from `vigil-core/src/analytics/posthog.ts:32` (no `token`/`auth`/`secret`/`bearer`/`apiKey`).

    Per Pitfall 7 (D-A2): do NOT create an `ALLOWED_REPLIES` constant. The spike's test string is the compute-prompt only.

    Per Pitfall 3 (D-G3): do NOT use `--bare`. Operator's Claude Max OAuth handles auth.
  </action>
  <verify>
    <automated>test -x .planning/spikes/128b-write-back/pathA-jsonl-append.sh && head -4 .planning/spikes/128b-write-back/pathA-jsonl-append.sh | grep -q "PHASE 128b SPIKE — TOSSABLE" && grep -q "set -euo pipefail" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && grep -q "spike-128b-pathA-\$\$" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && grep -q "CLAUDE_SESSION_ID" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && grep -q "trap.*kill-session\|trap.*rm -rf" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && ! grep -E "(TOKEN|AUTH|SECRET|BEARER|APIKEY)=" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && ! grep -q -- "--bare" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && ! grep -E "ptrace|/proc/[^/]+/fd/0" .planning/spikes/128b-write-back/pathA-jsonl-append.sh && bash .planning/spikes/128b-write-back/pathA-jsonl-append.sh && test -f .planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt && test -f .planning/spikes/128b-write-back/evidence/pathA-final-pane.txt && test -f .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md && grep -qE 'FAIL|PASS' .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md && grep -q 'D-T5' .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md && echo "Path A empirical record complete"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/pathA-jsonl-append.sh` exists, mode 0755, executable
    - First 4 lines of the script (after shebang) contain `PHASE 128b SPIKE — TOSSABLE`, `Phase 133 owns G2-REPLY productionization`, and `Path A: JSONL append + IPC`
    - Script contains `set -euo pipefail`
    - Script contains the unique tmux session pattern `spike-128b-pathA-$$` (clobber-protect via PID-suffixed session name)
    - Script contains a `CLAUDE_SESSION_ID` reference (verifies D-T5 clobber-protect logic is present)
    - Script contains a trap-cleanup with both `kill-session` AND `rm -rf` (cleans up tmux + scratch dir)
    - Script does NOT contain blocked-property-name variable assignments (grep `^(TOKEN|AUTH|SECRET|BEARER|APIKEY)=` returns 0 matches per D-G1)
    - Script does NOT contain `--bare` (Pitfall 3)
    - Script does NOT contain `ptrace` or `/proc/<pid>/fd/0` references (D-V3 unsafe-primitive ban)
    - Running the script via `bash .planning/spikes/128b-write-back/pathA-jsonl-append.sh` exits with status 0 in ≤120 seconds
    - `.planning/spikes/128b-write-back/evidence/pathA-jsonl-after.txt` exists and contains the literal substring `user` (the appended user-row content)
    - `.planning/spikes/128b-write-back/evidence/pathA-final-pane.txt` exists (forensic post-mortem; may be empty if tmux session died early — record that in TRANSCRIPT if observed)
    - `.planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md` exists and contains: (a) the D-V1 four-step gate table; (b) a `D-T5 Clobber-Protect Verification` section with `Target SID` and `Live session ID`; (c) a mini-verdict line resolving to either `FAIL` (predicted per RESEARCH) or `PASS (unexpected — see RESEARCH §Assumption A1 Risk note)` (mechanically computed from `$DETECTED`)
    - The live corpus file `~/.claude/projects/-home-morrillboss-dev-dailybrief/$TARGET_SID.jsonl` is unchanged after probe (NOT verified by automated grep — but ensured by the structural property that the script only writes to `$COPYDIR`, never to `$PROJDIR`. Reviewer can spot-check by comparing line counts before/after.)
    - Total Anthropic API spend ≤$0.05 (slightly higher than Path B because claude --resume + 8s settle + 30s poll; still well within budget per RESEARCH §Cost)
  </acceptance_criteria>
  <done>
    Path A empirically probed against a corpus copy (NOT the live session); the JSONL append landed on the copy (step 2 ✓ verified by `pathA-jsonl-after.txt`); SIGUSR1 dispatched best-effort; 30-iteration sentinel poll completed; transcript records the mechanical D-V1 mini-verdict (predicted FAIL per RESEARCH; recorded actual). D-T5 clobber-protect verified by the script's own `CLAUDE_SESSION_ID` check and the structural use of `$COPYDIR` not `$PROJDIR` for the append. Plan 06 MEASUREMENTS and Plan 05 SPIKE-DECISION can cite `pathA-TRANSCRIPT.md` directly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-runner shell → tmux server | Unique session name (`spike-128b-pathA-$$`) + 0700 tmux socket; no cross-uid access |
| live corpus dir → scratch dir | One-way `cp` of one JSONL file; live corpus is read-only for the probe |
| script → claude subprocess | Subprocess runs in disposable tmux pane against the scratch dir; cannot reach the operator's live Claude Code session by construction |
| script → signal surface | SIGUSR1 best-effort; failure to deliver is non-fatal (D-V3 forbids escalating to ptrace) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-02-01 | Tampering | spike-runner's live Claude Code session (D-T5) | mitigate | Script enforces `[ "${CLAUDE_SESSION_ID:-}" = "$SID" ] && continue` in the target-SID selection loop AND operates on a `cp`'d copy under `mktemp -d`, never on `$PROJDIR/*`. Unique tmux session name `spike-128b-pathA-$$` prevents collision with operator's tmux sessions. Trap-cleanup kills the disposable session on EXIT. |
| T-128b-02-02 | Tampering | live JSONL corpus (`~/.claude/projects/-home-morrillboss-dev-dailybrief/`) | mitigate | The append target is `$COPYDIR/$TARGET_SID.jsonl` (a fresh copy under `$SCRATCH`), NEVER `$PROJDIR/$TARGET_SID.jsonl`. Verified by the script's structural choice — no write paths reference `$PROJDIR`. |
| T-128b-02-03 | Information Disclosure | corpus JSONL content (may contain past conversation content) | mitigate | `pathA-jsonl-after.txt` is a `tail -5` of the appended file — captures the appended row plus 4 lines of context. Reviewer SHOULD spot-check this file before committing if the corpus session contained sensitive content (operator decides at commit time). The committed evidence covers only the appended portion structurally. |
| T-128b-02-04 | Elevation of Privilege | unsafe-primitive temptation if SIGUSR1 fails (D-V3) | mitigate | Script's signal branch is `kill -USR1 "$CLAUDE_PID" 2>/dev/null || true` — failure is non-fatal and does NOT escalate to ptrace. The 30-iteration poll observes natural behavior; absence of sentinel ⇒ FAIL, not "try harder primitive." |
| T-128b-02-05 | Repudiation | subjective verdict override (Pitfall 5) | mitigate | TRANSCRIPT.md is generated by a heredoc using `$DETECTED` (computed from grep), NOT by hand. Reviewer can re-run the probe and confirm reproducibility. |
| T-128b-02-06 | Denial of Service | trap-cleanup failure leaves disposable tmux session running | accept | Trap uses `|| true` so failure to kill doesn't cascade; the unique session name (`spike-128b-pathA-$$`) makes manual cleanup trivially `tmux kill-session -t spike-128b-pathA-*`. Operator can sweep stale sessions with `tmux ls | grep spike-128b- | cut -d: -f1 | xargs -I{} tmux kill-session -t {}`. |
</threat_model>

<verification>
1. Path A probe script has TOSSABLE header, `set -euo pipefail`, unique `spike-128b-pathA-$$` tmux name, D-T5 `CLAUDE_SESSION_ID` check, trap-cleanup.
2. Script does NOT contain blocked-property-name vars, `--bare`, `ptrace`, or `/proc/<pid>/fd/0` writes.
3. Probe runs to completion writing `pathA-jsonl-after.txt`, `pathA-final-pane.txt`, `pathA-TRANSCRIPT.md` into `evidence/`.
4. JSONL append lands on `$COPYDIR/$TARGET_SID.jsonl` (the copy), NOT `$PROJDIR/$TARGET_SID.jsonl` (the live corpus) — structurally enforced.
5. TRANSCRIPT records the mechanical D-V1 mini-verdict: FAIL (predicted per RESEARCH) or PASS-unexpected (per RESEARCH Assumption A1 Risk note). Either is structurally valid.
6. D-T5 Clobber-Protect Verification section in TRANSCRIPT names the target SID and the live session ID explicitly.
</verification>

<success_criteria>
- Path A probe runs once successfully against a corpus session COPY (D-T5 enforced)
- 30-iteration snapshot-at-detection poll completes; evidence files preserved at-detection if sentinel matches, final-pane always
- `pathA-TRANSCRIPT.md` resolves to either `FAIL` or `PASS (unexpected)` mechanically — no subjective edit
- Live corpus `~/.claude/projects/-home-morrillboss-dev-dailybrief/$TARGET_SID.jsonl` unchanged
- No `tmux ls | grep spike-128b-pathA-` sessions remain after script exit (trap cleanup verified by absence)
- Total wallclock ≤2h (RESEARCH §"Path A" bail at 90 min if no signal works; this script's 30-iteration poll is well inside)
- Plan 05 SPIKE-DECISION can cite `pathA-TRANSCRIPT.md` as the empirical source for Path A's row in the per-path verdict table
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-02-SUMMARY.md` recording:
- The probe run wallclock, exit code, and the verbatim `pathA-TRANSCRIPT.md` mini-verdict
- The selected `TARGET_SID` (which session ID from the corpus the probe operated on) and confirmation it was NOT `$CLAUDE_SESSION_ID`
- Whether `pgrep` found a claude PID and SIGUSR1 was delivered (yes/no with PID; or no — pgrep empty)
- Whether the sentinel `1337` was detected in the 30-iteration poll (DETECTED=0 expected per RESEARCH; record actual)
- Any deviations from the verbatim RESEARCH §"Path A" probe shape — and why each was necessary (e.g., the resume-vs-new-session decision recorded in the TRANSCRIPT's "Methodology caveat" line)
- A `## Clobber-protect audit` section confirming: (a) the live corpus file was unchanged (by structural inspection — script only writes to `$COPYDIR`); (b) the tmux session name was `spike-128b-pathA-$$`; (c) the disposable tmux session was killed on trap-EXIT (no stale `spike-128b-pathA-*` sessions remain)
</output>
