---
phase: 128b
plan: 05
type: execute
wave: 3
depends_on: [128b-01, 128b-02, 128b-03, 128b-04, 128b-06]
files_modified:
  - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md
  - .planning/spikes/128b-write-back/README.md
  - .planning/spikes/MANIFEST.md
autonomous: true
requirements: [G2-REPLY-01]
tags: [128b, write-back, spike, spike-decision, verdict, phase-133-handoff]
must_haves:
  truths:
    - "128b-SPIKE-DECISION.md exists with the verdict at the TOP (first non-frontmatter line is `**VERDICT: PASS**` per CONTEXT D-V4 + Phase 128a D-V1 precedent)"
    - "Per-path table contains FIVE rows (A + B + C + D + E ★) populated from the four per-path TRANSCRIPT.md files; verdict cells are mechanically computed (not subjectively edited)"
    - "Privilege/portability sketch section CITES spike 001 README §'Privilege & portability sketch' lines 182-218 verbatim (D-A2-compliant) — does NOT re-derive"
    - "Phase 133 scope-lock section copies the SURFACE-MAP.md 2026-05-14 architecture shift verbatim (Before/After/Trust-posture/Operator-workflow-target/Launcher-wrapper-UX/Local-network — five sub-sections per RESEARCH §'Phase 133 Scope-Lock Implications')"
    - "Trust posture (unknown-user-profile incident → pull-based architecture) is documented explicitly: vigil-tmux-bridge consumes outbound SSE from Railway; if Railway compromised, blast radius is 5 allowlisted strings (per RESEARCH §'Phase 133 Scope-Lock Implications' §2)"
    - "INBOUND vs OUTBOUND injection asymmetry documented (per RESEARCH §'Trust-Model Asymmetry') — 128b is OUTBOUND; CHAT-CTX-02's <thought> delimiter pattern does NOT apply"
    - "G2-REPLY-01 success criteria 1-4 are individually mapped to the artifacts that satisfy them (per RESEARCH §'Criteria Mapping' table)"
    - "MANIFEST.md verdict cell for the 128b-write-back row is updated from `in-progress` to `VALIDATED-PASS` if Plan 04 PASSed, or to the actual mechanical aggregate otherwise"
  artifacts:
    - path: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md"
      provides: "Verdict at TOP + per-path table + privilege sketch reference + Phase 133 scope-lock + trust posture + INBOUND/OUTBOUND asymmetry"
      contains: "VERDICT: PASS\\|VERDICT: DEGRADE\\|VERDICT: BLOCK"
    - path: ".planning/spikes/128b-write-back/README.md"
      provides: "Updated frontmatter verdict field reflecting Plan 05 outcome (in-progress → VALIDATED-PASS or actual aggregate)"
      contains: "verdict:"
    - path: ".planning/spikes/MANIFEST.md"
      provides: "Updated Verdict column for the 128b-write-back row"
      contains: "128b-write-back"
  key_links:
    - from: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md §Privilege & portability sketch"
      to: ".planning/spikes/001-tmux-write-back-128b/README.md lines 182-218"
      via: "verbatim citation; no re-derivation"
      pattern: "spike 001 README.*Privilege"
    - from: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md §Phase 133 scope-lock"
      to: ".planning/research/SURFACE-MAP.md §'Recent architecture shift (2026-05-14)'"
      via: "verbatim Before/After block copied per RESEARCH §'Phase 133 Scope-Lock Implications' §1"
      pattern: "vigil-tmux-bridge\\|Recent architecture shift"
    - from: ".planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md §Per-path table"
      to: ".planning/spikes/128b-write-back/evidence/{pathA,pathB,pathD,pathE}-TRANSCRIPT.md"
      via: "verdict cells populated from per-path TRANSCRIPT mini-verdicts (mechanically computed per Plan 01-04)"
      pattern: "pathA-TRANSCRIPT|pathB-TRANSCRIPT|pathD-TRANSCRIPT|pathE-TRANSCRIPT"
---

<objective>
Author `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md` — the phase's primary deliverable. The verdict at the TOP is `**VERDICT: PASS**` (per D-V4 max aggregation: Path E PASS dominates regardless of A/B/C/D outcomes, per RESEARCH §"Per-Path Predicted-Verdict Summary" and the spike 001 empirical evidence) UNLESS Plan 04's regression run FAILed — in which case the verdict is mechanically re-computed from the actual per-path TRANSCRIPT.md files produced by Plans 01-04.

Document structure (in this exact order, per Phase 128a D-V1 precedent + RESEARCH §"Phase 133 Scope-Lock Implications" + CONTEXT §"Phase Boundary" success criteria 1-4):

1. Verdict at TOP (first non-frontmatter line: `**VERDICT: <result>**`)
2. Driving evidence (one-paragraph summary citing the per-path TRANSCRIPT that established the verdict)
3. Per-path table (5 rows: A / B / C / D / E ★) with D-V1 four-step columns populated from each per-path TRANSCRIPT
4. G2-REPLY-01 success criteria mapping (4 criteria each mapped to an artifact per RESEARCH §"Criteria Mapping")
5. Privilege & portability sketch (REFERENCED, not re-derived — cite spike 001 README lines 182-218 verbatim with a one-line "copied below for self-containment" framing)
6. Phase 133 scope-lock implications (5 sub-sections per RESEARCH §"Phase 133 Scope-Lock Implications": writer-process location, trust posture, operator workflow target, launcher-wrapper UX, local-network constraint)
7. Trust-Model Asymmetry — INBOUND vs OUTBOUND injection (verbatim from RESEARCH §"Trust-Model Asymmetry")
8. Three Phase 133 implications callout (from spike 001 README §"Three Phase 133 implications")
9. Cost summary (per RESEARCH §Cost + Plan 06 MEASUREMENTS aggregate)
10. Re-activation conditions (ONLY if verdict is BLOCK or DEGRADE; SKIP this section if PASS — per CONTEXT D-V3 + SEED-003 DMARC pattern)

Purpose: This file is the gate. Phase 133 reads it to know whether to scope-lock to full G2-REPLY-02..04 (PASS), G2-REPLY-05 banner-ack-only (DEGRADE), or terminate G2-REPLY (BLOCK). Per CONTEXT D-V1: "Active-session test passes AND fresh-session test passes ⇒ PASS. Scope-locks Phase 133 to full G2-REPLY-02..04."

Also: update `.planning/spikes/128b-write-back/README.md` frontmatter `verdict:` field from `in-progress` to the actual verdict, and update MANIFEST.md's Verdict column for the 128b-write-back row.

Per CONTEXT D-V4: the verdict is mechanically computed. Plan 05's author does NOT subjectively decide; the verdict is the `MAX(per-path)` of the four per-path TRANSCRIPT.md mini-verdicts produced by Plans 01-04. If Path E's TRANSCRIPT records PASS (re-confirmed) ⇒ overall PASS. If Path E's TRANSCRIPT records FAIL ⇒ recompute from A+B+D (predicted A=FAIL, B=DEGRADE, D=DEGRADE ⇒ MAX = DEGRADE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-CONTEXT.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-VALIDATION.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-01-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-02-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-03-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-04-SUMMARY.md
@.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-06-SUMMARY.md
@.planning/spikes/001-tmux-write-back-128b/README.md
@.planning/research/SURFACE-MAP.md
@.planning/research/PITFALLS.md
@.planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md
@.planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md

<interfaces>
<!-- Locked from CONTEXT D-V1/D-V2/D-V3/D-V4 + RESEARCH §"Criteria Mapping" + spike 001 README + SURFACE-MAP §"Recent architecture shift" -->

Verdict-at-TOP invariant (Phase 128a D-V1 precedent):
  First non-frontmatter line of SPIKE-DECISION.md MUST be `**VERDICT: PASS**` (or DEGRADE / BLOCK).
  This line is NOT editable once written. Re-tests that contradict open Phase 128b.1, NOT edit-this-line.

D-V4 mechanical aggregation:
  overall_verdict = MAX_BY_ORDER(PASS > DEGRADE > FAIL > INCONCLUSIVE, [pathA_mini, pathB_mini, pathC_mini, pathD_mini, pathE_mini])
  - PASS > anything ⇒ overall PASS
  - DEGRADE > FAIL/INCONCLUSIVE ⇒ overall DEGRADE
  - FAIL or INCONCLUSIVE only ⇒ overall depends on path C/D presence

  BUT D-V2 BLOCK gate first bullet: "All 3+ tested paths produce no observable round-trip" ⇒ overall BLOCK.
  Path E TRANSCRIPT PASS (re-confirmed) ⇒ at least one observable round-trip exists ⇒ BLOCK gate not tripped.

Predicted per-path verdicts (per RESEARCH §"Per-Path Predicted-Verdict Summary"):
  Path E ★: PASS (already validated; Plan 04 regression re-confirms or FAILs)
  Path B: DEGRADE (fresh-only)
  Path A: FAIL (active-session)
  Path D: DEGRADE (inverted model) OR INCONCLUSIVE (SDK fetch failure fallback)
  Path C: NOT TESTED — recorded as `INCONCLUSIVE — covered by Path E (structural refinement)` with citation to spike 001 README §"Position vs. the 4 enumerated 128b paths"

G2-REPLY-01 success criteria → artifact mapping (RESEARCH §"Criteria Mapping"):
  Criterion 1 (record empirical results for ≥3 of (a)/(b)/(c)/(d)) ← Paths A + B + D each have TRANSCRIPT.md; C has analytical row in per-path table
  Criterion 2 (PASS/DEGRADE/BLOCK verdict) ← mechanical via D-V4; cited at TOP of this document
  Criterion 3 (working PoC round-trip if PASS) ← Path E (spike 001 L4 + Plan 04 regression); evidence files under .planning/spikes/128b-write-back/evidence/pathE-*
  Criterion 4 (privilege model sketched) ← spike 001 README lines 182-218 (referenced, not re-derived)

Privilege & portability sketch (verbatim from spike 001 README lines 182-218):
  - 5-string allowlist constant: ['yes', 'no', 'continue', 'abort', 'defer']
  - `inject(target: TmuxTarget, reply: Reply): void` — allowlist gate + privilege check + spawnSync('tmux', [-S socket, send-keys, -t session, reply, Enter])
  - `dismissDialog(target, choice)` variant for navigation
  - Production hardening list (session-discovery, rate-limit, clobber-protect prefix, audit log, macOS install)

Phase 133 scope-lock — verbatim source (SURFACE-MAP.md §"Recent architecture shift (2026-05-14)"):
  ### Before
  - vigil-watch (Mac): owned write-back via local Mac tmux session
  - Mac: hosted Claude Code dev environment AND write-back daemon
  - Constraint: vigil-core ↔ vigil-watch local-network-bound (D-N1)
  ### After
  - Claude Code dev environment moves to Ubuntu server (single-tenant, no inbound exposure)
  - vigil-tmux-bridge (new, Ubuntu daemon): owns write-back; consumes agent_stream SSE outbound; runs tmux send-keys locally
  - vigil-watch (Mac) shrinks to presentation-only; rename to vigil-mac-companion is SEED-018 candidate
  - Local-network constraint on write-back path disappears

Trust posture (RESEARCH §"Phase 133 Scope-Lock Implications" §2):
  - vigil-core on Railway is multi-tenant; unknown-user-profile incident showed signup is publicly reachable
  - If Railway compromised, attacker can SSE-emit allowlisted-string replies — bounded blast radius (5 strings)
  - Ubuntu is single-tenant, no inbound exposure, only outbound HTTPS to vigil-core
  - The harder install (Ubuntu daemon) IS the access boundary

INBOUND vs OUTBOUND asymmetry (RESEARCH §"Trust-Model Asymmetry"):
  INBOUND (Pitfall 6, CHAT-CTX-02 scope): adversarial content in captured thoughts → <thought> delimiters + tag-breakout sanitization
  OUTBOUND (this phase): replies from G2 → 5-string allowlist + privilege drop + tmux 0700 socket
  Different threat models — DO NOT conflate; <thought> delimiter pattern does NOT apply to OUTBOUND.

Three Phase 133 implications (spike 001 README §"Three Phase 133 implications"):
  1. Path E is the recommended primary path
  2. Launcher wrapper is required (vigil-claude or equivalent)
  3. Path E generalizes beyond Claude Code (any TTY-reading interactive program in tmux pane)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 128b-SPIKE-DECISION.md (verdict at TOP, per-path table, criteria mapping, privilege sketch reference, Phase 133 scope-lock, trust posture, asymmetry, implications)</name>
  <files>.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md</files>
  <read_first>
    - .planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md (READ EACH FIELD — particularly the Regression Re-Run row's mini-verdict, which determines whether the overall verdict is PASS or whether to fall through to A+B+D aggregation)
    - .planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md (read the mini-verdict line for Path B's row)
    - .planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md (read the mini-verdict line for Path A's row)
    - .planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md (read the mini-verdict line for Path D's row)
    - .planning/spikes/001-tmux-write-back-128b/README.md §"Privilege & portability sketch (D-A2 — markdown only)" lines 182-218 (the verbatim pseudo-code to COPY INTO this SPIKE-DECISION — citing the source — for self-containment per D-A4 artifact list)
    - .planning/spikes/001-tmux-write-back-128b/README.md §"Three Phase 133 implications" lines 228-232 (the verbatim three-implication callout to copy)
    - .planning/research/SURFACE-MAP.md §"Recent architecture shift (2026-05-14)" (the Before/After section to copy verbatim into the Phase 133 scope-lock section)
    - .planning/research/PITFALLS.md §"Pitfall 6" lines 167-194 (the INBOUND injection threat — cite for the asymmetry section, do NOT extend)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Trust-Model Asymmetry" lines 603-614 (the INBOUND vs OUTBOUND table to copy)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Criteria Mapping" lines 539-547 (the success-criterion → artifact mapping to copy)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-RESEARCH.md §"Phase 133 Scope-Lock Implications" lines 548-601 (the 5 sub-sections to author per the directive)
    - .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-06-PLAN.md frontmatter + objective (the precedent for SPIKE-DECISION shape — Phase 128a's authoring plan establishes the verdict-at-TOP invariant)
  </read_first>
  <action>
    Read the four per-path TRANSCRIPT.md files first to extract each path's mini-verdict. Construct the per-path table populated from those mini-verdicts (do NOT use the RESEARCH predicted verdicts — use the actual empirical TRANSCRIPT outcomes).

    Compute the overall verdict mechanically per D-V4:
    - If `pathE-TRANSCRIPT.md` records `PASS (re-confirmed)` ⇒ overall = `PASS`
    - Else if any of A/B/D records PASS ⇒ overall = `PASS`
    - Else if any of A/B/D/E records DEGRADE AND NONE records FAIL-only-mode AND at least one observable round-trip exists ⇒ overall = `DEGRADE`
    - Else (all FAIL or INCONCLUSIVE; no observable round-trip) ⇒ overall = `BLOCK`

    Author the document with EXACTLY this structure. Do NOT include frontmatter at the top of the file (SPIKE-DECISION.md is a content document; the verdict invariant requires the FIRST line of content to be the verdict).

    ```markdown
    **VERDICT: <PASS|DEGRADE|BLOCK>**

    Authored: <today's date YYYY-MM-DD>
    Phase: 128b — G2-REPLY-01 write-back path spike
    Requirement: G2-REPLY-01 (REQUIREMENTS.md line 44)
    Driving evidence: <one-sentence pointer to the per-path TRANSCRIPT that established the verdict — e.g., "Path E PASS (re-confirmed in `pathE-TRANSCRIPT.md` Regression Re-Run row); D-V4 max aggregation yields overall PASS regardless of A/B/D outcomes.">

    ## Verdict computation (mechanical — D-V4)

    | Path | Per-path mini-verdict | Source |
    |------|------------------------|--------|
    | A — JSONL append + IPC | <verdict from pathA-TRANSCRIPT.md> | `.planning/spikes/128b-write-back/evidence/pathA-TRANSCRIPT.md` |
    | B — stream-json (claude -p) | <verdict from pathB-TRANSCRIPT.md> | `.planning/spikes/128b-write-back/evidence/pathB-TRANSCRIPT.md` |
    | C — named-pipe / FIFO | INCONCLUSIVE — not empirically tested; covered analytically by Path E (structural refinement) | spike 001 README §"Position vs. the 4 enumerated 128b paths" + RESEARCH §"Open Questions §2" |
    | D — MCP server hook | <verdict from pathD-TRANSCRIPT.md> | `.planning/spikes/128b-write-back/evidence/pathD-TRANSCRIPT.md` |
    | E ★ — tmux send-keys | <verdict from pathE-TRANSCRIPT.md Regression Re-Run row> | `.planning/spikes/128b-write-back/evidence/pathE-TRANSCRIPT.md` + spike 001 evidence (originals preserved at `.planning/spikes/001-tmux-write-back-128b/evidence/`) |

    **Aggregation:** `MAX(PASS > DEGRADE > FAIL > INCONCLUSIVE)` = **<computed result>**.

    ## D-V1 Four-Step Round-Trip — Path E (the PASS driver, ★)

    | Step | Description | Verdict | Evidence |
    |------|-------------|---------|----------|
    | 1 | Reply originates from non-TTY writer | ✓ | spike 001 §Results step 1 — bash script outside the tmux pane |
    | 2 | String reaches input channel | ✓ | `pathE-L4-permission-pause-snapshot.txt` — dialog state change captured |
    | 3 | Claude processes as next user turn | ✓ | `pathE-L4-tool-output-marker.txt` — Bash tool produced its output file (L4-TOOL-RAN-<id>) only possible after permission grant via injected Enter |
    | 4 | Session continues healthy ≥60s | ✓ | `pathE-L4-health-check-snapshot.txt` — health probe `13 * 17` returned `221` after 60s idle |

    ## G2-REPLY-01 Success Criteria — Artifact Mapping

    (Per RESEARCH §"Criteria Mapping" lines 539-547. Each criterion is satisfied by the cited artifact.)

    | # | Success criterion | Satisfied by | Notes |
    |---|--------------------|---------------|-------|
    | 1 | 128b-SPIKE-DECISION.md records empirical results for at least 3 of (a)/(b)/(c)/(d) | Paths A + B + D empirical TRANSCRIPT.md files (3 of 4 — exceeds the bar); C documented analytically; E ★ is a 5th not substituting for (a)/(b)/(c)/(d) | The per-path table above contains 5 rows; A/B/D are empirical; C is analytical; E ★ is the validated 5th path |
    | 2 | Decision file resolves to exactly one verdict — PASS / DEGRADE / BLOCK | Mechanically computed at TOP of this document via D-V4 max aggregation | Non-editable once written (Phase 128a D-V1 precedent); re-tests that contradict open Phase 128b.1 |
    | 3 | If PASS — working PoC round-trip exists (the 4-step D-V1 round-trip) | Path E (spike 001 L4 + Plan 04 regression re-run); evidence files at `.planning/spikes/128b-write-back/evidence/pathE-L4-*` (originals at `.planning/spikes/001-tmux-write-back-128b/evidence/`) | The 60s portfolio Loom (C-2 operator wallclock — Plan 08) replays this empirically as the success-criterion proxy artifact |
    | 4 | Privilege model sketched in markdown pseudo-code | spike 001 README §"Privilege & portability sketch (D-A2 — markdown only)" lines 182-218 — REFERENCED in §"Privilege & portability sketch" below | The sketch is reused by reference (D-A4 self-containment); the full TypeScript pseudo-code is copied verbatim into the next section with a citation marker |

    ## Privilege & portability sketch

    > Copied below verbatim from `.planning/spikes/001-tmux-write-back-128b/README.md` §"Privilege & portability sketch (D-A2 — markdown only)" lines 182-218. NOT re-derived (D-A2 + RESEARCH §"Criteria Mapping" criterion 4 reuse-by-reference posture).

    ```typescript
    // PSEUDO-CODE — Phase 133 productionizes (G2-REPLY-04)
    const ALLOWED_REPLIES = ['yes', 'no', 'continue', 'abort', 'defer'] as const;
    type Reply = typeof ALLOWED_REPLIES[number];

    interface TmuxTarget {
      socketPath: string;   // $TMPDIR/tmux-$UID/default on macOS, /tmp/tmux-$UID/default on Linux
      sessionName: string;  // unique per-Claude-Code launch (e.g., "vigil-claude-1715712345")
    }

    function inject(target: TmuxTarget, reply: Reply): void {
      // 1. Allowlist gate — drift-detector test pins this at the call site (G2-REPLY-04)
      if (!ALLOWED_REPLIES.includes(reply)) {
        throw new Error(`disallowed reply: ${reply}`);
      }

      // 2. Privilege drop — already running as operator's user; tmux socket
      // is 0700 to operator. NO setuid, NO ptrace, NO root.

      // 3. Send the reply text + Enter. Note: tmux send-keys accepts the literal
      // string as a single arg, no shell-injection surface for the 5 allowlisted
      // strings (all alphanumeric, no special chars).
      spawnSync('tmux', [
        '-S', target.socketPath,
        'send-keys',
        '-t', target.sessionName,
        reply,
        'Enter',
      ], { stdio: 'inherit' });
    }

    // 4. For dialog-only dismissal (banner-ack without a typed reply):
    function dismissDialog(target: TmuxTarget, choice: 'accept' | 'deny'): void {
      // Default option is "Yes" (1); navigate to "No" (3) requires Down-Down
      const keys = choice === 'accept' ? ['Enter'] : ['Down', 'Down', 'Enter'];
      spawnSync('tmux', ['-S', target.socketPath, 'send-keys', '-t', target.sessionName, ...keys]);
    }
    ```

    Production hardening (Phase 133 G2-REPLY-04 scope, NOT this spike):
    - Session-discovery: `tmux list-sessions -S <socket> -F '#{session_name}'` to verify target exists before send-keys.
    - Rate-limit: max 1 reply per `needs_input` event (track event-id in vigil-tmux-bridge).
    - Clobber-protect: refuse to send-keys to a session that doesn't match a known-managed prefix (e.g., `vigil-claude-*`).
    - Audit log: append `{ts, session, reply, event_id}` to a redacted ledger.
    - macOS-specific (if Phase 133 ever ships a macOS variant): tmux must be installed (`brew install tmux`); operator-facing onboarding documents this.

    ## Phase 133 Scope-Lock Implications

    Per CONTEXT D-V1: "Active-session test passes AND fresh-session test passes ⇒ PASS. Scope-locks Phase 133 to full G2-REPLY-02..04 (DOUBLE_CLICK enter reply mode → cycle 5 prefabs → DOUBLE_CLICK send → reply lands)."

    ### 1. Writer-process implementation lives in `vigil-tmux-bridge` (Ubuntu daemon), NOT vigil-watch (Mac)

    Per the 2026-05-14 architecture shift documented in `.planning/research/SURFACE-MAP.md` §"Recent architecture shift (2026-05-14)" — copied verbatim:

    > ### Before
    > - **vigil-watch** (Mac): owned write-back to Claude Code via a local Mac tmux session.
    > - **Mac**: hosted both the Claude Code dev environment AND the write-back daemon.
    > - **Constraint**: vigil-core ↔ vigil-watch was local-network-bound (D-N1 in 128b CONTEXT).
    >
    > ### After
    > - **Claude Code dev environment** moves to **Ubuntu server** (operator personal infra; not publicly exposed).
    > - **vigil-tmux-bridge** (new, Ubuntu daemon): owns write-back. Consumes vigil-core's `agent_stream` SSE outbound (no inbound port exposure on Ubuntu); runs `tmux send-keys` locally.
    > - **vigil-watch** (Mac): shrinks to **presentation-only** — Companion HUD on Mac screen, G2 event relay. The "watch" name is now slightly misleading because the write-back-detection role evaporated. Rename to `vigil-mac-companion` is a candidate cleanup (see SEED-018).
    > - **Local-network constraint** on the write-back path disappears (replaced by outbound HTTPS from Ubuntu to Railway; the remaining local-network leg is G2 ↔ vigil-core, unchanged).

    This is **not a re-scope** of Phase 128b (CONTEXT D-A3 correctly leaves writer-process location unspecified). It is a **handoff clarification** for Phase 133 planning.

    ### 2. Trust-posture rationale (Path E architecture is intentional, not incidental)

    Per the unknown-user-profile incident (2026-05-14), `vigil-tmux-bridge` MUST be a **pull-based consumer** of `agent_stream` SSE outbound — never an inbound-exposed daemon:

    - Ubuntu is single-tenant, no inbound exposure, only outbound HTTPS to vigil-core.
    - The privileged surface (the tmux socket, `0700` to operator) is never publicly reachable.
    - If vigil-core (Railway) is compromised, attacker can SSE-emit allowlisted-string replies — **bounded blast radius = 5 strings** (`yes`/`no`/`continue`/`abort`/`defer`).
    - The harder install (Ubuntu daemon) becomes the access boundary: only people with Ubuntu hardware + the operator-token-paste step get write-back capability. PWA-tier users (incl. unknown signups) cannot.

    The 5-string allowlist drift-detector test (Phase 133 G2-REPLY-04 success criterion) pins this trust model at the source-of-truth call site.

    ### 3. Operator workflow target — Ubuntu, not Mac

    As of 2026-05-14 the operator is moving the dev environment to a remote Ubuntu server. The "live Claude Code session" the spike validates against IS the Ubuntu tmux. Phase 133 productionizes against:

    - `claude` running inside a `vigil-claude` launcher wrapper that wraps the session in a uniquely-named tmux pane (`vigil-claude-<timestamp>` prefix).
    - `vigil-tmux-bridge` (systemd unit on Ubuntu) consumes `GET /v1/agent-stream` outbound, filters for `needs_reply` events with allowlisted strings, runs `tmux send-keys -t "$VIGIL_TMUX_SESSION" "$ALLOWLISTED_REPLY" Enter`.
    - vigil-watch (Mac) sees `agent_reply_sent` echo back through SSE and renders confirmation banner on the Companion HUD.

    The Mac is no longer in the write-path. The Mac is presentation-only.

    ### 4. Launcher wrapper UX surface (Phase 133 onboarding, NOT 128b)

    If operator launches `claude` directly (no tmux wrapper), `vigil-tmux-bridge` cannot reach the input channel. Phase 133 must:
    - Detect non-wrapped sessions (e.g., absence of `VIGIL_TMUX_SESSION` env in the running claude's process tree).
    - Surface a user-facing "Launch Claude Code via `vigil-claude` to enable G2 replies" warning.
    - Gracefully degrade to G2-REPLY-05 banner-ack-only for that session.

    ### 5. Companion HUD local-network constraint (D-N1 carry-forward, shape changed)

    CONTEXT D-N1 noted the Companion HUD currently requires local network. Per the 2026-05-14 shift, this constraint **changes shape** — the Mac-side Companion HUD still needs local network for G2 ↔ vigil-core, but the write-back path (Ubuntu → tmux) is no longer local-network-bound. Phase 133 surfaces the remaining local-network constraint (G2 ↔ vigil-core leg) in the operator UX.

    ## Trust-Model Asymmetry — INBOUND vs OUTBOUND Injection

    > 128b is OUTBOUND injection (Vigil writing TO Claude Code). PITFALLS.md §"Pitfall 6 — Prompt injection via captured thoughts in chat context" addresses INBOUND injection (adversarial content getting into Vigil's chat context). These are different threat models; do not confuse them.

    | Direction | Where adversary controls | Threat | Defense |
    |-----------|--------------------------|--------|---------|
    | **INBOUND** (Pitfall 6) | Captured thoughts content (voice, popup, scrape) | Claude follows injected instructions inside thought context | `<thought>` delimiters + tag-breakout sanitization + injection-heuristic flag + token-budget cap. Defense pattern is **content wrapping**. |
    | **OUTBOUND** (this phase) | Replies from G2 → Vigil → Claude Code session | If Vigil is compromised, attacker can drive arbitrary commands into operator's dev environment | 5-string allowlist at the source-of-truth call site (`yes`/`no`/`continue`/`abort`/`defer`) + writer-process drops privileges + tmux socket is `0700` to operator. Defense pattern is **string-set restriction**. |

    CHAT-CTX-02's `<thought>` delimiter pattern does NOT apply here — wrapping a 5-string reply in delimiters adds nothing; the defense is that there are ONLY 5 strings, full stop.

    ## Three Phase 133 implications (verbatim from spike 001 README §"Three Phase 133 implications")

    1. **Path E is the recommended primary path.** It satisfies D-V1 PASS, is user-space, requires no ptrace/elevation, and uses a battle-tested IPC. The 5-string allowlist is the only injection surface that reaches `tmux send-keys`.
    2. **Launcher wrapper is required.** Phase 133's onboarding step is "operator launches Claude Code via `vigil-claude` (or equivalent)". If the operator launches Claude Code directly, vigil-tmux-bridge should detect (e.g., by absence of `VIGIL_TMUX_SESSION` env in the running claude's process tree) and degrade to G2-REPLY-05 banner-ack-only for that session.
    3. **Path E generalizes beyond Claude Code.** Any TTY-reading interactive program in a tmux pane is now a write-back target — including a future "select from list of open tmux sessions, drop into one live" UX (cf. the operator's framing question 2026-05-14 that motivated this spike). This is upside, not in scope for 128b.

    ## Cost summary

    (Aggregated from Plan 06 MEASUREMENTS.md; per RESEARCH §Cost the spike total is <$0.20 against operator's Claude Max OAuth.)

    | Path | Wallclock (actual) | Anthropic API cost | Source |
    |------|---------------------|---------------------|--------|
    | B | <from Plan 01 SUMMARY> | <from Plan 01 SUMMARY> | `pathB-fresh-out.jsonl` + Plan 01 SUMMARY |
    | A | <from Plan 02 SUMMARY> | <from Plan 02 SUMMARY> | `pathA-final-pane.txt` + Plan 02 SUMMARY |
    | D | <from Plan 03 SUMMARY> | <from Plan 03 SUMMARY> | `pathD-fresh-out.txt` + Plan 03 SUMMARY |
    | E (regression) | ~77s (~ same as spike 001 §Iteration 4) | ~$0.01 | `pathE-regression-run.log` + Plan 04 SUMMARY |
    | Spike 001 (historical) | 77s | <$0.02 | spike 001 README §Cost (preserved record) |
    | **Total** | <sum> | <sum (<$0.20 expected)> | Plan 06 MEASUREMENTS.md |

    GUARD-03 budget watermark per CONTEXT D-G3 is N/A (≤$0.20 against operator's personal Claude Max auth, not a multi-user budget). Documented for forensic reference.

    ## Re-activation conditions

    *(Skip this section if verdict at TOP is PASS — re-activation conditions only apply to BLOCK or DEGRADE per CONTEXT D-V3 + SEED-003 DMARC pattern.)*

    If verdict is **BLOCK** or **DEGRADE**, list three explicit re-activation conditions:
    1. <condition 1 — only authored if verdict ≠ PASS; otherwise SKIP this entire ## section>
    2. <condition 2>
    3. <condition 3>

    ## Cited sources

    - CONTEXT D-V1 / D-V2 / D-V3 / D-V4 (verdict gates + mechanical aggregation)
    - CONTEXT D-A1 / D-A2 / D-A3 (spike-dir isolation + sketch-only + scope reduction)
    - CONTEXT D-A4 artifact list (this file is one of those artifacts)
    - REQUIREMENTS.md lines 42-48 (G2-REPLY-01..05 verbatim)
    - ROADMAP.md lines 415-426 (Phase 128b goal + 4 success criteria)
    - RESEARCH.md §"Per-Path Predicted-Verdict Summary" + §"Criteria Mapping" + §"Phase 133 Scope-Lock Implications" + §"Trust-Model Asymmetry"
    - spike 001 README §Results + §"Privilege & portability sketch" + §"Three Phase 133 implications"
    - SURFACE-MAP.md §"Recent architecture shift (2026-05-14)"
    - PITFALLS.md §"Pitfall 6" lines 167-194 (INBOUND injection — cross-referenced for asymmetry, not extended)
    ```

    Substitution rules for the template above:
    - `<PASS|DEGRADE|BLOCK>` ⇒ the actual computed verdict
    - `<today's date YYYY-MM-DD>` ⇒ current date
    - `<one-sentence pointer>` ⇒ name the per-path TRANSCRIPT that established the verdict (typically pathE-TRANSCRIPT.md Regression Re-Run row)
    - `<verdict from path*-TRANSCRIPT.md>` ⇒ the literal mini-verdict line from each path's TRANSCRIPT (e.g., "DEGRADE (fresh-only)", "FAIL", "INCONCLUSIVE", "PASS (re-confirmed)")
    - `<computed result>` ⇒ same as the verdict at TOP
    - `<from Plan 0N SUMMARY>` ⇒ read each Plan 01-04 SUMMARY.md for wallclock + cost; if not yet populated, use RESEARCH's estimates with `(estimated)` suffix and let Plan 06 MEASUREMENTS supply final numbers
    - `<sum>` ⇒ arithmetic sum of the column

    If the verdict is **PASS** (predicted; the dominant case): the entire `## Re-activation conditions` section is REPLACED with the single line `*(N/A — verdict is PASS; re-activation conditions only apply to BLOCK/DEGRADE per CONTEXT D-V3 + SEED-003 DMARC pattern.)*`. Do NOT emit a section with empty bullet points.

    If the verdict is **DEGRADE** or **BLOCK** (unexpected — Plan 04 regression FAILed AND A/B/D didn't surface a PASS): author the three re-activation conditions using the candidate set from RESEARCH §"D-V3" examples adapted to the failure mode:
      1. `@anthropic-ai/claude-code` ships an explicit out-of-band-input API (Anthropic surface change)
      2. Claude Code SDK exposes a session-handle that can be appended to via IPC (SDK surface change)
      3. Operator adopts a tmux-wrapped launcher that gives Vigil control of the stdin FD (operator-side change — but if Plan 04 FAILed THIS is what failed, so the third condition must articulate a DIFFERENT operator-side change such as moving back to Mac vigil-watch + named-pipe c1 with launcher wrapper)

    Do NOT include frontmatter at the top of the file. The first line of the file MUST be `**VERDICT: <result>**` per the verdict-at-TOP invariant.

    Per CONTEXT D-V4: "Verdict is mechanically computed. The author does NOT decide at write-up time." If the computed verdict surprises you, do NOT edit it — record the surprise in the SUMMARY (NOT in this SPIKE-DECISION) and propose Phase 128b.1.
  </action>
  <verify>
    <automated>test -f .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && head -1 .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md | grep -qE '^\*\*VERDICT: (PASS|DEGRADE|BLOCK)\*\*$' && grep -q "Verdict computation (mechanical — D-V4)" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## G2-REPLY-01 Success Criteria — Artifact Mapping" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## Privilege & portability sketch" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "ALLOWED_REPLIES = \['yes', 'no', 'continue', 'abort', 'defer'\]" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## Phase 133 Scope-Lock Implications" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "vigil-tmux-bridge" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "Recent architecture shift" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## Trust-Model Asymmetry" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -qE 'INBOUND.*OUTBOUND|OUTBOUND.*INBOUND' .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## Three Phase 133 implications" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -qE 'Path E is the recommended primary path' .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -qE 'Launcher wrapper is required' .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -q "## Cost summary" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && grep -qE "5-string allowlist|5 strings|5 prefab|bounded blast radius" .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md && echo "SPIKE-DECISION structure verified"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md` exists
    - File's FIRST LINE matches the regex `^\*\*VERDICT: (PASS|DEGRADE|BLOCK)\*\*$` (verdict-at-TOP invariant; Phase 128a D-V1 precedent)
    - File does NOT start with YAML frontmatter (no `---` at line 1)
    - Per-path table contains exactly 5 rows (A / B / C / D / E ★) with `Source` column citing each TRANSCRIPT.md or analytical source
    - Per-path table verdict cells match the actual TRANSCRIPT mini-verdicts from the four `evidence/path*-TRANSCRIPT.md` files (no fabricated cells; reviewer can spot-check by opening each TRANSCRIPT and comparing)
    - `## G2-REPLY-01 Success Criteria — Artifact Mapping` section contains exactly 4 rows (criteria 1-4) each citing a specific artifact
    - `## Privilege & portability sketch` section contains the EXACT 5-string allowlist constant: `ALLOWED_REPLIES = ['yes', 'no', 'continue', 'abort', 'defer']`
    - `## Privilege & portability sketch` section contains a citation back to spike 001 README §"Privilege & portability sketch (D-A2 — markdown only)" lines 182-218 (NOT re-derived)
    - `## Phase 133 Scope-Lock Implications` section contains 5 numbered sub-sections (§1 writer-process location, §2 trust posture, §3 operator workflow target, §4 launcher wrapper UX, §5 local-network constraint)
    - `## Phase 133 Scope-Lock Implications §1` contains the verbatim `### Before` / `### After` block from SURFACE-MAP.md §"Recent architecture shift (2026-05-14)"
    - `## Phase 133 Scope-Lock Implications §2` contains the literal phrase `bounded blast radius` AND `5 strings` AND `unknown-user-profile incident` OR `unknown signups`
    - `## Trust-Model Asymmetry` section contains a two-row table with `INBOUND` and `OUTBOUND` rows
    - `## Trust-Model Asymmetry` explicitly states `<thought> delimiter pattern does NOT apply` (or equivalent: the OUTBOUND defense is string-set restriction, NOT content wrapping)
    - `## Three Phase 133 implications` section contains the three numbered items with the literal phrases `Path E is the recommended primary path`, `Launcher wrapper is required`, and `Path E generalizes beyond Claude Code`
    - `## Cost summary` table contains a row for each path with wallclock + cost columns; total row sums to ≤$0.20 (per RESEARCH expectation; record actual from Plan 06 MEASUREMENTS)
    - If verdict at TOP is PASS: `## Re-activation conditions` section reads `*(N/A — verdict is PASS; re-activation conditions only apply to BLOCK/DEGRADE per CONTEXT D-V3 + SEED-003 DMARC pattern.)*` (single line, no bulleted enumeration)
    - If verdict at TOP is DEGRADE or BLOCK: `## Re-activation conditions` section contains exactly 3 numbered conditions
    - The file does NOT contain any "v1", "v2", "simplified version", "placeholder", "TBD", or "TODO" markers (scope-reduction prohibition)
    - Per CONTEXT D-V4: the verdict at TOP matches the mechanical aggregation of the 5 per-path mini-verdicts; reviewer can re-compute by opening the four TRANSCRIPT files and applying `MAX(PASS > DEGRADE > FAIL > INCONCLUSIVE)`
  </acceptance_criteria>
  <done>
    `128b-SPIKE-DECISION.md` is the phase's primary deliverable, authored with the verdict at TOP, the per-path table populated from empirical TRANSCRIPTs, the G2-REPLY-01 criteria mapping, the privilege sketch referenced (not re-derived), the Phase 133 scope-lock implications copied verbatim from SURFACE-MAP.md, the trust-posture rationale documented, the INBOUND/OUTBOUND asymmetry stated, and the three Phase 133 implications enumerated. The verdict-at-TOP invariant is preserved (first line matches the regex). All artifact citations resolve to existing files in `.planning/spikes/128b-write-back/evidence/` or `.planning/spikes/001-tmux-write-back-128b/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update spike-dir README + MANIFEST verdict cell</name>
  <files>.planning/spikes/128b-write-back/README.md, .planning/spikes/MANIFEST.md</files>
  <read_first>
    - .planning/spikes/128b-write-back/README.md (existing; Task 1 created the in-progress version; this task updates the frontmatter verdict field)
    - .planning/spikes/MANIFEST.md (existing; row for 128b-write-back created by Plan 01 Task 1 with Verdict = `🚧 in-progress`)
    - .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md (the verdict at TOP determines the value to write into both files)
  </read_first>
  <action>
    Read the FIRST LINE of `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md` to extract the verdict (PASS / DEGRADE / BLOCK).

    Map the verdict to the markers:
    - PASS ⇒ frontmatter `verdict: VALIDATED-PASS`, MANIFEST Verdict column `✓ VALIDATED-PASS`
    - DEGRADE ⇒ frontmatter `verdict: DEGRADED-banner-ack-only`, MANIFEST Verdict column `⚠ DEGRADED-banner-ack-only`
    - BLOCK ⇒ frontmatter `verdict: BLOCKED`, MANIFEST Verdict column `✗ BLOCKED`

    Update `.planning/spikes/128b-write-back/README.md`: in the frontmatter block at the top, change the line `verdict: in-progress` to `verdict: <new-value-per-above-mapping>`. Do NOT modify any other frontmatter field. Do NOT modify the body of the README.

    Update `.planning/spikes/MANIFEST.md`: find the row for `128b-write-back` (the row added by Plan 01 Task 1). Update its Verdict column from `🚧 in-progress` to the corresponding marker per the mapping. Do NOT modify the 001-tmux-write-back-128b row.

    Both updates are mechanical — they read from `128b-SPIKE-DECISION.md` line 1 and write the corresponding marker. No subjective interpretation.
  </action>
  <verify>
    <automated>VERDICT=$(head -1 .planning/phases/128b-g2-reply-01-write-back-path-spike/128b-SPIKE-DECISION.md | sed -nE 's/^\*\*VERDICT: (PASS|DEGRADE|BLOCK)\*\*$/\1/p') && [ -n "$VERDICT" ] && grep -q "verdict: " .planning/spikes/128b-write-back/README.md && ! grep -q "verdict: in-progress" .planning/spikes/128b-write-back/README.md && ! grep -q "🚧 in-progress" .planning/spikes/MANIFEST.md && grep -qE 'VALIDATED-PASS|DEGRADED-banner-ack-only|BLOCKED' .planning/spikes/128b-write-back/README.md && grep -q "001 " .planning/spikes/MANIFEST.md && echo "Verdict propagated: $VERDICT"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/128b-write-back/README.md` frontmatter `verdict:` field is one of: `VALIDATED-PASS`, `DEGRADED-banner-ack-only`, `BLOCKED` (no longer `in-progress`)
    - The README's frontmatter mapping matches the SPIKE-DECISION verdict at TOP (PASS ⇒ VALIDATED-PASS, DEGRADE ⇒ DEGRADED-banner-ack-only, BLOCK ⇒ BLOCKED)
    - `.planning/spikes/MANIFEST.md` row for `128b-write-back` has Verdict column updated (no longer `🚧 in-progress`)
    - MANIFEST row for `001 tmux-write-back-128b` is UNCHANGED (still `✓ VALIDATED`)
    - No other frontmatter fields in the README are modified (no scope creep)
    - No other rows in MANIFEST are modified (no destructive edits)
  </acceptance_criteria>
  <done>
    Spike-dir README and MANIFEST verdict cells reflect the SPIKE-DECISION outcome. Downstream readers (Plan 06 MEASUREMENTS + Phase 133 planner + future spike-landscape consumers) see the final verdict at three locations consistently: SPIKE-DECISION line 1, README frontmatter, MANIFEST table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-path TRANSCRIPT.md files → SPIKE-DECISION verdict | The verdict at TOP is derived from the four TRANSCRIPT mini-verdicts; reviewer can re-verify by opening each TRANSCRIPT |
| SURFACE-MAP.md / spike 001 README → SPIKE-DECISION reused content | Cited verbatim with source line ranges; reviewer can `diff` the copied blocks against the originals |
| 5-string allowlist constant | Reproduced verbatim in SPIKE-DECISION's privilege sketch section; the source-of-truth call site (Phase 133 G2-REPLY-04) will pin via drift-detector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128b-05-01 | Tampering | verdict-at-TOP invariant (Phase 128a D-V1 precedent) | mitigate | Acceptance criterion: file's first line matches the regex `^\*\*VERDICT: (PASS|DEGRADE|BLOCK)\*\*$`. Phase 128b.1 procedure (NOT this plan) is the only mechanism to revise. |
| T-128b-05-02 | Repudiation | subjective verdict override (Pitfall 5; D-V4 violation) | mitigate | The verdict is computed by reading the per-path TRANSCRIPT mini-verdicts and applying `MAX(PASS > DEGRADE > FAIL > INCONCLUSIVE)`. Reviewer can re-compute by opening the four TRANSCRIPT files. If the SPIKE-DECISION verdict diverges from the TRANSCRIPT aggregation, the document is invalid. |
| T-128b-05-03 | Spoofing | 5-string allowlist drift (G2-REPLY-04 future drift-detector dependency) | mitigate | The allowlist is reproduced verbatim from spike 001 README; the production source-of-truth call site (Phase 133 G2-REPLY-04 implementation) will pin via drift-detector test. THIS document is informational; the binding is Phase 133's code. |
| T-128b-05-04 | Information Disclosure | Phase 133 scope-lock leaks operator infrastructure details (Ubuntu IP, tmux socket paths) | accept | All cited content is from already-public planning docs (SURFACE-MAP.md, RESEARCH.md). No new operator-specific secrets introduced. |
| T-128b-05-05 | Tampering | spike 001 evidence-by-reference rot if sibling dir is later moved | accept | Plan 04 already copied evidence into `.planning/spikes/128b-write-back/evidence/pathE-L4-*.txt` (self-containment); Plan 05's citations point to BOTH the copies (canonical for this phase) AND the originals (canonical for spike 001). If spike 001 dir is later archived, the phase-local copies remain intact. |
| T-128b-05-06 | Repudiation | INBOUND/OUTBOUND asymmetry conflated by future reader | mitigate | The asymmetry section EXPLICITLY states `<thought> delimiter pattern does NOT apply` and the table makes the threat-model split structural. Reviewer cannot conflate without ignoring the section. |
</threat_model>

<verification>
1. SPIKE-DECISION.md first line is `**VERDICT: <result>**` (verdict-at-TOP invariant; Phase 128a D-V1 precedent).
2. Per-path table has 5 rows; verdict cells trace back to the four per-path TRANSCRIPT.md mini-verdicts.
3. G2-REPLY-01 criteria mapping table has 4 rows, each citing an artifact.
4. Privilege sketch section contains the verbatim 5-string allowlist constant + citation to spike 001 README lines 182-218.
5. Phase 133 scope-lock has 5 numbered sub-sections with the SURFACE-MAP.md Before/After block copied verbatim in §1.
6. Trust-Model Asymmetry table is present; states explicitly that `<thought>` delimiter pattern does not apply.
7. Three Phase 133 implications enumerated with the three load-bearing literal phrases.
8. Cost summary table populated with values from Plan 06 MEASUREMENTS (or estimated with `(estimated)` suffix if not yet populated).
9. Re-activation conditions: present (3 numbered) ONLY if verdict ≠ PASS; otherwise the single-line N/A marker.
10. Spike-dir README frontmatter + MANIFEST row updated mechanically to match the SPIKE-DECISION verdict.
</verification>

<success_criteria>
- 128b-SPIKE-DECISION.md authored with the 10-section structure listed in the objective
- Verdict at TOP is mechanically computed from per-path TRANSCRIPTs (PASS in the dominant case; DEGRADE or BLOCK if Plan 04 regression FAILed)
- Privilege sketch is REFERENCED (not re-derived) — verbatim copy from spike 001 README lines 182-218 with citation
- Phase 133 scope-lock copies SURFACE-MAP.md §"Recent architecture shift" verbatim
- Trust posture (vigil-tmux-bridge on Ubuntu, pull-based, bounded blast radius) documented
- INBOUND/OUTBOUND asymmetry explicit
- Three Phase 133 implications enumerated
- Cost summary populated from Plan 06 MEASUREMENTS
- Spike-dir README + MANIFEST verdict cells propagate the SPIKE-DECISION verdict
- Plan 08 (60s Loom) can cite SPIKE-DECISION verdict at TOP to determine Loom shape (PASS = success demo; DEGRADE/BLOCK = failure-mode documentation)
</success_criteria>

<output>
After completion, create `.planning/phases/128b-g2-reply-01-write-back-path-spike/128b-05-SUMMARY.md` recording:
- The computed verdict at TOP (PASS / DEGRADE / BLOCK) and the per-path mini-verdicts that drove it (5 rows verbatim from the SPIKE-DECISION per-path table)
- A `## Verdict surprise check` section: was the computed verdict the expected PASS, or did Plan 04 regression FAIL and force DEGRADE/BLOCK? If surprise: propose Phase 128b.1 follow-up; do NOT edit the SPIKE-DECISION (CONTEXT D-V4 immutability)
- The spike-dir README + MANIFEST update results (verdict markers set; spike 001 row untouched)
- A `## Phase 133 handoff` section noting: (a) Phase 133 planner reads `128b-SPIKE-DECISION.md §"Phase 133 Scope-Lock Implications"` for the writer-process location (vigil-tmux-bridge on Ubuntu, NOT vigil-watch on Mac); (b) Phase 133 G2-REPLY-04 implementation pins the 5-string allowlist verbatim from the privilege sketch section
- A `## Citation audit` section confirming each cited source resolves: spike 001 README lines 182-218 exist; SURFACE-MAP.md §"Recent architecture shift" exists; PITFALLS.md §"Pitfall 6" exists; RESEARCH.md §"Phase 133 Scope-Lock Implications" + §"Trust-Model Asymmetry" + §"Criteria Mapping" exist
</output>
