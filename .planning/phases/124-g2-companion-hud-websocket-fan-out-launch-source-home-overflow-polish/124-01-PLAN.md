---
phase: 124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-g2-plugin/package.json
  - vigil-g2-plugin/tsconfig.json
  - vigil-g2-plugin/src/lib/__tests__/.gitkeep
  - vigil-g2-plugin/src/screens/__tests__/.gitkeep
  - vigil-g2-plugin/src/__tests__/smoke.test.ts
autonomous: true
requirements: [AGENT-HUD-01, AGENT-HUD-02, G2-POLISH-06, G2-POLISH-07]
tags: [phase-124, plugin, test-infra]

must_haves:
  truths:
    - "vigil-g2-plugin can run `npx tsx --test src/**/*.test.ts` and the runner executes (exit 0 with at least 1 passing test)"
    - "tsx is declared in vigil-g2-plugin/package.json devDependencies"
    - "Test directories exist for src/lib/, src/screens/, and src/ root"
  artifacts:
    - path: "vigil-g2-plugin/package.json"
      provides: "tsx devDep + npm test script"
      contains: "\"tsx\""
    - path: "vigil-g2-plugin/src/__tests__/smoke.test.ts"
      provides: "First node:test smoke test confirming runner works"
  key_links:
    - from: "vigil-g2-plugin/package.json"
      to: "node_modules/tsx"
      via: "npm install"
      pattern: "\"tsx\":\\s*\""
---

<objective>
Bootstrap a `node:test`-based test runner for vigil-g2-plugin so all subsequent Phase 124 plans (Plan 02-08) can author unit tests for SSE shim, Companion screen, navigation, and main.ts launch-source logic. The plugin currently has zero test infrastructure (verified RESEARCH §"Validation Architecture" — `vigil-g2-plugin framework: none today`). Wave 0 introduces `tsx` as a devDep and a smoke test that confirms the runner executes.

Purpose: Unblock Wave 0 plugin-side tests for the SSE shim parser (Plan 06), Companion body composition (Plan 07), navigation DOUBLE_CLICK branching (Plan 07), and onLaunchSource site (Plan 08).

Output:
- `vigil-g2-plugin/package.json` updated with `tsx` devDep + `test` script
- `vigil-g2-plugin/tsconfig.json` updated to include test files
- `vigil-g2-plugin/src/__tests__/smoke.test.ts` — first passing test
- Empty `__tests__` placeholder dirs for `src/lib/` and `src/screens/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-CONTEXT.md
@.planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-RESEARCH.md
@.planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-PATTERNS.md
@vigil-core/package.json
@vigil-g2-plugin/package.json
@vigil-g2-plugin/tsconfig.json

<interfaces>
<!-- vigil-core's tsx version is the source of truth — match it. -->
<!-- From vigil-core/package.json devDependencies (verify before editing): -->
<!-- "tsx": "^4.20.0" (or whatever the current version is — read first, copy verbatim) -->

<!-- node:test runner pattern, mirror agent-events.test.ts:1-15: -->
import { test } from "node:test";
import assert from "node:assert/strict";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tsx devDep + npm test script + smoke test</name>
  <files>
    vigil-g2-plugin/package.json,
    vigil-g2-plugin/src/__tests__/smoke.test.ts,
    vigil-g2-plugin/src/lib/__tests__/.gitkeep,
    vigil-g2-plugin/src/screens/__tests__/.gitkeep
  </files>
  <read_first>
    - vigil-g2-plugin/package.json (full file — confirm current devDeps shape, scripts block)
    - vigil-core/package.json (read tsx version line to copy verbatim)
    - vigil-core/src/routes/agent-events.test.ts (lines 1-20 — node:test + assert/strict pattern)
  </read_first>
  <action>
    1. Read `vigil-core/package.json` and grep for `"tsx":` — copy the exact version string (e.g., `"^4.20.0"`).
    2. Edit `vigil-g2-plugin/package.json` (use Edit tool, NOT heredoc):
       - Add to `devDependencies`: `"tsx": "{exact-version-from-vigil-core}"`. Insert alphabetically (between existing entries).
       - Add to `scripts`: `"test": "tsx --test \"src/**/*.test.ts\""`. If a `test` key already exists with a placeholder, replace its value.
       - Preserve all other keys verbatim.
    3. Run `cd vigil-g2-plugin && npm install` to install tsx.
    4. Create `vigil-g2-plugin/src/__tests__/smoke.test.ts` (use Write tool):
       ```typescript
       // Phase 124 Plan 01 — smoke test: confirms node:test + tsx runner works.
       // Mirrors vigil-core/src/routes/agent-events.test.ts:1-15 import pattern.
       import { test } from "node:test";
       import assert from "node:assert/strict";

       test("smoke: node:test runner executes", () => {
         assert.equal(1 + 1, 2);
       });

       test("smoke: TypeScript types compile under tsx loader", () => {
         const x: number = 42;
         assert.equal(x, 42);
       });
       ```
    5. Create empty placeholder files (use Write tool with single-line content) so the test directories exist for downstream plans:
       - `vigil-g2-plugin/src/lib/__tests__/.gitkeep` — content: `# Phase 124 — test directory for src/lib/ (sse-client tests land here in Plan 06)`
       - `vigil-g2-plugin/src/screens/__tests__/.gitkeep` — content: `# Phase 124 — test directory for src/screens/ (companion + home tests land here in Plan 04 + Plan 07)`
    6. Verify `vigil-g2-plugin/tsconfig.json` includes `src/**/*.ts` (test files match this glob already if "include" uses `src/**/*`); only edit if test files would be excluded. Do NOT edit if not needed.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `vigil-g2-plugin/package.json` contains `"tsx":` (verifiable via `grep '"tsx":' vigil-g2-plugin/package.json` exit 0)
    - `vigil-g2-plugin/package.json` contains `"test": "tsx --test` (grep `"test": "tsx --test` exits 0)
    - `vigil-g2-plugin/node_modules/tsx/package.json` exists after `npm install` (test: `test -f vigil-g2-plugin/node_modules/tsx/package.json`)
    - `vigil-g2-plugin/src/__tests__/smoke.test.ts` exists and contains `import { test } from "node:test"` (grep exits 0)
    - `cd vigil-g2-plugin && npm test` exits 0 with at least `# pass 2` in output (use grep on stdout)
    - `vigil-g2-plugin/src/lib/__tests__/` exists as a directory (`test -d`)
    - `vigil-g2-plugin/src/screens/__tests__/` exists as a directory (`test -d`)
  </acceptance_criteria>
  <done>
    npm test passes with 2/2 smoke tests green; tsx is installed; placeholder test directories exist; no other source files changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/build → npm registry | New devDep introduces supply-chain surface. tsx is widely-used (already vetted in vigil-core). |
| node test runner → source files | Test runner reads source files; no untrusted input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01-01 | Tampering / Supply chain | npm devDep `tsx` | accept | Same version as vigil-core (already vetted). Lockfile (`package-lock.json`) pins exact resolution. |
| T-124-01-02 | Information disclosure | smoke.test.ts contents | accept | No secrets, no env reads, no network. Pure arithmetic assertion. |
</threat_model>

<verification>
- `cd vigil-g2-plugin && npm test` exits 0 with 2 passing tests
- `vigil-g2-plugin/package-lock.json` lists `tsx` resolution (no manual lockfile edit; npm install handles it)
- No unrelated diffs in `vigil-g2-plugin/src/` (only `__tests__` files added)
</verification>

<success_criteria>
- Plugin test runner is bootstrapped: `npm test` is a working command.
- Subsequent Phase 124 plans (06, 07, 08) can drop test files into `src/**/__tests__/*.test.ts` and they will execute.
- No runtime impact (devDep only).
</success_criteria>

<output>
After completion, create `.planning/phases/124-g2-companion-hud-websocket-fan-out-launch-source-home-overflow-polish/124-01-SUMMARY.md`.
</output>
