---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dailybrief-doctor.sh
autonomous: true

must_haves:
  truths:
    - "Running scripts/dailybrief-doctor.sh on a machine where the vigilcore plist has been retired (Phase 107.1-04) emits an informational row 'retired Phase 107.1-04 (intentional)' instead of an ✗ drift row"
    - "DRIFT_COUNT is NOT incremented by the retired plist — doctor can exit 0 on a properly-retired machine"
    - "If the plist is absent AND the retirement marker is ALSO absent, the ✗ drift row still fires (real-drift detection preserved)"
    - "If the plist is present, the existing value-drift check still runs unchanged"
  artifacts:
    - path: "scripts/dailybrief-doctor.sh"
      provides: "Doctor with three-way retired-vs-missing-vs-present branch for vigilcore plist ANTHROPIC_API_KEY row"
      contains: "107.1-daemon-retirement.md"
  key_links:
    - from: "scripts/dailybrief-doctor.sh plist-check block (lines 113-126)"
      to: ".planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md"
      via: "[[ -f \"$RETIREMENT_MARKER\" ]] presence-check"
      pattern: "RETIREMENT_MARKER=.*107\\.1-daemon-retirement\\.md"
---

<objective>
Teach `scripts/dailybrief-doctor.sh` the difference between "plist intentionally retired (Phase 107.1-04)" and "plist unexpectedly missing (real drift)". Currently lines 113-126 treat any missing plist as drift and increment `DRIFT_COUNT`, which forces doctor to exit non-zero on a healthy post-retirement machine.

Purpose: Make `dailybrief-doctor` exit 0 on machines that have correctly completed Phase 107.1-04 daemon retirement, without losing the ability to detect a real accidental plist deletion.

Output: `scripts/dailybrief-doctor.sh` lines 113-126 replaced with a three-way branch (present / retired / missing). The retired branch uses the same informational `printf` pattern as the D-13 local-DB row (no MATCH column, no DRIFT_COUNT increment).
</objective>

<execution_context>
@/Users/jamesonmorrill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesonmorrill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-RESEARCH.md
@scripts/dailybrief-doctor.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace two-way plist check with three-way (present/retired/missing) branch</name>
  <files>scripts/dailybrief-doctor.sh</files>
  <action>
Open `scripts/dailybrief-doctor.sh` and locate the block at lines 113-126 titled `# 3. Plist EnvironmentVariables:ANTHROPIC_API_KEY`.

VERIFY CURRENT STATE before editing (grep for the exact block to make sure line numbers haven't drifted):
  grep -n "Plist EnvironmentVariables:ANTHROPIC_API_KEY" scripts/dailybrief-doctor.sh

EXACT OLD BLOCK (lines 113-126, copy verbatim — must match):
```
# 3. Plist EnvironmentVariables:ANTHROPIC_API_KEY
if [[ -f "$VIGILCORE_PLIST" ]]; then
    ANTHROPIC_PLIST=$(/usr/libexec/PlistBuddy -c "Print :EnvironmentVariables:ANTHROPIC_API_KEY" "$VIGILCORE_PLIST" 2>/dev/null || echo "")
    if [[ "$ANTHROPIC_PLIST" == "$ANTHROPIC_CANONICAL" ]]; then
        MATCH="✓"
    else
        MATCH="✗"
        DRIFT_COUNT=$((DRIFT_COUNT + 1))
    fi
    print_row "plist EnvironmentVariables" "$(prefix8 "$ANTHROPIC_PLIST")" "$(last_modified "$VIGILCORE_PLIST")" "$MATCH"
else
    print_row "plist EnvironmentVariables" "(missing)" "(missing)" "✗"
    DRIFT_COUNT=$((DRIFT_COUNT + 1))
fi
```

EXACT NEW BLOCK (replace with — matches RESEARCH.md Code Examples Fix 3 verbatim):
```
# 3. Plist EnvironmentVariables:ANTHROPIC_API_KEY (three-way: present / retired / missing)
# Phase 107.3 Fix 3: distinguish "retired by design" (Phase 107.1-04) from real drift.
# Retirement marker presence-check uses the in-repo phase file — no external state.
RETIREMENT_MARKER="$REPO_DIR/.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md"
if [[ -f "$VIGILCORE_PLIST" ]]; then
    ANTHROPIC_PLIST=$(/usr/libexec/PlistBuddy -c "Print :EnvironmentVariables:ANTHROPIC_API_KEY" "$VIGILCORE_PLIST" 2>/dev/null || echo "")
    if [[ "$ANTHROPIC_PLIST" == "$ANTHROPIC_CANONICAL" ]]; then
        MATCH="✓"
    else
        MATCH="✗"
        DRIFT_COUNT=$((DRIFT_COUNT + 1))
    fi
    print_row "plist EnvironmentVariables" "$(prefix8 "$ANTHROPIC_PLIST")" "$(last_modified "$VIGILCORE_PLIST")" "$MATCH"
elif [[ -f "$RETIREMENT_MARKER" ]]; then
    # Plist absent AND retirement marker present → intentionally retired (Phase 107.1-04).
    # Informational only — same pattern as D-13 local-DB row. Does NOT increment DRIFT_COUNT.
    printf "%-38s | %s\n" "plist EnvironmentVariables" "retired Phase 107.1-04 (intentional)"
else
    # Plist absent AND no retirement record → unexpected absence, real drift.
    print_row "plist EnvironmentVariables" "(missing)" "(missing)" "✗"
    DRIFT_COUNT=$((DRIFT_COUNT + 1))
fi
```

Verify that `$REPO_DIR` is already in scope at this line (it is — set near the top of the script; check with `grep -n '^REPO_DIR=' scripts/dailybrief-doctor.sh`). If for any reason it's not, use `"$SCRIPT_DIR/../.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md"` instead.

DO NOT change:
- Any other row in the drift table (.env, launchctl, railway, etc.)
- The informational D-13 section formatting
- The exit-code logic at the bottom of the file
  </action>
  <verify>
1. Syntax check:
   bash -n scripts/dailybrief-doctor.sh

2. Retirement marker exists where we expect:
   ls -la .planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md
   Expected: file exists.

3. Run doctor on this machine (plist has been retired, marker exists):
   bash scripts/dailybrief-doctor.sh 2>&1 | tee /tmp/doctor-107.3-03.log
   Then inspect:
   grep -E "plist EnvironmentVariables" /tmp/doctor-107.3-03.log
   Expected: one line containing `retired Phase 107.1-04 (intentional)`.
   Expected: NO `(missing) | ✗` row.

4. Check DRIFT_COUNT is not inflated by this row. Look at the summary at bottom of doctor output:
   tail -20 /tmp/doctor-107.3-03.log
   The plist row must no longer contribute to drift total on this machine.

5. Regression: simulate real-drift case (plist absent AND marker absent) by temporarily shadowing REPO_DIR to a tmpdir:
   mkdir -p /tmp/fake-repo-107.3/.planning/phases/xxx
   REPO_DIR=/tmp/fake-repo-107.3 bash -c 'source scripts/dailybrief-doctor.sh 2>&1' 2>&1 | grep -E "plist EnvironmentVariables" || true
   (Note: the full script sets its own REPO_DIR internally via SCRIPT_DIR, so this may not cleanly override. Alternative: manually verify by code-review that the `else` branch still prints the ✗ row and increments DRIFT_COUNT. The LOGIC is mechanical and preserved from the old `else`.)

6. Git diff is scoped correctly:
   git diff scripts/dailybrief-doctor.sh | head -50
   Expected: only the `# 3. Plist EnvironmentVariables:ANTHROPIC_API_KEY` block is modified.
  </verify>
  <done>
- `bash -n scripts/dailybrief-doctor.sh` passes
- Doctor output on this machine shows `retired Phase 107.1-04 (intentional)` instead of `(missing) | ✗`
- DRIFT_COUNT on this machine is no longer inflated by the retired plist
- `else` branch (real drift) logic is preserved byte-for-byte from the original
- Diff is confined to lines 113-126 (plus the RETIREMENT_MARKER assignment)
  </done>
</task>

</tasks>

<verification>
Single-task plan. Task's own <verify> block covers all checks.

Additional sanity: run `bash scripts/dailybrief-doctor.sh` on the iMac and confirm the overall exit code. On a properly retired machine with all other drift clean, exit should now be 0 where before it was non-zero purely because of this row.
</verification>

<success_criteria>
- `scripts/dailybrief-doctor.sh` lines 113-126 block replaced with three-way branch
- Retirement marker path references `$REPO_DIR/.planning/phases/107.1-local-dev-environment-with-postgres-and-hot-reload-stack/107.1-daemon-retirement.md`
- Retired-state row uses informational `printf` (two-column, no MATCH column)
- Real-drift `else` branch is byte-for-byte preserved
- Doctor run on this machine produces the informational row (not the drift row)
- No unrelated edits
</success_criteria>

<output>
After completion, create `.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-03-SUMMARY.md` using the summary template. Key fields:
- subsystem: tooling/doctor
- tech-stack.added: (none — pure shell refactor)
- affects: scripts/dailybrief-doctor.sh
- decisions: Use presence-check of in-repo retirement-marker file (NOT env var, NOT external state) — LOCKED by RESEARCH Fix 3 rationale
- patterns: Three-way retired/present/missing branch with informational printf matches existing D-13 local-DB pattern
</output>
