---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 01
subsystem: vigil-core/http-server
tags: [railway, hono, node-server, env-detection, bind-host, deployment]

# Dependency graph
requires:
  - phase: 107.2-cross-machine-tailscale-dev-access-with-secure-bind-and-cors
    provides: VIGIL_BIND_HOST env-gated bind hostname + prod CORS FATAL guard (src/index.ts:180-186)
  - phase: 107.3-prod-bind-install-sh-doctor-cleanup/107.3-04
    provides: Scripts/verify-phase-107.sh --external mode (check_external_health) used as post-deploy verification gate
provides:
  - Railway-aware bind detection via !!process.env.RAILWAY_SERVICE_ID presence check
  - Durable code-level 502-class regression prevention for api.vigilhub.io
  - Updated comment block citing the 2026-04-22 outage rationale
affects:
  - vigil-core/src/index.ts (production deploy path)
  - api.vigilhub.io (Railway-hosted prod endpoint)
  - Future vigil-core deploys that drop the manual VIGIL_BIND_HOST=0.0.0.0 Railway override

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Infrastructure-injected env-var presence check (RAILWAY_SERVICE_ID UUID) > user-convention env-var value comparison (NODE_ENV/RAILWAY_ENVIRONMENT_NAME string match)"

key-files:
  created: []
  modified:
    - "vigil-core/src/index.ts (lines 180-197): isRailway detection + 0.0.0.0-on-Railway default"

key-decisions:
  - "Detect Railway via RAILWAY_SERVICE_ID presence — NOT NODE_ENV (user convention), NOT RAILWAY_ENVIRONMENT (does not exist per official docs), NOT RAILWAY_ENVIRONMENT_NAME (user-named string)"
  - "Presence check !!process.env.RAILWAY_SERVICE_ID — presence alone, no string comparison"
  - "Local default stays 127.0.0.1 — Phase 107.2 security bias preserved"
  - "Explicit VIGIL_BIND_HOST still wins — local Tailscale dev override unchanged"

patterns-established:
  - "For Railway env detection use RAILWAY_SERVICE_ID presence (always-injected UUID, never set locally) — immune to naming-convention drift"
  - "Code-level prod defaults must be verified via external HTTP probe against the live deployment, not only a local NODE_ENV=production probe (Phase 107.2-01 lesson learned)"

# Metrics
duration: 5m 3s
completed: 2026-04-22
---

# Phase 107.3 Plan 01: vigil-core Railway Bind Detection Summary

**Code-level Railway detection via RAILWAY_SERVICE_ID presence; prod auto-binds 0.0.0.0 so the Railway proxy can reach the container; local dev stays 127.0.0.1; explicit VIGIL_BIND_HOST override preserved.**

## Performance

- **Duration:** 5m 3s
- **Started:** 2026-04-22T20:23:23Z
- **Completed:** 2026-04-22T20:28:26Z
- **Tasks:** 2
- **Files modified:** 1 (vigil-core/src/index.ts)

## Accomplishments

- Replaced the 127.0.0.1 fallback that caused the 2026-04-22 502 outage with a Railway-aware default (`isRailway ? "0.0.0.0" : "127.0.0.1"`)
- Detection signal locked on `RAILWAY_SERVICE_ID` (UUID always injected by Railway, never present locally) — not NODE_ENV, not RAILWAY_ENVIRONMENT (which doesn't exist in the official docs)
- Updated the misleading comment block to explain the new strategy, cite Railway docs, and reference the outage that motivated the fix (preventing regression via documentation)
- End-to-end verified via live `--external` probe after Railway auto-redeploy: HTTP 200 returned from api.vigilhub.io/v1/health on first attempt

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace bind-host default with Railway-aware detection via RAILWAY_SERVICE_ID** — `9a81ec3` (fix)
2. **Task 2: Post-deploy verification via --external probe** — no code changes; verification only (no commit per plan's conditional commit clause)

**Plan metadata:** pending (docs(107.3-01): complete vigil-core Railway-aware bind detection plan)

## Files Created/Modified

- `vigil-core/src/index.ts` — lines 180-197: added `isRailway = !!process.env.RAILWAY_SERVICE_ID`; hostname default became `process.env.VIGIL_BIND_HOST ?? (isRailway ? "0.0.0.0" : "127.0.0.1")`; comment block rewritten with outage citation

## Decisions Made

- **Detection via RAILWAY_SERVICE_ID (LOCKED by RESEARCH):** UUID always injected by Railway, never present locally. No false-positive risk. Alternatives rejected: `NODE_ENV` (user-set convention, not infra-enforced); `RAILWAY_ENVIRONMENT` (does not exist per official Railway variables reference fetched 2026-04-22); `RAILWAY_ENVIRONMENT_NAME` (user-named string, environment-naming-convention dependent)
- **Presence-only check (no string comparison):** `!!process.env.RAILWAY_SERVICE_ID` — cleaner than any value-dependent test
- **Local default stays 127.0.0.1:** Phase 107.2 security bias preserved; defaulting to 0.0.0.0 globally would have been a regression
- **Explicit override wins:** `VIGIL_BIND_HOST` still beats the isRailway branch, so a local dev machine can still force 0.0.0.0 for Tailscale exposure (Phase 107.2 workflow unchanged)

## Verification Evidence

### Task 1 — All 6 `<verify>` checks PASS

| # | Check | Result |
|---|---|---|
| 1 | `npx tsc --noEmit` | PASS (exit 0, after `npm install` restored missing deps — pre-existing env issue, not from edit) |
| 2 | `npm run build` | PASS (exit 0, `dist/index.js` emitted) |
| 3 | Local default (no RAILWAY_SERVICE_ID, no VIGIL_BIND_HOST) | `Vigil Core API running on 127.0.0.1:3001` |
| 4 | Railway simulation (`RAILWAY_SERVICE_ID=fake-uuid-test`) | `Vigil Core API running on 0.0.0.0:3001` |
| 5 | Explicit override (`VIGIL_BIND_HOST=192.168.1.1 RAILWAY_SERVICE_ID=fake`) | `listen EADDRNOTAVAIL 192.168.1.1:3001` — hostname resolution used explicit override (expected: bind fails because 192.168.1.1 isn't a local interface; resolution proven) |
| 6 | `git diff vigil-core/src/index.ts` | Scoped to lines 180-190 area (+16/-6); no unrelated edits |

### Task 2 — Live external probe PASS

- **Commit pushed:** 9a81ec3 pushed to origin/main at ~2026-04-22T20:26:30Z (push-hook had NOT auto-pushed; pushed explicitly)
- **Railway redeploy wait:** 90s
- **First attempt:** `bash Scripts/verify-phase-107.sh --external` exit 0 — `PASS — api.vigilhub.io/v1/health returned HTTP 200`
- **Live response body (captured 2026-04-22T20:28:08Z):**
  ```json
  {"status":"ok","timestamp":"2026-04-22T20:28:08.537Z","version":"0.1.0","database":"connected"}
  ```
- **HTTP status:** 200
- **Response time:** 0.24s

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Partial vigil-core/node_modules blocked tsc/build probes**

- **Found during:** Task 1 verification Check 1 (`npx tsc --noEmit`)
- **Issue:** tsc emitted 21 TS2307 errors ("Cannot find module 'posthog-node' / 'jose' / 'google-auth-library' / 'pdfkit' / '@node-rs/argon2'") because vigil-core/node_modules was partially populated in this environment. Pre-existing state — stash/pop comparison confirmed identical error count (21) before and after the src/index.ts edit, so errors are environmental, not from this plan
- **Fix:** Ran `cd vigil-core && npm install` to restore full dependency tree; re-ran `tsc --noEmit` → exit 0; `npm run build` → exit 0, dist/index.js emitted
- **Files modified:** vigil-core/node_modules (not committed; `package-lock.json` was not altered since install added packages that were already satisfied by lockfile)
- **Verification:** Pre-install tsc: 21 errors; post-install tsc: 0 errors; pre-install build: exit 2; post-install build: exit 0
- **Committed in:** N/A (environmental fix, no code change; not part of any task commit)

### Other Notes (not formal deviations)

- **Task 2 commit:** Plan's commit message format said `chore(107.3-01): {task-name}` "if any verification artifacts need committing; otherwise skip". Task 2 produced no files modified (verification-only). Skipped per plan guidance.
- **Push-hook:** Phase 56 auto-push-hook did NOT fire automatically — 9 local commits were ahead of origin/main when Task 2 started. Pushed explicitly with `git push origin main` per plan's conditional clause. Worth investigating separately whether push-hook is installed in this local clone; outside this plan's scope.
- **Temporary `.env` for runtime probes:** Created a minimal `vigil-core/.env` with probe values (DATABASE_URL, JWT_SECRET, OAuth secrets) to satisfy server early-boot checks for Checks 3-5. `.env` is gitignored via root `.gitignore` line 11 (`*.env`). Deleted after probes completed; not committed.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Environmental only. No scope creep. Zero new code beyond the plan's prescribed 10-line index.ts edit.

## Issues Encountered

### Railway CLI unavailable in this environment — manual env-var audit deferred

Plan's Task 2 step 4 retry-path instructed checking if a manual `VIGIL_BIND_HOST=0.0.0.0` override exists in Railway env vars (from the 2026-04-22 manual outage-fix) that might be masking our code-level fix. Railway CLI (`railway`) is not installed locally — cannot verify which path is currently active (code default vs. manual env-var override).

**Current state:** prod is reachable either way. The belt-and-suspenders state (both code default AND manual env var) is safe, but leaves uncertainty about whether the code-level fix alone would survive a Railway env-var wipe.

**Recommendation for user (DO NOT auto-unset per `safety.always_confirm_external_services=true`):**
1. On a machine with Railway CLI logged in, run: `cd vigil-core && railway variables | grep VIGIL_BIND_HOST`
2. If `VIGIL_BIND_HOST=0.0.0.0` is still set as a manual Railway env var:
   - **Option A (belt-and-suspenders):** Keep it. Slight redundancy, zero risk. Recommended if paranoid about regression.
   - **Option B (clean code-only):** `cd vigil-core && railway variables --unset VIGIL_BIND_HOST` then re-run `bash Scripts/verify-phase-107.sh --external` to prove the code default alone suffices. This is the stricter test of whether this plan's code change works.
3. If the env var is already absent, the code default is already the only thing keeping prod up — the external probe PASS on first attempt is proof.

User should pick Option A or B; this plan closes prod-reachability either way.

## User Setup Required

None — no external service configuration required for this plan. Open Railway env-var question above is optional follow-up.

## Next Phase Readiness

- Phase 107.3: **4/4 plans complete** (Plan 01 + the previously-landed Plans 02, 03, 04). Phase ready for orchestrator verification.
- Permanent external probe gate installed (Plan 04) — future vigil-core deploys can run `bash Scripts/verify-phase-107.sh --external` as a smoke test to catch 502-class regressions.
- Pitfall 1 from RESEARCH ("Testing only the local path for Railway bugs") is now structurally closed: phase shipped with end-to-end external verification, not a green-local-build assumption.

### Traceability

- **Closes:** RESEARCH Pitfall 1 (external probe required for Railway bind bugs)
- **Fixes:** 2026-04-22 502 "Application failed to respond" outage on api.vigilhub.io at the code level
- **Depends on:** Plan 107.3-04 `--external` mode (used as the verification gate here)

---
*Phase: 107.3-prod-bind-install-sh-doctor-cleanup*
*Completed: 2026-04-22*
