---
phase: 107.3-prod-bind-install-sh-doctor-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/verify-phase-107.sh
autonomous: true

must_haves:
  truths:
    - "Scripts/verify-phase-107.sh --external runs the external prod HTTP probe against https://api.vigilhub.io/v1/health and exits 0 on HTTP 200, non-zero on anything else"
    - "Scripts/verify-phase-107.sh --full runs static + runtime + external (so the full suite catches the 502 class Phase 107.2-01's local probe missed)"
    - "The existing --static and --runtime modes still behave identically (no regression)"
    - "Running without args still defaults to --static (preserves existing default behavior)"
  artifacts:
    - path: "Scripts/verify-phase-107.sh"
      provides: "Phase 107 verify harness extended with --external mode for live Railway prod reachability"
      contains: "check_external_health"
  key_links:
    - from: "Scripts/verify-phase-107.sh check_external_health"
      to: "https://api.vigilhub.io/v1/health"
      via: "curl -s -o /dev/null -w %{http_code} --max-time 10"
      pattern: "curl.*api\\.vigilhub\\.io/v1/health"
---

<objective>
Extend `Scripts/verify-phase-107.sh` with a new `--external` mode that probes the live prod URL `https://api.vigilhub.io/v1/health` and asserts HTTP 200. This is the regression test that would have caught Phase 107.2-01's bind-host bug: the local `NODE_ENV=production` probe booted green on localhost, never testing the Railway proxy → container path. An external probe is the only signal that matches the outage mode we actually ship to users.

Purpose: Give Phase 107.3 Fix 1 (vigil-core bind) a post-deploy verification target, and permanently arm the verify suite against the 502 class of bug.

Output: `Scripts/verify-phase-107.sh` gains a new `check_external_health` function, a new `run_external` runner, and the `--external` CLI mode. `--full` also incorporates the external probe after runtime.
</objective>

<execution_context>
@/Users/jamesonmorrill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesonmorrill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-RESEARCH.md
@Scripts/verify-phase-107.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add check_external_health function + --external mode to verify-phase-107.sh</name>
  <files>Scripts/verify-phase-107.sh</files>
  <action>
Open `Scripts/verify-phase-107.sh`. Three surgical edits — all additive, no logic changes to existing code.

EDIT 1 — Add `check_external_health` function immediately before the `run_static` function (currently at line 149). Copy verbatim from RESEARCH.md Code Examples "Fix 3 optional":

```
check_external_health() {
  info "Check N: External prod health probe — https://api.vigilhub.io/v1/health"
  local HTTP_STATUS
  HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 \
    "https://api.vigilhub.io/v1/health" 2>/dev/null || echo "000")
  if [[ "$HTTP_STATUS" == "200" ]]; then
    green "  PASS — api.vigilhub.io/v1/health returned HTTP 200"
  else
    red "  FAIL — api.vigilhub.io/v1/health returned HTTP $HTTP_STATUS"
    red "         Check VIGIL_BIND_HOST in Railway dashboard — must be 0.0.0.0"
    red "         (This probe catches the 502 class that a local NODE_ENV=production probe misses.)"
    FAIL=1
  fi
}
```

EDIT 2 — Add `run_external` runner immediately after the existing `run_runtime` function (currently ends at line 160). The exact insertion point is right before the blank line that precedes `case "$MODE" in`:

```
run_external() {
  check_external_health
}
```

EDIT 3 — Update the `case "$MODE"` block (currently lines 162-176) to add the `--external` case AND incorporate external into `--full`. The full new case block:

EXACT OLD case block (lines 162-176, copy verbatim):
```
case "$MODE" in
  --static)
    run_static
    ;;
  --runtime)
    run_runtime
    ;;
  --full|"")
    run_static
    run_runtime
    ;;
  *)
    echo "usage: $0 [--static|--runtime|--full]" >&2
    exit 2
    ;;
esac
```

EXACT NEW case block (replace with):
```
case "$MODE" in
  --static)
    run_static
    ;;
  --runtime)
    run_runtime
    ;;
  --external)
    # Phase 107.3 Fix 4: live Railway prod reachability. Separate mode because it
    # requires a deployed build and an outbound network; --static/--runtime do not.
    run_external
    ;;
  --full|"")
    run_static
    run_runtime
    run_external
    ;;
  *)
    echo "usage: $0 [--static|--runtime|--external|--full]" >&2
    exit 2
    ;;
esac
```

NOTES — LOCKED decisions (do NOT relitigate):
- Probe URL is `https://api.vigilhub.io/v1/health` (live response shape verified in RESEARCH: `{"status":"ok","timestamp":"...","version":"0.1.0","database":"connected"}`)
- Timeout is 10 seconds (matches RESEARCH)
- Use of `|| echo "000"` fallback ensures the `HTTP_STATUS` variable is always set even if `curl` itself errors under `set -euo pipefail`
- Default mode when no arg is passed is still `--static` (preserves legacy behavior — the `MODE="${1:---static}"` line at top of file must NOT be changed)

Wait — IMPORTANT: re-check the default. Re-read lines 10-11 of the existing script:
  grep -n '^MODE=' Scripts/verify-phase-107.sh
It reads `MODE="${1:---static}"`. So default is `--static`, NOT `--full`. Do NOT change this. The case `--full|""` matches only when `$1` is literally empty-string, which won't happen given the default. Leave the `--full|""` alternation as-is for safety.

DO NOT change:
- Any existing check functions (lines 18-147)
- The MODE default line
- The final FAIL-reporting block (lines 179-184)
- Helper color functions (red/green/info)
  </action>
  <verify>
1. Syntax check:
   bash -n Scripts/verify-phase-107.sh

2. Usage line updated:
   bash Scripts/verify-phase-107.sh --badflag 2>&1
   Expected output contains: `usage: ... [--static|--runtime|--external|--full]`
   Expected exit code: 2

3. Existing modes unchanged — run --static and confirm no new output:
   bash Scripts/verify-phase-107.sh --static 2>&1 | tail -5
   Expected: same "all checks passed" (or same failures as before — your change must not add or remove any check in --static mode).

4. New mode works against live prod (this is the whole point):
   bash Scripts/verify-phase-107.sh --external 2>&1
   Expected (with prod currently healthy): `PASS — api.vigilhub.io/v1/health returned HTTP 200`.
   Expected exit: 0.

   If prod happens to be unhealthy right now, expected: `FAIL — api.vigilhub.io/v1/health returned HTTP <code>` with exit 1. That's a correct failure — do NOT attempt to fix prod from this plan; just note it and move on.

5. Curl behavior under pipefail — verify the fallback works. Run without network by blocking the domain:
   (Simulate network fail: use a bogus URL to the same probe pattern in a shell-only test.)
   bash -c 'set -euo pipefail; HTTP=$(curl -s -o /dev/null -w "%{http_code}" --max-time 2 "https://127.0.0.1:1" 2>/dev/null || echo "000"); echo "HTTP=[$HTTP]"'
   Expected: `HTTP=[000]` and exit 0 (no pipefail abort).

6. Git diff is scoped:
   git diff Scripts/verify-phase-107.sh
   Expected: only three additive blocks (check_external_health function, run_external function, case-block `--external` branch + `run_external` added to `--full`).
  </verify>
  <done>
- `bash -n` passes
- `--badflag` usage message includes `--external`
- `--static` output identical to pre-change
- `--external` against live prod returns HTTP 200 PASS (or HTTP non-200 FAIL — both are acceptable, depending on prod state at run time)
- Pipefail fallback verified via 127.0.0.1:1 test
- Diff is confined to additive changes; no existing check function modified
  </done>
</task>

</tasks>

<verification>
Single-task plan. Task's own <verify> block covers all checks.

This plan only ADDS a new verification mode — no existing behavior changes, so regression risk for other phases is zero. Running `--static` or `--full` on any unrelated work after this merges must continue to behave as before for the static/runtime portions; the external portion is new signal.
</verification>

<success_criteria>
- `Scripts/verify-phase-107.sh` has `check_external_health` function matching RESEARCH.md Code Examples
- `--external` CLI mode available and works against live prod
- `--full` incorporates `run_external` after `run_runtime`
- Usage message updated to include `--external`
- `--static` and `--runtime` behaviors unchanged
- This plan's output is consumable by Plan 107.3-01 (which depends on this and runs `--external` after Railway redeploy)
</success_criteria>

<output>
After completion, create `.planning/phases/107.3-prod-bind-install-sh-doctor-cleanup/107.3-04-SUMMARY.md` using the summary template. Key fields:
- subsystem: tooling/verify-harness
- tech-stack.added: (none — uses curl which is already a dep)
- affects: Scripts/verify-phase-107.sh
- decisions: External probe gets its own `--external` mode (not merged into `--runtime`) — LOCKED by RESEARCH Open Question 2. Reason: `--runtime` doesn't require network; `--external` does.
- patterns: Additive-only extension — existing check functions untouched
- NOTE: Plan 107.3-01 depends on this plan (uses the new `--external` probe as its post-deploy verification)
</output>
