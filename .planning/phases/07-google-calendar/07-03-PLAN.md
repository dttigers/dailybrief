---
phase: 07-google-calendar
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Sources/JarvisCore/Models/DailyBriefData.swift
  - Sources/DailyBrief/DailyBrief.swift
  - Sources/DailyBrief/PDF/PageOneRenderer.swift
  - Sources/DailyBrief/PDF/PDFGenerator.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
  - Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
autonomous: true
---

<objective>
Wire Google Calendar events into the daily brief PDF and the dashboard, completing the end-to-end calendar integration.

Purpose: Users see today's schedule in their printed daily brief and in the dashboard app — the core value of the Google Calendar phase.
Output: Calendar events rendered in PDF Page 1, calendar section in dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-google-calendar/07-01-SUMMARY.md

@Sources/JarvisCore/Models/DailyBriefData.swift
@Sources/JarvisCore/Models/CalendarEvent.swift
@Sources/DailyBrief/DailyBrief.swift
@Sources/DailyBrief/PDF/PDFGenerator.swift
@Sources/DailyBrief/PDF/PageOneRenderer.swift
@Sources/DailyBrief/PDF/PDFStyles.swift
@Sources/DailyBrief/Services/GoogleCalendarService.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardView.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire calendar events into DailyBriefData and PDF brief</name>
  <files>Sources/JarvisCore/Models/DailyBriefData.swift, Sources/DailyBrief/DailyBrief.swift, Sources/DailyBrief/PDF/PageOneRenderer.swift</files>
  <action>
  1. Update DailyBriefData:
     - Add `public var calendarEvents: [CalendarEvent]` field with default `[]`
     - Update init to accept calendarEvents parameter with default `[]`

  2. Update Generate command in DailyBrief.swift:
     - After initializing other services, conditionally create GoogleCalendarService:
       ```
       let calendarService: GoogleCalendarService? = config.googleCalendar.enabled
           ? GoogleCalendarService(config: config.googleCalendar) : nil
       ```
     - Add to the async let block: `async let calendarResult = tryFetch("Calendar") { try await calendarService?.fetchTodayEvents() ?? [] }`
     - Pass calendarEvents to DailyBriefData init
     - Add calendar events to printSummary() dry-run output:
       ```
       print("\nTODAY'S SCHEDULE (\(data.calendarEvents.count)):")
       for event in data.calendarEvents {
           print("  \(event.timeString)  \(event.title)")
           if let loc = event.location { print("    📍 \(loc)") }
       }
       if data.calendarEvents.isEmpty { print("  (no events)") }
       ```
     - Graceful degradation: if google_calendar not in config or not enabled, calendarEvents stays empty. If fetch fails, tryFetch logs error and returns nil/empty.

  3. Render calendar events in PDF Page 1:
     - Add a "Today's Schedule" section in PageOneRenderer AFTER the "To Do" section and BEFORE the notes area
     - Section header: "Today's Schedule" in headerFont
     - Each event rendered as: "[timeString]  [title]" in bodyFont
       - All-day events first, then sorted by start time
       - If location exists, render it in smaller captionFont below the title, indented
     - Max 8 events displayed (truncate with "... and N more" if over 8)
     - If calendarEvents is empty, skip the section entirely (don't render header or "no events" — same pattern as Page 3 conditional rendering)

  Note: PageOneRenderer uses top-down Y tracking. Follow the existing pattern of incrementing `y` after each drawn element. Ensure the schedule section fits — if content would overflow, it will naturally truncate via the max 8 limit.
  </action>
  <verify>swift build compiles without errors. Run `dailybrief --dry-run` — should show "TODAY'S SCHEDULE" section (empty if not authorized, which is expected).</verify>
  <done>DailyBriefData carries calendarEvents. Generate command fetches events with graceful degradation. PDF renders schedule section when events exist. Dry-run output includes calendar section.</done>
</task>

<task type="auto">
  <name>Task 2: Add calendar events section to dashboard</name>
  <files>Sources/DailyBriefMonitor/Dashboard/DashboardView.swift, Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift</files>
  <action>
  1. Update DashboardViewModel:
     - Add `var calendarEvents: [CalendarEvent] = []` property
     - Add `var isLoadingCalendar: Bool = false` property
     - Add `func loadCalendarEvents() async` method that:
       a. Loads config via ConfigLoader
       b. If googleCalendar.enabled, creates GoogleCalendarService and calls fetchTodayEvents()
       c. Sets calendarEvents on success, empty on failure (graceful degradation — no error shown for calendar)
     - Call loadCalendarEvents() from existing refresh() method

  2. Update DashboardView:
     - Add a "Today's Schedule" section above the thought list in the detail area (or as a collapsible section at the top of the detail view)
     - Only show when calendarEvents is non-empty
     - Render as a compact list:
       ```
       Section("Today's Schedule") {
           ForEach(viewModel.calendarEvents) { event in
               HStack {
                   Text(event.timeString)
                       .font(.caption)
                       .foregroundStyle(.secondary)
                       .frame(width: 120, alignment: .leading)
                   Text(event.title)
                       .lineLimit(1)
               }
           }
       }
       ```
     - Keep it lightweight — this is supplementary info, not the primary content
     - If calendar not configured or no events, section simply doesn't appear

  Follow existing DashboardView patterns: @State viewModel, NavigationSplitView layout, .task for initial load.

  Note: GoogleCalendarService is in the DailyBrief target, not DailyBriefMonitor. To use it from the Monitor, either:
  (a) Move GoogleCalendarService to JarvisCore (preferred — it's a shared service like CaptureService/TriageService), OR
  (b) Duplicate the fetch logic in DashboardViewModel using URLSession directly

  Option (a) is cleaner. If GoogleCalendarService was created in Sources/DailyBrief/Services/, move it to Sources/JarvisCore/Services/ so both targets can import it. This matches CaptureService, TriageService, etc.
  </action>
  <verify>swift build compiles without errors. Dashboard loads and displays (may show no calendar events if not yet authorized — that's expected).</verify>
  <done>Dashboard shows "Today's Schedule" section when events exist. Calendar fetch integrated into dashboard refresh. Graceful degradation when calendar not configured.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] `dailybrief --dry-run` includes TODAY'S SCHEDULE section
- [ ] DailyBriefData.calendarEvents populated when calendar configured
- [ ] PDF renders schedule section when events exist
- [ ] PDF unchanged (no empty section) when no events
- [ ] Dashboard shows calendar events when available
- [ ] Dashboard works normally when calendar not configured
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Calendar events visible in PDF brief
- Calendar events visible in dashboard
- Graceful degradation everywhere (no crashes when calendar not set up)
</success_criteria>

<output>
After completion, create `.planning/phases/07-google-calendar/07-03-SUMMARY.md`
</output>
