---
phase: 68-bulk-actions-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-pwa/src/api/client.ts
  - vigil-pwa/src/hooks/useThoughts.ts
  - vigil-pwa/src/components/ThoughtRow.tsx
  - vigil-pwa/src/components/ThoughtList.tsx
  - vigil-pwa/src/components/BulkActionBar.tsx
  - vigil-pwa/src/pages/ThoughtsPage.tsx
autonomous: true
requirements:
  - BULK-01

must_haves:
  truths:
    - "User can tap a checkbox on each thought to select it"
    - "User can select all visible thoughts with a single action"
    - "User can bulk delete selected thoughts and they disappear from the list"
    - "User can bulk recategorize selected thoughts to a new category"
    - "Bulk action bar appears only when thoughts are selected"
  artifacts:
    - path: "vigil-pwa/src/components/BulkActionBar.tsx"
      provides: "Floating bar with delete/recategorize actions and selection count"
    - path: "vigil-pwa/src/api/client.ts"
      provides: "bulkDeleteThoughts and bulkRecategorizeThoughts functions"
  key_links:
    - from: "vigil-pwa/src/pages/ThoughtsPage.tsx"
      to: "vigil-pwa/src/api/client.ts"
      via: "bulkDeleteThoughts/bulkRecategorizeThoughts calls"
      pattern: "bulkDeleteThoughts|bulkRecategorizeThoughts"
    - from: "vigil-pwa/src/components/BulkActionBar.tsx"
      to: "vigil-pwa/src/pages/ThoughtsPage.tsx"
      via: "onDelete/onRecategorize callbacks"
      pattern: "onDelete|onRecategorize"
---

<objective>
Add multi-select capability to the thoughts list and a bulk action bar for deleting and recategorizing multiple thoughts at once.

Purpose: BULK-01 — users managing hundreds of thoughts need batch operations to stay organized without tedious one-by-one editing.
Output: Selection checkboxes on each thought row, select-all toggle, floating bulk action bar with delete and recategorize actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@vigil-pwa/src/pages/ThoughtsPage.tsx
@vigil-pwa/src/components/ThoughtRow.tsx
@vigil-pwa/src/components/ThoughtList.tsx
@vigil-pwa/src/hooks/useThoughts.ts
@vigil-pwa/src/api/client.ts
@vigil-core/src/routes/bulk.ts

<interfaces>
<!-- Backend bulk endpoints already exist in vigil-core/src/routes/bulk.ts -->

POST /v1/thoughts/bulk/delete
  Body: { ids: number[] }
  Response: { deleted: number }

POST /v1/thoughts/bulk/recategorize
  Body: { ids: number[], category: "task"|"therapy"|"idea"|"reflection"|"project" }
  Response: { updated: number }

<!-- Existing PWA types from vigil-pwa/src/api/client.ts -->
export interface ThoughtApiResponse {
  id: number
  content: string
  category: string | null
  confidence: number | null
  source: string
  createdAt: string
  modifiedAt: string
  taskStatus: string | null
  tags: string[]
  isFavorited: boolean
  projectId: number | null
}

<!-- Existing updateThought supports isFavorited -->
export async function updateThought(id, patch: { content?, category?, isFavorited?, taskStatus?, projectId? })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bulk API functions and wire selection into ThoughtRow/ThoughtList</name>
  <read_first>
    - vigil-pwa/src/api/client.ts (add bulk functions after existing exports)
    - vigil-pwa/src/components/ThoughtRow.tsx (add checkbox prop)
    - vigil-pwa/src/components/ThoughtList.tsx (pass selection state to rows)
  </read_first>
  <files>vigil-pwa/src/api/client.ts, vigil-pwa/src/components/ThoughtRow.tsx, vigil-pwa/src/components/ThoughtList.tsx</files>
  <action>
1. In `vigil-pwa/src/api/client.ts`, add two new exported functions after the existing `updateThought`:

```typescript
export async function bulkDeleteThoughts(ids: number[]): Promise<{ deleted: number }> {
  const res = await vigilFetch('/v1/thoughts/bulk/delete', {
    method: 'POST',
    body: JSON.stringify({ ids }),
  })
  if (!res.ok) throw new Error(`Bulk delete failed: ${res.status}`)
  return res.json()
}

export async function bulkRecategorizeThoughts(ids: number[], category: string): Promise<{ updated: number }> {
  const res = await vigilFetch('/v1/thoughts/bulk/recategorize', {
    method: 'POST',
    body: JSON.stringify({ ids, category }),
  })
  if (!res.ok) throw new Error(`Bulk recategorize failed: ${res.status}`)
  return res.json()
}
```

2. In `ThoughtRow.tsx`:
   - Add `isSelectable`, `isSelected`, and `onToggleSelect` to `ThoughtRowProps`:
     ```typescript
     isSelectable?: boolean
     isSelected?: boolean
     onToggleSelect?: (id: number) => void
     ```
   - When `isSelectable` is true, render a checkbox (`<input type="checkbox">`) as the first element inside the row div, before the category badge. Style: `w-5 h-5 rounded border-slate-600 bg-slate-800 accent-indigo-500 shrink-0 cursor-pointer mt-0.5`. The checkbox `checked` state = `isSelected`, `onChange` calls `onToggleSelect(thought.id)`.
   - When `isSelectable` is true, add a subtle left border highlight on selected rows: `border-l-2 border-l-indigo-500` (replace border-b-only with combined border).

3. In `ThoughtList.tsx`:
   - Add to `ThoughtListProps`: `selectedIds?: Set<number>`, `onToggleSelect?: (id: number) => void`, `isSelectable?: boolean`
   - Pass `isSelectable`, `isSelected={selectedIds?.has(thought.id)}`, and `onToggleSelect` through to each `ThoughtRow`.
  </action>
  <verify>
    <automated>cd /Users/jamesonmorrill/Desktop/Local\ AI/dailybrief/vigil-pwa && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - bulkDeleteThoughts and bulkRecategorizeThoughts exported from client.ts
    - ThoughtRow renders a checkbox when isSelectable=true
    - ThoughtList passes selection state to each ThoughtRow
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Bulk API client functions exist. ThoughtRow shows checkbox when selectable. ThoughtList forwards selection props.</done>
</task>

<task type="auto">
  <name>Task 2: Build BulkActionBar and wire multi-select into ThoughtsPage</name>
  <read_first>
    - vigil-pwa/src/pages/ThoughtsPage.tsx (add selection state and bulk action handlers)
    - vigil-pwa/src/components/BulkActionBar.tsx (new — floating action bar)
    - vigil-pwa/src/hooks/useThoughts.ts (need removeMany for optimistic deletes)
  </read_first>
  <files>vigil-pwa/src/components/BulkActionBar.tsx, vigil-pwa/src/pages/ThoughtsPage.tsx, vigil-pwa/src/hooks/useThoughts.ts</files>
  <action>
1. In `useThoughts.ts`, add a `removeMany` callback that filters out thoughts by id set, and decrements total:
```typescript
const removeMany = useCallback((ids: Set<number>) => {
  setThoughts((prev) => prev.filter((t) => !ids.has(t.id)))
  setTotal((prev) => prev - ids.size)
}, [])
```
Also add `updateMany` for bulk recategorize:
```typescript
const updateMany = useCallback((ids: Set<number>, patch: Partial<ThoughtApiResponse>) => {
  setThoughts((prev) => prev.map((t) => ids.has(t.id) ? { ...t, ...patch } : t))
}, [])
```
Return `removeMany` and `updateMany` from the hook.

2. Create `BulkActionBar.tsx`:
   - Props: `selectedCount: number`, `onDelete: () => void`, `onRecategorize: (category: string) => void`, `onClearSelection: () => void`, `isProcessing: boolean`
   - Render a fixed-bottom bar (Tailwind: `fixed bottom-20 left-1/2 -translate-x-1/2 z-50 bg-slate-800 border border-slate-700 rounded-xl shadow-2xl px-4 py-3 flex items-center gap-3`) that appears only when `selectedCount > 0`.
   - Content: "{selectedCount} selected" text label, a "Delete" button (red, `bg-red-600 hover:bg-red-700 text-white px-3 py-1.5 rounded-lg text-sm font-medium`), a "Recategorize" dropdown button that shows a small popover/dropdown with the 5 category options (task, therapy, idea, reflection, project) — use a simple `useState` toggle for the dropdown visibility. Each option calls `onRecategorize(category)`.
   - A "Clear" (x) button on the right to deselect all. Style: `text-slate-400 hover:text-white`.
   - When `isProcessing` is true, disable buttons and show "..." after button text.
   - Delete button should show a `window.confirm('Delete N thoughts?')` before calling `onDelete`.

3. In `ThoughtsPage.tsx`:
   - Add state: `const [selectedIds, setSelectedIds] = useState<Set<number>>(new Set())`
   - Add state: `const [isSelectable, setIsSelectable] = useState(false)`
   - Add state: `const [isBulkProcessing, setIsBulkProcessing] = useState(false)`
   - Add a "Select" toggle button next to the SearchBar. When tapped, toggles `isSelectable`. When toggled off, clear `selectedIds`.
   - Add a "Select All" checkbox in the list header area (visible only when `isSelectable` is true). Checked when `selectedIds.size === thoughts.length && thoughts.length > 0`. onChange: if checked, clear set; if unchecked, add all `thoughts.map(t => t.id)`.
   - Wire `onToggleSelect` to add/remove from `selectedIds` set (create new Set for immutability).
   - Pass `selectedIds`, `onToggleSelect`, `isSelectable` to `ThoughtList`.
   - Render `<BulkActionBar>` with handlers:
     - `onDelete`: call `bulkDeleteThoughts([...selectedIds])`, then `removeMany(selectedIds)`, then clear selection
     - `onRecategorize`: call `bulkRecategorizeThoughts([...selectedIds], category)`, then `updateMany(selectedIds, { category })`, then clear selection
   - Clear selection when `activeCategory`, `debouncedQuery` change (useEffect).
   - Import `bulkDeleteThoughts`, `bulkRecategorizeThoughts` from `../api/client`.
  </action>
  <verify>
    <automated>cd /Users/jamesonmorrill/Desktop/Local\ AI/dailybrief/vigil-pwa && npx tsc --noEmit 2>&1 | head -30 && echo "---BUILD CHECK---" && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BulkActionBar.tsx exists with delete and recategorize buttons
    - ThoughtsPage has a "Select" toggle that enables checkboxes on rows
    - Select All checkbox selects/deselects all visible thoughts
    - Bulk delete calls API and removes thoughts from view optimistically
    - Bulk recategorize calls API and updates categories optimistically
    - Selection clears when toggling off select mode or changing filters
    - TypeScript compiles, Vite builds successfully
  </acceptance_criteria>
  <done>Multi-select mode with bulk delete and recategorize works end-to-end. BulkActionBar shows when thoughts are selected. Optimistic UI updates on both operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA -> API | Bulk operations send user-selected IDs to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | bulk/delete endpoint | accept | Server already validates IDs are integers and uses bearer auth; no additional PWA-side mitigation needed |
| T-68-02 | Denial of Service | bulk operations | mitigate | Server already limits to existing thought IDs; PWA confirms deletion via window.confirm before sending |
</threat_model>

<verification>
1. Toggle select mode on ThoughtsPage
2. Select 2-3 thoughts via checkboxes
3. Bulk action bar appears with correct count
4. Click Delete — confirm dialog — thoughts removed
5. Select thoughts again, recategorize to "idea" — categories update
6. Toggle select mode off — checkboxes disappear, selection clears
</verification>

<success_criteria>
- Multi-select checkboxes appear when select mode toggled on
- Bulk action bar shows with delete and recategorize options
- Bulk delete removes thoughts from view and server
- Bulk recategorize updates categories in view and server
- Select All / deselect all works
- Clean build with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/68-bulk-actions-filters/68-01-SUMMARY.md`
</output>
