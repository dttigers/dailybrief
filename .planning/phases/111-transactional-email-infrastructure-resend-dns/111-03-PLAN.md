---
phase: 111
plan: 03
type: execute
wave: 2
depends_on:
  - 111-01
  - 111-02
files_modified:
  - vigil-core/scripts/smoke-test-email.ts
  - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md
autonomous: false
requirements:
  - EMAIL-01
user_setup:
  - service: resend
    why: "Capture API key for Railway; previously deferred from Plan 01"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key (Permission: Sending access, Domain: vigilhub.io)"
    dashboard_config:
      - task: "Generate a new API key scoped to sending-only access for vigilhub.io"
        location: "Resend Dashboard -> API Keys -> Create API Key"
  - service: railway
    why: "Inject RESEND_API_KEY + VIGIL_APP_BASE_URL into prod runtime"
    env_vars:
      - name: RESEND_API_KEY
        source: "Copy from Resend API Keys page (one-time reveal — save to 1Password immediately)"
      - name: VIGIL_APP_BASE_URL
        source: "Literal value https://app.vigilhub.io"
    dashboard_config:
      - task: "Add both env vars to the vigil-core Railway service"
        location: "Railway Dashboard -> vigil-core -> Variables -> New Variable (x2)"

must_haves:
  truths:
    - "A manual test email triggered from a dev machine (with the real key injected) lands in jamesonmorrill1@gmail.com INBOX (not Spam, not Promotions) — Success Criterion #1"
    - "Raw source of the received email (View Original in Gmail) shows DKIM=PASS, SPF=PASS, DMARC=PASS"
    - "Raw source shows the href attribute on the CTA link is a verbatim https URL starting with the passed-in origin — no tracking-domain rewrite (Success Criterion #3)"
    - "Resend dashboard Logs tab shows the delivery event with status 'delivered'"
    - "RESEND_API_KEY + VIGIL_APP_BASE_URL are set in Railway Variables for vigil-core service (Success Criterion #5)"
    - "Smoke script reads VIGIL_APP_BASE_URL from env (not hardcoded) — exercises the D-05/D-06 wiring that Phase 112 will depend on"
  artifacts:
    - path: "vigil-core/scripts/smoke-test-email.ts"
      provides: "Reusable one-shot smoke script — reads VIGIL_APP_BASE_URL from env, falls back to https://app.vigilhub.io, sends one reset email, exits"
      min_lines: 20
    - path: ".planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md"
      provides: "Log of the live send — Resend message id, Gmail raw-source auth headers, CTA href from raw source, delivery timestamp"
      contains: "DKIM=PASS"
  key_links:
    - from: "vigil-core/scripts/smoke-test-email.ts"
      to: "vigil-core/src/services/email-service.ts"
      via: "import { sendPasswordResetEmail } from '../src/services/email-service.js'"
      pattern: "sendPasswordResetEmail"
    - from: "vigil-core/scripts/smoke-test-email.ts"
      to: "process.env.VIGIL_APP_BASE_URL"
      via: "env read at the top of main() — exercises the D-05/D-06 wiring"
      pattern: "process.env\\[\"VIGIL_APP_BASE_URL\"\\]"
    - from: "Railway env injection"
      to: "vigil-core runtime process.env.RESEND_API_KEY"
      via: "Railway Variables → process env at container start"
      pattern: "RESEND_API_KEY"
---

<objective>
Close Phase 111 by proving the transport end-to-end with a live send. After Plan 01 verified DNS + Resend domain, and Plan 02 shipped the email-service code, this plan:
1. Generates the real RESEND_API_KEY at Resend
2. Injects it into Railway (and VIGIL_APP_BASE_URL)
3. Runs `scripts/smoke-test-email.ts` from a dev machine with the key exported, which hits Resend's live API and sends one reset email to jamesonmorrill1@gmail.com
4. Human opens Gmail, confirms inbox placement (not spam), inspects raw source for DKIM/SPF/DMARC PASS + verbatim CTA href

Purpose: Success Criterion #1 (deliverability) + #5 (Railway env set) from Phase 111. Rounds out EMAIL-01 end-to-end.

Output: `smoke-test-email.ts` (reusable, committed) + `111-03-LIVE-VERIFICATION.md` (log of the live send with auth headers + CTA href pasted from Gmail raw source).

This plan is NOT autonomous — it requires human at the Resend dashboard, Railway dashboard, and Gmail inbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-transactional-email-infrastructure-resend-dns/111-CONTEXT.md
@.planning/phases/111-transactional-email-infrastructure-resend-dns/111-01-SUMMARY.md
@.planning/phases/111-transactional-email-infrastructure-resend-dns/111-02-SUMMARY.md
@vigil-core/src/services/email-service.ts

<interfaces>
<!-- Interfaces consumed: Plan 02's sendPasswordResetEmail wrapper. -->

From vigil-core/src/services/email-service.ts (shipped in Plan 02):
```typescript
export function sendPasswordResetEmail(
  to: string,
  resetUrl: string,
): Promise<EmailSendResult>;

export type EmailSendResult =
  | { status: "sent"; id: string }
  | { status: "skipped_no_key" }
  | { status: "failed"; error: string };
```

Behavior the smoke script relies on:
- If `RESEND_API_KEY` is exported in the shell, the singleton lazy-inits a real Resend client → live send
- Result `{ status: "sent", id: "..." }` on success — print the id for cross-reference with Resend dashboard Logs
- Result `{ status: "failed", error }` on failure — print both and exit 1 so the smoke test surfaces problems

Env the smoke script consumes:
- `VIGIL_APP_BASE_URL` — origin for the constructed reset URL. Smoke script reads this from env (NOT hardcoded) so this phase actually exercises the D-05/D-06 wiring that Phase 112 will depend on. Falls back to `https://app.vigilhub.io` if unset so local dry-runs still work.
- `RESEND_API_KEY` — consumed transitively by the email-service singleton at module load.

Sender address: `noreply@vigilhub.io` (hardcoded in email-service per Plan 02) — must match the domain verified in Plan 01.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate RESEND_API_KEY at Resend + set RESEND_API_KEY and VIGIL_APP_BASE_URL in Railway</name>
  <read_first>
    - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-01-SUMMARY.md (confirms Plan 01's Resend domain is still Verified)
    - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-CONTEXT.md §decisions D-06 (VIGIL_APP_BASE_URL prod value)
    - Memory: project_railway_deploy.md (vigil-core Root Directory is `vigil-core`, custom domain api.vigilhub.io)
    - Memory: project_secret_drift.md (Anthropic key already sprawls; KEEP RESEND_API_KEY ONLY in Railway — do NOT copy into local .env or plist)
  </read_first>
  <what-built>A scoped Resend API key and two env variables injected into Railway's vigil-core service. No code in this task.</what-built>
  <how-to-verify>
    Step 1 — Generate the Resend key:
    1. Log in to https://resend.com/api-keys
    2. Click "Create API Key"
    3. Name: `railway-prod-2026-04` (or similar dated label)
    4. Permission: **Sending access** (NOT Full access — least privilege)
    5. Domain: **vigilhub.io** (scope the key to the verified domain only)
    6. Click Create. Resend reveals the key ONCE — copy it immediately. Format: `re_<random>` (starts with `re_`).
    7. Do NOT paste the key into Slack, any .env file, any committed artifact, or anywhere other than Railway Variables + 1Password.

    Step 2 — Add two env variables in Railway:
    1. Log in to https://railway.app, select the vigil-core service
    2. Variables tab → New Variable
       - Name: `RESEND_API_KEY`
       - Value: paste the `re_...` key from Step 1
       - Click Add
    3. New Variable again
       - Name: `VIGIL_APP_BASE_URL`
       - Value: `https://app.vigilhub.io` (literal — per CONTEXT D-06)
       - Click Add
    4. Railway auto-deploys on new vars (or click Redeploy manually if not). Wait ~30s for the deploy.
    5. Confirm live: `curl -sS https://api.vigilhub.io/v1/health` returns 200 OK (service is up post-deploy). Note: we are NOT testing email at the prod URL — the smoke script runs from the dev machine against the Resend API directly in Task 3.

    Step 3 — Log evidence in a new file:
    1. Create `.planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md`
    2. Add section "## Railway env set" with the ISO-8601 timestamp of when both vars were added. Do NOT paste the key value — log only the variable NAMES and the timestamp.
    3. Add section "## Post-deploy health check" with the curl output.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md`
    - File contains a "## Railway env set" section with ISO-8601 timestamp
    - File §Railway env set lists both variable NAMES literally: `RESEND_API_KEY` and `VIGIL_APP_BASE_URL`
    - File §Railway env set does NOT contain any string starting with `re_` (the actual key value must never be committed — grep -v enforced in human review)
    - `curl -sS https://api.vigilhub.io/v1/health` returns HTTP 200 (Railway deploy successful after env var injection)
    - File §Post-deploy health check includes the curl output
  </acceptance_criteria>
  <resume-signal>Type "railway-env-set" when both vars are in Railway and api.vigilhub.io/v1/health returns 200</resume-signal>
  <files>n/a — checkpoint task (manual dashboard / human verification). Artifacts captured per <how-to-verify> and <acceptance_criteria>.</files>
  <action>Execute the sequence in <how-to-verify>. Pause at the <resume-signal> prompt and wait for the human operator to confirm. Every numbered step is mandatory — do not skip. On any failure, stop and surface the specific step + error observed.</action>
  <verify>See <acceptance_criteria> — each bullet is directly checkable (grep, curl, dig, dashboard state, or committed-file presence). Checkpoint passes only when ALL acceptance_criteria bullets pass.</verify>
  <done>All bullets in <acceptance_criteria> pass AND the human has typed the <resume-signal>. Any artifact the task was supposed to create exists and is committed (or queued to commit).</done>
</task>

<task type="auto">
  <name>Task 2: Write the reusable smoke-test-email.ts script (reads VIGIL_APP_BASE_URL from env)</name>
  <files>vigil-core/scripts/smoke-test-email.ts</files>
  <read_first>
    - vigil-core/src/services/email-service.ts (the module to import)
    - vigil-core/scripts/smoke-test.ts (existing smoke-test script style reference — confirm it exists; if not, follow node:test + tsx conventions)
    - vigil-core/package.json (scripts section — note the existing `"smoke-test": "tsx scripts/smoke-test.ts"` entry for pattern reference)
    - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-CONTEXT.md §decisions D-05 (caller constructs full URL from VIGIL_APP_BASE_URL) + D-06 (prod value https://app.vigilhub.io)
  </read_first>
  <action>
    Create `vigil-core/scripts/smoke-test-email.ts` — a one-shot script that sends a single password reset email to a target address and prints the result. Intended to be run manually:
    - Dev machine with real key: `RESEND_API_KEY=re_... npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com`
    - Local dev without key: `npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com` → prints `skipped_no_key` and exits 0 (confirms the gate)

    **IMPORTANT — exercise the D-05/D-06 wiring:** the origin for the constructed reset URL MUST be read from `process.env["VIGIL_APP_BASE_URL"]` with a fallback to `https://app.vigilhub.io`. This is the ONLY place in Phase 111 that actually consumes `VIGIL_APP_BASE_URL` — Phase 112 will be the first place it matters in a route handler, so this smoke script is our canary that the env var plumbs through correctly. Do NOT hardcode the origin.

    Exact script contents:
    ```typescript
    // Phase 111 Plan 03 — one-shot smoke test for email-service.
    // Usage:
    //   RESEND_API_KEY=re_xxx npx tsx scripts/smoke-test-email.ts <to-address>
    // Reads VIGIL_APP_BASE_URL from env (D-05/D-06) — falls back to https://app.vigilhub.io.
    // This is the first phase that exercises the VIGIL_APP_BASE_URL wiring end-to-end;
    // Phase 112 will consume it in real route handlers.
    // Prints the EmailSendResult JSON + Resend message id (if sent) and exits 0/1.
    // Intended for manual verification — NOT wired into CI.

    import { sendPasswordResetEmail } from "../src/services/email-service.js";

    async function main() {
      const to = process.argv[2];
      if (!to) {
        console.error("Usage: npx tsx scripts/smoke-test-email.ts <to-address>");
        process.exit(2);
      }

      // D-05/D-06: read origin from env, fall back to prod value. Do NOT hardcode.
      const origin = process.env["VIGIL_APP_BASE_URL"] ?? "https://app.vigilhub.io";
      const testUrl = `${origin}/auth/reset?token=smoke-test-${Date.now()}`;
      console.log(`[smoke] Origin (from VIGIL_APP_BASE_URL env, fallback https://app.vigilhub.io): ${origin}`);
      console.log(`[smoke] Sending password-reset email to ${to}`);
      console.log(`[smoke] Reset URL (will appear verbatim in email body): ${testUrl}`);

      const result = await sendPasswordResetEmail(to, testUrl);
      console.log("[smoke] Result:", JSON.stringify(result));

      if (result.status === "sent") {
        console.log(`[smoke] SUCCESS — Resend message id: ${result.id}`);
        console.log(`[smoke] Cross-reference at https://resend.com/emails/${result.id}`);
        process.exit(0);
      }
      if (result.status === "skipped_no_key") {
        console.log("[smoke] SKIPPED — RESEND_API_KEY not set. Export the key and retry.");
        process.exit(0);
      }
      console.error("[smoke] FAILED —", result.error);
      process.exit(1);
    }

    main().catch((err) => {
      console.error("[smoke] Unexpected throw:", err);
      process.exit(1);
    });
    ```

    Run twice without the key to confirm BOTH the gate AND the env-read:

    Run A — default (env unset) — confirms fallback origin path:
    - `cd vigil-core && RESEND_API_KEY= VIGIL_APP_BASE_URL= npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com`
    - Expect stdout contains `Origin (...): https://app.vigilhub.io` (fallback engaged) AND `"status":"skipped_no_key"` AND exit 0

    Run B — env set — confirms env read works:
    - `cd vigil-core && RESEND_API_KEY= VIGIL_APP_BASE_URL=http://localhost:5173 npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com`
    - Expect stdout contains `Origin (...): http://localhost:5173` (env value, NOT the fallback) AND `"status":"skipped_no_key"` AND exit 0

    These two runs together prove the wiring: the env var is actually consumed and the fallback only fires when the var is absent.
  </action>
  <verify>
    <automated>cd vigil-core && test -f scripts/smoke-test-email.ts && RESEND_API_KEY= VIGIL_APP_BASE_URL= npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com 2>&1 | grep -q 'skipped_no_key' && RESEND_API_KEY= VIGIL_APP_BASE_URL=http://localhost:5173 npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com 2>&1 | grep -q 'http://localhost:5173'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `vigil-core/scripts/smoke-test-email.ts`
    - `grep -c 'sendPasswordResetEmail' vigil-core/scripts/smoke-test-email.ts` returns at least 2 (import + call)
    - `grep -c 'process.argv\[2\]' vigil-core/scripts/smoke-test-email.ts` returns 1 (target address is a required CLI arg, not hardcoded)
    - `grep -c 'skipped_no_key' vigil-core/scripts/smoke-test-email.ts` returns at least 1 (explicit handling path)
    - `grep -c 'process.env\["VIGIL_APP_BASE_URL"\]' vigil-core/scripts/smoke-test-email.ts` returns 1 (env read — NOT hardcoded origin; exercises D-05/D-06 wiring)
    - `grep -cE 'https://app\.vigilhub\.io' vigil-core/scripts/smoke-test-email.ts` returns exactly 1 (single fallback literal, NOT repeated — a second hardcoded occurrence would indicate the URL is hardcoded into the template string instead of interpolated from `origin`)
    - Fallback run: `cd vigil-core && RESEND_API_KEY= VIGIL_APP_BASE_URL= npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com 2>&1` contains `https://app.vigilhub.io` AND `skipped_no_key` AND exit code is 0
    - Env-read run: `cd vigil-core && RESEND_API_KEY= VIGIL_APP_BASE_URL=http://localhost:5173 npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com 2>&1` contains `http://localhost:5173` (proving env was read) AND `skipped_no_key` AND exit code is 0
  </acceptance_criteria>
  <done>
    Smoke script committed and runnable. Without the key it prints `skipped_no_key` and exits 0 — proving the gate still works end-to-end through the real (non-mocked) singleton. Setting `VIGIL_APP_BASE_URL` in the env changes the printed origin — proving the D-05/D-06 wiring is live in Phase 111, not deferred to Phase 112.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute live send + verify Gmail inbox placement + raw-source headers</name>
  <read_first>
    - vigil-core/scripts/smoke-test-email.ts (created in Task 2)
    - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md (created in Task 1)
    - .planning/phases/111-transactional-email-infrastructure-resend-dns/111-CONTEXT.md §specifics (Apple Mail pre-fetch + verbatim href reasoning)
  </read_first>
  <what-built>An actual email arrives in jamesonmorrill1@gmail.com via the Resend production path. This is the only task in Phase 111 that hits the real Resend API.</what-built>
  <how-to-verify>
    Step 1 — Export the Resend API key in your shell (do NOT echo it to stdout, do NOT put it in .env, do NOT commit it). Also export VIGIL_APP_BASE_URL so the smoke script constructs the same prod URL Phase 112's route handlers will:
    ```
    # Retrieve the key from 1Password (or Railway Variables — click the eye icon to reveal)
    read -s RESEND_API_KEY
    # paste the re_... value and press Enter
    export RESEND_API_KEY
    export VIGIL_APP_BASE_URL=https://app.vigilhub.io
    ```

    Step 2 — Run the smoke script against your own inbox:
    ```
    cd vigil-core
    npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com
    ```
    Expected stdout contains:
    - `[smoke] Origin (from VIGIL_APP_BASE_URL env, fallback https://app.vigilhub.io): https://app.vigilhub.io`
    - `[smoke] Sending password-reset email to jamesonmorrill1@gmail.com`
    - `[smoke] Reset URL (will appear verbatim in email body): https://app.vigilhub.io/auth/reset?token=smoke-test-<timestamp>`
    - `[smoke] Result: {"status":"sent","id":"<resend-message-id>"}`
    - `[smoke] SUCCESS — Resend message id: <id>`
    - Exit code 0

    Step 3 — Unset the key from your shell immediately to prevent accidental reuse or logging:
    ```
    unset RESEND_API_KEY
    unset VIGIL_APP_BASE_URL
    ```

    Step 4 — Open Gmail and confirm inbox placement:
    1. Go to https://mail.google.com
    2. Check the Inbox (NOT Spam, NOT Promotions, NOT Updates) for a message from `noreply@vigilhub.io` with subject `"Reset your Vigil password"`
    3. If the email is in Spam or any other tab → FAIL this task; investigate deliverability (usually DKIM/SPF/DMARC misalignment from Plan 01 — re-run the Plan 01 dig checks).

    Step 5 — Inspect raw source (critical for Success Criterion #3):
    1. Open the email in Gmail
    2. Click the three-dot menu → "Show original"
    3. In the raw source, confirm all three authentication results show `PASS`:
       - `Received-SPF: pass` (or `spf=pass`)
       - `dkim=pass`
       - `dmarc=pass`
    4. Scroll to the HTML body portion. Find the CTA `<a href="...">` tag. Confirm the `href` attribute value is EXACTLY the URL printed by the smoke script in Step 2 (`https://app.vigilhub.io/auth/reset?token=smoke-test-<timestamp>`) — verbatim, no click-tracking domain like `link.resend.com/r/...`, no URL rewriting.

    Step 6 — Confirm in Resend dashboard Logs:
    1. Open https://resend.com/emails
    2. Find the row with the message id printed by the smoke script
    3. Confirm status column shows "Delivered" (or "Sent" + a subsequent delivery event)

    Step 7 — Append to `111-03-LIVE-VERIFICATION.md`:
    1. "## Smoke send" section with:
       - Resend message id
       - Reset URL printed by the script (verbatim)
       - Send timestamp (from script stdout)
       - Origin used (to prove VIGIL_APP_BASE_URL was honored)
    2. "## Gmail auth headers" section with the literal copy-pasted lines from Gmail raw source showing `spf=pass`, `dkim=pass`, `dmarc=pass` (these are the success signal for all DNS work from Plan 01)
    3. "## CTA href (raw source)" section pasting the literal `<a href="...">...</a>` tag from the raw source, confirming the URL is verbatim and not rewritten
    4. "## Resend dashboard delivery" section with the delivered-timestamp from Resend Logs
  </how-to-verify>
  <acceptance_criteria>
    - Exit code of `npx tsx scripts/smoke-test-email.ts jamesonmorrill1@gmail.com` with real key exported is 0
    - Script stdout contains `"status":"sent"` and a message id matching `/re_|[a-f0-9-]{8,}/`
    - Script stdout contains `Origin (from VIGIL_APP_BASE_URL env, fallback https://app.vigilhub.io): https://app.vigilhub.io` — proves env read (not hardcoded)
    - The email arrives in the Gmail INBOX (not Spam, not Promotions, not Updates) within 60 seconds
    - `111-03-LIVE-VERIFICATION.md §Gmail auth headers` contains the literal strings `spf=pass` AND `dkim=pass` AND `dmarc=pass` (copied from Gmail "Show original")
    - `111-03-LIVE-VERIFICATION.md §CTA href (raw source)` contains a href value starting with `https://app.vigilhub.io/auth/reset?token=smoke-test-` — NO intermediate tracking domain (no `link.resend.com`, no `track.`, no `r.resend.com`)
    - Resend dashboard Logs shows the matching message id with Delivered status
  </acceptance_criteria>
  <resume-signal>Type "delivered" with the Resend message id, OR describe the failure and what raw-source / Resend logs show</resume-signal>
  <files>n/a — checkpoint task (manual dashboard / human verification). Artifacts captured per <how-to-verify> and <acceptance_criteria>.</files>
  <action>Execute the sequence in <how-to-verify>. Pause at the <resume-signal> prompt and wait for the human operator to confirm. Every numbered step is mandatory — do not skip. On any failure, stop and surface the specific step + error observed.</action>
  <verify>See <acceptance_criteria> — each bullet is directly checkable (grep, curl, dig, dashboard state, or committed-file presence). Checkpoint passes only when ALL acceptance_criteria bullets pass.</verify>
  <done>All bullets in <acceptance_criteria> pass AND the human has typed the <resume-signal>. Any artifact the task was supposed to create exists and is committed (or queued to commit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → Resend dashboard (API key creation) | Key is revealed once; if copied into clipboard, may leak via clipboard manager / screenshot apps |
| Human operator → Railway Variables input | Variables are at-rest in Railway's infrastructure; Railway ops staff theoretically have access |
| Dev machine shell → Resend API (live send) | Key is in shell env for the duration of the smoke test; shell history / process listings could expose it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-12 | Information Disclosure | RESEND_API_KEY leaked via shell history (`history` command shows `export RESEND_API_KEY=re_...`) or in ps output of the running process | mitigate | Task 3 Step 1 uses `read -s` (silent stdin read) + variable name without inline value, so shell history records only `read -s RESEND_API_KEY` — the secret never enters history. Task 3 Step 3 explicitly unsets after use. Node does not expose env vars in `ps` argv (they're in the /proc/<pid>/environ file readable only by same UID — acceptable on a trusted dev machine) |
| T-111-13 | Information Disclosure | RESEND_API_KEY leaked by smoke-test-email.ts accidentally echoing process.env | mitigate | Script reads the key ONLY via the email-service singleton (which reads process.env once at import); script source has zero `process.env.RESEND_API_KEY` references (confirmed by grep in Task 2 acceptance criteria — script just calls the wrapper). The singleton hides the key inside the Resend SDK client after construction. The only `process.env[...]` read in the script is for VIGIL_APP_BASE_URL, which is a public URL — safe to log |
| T-111-14 | Information Disclosure | VERIFICATION.md inadvertently committed with the API key pasted in | mitigate | Task 1 acceptance criteria explicitly prohibits any string starting with `re_` in the VERIFICATION.md — log only variable NAMES + timestamps, never values. Human review + grep check before commit |
| T-111-15 | Spoofing | Phishing attacker reuses the smoke script to send attacker-crafted URLs to arbitrary recipients from noreply@vigilhub.io | accept | Script requires RESEND_API_KEY in env to function; anyone with the key can already send arbitrary email via the Resend API directly — the script adds no additional attack surface. Mitigation: least-privilege key scoping (Sending access only, domain-restricted to vigilhub.io) + rotate the key if exposure is suspected |
| T-111-16 | Tampering | Deliverability regression: a new DNS change at Cloudflare (e.g., someone accidentally proxies the DKIM CNAME later) causes DKIM=FAIL but the smoke test was done weeks ago and nobody re-runs it | accept | Out of scope for this phase — ongoing deliverability monitoring is a future phase (email webhook ingestion deferred per CONTEXT). This plan captures a point-in-time PASS; DMARC rua reports to jamesonmorrill1@gmail.com will surface regressions proactively |
| T-111-17 | Denial of Service | Exposure of RESEND_API_KEY allows attacker to burn through the free-tier send quota, causing legitimate password-reset emails to be rate-limited | mitigate | Scoped key (sending-only, vigilhub.io-only) + Resend's per-account rate limits + ability to instantly revoke the key from the dashboard. If the smoke test ever triggers an unexpected rate-limit response, investigate key exposure immediately |
</threat_model>

<verification>
Phase 111 end-to-end verification. All five Phase 111 Success Criteria from ROADMAP must now be PASS:

| SC# | Criterion | Verified by |
|-----|-----------|-------------|
| 1 | Manual test email lands in Gmail inbox (not spam) | Plan 03 Task 3 |
| 2 | dig + Resend dashboard verified | Plan 01 Tasks 2 + 3 |
| 3 | Verbatim href in raw source (no click-tracking) | Plan 03 Task 3 §CTA href |
| 4 | Boots with RESEND_API_KEY unset | Plan 02 Task 3 |
| 5 | RESEND_API_KEY in Railway + commented in .env.example | Plan 03 Task 1 + Plan 02 Task 1 |

Confirming commands:
- `test -f .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md`
- `grep -c 'dkim=pass' .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md` returns at least 1
- `grep -c 'spf=pass' .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md` returns at least 1
- `grep -c 'dmarc=pass' .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md` returns at least 1
- `grep -c 'https://app.vigilhub.io/auth/reset' .planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-LIVE-VERIFICATION.md` returns at least 1 (verbatim CTA href proof)
- `grep -c 'process.env\["VIGIL_APP_BASE_URL"\]' vigil-core/scripts/smoke-test-email.ts` returns 1 (VIGIL_APP_BASE_URL wiring exercised, not deferred to Phase 112)
</verification>

<success_criteria>
All 5 Phase 111 Success Criteria from ROADMAP are PASS across the three plans. EMAIL-01 is ready to be marked Complete in REQUIREMENTS.md once Plan 03's verification file is committed. Phase 112 can now import `sendPasswordResetEmail` with confidence that the transport is live AND that the VIGIL_APP_BASE_URL env plumbing has been exercised by a live send (not just hardcoded in this phase).
</success_criteria>

<output>
After completion, create `.planning/phases/111-transactional-email-infrastructure-resend-dns/111-03-SUMMARY.md` documenting:
- Resend message id from the live send (safe to log — not a secret)
- Gmail inbox placement confirmation + raw-source auth headers
- Verbatim CTA href from raw source
- Railway deploy timestamp after env var injection
- Whether VIGIL_APP_BASE_URL was honored by the smoke script (copy the "Origin (...)" line from stdout)
- Any deliverability gotchas + resolution
- Phase 111 overall verdict (all 5 SC PASS)
</output>
</output>
