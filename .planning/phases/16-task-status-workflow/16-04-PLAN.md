---
phase: 16-task-status-workflow
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - Sources/DailyBrief/PDF/PageThreeRenderer.swift
  - Sources/DailyBrief/PDF/PageOneRenderer.swift
autonomous: true
---

<objective>
Update PDF daily brief to render task and work order status visually — filled checkboxes for done, half-filled for in-progress, empty for open.

Purpose: The printed daily brief should reflect current task/work order status so the user sees progress at a glance.
Output: Status-aware checkbox rendering for tasks (Page 3) and work orders (Page 1).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-task-status-workflow/16-01-SUMMARY.md
@.planning/phases/16-task-status-workflow/16-02-SUMMARY.md

@Sources/DailyBrief/PDF/PageThreeRenderer.swift
@Sources/DailyBrief/PDF/PageOneRenderer.swift
@Sources/DailyBrief/Services/CompletionStore.swift
@Sources/JarvisCore/Models/Thought.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Page 3 task rendering with status indicators</name>
  <files>Sources/DailyBrief/PDF/PageThreeRenderer.swift</files>
  <action>
  1. In the tasks section where checkboxes are drawn for data.taskThoughts:
     - Currently draws empty checkbox squares for all tasks
     - Update to check each thought's taskStatus:
       - .open (or nil): Empty checkbox □ (current behavior)
       - .inProgress: Checkbox with a dot/dash inside ⊡ (stroke the box, then draw a small filled circle or dash in center)
       - .done: Filled checkbox ■ or checkbox with checkmark ☑ (fill the box with dark color, or draw a checkmark path inside)
     - For .done tasks, also draw the text in a lighter gray color (0.5 gray) to visually de-emphasize
  
  2. Sort tasks so they display in a useful order:
     - inProgress tasks first, then open, then done (done at bottom)
     - Within each status group, maintain createdAt descending order
  
  3. Build to verify: swift build
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>PDF tasks section shows status-aware checkboxes, done tasks visually de-emphasized, sorted by status</done>
</task>

<task type="auto">
  <name>Task 2: Update Page 1 work order rendering with status indicators</name>
  <files>Sources/DailyBrief/PDF/PageOneRenderer.swift</files>
  <action>
  1. Read PageOneRenderer.swift to understand how work orders are currently rendered.
  
  2. Currently, completed work orders are FILTERED OUT entirely (by CompletionStore before PDF generation).
     Change behavior: Include all work orders but visually indicate status.
     - This requires the generate command to pass ALL work orders (not pre-filtered)
     - Check where filtering happens (likely in DailyBrief.swift generate command) and remove the CompletionStore filter
     - Instead, pass CompletionStore status data alongside work orders so the renderer can use it
  
  3. In PageOneRenderer, for each work order:
     - Look up its status via CompletionStore (or pass statuses as parameter)
     - .open: Normal rendering (current behavior for non-completed)
     - .inProgress: Add a small "▸" or "→" indicator before the case number
     - .done: Draw with strikethrough line through the row text, lighter gray color
  
  4. Sort work orders: inProgress first, then open, then done (done at bottom)
  
  5. Build to verify: swift build
  </action>
  <verify>swift build succeeds with no errors</verify>
  <done>PDF work orders section shows all orders with status indicators, done orders de-emphasized</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds for DailyBrief target
- [ ] Page 3 tasks render with status-aware checkboxes
- [ ] Page 1 work orders render with status indicators
- [ ] Done items are visually de-emphasized in both sections
- [ ] Items are sorted by status (active first, done last)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PDF daily brief visually reflects task and work order status
  </success_criteria>

<output>
After completion, create `.planning/phases/16-task-status-workflow/16-04-SUMMARY.md`
</output>
