---
phase: 08-launch-agent
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: [LaunchAgent/com.jamesonmorrill.dailybrief.plist, Scripts/install.sh, Sources/DailyBriefMonitor/MenuBarView.swift]
autonomous: false
---

<objective>
Remove the now-redundant CLI LaunchAgent (scheduling moved into monitor), update install script to clean up old CLI agent, and verify the complete system works end-to-end.

Purpose: Consolidate to a single LaunchAgent (monitor only) and validate the full launch-at-login + scheduled-brief flow.
Output: Clean single-agent architecture, verified working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-launch-agent/08-01-SUMMARY.md
@.planning/phases/08-launch-agent/08-02-SUMMARY.md

@LaunchAgent/com.jamesonmorrill.dailybrief.plist
@LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist
@Scripts/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CLI LaunchAgent and update install script</name>
  <files>LaunchAgent/com.jamesonmorrill.dailybrief.plist, Scripts/install.sh</files>
  <action>
1. Delete `LaunchAgent/com.jamesonmorrill.dailybrief.plist` from the repo — the monitor's built-in scheduler replaces it.
2. Keep `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist` as a reference/template (the install script generates the actual plist).
3. Update `Scripts/install.sh` to:
   - Add a cleanup step that unloads and removes the old CLI LaunchAgent if it exists: `launchctl unload ~/Library/LaunchAgents/com.jamesonmorrill.dailybrief.plist 2>/dev/null` then `rm -f ~/Library/LaunchAgents/com.jamesonmorrill.dailybrief.plist`
   - Print a message: "Removed old CLI LaunchAgent (scheduling now built into monitor)"
4. Verify the install script still works end-to-end after changes.
  </action>
  <verify>The CLI plist is removed from repo. Install script includes cleanup of old agent.</verify>
  <done>Single LaunchAgent architecture — only the monitor plist remains</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Launch Agent system: install script, built-in scheduler, single LaunchAgent</what-built>
  <how-to-verify>
    1. Run: `cd ~/Desktop/Local\ AI/dailybrief && bash Scripts/install.sh`
    2. Verify: Script builds and installs without errors
    3. Check: `ls ~/.local/bin/DailyBrief ~/.local/bin/DailyBriefMonitor` — both binaries exist
    4. Check: `launchctl list | grep dailybrief` — only monitor agent is loaded (no CLI agent)
    5. Check: Menu bar icon appears with "Next brief: [time]" in the dropdown
    6. Test: Click "Run Now" — brief generates successfully
    7. Verify: After run, status updates to show completion time
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CLI LaunchAgent plist removed from repo
- [ ] Install script cleans up old CLI agent
- [ ] Only monitor LaunchAgent is active
- [ ] Human verified end-to-end flow
</verification>

<success_criteria>
- Single LaunchAgent (monitor only) — no CLI scheduling agent
- Install script handles full lifecycle including cleanup
- End-to-end verified: install, launch at login, menu bar present, Run Now works, schedule shows
</success_criteria>

<output>
After completion, create `.planning/phases/08-launch-agent/08-03-SUMMARY.md`
</output>
