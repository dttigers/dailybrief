---
phase: 63-pwa-foundation
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified: []
autonomous: false
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - PWA-04
user_setup:
  - service: vercel
    why: "PWA deployment hosting per D-02"
    env_vars: []
    dashboard_config:
      - task: "Create Vercel project from GitHub repo with Root Directory = vigil-pwa/"
        location: "Vercel Dashboard -> New Project -> Import Git Repository"
      - task: "Add custom domain app.vigilhub.io"
        location: "Vercel Dashboard -> Project Settings -> Domains"
      - task: "Configure CNAME for app.vigilhub.io pointing to cname.vercel-dns.com in DNS provider"
        location: "DNS provider (same as api.vigilhub.io)"
  - service: railway
    why: "Add CORS origin for PWA domain"
    env_vars:
      - name: CORS_ORIGINS
        source: "Railway Dashboard -> vigil-core service -> Variables. Set to: https://app.vigilhub.io (or append to existing value with comma)"
    dashboard_config: []

must_haves:
  truths:
    - "app.vigilhub.io loads in a browser and shows the auth page"
    - "Entering a valid Vigil API key authenticates and shows the dashboard shell"
    - "The PWA is installable via Add to Home Screen on mobile"
    - "The app works on mobile (375px), tablet (768px), and desktop (1024px+) without horizontal scroll"
    - "When offline, the banner 'No internet connection' is visible"
  artifacts: []
  key_links:
    - from: "app.vigilhub.io"
      to: "api.vigilhub.io"
      via: "CORS-allowed fetch with Bearer auth"
      pattern: "CORS_ORIGINS.*app.vigilhub.io"
---

<objective>
Deploy the PWA to Vercel at app.vigilhub.io and verify all four PWA requirements work in production.

Purpose: The scaffold from Plan 01 needs to be live at the production URL before Phase 64+ can build on it. This plan handles Vercel project creation, DNS, CORS configuration, and end-to-end smoke testing.
Output: Live PWA at app.vigilhub.io passing all four requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-pwa-foundation/63-CONTEXT.md
@.planning/phases/63-pwa-foundation/63-RESEARCH.md
@.planning/phases/63-pwa-foundation/63-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push code and attempt Vercel deployment via CLI</name>
  <files></files>
  <read_first>
    vigil-pwa/vercel.json
    vigil-pwa/package.json
    vigil-core/src/index.ts
  </read_first>
  <action>
1. Ensure all vigil-pwa/ files from Plan 01 are committed and pushed to origin/main so Vercel can detect them.

2. Attempt to deploy via Vercel CLI if available:
   ```
   cd vigil-pwa
   npx vercel --yes --prod
   ```
   If the CLI prompts for auth or is not set up, create a checkpoint for the user to deploy via Vercel dashboard instead.

3. If Vercel CLI deployment works, note the production URL. If not, the user will handle deployment via dashboard in the next task's checkpoint.

4. Remind the user (in the summary) that Railway CORS_ORIGINS must include https://app.vigilhub.io for API calls to work from the production domain. The user should add this env var in Railway Dashboard -> vigil-core service -> Variables.
  </action>
  <verify>
    <automated>cd /Users/jamesonmorrill/Desktop/Local\ AI/dailybrief && git log --oneline -1 -- vigil-pwa/</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 -- vigil-pwa/ shows a commit
  </acceptance_criteria>
  <done>
    vigil-pwa/ code is pushed to origin. Vercel deployment attempted or instructions provided for dashboard deployment.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PWA deployment and all four requirements in production</name>
  <files></files>
  <action>
    User verifies the deployed PWA at app.vigilhub.io against all four PWA requirements.
  </action>
  <what-built>
    Complete PWA foundation: React/Vite/TypeScript app with API key auth, responsive layout, offline indicator, and PWA manifest — deployed to Vercel.
  </what-built>
  <how-to-verify>
    **Pre-requisites (if not already done):**
    1. Vercel: Create project from GitHub repo, set Root Directory to `vigil-pwa/`, deploy
    2. Vercel: Add custom domain `app.vigilhub.io` in project settings
    3. DNS: Add CNAME `app.vigilhub.io` -> `cname.vercel-dns.com`
    4. Railway: Set `CORS_ORIGINS=https://app.vigilhub.io` env var on vigil-core service

    **Smoke test (all four PWA requirements):**

    PWA-01 — Authentication:
    1. Open https://app.vigilhub.io in Chrome
    2. Verify the auth page loads with "Sign in to Vigil" and API key input
    3. Enter an invalid key (e.g., "bad-key") and click Sign In — verify error message appears
    4. Enter your real Vigil API key and click Sign In — verify redirect to dashboard shell

    PWA-02 — Responsive:
    5. Open Chrome DevTools (F12) -> Toggle Device Toolbar
    6. Set width to 375px (iPhone SE) — verify no horizontal scroll, layout fits
    7. Set width to 768px (iPad) — verify layout is usable
    8. Set width to 1024px+ (desktop) — verify full layout

    PWA-03 — Installable:
    9. In Chrome DevTools -> Application tab -> Manifest — verify manifest loads with display:standalone
    10. Check that 192x192 and 512x512 icons are listed and loadable
    11. (Optional) On mobile: verify "Add to Home Screen" option appears; installed app launches without browser chrome

    PWA-04 — Offline indicator:
    12. In Chrome DevTools -> Network tab -> check "Offline"
    13. Verify an amber banner appears at the top saying "No internet connection"
    14. Uncheck "Offline" — verify banner disappears
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — user confirms all 4 PWA requirements pass"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms PWA-01 (auth works)
    - User confirms PWA-02 (responsive layout)
    - User confirms PWA-03 (installable)
    - User confirms PWA-04 (offline indicator)
  </acceptance_criteria>
  <done>All four PWA requirements verified in production at app.vigilhub.io</done>
  <resume-signal>Type "approved" if all 4 requirements pass, or describe which ones failed and what you observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel CDN -> User browser | Static assets served over HTTPS |
| User browser -> Railway API | CORS-controlled cross-origin requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03 | Elevation of Privilege | CORS wildcard in production | mitigate | User sets CORS_ORIGINS=https://app.vigilhub.io on Railway in this plan's checkpoint |
| T-63-05 | Denial of Service | Vercel free tier bandwidth | accept | Single user; 100GB/month is far more than needed |
</threat_model>

<verification>
1. https://app.vigilhub.io loads the auth page
2. Valid API key authenticates successfully
3. Chrome DevTools Application -> Manifest shows valid PWA manifest
4. Offline toggle in DevTools shows the offline banner
5. No horizontal scroll at 375px width
</verification>

<success_criteria>
- app.vigilhub.io is live and accessible (PWA-01)
- API key auth works against production api.vigilhub.io (PWA-01)
- PWA manifest valid with display:standalone and required icons (PWA-03)
- Responsive at 375px, 768px, 1024px+ (PWA-02)
- Offline banner appears when network is off (PWA-04)
- CORS configured for production domain (T-63-03 mitigated)
</success_criteria>

<output>
After completion, create `.planning/phases/63-pwa-foundation/63-02-SUMMARY.md`
</output>
