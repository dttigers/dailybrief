---
phase: 126
plan: 11
type: execute
wave: 5
depends_on: [02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md
autonomous: false
requirements: [AUTH-126-07]

must_haves:
  truths:
    - "Wallclock operator todo file exists at .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md"
    - "Phase 126 cannot be marked complete until operator moves the todo to /done/ and records the spend-cap value"
    - "No code change ships in this plan — pure operator action staging + structural verification"
  artifacts:
    - path: ".planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md"
      provides: "Operator runbook for setting Anthropic monthly spend cap"
      contains: "AUTH-126-07"
  key_links:
    - from: "Phase 126 closure"
      to: "Operator wallclock action"
      via: "todo file presence + manual move to /done/ after Anthropic console action"
      pattern: "anthropic-spend-cap"
---

<objective>
Verify the wallclock operator todo file exists with the correct content, and document the closing checkpoint pattern so Phase 126 cannot be marked complete without the operator confirming the Anthropic spend cap. This plan ships ZERO code.

Per memory `feedback_wallclock_checkpoint_exempt.md`: yolo mode does NOT bypass real-world physical-action checkpoints. AUTH-126-07 is exactly that class — only the operator can log into Anthropic Console and set the cap.

The todo file was planted during /gsd-discuss-phase (verified existing at `.planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md`). This plan confirms its presence + content, then surfaces the operator checkpoint.

Purpose: Close AUTH-126-07 structurally — the only way to fail this requirement is operator-action neglect, which the checker enforces via filesystem state.

Output: A checkpoint that pauses Phase 126 verification until operator confirms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md

<interfaces>
<!-- The wallclock-checkpoint deferral pattern from prior phases -->

From Phase 123 Plan 05 / Phase 124 Plan 09 (precedent — STATE.md "Recent Decisions"):
- `autonomous: false` is the executor signal that this plan contains a checkpoint
- Plan body lands all autonomous prerequisites first
- Final checkpoint task uses `type="checkpoint:human-action"` because there is literally no CLI/API path

Per memory `feedback_wallclock_checkpoint_exempt.md`:
- Mode yolo / skip_checkpoints does NOT auto-skip wallclock + post-reboot + physical-host actions
- The Anthropic Console requires browser-based human action — no CLI/API equivalent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify operator todo file exists with correct content</name>
  <files>.planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md</files>
  <read_first>
    - .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md (current content — was planted during /gsd-discuss-phase)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md §Operator-Only (line 112 reference)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-07 (lines 540-556 — operator runbook content)
  </read_first>
  <action>
The todo file ALREADY exists from /gsd-discuss-phase (verified during planning context gathering). This task ensures it has the runbook content required for Phase 126 closure.

Check current content. The file MUST contain (verify via grep, edit only if anything is missing):

1) Frontmatter with `phase: 126`, `requirement: AUTH-126-07`, `type: operator-wallclock`, `blocks: phase-complete`.

2) Body sections covering:
   - **What:** Set Anthropic Console monthly spend cap
   - **Why:** Defense-in-depth before `VIGIL_ALLOWED_EMAILS="*"` flip (AUTH-126-08)
   - **Recommended cap:** 3× expected monthly baseline (or $100/mo starter if baseline unknown)
   - **Verification steps:** dashboard cap visible, optional test by setting deliberately low + hitting an AI endpoint
   - **Blocks:** Phase 126 cannot be marked complete until this todo is moved to /done/
   - **Once done:** Move file to `.planning/todos/done/` with cap value + date recorded

If the file already has all of the above (as verified during context gathering — it does), this task is a structural confirmation only. If anything is MISSING, edit the file to add it.

Do NOT move the file to `/done/` in this plan. That is the operator's action. The file's location in `/pending/` IS the gate.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md && echo TODO_EXISTS ; grep -c "AUTH-126-07" .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md ; grep -c "Anthropic" .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md ; grep -ci "spend.*cap\|cap.*spend\|spend limit" .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md` exists
    - Contains literal `AUTH-126-07` (grep ≥ 1)
    - Contains literal `Anthropic` (grep ≥ 1)
    - Contains spend-cap language (case-insensitive — "spend cap" or "spend limit")
    - Frontmatter has `type: operator-wallclock` (or equivalent type field)
    - Phase 126 cannot complete with this file still in /pending/
  </acceptance_criteria>
  <done>Todo file content verified; structural gate in place; operator can find the runbook.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator wallclock checkpoint — set Anthropic Console monthly spend cap</name>
  <files>.planning/todos/done/2026-05-11-phase-126-anthropic-spend-cap.md (target location after operator action)</files>
  <action>Operator-only step: log into Anthropic Console, set monthly spend cap, record value, and move the pending todo file to /done/. Claude cannot perform any of these actions — there is no CLI/API surface for Anthropic Console spend limits as of 2026-05-11 (verified). Plan execution pauses here; resume only after operator confirms via the resume-signal below.</action>
  <verify>
    <automated>! test -f .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md && test -f .planning/todos/done/2026-05-11-phase-126-anthropic-spend-cap.md && echo OPERATOR_DONE</automated>
  </verify>
  <done>Todo file moved from /pending/ to /done/ with cap value recorded inline; AUTH-126-07 satisfied; Phase 126 ready for /gsd-verify-work.</done>
  <what-built>All 8 Phase 126 code requirements are complete (AUTH-126-01..06, 08 shipped; AUTH-126-04 server + PWA wired; legal pages live). AUTH-126-07 is the operator-only Anthropic Console spend cap setting — Claude cannot do this; there is no CLI/API for Anthropic Console spend limits as of 2026-05-11.</what-built>
  <how-to-verify>
1. Open https://console.anthropic.com in a browser.
2. Navigate to: Settings → Plans & Billing → Usage Limits (or "Spend Limits").
3. Set a monthly cap. Recommended value: 3× expected baseline OR $100/mo starter if baseline unknown (per CONTEXT line 114).
4. Verify alert email destination is `jamesonmorrill1@gmail.com` (operator email).
5. Take a screenshot of the configured cap (optional but useful for SUMMARY.md attachment).
6. Edit `.planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md` to record:
   - The cap value chosen
   - The date set
   - Confirmation of alert email destination
7. Move the file: `mv .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md .planning/todos/done/`
8. Resume execution.

Until step 7 is complete, Phase 126 stays in `verifying` state and AUTH-126-07 remains incomplete.
  </how-to-verify>
  <resume-signal>
Type "done" or "approved" after the todo file has been moved to .planning/todos/done/ with the cap value recorded. If you want to defer this step (e.g., not flipping VIGIL_ALLOWED_EMAILS="*" yet), type "defer" — the gate stays in /pending/ and Phase 126 stays in verifying.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Anthropic Console | External-service configuration; no in-tree code touches this |
| Filesystem (/todos/pending/) → Phase 126 closure | File presence is the gate; absence in /pending/ + presence in /done/ unlocks completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-11-01 | Repudiation | Operator marks Phase 126 complete without setting spend cap | mitigate | File in /pending/ is the gate; verifier scans /todos/pending/ and refuses phase-complete if any phase-126-* file remains. Per memory `feedback_wallclock_checkpoint_exempt.md`. |
| T-126-11-02 | Denial of Service | Anthropic API spend exhausts before operator-set cap kicks in | accept | Defense-in-depth ordering: rate-limit (Plan 05) + Turnstile (Plan 05) + per-user quota deferred (CONTEXT Deferred Ideas) → spend cap is final backstop. Bad actor would need to bypass first two gates. |
| T-126-11-03 | Information Disclosure | Spend cap value recorded in /done/ leaks operator-specific cost data | accept | Internal-only planning artifact; not committed to public repo (assuming .planning/ is private). Operator may redact specific dollar figures if planning history goes public. |
| T-126-11-04 | Tampering | Auto-script moves todo to /done/ without operator action | mitigate | This is an autonomous: false plan with type="checkpoint:human-action" — execute-phase MUST pause; yolo mode does NOT bypass per memory. Plan checker re-asserts. |
| T-126-11-05 | Spoofing | A different cap value (lower than baseline) accidentally locks legit users out | accept | Operator's responsibility per the runbook's "verify alert email" + "test by setting low" guidance. Reversible at any time via Anthropic Console. |
</threat_model>

<verification>
- `test -f .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md` → success
- `grep -c "AUTH-126-07" .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md` → ≥ 1
- After operator action: file moves to `.planning/todos/done/`. Verify via `! test -f .planning/todos/pending/2026-05-11-phase-126-anthropic-spend-cap.md` AND `test -f .planning/todos/done/2026-05-11-phase-126-anthropic-spend-cap.md`
- Verifier sweep at phase-complete: `find .planning/todos/pending -name 'phase-126*' | wc -l` must be 0 to unblock closure
</verification>

<success_criteria>
- Todo file exists with correct content in /pending/
- Operator checkpoint surfaced to user with clear runbook
- Yolo mode honored exemption: this plan does NOT auto-skip
- Phase 126 closure structurally blocked until operator moves file
</success_criteria>

<output>
After completion (operator-signal received), create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-11-SUMMARY.md` documenting:
- Operator action taken (cap value + date — operator pastes from their action)
- Todo file moved /pending/ → /done/
- AUTH-126-07 closed
- Phase 126 ready for `/gsd-verify-work 126`

If operator chose `defer`: SUMMARY notes the deferral; AUTH-126-07 stays pending; Phase 126 stays in `verifying` until follow-up.
</output>
