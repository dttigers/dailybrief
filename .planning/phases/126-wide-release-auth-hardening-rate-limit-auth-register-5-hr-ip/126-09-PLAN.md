---
phase: 126
plan: 09
type: execute
wave: 3
depends_on: [07]
files_modified:
  - vigil-pwa/src/main.tsx
  - vigil-pwa/src/components/ErrorBoundary.tsx
autonomous: true
requirements: [AUTH-126-04]

must_haves:
  truths:
    - "Sentry.init runs BEFORE ReactDOM.createRoot(...).render() so React-construction errors are captured"
    - "Sentry init no-ops when VITE_SENTRY_DSN env var is unset (local-dev parity with server-side gate)"
    - "ErrorBoundary.componentDidCatch fires BOTH existing captureException(PostHog) AND Sentry.captureException — additive"
    - "tracesSampleRate: 0 (errors only — preserve 5k/mo free tier)"
  artifacts:
    - path: "vigil-pwa/src/main.tsx"
      provides: "Side-effect Sentry init BEFORE React render"
      contains: "Sentry.init"
    - path: "vigil-pwa/src/components/ErrorBoundary.tsx"
      provides: "Existing PostHog capture preserved + Sentry sibling added in componentDidCatch"
      contains: "Sentry.captureException"
  key_links:
    - from: "vigil-pwa/src/main.tsx"
      to: "@sentry/react SDK"
      via: "Sentry.init({dsn: VITE_SENTRY_DSN, tracesSampleRate: 0})"
      pattern: "Sentry\\.init"
    - from: "ErrorBoundary.componentDidCatch"
      to: "Sentry.captureException"
      via: "additive sibling to existing PostHog captureException call"
      pattern: "Sentry\\.captureException"
---

<objective>
Wire @sentry/react into the PWA at two sites:

1) Side-effect Sentry init in `main.tsx` BEFORE `createRoot(...).render()` — DSN env-gated (mirrors PostHog convention)
2) Sibling Sentry capture in `ErrorBoundary.componentDidCatch` — existing PostHog capture preserved

Package install lives in Plan 07; this plan only consumes.

Purpose: Close the PWA half of AUTH-126-04. PostHog stays as sibling sink per CONTEXT line 138.

Output: Two files modified (main.tsx + ErrorBoundary.tsx). No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-07-SUMMARY.md

@vigil-pwa/src/main.tsx
@vigil-pwa/src/components/ErrorBoundary.tsx

<interfaces>
<!-- The existing side-effect-init convention this plan extends -->

From vigil-pwa/src/main.tsx (line ~4 — analytics init pattern):
- `import './analytics/posthog'` — side-effect import; posthog.init() runs before createRoot

From vigil-pwa/src/components/ErrorBoundary.tsx (line ~19-21 — existing capture call):
- `componentDidCatch(error: Error, _info: ErrorInfo): void { captureException(error, { boundary: 'root' }) }`

<!-- @sentry/react SDK shape (Plan 07 installed) -->

From @sentry/react (RESEARCH §2):
- `Sentry.init({dsn, environment, tracesSampleRate})` — v10 functional API; must run BEFORE component mount for full coverage
- `Sentry.captureException(error, {tags: {...}})` — fire-and-forget; safe to call even if init never ran (SDK no-ops)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Sentry.init in main.tsx BEFORE createRoot.render</name>
  <files>vigil-pwa/src/main.tsx</files>
  <read_first>
    - vigil-pwa/src/main.tsx (FULL FILE — confirm existing posthog side-effect import line, locate createRoot call)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-04 PWA diff shape (lines 437-447)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §MODIFIED main.tsx
  </read_first>
  <action>
Edit `vigil-pwa/src/main.tsx`. Two additions:

1) Add namespace import for Sentry at top of imports (alongside existing `import './analytics/posthog'` line ~4):
   - `import * as Sentry from "@sentry/react";`

2) Add the DSN-gated init block BEFORE `ReactDOM.createRoot(...).render(...)` (or whatever the React 19 entry pattern is in this file — likely `createRoot(document.getElementById('root')!).render(...)`):
   ```
   // Phase 126 (AUTH-126-04 / D-04): Sentry browser-side init. DSN-gated mirroring
   // the POSTHOG_API_KEY convention at analytics/posthog.ts. tracesSampleRate: 0 keeps
   // us under the 5k events/mo free tier (only captureException fires).
   if (import.meta.env.VITE_SENTRY_DSN) {
     Sentry.init({
       dsn: import.meta.env.VITE_SENTRY_DSN as string,
       environment: import.meta.env.MODE,
       tracesSampleRate: 0,
     });
   }
   ```

   Place this block AFTER the posthog side-effect import (order between posthog and Sentry inits does not matter — the load-bearing constraint is that BOTH precede `createRoot(...).render()`).

3) Do NOT enable `sendDefaultPii` (Sentry default is `false`; opting in would risk Bearer-token leak via HTTP breadcrumbs — RESEARCH §Security Domain).

4) Do NOT wrap `<App />` in `<Sentry.ErrorBoundary>`. The existing `vigil-pwa/src/components/ErrorBoundary.tsx` already wraps the tree and Task 2 extends it. Two boundaries would double-capture every error → blow the quota.

Confirm tsc clean: `cd vigil-pwa && npx tsc --noEmit -p tsconfig.json` exits 0.

Confirm Vite build still works: `cd vigil-pwa && npx vite build` exits 0 (no missing-module errors for @sentry/react — installed in Plan 07).
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -c "@sentry/react" src/main.tsx ; grep -c "Sentry.init" src/main.tsx ; grep -c "VITE_SENTRY_DSN" src/main.tsx ; grep -c "tracesSampleRate" src/main.tsx ; node -e 'const fs=require("fs"); const s=fs.readFileSync("src/main.tsx","utf8"); const init=s.indexOf("Sentry.init"); const root=s.indexOf("createRoot"); console.log("init<createRoot:", init>=0 && root>=0 && init<root);'</automated>
  </verify>
  <acceptance_criteria>
    - `Sentry.init` appears in main.tsx (grep count ≥ 1)
    - `import * as Sentry from "@sentry/react"` (or equivalent) present
    - `VITE_SENTRY_DSN` env-gate present
    - `tracesSampleRate: 0` literal present
    - Source position: `Sentry.init` appears BEFORE `createRoot`
    - `npx vite build` exits 0 (no import errors)
    - Existing posthog side-effect import preserved (line still present)
  </acceptance_criteria>
  <done>main.tsx wires Sentry init BEFORE createRoot; DSN-gated; Vite build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Sentry sibling capture in ErrorBoundary.componentDidCatch</name>
  <files>vigil-pwa/src/components/ErrorBoundary.tsx</files>
  <read_first>
    - vigil-pwa/src/components/ErrorBoundary.tsx (FULL FILE — confirm existing captureException(PostHog) call shape)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-04 ErrorBoundary section
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §MODIFIED ErrorBoundary.tsx
  </read_first>
  <action>
Edit `vigil-pwa/src/components/ErrorBoundary.tsx`. Two additions:

1) Add Sentry namespace import at top: `import * as Sentry from "@sentry/react";`

2) Locate the existing `componentDidCatch(error, _info)` body (line ~19-21). Currently contains: `captureException(error, { boundary: 'root' })` (PostHog).

   ADD a sibling call IMMEDIATELY AFTER the captureException line:
   - `Sentry.captureException(error, { tags: { boundary: 'root' } })`

   Use `tags:` shape not `extra:` — tags are searchable in Sentry UI and align with the routing/method tagging in the server-side onError handler.

3) DO NOT remove or modify the existing captureException(PostHog) call — PostHog stays as sibling per CONTEXT line 138.

4) DO NOT add other Sentry methods (setUser, withScope, etc.) — boundary-level errors don't have userId context at this point (user may not be authed). Keep capture minimal.

Confirm:
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `npx vite build` exits 0
- ErrorBoundary's existing tests (if any) still pass (e.g., `grep -l 'ErrorBoundary' src/**/*.test.tsx` then run those — if none exist, skip)
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -c "@sentry/react" src/components/ErrorBoundary.tsx ; grep -c "Sentry.captureException" src/components/ErrorBoundary.tsx ; grep -c "captureException(error" src/components/ErrorBoundary.tsx ; grep -c "boundary: 'root'" src/components/ErrorBoundary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBoundary.tsx imports `@sentry/react`
    - `Sentry.captureException(error` appears in componentDidCatch (grep ≥ 1)
    - Existing `captureException(error` (PostHog) ALSO still present in componentDidCatch (NOT removed)
    - Tag value `'root'` preserved in both calls (consistent boundary identifier)
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>ErrorBoundary captures via both PostHog and Sentry sinks; existing call preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React render → Sentry SaaS | Outbound error reports; DSN client-public per design; no Bearer header capture |
| ErrorBoundary catch → both sinks | Each sink fires independently; one failure does not block the other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-09-01 | Information Disclosure | Sentry DSN leak via JS bundle | accept | DSNs are public-by-design (Sentry threat model). VITE_SENTRY_DSN intentionally embeds in bundle. RESEARCH §Security Domain confirms. |
| T-126-09-02 | Information Disclosure | Bearer token leak via Sentry HTTP breadcrumbs | mitigate | Default `sendDefaultPii: false` — Sentry SDK strips headers by default. JSDoc + comment notes this. |
| T-126-09-03 | Tampering | Existing PostHog captureException removed when adding Sentry | mitigate | Acceptance criteria assert both calls present post-edit. |
| T-126-09-04 | Denial of Service | Sentry CDN/SaaS outage breaks app | accept | `Sentry.init` is fire-and-forget. SDK gracefully degrades when SaaS unreachable. |
| T-126-09-05 | Repudiation | Future code adds Sentry.ErrorBoundary wrapper double-capturing | mitigate | Plan body explicitly forbids the wrap. Future Phase 127+ may consider but tradeoff is documented. |
| T-126-09-06 | Information Disclosure | Sentry context object captures user input via React error info | accept | `_info` (ErrorInfo) is component-stack only — already public-knowledge for app structure; no user data. |
</threat_model>

<verification>
- `cd vigil-pwa && npx vite build` exits 0
- `grep -c "Sentry.init" vigil-pwa/src/main.tsx` → 1
- `grep -c "VITE_SENTRY_DSN" vigil-pwa/src/main.tsx` → ≥ 1
- `grep -c "Sentry.captureException" vigil-pwa/src/components/ErrorBoundary.tsx` → 1
- `grep -c "captureException(error" vigil-pwa/src/components/ErrorBoundary.tsx` → ≥ 2 (existing PostHog + new Sentry)
</verification>

<success_criteria>
- main.tsx wires Sentry.init DSN-gated BEFORE createRoot
- ErrorBoundary fires both PostHog + Sentry capture
- Vite build clean
- No new dep installs (Plan 07 handled @sentry/react already)
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-09-SUMMARY.md` documenting:
- main.tsx diff (2 line additions: import + init block)
- ErrorBoundary.tsx diff (2 line additions: import + Sentry call)
- Vite build success
- Confirmed: PostHog capture preserved alongside Sentry
- Anchor: "Plan 10 wires the final PWA-side AuthPage surgery (Turnstile widget + error-code rendering + legal footer)"
</output>
