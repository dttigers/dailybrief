---
phase: 126
plan: 08
type: execute
wave: 2
depends_on: [01]
files_modified:
  - vigil-pwa/src/pages/PrivacyPolicyPage.tsx
  - vigil-pwa/src/pages/TermsOfServicePage.tsx
  - vigil-pwa/src/App.tsx
autonomous: true
requirements: [AUTH-126-06]

must_haves:
  truths:
    - "Visiting /legal/privacy returns 200 with substring 'privacy'"
    - "Visiting /legal/terms returns 200 with substring 'terms'"
    - "Both routes mount OUTSIDE the isAuthenticated guard (publicly reachable)"
    - "Both pages match existing Tailwind chrome (gray-900 + min-h-screen layout)"
  artifacts:
    - path: "vigil-pwa/src/pages/PrivacyPolicyPage.tsx"
      provides: "/legal/privacy publishable content + page chrome"
      min_lines: 30
    - path: "vigil-pwa/src/pages/TermsOfServicePage.tsx"
      provides: "/legal/terms publishable content + page chrome"
      min_lines: 30
    - path: "vigil-pwa/src/App.tsx"
      provides: "Public-route registrations for /legal/privacy and /legal/terms"
      contains: "PrivacyPolicyPage"
  key_links:
    - from: "vigil-pwa/src/App.tsx"
      to: "PrivacyPolicyPage / TermsOfServicePage"
      via: "<Route path='/legal/privacy' element={<PrivacyPolicyPage/>}/> outside isAuthenticated guard"
      pattern: "/legal/privacy"
    - from: "Plan 10 AuthPage footer"
      to: "legal routes"
      via: "<Link to='/legal/privacy'> ... </Link>"
      pattern: "/legal/privacy"
---

<objective>
Ship two new react-router pages at `/legal/privacy` and `/legal/terms` per AUTH-126-06. Mount them as public siblings (outside the `isAuthenticated` guard) in App.tsx.

Purpose: Legal pages must be linkable from the signup screen footer and return 200 with publishable content. Lawyer review optional pre-launch (CONTEXT line 109).

Output: Two new page components + one App.tsx edit adding the routes. AuthPage footer link is wired in Plan 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-01-SUMMARY.md

@vigil-pwa/src/pages/ForgotPasswordPage.tsx
@vigil-pwa/src/App.tsx
@vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx
@vigil-pwa/src/pages/TermsOfServicePage.test.tsx

<interfaces>
<!-- The existing public-route pattern this plan mirrors -->

From vigil-pwa/src/App.tsx (lines ~66-76 — Phase 112 public-route siblings):
- `<Route path="/auth/forgot" element={<ForgotPasswordPage />} />`
- `<Route path="/auth/reset" element={<ResetPasswordPage />} />`
- `<Route path="/auth/verify" element={<VerifyEmailPage />} />`

From vigil-pwa/src/pages/ForgotPasswordPage.tsx (chrome layout):
- `min-h-screen bg-gray-900 text-white px-6 py-8` outer container
- `<article>` semantic wrapper
- `<h1>` with Tailwind heading classes
- Stateless component (no fetch / no useState beyond local form state)

<!-- Wave 0 tests already define minimum content shape -->

From vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx (Plan 01):
- `AUTH-126-PRIVACY-RENDERS`: rendered output must contain substring "privacy" (case-insensitive)
- `AUTH-126-PRIVACY-HEADING`: a heading element must exist

From vigil-pwa/src/pages/TermsOfServicePage.test.tsx (Plan 01):
- `AUTH-126-TERMS-RENDERS`: substring "terms" case-insensitive
- `AUTH-126-TERMS-HEADING`: heading element exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrivacyPolicyPage.tsx + TermsOfServicePage.tsx</name>
  <files>vigil-pwa/src/pages/PrivacyPolicyPage.tsx, vigil-pwa/src/pages/TermsOfServicePage.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/ForgotPasswordPage.tsx (chrome layout — copy outer container + Tailwind classes)
    - vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx (Wave 0 stub — exact substring + heading assertions)
    - vigil-pwa/src/pages/TermsOfServicePage.test.tsx (Wave 0 stub)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-06 Implementation Map (minimum acceptable v1 content)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md (legal pages — partial analog from ForgotPasswordPage)
  </read_first>
  <action>
Create two new page components. Both are stateless function components, no fetches, no useState.

**PrivacyPolicyPage.tsx structure:**

1) Top-of-file JSDoc:
   - `Phase 126 (AUTH-126-06 / D-04) — Privacy Policy page mounted at /legal/privacy.`
   - `Hand-rolled per CONTEXT line 109 (Termly hosted would add CSP+iframe complexity).`
   - `Lawyer review recommended within 30 days post-revenue.`

2) Default export named function component `PrivacyPolicyPage`.

3) Outer container: `<main className="min-h-screen bg-gray-900 text-white px-6 py-8">` (mirror ForgotPasswordPage chrome).

4) `<article>` semantic wrapper with `max-w-3xl mx-auto`.

5) `<h1 className="text-3xl font-bold mb-6">Privacy Policy</h1>` (heading element — satisfies `AUTH-126-PRIVACY-HEADING`).

6) `<p className="text-sm text-gray-400 mb-4">Last updated: 2026-05-11</p>` — current date stamp (operator updates on republish).

7) Body sections (minimum publishable v1 — operator can refine post-launch per CONTEXT line 109):
   - `<section>` with `<h2>Data We Collect</h2>` and `<p>` covering: email address (for account + auth), encrypted password hash, app usage events (PostHog + Sentry for service health), agent-events from claude-code-companion vigil-watch daemon if installed.
   - `<section>` with `<h2>How We Use Your Data</h2>` and `<p>` covering: provide the Vigil service, fix bugs (Sentry stack traces), measure product usage (PostHog), never sold/rented.
   - `<section>` with `<h2>Third-Party Services</h2>` and `<p>` covering: Anthropic API (Claude requests proxied for AI features), Cloudflare Turnstile (signup-anti-bot), Resend (transactional email), PostHog + Sentry (error/usage telemetry), Railway (hosting), Vercel (PWA hosting).
   - `<section>` with `<h2>Data Retention</h2>` and `<p>` covering: account data kept until deletion; logs purged at 30 days; Sentry events at 90 days default.
   - `<section>` with `<h2>Your Rights</h2>` and `<p>` covering: email hello@vigilhub.io to request data export or account deletion (GDPR/CCPA endpoint deferred per CONTEXT.md Deferred Ideas).
   - `<section>` with `<h2>Contact</h2>` and `<p>` containing `<a href="mailto:hello@vigilhub.io">hello@vigilhub.io</a>` (matches REG_NOT_ALLOWED ctaHref from Plan 07 — consistent contact surface).

8) Each `<h2>` uses Tailwind `text-xl font-semibold mt-6 mb-2`. Each `<p>` uses `text-gray-300 leading-relaxed`.

Substring requirement: the literal lowercase substring "privacy" must appear in rendered output ≥ 1 time. With "Privacy Policy" as the h1 + "Privacy" in body sections, this is automatic.

**TermsOfServicePage.tsx structure:**

Same chrome and section pattern. Body sections:
- Acceptance of Terms — using Vigil means you agree
- Account Eligibility — must be 13+ (or operator chooses 18+ if revenue model triggers different threshold)
- Acceptable Use — no spam, no abuse, no automated scraping
- AI Disclaimer — Claude API responses are AI-generated; not legal/medical/financial advice; use judgment
- Modifications to Service — Vigil may change features; reasonable notice for breaking changes
- Termination — operator may suspend abusive accounts
- Limitation of Liability — service provided as-is; no warranty implied
- Governing Law — Colorado, USA (operator's jurisdiction per user_timezone.md)
- Contact — hello@vigilhub.io

Substring requirement: "terms" lowercase appears in rendered output (auto-satisfied by "Terms of Service" h1).

**NO** placeholder strings like "TBD" or "Lorem ipsum". Content must be real-enough to ship and refine. RESEARCH explicitly notes operator can paste Termly-generator output here later if preferred — current scope is to ship something publishable and linkable.

Do NOT introduce new imports beyond `react` (no useState, no fetch, no router hooks — pure presentational components).
  </action>
  <verify>
    <automated>cd vigil-pwa && npx vitest run src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx 2>&1 | tail -25 ; test -f src/pages/PrivacyPolicyPage.tsx && test -f src/pages/TermsOfServicePage.tsx && echo BOTH_OK ; wc -l src/pages/PrivacyPolicyPage.tsx src/pages/TermsOfServicePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - Wave 0 vitest exits 0 for both `PrivacyPolicyPage.test.tsx` and `TermsOfServicePage.test.tsx`
    - PrivacyPolicyPage.tsx ≥ 30 lines (real content, not stub)
    - TermsOfServicePage.tsx ≥ 30 lines
    - Both pages contain an h1 element
    - PrivacyPolicyPage rendered output contains lowercase substring "privacy"
    - TermsOfServicePage rendered output contains lowercase substring "terms"
    - Both pages use `min-h-screen bg-gray-900 text-white` chrome (consistent with PWA dark surface)
  </acceptance_criteria>
  <done>Both legal pages exist with publishable content; Wave 0 tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Register /legal/privacy and /legal/terms routes in App.tsx</name>
  <files>vigil-pwa/src/App.tsx</files>
  <read_first>
    - vigil-pwa/src/App.tsx (FULL FILE — find the public-route siblings block at lines ~66-76)
    - vigil-pwa/src/pages/PrivacyPolicyPage.tsx (verify export name)
    - vigil-pwa/src/pages/TermsOfServicePage.tsx (verify export name)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §MODIFIED App.tsx (public-route sibling pattern)
  </read_first>
  <action>
Edit `vigil-pwa/src/App.tsx`. Add two route registrations as siblings of the existing `/auth/forgot|reset|verify` block.

1) Add imports at the top of file (alongside existing page imports):
   - `import PrivacyPolicyPage from './pages/PrivacyPolicyPage';`
   - `import TermsOfServicePage from './pages/TermsOfServicePage';`

2) Find the existing block (lines ~66-76 — public-route siblings comment block from Phase 112). Insert AFTER the last `/auth/*` route in that block:
   ```
   {/* Phase 126 (AUTH-126-06) — legal pages outside isAuthenticated guard.
       Publicly reachable for signup-screen footer link consumption + SEO. */}
   <Route path="/legal/privacy" element={<PrivacyPolicyPage />} />
   <Route path="/legal/terms" element={<TermsOfServicePage />} />
   ```

Routes MUST be OUTSIDE the `isAuthenticated` guard cluster. RESEARCH §R10 confirms Vercel SPA rewrite (`{ "rewrites": [{ "source": "/(.*)", "destination": "/" }] }`) already handles deep-linking — no vercel.json changes needed.

Verify with `npx vitest run src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx` (still GREEN — adding routes doesn't break the per-component test). For full integration, an `App.test.tsx` test would confirm but is not in scope here — Wave 0 covers component-level rendering.

Confirm the build still works: `cd vigil-pwa && npx tsc --noEmit -p tsconfig.json` (or `npx vite build --mode development` if tsc is misconfigured) exits 0 (no new type errors).
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -c "PrivacyPolicyPage" src/App.tsx ; grep -c "TermsOfServicePage" src/App.tsx ; grep -c '"/legal/privacy"' src/App.tsx ; grep -c '"/legal/terms"' src/App.tsx ; npx vitest run src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx imports both `PrivacyPolicyPage` AND `TermsOfServicePage` (grep count ≥ 2 each: import + JSX use)
    - App.tsx has literal paths `"/legal/privacy"` AND `"/legal/terms"` (grep count == 1 each)
    - Both Route elements positioned OUTSIDE any `isAuthenticated` conditional (must be sibling of `/auth/forgot` block)
    - Wave 0 tests stay GREEN
    - `npx tsc --noEmit` shows no new type errors
  </acceptance_criteria>
  <done>App.tsx registers both legal routes as public siblings; build clean; Wave 0 tests preserved GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /legal/* routes | Public unauthenticated content; no input crosses boundary; no PII rendered |
| Static page → user clipboard | Contact email displayed; intentional public-disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-08-01 | Information Disclosure | Page exposes operator's personal address / phone | mitigate | Contact email hello@vigilhub.io is intentional brand-domain alias; no personal PII. Verify before commit. |
| T-126-08-02 | Tampering | Privacy policy contradicts actual data handling | accept | Operator owns accuracy; Phase 127+ may add lawyer review. v1 ships honest minimum (CONTEXT line 109). |
| T-126-08-03 | Repudiation | Page edited later without dated revision | mitigate | "Last updated: 2026-05-11" line at top is the revision anchor. Operator updates on each republish. |
| T-126-08-04 | Spoofing | Phishing site clones /legal/privacy verbatim | accept | Publicly accessible content; no copyright defense applies. Domain `app.vigilhub.io` is the source-of-truth. |
| T-126-08-05 | Elevation of Privilege | Route accidentally mounted INSIDE isAuthenticated guard | mitigate | Acceptance criteria asserts routes sit alongside `/auth/forgot|reset|verify` siblings (Plan 01 review checks). |
| T-126-08-06 | Denial of Service | Static pages survive even if API + DB are down | mitigate by design | Pages are pure presentational — no fetch / no async / no useEffect. Vercel CDN serves regardless of vigil-core state. |
</threat_model>

<verification>
- `cd vigil-pwa && npx vitest run src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx` → both green
- `grep -c "/legal/privacy" vigil-pwa/src/App.tsx` → 1
- `grep -c "/legal/terms" vigil-pwa/src/App.tsx` → 1
- `wc -l vigil-pwa/src/pages/PrivacyPolicyPage.tsx vigil-pwa/src/pages/TermsOfServicePage.tsx` — both should be > 30 lines
</verification>

<success_criteria>
- Two legal pages exist + return 200 with substring "privacy" / "terms"
- Both routes mounted in App.tsx outside any auth guard
- Wave 0 PrivacyPolicyPage.test.tsx + TermsOfServicePage.test.tsx are GREEN
- AuthPage footer link consumption (Plan 10) has valid targets
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-08-SUMMARY.md` documenting:
- Lines of PrivacyPolicyPage.tsx + TermsOfServicePage.tsx
- App.tsx diff: 2 imports + 2 routes added
- Wave 0 legal-page tests GREEN
- Note: operator may want lawyer review within 30d post-revenue (CONTEXT line 109)
- Anchor: "Plan 10 AuthPage adds footer Link components targeting these routes"
</output>
