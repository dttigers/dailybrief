---
status: complete
phase: 126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip
source:
  - 126-01-SUMMARY.md
  - 126-02-SUMMARY.md
  - 126-03-SUMMARY.md
  - 126-04-SUMMARY.md
  - 126-05-SUMMARY.md
  - 126-06-SUMMARY.md
  - 126-07-SUMMARY.md
  - 126-08-SUMMARY.md
  - 126-09-SUMMARY.md
  - 126-10-SUMMARY.md
  - 126-11-SUMMARY.md
started: 2026-05-11T20:10:00.000Z
updated: 2026-05-11T20:55:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running vigil-core + vigil-pwa processes. From a clean shell:
  cd vigil-core && npm run dev (or `npx tsx src/index.ts`) — vigil-core boots
  without crashing on Sentry init, mount-order, or env-gate errors. JWT_SECRET
  warning is OK if you haven't set it; a crash on initSentry() or
  `requireVerifiedEmailWithGrace` middleware load is NOT ok.
  Then: cd vigil-pwa && npm run dev — Vite serves the PWA cleanly; opening
  http://localhost:5173 shows the AuthPage (no white screen / no Sentry init
  exception in console).
result: pass
note: |
  Surfaced a real environment-drift bug on first cold-start: Phase 125
  migration 0019_add_users_quiet_mode.sql was missing from the local dev DB.
  Login query failed with PostgreSQL 42703 "column quiet_mode does not exist".
  Not a Phase 126 regression — pre-existing dev DB drift. Resolved by running
  `cd vigil-core && npm run db:migrate`. Cold-start then booted clean. The
  smoke test caught exactly what it's designed to catch.

### 2. Turnstile widget renders in signup mode
expected: |
  On the AuthPage (default route /auth or /), toggle to the Sign Up tab. A
  Cloudflare Turnstile challenge appears above the Submit button (managed
  checkbox or invisible verification). In login mode, the Turnstile widget
  is NOT rendered.
  (If VITE_TURNSTILE_SITE_KEY is unset in vigil-pwa env, a dev-safe fallback
  div renders instead — that's expected behavior, not a bug.)
result: pass
note: |
  Initially verified via the dev-safe fallback "Captcha not configured" yellow
  notice (TurnstileWidget.tsx:48-55). Later upgraded the test by adding
  Cloudflare's always-passes test sitekey (1x00000000000000000000AA) to
  vigil-pwa/.env.local — full Turnstile widget then rendered as expected.

### 3. Signup submit disabled until Turnstile token captured
expected: |
  In Sign Up mode, fill in email + password but DO NOT complete the Turnstile
  challenge. The "Create Account" button is disabled (grayed out / not
  clickable). Complete the Turnstile challenge — button becomes enabled.
  (If using the dev fallback div, this test is N/A and can be skipped.)
result: pass

### 4. Signup with disallowed email shows specific error
expected: |
  With VIGIL_ALLOWED_EMAILS set to an explicit allowlist (NOT "*"), try to
  sign up with an email NOT in the allowlist. Expected: a specific message
  about the email not being permitted — NOT the generic "Invalid email or
  password" string that used to mask this case before Phase 126. The error
  code REG_NOT_ALLOWED maps to a clear "this email isn't allowed to sign up
  right now" or similar via resolveApiError().
result: pass
note: |
  Resolved one transient bug during this test: vigil-core dev server needed
  restart after appending TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
  to vigil-core/.env (tsx watch doesn't reload .env on append). After restart,
  Turnstile siteverify succeeded against Cloudflare's test secret and the
  request flowed through to the allowlist branch which returned the
  REG_NOT_ALLOWED-specific UX. This is the exact bug that triggered Phase 126
  (family signup error triage — generic "Invalid email or password" was
  masking a real 403 allowlist rejection).

### 5. Login with wrong password shows credentials-specific error
expected: |
  In Sign In mode, enter a valid registered email with a wrong password.
  Expected: "Invalid email or password" message (INVALID_CREDENTIALS code).
  The CTA stays on the auth page (no redirect). Distinct from any verify-email
  or session-expired UX.
result: pass

### 6. Session expired (INVALID_TOKEN_SUBJECT) routes to /auth with proper message
expected: |
  Tricky to trigger naturally — but if a request comes through with a valid
  JWT pointing to a user that no longer exists (or you fake it by deleting
  a user mid-session), the PWA shows "Session expired — please sign in again"
  with a CTA labeled "Sign in" pointing to /auth. Test this by inspecting
  the resolveApiError mapping for INVALID_TOKEN_SUBJECT, or skip if you can't
  easily trigger it in the browser.
result: skipped
reason: code-locked at test time — Plan 04 13-test regression suite asserts INVALID_TOKEN_SUBJECT emission from the defensive !user branch; Plan 07 unit tests pin the resolveApiError mapping (message="Session expired — please sign in again", ctaLabel="Sign in", ctaHref="/auth").

### 7. /legal/privacy renders without login
expected: |
  Visit http://localhost:5173/legal/privacy in an incognito/logged-out window.
  The Privacy Policy page renders with content (NOT a redirect to /auth, NOT
  a white screen, NOT a 404). Hand-rolled markup — no third-party iframe.
result: pass

### 8. /legal/terms renders without login
expected: |
  Visit http://localhost:5173/legal/terms in an incognito/logged-out window.
  The Terms of Service page renders with content. Same expectation as test 7.
result: pass

### 9. AuthPage footer has Privacy + Terms links
expected: |
  On the AuthPage (either Sign In or Sign Up mode), scroll to the bottom of
  the form. Two text links visible: "Privacy" and "Terms" (or similar
  wording). Clicking each navigates to /legal/privacy and /legal/terms
  respectively without leaving the SPA.
result: pass

### 10. Signup rate-limit fires after burst (5/hr per email, 20/hr per IP)
expected: |
  From the same IP, attempt to sign up 6 times in rapid succession with the
  same (new) email. The 6th attempt returns 429 with a specific
  "RATE_LIMITED" message + a countdown / retry-after hint (NOT generic).
  Alternative: try 21 signups with different emails from same IP within
  an hour to hit the IP cap.
  Note: this is tedious to do by hand — feel free to mark blocked if you'd
  rather verify via curl burst or skip until needed.
result: skipped
reason: structurally verified by Plan 05 dual-counter rate-limit unit tests (RATE_LIMIT_MAX_IP=20, RATE_LIMIT_MAX_EMAIL=5 verbatim pin + drift detector); operator can re-verify via curl burst at any time.

### 11. Email-verify 24h grace honored on /v1/* routes
expected: |
  Sign up a fresh user. WITHOUT clicking the verification email, you can
  navigate the app and hit /v1/* endpoints for the first 24h. After 24h
  (or by faking last_verification_sent_at to be >24h ago in the DB), the
  same /v1/* call returns 403 EMAIL_NOT_VERIFIED with a verified_after_iso
  field, and the PWA surfaces a clear "Please verify your email" message
  with a "Resend verification" CTA.
  This has a 24h wallclock component — mark blocked if you don't want to
  fake the DB row.
result: skipped
reason: 24h wallclock + DB-row fake required to trigger; grace matrix structurally locked by Plan 04's 13 regression tests including AUTH-126-VERIFY-GATE-POST-GRACE (unverified user signed up 25h ago → 403 EMAIL_NOT_VERIFIED + verified_after_iso).

### 12. Sentry captures server error + PWA error
expected: |
  Requires SENTRY_DSN (vigil-core) + VITE_SENTRY_DSN (vigil-pwa) set.
  (a) Trigger a server error: hit a /v1/* endpoint that throws (or temporarily
      add a `throw new Error('test')` in a route handler). Check Sentry
      dashboard — event appears with {route, method} context + userId.
  (b) Trigger a PWA render error: temporarily throw inside a child of
      ErrorBoundary. The boundary catches; Sentry dashboard shows the event
      with boundary: 'root' tag.
  Skip if Sentry DSN is not configured in your dev env.
result: skipped
reason: DSNs unset in dev — DSN-unset no-op path locked by Plan 03 (vigil-core sentry.test.ts) + Plan 09 (vigil-pwa captureToSentry shape test); production DSNs wired via Railway (vigil-core) + Vercel (vigil-pwa) env, exercised on real prod errors.

## Summary

total: 12
passed: 8
issues: 0
pending: 0
skipped: 4

## Gaps

[none yet]
