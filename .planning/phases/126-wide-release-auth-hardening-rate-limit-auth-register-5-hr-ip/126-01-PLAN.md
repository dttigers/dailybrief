---
phase: 126
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-core/src/lib/turnstile.test.ts
  - vigil-core/src/lib/sentry.test.ts
  - vigil-core/src/middleware/require-verified-email.test.ts
  - vigil-core/src/__tests__/mount-order.test.ts
  - vigil-pwa/src/lib/api-error-codes.test.ts
  - vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx
  - vigil-pwa/src/pages/TermsOfServicePage.test.tsx
autonomous: true
requirements: [AUTH-126-01, AUTH-126-02, AUTH-126-03, AUTH-126-04, AUTH-126-05, AUTH-126-06]

must_haves:
  truths:
    - "Every Wave 1+ task has an automated <verify> command pointing at an existing test file"
    - "Wave 0 stubs fail RED until corresponding production code lands"
    - "No production file is touched in Wave 0 — only new test files are created"
  artifacts:
    - path: "vigil-core/src/lib/turnstile.test.ts"
      provides: "stub describing siteverify URL drift + 4 behavior cases for AUTH-126-02"
      contains: "https://challenges.cloudflare.com/turnstile/v0/siteverify"
    - path: "vigil-core/src/lib/sentry.test.ts"
      provides: "stub describing DSN-unset no-op + captureToSentry shape for AUTH-126-04"
      contains: "initSentry"
    - path: "vigil-core/src/middleware/require-verified-email.test.ts"
      provides: "stub describing grace-window matrix + bypass list for AUTH-126-03"
      contains: "requireVerifiedEmailWithGrace"
    - path: "vigil-core/src/__tests__/mount-order.test.ts"
      provides: "stub asserting initSentry() and app.use mount positions in index.ts"
      contains: "initSentry"
    - path: "vigil-pwa/src/lib/api-error-codes.test.ts"
      provides: "stub for resolveApiError code-map / fallback behavior for AUTH-126-05"
      contains: "resolveApiError"
    - path: "vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx"
      provides: "stub asserting /legal/privacy renders substring 'privacy' (AUTH-126-06)"
      contains: "PrivacyPolicyPage"
    - path: "vigil-pwa/src/pages/TermsOfServicePage.test.tsx"
      provides: "stub asserting /legal/terms renders substring 'terms' (AUTH-126-06)"
      contains: "TermsOfServicePage"
  key_links:
    - from: "Wave 1 production tasks"
      to: "Wave 0 test files"
      via: "npx tsx --test path/to/<lib>.test.ts"
      pattern: "tsx --test"
---

<objective>
Land Wave 0 test scaffolds so every Wave 1+ task has a real `<automated>` verify target. All stubs are RED-by-construction — they import the production module that does not yet exist (or expects a behavior that will land in Wave 1). Drift detectors are wired against literal source strings per Phase 117 convention (mirror `vigil-core/src/routes/forgot-password.test.ts:421-446`).

Purpose: Prevent Nyquist violations downstream. Per RESEARCH §Validation Architecture, all 6 new test files are NEW Wave 0 gaps and MUST exist before code lands.

Output: Six test files (vigil-core + vigil-pwa) and one mount-order test scaffold. Zero production-file modifications in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md

@vigil-core/src/routes/forgot-password.test.ts
@vigil-core/src/routes/auth.test.ts

<interfaces>
<!-- Production modules these stubs will validate when Wave 1 lands. -->
<!-- Executor: each test imports a not-yet-existing module — that is intentional (RED). -->

From vigil-core/src/lib/turnstile.ts (Wave 1, plan 02):
```typescript
export interface TurnstileResult { ok: boolean; errorCodes: string[]; }
export async function verifyTurnstileToken(token: string, remoteIp: string | null): Promise<TurnstileResult>;
export function __setVerifyTurnstileTokenForTest(fn: typeof verifyTurnstileToken): void;
export function __resetVerifyTurnstileTokenForTest(): void;
```

From vigil-core/src/lib/sentry.ts (Wave 1, plan 03):
```typescript
export function initSentry(): void;
export function captureToSentry(userId: number | null, err: unknown, context?: Record<string, unknown>): void;
```

From vigil-core/src/middleware/require-verified-email.ts (Wave 1, plan 04):
```typescript
import type { MiddlewareHandler } from "hono";
export const requireVerifiedEmailWithGrace: MiddlewareHandler;
```

From vigil-pwa/src/lib/api-error-codes.ts (Wave 1, plan 07):
```typescript
export interface ApiErrorUx { message: string; ctaLabel?: string; ctaHref?: string; }
export const ERROR_CODE_MAP: Record<string, ApiErrorUx>;
export function resolveApiError(body: {error?: string; code?: string} | null | undefined, fallback: string): ApiErrorUx;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vigil-core Wave 0 test stubs (turnstile, sentry, middleware, mount-order)</name>
  <files>vigil-core/src/lib/turnstile.test.ts, vigil-core/src/lib/sentry.test.ts, vigil-core/src/middleware/require-verified-email.test.ts, vigil-core/src/__tests__/mount-order.test.ts</files>
  <read_first>
    - vigil-core/src/routes/forgot-password.test.ts (canonical drift-detector + behavior test shape — lines 1-50 imports + lines 421-446 drift block)
    - vigil-core/src/routes/auth.test.ts (canonical buildApp() + post() helper — lines 1-50)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §Wave 0 Gaps and §Validation Architecture
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §Wave 0 Test Files To Create
  </read_first>
  <action>
Create four NEW test files. Each MUST import the not-yet-existing production module to fail RED until Wave 1 lands.

1) `vigil-core/src/lib/turnstile.test.ts` — node:test + node:assert/strict. Top-of-file imports mirror `forgot-password.test.ts:1-30`. Test cases:
   - drift detector `AUTH-126-TURNSTILE-URL`: fs.readFileSync of `./turnstile.ts` matches literal `https://challenges.cloudflare.com/turnstile/v0/siteverify`
   - behavior 1: `verifyTurnstileToken("valid", "1.2.3.4")` with mocked fetch returning `{success:true}` → `{ok:true, errorCodes:[]}`
   - behavior 2: mocked fetch returning `{success:false, "error-codes":["invalid-input-response"]}` → `{ok:false, errorCodes:["invalid-input-response"]}`
   - behavior 3: mocked fetch throwing → helper throws (caller must convert to 503 per D-01)
   - behavior 4: missing TURNSTILE_SECRET_KEY env → helper throws synchronously
   Mock fetch via `globalThis.fetch = async (..) => ...` save/restore in `beforeEach`/`afterEach`. Until Wave 1 lands, every test will fail with `Cannot find module './turnstile.js'` — that is correct RED state.

2) `vigil-core/src/lib/sentry.test.ts` — node:test. Test cases:
   - `initSentry()` with `SENTRY_DSN` unset → no-op, no throw, `captureToSentry` afterward also no-ops
   - `initSentry()` with `SENTRY_DSN=https://fake@example.com/1` → initialized = true (verify via spying on Sentry.init or via behavior of subsequent captureToSentry)
   - `captureToSentry(123, new Error("x"), {route:"/v1/foo", method:"GET"})` after init does NOT throw
   - drift detector `AUTH-126-SENTRY-PROPNAMES`: fs.readFileSync of `./sentry.ts` contains JSDoc string mentioning `route` OR `method` (per Phase 103 denylist awareness — RESEARCH §R12)
   Save/restore `process.env.SENTRY_DSN` between tests.

3) `vigil-core/src/middleware/require-verified-email.test.ts` — node:test + Hono `app.fetch(new Request(...))` pattern from `auth.test.ts:26-44`. Test cases:
   - verified user (emailVerifiedAt non-null) on `/v1/sessions` → next() called → 200 from stub handler
   - unverified user signed up 5min ago → next() called (within 24h grace) → 200
   - unverified user signed up 25h ago → 403 with body containing `"code":"EMAIL_NOT_VERIFIED"` AND `verified_after_iso` ISO string
   - bypass: `/v1/health` returns 200 even with unverified+expired user
   - bypass: `/v1/auth/resend-verification` returns 200 even with unverified+expired user
   - drift detector `AUTH-126-VERIFY-BYPASS`: fs.readFileSync of `./require-verified-email.ts` contains BOTH literals `"/v1/health"` AND `"/v1/auth/"`
   Mock db via a stub injected with a fake `users` row. Use a minimal Hono app composition: `app.use(bearerAuthStub).use(requireVerifiedEmailWithGrace).get('/v1/sessions', ...)`.

4) `vigil-core/src/__tests__/mount-order.test.ts` — node:test, fs.readFileSync of `../index.ts`. Test cases:
   - `AUTH-126-MOUNT-SENTRY-BEFORE-HONO`: source.indexOf('initSentry()') < source.indexOf('new Hono()')
   - `AUTH-126-MOUNT-VERIFY-AFTER-BEARER`: source.indexOf('requireVerifiedEmailWithGrace') > source.indexOf('return bearerAuth(c, next)')
   - `AUTH-126-MOUNT-VERIFY-BEFORE-PROTECTED`: source.indexOf('requireVerifiedEmailWithGrace') < source.indexOf('app.route("/v1", summary')
   Until Wave 1+2 land, all three assertions return -1 < -1 → expected failure with clear "not found" message. Use `assert.ok(source.indexOf('X') < source.indexOf('Y'), 'X must precede Y')` shape so message is readable.

Naming convention: each `it()` description starts with the literal test ID (e.g., `"AUTH-126-TURNSTILE-URL: turnstile.ts declares siteverify URL"`). Mirror Phase 117's `AUTH-13-FP-CAP-IP-20` naming convention.

Do NOT touch production files in this task. Each test file's failure today is documentation for the executor of Wave 1.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/lib/turnstile.test.ts src/lib/sentry.test.ts src/middleware/require-verified-email.test.ts src/__tests__/mount-order.test.ts 2>&1 | grep -E '(fail|pass)' | head -20; test -f src/lib/turnstile.test.ts && test -f src/lib/sentry.test.ts && test -f src/middleware/require-verified-email.test.ts && test -f src/__tests__/mount-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist on disk
    - Each file imports the not-yet-existing production module (`./turnstile.js`, `./sentry.js`, `./require-verified-email.js`, `../index.js` for source-read)
    - Each test ID is in `it()` description as `AUTH-126-*` literal
    - Test command exits non-zero (RED) with at least one "Cannot find module" or "expected ... to match" error per file — this is the intended state
    - grep -c "AUTH-126-" src/lib/turnstile.test.ts >= 1
    - grep -c "AUTH-126-" src/__tests__/mount-order.test.ts >= 3
  </acceptance_criteria>
  <done>Four test files exist, contain AUTH-126-* test IDs, and fail with module-not-found errors documenting what Wave 1 must build.</done>
</task>

<task type="auto">
  <name>Task 2: Create vigil-pwa Wave 0 test stubs (api-error-codes, PrivacyPolicyPage, TermsOfServicePage)</name>
  <files>vigil-pwa/src/lib/api-error-codes.test.ts, vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx, vigil-pwa/src/pages/TermsOfServicePage.test.tsx</files>
  <read_first>
    - vigil-pwa/src/api/client.test.ts (canonical vitest unit-test shape if present; otherwise any *.test.ts file under vigil-pwa/src/ for imports + assertion style)
    - vigil-pwa/src/api/client.ts (lines 905-988 — ErrorClass + classifyFetchError analog per PATTERNS §api-error-codes)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-05 Implementation Map
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §api-error-codes
  </read_first>
  <action>
Create three NEW vitest test files. Each imports the not-yet-existing production module/page to fail RED.

1) `vigil-pwa/src/lib/api-error-codes.test.ts` — vitest. Import `resolveApiError` and `ERROR_CODE_MAP` from `./api-error-codes`. Test cases:
   - `AUTH-126-CODE-MAP-CAPTCHA`: `resolveApiError({error:'x', code:'CAPTCHA_FAILED'}, 'fallback').message` equals the mapped `CAPTCHA_FAILED` message (substring 'captcha' case-insensitive is sufficient — exact copy is locked in Wave 1)
   - `AUTH-126-CODE-MAP-RATE-LIMITED`: same shape for `RATE_LIMITED` (substring 'too many' case-insensitive)
   - `AUTH-126-CODE-MAP-REG-NOT-ALLOWED`: code 'REG_NOT_ALLOWED' returns mapped message + ctaLabel + ctaHref (assert all three fields present)
   - `AUTH-126-CODE-MAP-UNKNOWN-FALLS-BACK-RAW`: `resolveApiError({error:'raw error', code:'UNKNOWN_CODE'}, 'fb').message === 'raw error'` (unknown code → raw error string)
   - `AUTH-126-CODE-MAP-EMPTY-FALLS-BACK-DEFAULT`: `resolveApiError({}, 'fb').message === 'fb'` AND `resolveApiError(null, 'fb').message === 'fb'`
   - `AUTH-126-CODE-MAP-LOCKED-ENUM`: assert ERROR_CODE_MAP has all 9 LOCKED keys (per D-04): CAPTCHA_FAILED, RATE_LIMITED, REG_NOT_ALLOWED, INVALID_EMAIL_FORMAT, PASSWORD_TOO_SHORT, PASSWORD_TOO_LONG, EMAIL_TAKEN, EMAIL_NOT_VERIFIED, INVALID_CREDENTIALS

2) `vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx` — vitest + @testing-library/react + MemoryRouter. Import PrivacyPolicyPage from `./PrivacyPolicyPage`. Test cases:
   - `AUTH-126-PRIVACY-RENDERS`: render `<MemoryRouter><PrivacyPolicyPage/></MemoryRouter>`, assert document body contains case-insensitive substring "privacy"
   - `AUTH-126-PRIVACY-HEADING`: getByRole('heading') exists (h1 or h2 present)

3) `vigil-pwa/src/pages/TermsOfServicePage.test.tsx` — same shape:
   - `AUTH-126-TERMS-RENDERS`: substring "terms" case-insensitive in rendered output
   - `AUTH-126-TERMS-HEADING`: heading element exists

Use vitest's `describe`/`it`/`expect` syntax. Imports: `import { describe, it, expect } from 'vitest'`; `import { render, screen } from '@testing-library/react'`; `import { MemoryRouter } from 'react-router-dom'`.

Do NOT create production files (`api-error-codes.ts`, `PrivacyPolicyPage.tsx`, `TermsOfServicePage.tsx`) in this plan — those land in plans 07 and 10.
  </action>
  <verify>
    <automated>cd vigil-pwa && npx vitest run src/lib/api-error-codes.test.ts src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx 2>&1 | tail -30; test -f src/lib/api-error-codes.test.ts && test -f src/pages/PrivacyPolicyPage.test.tsx && test -f src/pages/TermsOfServicePage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist on disk
    - Each file uses `import { describe, it, expect } from 'vitest'`
    - Page tests use `MemoryRouter` for routing context
    - vitest exits non-zero (RED) with module-not-found for `./api-error-codes`, `./PrivacyPolicyPage`, `./TermsOfServicePage`
    - grep -c "AUTH-126-CODE-MAP-LOCKED-ENUM" src/lib/api-error-codes.test.ts == 1
    - grep -c "AUTH-126-PRIVACY-" src/pages/PrivacyPolicyPage.test.tsx >= 2
  </acceptance_criteria>
  <done>Three PWA test files exist, scaffold all locked AUTH-126-05/06 behaviors, and fail RED with clear module-not-found documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 → Wave 1 | Test files commit RED; production code must satisfy them. Tampering = removing tests or weakening assertions. |
| Test files → filesystem | Drift detectors read source files via fs.readFileSync — boundary is read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01-01 | Tampering | Wave 0 test files | mitigate | Each test ID is unique and `AUTH-126-*` prefixed; checker can grep for missing IDs after Wave 1 lands. Plan 05 + Plan 07 explicitly re-verify these stay present. |
| T-126-01-02 | Repudiation | "Tests fail because code isn't written yet" excuse for shipping without coverage | mitigate | Each failing stub names the missing module/literal. Wave 1 cannot claim done without matching test green. |
| T-126-01-03 | Information Disclosure | Test file imports leaking sensitive constants | accept | No constants in Wave 0 are sensitive — only public siteverify URL + LOCKED enum names. |
</threat_model>

<verification>
- `cd vigil-core && npx tsx --test src/lib/turnstile.test.ts src/lib/sentry.test.ts src/middleware/require-verified-email.test.ts src/__tests__/mount-order.test.ts` — expected: exit non-zero with module-not-found / source-not-found errors
- `cd vigil-pwa && npx vitest run src/lib/api-error-codes.test.ts src/pages/PrivacyPolicyPage.test.tsx src/pages/TermsOfServicePage.test.tsx` — expected: exit non-zero with module-not-found
- `grep -rE 'AUTH-126-' vigil-core/src/lib/turnstile.test.ts vigil-core/src/lib/sentry.test.ts vigil-core/src/middleware/require-verified-email.test.ts vigil-core/src/__tests__/mount-order.test.ts vigil-pwa/src/lib/api-error-codes.test.ts vigil-pwa/src/pages/PrivacyPolicyPage.test.tsx vigil-pwa/src/pages/TermsOfServicePage.test.tsx | wc -l` — should be ≥ 20 (every test ID covered)
</verification>

<success_criteria>
- All seven test files exist
- Every test file fails RED (production module not yet created) — this is intentional
- AUTH-126-* test IDs are searchable by grep across the test corpus
- No production files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-01-SUMMARY.md` describing:
- Which test files were created
- Confirmed RED state (paste failing test output)
- List of AUTH-126-* test IDs registered
- Anchor: "Wave 1 must turn these GREEN"
</output>
