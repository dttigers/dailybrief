---
phase: 126
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - vigil-core/src/middleware/require-verified-email.ts
autonomous: true
requirements: [AUTH-126-03]

must_haves:
  truths:
    - "Verified user (emailVerifiedAt non-null) passes the gate regardless of createdAt age"
    - "Unverified user signed up within 24h passes (grace window)"
    - "Unverified user signed up >24h ago returns 403 EMAIL_NOT_VERIFIED with verified_after_iso"
    - "Phase 113 seed users (backfilled emailVerifiedAt) always pass — no regression"
    - "/v1/health and /v1/auth/* are bypassed unconditionally (defense in depth with index.ts dispatcher)"
    - "Grace anchor is users.createdAt (NOT passwordChangedAt) — R5"
  artifacts:
    - path: "vigil-core/src/middleware/require-verified-email.ts"
      provides: "requireVerifiedEmailWithGrace MiddlewareHandler + isBypass helper"
      exports: ["requireVerifiedEmailWithGrace"]
      contains: "GRACE_WINDOW_MS"
  key_links:
    - from: "Plan 06 (index.ts)"
      to: "requireVerifiedEmailWithGrace"
      via: "app.use('/v1/*', requireVerifiedEmailWithGrace) mounted between bearerAuth dispatcher and protected routes"
      pattern: "requireVerifiedEmailWithGrace"
    - from: "requireVerifiedEmailWithGrace"
      to: "users table"
      via: "drizzle db.select({emailVerifiedAt, createdAt}).from(users).where(eq(users.id, userId))"
      pattern: "users\\.emailVerifiedAt"
---

<objective>
Implement the email-verification grace-window middleware at `vigil-core/src/middleware/require-verified-email.ts` per D-02. Soft-to-strict transition: 24h grace from `users.createdAt`, then 403 EMAIL_NOT_VERIFIED with `verified_after_iso` in body.

Purpose: Close AUTH-126-03 — the column exists (since Phase 113 migration 0017) but is never read for gating. After Plan 06 mounts this middleware in `index.ts` between bearerAuth dispatcher and protected routes, `/v1/*` becomes gated.

Output: One new middleware file with `requireVerifiedEmailWithGrace` export + `isBypass` internal helper. No index.ts edits in this plan (that's Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-01-SUMMARY.md

@vigil-core/src/middleware/auth.ts
@vigil-core/src/db/schema.ts
@vigil-core/src/middleware/require-verified-email.test.ts

<interfaces>
<!-- The canonical Hono middleware-with-DB-read shape this module mirrors -->

From vigil-core/src/middleware/auth.ts (lines 1-6 — imports + types):
- `import type { MiddlewareHandler } from "hono";`
- `import { eq } from "drizzle-orm";`
- `import { db } from "../db/connection.js";`
- `import { users } from "../db/schema.js";`

From vigil-core/src/middleware/auth.ts (lines 115-145 — DB read + decision pattern, paraphrased):
- Guard `!db` returns 503 "Database unavailable"
- Drizzle .select(...).from(users).where(eq(users.id, userId)).limit(1) → destructure [user]
- `!user` returns 401
- Otherwise compute decision from the selected columns

From vigil-core/src/db/schema.ts:
- users.emailVerifiedAt: nullable timestamp (line ~52)
- users.createdAt: notNull timestamp (line ~33)
- users.passwordChangedAt: notNull timestamp (line ~44) — DO NOT use as grace anchor (R5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement requireVerifiedEmailWithGrace middleware</name>
  <files>vigil-core/src/middleware/require-verified-email.ts</files>
  <read_first>
    - vigil-core/src/middleware/auth.ts (canonical Hono middleware shape — full file, especially lines 1-6 imports, 38-153 bearerAuth body, 115-145 DB-read decision)
    - vigil-core/src/db/schema.ts (confirm columns emailVerifiedAt / createdAt / passwordChangedAt — DO NOT use passwordChangedAt per R5)
    - vigil-core/src/middleware/require-verified-email.test.ts (Wave 0 stub — defines required test contract)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-03 Implementation Map + §Code Examples (require-verified-email skeleton)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §require-verified-email.ts
  </read_first>
  <action>
Create `vigil-core/src/middleware/require-verified-email.ts` per RESEARCH §AUTH-126-03 and §Code Examples lines 923-953.

Module-header JSDoc MUST cover:
- Purpose (AUTH-126-03 / D-02): 24h grace from signup, then strict 403
- Mount-order contract: mounted AFTER bearerAuth dispatcher (so `c.get("userId")` is populated) and BEFORE first protected `/v1/*` route — Plan 06 enforces
- Why `createdAt` not `passwordChangedAt` (R5): claim-flow users would otherwise re-arm grace on password change
- Bypass list semantics (defense in depth with dispatcher)

Implementation per PATTERNS §require-verified-email.ts:

1) Imports mirror middleware/auth.ts:1-6 — type-only `MiddlewareHandler` from `hono`, `eq` from `drizzle-orm`, `db` from `../db/connection.js`, `users` from `../db/schema.js`.

2) Constant: `const GRACE_WINDOW_MS = 24 * 60 * 60 * 1000;` — literal verbatim (drift-detector locks this exact expression).

3) Function `isBypass(path: string): boolean` — body MUST contain BOTH literals `"/v1/health"` AND `"/v1/auth/"` exactly. Mirror shape from RESEARCH §AUTH-126-03:
   - First line: if path === "/v1/health" return true
   - Second line: if path.startsWith("/v1/auth/") return true
   - Final: return false

4) `export const requireVerifiedEmailWithGrace: MiddlewareHandler = async (c, next) => { ... }` body:
   - First line: bypass shortcut — `if (isBypass(c.req.path)) return next();`
   - Read `userId = c.get("userId")` — typed as `number` since bearerAuth ran first (mount-order contract)
   - If `!db`: return `c.json({ error: "Database unavailable" }, 503)` — mirror middleware/auth.ts:115
   - Query users table: `db.select({ emailVerifiedAt: users.emailVerifiedAt, createdAt: users.createdAt }).from(users).where(eq(users.id, userId)).limit(1)` — destructure `[user]`
   - If `!user`: return `c.json({ error: "Invalid token subject", code: "INVALID_CREDENTIALS" }, 401)` — defensive (bearerAuth should have rejected, but defense in depth)
   - If `user.emailVerifiedAt !== null`: return `next()` (verified path — pass)
   - Compute `const verifiedAfter = user.createdAt.getTime() + GRACE_WINDOW_MS;`
   - If `Date.now() < verifiedAfter`: return `next()` (within grace — pass)
   - Else: return `c.json({ error: "Verify your email to continue", code: "EMAIL_NOT_VERIFIED", verified_after_iso: new Date(verifiedAfter).toISOString() }, 403)`

Naming + behavior NOTES:
- `emailVerifiedAt: Date | null` per schema — null-check uses `!== null` NOT truthiness (because `new Date(0)` would be truthy in a falsy guard; avoid edge case even if backfill never produces 0)
- `createdAt.getTime()` returns ms-since-epoch (Date is `notNull` per schema)
- Response keys are EXACTLY `error`, `code`, `verified_after_iso` (snake_case per RESEARCH AUTH-126-05 + PWA api-error-codes.ts contract)

R5 GUARD: do NOT import `passwordChangedAt`. The select clause must NOT include it. Plan 06 mount-order test will assert `passwordChangedAt` literal does NOT appear in this file.
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/middleware/require-verified-email.test.ts 2>&1 | tail -30 ; grep -c 'GRACE_WINDOW_MS' src/middleware/require-verified-email.ts ; grep -c '"/v1/health"' src/middleware/require-verified-email.ts ; grep -c '"/v1/auth/"' src/middleware/require-verified-email.ts ; ! grep -q 'passwordChangedAt' src/middleware/require-verified-email.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `vigil-core/src/middleware/require-verified-email.ts`
    - Exports `requireVerifiedEmailWithGrace` (named export, MiddlewareHandler type)
    - Contains literal `GRACE_WINDOW_MS = 24 * 60 * 60 * 1000`
    - Contains BOTH bypass literals `"/v1/health"` AND `"/v1/auth/"`
    - Does NOT contain `passwordChangedAt` anywhere (R5 lock)
    - Wave 0 test `src/middleware/require-verified-email.test.ts` exits 0 (all tests green)
    - Response uses `code: "EMAIL_NOT_VERIFIED"` and `verified_after_iso` keys exactly
  </acceptance_criteria>
  <done>require-verified-email.ts ships the middleware with grace window + bypass list; Wave 0 test is GREEN; R5 guard holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bearerAuth → requireVerifiedEmailWithGrace | userId guaranteed present in context; trust boundary is INSIDE the auth chain |
| requireVerifiedEmailWithGrace → users table | Read-only DB query; never writes; userId is the only filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-04-01 | Spoofing | Forged userId in c.get() | mitigate | bearerAuth has already validated JWT signature + iat-gate before this middleware runs. Trust boundary respected by mount-order (Plan 06 enforces). |
| T-126-04-02 | Tampering | JWT manipulation to bypass verification | mitigate | bearerAuth (existing) handles signature + iat. This middleware is a SECOND gate on top — claim is "even if bearerAuth passes, if email unverified after 24h, reject". |
| T-126-04-03 | Information Disclosure | DB error leaking schema info via 503 | mitigate | `if (!db) return 503 "Database unavailable"` — generic message; no error details exposed. Mirror of middleware/auth.ts pattern. |
| T-126-04-04 | Denial of Service | DB query on every /v1/* request | accept | Single SELECT by primary key; PostgreSQL handles thousands/sec. No N+1. Consider Phase 127+ caching if metrics show load. |
| T-126-04-05 | Elevation of Privilege | Bypass via path crafting (e.g., "/v1/health/admin") | mitigate | Bypass uses `===` for exact "/v1/health" match (NOT startsWith); only `/v1/auth/` uses startsWith and that prefix is owned by auth.ts route mounts. |
| T-126-04-06 | Repudiation | Operator account hits 403 unexpectedly | accept | Phase 113 migration 0017 backfilled `emailVerifiedAt = created_at` for all seed users (RESEARCH §AUTH-126-03 schema-regression-risk-zero clause). Operator's account passes. |
</threat_model>

<verification>
- `cd vigil-core && npx tsx --test src/middleware/require-verified-email.test.ts` → all tests green (verified user pass, in-grace pass, expired 403, /v1/health bypass, /v1/auth/* bypass, drift detector for bypass literals)
- `grep -c "GRACE_WINDOW_MS" vigil-core/src/middleware/require-verified-email.ts` → ≥ 2 (declaration + use)
- `grep -c "passwordChangedAt" vigil-core/src/middleware/require-verified-email.ts` → 0 (R5 guard)
- `grep -c '"EMAIL_NOT_VERIFIED"' vigil-core/src/middleware/require-verified-email.ts` → 1
</verification>

<success_criteria>
- require-verified-email.ts exists with `requireVerifiedEmailWithGrace` export
- Wave 0 require-verified-email.test.ts transitions from RED to GREEN
- Bypass list contains both `/v1/health` AND `/v1/auth/` literals
- Zero references to `passwordChangedAt` (R5 lock)
- Response uses `code: "EMAIL_NOT_VERIFIED"` and `verified_after_iso` body keys
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-04-SUMMARY.md` documenting:
- Lines of require-verified-email.ts
- Wave 0 require-verified-email.test.ts GREEN count
- Confirmed: createdAt used (not passwordChangedAt) per R5
- Anchor: "Plan 06 mounts this middleware in index.ts between bearerAuth dispatcher and metricsMiddleware"
</output>
