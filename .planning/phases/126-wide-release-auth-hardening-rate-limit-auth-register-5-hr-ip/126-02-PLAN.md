---
phase: 126
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - vigil-core/src/lib/turnstile.ts
autonomous: true
requirements: [AUTH-126-02]

must_haves:
  truths:
    - "verifyTurnstileToken POSTs to the Cloudflare siteverify endpoint with secret + response (+ optional remoteip)"
    - "Network errors / non-2xx propagate as thrown errors so callers convert to 503 (no fail-open per D-01)"
    - "Missing TURNSTILE_SECRET_KEY env throws synchronously — caller surfaces 503"
    - "DI seam pair (__set / __reset) is exported at module scope per Phase 113 convention"
    - "JSDoc explicitly documents seam scope: helper-unit tests only; route-level integration tests use auth.ts's separately-named seam"
  artifacts:
    - path: "vigil-core/src/lib/turnstile.ts"
      provides: "verifyTurnstileToken helper + DI seam + siteverify URL literal"
      contains: "https://challenges.cloudflare.com/turnstile/v0/siteverify"
      exports: ["verifyTurnstileToken", "__setVerifyTurnstileTokenForTest", "__resetVerifyTurnstileTokenForTest"]
  key_links:
    - from: "vigil-core/src/lib/turnstile.ts"
      to: "Cloudflare siteverify endpoint"
      via: "global fetch + AbortController(5s)"
      pattern: "challenges\\.cloudflare\\.com/turnstile/v0/siteverify"
    - from: "Plan 05 (auth.ts) call site"
      to: "verifyTurnstileToken"
      via: "DI-seam indirection via auth.ts's __setRegisterTurnstileFnForTest (DISTINCT from this module's seam)"
      pattern: "verifyTurnstileTokenFn\\("
---

<objective>
Implement the Cloudflare Turnstile server-side verification helper per D-01. Helper lives at `vigil-core/src/lib/turnstile.ts`. No fail-open: network errors and `success: false` are surfaced upward so the route handler can map to 400 CAPTCHA_FAILED or 503.

Purpose: Ship the load-bearing primitive that AUTH-126-02 depends on. Decouples Cloudflare call from auth.ts so it can be DI-stubbed in tests (Phase 113 seam pattern, mirrors `auth.ts:27-39`).

Output: One new helper file with three exports and zero new npm dependencies (Node 18+ native fetch covers it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-01-SUMMARY.md

@vigil-core/src/lib/quiet-mode-suppression.ts
@vigil-core/src/routes/auth.ts
@vigil-core/src/lib/turnstile.test.ts

<interfaces>
<!-- Existing DI-seam pattern from vigil-core/src/routes/auth.ts:27-39 — mirror this shape -->

From vigil-core/src/routes/auth.ts (lines 27-39 — DI seam convention):
```typescript
let sendEmailVerificationEmailFn = realSendEmailVerificationEmail;
export function __setSendEmailVerificationEmailForTest(fn: typeof realSendEmailVerificationEmail): void {
  sendEmailVerificationEmailFn = fn;
}
export function __resetSendEmailVerificationEmailForTest(): void {
  sendEmailVerificationEmailFn = realSendEmailVerificationEmail;
}
```

From vigil-core/src/lib/quiet-mode-suppression.ts (module header + export style — mirror for turnstile.ts header):
```typescript
/**
 * <module-header JSDoc block describing purpose, ordering, contract>
 */
```

Two-seam coordination (Plan 05 disambiguation):
- THIS module's seam (`__setVerifyTurnstileTokenForTest`) is for UNIT TESTS of this helper.
- Plan 05's auth.ts adds a SEPARATELY NAMED seam (`__setRegisterTurnstileFnForTest`) for route-level integration tests, because route tests bypass this helper entirely via the auth.ts-level indirection.
- Identically-named seams would be a maintenance footgun; the JSDoc documents the boundary explicitly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement verifyTurnstileToken helper in vigil-core/src/lib/turnstile.ts</name>
  <files>vigil-core/src/lib/turnstile.ts</files>
  <read_first>
    - vigil-core/src/lib/quiet-mode-suppression.ts (lib-module header convention — lines 1-50)
    - vigil-core/src/routes/auth.ts (DI-seam pattern at lines 27-39)
    - vigil-core/src/lib/turnstile.test.ts (Wave 0 test file — defines required shape)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-02 Implementation Map + §Code Examples (turnstile.ts skeleton)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §turnstile.ts
  </read_first>
  <action>
Create `vigil-core/src/lib/turnstile.ts` per RESEARCH §AUTH-126-02 and §Code Examples lines 846-876. Module shape:

1) Top-of-file JSDoc header describing the module purpose, failure-mode policy (success:false → caller 400, network/timeout → caller 503 no fail-open), and the DI seam contract.

   JSDoc MUST include this seam-scope note verbatim (resolves the two-seam ambiguity flagged at checker review):
   ```
   * DI seam scope: __setVerifyTurnstileTokenForTest / __resetVerifyTurnstileTokenForTest below
   *   are for UNIT TESTS of THIS helper only (turnstile.test.ts). Route-level integration
   *   tests of /auth/register MUST use auth.ts's separately-named seam
   *   __setRegisterTurnstileFnForTest (Plan 05), which bypasses this helper entirely via
   *   the auth.ts-level indirection. Do NOT rename the seams to match — distinct names
   *   prevent future maintainers from accidentally double-stubbing.
   ```

   Mirror header style from `quiet-mode-suppression.ts:1-17` otherwise.

2) Module-scope constant `const SITEVERIFY_URL = "https://challenges.cloudflare.com/turnstile/v0/siteverify"` (literal verbatim — this string is the drift-detector anchor `AUTH-126-TURNSTILE-URL`).

3) Exported interface `TurnstileResult { ok: boolean; errorCodes: string[] }`.

4) Real implementation `async function realVerifyTurnstileToken(token, remoteIp): Promise<TurnstileResult>`:
   - Read `process.env["TURNSTILE_SECRET_KEY"]` — if absent, throw `new Error("TURNSTILE_SECRET_KEY not set")` synchronously
   - Build `AbortController` with 5000ms timeout
   - POST to SITEVERIFY_URL with `Content-Type: application/json`, body `JSON.stringify({secret, response: token, ...(remoteIp ? {remoteip: remoteIp} : {})})`
   - If `!res.ok` → return `{ok: false, errorCodes: [`http-${res.status}`]}`
   - Parse JSON as `{success: boolean; "error-codes"?: string[]}` — note the HYPHENATED key per R4 (do NOT camelCase)
   - Return `{ok: data.success === true, errorCodes: data["error-codes"] ?? []}`
   - Wrap fetch in try/finally to clearTimeout. Errors propagate (do NOT catch) so callers can map to 503.

5) DI seam at module scope (mirror auth.ts:27-39 verbatim shape):
   ```
   let verifyTurnstileTokenFn = realVerifyTurnstileToken;
   export const verifyTurnstileToken: typeof realVerifyTurnstileToken = (...args) => verifyTurnstileTokenFn(...args);
   export function __setVerifyTurnstileTokenForTest(fn: typeof realVerifyTurnstileToken): void { verifyTurnstileTokenFn = fn; }
   export function __resetVerifyTurnstileTokenForTest(): void { verifyTurnstileTokenFn = realVerifyTurnstileToken; }
   ```
   Pattern note: tests injecting via `__setVerifyTurnstileTokenForTest` MUST observe the patched fn through the exported `verifyTurnstileToken`, not through `realVerifyTurnstileToken` directly. Match Phase 113 seam semantics.

Do NOT add new dependencies. Node 18+ native `fetch` and `AbortController` are sufficient (existing vigil-core targets Node 18+ per `package.json` and existing usage in `services/email-service.ts`).

JSDoc on exported helper MUST mention: failure-mode policy, DI seam pair name, two-seam-coordination boundary (see header note above), and Phase 103 property-name awareness (do not include `body`/`content`/`message` in error context — R12).
  </action>
  <verify>
    <automated>cd vigil-core && npx tsx --test src/lib/turnstile.test.ts 2>&1 | tail -20; grep -c "https://challenges.cloudflare.com/turnstile/v0/siteverify" src/lib/turnstile.ts; grep -c "__setVerifyTurnstileTokenForTest" src/lib/turnstile.ts; grep -c "__resetVerifyTurnstileTokenForTest" src/lib/turnstile.ts; grep -c "__setRegisterTurnstileFnForTest" src/lib/turnstile.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-core/src/lib/turnstile.ts` exists
    - Literal `https://challenges.cloudflare.com/turnstile/v0/siteverify` appears verbatim (grep count == 1)
    - Both DI seam exports exist: `__setVerifyTurnstileTokenForTest` and `__resetVerifyTurnstileTokenForTest`
    - JSDoc references `__setRegisterTurnstileFnForTest` (Plan 05's auth.ts seam) at least once to document the two-seam boundary — grep count >= 1
    - Wave 0 test `vigil-core/src/lib/turnstile.test.ts` exits 0 (all tests green)
    - `npx tsc --noEmit` (if attempted in this dir) shows no errors against the new file
    - No new entry in vigil-core/package.json `dependencies`
    - Hyphenated key access `data["error-codes"]` appears (NOT `data.errorCodes`)
  </acceptance_criteria>
  <done>turnstile.ts ships the verify helper + DI seam + literal URL anchor + JSDoc disambiguating the two-seam boundary; Wave 0 turnstile.test.ts is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/PWA → /auth/register (HTTP) | Untrusted Turnstile token enters server here; verified before allowlist check |
| vigil-core → Cloudflare siteverify | Outbound HTTPS POST with secret env-only; response shape trusted only if `success === true` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-02-01 | Spoofing | Client-submitted `success:true` injection | mitigate | Helper never reads client-side success — always re-verifies via Cloudflare siteverify with server-only secret. Token is single-use; Cloudflare rejects replays. |
| T-126-02-02 | Tampering | Token replay across users | mitigate | Cloudflare enforces single-use semantics + hostname + challenge_ts on siteverify response. Out-of-tree mitigation per RESEARCH §Security Domain. |
| T-126-02-03 | Information Disclosure | TURNSTILE_SECRET_KEY in stack traces / logs | mitigate | Secret only flows into fetch body; never logged. JSDoc + Phase 103 BLOCKED_PROPERTY_NAMES guidance reminds future authors not to include token/secret in Sentry context. |
| T-126-02-04 | Denial of Service | Cloudflare siteverify outage | mitigate (fail-closed) | 5s AbortController → throw → caller returns 503 (NOT 200). Per D-01 explicit no-fail-open policy. |
| T-126-02-05 | Repudiation | Helper silently disabled at call site | mitigate | Plan 05 drift detector `AUTH-126-TURNSTILE-CALLSITE` asserts `verifyTurnstileToken(` appears inside auth.ts /auth/register body. Plan 01's `AUTH-126-TURNSTILE-URL` drift detector locks the endpoint literal. |
| T-126-02-06 | Tampering | Future maintainer renames this module's seam to match auth.ts's, then accidentally double-stubs in tests | mitigate | JSDoc names both seams explicitly + states they are intentionally distinct. Plan 05 test uses auth.ts-side seam exclusively. |
</threat_model>

<verification>
- `cd vigil-core && npx tsx --test src/lib/turnstile.test.ts` → all tests green
- `cd vigil-core && npx tsc --noEmit -p tsconfig.json` → no new type errors attributable to turnstile.ts
- `grep -c "https://challenges.cloudflare.com/turnstile/v0/siteverify" vigil-core/src/lib/turnstile.ts` → 1
- `grep -v '^[[:space:]]*\\(\\*\\|//\\|/\\*\\)' vigil-core/src/lib/turnstile.ts | grep -c "verifyTurnstileTokenFn"` → ≥ 2 (assignment + getter)
- `grep -c "__setRegisterTurnstileFnForTest" vigil-core/src/lib/turnstile.ts` → ≥ 1 (JSDoc cross-reference to Plan 05's distinct seam)
</verification>

<success_criteria>
- turnstile.ts exists with verifyTurnstileToken + DI seam pair exported
- Wave 0 turnstile.test.ts transitions from RED to GREEN
- No new npm dependency added to vigil-core
- Helper throws on missing TURNSTILE_SECRET_KEY and on fetch failure (fail-closed)
- JSDoc disambiguates the two-seam boundary (this module's seam vs auth.ts's seam)
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-02-SUMMARY.md` documenting:
- Lines of turnstile.ts (should be ~60-90)
- Wave 0 turnstile.test.ts test count GREEN
- Decision: native fetch vs library (cite RESEARCH §1)
- Anchor: "Plan 05 will wire the call site in auth.ts AND introduce a separately-named __setRegisterTurnstileFnForTest seam for route-level tests"
</output>
</output>
