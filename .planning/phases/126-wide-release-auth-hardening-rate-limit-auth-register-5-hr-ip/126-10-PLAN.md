---
phase: 126
plan: 10
type: execute
wave: 4
depends_on: [05, 07, 08]
files_modified:
  - vigil-pwa/src/components/TurnstileWidget.tsx
  - vigil-pwa/src/pages/AuthPage.tsx
autonomous: true
requirements: [AUTH-126-02, AUTH-126-05, AUTH-126-06]

must_haves:
  truths:
    - "Signup mode renders Turnstile widget; token captured in state; submit button disabled while token is null"
    - "Login mode does NOT render Turnstile widget (per CONTEXT D-01 / RESEARCH Open-Q-3)"
    - "AuthPage 3 error-collapse sites (lines 53, 76-77, 86-87) parse {error, code} JSON and call resolveApiError"
    - "AuthPage 4th collapse site (catch-block line 103) preserves GENERIC_ERROR (no Response body to parse) — R2"
    - "Signup POST body includes turnstileToken alongside email + password"
    - "Footer of AuthPage form contains <Link> elements to /legal/privacy and /legal/terms"
  artifacts:
    - path: "vigil-pwa/src/components/TurnstileWidget.tsx"
      provides: "Thin React wrapper around @marsidev/react-turnstile <Turnstile> with controlled onSuccess/onError/onExpire"
      exports: ["default TurnstileWidget"]
    - path: "vigil-pwa/src/pages/AuthPage.tsx"
      provides: "Turnstile widget mount + structured error rendering at 3 sites + legal footer links"
      contains: "resolveApiError"
  key_links:
    - from: "AuthPage form submit handler (signup)"
      to: "/v1/auth/register"
      via: "POST {email, password, turnstileToken}"
      pattern: "turnstileToken"
    - from: "AuthPage 4xx response handler"
      to: "resolveApiError"
      via: "look up code → user-facing UX, fallback to raw error or GENERIC_ERROR"
      pattern: "resolveApiError\\("
    - from: "AuthPage footer"
      to: "/legal/privacy and /legal/terms"
      via: "<Link to=...> components"
      pattern: "/legal/(privacy|terms)"
---

<objective>
The final PWA-side surgery: wire Turnstile widget into the signup form, fix the four error-collapse sites identified at AuthPage.tsx lines 53, 76-77, 86-87, 103 (per R2), and add legal-page footer links.

Three concerns co-located in one plan because all touch `vigil-pwa/src/pages/AuthPage.tsx` — splitting would create same-file ownership conflicts.

Purpose: Close the user-visible half of AUTH-126-02 (captcha widget), AUTH-126-05 (error UX), and AUTH-126-06 (legal links). After this plan, the family-member triggering incident path is closed.

Output: One new component (TurnstileWidget.tsx) + AuthPage.tsx modified at multiple sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-CONTEXT.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-05-SUMMARY.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-07-SUMMARY.md
@.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-08-SUMMARY.md

@vigil-pwa/src/pages/AuthPage.tsx
@vigil-pwa/src/lib/api-error-codes.ts
@vigil-pwa/src/pages/ForgotPasswordPage.tsx

<interfaces>
<!-- Plan 07 delivered resolveApiError + ERROR_CODE_MAP -->

From vigil-pwa/src/lib/api-error-codes.ts:
- `export interface ApiErrorUx { message: string; ctaLabel?: string; ctaHref?: string; }`
- `export function resolveApiError(body: {error?: string; code?: string} | null | undefined, fallback: string): ApiErrorUx`

<!-- @marsidev/react-turnstile (Plan 07 installed) — RESEARCH §1 widget shape -->

From @marsidev/react-turnstile:
- `<Turnstile siteKey={string} onSuccess={(token)=>void} onError={()=>void} onExpire={()=>void} options={{theme:'dark'}} />`

<!-- Existing AuthPage.tsx structure — bug sites per RESEARCH §AUTH-126-05 -->

From vigil-pwa/src/pages/AuthPage.tsx:
- line ~9: `const GENERIC_ERROR = 'Invalid email or password';` (constant kept as fallback)
- line ~53: login mode: `setError(GENERIC_ERROR)` after `if (!res.ok)` — REGRESSION TARGET
- lines ~76-77: signup register: `setError(GENERIC_ERROR)` after register `if (!res.ok)` — REGRESSION TARGET
- lines ~86-87: signup auto-login: `setError(GENERIC_ERROR)` after auto-login `if (!res.ok)` — REGRESSION TARGET
- line ~103: catch block: `setError(GENERIC_ERROR)` — KEEP VERBATIM (no Response body to parse — R2)
- lines ~136-148: email/password input pattern (mirror for Turnstile widget position)
- lines ~188-196: toggle-mode footer (mirror for legal links footer position)

<!-- Existing env-var pattern in vigil-pwa -->

From vigil-pwa/src/api/client.ts (line ~3):
- `import.meta.env.VITE_API_BASE` pattern — VITE_TURNSTILE_SITE_KEY follows same convention
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TurnstileWidget.tsx wrapper component</name>
  <files>vigil-pwa/src/components/TurnstileWidget.tsx</files>
  <read_first>
    - @marsidev/react-turnstile README (or look at node_modules/@marsidev/react-turnstile/dist/index.d.ts for the actual exported API)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-02 + §Code Examples lines 878-896
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §TurnstileWidget
  </read_first>
  <action>
Create `vigil-pwa/src/components/TurnstileWidget.tsx`. Thin wrapper around `@marsidev/react-turnstile`'s `<Turnstile>` component.

Top-of-file JSDoc:
- Purpose (AUTH-126-02 / D-01): client-side captcha widget for /auth/register signup form
- Token lifecycle: onSuccess sets token (string), onError/onExpire reset to null
- Env var: VITE_TURNSTILE_SITE_KEY from Vercel build-time env
- Consumer contract: caller stores token in state, gates submit button on token presence, includes token in POST body as `turnstileToken`

Implementation per RESEARCH §Code Examples lines 882-895:

1) Named import: `import { Turnstile } from "@marsidev/react-turnstile";`

2) Props interface:
   - `interface TurnstileWidgetProps { onToken: (token: string | null) => void; }`

3) Default export function component `TurnstileWidget({ onToken })`:
   - Read site key: `const siteKey = import.meta.env.VITE_TURNSTILE_SITE_KEY as string;`
   - If `!siteKey`, render a fallback `<div className="text-yellow-500 text-sm">Captcha not configured</div>` and skip widget render. This is operator-dev safety — local dev without VITE_TURNSTILE_SITE_KEY won't crash with a misleading error.
   - Otherwise render:
     ```tsx
     <Turnstile
       siteKey={siteKey}
       onSuccess={(token) => onToken(token)}
       onError={() => onToken(null)}
       onExpire={() => onToken(null)}
       options={{ theme: "dark" }}
     />
     ```

4) options.theme: "dark" matches AuthPage's gray-900 surface chrome.

5) Do NOT add Turnstile.execute() programmatic call — use the default visible/managed widget; auto-renders on mount.

Wrap in a `<div className="my-3">` so consumer placement is consistent with existing input spacing.

Confirm tsc clean: `cd vigil-pwa && npx tsc --noEmit -p tsconfig.json` exits 0.
  </action>
  <verify>
    <automated>cd vigil-pwa && test -f src/components/TurnstileWidget.tsx && grep -c "@marsidev/react-turnstile" src/components/TurnstileWidget.tsx ; grep -c "VITE_TURNSTILE_SITE_KEY" src/components/TurnstileWidget.tsx ; grep -c "onToken" src/components/TurnstileWidget.tsx ; npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-pwa/src/components/TurnstileWidget.tsx` exists
    - Imports `@marsidev/react-turnstile`
    - Reads `import.meta.env.VITE_TURNSTILE_SITE_KEY`
    - Exposes `onToken(string | null)` prop
    - Renders `<Turnstile>` with onSuccess/onError/onExpire wired to onToken
    - Falls back to a placeholder div when VITE_TURNSTILE_SITE_KEY is missing (dev safety)
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>TurnstileWidget.tsx exists with controlled onToken callback; dev-safe fallback when site key missing; Vite build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Surgery on AuthPage.tsx — wire Turnstile widget, fix 3 error-collapse sites, add legal footer</name>
  <files>vigil-pwa/src/pages/AuthPage.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/AuthPage.tsx (FULL FILE — every line will be touched at least adjacent)
    - vigil-pwa/src/lib/api-error-codes.ts (Plan 07 — resolveApiError contract)
    - vigil-pwa/src/components/TurnstileWidget.tsx (Task 1 — onToken prop)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-RESEARCH.md §AUTH-126-02 PWA + §AUTH-126-05 PWA Files-to-edit + §AUTH-126-06 footer + §R2 (catch-block exception)
    - .planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-PATTERNS.md §MODIFIED AuthPage.tsx
  </read_first>
  <action>
Edit `vigil-pwa/src/pages/AuthPage.tsx`. Five additions/changes in this order:

**EDIT 1 — Add imports (top of file, alongside existing imports):**
- `import { resolveApiError } from '../lib/api-error-codes';`
- `import TurnstileWidget from '../components/TurnstileWidget';`
- `import { Link } from 'react-router-dom';` — IF NOT ALREADY IMPORTED. Verify via grep first; AuthPage may already import Link from react-router for other navigation.

**EDIT 2 — Add Turnstile token state:**
Locate the existing `useState` hooks (likely email/password/loading/error). Add:
- `const [turnstileToken, setTurnstileToken] = useState<string | null>(null);`

**EDIT 3 — Render TurnstileWidget in signup mode only:**
Locate the form JSX. After the password input field (around line 161 — confirm via read) and BEFORE the "Forgot password?" link or any submit button, conditionally render Turnstile:
- `{!isLogin && (<TurnstileWidget onToken={setTurnstileToken} />)}`

Place inside a `<div className="my-3">` for spacing (or rely on TurnstileWidget's internal wrap from Task 1 — pick one, don't double-wrap).

**EDIT 4 — Disable submit button when signup mode + token null:**
Locate the submit `<button type="submit" ...>`. Update its `disabled={...}` prop:
- Current likely `disabled={loading}`
- New: `disabled={loading || (!isLogin && !turnstileToken)}`

This prevents form submission until the user completes the captcha.

**EDIT 5 — Update the signup POST body to include turnstileToken:**
Locate the `fetch('/v1/auth/register', ...)` call. Update the JSON body:
- Current: `JSON.stringify({ email, password })`
- New: `JSON.stringify({ email, password, turnstileToken })`

Login POST does NOT include turnstileToken (Open-Q-3 — login captcha deferred).

**EDIT 6 — Fix 3 error-collapse sites at lines 53, 76-77, 86-87 (R2 — NOT line 103 catch block):**

For each of the three `if (!res.ok) { setError(GENERIC_ERROR); return; }` sites:

Replace with:
```
if (!res.ok) {
  const body = await res.json().catch(() => ({})) as { error?: string; code?: string };
  const ux = resolveApiError(body, GENERIC_ERROR);
  setError(ux.message);
  return;
}
```

Three sites only:
- Login mode site (~line 53)
- Signup register call site (~line 76-77)
- Signup auto-login call site (~line 86-87)

R2 GUARD: DO NOT touch the catch-block site at line ~103. That branch catches network-level throws where `res` doesn't exist — keep `setError(GENERIC_ERROR)` verbatim. The plan-checker will assert this site is untouched via diff inspection.

**EDIT 7 — Add legal footer links:**
Locate the existing toggle-mode block at lines ~188-196. Below (or alongside) the toggle-mode button, add a new sibling block:
```
<div className="mt-4 text-center text-xs text-gray-500">
  <Link to="/legal/privacy" className="hover:text-gray-300">Privacy</Link>
  {' · '}
  <Link to="/legal/terms" className="hover:text-gray-300">Terms</Link>
</div>
```

Use `<Link>` from react-router-dom (NOT `<a>` — `<a>` would full-reload the page).

**Self-check after all edits:**
- `npx tsc --noEmit` exits 0
- `npx vite build` exits 0
- Manual visual check (mental simulation): signup form has Turnstile widget below password input, submit disabled until token, footer shows Privacy · Terms links
- `grep -c "GENERIC_ERROR" src/pages/AuthPage.tsx` should still be ≥ 2 (the constant declaration at top + the catch-block fallback at line ~103 — R2 lock)
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -c "resolveApiError" src/pages/AuthPage.tsx ; grep -c "TurnstileWidget" src/pages/AuthPage.tsx ; grep -c "turnstileToken" src/pages/AuthPage.tsx ; grep -c '/legal/privacy' src/pages/AuthPage.tsx ; grep -c '/legal/terms' src/pages/AuthPage.tsx ; grep -c "GENERIC_ERROR" src/pages/AuthPage.tsx ; npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `resolveApiError(` appears ≥ 3 times in AuthPage.tsx (the three error-collapse sites)
    - `TurnstileWidget` import + JSX render present (grep count ≥ 2)
    - `turnstileToken` state variable + POST body inclusion (grep count ≥ 3: useState + onToken setter + JSON.stringify)
    - Both `/legal/privacy` AND `/legal/terms` paths present (grep count == 1 each)
    - `GENERIC_ERROR` count ≥ 2 (constant declaration + catch-block preservation — R2 lock)
    - `npx vite build` exits 0
    - Submit button disabled prop includes `!turnstileToken` guard for signup mode
    - Login POST body does NOT include turnstileToken (verify by inspecting login mode branch)
  </acceptance_criteria>
  <done>AuthPage wires Turnstile, fixes 3 error-collapse sites (preserves 4th catch-block per R2), adds legal footer links; Vite build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → Turnstile iframe | Cloudflare-managed; token issued in cf-managed iframe; PWA only receives the token string |
| Token state → POST body | PWA forwards token to vigil-core /auth/register; server re-verifies via siteverify |
| Server `{error, code}` response → resolveApiError → setError | Structured input → typed UX; unknown codes fall back to raw `error` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-10-01 | Spoofing | Bot bypasses Turnstile widget render via DevTools console | mitigate | Submit button disabled until token state is non-null. Even with disabled bypass via DevTools, server-side rate-limit + siteverify (Plan 05) catches; widget is defense-in-depth, not sole gate. |
| T-126-10-02 | Tampering | Token reused across users (replay) | mitigate | Tokens are single-use server-side (Cloudflare enforces). PWA-side discards token after submit (state reset on toggleMode). |
| T-126-10-03 | Information Disclosure | Error-code resolver leaks server-side message verbatim for unknown codes | accept | Fallback is intentional forward-compat: new codes added server-side surface raw `error` until PWA map updates. RESEARCH §AUTH-126-05. |
| T-126-10-04 | Repudiation | Future regression silently re-introduces `setError(GENERIC_ERROR)` collapse | mitigate | The four-site identification is documented (lines 53, 76-77, 86-87 vs catch-block 103). Acceptance criteria grep counts catch regression. |
| T-126-10-05 | Elevation of Privilege | Legal pages link to malicious external host | accept | `<Link to="/legal/*">` is internal-router only — no external href reachable from this code. |
| T-126-10-06 | Denial of Service | Turnstile widget fails to load — user cannot sign up | mitigate | TurnstileWidget falls back to placeholder when VITE_TURNSTILE_SITE_KEY is missing (dev safety). Production failure to load is operator-monitored via Sentry breadcrumbs. Submit disabled in this case prevents broken-state submissions. |
| T-126-10-07 | Tampering | Catch-block site (line 103) accidentally also gets resolveApiError treatment | mitigate | R2 explicit guard in plan body; GENERIC_ERROR grep count ≥ 2 in acceptance criteria proves preservation. |
| T-126-10-08 | Information Disclosure | Login form leaks email-existence by differential error code | accept | Login uses generic `INVALID_CREDENTIALS` (401) regardless of whether email exists — server-side enum-safe pattern (CONTEXT D-08). Login captcha + brute-force protection deferred. |
</threat_model>

<verification>
- `cd vigil-pwa && npx vite build` exits 0
- `grep -c "resolveApiError(" vigil-pwa/src/pages/AuthPage.tsx` → ≥ 3
- `grep -c "turnstileToken" vigil-pwa/src/pages/AuthPage.tsx` → ≥ 3
- `grep -c "GENERIC_ERROR" vigil-pwa/src/pages/AuthPage.tsx` → ≥ 2 (constant + catch-block fallback preserved — R2 lock)
- `grep -c "/legal/privacy" vigil-pwa/src/pages/AuthPage.tsx` → 1
- `grep -c "/legal/terms" vigil-pwa/src/pages/AuthPage.tsx` → 1
- `grep -c "@marsidev/react-turnstile" vigil-pwa/src/components/TurnstileWidget.tsx` → ≥ 1
</verification>

<success_criteria>
- TurnstileWidget.tsx ships thin wrapper with onToken callback
- AuthPage signup mode renders widget + disables submit until token
- Signup POST body includes turnstileToken; login POST does NOT
- 3 error-collapse sites (lines 53, 76-77, 86-87) parse JSON + call resolveApiError
- 4th site (catch-block line 103) preserves GENERIC_ERROR verbatim — R2 lock
- Footer renders Privacy + Terms Links to /legal/privacy and /legal/terms
- Vite build clean
</success_criteria>

<output>
After completion, create `.planning/phases/126-wide-release-auth-hardening-rate-limit-auth-register-5-hr-ip/126-10-SUMMARY.md` documenting:
- Lines of TurnstileWidget.tsx
- AuthPage.tsx diff stats (~10-15 edits across imports, state, JSX, fetch bodies, error handlers, footer)
- Confirmed R2 lock: GENERIC_ERROR fallback preserved in catch block
- Confirmed line-103 site UNTOUCHED via git blame inspection
- Vite build success
- Anchor: "Plan 11 is the final operator-wallclock confirmation for AUTH-126-07 spend cap"
</output>
