---
phase: 18-polish-integration
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified: [.planning/STATE.md, .planning/ROADMAP.md, .planning/PROJECT.md]
autonomous: true
---

<objective>
Final build verification and v1.2 milestone completion. Confirm everything builds cleanly after all polish changes, update project state to mark Phase 18 and v1.2 milestone as complete.

Purpose: Close out the v1.2 Daily Driver milestone with confidence that the codebase is in a ship-ready state.
Output: Updated project state files, v1.2 milestone marked complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-polish-integration/18-01-SUMMARY.md
@.planning/phases/18-polish-integration/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files>Package.swift</files>
  <action>
1. Run `swift build` and confirm zero errors and zero warnings.
2. Run `swift build -c release` to verify release build also succeeds.
3. Count total Swift source files and lines of code to update project metrics.
4. Verify all three targets build: DailyBrief (CLI), JarvisCore (library), DailyBriefMonitor (app).

If any build issues are found, fix them before proceeding. This is the gate — nothing ships with build errors.
  </action>
  <verify>swift build and swift build -c release both succeed with zero errors</verify>
  <done>All three targets build cleanly in both debug and release configurations.</done>
</task>

<task type="auto">
  <name>Task 2: Update project state for v1.2 milestone completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/PROJECT.md</files>
  <action>
1. Update STATE.md:
   - Current position: Phase 18 complete, all phases complete
   - Progress: 100%
   - Status: v1.2 Daily Driver milestone complete
   - Add Phase 18 to velocity table
   - Clear any pending todos addressed by this phase (imap-direct-work-email)

2. Update ROADMAP.md:
   - Mark Phase 18 as complete with date
   - Mark v1.2 milestone as complete
   - Move v1.2 into completed milestones section
   - Update progress table

3. Update PROJECT.md:
   - Update context section with final LOC count and file count
   - Move "IMAP configurability" from active to validated requirements
   - Note v1.2 shipped date

Keep updates factual and concise.
  </action>
  <verify>All three files updated with consistent completion dates and status.</verify>
  <done>v1.2 Daily Driver milestone marked complete across all project state files. Metrics updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds (debug)
- [ ] `swift build -c release` succeeds (release)
- [ ] STATE.md shows Phase 18 complete, v1.2 shipped
- [ ] ROADMAP.md shows v1.2 milestone complete
- [ ] PROJECT.md metrics updated
</verification>

<success_criteria>

- All tasks completed
- Zero build errors in debug and release
- All project state files consistently show v1.2 complete
- Pending IMAP todo resolved
</success_criteria>

<output>
After completion, create `.planning/phases/18-polish-integration/18-03-SUMMARY.md`
</output>
