---
phase: 120-day-1-jsonl-schema-verification-detection-strategy-lock
plan: 03
type: execute
wave: 3
depends_on:
  - "120-01"
  - "120-02"
files_modified:
  - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md
autonomous: true
requirements:
  - VERIFY-01

must_haves:
  truths:
    - "User can read the public github.com/dttigers/vigil-watch README and find a verbatim 8-row mapping table — one row per assumed JSONL line type from spec §'Expected JSONL line types' — each row labeled `Confirmed`, `Corrected`, or `Missing` with field-path evidence (e.g., `$.type=tool_use, $.message.content[].type=tool_use`)."
    - "User can read explicit answers to all 4 spec-flagged questions in the README, each citing a specific JSONL field path AND at least one excerpt in the appendix."
    - "User can read a single Verdict line stating exactly one of: `proceed-as-spec`, `spec-correct-and-proceed`, `fallback-path-A`, `fallback-path-B`, or `fallback-path-C` — applying D-06's pragmatic-fallback rule (escalate ONLY if JSONL fundamentally lacks signal for one of the 5 Vigil event types; field renames trigger spec-correct, not fallback)."
    - "User can read the Downstream Phase Impact section: if Verdict is `proceed-as-spec` it says 'No downstream goal shifts.'; if `spec-correct-and-proceed` it lists the corrected mapping rows Phase 122 must implement; if `fallback-path-N` it explicitly names which Phase 122 and Phase 123 success-criteria items shift, with the new behavior."
    - "User can read the Appendix: Raw JSONL Excerpts section containing ≥8 fenced jsonl excerpts (one grounding each mapping-table row, minimum), each headed with the source filename and line range."
    - "Every excerpt copied into the public README is hand-curated from `verification-log/excerpts/` — no bulk dump of user content. Sensitive substrings (filesystem paths beyond home directory shorthand, code being worked on, API keys, names) are reviewed before copy and redacted with `[redacted]` if present."
    - "After this plan, the README is committed and pushed to origin/main — the Day-1 findings are now publicly readable, fulfilling VERIFY-01's deliverable: 'Findings written to vigil-watch repo README before any production-mapping code is written.'"
  artifacts:
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md"
      provides: "Authoritative Day-1 findings document committed to the public repo before Phase 121/122/123 plan against assumed schema"
      contains: "### Verdict"
  key_links:
    - from: "vigil-watch README §Verdict"
      to: "Phase 121 (POST /v1/agent-events payload contract) + Phase 122 (Watcher actor) + Phase 123 (CLI surface)"
      via: "downstream phases read this README before plan-phase"
      pattern: "Verdict.*(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])"
    - from: "vigil-watch README §JSONL Line-Type Mapping"
      to: "Phase 122 Vigil-event detection rules"
      via: "8-row table is the source of truth for the daemon's parser"
      pattern: "tool_use.*needs_input"
    - from: "every claim in the README"
      to: "an excerpt in §Appendix: Raw JSONL Excerpts"
      via: "inline excerpt-number references like [Excerpt 3]"
      pattern: "Excerpt [0-9]+"
---

<objective>
Synthesize Plan 120.02's verification-log into the public Day-1 findings document on the github.com/dttigers/vigil-watch README. This is the deliverable that VERIFY-01 demands: a publicly-readable document that downstream Phase 121/122/123 planning agents read before coding against any assumed schema.

The plan is autonomous because all the empirical work was done in 120.02. This plan reads `verification-log/corpus-mining.md` + `verification-log/scripted-session.md` + the excerpt files, then writes the README sections in the skeleton placeholders Plan 120.01 created.

Purpose:
- Apply D-06 pragmatic-fallback rule to assign final Verdict (Plan 120.02 produced a tentative; this plan finalizes).
- Convert Plan 120.02's 4 per-question evidence pointers into prose answers with field paths.
- Convert spec's 8-row assumed mapping table into observed mapping table with `Confirmed`/`Corrected`/`Missing` per row.
- Hand-curate the appendix excerpts (NOT bulk copy from `verification-log/excerpts/`) — Plan 120.02's excerpts are full-fidelity dumps from the user's working session and may contain incidental PII; this plan reviews + redacts before copying.
- If Verdict is fallback-path-N, explicitly enumerate which Phase 122/123 success criteria shift, so downstream planning agents can detect the shift without re-reading the verification log.

Output: README.md updated with all 4 placeholder sections filled, committed and pushed to `origin/main`. The repo is now fulfilling VERIFY-01 publicly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md
@.planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
@.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-01-SUMMARY.md
@.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-02-SUMMARY.md

<input_artifacts>
<!-- Plan 120.03 reads these (produced by Plan 120.02). The executor MUST read them before writing. -->

/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md
/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md
/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/  (entire directory contents)
</input_artifacts>

<contract>
<!-- Section headers Plan 120.01 wrote into README.md placeholder. This plan replaces the placeholder content under each header but MUST preserve header text verbatim. -->

Sections to fill:
- `### Verdict`
- `### JSONL Line-Type Mapping`
- `### Spec-Flagged Questions`
- `### Downstream Phase Impact`
- `## Appendix: Raw JSONL Excerpts`

The 4 spec-flagged questions (verbatim from spec §"Day-1 verification gate"):
1. How do tool-approval prompts appear in JSONL?
2. What fields indicate "awaiting input"?
3. How is session end signaled?
4. What is the structure of an errored tool result?

The 8 spec-assumed line types (verbatim from spec §"Expected JSONL line types" — these are the 8 rows the mapping table MUST address, in this order):
1. `user`
2. `assistant`
3. `tool_use` (with `awaiting_approval`)
4. `tool_result` (success)
5. `tool_result` (error)
6. `session_end` / `stop`
7. (no new lines for N seconds) — silence/heartbeat
8. (custom regex match in any text) — milestone

The 5 allowed Verdict values (D-05):
- `proceed-as-spec`
- `spec-correct-and-proceed`
- `fallback-path-A` (Notification observation)
- `fallback-path-B` (VS Code extension)
- `fallback-path-C` (Process inspection)

D-06 pragmatic rule: escalate to fallback-path ONLY if JSONL fundamentally lacks signal for one or more of the 5 Vigil event types (`needs_input`, `task_complete`, `task_failed`, `milestone`, `heartbeat`). Field-name renames or restructured payloads → spec-correct-and-proceed.
</contract>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author Verdict + Mapping Table + Spec-Flagged Questions</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md
  </files>
  <read_first>
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md (full file)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md (full file)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md (current state — Plan 120.01 skeleton — to confirm placeholder section headers are intact before editing)
    - .planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md (D-05 minimal-main shape; D-06 pragmatic-fallback rule)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md §"Expected JSONL line types" (the 8-row assumed mapping) and §"Fallback strategies" (the 3 fallback options)
  </read_first>
  <action>
    This task fills the four sections in the main body of README.md (the appendix comes in Task 2). Use the Edit tool on README.md to replace placeholder content section by section, NOT the Write tool — preserves the surrounding header text and any other content.

    **Step 1: Apply D-06 pragmatic-fallback rule to choose final Verdict.**

    Read Plan 120.02's `scripted-session.md` "Pragmatic-fallback assessment" table (5 rows, one per Vigil event). Apply this decision tree literally:

    - If ALL 5 Vigil events have `yes` or `partial` signal in JSONL → Verdict is `proceed-as-spec` OR `spec-correct-and-proceed`.
      - If 0 mapping-table rows from the spec need correction (i.e., observed types/fields exactly match spec wording) → Verdict is `proceed-as-spec`.
      - If ≥1 mapping-table rows need correction (renamed field, restructured payload, different discriminator) → Verdict is `spec-correct-and-proceed`.
    - If ANY of the 5 Vigil events has `no` signal in JSONL → Verdict is `fallback-path-A` / `B` / `C` per spec priority order:
      - `fallback-path-A` (Notification observation): preferred if the missing signal is `needs_input` (notification-driven) or `task_complete`.
      - `fallback-path-B` (VS Code extension): preferred if multiple signals are missing AND the gap is structural (no JSONL discrimination at all).
      - `fallback-path-C` (Process inspection): last resort — only if both A and B are also untenable.

    Write the chosen Verdict on the SINGLE line directly under the `### Verdict` header in README.md, using this exact format (no extra prose between header and verdict line):

    ```markdown
    ### Verdict

    **`<chosen-verdict-value>`** — <one-paragraph rationale, ≤120 words, citing the pragmatic-fallback table from scripted-session.md and naming which event(s) drove the decision>.

    **Captured:** <ISO-8601 date> on host `<hostname from corpus-mining.md>` against Claude Code session JSONL written by VS Code Claude extension <version-if-known-else "version not captured">.
    ```

    The verdict-value MUST be one of the 5 allowed strings, surrounded by backticks, bolded.

    **Step 2: Fill `### JSONL Line-Type Mapping` with the observed 8-row table.**

    Replace the placeholder table (with `_TBD_` cells from Plan 120.01) with the OBSERVED mapping. Preserve the 4-column structure (`Spec-assumed type | Observed type / field path | Vigil event emitted | Confirmed / Corrected / Missing`). For each of the 8 spec rows in their original order:

    - Cross-reference Plan 120.02's Step 4 token search + Step 3 exemplar excerpts.
    - Column 2 (Observed type / field path): write the actual JSONL field path observed, e.g., `$.type=tool_use, $.message.content[?(@.type=="tool_use")]` — using JSONPath-like notation. If the spec's wording is wrong but the semantic exists, write what was actually observed.
    - Column 4 (status): exactly one of:
      - `Confirmed` — observed exactly matches spec wording.
      - `Corrected: <one-line fix>` — observed differs from spec wording but signal is present (this is the spec-correct-and-proceed case at the row level).
      - `Missing` — no JSONL signal observed for this row (this is the row-level fallback signal).

    Each row's "Vigil event emitted" column MUST also reference the appendix excerpt that grounds the row, in the form `→ [Excerpt N](#excerpt-N-...)`. Plan 120.03 Task 2 will create those anchor IDs; this task uses placeholder anchor names that match Task 2's output.

    Below the table, add a brief paragraph (≤80 words) noting any rows marked `Missing` and explaining why D-06's pragmatic-fallback rule did or did not trigger escalation for this row's missing signal.

    **Step 3: Fill `### Spec-Flagged Questions` with prose answers.**

    Replace each of the 4 numbered placeholders with a real answer. Required structure for each question:

    ```markdown
    1. **How do tool-approval prompts appear in JSONL?**
       <2–4 sentence answer naming the JSONL field path(s) used to detect the prompt state>. Field path: `<jsonpath>`. The transition from "Claude calls tool" to "tool starts running" is signaled by `<field>=<value>` flipping to `<field>=<value>`.
       Evidence: [Excerpt N](#excerpt-N-...) (lines <a>–<b> of `<filename>`).
    ```

    Each answer MUST:
    - Name a specific JSONL field path (e.g., `$.message.content[].type`, `$.subtype`, `$.tool_use.input`, `$.is_error`).
    - Reference at least one excerpt in the appendix.
    - Cite the source `<filename>:<lines>` (matching what Task 2 will preserve).

    Question 4 (errored tool result structure) gets special treatment: walk through the actual error-carrying field hierarchy observed in Plan 120.02's Scenario C excerpt (e.g., `$.message.content[?(@.type=="tool_result")].is_error`, `$.message.content[?(@.type=="tool_result")].content` containing the error text). Plan 122's parser will be implemented directly against this answer.

    **Step 4: Fill `### Downstream Phase Impact`.**

    Logic:
    - If Verdict is `proceed-as-spec`: section body is exactly `No downstream goal shifts. Phase 122 (vigil-watch core) and Phase 123 (vigil-watch shell) plan against the spec mapping verbatim.`
    - If Verdict is `spec-correct-and-proceed`: section body is a bulleted list of each `Corrected: <fix>` row from the mapping table, with each bullet stating "Phase 122 implementation note: when emitting `<vigil-event>`, read field path `<observed-path>` instead of spec's `<spec-path>`."
    - If Verdict is `fallback-path-A`: section body explicitly states (verbatim phrasing required so downstream agents can grep for it):
      - `Phase 122 SC #1 shifts: 'FSEventStream rooted at ~/.claude/projects/' becomes 'NSDistributedNotificationCenter observer for VS Code Claude notifications (requires Full Disk Access entitlement)'.`
      - `Phase 122 §"Internal architecture" 'Watcher actor' rewritten as 'NotificationObserver actor reading macOS Notification Center DB at ~/Library/Group Containers/group.com.apple.usernoted/db2/db'.`
      - `Phase 122 SC #5 (config keys) keeps watch.toml shape but 'projects_dir' becomes 'notification_db_path'.`
      - `Phase 123 unaffected — CLI surface, launchd plist, 24h soak all valid against any backend.`
    - If Verdict is `fallback-path-B`: section body states the analogous shift to a TypeScript VS Code extension hook (Phase 122 becomes a separate npm package; Phase 123's launchd discussion is replaced by VS Code extension marketplace publish flow; SC #4 24h-soak still applies).
    - If Verdict is `fallback-path-C`: section body states process-inspection shift (Phase 122 becomes a `ps`-polling daemon; emit only `running` vs `not running` granularity; explicitly note `needs_input`/`task_complete`/`task_failed`/`milestone` are NOT distinguishable and request roadmap revision before Phase 122 plan).

    For ALL fallback verdicts, end the section with this exact line so the orchestrator can detect it:
    `> **Action required:** Phase 121/122/123 planners MUST read this Downstream Phase Impact section before issuing /gsd-plan-phase.`

    **Step 5: Smoke-check the README before commit.**

    Run these greps to verify the contract is satisfied (these mirror the acceptance criteria; running them now catches missing pieces before commit):

    ```
    cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"
    grep -c "^### " README.md  # ≥4 H3 headers
    grep -E "^\*\*\`(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])\`\*\*" README.md  # exactly 1 verdict line
    grep -c "^| " README.md  # mapping table rows + token rows ≥10
    grep -cE "^[1-4]\. \*\*" README.md  # 4 spec-flagged questions, numbered, bolded
    ```

    Do NOT commit yet — Task 2 fills the appendix and commits both task outputs together.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" &amp;&amp; grep -qE "^\*\*\`(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])\`\*\*" README.md &amp;&amp; grep -q "^### Verdict$" README.md &amp;&amp; grep -q "^### JSONL Line-Type Mapping$" README.md &amp;&amp; grep -q "^### Spec-Flagged Questions$" README.md &amp;&amp; grep -q "^### Downstream Phase Impact$" README.md &amp;&amp; [ "$(grep -cE '^[1-4]\. \*\*' README.md)" -ge 4 ] &amp;&amp; ! grep -q "_TBD_" README.md &amp;&amp; [ "$(grep -c '^| ' README.md)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - README.md still contains all 7 verbatim section headers from Plan 120.01 (no header was accidentally renamed): `# vigil-watch`, `## Day-1 JSONL Schema Verification`, `### Verdict`, `### JSONL Line-Type Mapping`, `### Spec-Flagged Questions`, `### Downstream Phase Impact`, `## Appendix: Raw JSONL Excerpts`.
    - README.md contains exactly one Verdict line matching the regex `^\*\*\`(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])\`\*\*` (verified by `grep -cE` returning 1 — not 0, not 2+).
    - README.md no longer contains any `_TBD_` placeholder string (verified by `grep -q "_TBD_" README.md` returning non-zero exit).
    - JSONL Line-Type Mapping table has exactly 8 data rows whose first column matches the spec's 8 assumed types in their original order (`user`, `assistant`, `tool_use`, `tool_result` (success), `tool_result` (error), `session_end`/`stop`, silence-row, regex-row).
    - Each of those 8 rows ends in a status cell containing exactly one of: `Confirmed`, `Corrected:` (followed by fix text), or `Missing` (case-sensitive).
    - Spec-Flagged Questions section contains exactly 4 numbered items, each bolded (regex `^[1-4]\. \*\*` matches 4 times).
    - Each of the 4 question answers contains a JSONPath-like reference (verified by `grep -c "\\\$\\." README.md` ≥4 — at least one `$.` per question).
    - Each of the 4 question answers contains a reference to an appendix excerpt (verified by `grep -c "Excerpt [0-9]" README.md` ≥4).
    - Downstream Phase Impact section is non-empty.
    - If verdict is a fallback (matches `fallback-path-[ABC]`), the README contains the exact line `> **Action required:** Phase 121/122/123 planners MUST read this Downstream Phase Impact section before issuing /gsd-plan-phase.` (verified with `grep -qF`).
    - File saved successfully — `git diff --stat README.md` reports a non-zero line change.
  </acceptance_criteria>
  <done>README.md main body fully replaces Plan 120.01's placeholders with empirical findings: 1 Verdict line, 8-row observed mapping, 4 prose answers with field paths, downstream-impact statement, all grounded in named excerpt references that Task 2 will materialize.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hand-curate appendix excerpts + commit + push to origin/main</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md
  </files>
  <read_first>
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md (current state from Task 1 — to identify which Excerpt N references Task 1 created and need materialization)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/  (every file in this directory — the source of excerpt content)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md (to recover the source filename + line range for each corpus-* excerpt)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md (to recover the source filename + line range for each scripted-* excerpt)
    - .planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md (D-05 — appendix grounds every claim)
  </read_first>
  <action>
    Task 1 left `Excerpt N` references throughout README.md main body. This task materializes those references in the `## Appendix: Raw JSONL Excerpts` section and pushes the final README.

    **Step 1: Build the excerpt inventory.**

    Enumerate every `Excerpt N` reference Task 1 created in README.md:
    ```
    grep -nE "Excerpt [0-9]+" README.md
    ```
    Result is a list like `Excerpt 1, Excerpt 2, ...`. Determine the maximum N — the appendix needs N excerpts numbered 1..N, contiguously.

    For each Excerpt N:
    - Identify which `verification-log/excerpts/<filename>.jsonl` file it pulls from (use the surrounding mapping-table row or question answer to disambiguate).
    - Look up the source filename and line range from corpus-mining.md or scripted-session.md.

    Minimum N is 8 (one excerpt per mapping-table row). If Task 1 produced fewer than 8 unique Excerpt references, add additional ones in this task to bring the total to ≥8 — the must-have requires "≥8 fenced jsonl excerpts (one grounding each mapping-table row, minimum)."

    **Step 2: Sanitize each excerpt before copying into public README.**

    Each excerpt file in `verification-log/excerpts/` was extracted from a real working session and may contain:
    - Filesystem paths beyond home shorthand (e.g., absolute paths revealing folder structures the user prefers private)
    - Snippets of code being worked on (intellectual property of consulting clients, etc.)
    - Names of people, projects, or third-party services from prompts
    - Tokens, API keys, or other credentials accidentally typed into prompts

    For each excerpt to be copied:
    1. Read the file with the Read tool.
    2. Visually scan each line for sensitive substrings. Use these heuristics:
       - Replace any sequence matching `vk_[a-zA-Z0-9]{8,}` with `vk_[REDACTED]`.
       - Replace any sequence matching `sk-ant-[a-zA-Z0-9_-]{8,}` (Anthropic key prefix) with `sk-ant-[REDACTED]`.
       - Replace any sequence matching `Bearer [a-zA-Z0-9._-]{20,}` with `Bearer [REDACTED]`.
       - Replace absolute filesystem paths beyond `/Users/jamesonmorrill/` (i.e., the user's home is fine since memory already documents the dailybrief working tree) with the same path. But replace any OTHER user's home (e.g., `/Users/<somethingelse>/`) with `/Users/[REDACTED]/`.
       - For any line that is clearly a long verbatim user prompt (>500 chars of prose), TRUNCATE to the first 100 chars + `... [truncated for appendix; full content in verification-log/]`.
       - Email addresses other than `jamesonmorrill1@gmail.com` (the user's own, per memory): replace with `[REDACTED]@example.com`.

    Sanitization is conservative: when in doubt, redact. The goal is to publish enough JSONL structure for downstream agents to ground claims, NOT to reproduce session content faithfully.

    **Step 3: Write the appendix.**

    Append (Edit tool, after the existing `## Appendix: Raw JSONL Excerpts` header) the excerpts in this exact form:

    ```markdown
    ### Excerpt 1: <short label, e.g., "tool_use awaiting approval">

    **Source:** `<filename without leading path>` (from `~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/`)
    **Lines:** <a>–<b>
    **Captured:** <ISO date matching corpus-mining.md or scripted-session.md>
    **Sanitization applied:** <list — e.g., "absolute paths preserved (user-home only); no credentials present"; or "truncated 1 long prompt; 1 email redacted">

    ```jsonl
    <verbatim sanitized JSONL — one object per line; preserve original formatting>
    ```

    ---

    ### Excerpt 2: <label>
    (same shape)

    ...
    ```

    Each Excerpt heading MUST end with an HTML anchor that Task 1's references can link to. Markdown auto-generates anchor IDs from headings; format the heading so `### Excerpt 1: tool_use awaiting approval` becomes `#excerpt-1-tool_use-awaiting-approval` (GitHub-flavored markdown lower-cases and replaces spaces with `-`). If Task 1 used a different anchor convention (e.g., `#excerpt-1`), use Task 1's convention and either (a) match the heading slug to Task 1's anchor, or (b) add an explicit HTML anchor `<a id="excerpt-1"></a>` immediately above the heading. Verify post-write that every `Excerpt N` reference Task 1 wrote resolves to a real anchor.

    **Step 4: Final README integrity check.**

    ```
    cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"

    # Excerpt count must be ≥8
    EXCERPT_COUNT=$(grep -cE "^### Excerpt [0-9]+:" README.md)
    [ "$EXCERPT_COUNT" -ge 8 ] || { echo "FAIL: only $EXCERPT_COUNT excerpts; need ≥8"; exit 1; }

    # Every Excerpt N reference in main body must have a matching ### Excerpt N: heading
    REFS=$(grep -oE "Excerpt [0-9]+" README.md | sort -u)
    for ref in $REFS; do
      N=$(echo "$ref" | tr -d 'Excerpt ')
      grep -qE "^### Excerpt $N:" README.md || { echo "FAIL: missing heading for $ref"; exit 1; }
    done

    # No leftover _TBD_
    ! grep -q "_TBD_" README.md || { echo "FAIL: unfilled placeholders remain"; exit 1; }

    # No common credential prefixes leaked
    ! grep -qE "vk_[a-zA-Z0-9]{8,}" README.md || { echo "FAIL: vk_ key not redacted"; exit 1; }
    ! grep -qE "sk-ant-[a-zA-Z0-9_-]{8,}" README.md || { echo "FAIL: sk-ant key not redacted"; exit 1; }
    ! grep -qE "Bearer [a-zA-Z0-9._-]{20,}" README.md || { echo "FAIL: Bearer token not redacted"; exit 1; }
    ```

    If any check fails, fix the issue (more excerpts, fix anchor, redact a missed credential) and re-run before commit.

    **Step 5: Verify gitignore boundary still holds.**

    ```
    cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"
    git status --porcelain
    ```
    Output MUST list ONLY `M  README.md` (modified). It MUST NOT list any path under `verification-log/`. If a `verification-log/` path appears in the output, the gitignore is broken and the executor MUST halt.

    **Step 6: Commit and push.**

    ```
    cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"
    git add README.md
    git commit -m "docs(120): Day-1 findings — JSONL schema verified, verdict locked

    - Verdict: <chosen-verdict-value>
    - 8-row mapping table populated against observed JSONL
    - 4 spec-flagged questions answered with field paths and excerpt evidence
    - Downstream Phase 121/122/123 impact captured

    Closes VERIFY-01."
    git push origin main
    ```

    If push fails (network, auth), retry once. If still fails, report the error verbatim and halt — do not amend, do not force-push.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" &amp;&amp; [ "$(grep -cE '^### Excerpt [0-9]+:' README.md)" -ge 8 ] &amp;&amp; ! grep -q "_TBD_" README.md &amp;&amp; ! grep -qE "vk_[a-zA-Z0-9]{8,}" README.md &amp;&amp; ! grep -qE "sk-ant-[a-zA-Z0-9_-]{8,}" README.md &amp;&amp; ! grep -qE "Bearer [a-zA-Z0-9._-]{20,}" README.md &amp;&amp; [ -z "$(git status --porcelain | grep -v '^?? ' | grep verification-log)" ] &amp;&amp; git log --oneline -1 | grep -q "VERIFY-01" &amp;&amp; [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^### Excerpt [0-9]+:" README.md` returns ≥8.
    - Every `Excerpt N` reference present in the main body of README.md (above the appendix) has a matching `### Excerpt N: ...` heading in the appendix (verified by extracting all referenced N values and checking each has a heading).
    - `grep -q "_TBD_" README.md` returns non-zero (no placeholders left).
    - `grep -qE "vk_[a-zA-Z0-9]{8,}" README.md` returns non-zero (no `vk_` keys leaked).
    - `grep -qE "sk-ant-[a-zA-Z0-9_-]{8,}" README.md` returns non-zero (no Anthropic keys leaked).
    - `grep -qE "Bearer [a-zA-Z0-9._-]{20,}" README.md` returns non-zero (no bearer tokens leaked).
    - Each excerpt heading is followed by `**Source:**`, `**Lines:**`, `**Captured:**`, `**Sanitization applied:**` lines and a fenced ```jsonl block (verifiable structurally with `grep -A 5 "^### Excerpt 1:" README.md` showing the four bold metadata lines).
    - `git status --porcelain` shows ONLY `M  README.md` and no `verification-log/...` paths (verification-log/ stayed gitignored).
    - `git log --oneline -1` includes the substring `VERIFY-01` (commit message references the requirement).
    - `git rev-parse HEAD` equals `git rev-parse origin/main` (push succeeded).
    - Visiting `https://github.com/dttigers/vigil-watch/blob/main/README.md` in a browser shows the rendered findings (executor confirms by `gh api repos/dttigers/vigil-watch/contents/README.md --jq '.content' | base64 -d | grep -c "^### Excerpt"` returning ≥8 — proves the pushed remote content matches local).
  </acceptance_criteria>
  <done>Public README on github.com/dttigers/vigil-watch contains the complete Day-1 findings: Verdict, 8-row mapping, 4 prose answers, downstream impact, ≥8 sanitized excerpts. Commit message references VERIFY-01. Verification log stayed gitignored. VERIFY-01 deliverable structurally satisfied: "Findings written to vigil-watch repo README before any production-mapping code is written."</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `verification-log/` (private, gitignored) → `README.md` (public, pushed to GitHub) | This is the load-bearing privacy boundary of the entire phase. The verification log holds verbatim chunks of user sessions — code being worked on, possibly proprietary client data, possibly accidentally-typed credentials. The public README must contain ONLY hand-curated, sanitized excerpts. Bulk-copy is prohibited. |
| local working tree → github.com/dttigers/vigil-watch (public on internet) | Once `git push` lands, content is mirrored, cached, and forkable. Redaction-after-the-fact is impractical (git history). |
| `gh api` access to dttigers org → executor's session | Same identity boundary as Plan 120.01; nothing new here, but a botched commit would still publish under the user's identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-12 | I (Information disclosure) | Sanitization process for appendix excerpts | mitigate | Task 2 Step 2 enforces 5 specific redaction rules (vk_ prefix, sk-ant prefix, Bearer tokens, cross-user paths, long prompts truncated, foreign emails). Step 4 then runs explicit grep checks for the 3 highest-risk credential prefixes BEFORE commit. If any prefix matches, the commit is aborted. |
| T-120-13 | I (Information disclosure) | Accidental commit of verification-log/ | mitigate | Plan 120.01 added `/verification-log/` to .gitignore. Task 2 Step 5 explicitly verifies `git status --porcelain` does NOT list any verification-log/ path before commit — if it does, halt. Defense in depth: even if the gitignore were missing, only `git add README.md` (specific file) is used in Step 6, not `git add .` or `git add -A`. |
| T-120-14 | I (Information disclosure) | Excerpts containing client code from consulting work | mitigate | Long-prompt truncation rule (>500 chars → first 100 + "[truncated]") in Step 2 caps verbose user content. Sanitization-applied bolded line on each excerpt makes the redaction posture auditable post-hoc by the user reading the public README. |
| T-120-15 | T (Tampering) | Dangling `Excerpt N` references with no heading in appendix | mitigate | Task 2 Step 4 explicitly cross-checks every `Excerpt N` reference in main body against `### Excerpt N:` headings in appendix; if any reference lacks a heading, commit is aborted. |
| T-120-16 | I (Information disclosure) | `vk_` API keys committed via verification-log | mitigate | Plan 120.01 .gitignore covers `vk_*` files. This plan additionally regex-scans README.md content for `vk_[a-zA-Z0-9]{8,}` substrings before commit (Step 4) — covers the case where a user's prompt accidentally contained a `vk_...` string copy-pasted as test data. |
| T-120-17 | R (Repudiation) | Commit message that doesn't trace back to VERIFY-01 | mitigate | Step 6 commit message hard-codes "Closes VERIFY-01." trailer. Acceptance criterion verifies `git log --oneline -1 \| grep -q VERIFY-01`. |
| T-120-18 | E (Elevation of privilege) | Force-push or amend if push fails | mitigate | Step 6 explicitly forbids `--force` / `--amend`. Per get-shit-done git safety protocol baked into the planner role. |
</threat_model>

<verification>
**Plan-level checks:**
- README.md contains all 7 verbatim Plan 120.01 section headers — none renamed, none deleted.
- Verdict line matches the regex `^\*\*\`(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])\`\*\*` exactly once.
- 8-row mapping table populated; each row's status is `Confirmed` / `Corrected: ...` / `Missing`.
- 4 prose answers, each with a JSONPath reference and an Excerpt reference.
- Downstream Phase Impact section non-empty; if fallback verdict, contains the verbatim "Action required" line.
- Appendix has ≥8 excerpts, each with metadata (Source, Lines, Captured, Sanitization applied) and a fenced jsonl block.
- No `_TBD_`, no `vk_`, no `sk-ant-`, no `Bearer ...` substrings.
- `git status --porcelain` shows only `M  README.md` (no verification-log/ paths).
- `git rev-parse HEAD == git rev-parse origin/main` (push succeeded).
- Public-readable confirmation: `gh api repos/dttigers/vigil-watch/contents/README.md` retrieval shows ≥8 Excerpt headings on remote.
</verification>

<success_criteria>
- [ ] VERIFY-01 deliverable satisfied: Day-1 findings in vigil-watch README before any production-mapping code lands.
- [ ] Verdict assigned per D-06 pragmatic-fallback rule (one of 5 allowed values).
- [ ] 8-row observed mapping table replaces spec's assumed table.
- [ ] 4 spec-flagged questions answered with field paths.
- [ ] Downstream phase impact captured (no shifts OR explicit per-fallback guidance).
- [ ] ≥8 hand-sanitized excerpts in appendix.
- [ ] Verification log stayed gitignored throughout — no user content reached the public repo.
- [ ] Commit references VERIFY-01; push to origin/main succeeded.
</success_criteria>

<output>
After completion, create `.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-03-SUMMARY.md` documenting:
- Final Verdict value + one-paragraph rationale
- Mapping-table status counts (e.g., "5 Confirmed, 2 Corrected, 1 Missing")
- For each Corrected row: spec wording → observed wording
- For each Missing row: rationale for D-06 pragmatic-fallback decision
- Spec-flagged-question answers in 1–2 sentences each
- Downstream Phase 122/123 shifts (if any) — copied verbatim for the orchestrator's milestone trace
- Excerpt count + sanitization summary (how many excerpts, which redaction rules fired)
- Pushed commit SHA on origin/main
- Public URL: https://github.com/dttigers/vigil-watch#day-1-jsonl-schema-verification
</output>
