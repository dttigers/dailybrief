---
phase: 120-day-1-jsonl-schema-verification-detection-strategy-lock
plan: 02
type: execute
wave: 2
depends_on:
  - "120-01"
files_modified:
  - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md
  - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md
  - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/
autonomous: false
requirements:
  - VERIFY-01

must_haves:
  truths:
    - "User can read a `corpus-mining.md` log that names ≥3 distinct JSONL files from `~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/`, lists every distinct top-level `type` value observed across them with frequency counts, and quotes ≥1 verbatim example line for each distinct type."
    - "User can read a `scripted-session.md` log capturing the 5 D-02 scenarios in order — (a) tool-approval prompt, (b) successful tool result, (c) errored tool result (forced), (d) ≥60s idle gap, (e) clean session end — each scenario annotated with the human action taken and the JSONL line range it produced."
    - "Every claim about JSONL structure made in either log is grounded in a quoted, line-numbered excerpt from a real `*.jsonl` file under `~/.claude/projects/`."
    - "The verification log is gitignored (per Plan 120.01's `/verification-log/` rule) and therefore NEVER pushed to the public repo, but lives in the local clone working tree where Plan 120.03 can read it."
    - "On iMac (Morrill House) only, per D-07; MacBook Pro re-verification deferred to Phase 122/123."
  artifacts:
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md"
      provides: "Full inventory of distinct JSONL line types observed in the existing on-disk corpus"
      contains: "## Distinct top-level `type` values observed"
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md"
      provides: "5-scenario observation log with timestamps and line ranges"
      contains: "### Scenario A: tool-approval prompt"
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/"
      provides: "Directory of raw JSONL excerpt files preserved verbatim"
      contains: "(directory contains *.jsonl excerpt files referenced by line range from the two logs)"
  key_links:
    - from: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md"
      to: "~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl"
      via: "explicit file path + line-range citations in markdown"
      pattern: "\\.jsonl.*lines? [0-9]+"
    - from: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md"
      to: "the new JSONL file produced by the user's scripted Claude Code session"
      via: "filename + scenario-to-line-range mapping table"
      pattern: "Scenario [A-E]"
    - from: "Plan 120.03 (next wave)"
      to: "this verification log"
      via: "Plan 120.03's read_first list"
      pattern: "verification-log/(corpus-mining|scripted-session)\\.md"
---

<objective>
Capture the empirical JSONL data that Plan 120.03 will synthesize into the findings document. Two collection passes per D-01:

1. **Corpus mining (autonomous):** read existing `~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl` files (4+ files, including the active session) and inventory every distinct top-level line type with frequency counts and exemplar excerpts. This is the cheap, large-natural-sample signal per D-01.

2. **Scripted session (NOT autonomous — requires user):** the user opens a fresh Claude Code session in VS Code on iMac and walks through 5 specific scenarios in sequence per D-02 while the executor agent tails the new JSONL file and annotates the line ranges produced by each scenario. The executor agent CANNOT start a Claude Code session inside its own session and produce meaningful "tool-approval prompt" lines for it — only the user, in a separate VS Code window, can.

Purpose: VERIFY-01 demands the verification gate be empirical. The findings doc Plan 120.03 writes is only as good as the raw data this plan captures. D-06's pragmatic-fallback rule depends on this plan reliably observing whether each of the 5 Vigil event types has any JSONL signal at all.

Output: A `verification-log/` directory inside the local vigil-watch clone (gitignored per Plan 120.01) containing two markdown logs and a directory of raw JSONL excerpts. This directory is read-only input to Plan 120.03 — it is NEVER committed to the public repo, but its content is the evidence basis for everything Plan 120.03 puts in the public README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md
@.planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md
@.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-01-SUMMARY.md

<interfaces>
<!-- Live filesystem "interfaces" — these paths must be reachable at execution time. -->

Primary corpus directory (D-01, D-07 — iMac only):
~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/
  └── *.jsonl  (≥4 files at planning time per CONTEXT.md, including the active session)

Secondary corpus (D-01 — for cross-project schema sanity check):
~/.claude/projects/-Users-jamesonmorrill/
~/.claude/projects/-Users-jamesonmorrill-Documents-GitHub-ADHD-Needs-App/
~/.claude/projects/-Users-jamesonmorrill-Documents-GitHub-BrickRebuilder/
~/.claude/projects/-Users-jamesonmorrill-Documents-GitHub-budget-tracker/

Output destination (gitignored per Plan 120.01's `/verification-log/` rule):
/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/
  ├── corpus-mining.md
  ├── scripted-session.md
  └── excerpts/
      ├── corpus-<short-hash>-<line-range>.jsonl  (raw extracts)
      └── scripted-<scenario-id>-<line-range>.jsonl
</interfaces>

<spec_reference>
<!-- The 5 Vigil event types this verification must find signal for (per D-06 pragmatic-fallback rule). Spec table § "Expected JSONL line types": -->

| Vigil event | Spec-assumed JSONL signal |
|---|---|
| `needs_input` | `tool_use` line with `awaiting_approval` |
| `task_complete` | `session_end` or `stop` line |
| `task_failed` | `tool_result` line with error structure |
| `milestone` | regex match in any text field (user-defined) |
| `heartbeat` | absence of new lines for ≥60s while running |

Spec-flagged questions (4) Plan 120.03 will answer using this plan's output:
1. How do tool-approval prompts appear in JSONL?
2. What fields indicate "awaiting input"?
3. How is session end signaled?
4. What is the structure of an errored tool result?
</spec_reference>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Corpus mining — inventory existing ~/.claude/projects/ JSONL files</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/corpus-*.jsonl
  </files>
  <read_first>
    - .planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md (D-01 ordering: corpus first, scripted second; D-07 iMac only)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md §"Expected JSONL line types" (the 8-row assumed mapping table this corpus must be measured against)
    - The two living JSONL files most recently modified in `~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/` (use `ls -lt` to identify, then read up to ~500 lines from each — DO NOT slurp entire files; they may be very large)
  </read_first>
  <action>
    Per D-01 (corpus-first ordering) and D-07 (iMac only), do NOT touch MacBook Pro paths. Steps:

    **Step 1: Inventory the primary corpus directory.**
    ```
    ls -lt ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl
    ```
    Capture: total count of files, mtime range (oldest → newest), total size.

    **Step 2: Identify distinct top-level `type` values across the primary corpus.**
    JSONL = one JSON object per line. The "type" field is the primary discriminator the spec assumes. Run (do NOT use `cat`/`head`/`tail` — use a single bash pipeline that streams):
    ```
    cd ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief
    for f in *.jsonl; do
      jq -r 'try .type // "<NO_TYPE_FIELD>"' "$f" 2>/dev/null
    done | sort | uniq -c | sort -rn
    ```
    Record the full output verbatim into `corpus-mining.md` under the section `## Distinct top-level \`type\` values observed`. If `jq` is missing, fall back to:
    ```
    for f in *.jsonl; do
      python3 -c "import json,sys
    for line in open('$f'):
      try: print(json.loads(line).get('type','<NO_TYPE_FIELD>'))
      except: print('<PARSE_ERROR>')"
    done | sort | uniq -c | sort -rn
    ```

    **Step 3: For each distinct `type` value found, extract one verbatim exemplar line + 2 lines of surrounding context.**
    For each type T:
      - Find a representative line: `grep -n "\"type\":\"$T\"" *.jsonl | head -1` → gives `<file>:<lineNo>:<line>`.
      - Use the Read tool with offset=lineNo-2, limit=5 to capture the exemplar plus context.
      - Save the raw 5-line block to `verification-log/excerpts/corpus-<short-hash>-<typeName>-<lineRange>.jsonl` where `<short-hash>` is the first 8 chars of the source filename. (Use Write tool — destination is inside the local clone, gitignored.)
      - Reference the saved excerpt path from `corpus-mining.md` using a markdown link.

    **Step 4: Specifically search for spec-flagged tokens** (these answer Plan 120.03's 4 questions). For each token, run a grep across the primary corpus and record the first 5 hits with file + line + line content. If a token has 0 hits in the primary corpus, mark it as `not observed in primary corpus — defer to scripted session`:
      - `awaiting_approval` (spec assumes this triggers `needs_input`)
      - `tool_use` (spec assumes this is the parent type for approvals)
      - `tool_result` (spec assumes this carries success/error)
      - `is_error` (common Anthropic SDK field for tool errors — check)
      - `error` (broader)
      - `session_end` (spec assumes this signals clean stop)
      - `stop_reason` (Anthropic SDK convention — check)
      - `result` (some Claude Code variants use top-level `result`)
      - `summary` (post-session summarization)
      - `subtype` (line-type discriminator below `type`, common in agentic JSONL)

    Command form (per token, do not concatenate):
    ```
    grep -nH "awaiting_approval" ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl | head -5
    ```
    Record each token's hit count + sample under `## Spec-flagged token search` in corpus-mining.md.

    **Step 5: Cross-project sanity check (one quick read of a secondary corpus).**
    Pick the `~/.claude/projects/-Users-jamesonmorrill/` directory's most-recently-modified JSONL (or skip if empty). Run the same Step 2 type-distribution one-liner against just that one file. Compare the type distribution to the primary corpus — note in `corpus-mining.md` whether the type vocabulary matches (host-independent schema, per D-07's assumption) or diverges (worth flagging for Plan 120.03).

    **Step 6: Write `corpus-mining.md`** with this exact structure:
    ```markdown
    # Corpus Mining Log — Phase 120 Day-1 Verification

    **Captured:** <ISO-8601 timestamp at execution>
    **Host:** <output of `hostname`>
    **Primary corpus:** ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/
    **File count:** <N>
    **mtime range:** <oldest> → <newest>
    **Total size:** <bytes>

    ## Files inventoried

    | Filename (truncated) | Size | Lines | Last modified |
    |---|---|---|---|
    | <short-hash>...jsonl | <bytes> | <line count> | <mtime> |
    | (one row per file in primary corpus) |

    ## Distinct top-level `type` values observed

    ```
    <verbatim output of Step 2 sort/uniq pipeline>
    ```

    ## Exemplar per distinct `type`

    ### Type: `<typeName>` (count: <N>)
    Source: [excerpts/corpus-<short-hash>-<typeName>-<lineRange>.jsonl](excerpts/corpus-<short-hash>-<typeName>-<lineRange>.jsonl)

    ```jsonl
    <5-line context block from Step 3>
    ```

    (... one section per distinct type ...)

    ## Spec-flagged token search

    | Token | Total hits in primary corpus | First file:line where seen |
    |---|---|---|
    | awaiting_approval | <N> | <file>:<line> or "not observed" |
    | tool_use | <N> | ... |
    | tool_result | <N> | ... |
    | is_error | <N> | ... |
    | error | <N> | ... |
    | session_end | <N> | ... |
    | stop_reason | <N> | ... |
    | result | <N> | ... |
    | summary | <N> | ... |
    | subtype | <N> | ... |

    ### Token sample excerpts (5 hits per token, where present)

    #### `awaiting_approval`

    ```
    <verbatim grep -nH output, top 5 hits>
    ```

    (... one block per token with ≥1 hit ...)

    ## Cross-project sanity check (D-07 assumption)

    **Secondary file used:** <path>
    **Secondary file type distribution:**
    ```
    <output>
    ```
    **Verdict on host-independence assumption:** [matches primary | diverges in <ways>]

    ## Gap list — what corpus mining did NOT cover

    Items the scripted session (Task 2) MUST capture because the primary corpus lacks them:
    - <e.g., "no `awaiting_approval` lines found — Scenario A in scripted session must produce this">
    - <e.g., "no errored tool_result observed — Scenario C must force one">
    - <e.g., "no session_end / stop line — Scenario E must trigger this">
    ```

    Use the Write tool for the markdown file. Use the Write tool for each excerpt file in `excerpts/`.

    **Step 7: Sanity-confirm gitignore is in effect** (per Plan 120.01 T-120-01 mitigation):
    ```
    cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"
    git status --porcelain | grep verification-log
    ```
    Output MUST be empty. If a `verification-log/...` path appears in `git status`, the .gitignore is wrong and the executor MUST halt before any further writes — user content is at risk of being committed.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" &amp;&amp; test -f verification-log/corpus-mining.md &amp;&amp; test -d verification-log/excerpts &amp;&amp; grep -q "^## Distinct top-level" verification-log/corpus-mining.md &amp;&amp; grep -q "^## Spec-flagged token search$" verification-log/corpus-mining.md &amp;&amp; grep -q "^## Gap list" verification-log/corpus-mining.md &amp;&amp; [ "$(ls verification-log/excerpts/corpus-*.jsonl 2>/dev/null | wc -l | tr -d ' ')" -ge 3 ] &amp;&amp; [ -z "$(git status --porcelain | grep verification-log)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `verification-log/corpus-mining.md` exists and is non-empty.
    - File contains the verbatim section headers `## Distinct top-level \`type\` values observed`, `## Exemplar per distinct \`type\``, `## Spec-flagged token search`, `## Cross-project sanity check (D-07 assumption)`, `## Gap list — what corpus mining did NOT cover` (verifiable with `grep -q "^## Distinct top-level" verification-log/corpus-mining.md` etc.).
    - The "Files inventoried" table has ≥3 rows of real filenames from `~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/` (count rows after the header+separator: `awk '/^\| /{c++} END{print c}' verification-log/corpus-mining.md` should report a value consistent with multiple inventoried files).
    - Spec-flagged-token table has exactly 10 rows for the 10 tokens listed (one row per token, even if hit count is 0).
    - `verification-log/excerpts/` directory contains ≥3 `corpus-*.jsonl` files (one per distinct observed type, minimum).
    - Each excerpt referenced by a markdown link in `corpus-mining.md` resolves to an actual file on disk (executor verifies by listing then grep-cross-checking).
    - `git status --porcelain | grep verification-log` returns empty (gitignore is working — user content NOT staged).
    - File explicitly states host: `grep -q "^\*\*Host:\*\*" verification-log/corpus-mining.md` matches.
  </acceptance_criteria>
  <done>Corpus inventory log committed to local working tree (gitignored, NOT pushed), with ≥3 exemplar excerpts on disk, all 10 spec-flagged tokens searched and tabulated, and a Gap List naming exactly which scenarios the scripted-session task must cover. The cross-project sanity check confirms or flags the D-07 host-independence assumption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Scripted Claude Code session (5 D-02 scenarios) + tail capture</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/scripted-session.md
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/scripted-*.jsonl
  </files>
  <read_first>
    - .planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-CONTEXT.md (D-02 — exact 5 scenarios in sequence)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/corpus-mining.md (the Gap List — scenarios MUST cover gaps the corpus did not)
    - .planning/v3.8-CLAUDE-CODE-COMPANION-SPEC.md §"Day-1 verification gate (BLOCKING)"
  </read_first>
  <action>
    Why this is `checkpoint:human-verify`: the executor agent is itself a Claude Code session. It cannot, from inside its own session, produce a fresh `tool_use` `awaiting_approval` line in a separate VS Code window's JSONL — only the user can drive that other window. The executor's role here is (1) provide the user a precise runbook, (2) tail the newly-created JSONL file in real time during the user's session, (3) annotate the captured line ranges, (4) wait for human confirmation that all 5 scenarios completed before producing the log.

    **Pre-flight (executor, autonomous before checkpoint):**

    1. Snapshot the current state of the primary corpus directory — list all `*.jsonl` files with their line counts:
       ```
       wc -l ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl > /tmp/vigil-watch-corpus-pre.txt
       ```
       This baseline lets us identify which file the user's NEW session writes to (will be the file that didn't exist before, or had 0 lines).

    2. Print this exact runbook to stdout for the user (this becomes the prompt block in the checkpoint):

       ```
       === SCRIPTED SESSION RUNBOOK ===
       I will tail ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/ for new JSONL files.
       Please do the following IN A FRESH VS CODE WINDOW (NOT this terminal/session):

       1. Open VS Code on iMac (Morrill House).
       2. Open the dailybrief workspace folder.
       3. Start a NEW Claude Code session (Cmd+Shift+P → "Claude: New Conversation"
          OR equivalent for your VS Code Claude extension version).
       4. Note the SESSION START TIME (so I can match it to the JSONL filename mtime).
       5. Run the 5 SCENARIOS BELOW IN ORDER. Wait for each one to fully complete (or
          for the prompt described) before moving to the next.

       SCENARIO A — Tool-approval prompt:
         - Type EXACTLY: "Please run `ls -la ~/Desktop` and tell me how many items are there."
         - Claude will request to use the Bash tool. STOP at the approval prompt.
         - DO NOT approve yet. Wait ~10 seconds (so the awaiting_approval state is
           clearly captured in the JSONL).
         - Then APPROVE the tool call.

       SCENARIO B — Successful tool result:
         - The previous Bash call should now run and return successfully.
         - Wait for Claude to respond with a count.
         - This produces a tool_result (success) line.

       SCENARIO C — Errored tool result (forced):
         - Type EXACTLY: "Please run `ls /this/path/definitely/does/not/exist/<random>` and report what happens."
         - Approve the tool call.
         - The Bash tool will return a non-zero exit / stderr.
         - Wait for Claude to acknowledge the error.
         - This produces a tool_result with error structure.

       SCENARIO D — ≥60s idle:
         - DO NOTHING for at least 70 seconds (set a timer).
         - Do NOT type, click, or interact with the Claude Code window.
         - This produces a "no new lines for ≥60s" gap — testing the heartbeat
           detection threshold.

       SCENARIO E — Clean session end:
         - Type: "Thanks, that's all for now."
         - Wait for Claude's reply.
         - Close the conversation cleanly via the VS Code Claude extension's
           "End conversation" / "Close session" affordance (NOT by force-quitting
           VS Code — we want the clean-stop signal, whatever it is).

       When all 5 scenarios are done, type "scenarios complete <session-start-time>"
       in the executor terminal.
       ```

    3. While the user is performing the scenarios (this is the human-verify wait
       state — see resume-signal below), the executor agent is paused. Do NOT
       attempt to run a tail in the background; wait for the human-resume signal.

    **Post-checkpoint (executor, autonomous after user confirms):**

    4. Identify the new session's JSONL file:
       ```
       wc -l ~/.claude/projects/-Users-jamesonmorrill-Desktop-Local-AI-dailybrief/*.jsonl > /tmp/vigil-watch-corpus-post.txt
       diff /tmp/vigil-watch-corpus-pre.txt /tmp/vigil-watch-corpus-post.txt
       ```
       The diff reveals: (a) any newly-created file, (b) any file whose line count grew. If a brand-new file appeared, that's the scripted-session file. If only an existing file grew, ask the user (followup checkpoint not needed — usually a new file appears for a new session).

    5. Read the new session's JSONL file in full. For each scenario A–E, identify
       the line range that corresponds to it by aligning the user-provided session
       start time against `timestamp` fields in the JSONL (most Claude Code JSONL
       lines carry a timestamp). Build a mapping table:
       ```
       | Scenario | Line range | Approx. timestamp | Top-level types observed |
       |---|---|---|---|
       | A: tool-approval prompt | <a>-<b> | <iso> | <list> |
       | B: successful tool result | <c>-<d> | ... | ... |
       | C: errored tool result | <e>-<f> | ... | ... |
       | D: ≥60s idle | n/a (silence between line <g> and line <h>) | gap = <seconds>s | n/a |
       | E: clean session end | <i>-<j> | ... | ... |
       ```

    6. For each scenario, save the corresponding line range to a separate excerpt file:
       `verification-log/excerpts/scripted-A-tool-approval-lines<a>-<b>.jsonl`
       `verification-log/excerpts/scripted-B-tool-result-success-lines<c>-<d>.jsonl`
       `verification-log/excerpts/scripted-C-tool-result-error-lines<e>-<f>.jsonl`
       `verification-log/excerpts/scripted-D-idle-context-lines<g-1>-<h+1>.jsonl` (capture 1 line before + 1 line after the silence gap)
       `verification-log/excerpts/scripted-E-session-end-lines<i>-<j>.jsonl`

       Use Read with offset/limit per range, then Write each excerpt verbatim.

    7. Write `verification-log/scripted-session.md` with this exact structure:
       ```markdown
       # Scripted Session Verification Log — Phase 120 Day-1

       **Captured:** <ISO-8601 timestamp at execution>
       **Host:** <output of `hostname`>
       **Session-start time (user-reported):** <user-provided>
       **JSONL file produced:** <full path>
       **Total lines in new file:** <N>
       **VS Code Claude extension version:** <if user volunteered; else "not captured">

       ## Scenario-to-line-range map

       | Scenario | Line range | Approx. timestamp | Top-level types observed | Excerpt file |
       |---|---|---|---|---|
       | A: tool-approval prompt | <a>-<b> | <iso> | <types> | [link](excerpts/scripted-A-...) |
       | B: successful tool result | ... | ... | ... | ... |
       | C: errored tool result | ... | ... | ... | ... |
       | D: ≥60s idle | (gap line<g>→line<h>, dur=<s>s) | ... | n/a | [link](excerpts/scripted-D-...) |
       | E: clean session end | ... | ... | ... | ... |

       ## Per-scenario observations

       ### Scenario A: tool-approval prompt
       **What the user did:** typed "Please run `ls -la ~/Desktop` ..."
       **JSONL line range:** <a>-<b>
       **Distinct top-level types in this range:** <list>
       **Spec-flagged tokens present:** <e.g., "awaiting_approval ✓ (line <n>)" or "awaiting_approval ✗ — see field <X> instead">
       **Raw excerpt:** [excerpts/scripted-A-...](excerpts/scripted-A-...)

       ### Scenario B: successful tool result
       (same shape)

       ### Scenario C: errored tool result
       (same shape — pay special attention to error-carrying field path: `is_error`, `error`, nested in `content[].`, etc.)

       ### Scenario D: ≥60s idle
       **What the user did:** waited <s> seconds without interacting.
       **Line BEFORE silence:** line <g>, timestamp <iso>
       **Line AFTER silence:** line <h>, timestamp <iso>
       **Measured silence duration:** <s> seconds (from timestamp delta)
       **Note:** there is no JSONL line emitted during silence — `heartbeat` will be inferred from absence per spec § "Detection rules". This scenario confirms whether sessions actually go quiet (vs. emitting periodic keepalives we'd need to filter).

       ### Scenario E: clean session end
       (same shape)

       ## Per-question evidence pointers (for Plan 120.03)

       Each of the 4 spec-flagged questions, with the most relevant excerpt(s) Plan 120.03 should cite:

       1. **How do tool-approval prompts appear in JSONL?** → Scenario A excerpt + line <n> (`awaiting_approval` or equivalent field path).
       2. **What fields indicate "awaiting input"?** → Scenario A excerpt; specifically observe field `<path>` with value `<value>`.
       3. **How is session end signaled?** → Scenario E excerpt; observe top-level `type` = `<value>` and/or stop_reason field.
       4. **What is the structure of an errored tool result?** → Scenario C excerpt; observe error-carrying fields `<paths>`.

       ## Pragmatic-fallback assessment (input to Plan 120.03 verdict)

       Per D-06, we escalate to a fallback path ONLY if the JSONL fundamentally lacks signal for one or more of the 5 Vigil event types. Initial assessment (Plan 120.03 makes the final call):

       | Vigil event | Has JSONL signal? | Evidence |
       |---|---|---|
       | needs_input | yes / no / partial | Scenario A line <n> |
       | task_complete | yes / no / partial | Scenario E line <n> |
       | task_failed | yes / no / partial | Scenario C line <n> |
       | milestone | yes / partial | Any scenario — text fields are searchable per spec |
       | heartbeat | yes / no | Scenario D — silence is observable from timestamp gaps |

       **Tentative verdict (subject to Plan 120.03 review):** [proceed-as-spec | spec-correct-and-proceed | fallback-path-A | fallback-path-B | fallback-path-C]
       ```

    8. Confirm gitignore is still working:
       ```
       cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch"
       git status --porcelain | grep verification-log
       ```
       MUST be empty. If not, halt — DO NOT push.
  </action>
  <what-built>
    A scripted-session log capturing JSONL output from 5 user-driven scenarios (tool-approval prompt, successful tool result, errored tool result, ≥60s idle, clean session end), with per-scenario raw excerpt files and a tentative pragmatic-fallback assessment for Plan 120.03 to finalize.
  </what-built>
  <how-to-verify>
    Before resuming, the user verifies in their own VS Code window:
    1. They actually ran a NEW Claude Code session (not the executor's session).
    2. All 5 scenarios A–E were performed in order, with the ≥60s idle (D) actually being ≥60 seconds.
    3. Scenario A produced a real tool-approval prompt that was paused before approval (~10s gap is visible in timestamps).
    4. Scenario C produced a real tool-result with non-zero exit / error.
    5. Scenario E ended via the clean "End conversation" affordance, NOT a VS Code force-quit.
    6. They report back the session-start time (HH:MM:SS local) so the executor can match it to the new JSONL file's timestamps.

    After resume, the user verifies the executor produced:
    - A `scripted-session.md` whose scenario→line-range table aligns with their memory of when each scenario happened.
    - 5 excerpt files in `excerpts/scripted-*.jsonl`, each containing real lines from the new session JSONL.
    - The "Tentative verdict" line is one of the 5 allowed strings.
  </how-to-verify>
  <resume-signal>
    Type: `scenarios complete <session-start-time>` (e.g., `scenarios complete 14:32:08`).
    Or describe issues: `scenario C did not error — Bash returned 0` / `couldn't find a clean session-end affordance` / `extension wrote no new file in ~/.claude/projects/dailybrief/` / etc.
  </resume-signal>
  <acceptance_criteria>
    - File `verification-log/scripted-session.md` exists and is non-empty.
    - File contains all 5 scenario subsection headers verbatim: `### Scenario A: tool-approval prompt`, `### Scenario B: successful tool result`, `### Scenario C: errored tool result`, `### Scenario D: ≥60s idle`, `### Scenario E: clean session end` (each verifiable with `grep -q "^### Scenario [A-E]:" verification-log/scripted-session.md`).
    - Scenario-to-line-range table has exactly 5 data rows (A through E).
    - Per-question evidence pointers section has exactly 4 numbered items, each citing a specific scenario and line number.
    - File `verification-log/scripted-session.md` contains a "Tentative verdict" line whose value is one of the 5 strings: `proceed-as-spec`, `spec-correct-and-proceed`, `fallback-path-A`, `fallback-path-B`, `fallback-path-C` (verified by `grep -E "Tentative verdict.*(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])" verification-log/scripted-session.md`).
    - `verification-log/excerpts/` directory contains exactly 5 files matching `scripted-[A-E]-*.jsonl` (one per scenario, plus the idle-context capture for D).
    - Each excerpt file is non-empty and contains valid JSONL (each line, when piped through `jq .`, parses without error — except possibly the idle-context capture which may include EOF blank lines; allow up to 1 empty trailing line).
    - The Scenario D entry contains a measured silence duration ≥60 seconds (`grep -E "Measured silence duration: [6-9][0-9]|[1-9][0-9][0-9]+ seconds" verification-log/scripted-session.md` matches).
    - `git status --porcelain | grep verification-log` returns empty (verification-log/ is still gitignored — user content not staged).
    - The "JSONL file produced" path in the log corresponds to a file that exists on disk (executor confirms with `test -f` on the value extracted from the markdown).
  </acceptance_criteria>
  <done>5 scenarios completed in a fresh user-driven Claude Code session; 5 excerpt files saved; per-scenario observations recorded; per-question evidence pointers ready for Plan 120.03; tentative pragmatic-fallback verdict assigned; verification-log/ stays gitignored throughout.</done>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" &amp;&amp; test -f verification-log/scripted-session.md &amp;&amp; grep -q "^### Scenario A: tool-approval prompt$" verification-log/scripted-session.md &amp;&amp; grep -q "^### Scenario B: successful tool result$" verification-log/scripted-session.md &amp;&amp; grep -q "^### Scenario C: errored tool result$" verification-log/scripted-session.md &amp;&amp; grep -q "^### Scenario D: ≥60s idle$" verification-log/scripted-session.md &amp;&amp; grep -q "^### Scenario E: clean session end$" verification-log/scripted-session.md &amp;&amp; grep -E "Tentative verdict.*(proceed-as-spec|spec-correct-and-proceed|fallback-path-[ABC])" verification-log/scripted-session.md &amp;&amp; [ "$(ls verification-log/excerpts/scripted-[A-E]-*.jsonl 2>/dev/null | wc -l | tr -d ' ')" -eq 5 ] &amp;&amp; [ -z "$(git status --porcelain | grep verification-log)" ]</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.claude/projects/` (user's working sessions) → vigil-watch local working tree | Raw JSONL files contain user prompts, code being worked on, AI responses — full PII surface. Anything copied into the local clone must NOT be pushed to the public repo. |
| local working tree → github.com (public) | `git push` would expose any committed user content to the world. |
| executor's session → fresh user VS Code session | Two distinct Claude Code processes; executor cannot drive the user's session for it. |
| fresh user VS Code session → newly-written JSONL file | The user's typed prompts and Claude's responses are written to disk in plaintext — this is the data we're sampling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-06 | I (Information disclosure) | verification-log/excerpts/ files | mitigate | Plan 120.01 already added `/verification-log/` to `.gitignore`. Both tasks in this plan re-confirm with `git status --porcelain \| grep verification-log` returning empty before completing. If gitignore fails, executor halts before any push attempt. |
| T-120-07 | I (Information disclosure) | scripted session content (Scenario A: `ls ~/Desktop`) | mitigate | Runbook deliberately uses non-sensitive shell commands (`ls -la ~/Desktop`, `ls /this/path/does/not/exist/<random>`). The user is the source of typed content; runbook does not solicit secrets. Plan 120.03 must hand-pick excerpts (not bulk copy) when promoting to the public README. |
| T-120-08 | I (Information disclosure) | corpus-mining excerpts | mitigate | Excerpts are limited to 5 lines of context per type — large enough to confirm structure, small enough to minimize incidental PII surface. Files live in gitignored `verification-log/excerpts/`, never pushed. Plan 120.03 makes the curation call when copying selected excerpts into the public README appendix. |
| T-120-09 | T (Tampering) | misidentifying which JSONL belongs to the scripted session | mitigate | Pre-flight baseline (`wc -l ... > pre.txt`) followed by post-flight diff makes the new/grown file unambiguous. If the diff is empty (impossible if the user actually started a session), executor halts and asks user to verify the extension is writing files. |
| T-120-10 | R (Repudiation) | timestamps on excerpts not matching user's reported session-start | accept | Best-effort alignment via JSONL `timestamp` field deltas; misalignment within a few seconds is tolerable for Plan 120.03's downstream analysis. If wildly off, the user's resume-signal description will reveal it. |
| T-120-11 | D (Denial of service) | very large JSONL files (active session may be 100k+ lines) | mitigate | Step 2 of Task 1 streams via shell pipeline (no in-memory slurp). Reads of individual lines for exemplar capture use Read tool with explicit offset+limit, never reading entire files. |
</threat_model>

<verification>
**Plan-level checks:**
- `verification-log/corpus-mining.md` exists with all 5 required sections + 10-row token table + ≥3 inventoried files.
- `verification-log/scripted-session.md` exists with all 5 scenario sections + 5-row scenario-to-line-range table + 4 per-question evidence pointers + tentative verdict.
- `verification-log/excerpts/` contains ≥3 corpus-* files + exactly 5 scripted-[A-E]-* files.
- `git status --porcelain | grep verification-log` returns empty (gitignore working — no user content staged or pushed).
- All claims in both logs are grounded in either a quoted excerpt file or a `<file>:<line>` citation.
</verification>

<success_criteria>
- [ ] Corpus mining inventoried ≥3 JSONL files from primary corpus directory.
- [ ] All 10 spec-flagged tokens searched and tabulated with hit counts.
- [ ] ≥3 distinct top-level `type` values observed and exemplar excerpts saved.
- [ ] Scripted session covered all 5 scenarios A–E in order, on iMac per D-07.
- [ ] Scenario D measured silence ≥60 seconds confirmed by timestamp delta.
- [ ] 5 scripted-* excerpt files saved, each containing valid JSONL.
- [ ] Per-question evidence pointers prepared for Plan 120.03.
- [ ] Tentative verdict line present with one of the 5 allowed values.
- [ ] verification-log/ stays gitignored throughout — no user content reaches the public repo.
</success_criteria>

<output>
After completion, create `.planning/phases/120-day-1-jsonl-schema-verification-detection-strategy-lock/120-02-SUMMARY.md` documenting:
- Total files mined from primary corpus + counts of distinct top-level `type` values observed
- 10-row spec-flagged token search outcome (which tokens appeared, which did not)
- 5-scenario completion status + new JSONL filename produced
- Per-question evidence map (which scenario/line pairs Plan 120.03 should cite for each of the 4 spec-flagged questions)
- Tentative pragmatic-fallback verdict (per D-06)
- Confirmation gitignore prevented any verification-log content from being staged
</output>
