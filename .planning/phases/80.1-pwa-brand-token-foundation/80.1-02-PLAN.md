---
phase: 80.1-pwa-brand-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["80.1-01"]
files_modified:
  - vigil-pwa/src/components/Layout.tsx
  - vigil-pwa/src/components/CaptureBar.tsx
  - vigil-pwa/src/components/ThoughtRow.tsx
  - vigil-pwa/src/components/ThoughtList.tsx
  - vigil-pwa/src/components/ThoughtAssignmentRow.tsx
  - vigil-pwa/src/components/WorkOrderRow.tsx
  - vigil-pwa/src/components/CategoryTabs.tsx
  - vigil-pwa/src/components/FilterBar.tsx
  - vigil-pwa/src/components/SearchBar.tsx
  - vigil-pwa/src/components/BulkActionBar.tsx
  - vigil-pwa/src/components/ProjectCard.tsx
  - vigil-pwa/src/components/AudioUploadSection.tsx
autonomous: true
requirements:
  - SC-03
  - SC-04
  - SC-05

must_haves:
  truths:
    - "No component file contains slate-, indigo-, bg-blue-, or text-blue- Tailwind classes"
    - "No component file contains font-semibold or font-bold"
    - "Category tags use rounded-full (pill shape) with brand colors"
    - "Therapy classification tags use rounded (square badge) instead of rounded-full"
  artifacts:
    - path: "vigil-pwa/src/components/ThoughtRow.tsx"
      provides: "Brand-compliant category tags (pill) and therapy tags (square badge)"
      contains: "bg-info-50 text-info-400"
    - path: "vigil-pwa/src/components/Layout.tsx"
      provides: "Brand nav with teal active indicator"
      contains: "border-teal-600"
    - path: "vigil-pwa/src/components/CaptureBar.tsx"
      provides: "Brand primary button"
      contains: "bg-teal-600"
  key_links:
    - from: "vigil-pwa/src/components/ThoughtRow.tsx"
      to: "vigil-pwa/src/index.css @theme tokens"
      via: "Tailwind utility classes referencing brand tokens"
      pattern: "bg-teal-|text-teal-|bg-info-|text-info-"
---

<objective>
Migrate all 12 PWA component files from hardcoded slate/indigo/blue Tailwind classes to Vigil brand tokens, fix font-weight violations, and correct tag shapes.

Purpose: Components are the building blocks of every page. Migrating them first ensures every page inherits brand-compliant styling through composition.

Output: All 12 component files use only brand token classes. Zero slate/indigo/blue classes remain. All font weights are 400 or 500 only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80.1-pwa-brand-token-foundation/80.1-RESEARCH.md
@docs/BRAND-GUIDELINES.md

<interfaces>
<!-- Brand token classes available from Plan 01's @theme block in index.css -->
<!-- Teal: bg-teal-50, bg-teal-100, bg-teal-400, bg-teal-600, bg-teal-800 (and text-, border- variants) -->
<!-- Gray: bg-gray-50, bg-gray-100, bg-gray-400, bg-gray-900 (and text-, border- variants) -->
<!-- Surface: bg-surface -->
<!-- Status: bg-info-50, bg-info-400, bg-warning-50, bg-warning-400, bg-success-50, bg-success-400 -->
<!-- Font: Inter is the default font-sans -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Layout, CaptureBar, SearchBar, FilterBar, CategoryTabs, BulkActionBar components</name>
  <files>
    vigil-pwa/src/components/Layout.tsx,
    vigil-pwa/src/components/CaptureBar.tsx,
    vigil-pwa/src/components/SearchBar.tsx,
    vigil-pwa/src/components/FilterBar.tsx,
    vigil-pwa/src/components/CategoryTabs.tsx,
    vigil-pwa/src/components/BulkActionBar.tsx
  </files>
  <read_first>
    - vigil-pwa/src/components/Layout.tsx
    - vigil-pwa/src/components/CaptureBar.tsx
    - vigil-pwa/src/components/SearchBar.tsx
    - vigil-pwa/src/components/FilterBar.tsx
    - vigil-pwa/src/components/CategoryTabs.tsx
    - vigil-pwa/src/components/BulkActionBar.tsx
    - docs/BRAND-GUIDELINES.md
  </read_first>
  <action>
Apply the following class replacements across all 6 files. Read each file fully before editing.

**Color replacement table (apply to ALL 6 files):**

| Find | Replace With |
|------|-------------|
| `bg-slate-950` | `bg-gray-900` |
| `bg-slate-900/50` | `bg-gray-900/50` |
| `bg-slate-900` | `bg-gray-900` |
| `bg-slate-800/50` | `bg-gray-900/50` |
| `bg-slate-800` | `bg-gray-900/80` |
| `bg-slate-700` | `bg-gray-400/30` |
| `bg-slate-600` | `bg-gray-400/50` |
| `hover:bg-slate-800` | `hover:bg-gray-900/80` |
| `hover:bg-slate-700` | `hover:bg-gray-400/30` |
| `hover:bg-slate-600` | `hover:bg-gray-400/50` |
| `text-slate-100` | `text-gray-50` |
| `text-slate-200` | `text-gray-100` |
| `text-slate-300` | `text-gray-100` |
| `text-slate-400` | `text-gray-400` |
| `text-slate-500` | `text-gray-400` |
| `hover:text-white` | `hover:text-gray-50` |
| `placeholder-slate-500` | `placeholder-gray-400` |
| `border-slate-800` | `border-gray-900/40` |
| `border-slate-700` | `border-gray-400/30` |
| `border-slate-600` | `border-gray-400/30` |
| `hover:border-slate-600` | `hover:border-gray-400` |
| `hover:border-slate-500` | `hover:border-gray-400` |
| `bg-indigo-600` | `bg-teal-600` |
| `bg-indigo-500` | `bg-teal-400` |
| `hover:bg-indigo-500` | `hover:bg-teal-400` |
| `hover:bg-indigo-700` | `hover:bg-teal-800` |
| `border-indigo-500` | `border-teal-600` |
| `focus:ring-indigo-500` | `focus:ring-teal-600` |
| `focus:border-indigo-500` | `focus:border-teal-600` |
| `text-indigo-400` | `text-teal-400` |
| `text-indigo-300` | `text-teal-400` |
| `accent-indigo-500` | `accent-teal-600` |
| `border-b-2 border-indigo-500` | `border-b-2 border-teal-600` |

**Font weight fixes (apply to all 6 files):**
| Find | Replace With |
|------|-------------|
| `font-semibold` | `font-medium` |
| `font-bold` | `font-medium` |

**File-specific notes:**

- **Layout.tsx line 35**: `bg-slate-950` -> `bg-gray-900`, `text-slate-100` -> `text-gray-50`
- **Layout.tsx line 38**: `font-semibold` -> `font-medium`
- **Layout.tsx line 53**: `border-indigo-500 text-white` -> `border-teal-600 text-gray-50`
- **Layout.tsx line 54**: `text-slate-400 hover:text-white hover:border-slate-600` -> `text-gray-400 hover:text-gray-50 hover:border-gray-400`
- **CaptureBar.tsx line 65**: `bg-indigo-600 hover:bg-indigo-500` -> `bg-teal-600 hover:bg-teal-400`
- **CategoryTabs.tsx line 26**: `bg-indigo-600 text-white` -> `bg-teal-600 text-white`
- **CategoryTabs.tsx line 27**: `bg-slate-800 text-slate-300 hover:bg-slate-700` -> `bg-gray-900/80 text-gray-100 hover:bg-gray-400/30`

Do NOT change `text-white` to `text-gray-50` on button text that sits on teal-600 backgrounds — white provides better contrast on teal.
Do NOT change `text-red-*` or `bg-red-*` classes (used for delete/error states — these must remain).
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -rn "slate-\|indigo-\|font-semibold\|font-bold" src/components/Layout.tsx src/components/CaptureBar.tsx src/components/SearchBar.tsx src/components/FilterBar.tsx src/components/CategoryTabs.tsx src/components/BulkActionBar.tsx && echo "FAIL: off-brand classes found" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/Layout.tsx` returns empty
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/CaptureBar.tsx` returns empty
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/SearchBar.tsx` returns empty
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/FilterBar.tsx` returns empty
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/CategoryTabs.tsx` returns empty
    - `grep -r "slate-\|indigo-" vigil-pwa/src/components/BulkActionBar.tsx` returns empty
    - `grep -r "font-semibold\|font-bold" vigil-pwa/src/components/Layout.tsx` returns empty
    - Layout.tsx contains `border-teal-600`
    - CaptureBar.tsx contains `bg-teal-600`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 6 structural/UI-chrome components use brand tokens. No slate, indigo, font-semibold, or font-bold classes remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ThoughtRow, ThoughtList, ThoughtAssignmentRow, WorkOrderRow, ProjectCard, AudioUploadSection + fix tag shapes</name>
  <files>
    vigil-pwa/src/components/ThoughtRow.tsx,
    vigil-pwa/src/components/ThoughtList.tsx,
    vigil-pwa/src/components/ThoughtAssignmentRow.tsx,
    vigil-pwa/src/components/WorkOrderRow.tsx,
    vigil-pwa/src/components/ProjectCard.tsx,
    vigil-pwa/src/components/AudioUploadSection.tsx
  </files>
  <read_first>
    - vigil-pwa/src/components/ThoughtRow.tsx
    - vigil-pwa/src/components/ThoughtList.tsx
    - vigil-pwa/src/components/ThoughtAssignmentRow.tsx
    - vigil-pwa/src/components/WorkOrderRow.tsx
    - vigil-pwa/src/components/ProjectCard.tsx
    - vigil-pwa/src/components/AudioUploadSection.tsx
    - docs/BRAND-GUIDELINES.md
  </read_first>
  <action>
Apply the same color and font-weight replacement table from Task 1 across all 6 files.

**Additional file-specific changes:**

**ThoughtRow.tsx — Category tag colors:**
Replace the CATEGORY_STYLES (or equivalent color map) values:
- `task`: change from `bg-blue-500/20 text-blue-400` to `bg-info-50 text-info-400`
- `therapy`: change to `bg-teal-50 text-teal-600`
- `idea`: change to `bg-warning-50 text-warning-400`
- `reflection`: change to `bg-success-50 text-success-400`
- `project`: change to `bg-teal-50 text-teal-400`
- fallback/uncategorized: change from `bg-slate-700 text-slate-400` to `bg-gray-50 text-gray-400`

**ThoughtRow.tsx — Status colors:**
- `inProgress`: change from `bg-blue-500/20 text-blue-400 hover:bg-blue-500/30` to `bg-info-50 text-info-400 hover:bg-info-50/80`
- Other status colors using blue: replace `blue-*` with `info-*` equivalents

**ThoughtRow.tsx — Therapy classification tag shape (SC-05):**
Find the therapy classification span (around line 149) that uses `rounded-full`. Change ONLY the therapy/routing tag's `rounded-full` to `rounded`. Category tags KEEP `rounded-full`.

The therapy tag looks like:
```tsx
<span className="inline-flex px-2 py-0.5 rounded-full text-xs font-medium ...">
```
Change to:
```tsx
<span className="inline-flex px-2 py-0.5 rounded text-xs font-medium ...">
```

**ThoughtRow.tsx — Checkbox accent:**
- `accent-indigo-500` -> `accent-teal-600`

**ThoughtAssignmentRow.tsx — Category colors:**
- `project`: change from `bg-indigo-900 text-indigo-300` to `bg-teal-50 text-teal-400`
- `task`: change from `bg-blue-900 text-blue-300` to `bg-info-50 text-info-400`
- fallback: change from `bg-slate-700 text-slate-300` to `bg-gray-50 text-gray-400`

**WorkOrderRow.tsx — Priority badge and status:**
- Priority badge: `bg-indigo-500/20 text-indigo-400` -> `bg-teal-50 text-teal-600`
- `font-bold` on WO number badge -> `font-medium`
- Status `inProgress`: `bg-blue-500/20 text-blue-400 hover:bg-blue-500/30` -> `bg-info-50 text-info-400 hover:bg-info-50/80`

**ProjectCard.tsx — Status colors:**
- `archived`: `bg-slate-700 text-slate-400` -> `bg-gray-50 text-gray-400`
- `done`: `bg-blue-900 text-blue-300` -> `bg-info-50 text-info-400`
- Thought count badge: `bg-slate-700 ... text-slate-300` -> `bg-gray-400/30 ... text-gray-100`
- focus ring: `focus:ring-1 focus:ring-indigo-500` -> `focus:ring-1 focus:ring-teal-600`

**AudioUploadSection.tsx:**
- All `bg-indigo-600 hover:bg-indigo-500` -> `bg-teal-600 hover:bg-teal-400`
- All `font-semibold` -> `font-medium` (5 occurrences)
- `hover:border-indigo-500` -> `hover:border-teal-600`
- Keep `text-green-300` and `text-red-300` (status feedback — these are acceptable)

Do NOT change `text-red-*` or `bg-red-*` classes anywhere — these are error/danger states and must remain.
  </action>
  <verify>
    <automated>cd vigil-pwa && grep -rn "slate-\|indigo-\|font-semibold\|font-bold\|bg-blue-\|text-blue-\|border-blue-" src/components/ThoughtRow.tsx src/components/ThoughtList.tsx src/components/ThoughtAssignmentRow.tsx src/components/WorkOrderRow.tsx src/components/ProjectCard.tsx src/components/AudioUploadSection.tsx && echo "FAIL: off-brand classes found" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/ThoughtRow.tsx` returns empty
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/ThoughtList.tsx` returns empty
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/ThoughtAssignmentRow.tsx` returns empty
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/WorkOrderRow.tsx` returns empty
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/ProjectCard.tsx` returns empty
    - `grep -r "slate-\|indigo-\|bg-blue-\|text-blue-" vigil-pwa/src/components/AudioUploadSection.tsx` returns empty
    - `grep -r "font-semibold\|font-bold" vigil-pwa/src/components/AudioUploadSection.tsx` returns empty
    - `grep -r "font-bold" vigil-pwa/src/components/WorkOrderRow.tsx` returns empty
    - ThoughtRow.tsx contains `bg-info-50 text-info-400` (task category)
    - ThoughtRow.tsx contains `bg-teal-50 text-teal-600` (therapy category)
    - ThoughtRow.tsx therapy classification tag uses `rounded` not `rounded-full`
    - ThoughtRow.tsx contains `accent-teal-600`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 6 data-display components use brand tokens. Category tags use pill shape with brand colors. Therapy routing tags use square badge shape. No off-brand classes remain. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only CSS class names in existing components. No new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80.1-03 | N/A | Component class names | accept | Pure CSS class renaming. No logic, data flow, or input handling changes. Zero attack surface. |
</threat_model>

<verification>
1. `cd vigil-pwa && npm run build` succeeds
2. `grep -rn "slate-\|indigo-\|font-semibold\|font-bold\|bg-blue-\|text-blue-\|border-blue-" vigil-pwa/src/components/*.tsx` returns empty
3. `grep -q "rounded text-xs" vigil-pwa/src/components/ThoughtRow.tsx` exits 0 (therapy tag uses square badge)
</verification>

<success_criteria>
- All 12 component files contain zero slate/indigo/blue off-brand classes
- All 12 component files contain zero font-semibold/font-bold violations
- Category tags render with brand colors in pill shape (rounded-full)
- Therapy classification tags render with brand colors in square badge shape (rounded)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/80.1-pwa-brand-token-foundation/80.1-02-SUMMARY.md`
</output>
