---
phase: 999.2-capturebar-multi-line-input-support
plan: 01
subsystem: ui
tags: [pwa, capturebar, textarea, multi-line, react19, vitest, rtl, user-event]

# Dependency graph
requires:
  - phase: 100-edit-mode (EDIT-01)
    provides: keyboard grammar (plain Enter = newline, Cmd/Ctrl+Enter = save, Escape = blur/cancel) in ThoughtRow.tsx:294-308
  - phase: 115-polish (POLISH-01 D-16)
    provides: whitespace-pre-line display rendering on ThoughtRow.tsx:399 — newlines render once data carries them
provides:
  - Multi-line capture entry point — CaptureBar accepts \n via paste, typing, and Cmd/Ctrl+Enter submit
  - Mobile soft-keyboard newline via enterKeyHint="enter"
  - Auto-grow textarea (1→6 lines, then internal scroll) via useLayoutEffect + scrollHeight recipe
  - Single Vigil keyboard grammar across capture and edit (alignment with Phase 100 EDIT-01)
  - CaptureBar.test.tsx — first unit-test coverage for the capture entry point
affects: [future-voice-capture, draft-persistence-phase, triage-prompt-hardening-phase]

# Tech tracking
tech-stack:
  added: []  # zero new packages
  patterns:
    - "scrollHeight auto-grow recipe via useLayoutEffect keyed on input state"
    - "enterKeyHint='enter' for mobile soft-keyboard newline hint"
    - "userEvent.paste / userEvent.keyboard pattern for multi-line capture tests (no fireEvent)"

key-files:
  created:
    - vigil-pwa/src/components/CaptureBar.test.tsx
    - .planning/phases/999.2-capturebar-multi-line-input-support/999.2-UAT.md
  modified:
    - vigil-pwa/src/components/CaptureBar.tsx

key-decisions:
  - "D-01: Cmd/Ctrl+Enter submits; plain Enter inserts newline (mirrors Phase 100 EDIT-01)"
  - "D-03: Auto-grow capped at max-h-36 (~6 lines) with internal scroll past cap"
  - "D-04: Post-submit setInput('') triggers useLayoutEffect → textarea height resets to single-line baseline"
  - "D-05: enterKeyHint='enter' on textarea — mobile return key inserts newline"
  - "D-07/D-09: Existing input.trim() guard and fire-and-forget triage block preserved verbatim"
  - "Discretion: Escape blurs without clearing (low risk; preserves long pastes from accidental Escape)"
  - "Discretion: title='⌘+Enter' tooltip on Save button (cheap discoverability)"

patterns-established:
  - "Pattern: scrollHeight auto-grow — useLayoutEffect keyed on [input] sets height='0px' then height=scrollHeight+'px'. Caps via CSS max-h-* + overflow-y-auto. No ResizeObserver, no state-tracked height."
  - "Pattern: Multi-line capture tests use userEvent.paste and userEvent.keyboard('{Meta>}{Enter}{/Meta}'). fireEvent.paste does not mutate textarea value in jsdom (RESEARCH Pitfall 2)."
  - "Pattern: Trap test for plain-Enter newline insertion — never delete. Regresses to single-line submit if removed (RESEARCH Pitfall 3)."

requirements-completed:
  - CAP-MULTI-01
  - CAP-MULTI-02
  - CAP-MULTI-03
  - CAP-MULTI-04
  - CAP-MULTI-05

# Metrics
duration: 7min
completed: 2026-05-19
---

# Phase 999.2 Plan 01: CaptureBar Multi-Line Input Support Summary

**Converted CaptureBar `<input type="text">` to controlled `<textarea>` with auto-grow (1→6 lines), Cmd/Ctrl+Enter submit grammar matching ThoughtRow edit mode, `enterKeyHint="enter"` for mobile soft-keyboards, and seven new vitest cases pinning CAP-MULTI-01..05 + D-04 + D-07 — closing the Phase 115 UAT 2 paste-collapse gap (display side already preserved newlines; capture side now does too).**

## Performance

- **Duration:** ~7 min (one sequential executor pass)
- **Started:** 2026-05-19T21:22:49Z
- **Completed:** 2026-05-19T21:29:36Z
- **Tasks:** 3 (T1 RED test, T2 GREEN impl, T3 UAT runbook setup — awaiting operator execution)
- **Files modified:** 1 (CaptureBar.tsx 79→95 lines)
- **Files created:** 2 (CaptureBar.test.tsx 116 lines, 999.2-UAT.md 163 lines)

## Accomplishments

- Element swap `<input type="text">` → `<textarea>` with auto-grow (D-03) and post-submit reset (D-04).
- New keyboard grammar: Cmd/Ctrl+Enter submits (D-01), plain Enter falls through to native textarea newline insertion, Escape blurs without clearing.
- `enterKeyHint="enter"` attribute added so iOS Safari 13.4+ and Android Chrome 77+ render a newline-insert return key on the soft keyboard (D-05).
- Inner flex container gained `items-end` so the Save button stays pinned to the bottom of the growing textarea (RESEARCH Code Example 1 note).
- Discretion `title="⌘+Enter"` tooltip on Save button (desktop discoverability — desktop only, harmless on touch).
- Seven vitest tests in `CaptureBar.test.tsx` covering all five CAP-MULTI requirements + D-04 + D-07 — all GREEN against T2 implementation.
- 8-vector operator UAT runbook authored at `999.2-UAT.md` (awaiting operator execution against deployed staging build).

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Write failing CaptureBar.test.tsx** — `8c3992c` (test)
2. **Task 2 (GREEN): Convert CaptureBar.tsx input→textarea** — `eb47bda` (feat)
3. **Task 3: 8-vector UAT runbook setup** — `67358d3` (docs)

T3 checkpoint is `gate="blocking"` — the runbook file is in place and the automated verify command passes (`grep -cE '^[0-9]+\.' 999.2-UAT.md` returns 8). The 8 vectors themselves require operator execution against a real browser session (desktop + iOS + Android) before the phase can be marked complete via `/gsd:verify-work`. Resume signal: type `approved` once the runbook is filled in.

## Files Created/Modified

- `vigil-pwa/src/components/CaptureBar.tsx` (79→95 lines) — element swap, useLayoutEffect auto-grow, rewritten handleKeyDown, items-end alignment, enterKeyHint, Save tooltip. handleSubmit + fire-and-forget triage + error display + sticky-bottom container preserved verbatim per D-09.
- `vigil-pwa/src/components/CaptureBar.test.tsx` (NEW, 116 lines) — 7 vitest cases. RED at T1 commit (4/7 failing behaviorally). GREEN at T2 commit (7/7 passing).
- `.planning/phases/999.2-capturebar-multi-line-input-support/999.2-UAT.md` (NEW, 163 lines) — 8-vector operator UAT runbook with table rows for per-step observed outcomes and a final sign-off block.

## Test Coverage Map

| Test | Pins | Mechanism |
|------|------|-----------|
| CAP-MULTI-02: paste preserves \\n | CAP-MULTI-02 | `user.paste('line one\\nline two\\nline three')` → assert textarea value |
| CAP-MULTI-01 / D-01: Cmd+Enter submits | CAP-MULTI-01, D-01 | `user.keyboard('{Meta>}{Enter}{/Meta}')` → assert createThought called with the multi-line content |
| CAP-MULTI-01 / D-01: plain Enter inserts newline | CAP-MULTI-01, D-01 (trap-test) | `user.type(ta, 'first{Enter}second')` → assert textarea value AND createThought NOT called |
| CAP-MULTI-04 / D-07: whitespace-only rejected | CAP-MULTI-04, D-07 | `user.type(ta, '   {Enter}{Enter}   ')` + Cmd+Enter → assert createThought NOT called |
| D-04: textarea clears after submit | D-04 | `vi.waitFor(() => expect(ta.value).toBe(''))` |
| CAP-MULTI-05 / D-05: enterKeyHint='enter' | CAP-MULTI-05, D-05 | DOM attribute assertion |
| D-02 / CAP-MULTI-03: Save button disabled state | CAP-MULTI-03, D-02 | empty/whitespace/real-content button-state transitions |

All five phase requirements (CAP-MULTI-01..05) are covered by at least one automated test. The Phase 115 UAT 2 replay (real-world paste→display round-trip) lives in the manual UAT runbook (Vector 1) because jsdom does not run an actual server.

## Decisions Made

- All locked decisions D-01..D-09 honored.
- All Discretion items implemented:
  - resize-none on the textarea (would fight the auto-grow logic).
  - Escape blurs without clearing (preserves long pastes from accidental Escape).
  - `title="⌘+Enter"` tooltip on Save button.
- TS errors at the triage block (`result.tags`, `result.therapyClassification`) confirmed PRE-EXISTING via diff against HEAD~1 — same 4 errors on different lines because T2 added the useLayoutEffect block above the triage. Out of scope per D-09. See "Deferred Issues" below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used `./node_modules/.bin/vitest` instead of `pnpm vitest`**
- **Found during:** Task 1 (verify command)
- **Issue:** Plan's verify command uses `pnpm vitest run …` but pnpm is not installed on this machine (only npm is available). The project's `package.json` script uses `vitest` from `./node_modules/.bin`.
- **Fix:** Invoked `./node_modules/.bin/vitest run src/components/CaptureBar.test.tsx` directly. Identical to what `npm test` invokes (minus the denylist-parity step that's irrelevant here).
- **Files modified:** none (tooling-only deviation)
- **Verification:** vitest binary exists at `vigil-pwa/node_modules/.bin/vitest`; command produced identical test-runner output.
- **Documented in:** plan `<deviation_handling>` block explicitly authorized this fallback.

**2. [Rule 3 - Cleanup] Removed RESEARCH-pitfall reminder comments from test file to satisfy literal grep counts**
- **Found during:** Task 1 (acceptance criteria grep checks)
- **Issue:** Initial test file had leading "RESEARCH pitfalls observed: …" comments listing `fireEvent.paste` and `scrollHeight` by name — these substrings triggered the literal acceptance grep `grep -c "fireEvent" = 0` and `grep -c "scrollHeight" = 0` to return non-zero.
- **Fix:** Rephrased the leading comment to describe the constraints without using the literal strings.
- **Files modified:** vigil-pwa/src/components/CaptureBar.test.tsx
- **Verification:** all 6 acceptance grep checks now pass with their target values.
- **Committed in:** 8c3992c (Task 1 commit, as part of the same file)

**3. [Rule 3 - Cleanup] Rephrased `max-h-36` comment in CaptureBar.tsx to "the visual cap"**
- **Found during:** Task 2 (acceptance criteria grep)
- **Issue:** Initial implementation had `// D-03 / D-04: auto-grow up to max-h-36, …` — the literal string `max-h-36` appeared twice (once in className, once in comment), violating the literal `grep -c "max-h-36" = 1` acceptance criterion.
- **Fix:** Rephrased comment to "auto-grow up to the visual cap"; className unchanged.
- **Files modified:** vigil-pwa/src/components/CaptureBar.tsx
- **Verification:** `grep -c "max-h-36" CaptureBar.tsx` now returns 1.
- **Committed in:** eb47bda (Task 2 commit, as part of the same file)

---

**Total deviations:** 3 minor (all Rule 3 - blocking/cleanup) — none affect functional behavior or acceptance criteria intent. No scope creep, no architectural changes.

## Deferred Issues

**1. Pre-existing TS errors in triage block (NOT introduced by this phase)**
- `tsc -b --force` reports 4 errors in CaptureBar.tsx at the fire-and-forget triage block accessing `result.tags` and `result.therapyClassification` — these are not in the return type of `triageThought()` in `vigil-pwa/src/api/client.ts:183` (which is `Promise<{ category: string; confidence: number }>`).
- **Diff against HEAD~1 confirmed identical 4 errors existed pre-T2 at lines 32-33** (post-T2 they're at lines 41-42 because T2 added the useLayoutEffect block above). Same errors, shifted line numbers. T2 did not introduce them.
- **D-09 explicitly forbids touching this block.** Fix belongs to a future phase that updates the triageThought return type or the server-side `/v1/triage` response shape to match the runtime contract.
- Out of scope for this plan.

**2. Pre-existing PWA test failures (unrelated to CaptureBar)**
- Full `vitest run` reports 4 failed tests across 3 files (`src/api/client.test.ts`, `src/pages/AuthPage.test.tsx`, `src/pages/SettingsPage.test.tsx`) — confirmed pre-existing by stashing T2 and re-running. Failures involve Google auth redirect and signup-mode tests, no connection to CaptureBar.
- 264/268 tests pass; CaptureBar.test.tsx contributes 7 NEW passing tests.
- Out of scope for this plan. Likely belong to their own remediation phase or have been intentionally deferred.

**3. Untracked TypeScript build artifacts cleanup**
- Running `tsc -b --force` for verification generated 170+ `.d.ts` and `.js` files in `vigil-pwa/src/` as a side effect (`composite: true` in tsconfig.app.json). These were deleted before commit. The project's `.gitignore` does not exclude these patterns — a future hygiene phase could add `vigil-pwa/src/**/*.d.ts` and `vigil-pwa/src/**/*.js` (excepting any genuinely-checked-in JS files) to prevent accidental commits in future executor runs.
- Out of scope for this plan.

## Issues Encountered

- **Stash-pop reverted STATE.md to an older version.** When stashing CaptureBar.tsx to verify pre-existing test failures, `git stash pop` brought back an older STATE.md from a workflow earlier in the session. Resolved with `git checkout HEAD -- .planning/STATE.md` to restore the orchestrator's just-committed state file. No impact on plan output.
- No CaptureBar-related bugs surfaced during T2 — the implementation matched the locked decisions exactly and the test file went RED→GREEN cleanly on first try.

## Threat Flags

None. The threat model in PLAN.md (T-999.2-01..06) is unchanged by execution; no new trust boundaries or surfaces introduced. The element swap is a strict subset of the `<input>`'s attack surface, and the JSON wire-encoding of `\n` (RFC 8259) was already in place.

## Known Stubs

None. CaptureBar fully wires user input → controlled state → createThought → onCapture → ThoughtRow display.

## User Setup Required

None — no environment variables, no external services, no migrations. Operator UAT (Task 3) requires a deployed PWA build at `eb47bda` or later but does not require any configuration changes.

## TDD Gate Compliance

| Gate | Commit | Status |
|------|--------|--------|
| RED (test) | `8c3992c` test(999.2-01): add failing tests | OK — 4/7 tests failed behaviorally (no syntax/type/import errors) |
| GREEN (feat) | `eb47bda` feat(999.2-01): convert CaptureBar input→textarea | OK — 7/7 tests pass |
| REFACTOR | (none needed) | T2's implementation matched the locked scaffold exactly; no separate refactor commit produced. |

Plan-level TDD gate sequence satisfied: test commit precedes feat commit; behavioral RED confirmed before GREEN landed.

## Next Phase Readiness

**Plan technically complete pending T3 operator UAT.**

- All T1+T2 automated criteria pass.
- 7/7 CaptureBar vitest tests GREEN.
- Full PWA suite has only pre-existing failures (unrelated to this change).
- `tsc -b` reports only pre-existing errors.
- UAT runbook is in place at `999.2-UAT.md` with all 8 vectors enumerated.

**Awaiting:** Operator executes the 8-vector script (Vector 1 desktop paste replay is the phase success criterion) and signs off in `999.2-UAT.md`. Resume signal: type `approved` on the checkpoint.

**Follow-on phases enabled:**
- Voice capture (Phase 130 — VOICE-02..08) can now rely on `\n`-preserving capture for transcribed multi-paragraph output.
- Future draft-persistence phase has a clean capture state shape (single `input` string controlled by React).
- Triage prompt hardening phase (RESEARCH Pitfall 6) can use real multi-line content from production for prompt-tuning if Vector 8 surfaces category drift.

## Self-Check

**Files created:**
- `vigil-pwa/src/components/CaptureBar.test.tsx` — FOUND
- `.planning/phases/999.2-capturebar-multi-line-input-support/999.2-UAT.md` — FOUND

**Files modified:**
- `vigil-pwa/src/components/CaptureBar.tsx` — FOUND (modified per T2)

**Commits:**
- `8c3992c` (T1) — FOUND in git log
- `eb47bda` (T2) — FOUND in git log
- `67358d3` (T3) — FOUND in git log

## Self-Check: PASSED

---

*Phase: 999.2-capturebar-multi-line-input-support*
*Plan: 01*
*Completed: 2026-05-19*
