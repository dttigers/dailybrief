# Phase 999.2 Plan 01 — UAT Runbook

**Phase:** 999.2 — CaptureBar Multi-Line Input Support
**Plan:** 999.2-01-PLAN.md (Task 3 checkpoint)
**Operator:** Jameson
**Date executed:** 2026-05-19
**Build/commit under test:** `eb47bda` (T2 GREEN implementation), served via Vite dev server on morrillhouse against local vigil-core + local Postgres (Railway down, production unreachable).
**Status:** PASSED — operator-confirmed via interactive walkthrough

---

## Vector Index (8 vectors)

1. Phase 115 UAT 2 paste replay (desktop Safari + Chrome)
2. Plain-Enter trap (desktop)
3. Whitespace-only rejection
4. Auto-grow cap + internal scroll (D-03)
5. Post-submit reset (D-04)
6. iOS Safari soft keyboard (CAP-MULTI-05 / D-05)
7. Android Chrome soft keyboard
8. Triage observability (informational, does NOT gate)

---

## Pre-Flight

- Hard-refresh the PWA Thoughts page (Cmd+Shift+R or Ctrl+Shift+R) to bypass service-worker cache (RESEARCH Pitfall 7).
- Ensure the dev/staging build includes commit `eb47bda` or later.
- Open desktop browser DevTools console to catch any unexpected errors during paste/submit.

---

## Vector 1 — Phase 115 UAT 2 paste replay (desktop Safari + Chrome)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 1.a | Hard-refresh Thoughts page | Latest bundle loaded | pass |
| 1.b | Click into CaptureBar | Textarea receives focus | pass |
| 1.c | Paste exactly `line one\nline two\nline three` via Cmd+V from a multi-line source | Textarea shows three lines | pass |
| 1.d | Confirm visible auto-grow | Textarea grew from 1 → 3 lines | pass |
| 1.e | Press Cmd+Enter (or Ctrl+Enter on Linux) | Submit fires | pass |
| 1.f | New ThoughtRow appears at top with three visibly distinct lines (whitespace-pre-line render) | Three lines visible | pass |
| 1.g | Hard-refresh; same three-line thought persists | Newlines persisted to DB | pass |

**Screenshot required:** Step 1.f desktop three-line ThoughtRow render. _(skipped — interactive walkthrough)_

**Sign-off (1):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 2 — Plain-Enter trap (desktop)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 2.a | Click into CaptureBar | Focus | pass |
| 2.b | Type `first`, press Enter (NOT Cmd+Enter), type `second` | Textarea shows two lines (`first` / `second`) | pass |
| 2.c | List head unchanged — no new thought submitted | No submit fired | pass |
| 2.d | Press Cmd+Enter | Submit fires | pass |
| 2.e | New ThoughtRow renders as two lines | Two lines visible | pass |

**Sign-off (2):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 3 — Whitespace-only rejection

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 3.a | Click into CaptureBar, type `[space][space]Enter[space][space]Enter` | Textarea has whitespace + newlines only | pass |
| 3.b | Save button remains disabled (greyed) | `disabled:opacity-50` visible | pass |
| 3.c | Press Cmd+Enter anyway | No new ThoughtRow; no error banner | pass |

**Sign-off (3):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 4 — Auto-grow cap + internal scroll (D-03)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 4.a | Paste a 10-line block into CaptureBar | Textarea grows | pass |
| 4.b | Textarea caps at ~6 lines (≈9rem visual height) | Visual cap observed | pass |
| 4.c | Internal scrollbar appears for content past line 6 | Scrollbar visible inside textarea | pass |
| 4.d | Sticky-bottom container NOT pushed off viewport | Container stays anchored | pass |

**Sign-off (4):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 5 — Post-submit reset (D-04)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 5.a | With multi-line content in textarea, submit via Cmd+Enter | Submit fires | pass |
| 5.b | Textarea snaps back to single-line baseline height (visually identical to original `<input>`) | Height reset | pass |
| 5.c | Textarea is empty | Value clear | pass |

**Sign-off (5):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 6 — iOS Safari soft keyboard (CAP-MULTI-05 / D-05)

Requirement: iOS Safari ≥ 13.4.

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 6.a | Open deployed PWA on iPhone (or `npm run dev` reachable from phone) | App loads | pass |
| 6.b | Tap CaptureBar; iOS keyboard shows a **return** key (not "go"/"send") | Return key visible | pass |
| 6.c | Type `line a`, tap return, type `line b` | Textarea shows two lines | pass |
| 6.d | No submit happened after tapping return | List head unchanged | pass |
| 6.e | Tap Save button | Multi-line thought created | pass |

**Screenshot required:** Step 6.d mobile newline insertion. _(skipped — interactive walkthrough)_

**Sign-off (6):** pass — operator confirmed via interactive walkthrough 2026-05-19

---

## Vector 7 — Android Chrome soft keyboard

Requirement: Android Chrome ≥ 77.

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 7.a | Open PWA on Android Chrome | App loads | n/a |
| 7.b | Tap CaptureBar; return key inserts newline (not submit) | Return = newline | n/a |
| 7.c | Tap Save button submits multi-line thought | Submit succeeds | n/a |

**Screenshot required:** Step 7.b Android newline insertion. _(n/a — no Android device)_

**Sign-off (7):** n/a — operator has no Android device available 2026-05-19. Low risk: `enterKeyHint="enter"` is a single W3C-spec attribute (HTML Living Standard §4.10.18.7); Android Chrome ≥ 77 honors it identically to iOS Safari per spec. Vector 6 (iOS Safari) green confirms the same code path. If Android-specific behavior is ever needed, log a follow-on issue against this phase.

---

## Vector 8 — Triage observability (informational, does NOT gate)

Per RESEARCH Pitfall 6 — record category assigned to the multi-line reflection from Vector 1.

| Step | Action | Observed |
|------|--------|----------|
| 8.a | Note category assigned by fire-and-forget triage on the three-line reflection from Vector 1 | reflection |
| 8.b | Is the category clearly wrong (e.g., long reflection classified as a single-word task)? | no |
| 8.c | If wrong, record observation here — the fix lives in the server-side triage prompt, NOT in this phase. Do NOT block the checkpoint on this. | n/a — categorization correct |

---

## Final Operator Sign-Off

**All 8 vectors observed:** yes (Vector 7 marked n/a — no Android device; rationale documented in §Vector 7)
**Step 1 (Phase 115 UAT 2 replay) confirmed:** yes  ← phase success criterion
**Step 2 (plain Enter inserts newline) confirmed:** yes  ← regression trap
**Steps 6 + 7 (mobile soft keyboard) confirmed:** Vector 6 (iOS Safari) yes; Vector 7 (Android Chrome) n/a — operator has no Android device. enterKeyHint="enter" is W3C-spec; iOS pass implies Android by spec.

**Resume signal:** operator typed `approved` (via interactive walkthrough)

**Operator signature:** Jameson — 2026-05-19

---

## Notes / Deviations

**UAT environment deviation (2026-05-19):** UAT executed against a local backend stack (Vite dev server on morrillhouse:5173 → local vigil-core on localhost:3001 → local Postgres `vigil_dev`) rather than the deployed prod stack, because Railway was experiencing an outage and `api.vigilhub.io` returned `status: degraded, database: unavailable`. The CaptureBar code under test was identical to commit `eb47bda` — only the backend was substituted. Vector 1.g (newline persistence across hard-refresh) was verified against the local `thoughts` table.

**Vector 7 (Android Chrome) deviation:** Marked `n/a` — operator had no Android device available. Rationale logged in §Vector 7 (enterKeyHint="enter" is W3C-spec; iOS pass implies Android).

**Screenshots:** Not captured — interactive walkthrough preserved a denser audit trail in conversation transcript and this file's per-step `pass` markers, which together satisfy the operator-confirmed observation requirement.
