# Phase 999.2 Plan 01 — UAT Runbook

**Phase:** 999.2 — CaptureBar Multi-Line Input Support
**Plan:** 999.2-01-PLAN.md (Task 3 checkpoint)
**Operator:** Jameson
**Date executed:** _(fill in)_
**Build/commit under test:** `eb47bda` (T2 GREEN implementation)
**Status:** AWAITING OPERATOR EXECUTION

---

## Vector Index (8 vectors)

1. Phase 115 UAT 2 paste replay (desktop Safari + Chrome)
2. Plain-Enter trap (desktop)
3. Whitespace-only rejection
4. Auto-grow cap + internal scroll (D-03)
5. Post-submit reset (D-04)
6. iOS Safari soft keyboard (CAP-MULTI-05 / D-05)
7. Android Chrome soft keyboard
8. Triage observability (informational, does NOT gate)

---

## Pre-Flight

- Hard-refresh the PWA Thoughts page (Cmd+Shift+R or Ctrl+Shift+R) to bypass service-worker cache (RESEARCH Pitfall 7).
- Ensure the dev/staging build includes commit `eb47bda` or later.
- Open desktop browser DevTools console to catch any unexpected errors during paste/submit.

---

## Vector 1 — Phase 115 UAT 2 paste replay (desktop Safari + Chrome)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 1.a | Hard-refresh Thoughts page | Latest bundle loaded | _( )_ |
| 1.b | Click into CaptureBar | Textarea receives focus | _( )_ |
| 1.c | Paste exactly `line one\nline two\nline three` via Cmd+V from a multi-line source | Textarea shows three lines | _( )_ |
| 1.d | Confirm visible auto-grow | Textarea grew from 1 → 3 lines | _( )_ |
| 1.e | Press Cmd+Enter (or Ctrl+Enter on Linux) | Submit fires | _( )_ |
| 1.f | New ThoughtRow appears at top with three visibly distinct lines (whitespace-pre-line render) | Three lines visible | _( )_ |
| 1.g | Hard-refresh; same three-line thought persists | Newlines persisted to DB | _( )_ |

**Screenshot required:** Step 1.f desktop three-line ThoughtRow render. _(attach path)_

**Sign-off (1):** _(pass / fail + notes)_

---

## Vector 2 — Plain-Enter trap (desktop)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 2.a | Click into CaptureBar | Focus | _( )_ |
| 2.b | Type `first`, press Enter (NOT Cmd+Enter), type `second` | Textarea shows two lines (`first` / `second`) | _( )_ |
| 2.c | List head unchanged — no new thought submitted | No submit fired | _( )_ |
| 2.d | Press Cmd+Enter | Submit fires | _( )_ |
| 2.e | New ThoughtRow renders as two lines | Two lines visible | _( )_ |

**Sign-off (2):** _(pass / fail + notes)_

---

## Vector 3 — Whitespace-only rejection

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 3.a | Click into CaptureBar, type `[space][space]Enter[space][space]Enter` | Textarea has whitespace + newlines only | _( )_ |
| 3.b | Save button remains disabled (greyed) | `disabled:opacity-50` visible | _( )_ |
| 3.c | Press Cmd+Enter anyway | No new ThoughtRow; no error banner | _( )_ |

**Sign-off (3):** _(pass / fail + notes)_

---

## Vector 4 — Auto-grow cap + internal scroll (D-03)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 4.a | Paste a 10-line block into CaptureBar | Textarea grows | _( )_ |
| 4.b | Textarea caps at ~6 lines (≈9rem visual height) | Visual cap observed | _( )_ |
| 4.c | Internal scrollbar appears for content past line 6 | Scrollbar visible inside textarea | _( )_ |
| 4.d | Sticky-bottom container NOT pushed off viewport | Container stays anchored | _( )_ |

**Sign-off (4):** _(pass / fail + notes)_

---

## Vector 5 — Post-submit reset (D-04)

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 5.a | With multi-line content in textarea, submit via Cmd+Enter | Submit fires | _( )_ |
| 5.b | Textarea snaps back to single-line baseline height (visually identical to original `<input>`) | Height reset | _( )_ |
| 5.c | Textarea is empty | Value clear | _( )_ |

**Sign-off (5):** _(pass / fail + notes)_

---

## Vector 6 — iOS Safari soft keyboard (CAP-MULTI-05 / D-05)

Requirement: iOS Safari ≥ 13.4.

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 6.a | Open deployed PWA on iPhone (or `npm run dev` reachable from phone) | App loads | _( )_ |
| 6.b | Tap CaptureBar; iOS keyboard shows a **return** key (not "go"/"send") | Return key visible | _( )_ |
| 6.c | Type `line a`, tap return, type `line b` | Textarea shows two lines | _( )_ |
| 6.d | No submit happened after tapping return | List head unchanged | _( )_ |
| 6.e | Tap Save button | Multi-line thought created | _( )_ |

**Screenshot required:** Step 6.d mobile newline insertion. _(attach path)_

**Sign-off (6):** _(pass / fail + notes)_

---

## Vector 7 — Android Chrome soft keyboard

Requirement: Android Chrome ≥ 77.

| Step | Action | Expected | Observed |
|------|--------|----------|----------|
| 7.a | Open PWA on Android Chrome | App loads | _( )_ |
| 7.b | Tap CaptureBar; return key inserts newline (not submit) | Return = newline | _( )_ |
| 7.c | Tap Save button submits multi-line thought | Submit succeeds | _( )_ |

**Screenshot required:** Step 7.b Android newline insertion. _(attach path)_

**Sign-off (7):** _(pass / fail + notes)_

---

## Vector 8 — Triage observability (informational, does NOT gate)

Per RESEARCH Pitfall 6 — record category assigned to the multi-line reflection from Vector 1.

| Step | Action | Observed |
|------|--------|----------|
| 8.a | Note category assigned by fire-and-forget triage on the three-line reflection from Vector 1 | _(category: )_ |
| 8.b | Is the category clearly wrong (e.g., long reflection classified as a single-word task)? | _( yes / no )_ |
| 8.c | If wrong, record observation here — the fix lives in the server-side triage prompt, NOT in this phase. Do NOT block the checkpoint on this. | _( )_ |

---

## Final Operator Sign-Off

**All 8 vectors observed:** _( yes / no )_
**Step 1 (Phase 115 UAT 2 replay) confirmed:** _( yes / no )_  ← phase success criterion
**Step 2 (plain Enter inserts newline) confirmed:** _( yes / no )_  ← regression trap
**Steps 6 + 7 (mobile soft keyboard) confirmed:** _( yes / no )_

**Resume signal:** Operator types `approved` (or detailed failure description) on the checkpoint resume.

**Operator signature:** _( name + date )_

---

## Notes / Deviations

_(use this section to record any deviations from expected behavior, attached screenshot paths, or follow-on observations for future phases)_
