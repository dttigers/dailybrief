---
phase: 999.2
slug: capturebar-multi-line-input-support
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-19
---

# Phase 999.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 + @testing-library/react 16.3.2 + @testing-library/user-event 14.6.1 |
| **Config file** | `vigil-pwa/vitest.config.ts` (jsdom env, setupFiles=`src/test/setup.ts`) |
| **Quick run command** | `cd vigil-pwa && pnpm vitest run src/components/CaptureBar.test.tsx` |
| **Full suite command** | `cd vigil-pwa && pnpm vitest run` |
| **Estimated runtime** | ~5s (CaptureBar.test.tsx only) · ~30s (full vigil-pwa suite) |

---

## Sampling Rate

- **After every task commit:** Run `cd vigil-pwa && pnpm vitest run src/components/CaptureBar.test.tsx`
- **After every plan wave:** Run `cd vigil-pwa && pnpm vitest run`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Filled by gsd-planner once PLAN.md tasks are issued. Each task that produces verifiable behavior must map to a vitest case in `CaptureBar.test.tsx` covering CAP-MULTI-01..05 + D-04 height-reset + D-07 whitespace-only guard.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.2-01-01 | 01 | 1 | CAP-MULTI-01 | — | textarea renders, accepts multi-line | unit | `pnpm vitest run src/components/CaptureBar.test.tsx -t "renders textarea"` | ❌ W0 | ⬜ pending |
| 999.2-01-02 | 01 | 1 | CAP-MULTI-02 | — | paste preserves `\n` end-to-end | unit | `pnpm vitest run src/components/CaptureBar.test.tsx -t "paste preserves newlines"` | ❌ W0 | ⬜ pending |
| 999.2-01-03 | 01 | 1 | CAP-MULTI-03 | — | Cmd+Enter submits, plain Enter inserts newline | unit | `pnpm vitest run src/components/CaptureBar.test.tsx -t "Cmd\\+Enter|plain Enter"` | ❌ W0 | ⬜ pending |
| 999.2-01-04 | 01 | 1 | CAP-MULTI-04 | — | whitespace-only rejected | unit | `pnpm vitest run src/components/CaptureBar.test.tsx -t "whitespace-only"` | ❌ W0 | ⬜ pending |
| 999.2-01-05 | 01 | 1 | CAP-MULTI-05 | — | `enterKeyHint="enter"` set on textarea | unit | `pnpm vitest run src/components/CaptureBar.test.tsx -t "enterKeyHint"` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `vigil-pwa/src/components/CaptureBar.test.tsx` — new test file covering CAP-MULTI-01..05 + D-04 height reset + D-07 whitespace-only guard
- [ ] No framework install needed (vitest 2.1.9 + RTL 16.3.2 + user-event 14.6.1 already in `vigil-pwa/package.json`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Three-line paste replay (the Phase 115 UAT 2 incident) | CAP-MULTI-02 end-to-end | jsdom does not exercise real DOM clipboard + server round-trip; need real browser + real `/v1/thoughts` POST + ThoughtRow render | 1. Open Thoughts page in Vigil PWA (Safari + Chrome). 2. Paste `line one\nline two\nline three` into CaptureBar. 3. Press Cmd+Enter. 4. Verify the new ThoughtRow renders three visible lines (`whitespace-pre-line` already shipped in Phase 115). |
| iOS Safari return key inserts newline | CAP-MULTI-05 | jsdom does not render mobile soft keyboard; `enterKeyHint="enter"` is a hint to the OS, not a JS property | iOS Safari ≥ 13.4: tap CaptureBar, tap return — confirm a newline is inserted (textarea grows one row), not a submit. Save button still submits. |
| Android Chrome return key inserts newline | CAP-MULTI-05 | Same as above | Android Chrome ≥ 77: tap CaptureBar, tap return — confirm newline insertion, not submit. |
| Auto-grow caps at 6 lines then scrolls (D-03) | CAP-MULTI-01 + D-03 | jsdom does not compute layout for `scrollHeight` reliably; visual cap is a layout assertion best done by eye | Paste a 10-line block; confirm textarea visually maxes at ~6 lines and the remaining content scrolls internally. Sticky-bottom container does not get pushed off screen. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
