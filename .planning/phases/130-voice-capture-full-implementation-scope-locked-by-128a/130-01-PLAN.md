---
phase: 130
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vigil-g2-plugin/scripts/voice-spike-encoder.ts
  - vigil-g2-plugin/src/screens/voice-spike.ts
  - vigil-core/src/routes/voice-spike.ts
  - vigil-core/src/ai/transcribe-spike.ts
  - vigil-core/src/routes/__tests__/voice-spike.test.ts
  - vigil-g2-plugin/src/navigation.ts
  - vigil-g2-plugin/src/main.ts
  - vigil-g2-plugin/src/constants.ts
  - vigil-g2-plugin/app.json
  - vigil-core/src/index.ts
autonomous: true
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "Five spike files no longer exist in repo"
    - "Five spike-only modifications reverted to pre-128a baseline (modulo Phase 127 carry-forward)"
    - "g2-microphone permission entry PRESERVED in app.json"
    - "Both vigil-core and vigil-g2-plugin builds + tests pass after the deletion (no orphan imports)"
  artifacts:
    - path: "vigil-g2-plugin/scripts/voice-spike-encoder.ts"
      provides: "does NOT exist"
    - path: "vigil-g2-plugin/src/screens/voice-spike.ts"
      provides: "does NOT exist"
    - path: "vigil-core/src/routes/voice-spike.ts"
      provides: "does NOT exist"
    - path: "vigil-core/src/ai/transcribe-spike.ts"
      provides: "does NOT exist"
    - path: "vigil-core/src/routes/__tests__/voice-spike.test.ts"
      provides: "does NOT exist"
  key_links:
    - from: "vigil-core/src/index.ts"
      to: "voice-spike route"
      via: "import + app.route() — MUST be absent"
      pattern: "voice-spike"
    - from: "vigil-g2-plugin/src/navigation.ts"
      to: "VOICE_SPIKE Screen entry"
      via: "VOICE_SPIKE constant — MUST be absent"
      pattern: "VOICE_SPIKE"
    - from: "vigil-g2-plugin/app.json"
      to: "g2-microphone permission"
      via: "permissions[].name — MUST be PRESERVED"
      pattern: "g2-microphone"
---

<objective>
Atomic spike-removal commit per CONTEXT D-C1 + D-C2 + D-C4. Delete five spike-only files and revert five spike-only modifications to the pre-128a baseline (modulo Phase 127 carry-forward). NO production code is added in this plan — that is Plans 02..06.

Purpose: Keeps the spike-removal commit auditable as a single atomic step. Prevents the "production code reuses spike code by accident" failure mode (D-C4). Per SPIKE-DECISION "Tossable Code Cleanup Note": treat spike code as a clean reference, not as code to keep.
Output: Five files deleted, five modifications reverted, `g2-microphone` permission preserved, both packages build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md
@.planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete five spike-only files (D-C1)</name>
  <files>
    vigil-g2-plugin/scripts/voice-spike-encoder.ts,
    vigil-g2-plugin/src/screens/voice-spike.ts,
    vigil-core/src/routes/voice-spike.ts,
    vigil-core/src/ai/transcribe-spike.ts,
    vigil-core/src/routes/__tests__/voice-spike.test.ts
  </files>
  <read_first>
    .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md (lines 56-66 — canonical 5-file delete list),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-C1),
    vigil-g2-plugin/scripts/voice-spike-encoder.ts (verify file exists before delete),
    vigil-g2-plugin/src/screens/voice-spike.ts (verify file exists before delete),
    vigil-core/src/routes/voice-spike.ts (verify file exists before delete),
    vigil-core/src/ai/transcribe-spike.ts (verify file exists before delete),
    vigil-core/src/routes/__tests__/voice-spike.test.ts (verify file exists before delete)
  </read_first>
  <action>
    Per D-C1, run `git rm` (or `rm`) for each of the five paths listed in `<files>`. These are spike-only TS — every file has `PHASE 128a SPIKE` leading-comment marker. No alternative filename exists for any of them. Do NOT in-place rename any of these to production names (D-U1 forbids it — production route lands under fresh filenames in Plan 02).

    After deletion, confirm none of the five paths return results from `ls`. Do NOT yet attempt `npm test` — Task 2 reverts the 5 spike-only modifications that still import these deleted files, so a test run between Task 1 and Task 2 will fail with "cannot find module" errors.
  </action>
  <verify>
    <automated>test -z "$(ls vigil-g2-plugin/scripts/voice-spike-encoder.ts vigil-g2-plugin/src/screens/voice-spike.ts vigil-core/src/routes/voice-spike.ts vigil-core/src/ai/transcribe-spike.ts vigil-core/src/routes/__tests__/voice-spike.test.ts 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `ls vigil-g2-plugin/scripts/voice-spike-encoder.ts 2>&1` returns "No such file or directory"
    - CLI: `ls vigil-g2-plugin/src/screens/voice-spike.ts 2>&1` returns "No such file or directory"
    - CLI: `ls vigil-core/src/routes/voice-spike.ts 2>&1` returns "No such file or directory"
    - CLI: `ls vigil-core/src/ai/transcribe-spike.ts 2>&1` returns "No such file or directory"
    - CLI: `ls vigil-core/src/routes/__tests__/voice-spike.test.ts 2>&1` returns "No such file or directory"
    - Source: `git status` shows 5 entries under "deleted:" matching the five paths above
  </acceptance_criteria>
  <done>Five spike files no longer exist on disk; `git status` reports five deletions; no production-side code yet modified.</done>
</task>

<task type="auto">
  <name>Task 2: Revert five spike-only modifications (D-C2)</name>
  <files>
    vigil-g2-plugin/src/navigation.ts,
    vigil-g2-plugin/src/main.ts,
    vigil-g2-plugin/src/constants.ts,
    vigil-g2-plugin/app.json,
    vigil-core/src/index.ts
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 50-95 — exact line numbers + diff shape for each file),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-C2 + D-C3 carry-forward list),
    .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md (lines 68-85 — revert list + g2-microphone PRESERVE notice),
    vigil-g2-plugin/src/navigation.ts,
    vigil-g2-plugin/src/main.ts,
    vigil-g2-plugin/src/constants.ts,
    vigil-g2-plugin/app.json,
    vigil-core/src/index.ts
  </read_first>
  <action>
    Per D-C2, remove spike-only entries from each file. Specifics from PATTERNS.md §"D-C1/D-C2: Spike Cleanup":

    - `vigil-g2-plugin/src/navigation.ts`: delete the `buildVoiceSpikeScreen / getRecording / toggleVoiceSpikeRecording` import block (around lines 24-30), delete the `VOICE_SPIKE: 'voice-spike'` Screen enum entry (around line 41), delete `Screen.VOICE_SPIKE` from `SCREEN_ORDER` (around line 51), delete the matching `case Screen.VOICE_SPIKE:` branch in the `buildScreen` switch.
    - `vigil-g2-plugin/src/main.ts`: delete `appendPcmChunk` import from `./screens/voice-spike.ts` (around line 62) and any `voice-spike` screen registration or event-routing branch that references the deleted screen module.
    - `vigil-g2-plugin/src/constants.ts`: delete the three `VOICE_SPIKE_HEADER / VOICE_SPIKE_BODY / VOICE_SPIKE_FOOTER` entries (around lines 28-30).
    - `vigil-g2-plugin/app.json`: KEEP the `g2-microphone` permission entry (D-C2 + D-C3 explicit). REWORD its `desc` field — remove the `Phase 128a VOICE-01 spike:` prefix; final string: `"Push-to-record voice capture for thought intake."` (per PATTERNS.md lines 88-94). Do NOT touch the `name: "g2-microphone"` value.
    - `vigil-core/src/index.ts`: delete `import { voiceSpike } from "./routes/voice-spike.js";` (line 29) and `app.route("/v1", voiceSpike);` (line 232). No replacement import — Plan 02 re-mounts a fresh `voiceTranscribe` import.

    Do NOT touch the carry-forward files per D-C3: `vigil-g2-plugin/src/lib/audio-session-guard.ts`, `vigil-core/src/lib/ai-budget.ts`, `vigil-core/src/lib/audio-cap.ts`. (`audio-session-guard.ts` gets its signature change in Plan 04 — not here.)
  </action>
  <verify>
    <automated>grep -RIl 'voice-spike\|VOICE_SPIKE\|voiceSpike\|voice_spike' vigil-g2-plugin/src/ vigil-g2-plugin/scripts/ vigil-g2-plugin/app.json vigil-core/src/ 2>/dev/null | grep -v node_modules | wc -l | grep -q '^0$' &amp;&amp; grep -q '"name": "g2-microphone"' vigil-g2-plugin/app.json &amp;&amp; cd vigil-g2-plugin &amp;&amp; npm test 2>&amp;1 | tail -20 &amp;&amp; cd ../vigil-core &amp;&amp; npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `grep -RIl 'voice-spike\|VOICE_SPIKE\|voiceSpike' vigil-g2-plugin/src/ vigil-g2-plugin/scripts/ vigil-core/src/ 2>/dev/null | grep -v node_modules | wc -l` outputs `0`
    - Source: `vigil-g2-plugin/app.json` contains the literal string `"name": "g2-microphone"` (g2-microphone permission PRESERVED)
    - Source: `vigil-g2-plugin/app.json` does NOT contain the substring `Phase 128a` (desc reworded)
    - Source: `vigil-core/src/index.ts` does NOT contain the substring `voiceSpike` or `voice-spike`
    - Source: `vigil-g2-plugin/src/navigation.ts` does NOT contain the substring `VOICE_SPIKE`
    - Source: `vigil-g2-plugin/src/constants.ts` does NOT contain the substring `VOICE_SPIKE`
    - Behavior: `cd vigil-core && npm test` exits 0 (no orphan voice-spike imports break the build)
    - Behavior: `cd vigil-g2-plugin && npm test` exits 0 (plugin still builds after revert)
    - Source: `vigil-g2-plugin/src/lib/audio-session-guard.ts` still exists and is UNCHANGED at this point (D-C3 carry-forward; Plan 04 modifies the signature)
  </acceptance_criteria>
  <done>Both packages build green; no `voice-spike` / `VOICE_SPIKE` / `voiceSpike` references remain anywhere except inside `.planning/` docs. `g2-microphone` permission preserved. Single atomic spike-removal commit ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan is delete-only across files Phase 128a authored.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01-SC | Tampering | spike-file removal | accept | No new package installs; pure file deletion + module-revert. No supply-chain surface. |
| T-130-01-OR | Repudiation | git history | accept | Single atomic commit makes the deletion auditable; SPIKE-DECISION "Tossable Code Cleanup Note" is the source-of-truth list. |

No HIGH severity threats. Inherits Phase 127/128a guardrails — `g2-microphone` permission PRESERVED so Phase 130 production code does not re-trigger the Even Hub portal C-2 approval pipeline.
</threat_model>

<verification>
Both packages build + test green after revert. `g2-microphone` PRESERVED. No stale spike references survive.
</verification>

<success_criteria>
- All 5 D-C1 file paths return "No such file or directory"
- All 5 D-C2 modifications are reverted (greps return 0 matches)
- `app.json` still contains `"name": "g2-microphone"` (permission PRESERVED)
- `app.json` does NOT contain `Phase 128a` (desc reworded)
- `cd vigil-core && npm test` and `cd vigil-g2-plugin && npm test` both exit 0
- D-C3 files untouched at this point: `audio-session-guard.ts`, `ai-budget.ts`, `audio-cap.ts`
</success_criteria>

<output>
Create `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-01-SUMMARY.md` when done.
</output>
