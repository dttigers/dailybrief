---
phase: 130
plan: 02
type: execute
wave: 2
depends_on:
  - 130-01
files_modified:
  - vigil-core/drizzle/0023_voice_capture_dedup.sql
  - vigil-core/src/db/schema.ts
  - vigil-core/src/routes/voice-transcribe.ts
  - vigil-core/src/routes/voice-errors.ts
  - vigil-core/src/ai/transcribe.ts
  - vigil-core/src/lib/ai-budget.ts
  - vigil-core/src/index.ts
  - vigil-core/src/analytics/posthog.ts
  - vigil-core/src/routes/__tests__/voice-transcribe.test.ts
  - vigil-core/src/lib/__tests__/ai-budget.test.ts
autonomous: true
requirements:
  - VOICE-05
requirements_addressed:
  - VOICE-05
user_setup: []
must_haves:
  truths:
    - "Migration 0023 applied locally — `voice_captures` table exists with composite partial unique index (user_id, client_capture_id)"
    - "`POST /v1/voice/transcribe` accepts JSON `{audio, clientCaptureId}` with bearerAuth + email-verified + AI-budget + 60s audio cap"
    - "Duplicate clientCaptureId on retry returns 200 with EXISTING thoughtId (no second OpenAI call, no second thought row)"
    - "Successful transcribe inserts thoughts row with source='g2_voice' and inserts voice_captures dedup row"
    - "OpenAI 30s AbortController timeout throws VoiceTranscribeTimeoutError → HTTP 504 + code VOICE_TRANSCRIBE_TIMEOUT"
    - "OpenAI 5xx / network failure throws VoiceTranscribeProviderDownError → HTTP 502 + code VOICE_TRANSCRIBE_PROVIDER_DOWN"
    - "OpenAI 429-quota throws VoiceTranscribeQuotaError → HTTP 503 + code VOICE_TRANSCRIBE_QUOTA"
    - "OpenAI duration-based cost accumulates into ai_usage_daily via withOpenAIBudgetTracking"
  artifacts:
    - path: "vigil-core/drizzle/0023_voice_capture_dedup.sql"
      provides: "voice_captures table + composite partial unique index"
      contains: "CREATE TABLE IF NOT EXISTS \"voice_captures\""
    - path: "vigil-core/src/db/schema.ts"
      provides: "voiceCaptures Drizzle table export"
      contains: "voiceCaptures"
    - path: "vigil-core/src/routes/voice-transcribe.ts"
      provides: "POST /v1/voice/transcribe production route"
      exports: ["createVoiceTranscribeRoute", "voiceTranscribe"]
    - path: "vigil-core/src/routes/voice-errors.ts"
      provides: "Three locked-enum error classes"
      exports: ["VoiceTranscribeTimeoutError", "VoiceTranscribeProviderDownError", "VoiceTranscribeQuotaError"]
    - path: "vigil-core/src/ai/transcribe.ts"
      provides: "transcribeWav helper + getTranscribeClient lazy-init"
      exports: ["transcribeWav", "getTranscribeClient"]
    - path: "vigil-core/src/lib/ai-budget.ts"
      provides: "withOpenAIBudgetTracking duration-based accumulator"
      exports: ["withOpenAIBudgetTracking"]
  key_links:
    - from: "vigil-core/src/routes/voice-transcribe.ts"
      to: "vigil-core/src/lib/ai-budget.ts (requireAiBudget + withOpenAIBudgetTracking)"
      via: "import + pre-flight throw"
      pattern: "requireAiBudget\\(userId\\)"
    - from: "vigil-core/src/routes/voice-transcribe.ts"
      to: "vigil-core/src/lib/audio-cap.ts (assertAudioSessionWithinCap)"
      via: "import + post-body-parse cap check"
      pattern: "assertAudioSessionWithinCap"
    - from: "vigil-core/src/routes/voice-transcribe.ts"
      to: "vigil-core/src/db/schema.ts (voiceCaptures, thoughts)"
      via: "drizzle insert + dedup SELECT"
      pattern: "voiceCaptures"
    - from: "vigil-core/src/index.ts"
      to: "voice-transcribe route + voice-errors"
      via: "import + app.route() + app.onError translation table"
      pattern: "VoiceTranscribeTimeoutError|VoiceTranscribeProviderDownError|VoiceTranscribeQuotaError"
    - from: "vigil-core/src/analytics/posthog.ts"
      to: "SENSITIVE_ROUTES Set"
      via: "/v1/voice/transcribe entry"
      pattern: "/v1/voice/transcribe"
---

<objective>
Land the production `POST /v1/voice/transcribe` route, the OpenAI transcription helper, the `voice_captures` dedup primitive (migration 0023 + schema.ts table), and the OpenAI duration-based budget adapter (`withOpenAIBudgetTracking`). Wire the three new error classes through `app.onError`. After this plan, the server side of the voice pipeline is complete; G2 plugin and SSE fan-out land in subsequent plans.

Purpose: Closes VOICE-05 server-side (bearerAuth + email-verified + AI-budget + 60s cap + dedup + OpenAI transcribe + thought insert + triage). Establishes the SSE publish call-site that Plan 03 consumes.
Output: Production route mounted at `/v1/voice/transcribe`; migration 0023 applied; three locked-enum errors translated; OpenAI cost accumulates against the existing $0.50/user/day cap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@vigil-core/src/routes/captures-screenshot.ts
@vigil-core/src/ai/client.ts
@vigil-core/src/lib/ai-budget.ts
@vigil-core/src/lib/audio-cap.ts
@vigil-core/drizzle/0021_add_work_orders_client_capture_id.sql
@vigil-core/src/db/schema.ts
@vigil-core/src/index.ts
@vigil-core/src/analytics/posthog.ts

<interfaces>
<!-- Contracts the executor needs. Plan 03 consumes bus.emitThoughtCreated which is added in Plan 03 itself; the route must call it but the bus method ships in Plan 03. Order: this plan stubs the bus call against the existing `bus` import; Plan 03 adds the typed method on agent-events-bus.ts. -->

From `vigil-core/src/lib/audio-cap.ts`:
```typescript
export const MAX_PCM_BYTES = 1_920_000;
export class AudioSessionTooLongError extends Error { readonly code = "AUDIO_SESSION_TOO_LONG"; ... }
export function assertAudioSessionWithinCap(base64: string): void;
```

From `vigil-core/src/lib/ai-budget.ts`:
```typescript
export class DailyBudgetExceededError extends Error { readonly code = "DAILY_AI_BUDGET_EXCEEDED"; ... }
export async function requireAiBudget(userId: number): Promise<void>;
export async function withBudgetTracking<T extends { usage?: { input_tokens: number; output_tokens: number } }>(
  userId: number,
  fn: () => Promise<T>,
): Promise<T>;
```

From `vigil-core/src/lib/agent-events-bus.ts` (this plan calls; Plan 03 adds the method):
```typescript
// AFTER Plan 03 ships:
bus.emitThoughtCreated(userId: number, payload: { thoughtId: number; content: string }): void;
// This plan writes the call site; Plan 03 wires the method.
// To keep this plan green, this plan ALSO adds the no-op stub method (just emits to a not-yet-listened-on channel) — Plan 03 then adds the listener triple.
```

From `vigil-core/src/db/schema.ts` existing tables:
```typescript
export const thoughts = pgTable("thoughts", { id, userId, content, source, cloudKitRecordID, ... });
export const users = pgTable("users", { id, ... });
```

From `vigil-core/src/index.ts` existing translation pattern (Phase 127):
```typescript
app.onError((err, c) => {
  if (err instanceof DailyBudgetExceededError) return c.json({ error: err.message, code: err.code }, 429);
  if (err instanceof AudioSessionTooLongError) return c.json({ error: err.message, code: err.code }, 413);
  // Phase 130 adds: VoiceTranscribeTimeoutError → 504, VoiceTranscribeProviderDownError → 502, VoiceTranscribeQuotaError → 503
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — author failing tests for route + dedup + error taxonomy + OpenAI budget adapter</name>
  <files>
    vigil-core/src/routes/__tests__/voice-transcribe.test.ts,
    vigil-core/src/lib/__tests__/ai-budget.test.ts
  </files>
  <behavior>
    voice-transcribe.test.ts (NEW):
    - Test 1: POST `/v1/voice/transcribe` with valid base64 WAV (16kHz mono LE, ≤60s) → 201 `{ thoughtId, content: "<mock transcript>" }`; assert thoughts row exists with `source='g2_voice'`; assert voice_captures row exists with the clientCaptureId.
    - Test 2: POST with duplicate `clientCaptureId` (after a successful first POST) → 200 `{ thoughtId: <same>, content: "<same>" }`; OpenAI mock called ONLY ONCE; no second thoughts row inserted.
    - Test 3: POST when OpenAI mock throws `AbortError` → response 504 + body `{ code: "VOICE_TRANSCRIBE_TIMEOUT", ... }`.
    - Test 4: POST when OpenAI mock throws a 5xx-like error → response 502 + `{ code: "VOICE_TRANSCRIBE_PROVIDER_DOWN" }`.
    - Test 5: POST when OpenAI mock throws a quota error (err.status === 429 or message contains "quota") → response 503 + `{ code: "VOICE_TRANSCRIBE_QUOTA" }`.
    - Test 6: POST when daily AI budget exceeded (mock `requireAiBudget` to throw `DailyBudgetExceededError`) → response 429 + `{ code: "DAILY_AI_BUDGET_EXCEEDED" }`; OpenAI mock NEVER called.
    - Test 7: POST with body.audio decoded length > MAX_PCM_BYTES → response 413 + `{ code: "AUDIO_SESSION_TOO_LONG" }`; OpenAI mock NEVER called.
    - Test 8: POST without `clientCaptureId` → 400 `{ error: "audio and clientCaptureId are required" }`.
    - Test 9: POST without bearer token → 401 (middleware handles).

    ai-budget.test.ts (EXTEND):
    - Test A: `withOpenAIBudgetTracking(userId, durationMs=10000, fn)` runs fn and inserts row in `ai_usage_daily` for `userId, CURRENT_DATE` with usd_estimate = 10 * (0.003/60) = 0.0005 (approximately).
    - Test B: `withOpenAIBudgetTracking` on retry for the same user same day uses ON CONFLICT to ADD to existing `usd_estimate`.
    - Test C: `withOpenAIBudgetTracking` accumulator errors do NOT throw — caller still receives fn's resolved value (non-fatal-by-design per ai-budget.ts:235 convention).

    All tests RED before Task 2. Use existing test fixtures + dependency-injection factory pattern from `captures-screenshot.ts` (Plan 02 also lands the DI factory).
  </behavior>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md (Wave 0 list + D8 round-trip),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Pattern 2 + Pitfall 3 strict call order),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (voice-transcribe.ts pattern),
    vigil-core/src/routes/__tests__/captures-screenshot.test.ts (closest test analog — mocked deps, DI),
    vigil-core/src/lib/__tests__/ai-budget.test.ts (existing test file to extend; if absent, create alongside),
    vigil-core/src/lib/audio-cap.ts,
    vigil-core/src/lib/ai-budget.ts
  </read_first>
  <action>
    Create `vigil-core/src/routes/__tests__/voice-transcribe.test.ts` using the Node `node:test` + `assert/strict` framework consistent with existing tests in `vigil-core/src/routes/__tests__/`. Mirror `captures-screenshot.test.ts` DI shape — pass a `Partial<VoiceTranscribeDeps>` factory with mocked `db`, mocked OpenAI client (via a `transcribeWav` override), and mocked `bus`. Mock the bearer-auth middleware by directly invoking the router and `c.set("userId", 1)` in test fixtures.

    Extend `vigil-core/src/lib/__tests__/ai-budget.test.ts` (creating it if absent) with tests A–C above. Use an in-memory or transactional DB fixture pattern already in use under `vigil-core/src/lib/__tests__/`.

    Tests MUST be RED at the end of this task — i.e., `voice-transcribe.ts`, `transcribe.ts`, `voice-errors.ts`, `withOpenAIBudgetTracking`, and `voiceCaptures` schema export do NOT yet exist. Run `cd vigil-core && npm test -- --test-name-pattern="voice-transcribe"` and confirm test-import failures (this is the RED state; do not commit yet).
  </action>
  <verify>
    <automated>test -f vigil-core/src/routes/__tests__/voice-transcribe.test.ts &amp;&amp; test -f vigil-core/src/lib/__tests__/ai-budget.test.ts &amp;&amp; cd vigil-core &amp;&amp; npm test -- --test-name-pattern="voice-transcribe" 2>&amp;1 | grep -qE "Cannot find module|fail"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-core/src/routes/__tests__/voice-transcribe.test.ts` exists
    - Source: `vigil-core/src/lib/__tests__/ai-budget.test.ts` exists and contains a test for `withOpenAIBudgetTracking`
    - Source: voice-transcribe.test.ts contains at least 9 distinct `test(...)` blocks covering happy-path, dedup, three transcribe-error codes, budget-exceeded, audio-cap, missing-id, no-auth
    - Source: voice-transcribe.test.ts references `VoiceTranscribeTimeoutError`, `VoiceTranscribeProviderDownError`, `VoiceTranscribeQuotaError` (RED — module not yet created)
    - Behavior: `cd vigil-core && npm test -- --test-name-pattern="voice-transcribe"` fails with module-not-found OR assertion failures (RED state confirmed)
  </acceptance_criteria>
  <done>Both test files exist; all 12 tests RED; no production code yet shipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0023 + voiceCaptures schema + run migration locally [BLOCKING]</name>
  <files>
    vigil-core/drizzle/0023_voice_capture_dedup.sql,
    vigil-core/src/db/schema.ts
  </files>
  <behavior>
    After this task:
    - `voice_captures` table exists in local Postgres with columns `id serial PK, user_id integer FK→users.id ON DELETE CASCADE, thought_id integer FK→thoughts.id ON DELETE SET NULL, client_capture_id text NOT NULL, queued_at timestamptz NOT NULL DEFAULT now(), retry_count integer NOT NULL DEFAULT 0`.
    - Composite partial unique index `uq_voice_captures_user_client_capture_id` exists on `(user_id, client_capture_id) WHERE client_capture_id IS NOT NULL`.
    - `voiceCaptures` exported from `vigil-core/src/db/schema.ts` with matching Drizzle column types.
    - Re-running `npm run db:migrate` is idempotent (IF NOT EXISTS throughout).
  </behavior>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 437-460 — full migration shape),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Gray Area #1 + #2 — `0023` + sibling table justification),
    vigil-core/drizzle/0021_add_work_orders_client_capture_id.sql (analog migration),
    vigil-core/drizzle/0022_add_work_orders_maintenance_problem_department.sql (latest applied migration — verify next slot is 0023),
    vigil-core/src/db/schema.ts (existing tables — append voiceCaptures pattern),
    vigil-core/package.json (db:migrate script entry — drizzle-kit migrate)
  </read_first>
  <action>
    Hand-craft `vigil-core/drizzle/0023_voice_capture_dedup.sql` (do NOT use `drizzle-kit generate` — per RESEARCH Anti-Patterns the partial unique WHERE clause requires hand-crafting). Use the exact SQL from PATTERNS.md §"0023_voice_capture_dedup.sql" — `CREATE TABLE IF NOT EXISTS "voice_captures"` + `--> statement-breakpoint` + `CREATE UNIQUE INDEX IF NOT EXISTS "uq_voice_captures_user_client_capture_id" ON "voice_captures" ("user_id", "client_capture_id") WHERE "client_capture_id" IS NOT NULL`.

    Append a `voiceCaptures = pgTable("voice_captures", { ... })` definition to `vigil-core/src/db/schema.ts` matching the SQL — columns `id (serial pk)`, `userId (integer notNull references users.id onDelete cascade)`, `thoughtId (integer references thoughts.id onDelete set null)`, `clientCaptureId (text notNull)`, `queuedAt (timestamp withTimezone defaultNow notNull)`, `retryCount (integer notNull default 0)`. Drizzle table builder cannot express the partial WHERE on the unique index — that lives only in the migration SQL.

    Run `cd vigil-core && npm run db:migrate` against local Postgres (DATABASE_URL points to local dev DB per `[feedback_wallclock_checkpoint_exempt]` auto-memory — dev migration is autonomous; prod migration is the operator wallclock in Plan 07). Verify the migration is applied via `psql $DATABASE_URL -c "\d voice_captures"` (table exists with expected columns) and `psql $DATABASE_URL -c "\d+ voice_captures"` (index `uq_voice_captures_user_client_capture_id` listed). Re-run `npm run db:migrate` and confirm it is a no-op (idempotent).

    **NOTE on production:** Production deploy must run `npm run db:migrate-prod` per `package.json` script. Per the orchestrator's note `[feedback_wallclock_checkpoint_exempt]`, the prod migration is an operator wallclock step — flagged in Plan 07 hardware UAT as a pre-condition.
  </action>
  <verify>
    <automated>test -f vigil-core/drizzle/0023_voice_capture_dedup.sql &amp;&amp; grep -q "voice_captures" vigil-core/drizzle/0023_voice_capture_dedup.sql &amp;&amp; grep -q "uq_voice_captures_user_client_capture_id" vigil-core/drizzle/0023_voice_capture_dedup.sql &amp;&amp; grep -q "voiceCaptures" vigil-core/src/db/schema.ts &amp;&amp; cd vigil-core &amp;&amp; npm run db:migrate 2>&amp;1 | tail -10 &amp;&amp; npm run db:migrate 2>&amp;1 | grep -qE "No migrations to apply|already" &amp;&amp; npm test -- --test-name-pattern="schema" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-core/drizzle/0023_voice_capture_dedup.sql` contains `CREATE TABLE IF NOT EXISTS "voice_captures"`
    - Source: 0023 SQL contains `CREATE UNIQUE INDEX IF NOT EXISTS "uq_voice_captures_user_client_capture_id"` and `WHERE "client_capture_id" IS NOT NULL`
    - Source: `vigil-core/src/db/schema.ts` exports `voiceCaptures` with `pgTable("voice_captures", { ... })`
    - Source: schema.ts voiceCaptures definition references `users.id` and `thoughts.id` foreign keys
    - Behavior: `cd vigil-core && npm run db:migrate` exits 0 (first run applies migration)
    - Behavior: `cd vigil-core && npm run db:migrate` second run exits 0 with output containing "No migrations to apply" or equivalent (idempotent)
    - CLI: `psql "$DATABASE_URL" -c "\d voice_captures"` lists all 6 columns (id, user_id, thought_id, client_capture_id, queued_at, retry_count)
    - CLI: `psql "$DATABASE_URL" -c "SELECT indexname FROM pg_indexes WHERE tablename='voice_captures';"` includes `uq_voice_captures_user_client_capture_id`
  </acceptance_criteria>
  <done>Migration 0023 applied locally; `voice_captures` table + partial unique index present; `voiceCaptures` Drizzle export available for import in voice-transcribe.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Production route + transcribe helper + error classes + OpenAI budget adapter</name>
  <files>
    vigil-core/src/routes/voice-transcribe.ts,
    vigil-core/src/routes/voice-errors.ts,
    vigil-core/src/ai/transcribe.ts,
    vigil-core/src/lib/ai-budget.ts,
    vigil-core/src/index.ts,
    vigil-core/src/analytics/posthog.ts
  </files>
  <behavior>
    - All 9 Task 1 voice-transcribe.test.ts tests pass (RED → GREEN).
    - All 3 Task 1 ai-budget.test.ts withOpenAIBudgetTracking tests pass.
    - `app.onError` translates the three new errors to 504/502/503 with the locked-enum codes.
    - `/v1/voice/transcribe` is mounted on the Hono app under `/v1`.
    - PostHog `SENSITIVE_ROUTES` Set contains `/v1/voice/transcribe`.
    - Strict call order per RESEARCH Pitfall 3: `requireAiBudget(userId)` → `c.req.json()` → `assertAudioSessionWithinCap(body.audio)` → dedup SELECT → `withOpenAIBudgetTracking(userId, durationMs, () => transcribeWav(wav))` → `db.insert(thoughts)` → `db.insert(voiceCaptures)` → `bus.emitThoughtCreated(userId, payload)` (call site only; method ships in Plan 03 as a stub method here) → fire-and-forget triage → return JSON.
  </behavior>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 98-259 — exact pattern + import block + DI factory + auth-chain + dedup + insert sequence + error class shape; lines 660-693 — withOpenAIBudgetTracking pattern),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Pattern 2 lines 419-503 + Pitfall 3 + Gray Area #5 timeout shape lines 167-195 + Pitfall 6 SSE-after-commit + Anti-Patterns "Using withBudgetTracking for OpenAI without adapting the accumulator" lines 631),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-U3 + D-U4 + D-E1 + D-E3),
    vigil-core/src/routes/captures-screenshot.ts (closest route analog — DI factory, dedup SELECT, drizzle insert sequence),
    vigil-core/src/ai/client.ts (Anthropic lazy-init analog for OpenAI mirror),
    vigil-core/src/lib/ai-budget.ts (existing `withBudgetTracking` + `DailyBudgetExceededError` — append `withOpenAIBudgetTracking`),
    vigil-core/src/lib/audio-cap.ts (assertAudioSessionWithinCap + AudioSessionTooLongError reference),
    vigil-core/src/index.ts (lines around app.onError translation table — add 3 new branches),
    vigil-core/src/analytics/posthog.ts (SENSITIVE_ROUTES Set + BLOCKED_PROPERTY_NAMES verification),
    vigil-core/src/lib/agent-events-bus.ts (existing emit pattern — write call site `bus.emitThoughtCreated`; method body ships in Plan 03)
  </read_first>
  <action>
    1. Create `vigil-core/src/routes/voice-errors.ts` with three error classes — `VoiceTranscribeTimeoutError`, `VoiceTranscribeProviderDownError`, `VoiceTranscribeQuotaError`. Each has `readonly code = "VOICE_TRANSCRIBE_TIMEOUT" as const` (etc.) and `super(message)` + `this.name = "..."`. Follow shape from `ai-budget.ts:99-110` `DailyBudgetExceededError`.

    2. Create `vigil-core/src/ai/transcribe.ts`:
       - `getTranscribeClient(): OpenAI | null` lazy-init reading `process.env.OPENAI_API_KEY` (mirror `client.ts` Anthropic pattern).
       - `export async function transcribeWav(wav: Buffer): Promise<{ text: string; durationMs: number }>`. Estimate `durationMs = (wav.length - 44) / 32` (16 kHz × 16 bit × mono = 32,000 bytes/sec ÷ 1000 = 32 bytes/ms; subtract 44-byte header). Use `OpenAI.toFile(wav, "audio.wav", { type: "audio/wav" })` + `ai.audio.transcriptions.create({ file, model: "gpt-4o-mini-transcribe" }, { signal: controller.signal })`. AbortController timeout 30,000 ms. Catch `AbortError` → throw `VoiceTranscribeTimeoutError`. Catch OpenAI `status === 429` OR message containing "quota" → throw `VoiceTranscribeQuotaError`. Other errors → throw `VoiceTranscribeProviderDownError`. `finally { clearTimeout(timeout); }`. If `getTranscribeClient()` returns null → throw `VoiceTranscribeProviderDownError`.

    3. Extend `vigil-core/src/lib/ai-budget.ts`:
       - Add constant `const OPENAI_TRANSCRIBE_PRICE_PER_SEC = 0.003 / 60;`.
       - Add `export async function withOpenAIBudgetTracking<T>(userId: number, durationMs: number, fn: () => Promise<T>): Promise<T>` that runs `fn()` first then accumulates `usd = (durationMs / 1000) * OPENAI_TRANSCRIBE_PRICE_PER_SEC` into `ai_usage_daily` via the same `INSERT … ON CONFLICT (user_id, usage_date) DO UPDATE SET usd_estimate = ai_usage_daily.usd_estimate + EXCLUDED.usd_estimate` pattern used by existing `withBudgetTracking`. Accumulator errors are non-fatal (try/catch + `console.error` per existing convention at line 235).

    4. Create `vigil-core/src/routes/voice-transcribe.ts`:
       - Imports per PATTERNS.md lines 102-111.
       - `createVoiceTranscribeRoute(deps: Partial<VoiceTranscribeDeps> = {}): Hono` DI factory (mirror `captures-screenshot.ts:155-165`).
       - Route handler `router.post("/voice/transcribe", async (c) => { ... })` with strict call order: `userId = c.get("userId") as number;` → `await requireAiBudget(userId)` → parse JSON body `{ audio: string; clientCaptureId: string }` → 400 if either missing → `assertAudioSessionWithinCap(body.audio)` → SELECT `voiceCaptures` by `(userId, clientCaptureId)`. If hit AND existing.thoughtId not null → SELECT thought by id → return 200 `{ thoughtId, content }`. Else: `const wav = Buffer.from(body.audio, "base64")` → estimate `durationMs` from `wav.length` → `const { text } = await withOpenAIBudgetTracking(userId, durationMs, () => transcribeWav(wav))`. Insert thoughts row `{ userId, content: text, source: "g2_voice", cloudKitRecordID: crypto.randomUUID() }` returning. Insert voiceCaptures row `{ userId, thoughtId: row.id, clientCaptureId: body.clientCaptureId }`. Call `bus.emitThoughtCreated(userId, { thoughtId: row.id, content: text })` (per Pitfall 6 — AFTER db commit, NEVER inside a transaction). Fire-and-forget `void runTriage(userId, row.id, text)` per existing `process-audio.ts:172-194` pattern. Return `c.json({ thoughtId: row.id, content: text }, 201)`.
       - Export `export const voiceTranscribe = createVoiceTranscribeRoute();` production singleton.
       - NOTE: `bus.emitThoughtCreated` does not exist yet — add a TEMPORARY shim method to `agent-events-bus.ts` that emits to a `THOUGHT_CREATED_NAME = "thought-created"` channel without yet adding the listener triple. Plan 03 then promotes this to the full `emit/on/off + joint cleanup gate` triple per PATTERNS.md lines 503-535.

    5. Modify `vigil-core/src/index.ts`:
       - Add `import { voiceTranscribe } from "./routes/voice-transcribe.js";` (replacing the deleted `voiceSpike` import).
       - Add `app.route("/v1", voiceTranscribe);` near the existing route mounts (mirror line where `voiceSpike` was mounted at line 232).
       - Extend `app.onError`: import `VoiceTranscribeTimeoutError`, `VoiceTranscribeProviderDownError`, `VoiceTranscribeQuotaError` from `./routes/voice-errors.js`. Add three branches: `if (err instanceof VoiceTranscribeTimeoutError) return c.json({ error: err.message, code: err.code }, 504);` (analog for 502 and 503).

    6. Modify `vigil-core/src/analytics/posthog.ts`: add `"/v1/voice/transcribe"` to the `SENSITIVE_ROUTES` Set. Verify `BLOCKED_PROPERTY_NAMES` already includes `audio`, `audioPcm`, `audio_pcm`, `pcm` (RESEARCH note — no add needed).

    Run `cd vigil-core && npm test -- --test-name-pattern="voice-transcribe"` and `npm test -- --test-name-pattern="ai-budget"` — all RED tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd vigil-core &amp;&amp; npm test -- --test-name-pattern="voice-transcribe" 2>&amp;1 | tail -25 &amp;&amp; npm test -- --test-name-pattern="ai-budget" 2>&amp;1 | tail -15 &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-core/src/routes/voice-errors.ts` exports `VoiceTranscribeTimeoutError`, `VoiceTranscribeProviderDownError`, `VoiceTranscribeQuotaError` each with `readonly code` matching `VOICE_TRANSCRIBE_TIMEOUT` / `VOICE_TRANSCRIBE_PROVIDER_DOWN` / `VOICE_TRANSCRIBE_QUOTA`
    - Source: `vigil-core/src/ai/transcribe.ts` exports `transcribeWav` with signature returning `Promise<{ text: string; durationMs: number }>` AND contains `gpt-4o-mini-transcribe` model literal AND uses `AbortController` with `30_000` ms timeout
    - Source: `vigil-core/src/routes/voice-transcribe.ts` contains the literal `bus.emitThoughtCreated(`
    - Source: `vigil-core/src/routes/voice-transcribe.ts` contains the literal `withOpenAIBudgetTracking(`
    - Source: `vigil-core/src/routes/voice-transcribe.ts` contains the literal `source: "g2_voice"`
    - Source: `vigil-core/src/lib/ai-budget.ts` exports `withOpenAIBudgetTracking` AND contains `0.003 / 60` rate constant
    - Source: `vigil-core/src/index.ts` contains `voiceTranscribe` import + `app.route("/v1", voiceTranscribe)` mount
    - Source: `vigil-core/src/index.ts` has three new `app.onError` branches for `VoiceTranscribeTimeoutError → 504`, `VoiceTranscribeProviderDownError → 502`, `VoiceTranscribeQuotaError → 503`
    - Source: `vigil-core/src/analytics/posthog.ts` SENSITIVE_ROUTES Set includes `"/v1/voice/transcribe"`
    - Behavior: `cd vigil-core && npm test -- --test-name-pattern="voice-transcribe"` exits 0 with all 9 tests passing
    - Behavior: `cd vigil-core && npm test -- --test-name-pattern="ai-budget"` exits 0 with `withOpenAIBudgetTracking` tests A, B, C passing
    - Behavior: `cd vigil-core && npm test` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Production route mounted, three error classes translated to locked-enum HTTP codes, OpenAI budget adapter accumulating duration-based USD, all RED tests now GREEN. Plan 03 takes over for SSE wiring + PWA-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| G2 plugin → `POST /v1/voice/transcribe` | Untrusted base64 audio + UUID crosses to server |
| Server → OpenAI Whisper-mini API | Server-side egress, third-party LLM provider (no PII in audio per project scope) |
| Server → Postgres | Authenticated DB; voice_captures and thoughts tables receive new writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-02-S | Spoofing | route auth | mitigate | `bearerAuth` + `requireVerifiedEmail` middleware enforced by `app.route("/v1", voiceTranscribe)` mount — userId derived from `c.get("userId")`, NEVER from body (T-129.1-09 reuse) |
| T-130-02-T-1 | Tampering | audio cap bypass | mitigate | `assertAudioSessionWithinCap(body.audio)` runs after JSON parse, BEFORE `Buffer.from` — RESEARCH Pitfall 3 (prevents 100MB DoS via base64-only buffer growth) |
| T-130-02-T-2 | Tampering | dedup race | mitigate | Composite partial unique index `(user_id, client_capture_id) WHERE NOT NULL` on `voice_captures` — concurrent retries with same clientCaptureId collide on insert; SELECT-first dedup short-circuits BEFORE OpenAI call (RESEARCH Gray Area #2) |
| T-130-02-I | Information Disclosure | OpenAI cost leak | mitigate | `withOpenAIBudgetTracking` accumulates duration-based USD into `ai_usage_daily` per-user; `requireAiBudget(userId)` pre-flight throws 429 BEFORE body parse if cap exceeded — Phase 127 GUARD-03 cascades correctly per D-E3 |
| T-130-02-D | Denial of Service | cost runaway | mitigate | Three-layer cost gate: (1) `requireAiBudget` pre-flight per-user daily cap $0.50; (2) `assertAudioSessionWithinCap` 60s/1.92 MB per-request hard cap; (3) `withOpenAIBudgetTracking` accumulator increments after each successful transcribe |
| T-130-02-R | Repudiation | thought provenance | mitigate | `source: "g2_voice"` on every insert from this route; voice_captures dedup row records `(userId, thoughtId, clientCaptureId, queuedAt)` for audit trail |
| T-130-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 130 (RESEARCH Package Legitimacy Audit) — `openai@^4.79.0` already in `package.json` from 128a Plan 01 |
</threat_model>

<verification>
- All Wave 0 tests green
- Migration 0023 applied; `voice_captures` table + partial unique index present locally
- `app.onError` translation table has three new branches (504/502/503)
- `bus.emitThoughtCreated` stub method exists on `agent-events-bus.ts` (full triple wiring in Plan 03)
</verification>

<success_criteria>
- VOICE-05 requirement satisfied: bearerAuth + email-verified + JSON-base64 + OpenAI transcribe + thoughts row with `source='g2_voice'` + fire-and-forget triage
- Three locked-enum error codes mapped through `app.onError`
- Dedup primitive functional — second POST with same clientCaptureId returns 200 with existing thoughtId, OpenAI called exactly once
- OpenAI duration-based USD accumulates against existing $0.50/user/day cap
- Full vigil-core test suite green
</success_criteria>

<output>
Create `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-02-SUMMARY.md` when done.
</output>
