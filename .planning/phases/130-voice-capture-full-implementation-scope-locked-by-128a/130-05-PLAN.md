---
phase: 130
plan: 05
type: execute
wave: 4
depends_on:
  - 130-04
files_modified:
  - vigil-g2-plugin/src/lib/voice-queue.ts
  - vigil-g2-plugin/src/lib/__tests__/voice-queue.test.ts
  - vigil-g2-plugin/src/screens/voice.ts
  - vigil-g2-plugin/src/screens/companion.ts
autonomous: true
requirements:
  - VOICE-07
  - VOICE-08
requirements_addressed:
  - VOICE-07
  - VOICE-08
must_haves:
  truths:
    - "Queue persists to localStorage at key 'vigil:voice-queue:v1' as JSON array of QueueEntry"
    - "Failed POST enqueues entry with retryCount=0 and queuedAt=now"
    - "Retry backoff is [1s, 2s, 4s, 8s, 16s, 30s] indexed by retryCount; entries with retryCount >= 6 are evicted"
    - "Max queue size = 10; on 11th enqueue, OLDEST entry is evicted LRU-style and a PostHog 'voice_queue_evicted' event fires"
    - "HTTP 429 DAILY_AI_BUDGET_EXCEEDED is a permanent failure — entry evicted without retry"
    - "Companion HUD body line 3 shows 'syncing N voice captures…' when queue depth > 0; hidden when queue depth = 0"
    - "[NO MIC] state on Voice screen has priority over queue indicator on Companion HUD body line 3"
    - "PostHog 'voice_capture_completed' event fires on success with keys { stop_to_http_ms, chunks, bytes, retry_count, transcript_chars } — no audioPcm/audio/pcm property names"
    - "PostHog 'voice_capture_dropout' event fires on inter-chunk gap > 2× first-5s baseline with keys { gap_ms, recording_id }"
  artifacts:
    - path: "vigil-g2-plugin/src/lib/voice-queue.ts"
      provides: "enqueue + retry loop + LRU eviction + persistence"
      exports: ["enqueue", "drainQueue", "queueDepth", "loadQueue"]
  key_links:
    - from: "vigil-g2-plugin/src/screens/voice.ts (Plan 04)"
      to: "vigil-g2-plugin/src/lib/voice-queue.ts (this plan)"
      via: "On network error or 5xx response → enqueue + show [ERR]"
      pattern: "enqueue"
    - from: "vigil-g2-plugin/src/lib/voice-queue.ts"
      to: "vigil-g2-plugin/src/screens/companion.ts (HUD body line 3)"
      via: "queueDepth() consumed in body-builder; line hidden when 0"
      pattern: "syncing"
    - from: "vigil-g2-plugin/src/lib/voice-queue.ts"
      to: "PostHog client"
      via: "posthog.capture('voice_capture_evicted', ...) + 'voice_capture_completed' + 'voice_capture_dropout'"
      pattern: "voice_capture_"
---

<objective>
Land the offline queue + telemetry layer: (1) `voice-queue.ts` localStorage-persisted queue with `[1s, 2s, 4s, 8s, 16s, 30s]` backoff (Phase 124 D-11) and LRU eviction at max 10 entries; (2) wire `voice.ts` so failed POSTs enqueue instead of just surfacing `[ERR]`; (3) Companion HUD body line 3 shows `syncing N voice captures…` when queue depth > 0 with `[NO MIC]` priority override per D-O3 + RESEARCH Gray Area #6; (4) PostHog `voice_capture_completed` + `voice_capture_dropout` + `voice_capture_evicted` events with safe key names per D-T1 + D-T2.

Purpose: Closes VOICE-07 (offline queue + HUD indicator) and the telemetry portion of VOICE-08 (drift detectors land in Plan 06). Operator can record offline; the queue drains when network returns; full queue evicts oldest entry LRU-style.
Output: Plug-in handles network outages gracefully + emits operator-facing telemetry; drift detectors in Plan 06 lock the safe-key contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-04-SUMMARY.md
@vigil-g2-plugin/src/screens/voice.ts
@vigil-g2-plugin/src/screens/companion.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — voice-queue tests for backoff + LRU + persistence + permanent-failure semantics</name>
  <files>
    vigil-g2-plugin/src/lib/__tests__/voice-queue.test.ts
  </files>
  <behavior>
    - Test 1: `enqueue({clientCaptureId, base64Audio, queuedAt, retryCount: 0})` then `loadQueue()` returns array with that entry; `localStorage.getItem('vigil:voice-queue:v1')` is JSON-parseable.
    - Test 2: Enqueue 11 entries → oldest evicted (LRU; first-in is first-out); `localStorage` array has exactly 10 entries; `posthog.capture` called with event `'voice_queue_evicted'` and props `{ clientCaptureId, retryCount }` (no `base64Audio` in props).
    - Test 3: `drainQueue(fetchMock)` where the first entry's POST resolves 201 → entry removed from queue; `queueDepth()` decrements by 1.
    - Test 4: Entry with `retryCount = 0` → first retry delay = 1000 ms; `retryCount = 1` → 2000 ms; ...; `retryCount = 5` → 30000 ms.
    - Test 5: Entry with `retryCount = 6` (past schedule) → entry evicted without retry; `posthog.capture('voice_queue_evicted', ...)`.
    - Test 6: Mock POST returns 429 with code `DAILY_AI_BUDGET_EXCEEDED` → entry evicted immediately (no retry), separate `posthog.capture('voice_queue_evicted', { reason: 'daily_budget_cap_hit' })`.
    - Test 7: Mock POST returns 504 `VOICE_TRANSCRIBE_TIMEOUT` → entry NOT evicted; `retryCount` incremented; remains in queue for next drain cycle.
    - Test 8: Persistence across reload — `enqueue(...)`, then simulate WebView reload by clearing in-memory state and calling `loadQueue()` → entry recovered from localStorage.
    - Test 9: `voice_capture_completed` PostHog event fires on success with exact key set `{ stop_to_http_ms, chunks, bytes, retry_count, transcript_chars }` and NO `audioPcm` / `audio` / `pcm` keys.
    - Test 10: `voice_capture_dropout` fires when synthetic inter-chunk gap > 2× baseline; props `{ gap_ms, recording_id }` only.
  </behavior>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 380-432 — voice-queue shape + LRU eviction + backoff loop),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Pattern 5 lines 600-619 — queue shape + persistence note),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-O1 / D-O2 / D-O3 / D-O4 + D-T1 / D-T2 + D-E3 — DAILY_AI_BUDGET cascade),
    vigil-g2-plugin/src/__tests__ existing tests (test framework convention)
  </read_first>
  <action>
    Create `vigil-g2-plugin/src/lib/__tests__/voice-queue.test.ts` using `node:test` + `assert/strict`. Mock `localStorage` (Node `node:test` does not provide a DOM — use a minimal in-memory shim: `globalThis.localStorage = { _data: {}, getItem(k){return this._data[k]||null}, setItem(k,v){this._data[k]=v}, removeItem(k){delete this._data[k]} }`). Mock `posthog.capture` via a spy array.

    For Test 4 backoff delays: do not actually wait — instead expose a `getNextDelayMs(retryCount: number)` helper on the queue module and assert directly on the returned value. The real-time retry loop uses `setTimeout(getNextDelayMs(retryCount))` internally.

    For Test 7: mock fetch returns `Response` with status 504 and body `{ code: "VOICE_TRANSCRIBE_TIMEOUT" }`; call `drainQueue(fetchMock)`; assert entry still present with `retryCount += 1`.

    All 10 tests RED at end of task.
  </action>
  <verify>
    <automated>test -f vigil-g2-plugin/src/lib/__tests__/voice-queue.test.ts &amp;&amp; cd vigil-g2-plugin &amp;&amp; npm test -- --test-name-pattern="voice-queue" 2>&amp;1 | grep -qE "fail|Cannot find"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-g2-plugin/src/lib/__tests__/voice-queue.test.ts` exists and contains at least 10 distinct `test(...)` blocks
    - Source: test file asserts the literal backoff schedule values `1000, 2000, 4000, 8000, 16000, 30000`
    - Source: test file asserts `MAX_QUEUE_SIZE === 10` and references `vigil:voice-queue:v1` localStorage key
    - Source: test file asserts PostHog event NAMES `voice_capture_completed`, `voice_capture_dropout`, `voice_queue_evicted`
    - Source: test file asserts the safe key set `{ stop_to_http_ms, chunks, bytes, retry_count, transcript_chars }` for `voice_capture_completed`
    - Behavior: `cd vigil-g2-plugin && npm test -- --test-name-pattern="voice-queue"` fails (RED — voice-queue.ts not yet implemented)
  </acceptance_criteria>
  <done>All 10 queue tests RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voice-queue.ts implementation + voice.ts integration + Companion HUD body line 3</name>
  <files>
    vigil-g2-plugin/src/lib/voice-queue.ts,
    vigil-g2-plugin/src/screens/voice.ts,
    vigil-g2-plugin/src/screens/companion.ts
  </files>
  <behavior>
    - All 10 Wave 0 tests GREEN.
    - On 5xx / network error in voice.ts POST: enqueue entry + show `[ERR]` retry-tap-to-dismiss; queue drains on next online detection or scheduled retry tick.
    - On 429 `DAILY_AI_BUDGET_EXCEEDED`: do NOT enqueue (evict + permanent fail per D-E3); show `[ERR]` body line 2 `daily AI cost cap hit — try tomorrow`.
    - Companion HUD body line 3 renders `syncing N voice captures…` whenever `queueDepth() > 0`; renders nothing when queue depth = 0; `[NO MIC]` state (sourced from voice.ts module state) wins per Gray Area #6 priority ladder.
    - On every successful POST (online path OR queue drain): emit `posthog.capture('voice_capture_completed', { stop_to_http_ms, chunks, bytes, retry_count, transcript_chars })` with safe keys only.
    - On detected drop-out (inter-chunk gap > 2× first-5s baseline): emit `posthog.capture('voice_capture_dropout', { gap_ms, recording_id })` with safe keys only.
  </behavior>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 380-432 — voice-queue full pattern with QUEUE_KEY + BACKOFF_MS + MAX_QUEUE_SIZE + enqueue + retryEntry; lines 309-377 — voice.ts current shape from Plan 04 to extend),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Pattern 5 lines 596-621 — queue shape + Anti-Patterns lines 627-631 — base64Audio MUST NOT leak into PostHog events; Gray Area #6 lines 197-208 — priority ladder for Companion HUD body line 3),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-O1 backoff + D-O2 persistence + D-O3 HUD line + D-O4 LRU + D-E3 permanent-failure cascade + D-T1 / D-T2),
    vigil-g2-plugin/src/screens/voice.ts (Plan 04 shipped this file — extend the error branch to enqueue),
    vigil-g2-plugin/src/screens/companion.ts (existing 3-container body block; this plan adds a third-body-line conditional render)
  </read_first>
  <action>
    Create `vigil-g2-plugin/src/lib/voice-queue.ts`:
    - Constants: `QUEUE_KEY = 'vigil:voice-queue:v1'`, `MAX_QUEUE_SIZE = 10`, `BACKOFF_MS = [1000, 2000, 4000, 8000, 16000, 30000]`.
    - `interface QueueEntry { clientCaptureId: string; base64Audio: string; queuedAt: number; retryCount: number; }`.
    - `loadQueue(): QueueEntry[]` reads localStorage with try/catch → empty array on parse failure.
    - `saveQueue(q: QueueEntry[]): void` JSON.stringify + setItem.
    - `enqueue(entry: QueueEntry): void` — if `q.length >= MAX_QUEUE_SIZE`, shift oldest and `posthog.capture('voice_queue_evicted', { clientCaptureId, retryCount })`; push new entry; save.
    - `getNextDelayMs(retryCount: number): number` returns `BACKOFF_MS[Math.min(retryCount, BACKOFF_MS.length - 1)]`.
    - `drainQueue(fetcher: typeof fetch, apiKey: string): Promise<void>` — for each entry in queue, awaits `getNextDelayMs(retryCount)`, POSTs, on 2xx removes from queue + fires `voice_capture_completed` event, on 429 with `DAILY_AI_BUDGET_EXCEEDED` evicts permanently + fires `voice_queue_evicted` with `{ reason: 'daily_budget_cap_hit' }`, on 5xx/timeout/network error increments retryCount + re-saves; if retryCount >= 6 after increment, evicts.
    - `queueDepth(): number` returns `loadQueue().length`.

    Modify `vigil-g2-plugin/src/screens/voice.ts`:
    - In the POST error branch, call `enqueue({ clientCaptureId, base64Audio: audio, queuedAt: Date.now(), retryCount: 0 })` BEFORE setting stateLine `[ERR]`.
    - Distinguish 429 `DAILY_AI_BUDGET_EXCEEDED` — do NOT enqueue; show `[ERR]` with body line 2 `daily AI cost cap hit — try tomorrow` (per D-E3).
    - On successful 2xx response: emit `posthog.capture('voice_capture_completed', { stop_to_http_ms, chunks: pcmChunks.length, bytes: wavBlob.length, retry_count: 0, transcript_chars: response.content.length })`.
    - Implement drop-out detection: maintain a `firstFiveSecBaseline` rolling average of inter-chunk gaps during the first 5 s of recording; for subsequent chunks where `gap_ms > 2 * baseline`, fire `posthog.capture('voice_capture_dropout', { gap_ms, recording_id })` with `recording_id` being the same UUID v4 as `clientCaptureId`.

    Modify `vigil-g2-plugin/src/screens/companion.ts`:
    - Read `queueDepth()` from `../lib/voice-queue.ts`.
    - Read `[NO MIC]` flag from voice screen module state (via exported getter — add `getVoiceStateLine(): StateLine` to voice.ts if not already exported).
    - In the body-builder, body line 3 renders:
      - `enable mic in Hub` if voiceStateLine === `[NO MIC]` (priority 1 per Gray Area #6).
      - `syncing ${n} voice captures…` if voiceStateLine !== `[NO MIC]` AND queueDepth() > 0 (priority 2).
      - Empty otherwise.

    Run `cd vigil-g2-plugin && npm test -- --test-name-pattern="voice-queue"` and full suite — RED → GREEN.
  </action>
  <verify>
    <automated>test -f vigil-g2-plugin/src/lib/voice-queue.ts &amp;&amp; cd vigil-g2-plugin &amp;&amp; npm test -- --test-name-pattern="voice-queue" 2>&amp;1 | tail -15 &amp;&amp; npm test 2>&amp;1 | tail -10 &amp;&amp; grep -q "1000, 2000, 4000, 8000, 16000, 30000" vigil-g2-plugin/src/lib/voice-queue.ts &amp;&amp; grep -q "MAX_QUEUE_SIZE = 10" vigil-g2-plugin/src/lib/voice-queue.ts &amp;&amp; grep -q "vigil:voice-queue:v1" vigil-g2-plugin/src/lib/voice-queue.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-g2-plugin/src/lib/voice-queue.ts` exports `enqueue`, `drainQueue`, `queueDepth`, `loadQueue`, `getNextDelayMs`
    - Source: voice-queue.ts contains the literal arrays/constants `[1000, 2000, 4000, 8000, 16000, 30000]`, `MAX_QUEUE_SIZE = 10`, `'vigil:voice-queue:v1'`
    - Source: voice-queue.ts contains `posthog.capture` calls for `voice_queue_evicted` AND `voice_capture_completed` event names
    - Source: voice-queue.ts does NOT include the literal property name `audioPcm` / `audio_pcm` / `pcm` / `audio` in any PostHog event props (only safe keys)
    - Source: `vigil-g2-plugin/src/screens/voice.ts` references `enqueue(` and routes 5xx/network errors there; routes 429 `DAILY_AI_BUDGET_EXCEEDED` to permanent-fail copy
    - Source: voice.ts emits `voice_capture_completed` with the exact safe-key set on success
    - Source: voice.ts emits `voice_capture_dropout` on inter-chunk gap > 2× baseline
    - Source: `vigil-g2-plugin/src/screens/companion.ts` body line 3 reads `queueDepth()` AND conditionally renders `syncing` substring; `[NO MIC]` priority override pinned
    - Behavior: `cd vigil-g2-plugin && npm test -- --test-name-pattern="voice-queue"` exits 0 (all 10 tests GREEN)
    - Behavior: `cd vigil-g2-plugin && npm test` exits 0 (full plugin suite green)
  </acceptance_criteria>
  <done>Offline queue + telemetry events fully wired. Drift detectors in Plan 06 lock the contracts so future commits cannot regress the safe-key invariant.</done>
</task>

</tasks>

<threat_model>
## No new threats — inherits Phase 127/128a guardrails

This plan adds offline queue + telemetry events. All PostHog events use safe key names (`bytes`, `chunks`, `stop_to_http_ms`, `retry_count`, `transcript_chars`, `gap_ms`, `recording_id`, `clientCaptureId`, `reason`). Phase 127 GUARD-01 `BLOCKED_PROPERTY_NAMES` already filters `audio` / `audioPcm` / `pcm` — Plan 06 D-D2 drift detector source-greps the plugin to prevent regression. `base64Audio` lives only in the in-memory + localStorage queue entry; it never crosses to PostHog (Test 9 pins).

No new packages installed. No new trust boundaries.
</threat_model>

<verification>
- All 10 voice-queue Wave 0 tests GREEN
- Full vigil-g2-plugin suite green
- Companion HUD body line 3 conditional render pinned by voice.test.ts cross-screen test (extended in this plan)
</verification>

<success_criteria>
- VOICE-07 satisfied: exact `[1s, 2s, 4s, 8s, 16s, 30s]` backoff + max 10 queue + LRU eviction + "syncing N voice captures" HUD line
- VOICE-08 telemetry portion satisfied: `voice_capture_completed` + `voice_capture_dropout` events with safe keys
- D-E3 cascade pinned: 429 `DAILY_AI_BUDGET_EXCEEDED` is permanent failure, evicts immediately
</success_criteria>

<output>
Create `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-05-SUMMARY.md` when done.
</output>
