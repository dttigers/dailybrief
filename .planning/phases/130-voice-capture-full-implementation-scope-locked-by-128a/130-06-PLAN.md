---
phase: 130
plan: 06
type: execute
wave: 4
depends_on:
  - 130-02
  - 130-04
files_modified:
  - vigil-g2-plugin/src/__tests__/wav-encoder.test.ts
  - vigil-core/src/__tests__/audio-log-redaction.test.ts
  - vigil-pwa/src/__tests__/denylist-parity.test.ts
  - vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts
autonomous: true
requirements:
  - VOICE-08
requirements_addressed:
  - VOICE-08
must_haves:
  truths:
    - "D-D1: wav-encoder produces a 44-byte header pinned byte-for-byte at all 8 D-D1 positions; regression to 8 kHz or stereo breaks the test"
    - "D-D2: source-grep across vigil-g2-plugin/src/, vigil-core/src/routes/voice-transcribe.ts, vigil-pwa/src/ confirms no `audioPcm` / `audio_pcm` / `pcm` property name reaches console.log / Sentry.captureException / posthog.capture call sites"
    - "D-D3: across vigil-g2-plugin/src/ (excluding tests + comments), count of `safeAudioControl(true,` calls equals count of `safeAudioControl(false,` calls"
  artifacts:
    - path: "vigil-g2-plugin/src/__tests__/wav-encoder.test.ts"
      provides: "D-D1 byte-for-byte WAV header pin"
      contains: "RIFF"
    - path: "vigil-core/src/__tests__/audio-log-redaction.test.ts"
      provides: "D-D2 extended scope — vigil-g2-plugin source + voice-transcribe.ts route + plugin telemetry"
      contains: "audioPcm"
    - path: "vigil-pwa/src/__tests__/denylist-parity.test.ts"
      provides: "D-D2 PWA-side parity"
      contains: "audioPcm"
    - path: "vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts"
      provides: "D-D3 source-grep parity"
      contains: "safeAudioControl"
  key_links:
    - from: "vigil-g2-plugin/src/__tests__/wav-encoder.test.ts"
      to: "vigil-g2-plugin/src/lib/wav-encoder.ts (Plan 04)"
      via: "import buildWav + assert all 8 D-D1 header positions"
      pattern: "buildWav"
    - from: "vigil-core/src/__tests__/audio-log-redaction.test.ts"
      to: "vigil-g2-plugin/src/ + vigil-core/src/routes/voice-transcribe.ts"
      via: "extended ROOT walk + grep patterns for audioPcm/audio_pcm/pcm in console.log/Sentry/PostHog call sites"
      pattern: "audioPcm"
    - from: "vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts"
      to: "vigil-g2-plugin/src/ source"
      via: "grep -c 'safeAudioControl(true,' must equal grep -c 'safeAudioControl(false,'"
      pattern: "safeAudioControl"
---

<objective>
Lock the three D-D drift detectors so future commits cannot silently regress the WAV header structure, the no-audioPcm-in-logs invariant, or the audioControl pairing invariant. After Plans 02 and 04 ship the production code, this plan pins the contracts in CI.

Purpose: Closes VOICE-08 — the drift detector portion. Phase 130 production code is well-formed today; this plan ensures it stays well-formed across all future v3.9+ work that touches the plugin or the voice-transcribe route.
Output: Three drift detector tests (one new, two extensions) all GREEN; CI fails on any future commit that regresses the WAV header, leaks audioPcm to a log sink, or introduces an orphan `safeAudioControl(true,` call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md
@.planning/phases/127-pre-spike-guardrails/127-CONTEXT.md
@vigil-core/src/__tests__/audio-log-redaction.test.ts
@vigil-pwa/src/__tests__/denylist-parity.test.ts
@vigil-g2-plugin/src/lib/wav-encoder.ts
@vigil-g2-plugin/src/screens/voice.ts
@vigil-g2-plugin/src/main.ts
@vigil-core/src/routes/voice-transcribe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-D1 — finalize wav-encoder.test.ts with all 8 header byte positions pinned</name>
  <files>
    vigil-g2-plugin/src/__tests__/wav-encoder.test.ts
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 796-820 — wav-encoder.test.ts template; lines 270-303 — D-D1 byte map),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-D1 — RIFF / WAVE / fmt / 1ch / 16kHz / 16bit / data positions),
    vigil-g2-plugin/src/lib/wav-encoder.ts (Plan 04 implementation — the test pins this implementation against drift)
  </read_first>
  <action>
    Extend (or fully author if Plan 04 only stubbed it) `vigil-g2-plugin/src/__tests__/wav-encoder.test.ts` so it asserts ALL 8 D-D1 header positions byte-for-byte:

    1. Bytes 0-3 = `0x52 0x49 0x46 0x46` (`"RIFF"`)
    2. Bytes 4-7 = uint32 LE total length (verify via DataView.getUint32(4, true) === 36 + pcm.length)
    3. Bytes 8-11 = `0x57 0x41 0x56 0x45` (`"WAVE"`)
    4. Bytes 12-15 = `0x66 0x6d 0x74 0x20` (`"fmt "`)
    5. Byte 22 (uint16 LE) === 1 (mono channel count)
    6. Byte 24 (uint32 LE) === 16000 (sample rate)
    7. Byte 34 (uint16 LE) === 16 (bit depth)
    8. Bytes 36-39 = `0x64 0x61 0x74 0x61` (`"data"`)

    Bonus assertions (low-cost, high-signal):
    - Byte 28 (uint32 LE) === 32000 (byte rate)
    - Byte 32 (uint16 LE) === 2 (block align)
    - Byte 40 (uint32 LE) === pcm.length (data length)
    - Total output length === 44 + pcm.length
    - Bytes 44..end equal the input PCM bytes (verify a few sample positions)

    Use `DataView` on the returned `Uint8Array` for precise endianness assertions.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin &amp;&amp; npm test -- --test-name-pattern="wav-encoder" 2>&amp;1 | tail -15 &amp;&amp; grep -c "^test\(" vigil-g2-plugin/src/__tests__/wav-encoder.test.ts | grep -qE '^[8-9]$|^[1-9][0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-g2-plugin/src/__tests__/wav-encoder.test.ts` contains at least 8 distinct `test(...)` blocks (one per D-D1 byte position)
    - Source: test file references the exact ASCII literals `'RIFF'`, `'WAVE'`, `'fmt '`, `'data'`
    - Source: test file references the exact values `16000`, `32000`, `16` (sample rate / byte rate / bit depth)
    - Source: test file uses `DataView` AND `.getUint32(..., true)` (little-endian) for multi-byte assertions
    - Behavior: `cd vigil-g2-plugin && npm test -- --test-name-pattern="wav-encoder"` exits 0
  </acceptance_criteria>
  <done>D-D1 drift detector locked. Any future commit that mutates the WAV header (8 kHz, stereo, 8-bit, wrong RIFF magic) breaks CI before merging.</done>
</task>

<task type="auto">
  <name>Task 2: D-D2 — extend audio-log-redaction.test.ts (server) + denylist-parity.test.ts (PWA) to cover vigil-g2-plugin + voice-transcribe.ts</name>
  <files>
    vigil-core/src/__tests__/audio-log-redaction.test.ts,
    vigil-pwa/src/__tests__/denylist-parity.test.ts
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 841-849 — extend redaction-test ROOT + safe-list),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-D2 — exact grep patterns audioPcm / audio_pcm / pcm AND log sinks console.log / Sentry.captureException / posthog.capture),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-RESEARCH.md (Pitfall 1 lines 13-46 — three-rail drift detector pattern),
    vigil-core/src/__tests__/audio-log-redaction.test.ts (existing Rail 3 source-grep pattern at lines 45-57 and following),
    vigil-pwa/src/__tests__/denylist-parity.test.ts (existing PWA-side parity test)
  </read_first>
  <action>
    Modify `vigil-core/src/__tests__/audio-log-redaction.test.ts`:
    - Extend the source-grep ROOT to include BOTH `vigil-core/src/` AND `vigil-g2-plugin/src/` AND explicitly the file `vigil-core/src/routes/voice-transcribe.ts` (already inside `vigil-core/src/` but call it out in a dedicated assertion to make the contract explicit).
    - For each non-test `.ts` file in the extended ROOT, walk the file, find every line that contains one of `console.log(`, `console.warn(`, `console.error(`, `Sentry.captureException(`, `posthog.capture(`. For each such line, assert the line does NOT contain any of the regex patterns `/\baudioPcm\b/`, `/\baudio_pcm\b/`, `/\bpcm\b/` (word-bounded to avoid matching `pcmChunks` variable names — but careful: `pcm` as a property KEY inside an object literal is the regression class. Use a property-key-position pattern: `/['"]pcm['"]:|\bpcm:/` for object-literal keys, leaving variable names alone. Mirror the existing audio-log-redaction.test.ts grep approach exactly — do not re-invent the regex shape).
    - **Filter out comments**: per the prompt's "Grep gate hygiene" note, use `grep -v '^\s*//'` or equivalent line-comment filter when counting matches.

    Modify `vigil-pwa/src/__tests__/denylist-parity.test.ts`:
    - Same shape but the ROOT is `vigil-pwa/src/`. Phase 127 GUARD-01 already enforces this for `audio` and `audioBuffer` patterns; extend with the three Phase 130 patterns `audioPcm` / `audio_pcm` / `pcm`.

    Run both tests — they MUST be GREEN against the current Phase 130 production source (Plan 02 + Plan 04 + Plan 05 all already use safe key names). If any test fails, the production code is leaking a banned key name and Plan 04 / Plan 05 must be re-opened to fix.
  </action>
  <verify>
    <automated>cd vigil-core &amp;&amp; npm test -- --test-name-pattern="audio-log-redaction" 2>&amp;1 | tail -10 &amp;&amp; cd ../vigil-pwa &amp;&amp; npm test -- --test-name-pattern="denylist-parity" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-core/src/__tests__/audio-log-redaction.test.ts` references walking BOTH `vigil-core/src/` AND `vigil-g2-plugin/src/`
    - Source: audio-log-redaction.test.ts references `vigil-core/src/routes/voice-transcribe.ts` explicitly
    - Source: audio-log-redaction.test.ts uses comment-stripped line scanning (`grep -v '^\s*//'` or equivalent line filter) — NOT a naive `grep -c` that would count comments as matches
    - Source: audio-log-redaction.test.ts asserts patterns `audioPcm` AND `audio_pcm` AND `pcm` (as an object-key) are absent from `console.log` / `Sentry.captureException` / `posthog.capture` call lines in both packages
    - Source: `vigil-pwa/src/__tests__/denylist-parity.test.ts` references the same three patterns
    - Behavior: `cd vigil-core && npm test -- --test-name-pattern="audio-log-redaction"` exits 0
    - Behavior: `cd vigil-pwa && npm test -- --test-name-pattern="denylist-parity"` exits 0
  </acceptance_criteria>
  <done>D-D2 drift detector locked across all three packages. Any future commit adding `audioPcm` / `audio_pcm` / `pcm` to a log sink breaks CI.</done>
</task>

<task type="auto">
  <name>Task 3: D-D3 — author audiocontrol-pairing.test.ts source-grep parity test</name>
  <files>
    vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-PATTERNS.md (lines 821-838 — audiocontrol-pairing.test.ts template + grep-pairing approach),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-D3 — equal count requirement + excluded paths),
    vigil-core/src/__tests__/audio-log-redaction.test.ts (source-grep test framework reference),
    vigil-g2-plugin/src/lib/audio-session-guard.ts (the wrapper being audited),
    vigil-g2-plugin/src/screens/voice.ts (the primary caller — `await safeAudioControl(true, bridge)` start + `await safeAudioControl(false, bridge)` stop)
  </read_first>
  <action>
    Create `vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts`:
    - Walk `vigil-g2-plugin/src/` recursively, collecting all non-test `.ts` files (exclude any path containing `/__tests__/` or ending in `.test.ts`).
    - For each file, read content and strip single-line comments (`^\s*//`) and block comments (`/* ... */`) to prevent comment lines from inflating counts. Per the prompt's "Grep gate hygiene" rule, naive `grep -c` is forbidden because header prose can self-invalidate.
    - Count occurrences of `safeAudioControl(true,` and `safeAudioControl(false,` (literal substring with the comma — this targets the call form specifically; variable assignments like `const cleanup = () => safeAudioControl(false, bridge)` are intentionally included because they still represent a paired call).
    - Sum across all files.
    - Assert `count_true === count_false`. If unequal, the test reports both counts AND which file(s) contain unmatched calls.
    - Exclude file `vigil-g2-plugin/src/lib/audio-session-guard.ts` itself (the wrapper implementation — its 4 cleanup hooks reference `bridge.audioControl(false)` not `safeAudioControl(false,`; nothing to count there).

    The test should be GREEN today: Plan 04's voice.ts has exactly one `safeAudioControl(true,` (START path) and one `safeAudioControl(false,` (STOP path). No other file should call `safeAudioControl` directly — all callers route through voice.ts.

    Plug-in source-grep should yield `count_true === count_false === 1` (or 2 if Plan 04's try/catch flow includes a redundant cleanup `safeAudioControl(false,` on the throw path — which is correct safety; the test still passes because both counts move together).
  </action>
  <verify>
    <automated>test -f vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts &amp;&amp; cd vigil-g2-plugin &amp;&amp; npm test -- --test-name-pattern="audiocontrol-pairing" 2>&amp;1 | tail -15 &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `vigil-g2-plugin/src/__tests__/audiocontrol-pairing.test.ts` walks `vigil-g2-plugin/src/` recursively
    - Source: test file strips comments before counting (substring filter `'//'` or block-comment regex) — comments do NOT inflate counts
    - Source: test file asserts `count_true === count_false`
    - Source: test file excludes `audio-session-guard.ts` (the wrapper implementation does not count toward caller pairing)
    - Source: test file produces a useful failure message including both counts AND the offending file(s) when imbalanced
    - Behavior: `cd vigil-g2-plugin && npm test -- --test-name-pattern="audiocontrol-pairing"` exits 0 (counts equal — Plan 04 voice.ts is well-formed)
    - Behavior: `cd vigil-g2-plugin && npm test` exits 0 (full plugin suite green)
  </acceptance_criteria>
  <done>D-D3 drift detector locked. Any future commit introducing an orphan `safeAudioControl(true,` without a matching `(false,` breaks CI — catching the Phase 127 GUARD-02 "spike's safeAudioControl(true) without matching (false)" regression class at source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — this plan ships tests only. The boundaries being pinned are Phase 127 GUARD-01 (audioPcm → log sinks) and Phase 127 GUARD-02 (audioControl pairing).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-06-1 | Information Disclosure regression | log-sink audioPcm leak | mitigate | D-D2 source-grep extended to cover vigil-g2-plugin + voice-transcribe.ts; CI fails on any new commit that adds a banned key |
| T-130-06-2 | Repudiation / mic-runaway regression | orphan audioControl(true) | mitigate | D-D3 source-grep parity test; CI fails on imbalanced count |
| T-130-06-3 | Integrity / wire-format regression | WAV header drift | mitigate | D-D1 byte-for-byte pin; CI fails on any header mutation |
| T-130-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- All 3 drift detector tests GREEN against current Phase 130 production source
- Full vigil-core + vigil-g2-plugin + vigil-pwa suites green
- D-D2 test comment-stripping verified (per prompt "Grep gate hygiene" rule)
</verification>

<success_criteria>
- VOICE-08 satisfied: WAV header byte-for-byte pin + audioPcm-in-logs ban extended to plugin + audioControl pairing parity
- All three drift detectors will fail CI on future regression — operator does not need to remember to check
- No source file in any of the three packages contains a banned key in a log sink or an unbalanced safeAudioControl call
</success_criteria>

<output>
Create `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-06-SUMMARY.md` when done.
</output>
