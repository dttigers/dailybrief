---
phase: 130
plan: 07
type: execute
wave: 5
depends_on:
  - 130-01
  - 130-02
  - 130-03
  - 130-04
  - 130-05
  - 130-06
files_modified:
  - .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-HARDWARE-UAT.md
autonomous: false
requirements:
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05
  - VOICE-06
  - VOICE-07
  - VOICE-08
requirements_addressed:
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05
  - VOICE-06
  - VOICE-07
  - VOICE-08
must_haves:
  truths:
    - "Operator confirms `cd vigil-core && npm run db:migrate-prod` succeeded against Railway production DB (operator wallclock — per [feedback_wallclock_checkpoint_exempt] auto-memory)"
    - "Operator confirms G2 plugin packed + installed on G2-tethered iPhone via Even Hub portal"
    - "Operator wallclock-measures: DOUBLE_CLICK stop → PWA dashboard row visible ≤ 8 s on dev laptop browser (VOICE-06 acceptance criterion)"
    - "Operator confirms recording m:ss timer survives carousel swipe (VOICE-03)"
    - "Operator confirms [NO MIC] body line appears after revoking g2-microphone in Even Hub (VOICE-02 Run 4 §5)"
    - "Operator confirms airplane-mode toggle: 2 recordings produce queue depth = 2 on HUD; disabling airplane mode drains queue to 0 (VOICE-07)"
    - "Operator captures console-log timing screenshot + PWA dashboard render screenshot (Loom waived per [feedback_loom_waived_g2_not_screen_mirrorable] auto-memory)"
  artifacts:
    - path: ".planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-HARDWARE-UAT.md"
      provides: "Operator wallclock log with timestamps, screenshots, pass/fail per UAT line"
      contains: "round-trip ≤ 8s"
  key_links:
    - from: "operator G2-tethered iPhone"
      to: "Railway production /v1/voice/transcribe"
      via: "production migration 0023 applied; G2 plugin packed with latest plugin source"
      pattern: "production"
---

<objective>
Operator hardware UAT closes the phase. Phase 130's automated tests prove the code paths; this plan proves the integrated behavior on real hardware (G2-tethered iPhone + dev laptop browser) against the Railway production deployment.

Purpose: Closes the VOICE-06 8-second user-perceived round-trip acceptance criterion — the criterion is cross-device (G2 records; dashboard browser on a different machine), so it cannot be measured in CI. Also closes the manual-only verifications from VALIDATION.md (recording-indicator-survives-swipe, [NO MIC] surface, airplane-mode queue drain).
Output: `130-HARDWARE-UAT.md` with operator timestamps + screenshots + pass/fail per UAT line; Phase 130 ready to mark Complete in ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-01-SUMMARY.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-02-SUMMARY.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-03-SUMMARY.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-04-SUMMARY.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-05-SUMMARY.md
@.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs production migration 0023 against Railway DB (wallclock)</name>
  <files>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-HARDWARE-UAT.md
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-02-SUMMARY.md (verify migration 0023 SQL is committed),
    vigil-core/package.json (db:migrate-prod script),
    vigil-core/drizzle/0023_voice_capture_dedup.sql
  </read_first>
  <what-built>
    Plan 02 authored migration 0023 + applied it locally. Production deploy requires `npm run db:migrate-prod` against Railway. Per `[feedback_wallclock_checkpoint_exempt]` auto-memory, this step is an explicit operator wallclock — it cannot be `--auto`-executed because production credentials live in operator-only secrets.
  </what-built>
  <action>
    Operator-only manual step (no autonomous automation available — production credentials are operator-scoped). Operator executes the production migration against Railway, verifies the table + index landed, then records the wallclock timestamp + verification output in `130-HARDWARE-UAT.md`. Detailed steps in `<how-to-verify>` below.
  </action>
  <how-to-verify>
    1. From operator dev laptop: `cd vigil-core && DATABASE_URL=$RAILWAY_PROD_DATABASE_URL npm run db:migrate-prod` (or via Railway CLI / dashboard, whichever path the operator uses for prod migrations).
    2. Verify the migration ran: `psql $RAILWAY_PROD_DATABASE_URL -c "\d voice_captures"` lists 6 columns (id, user_id, thought_id, client_capture_id, queued_at, retry_count).
    3. Verify the index exists: `psql $RAILWAY_PROD_DATABASE_URL -c "SELECT indexname FROM pg_indexes WHERE tablename='voice_captures';"` includes `uq_voice_captures_user_client_capture_id`.
    4. Record the wallclock timestamp of completion in `130-HARDWARE-UAT.md`.

    Per `[Railway variables leak]` auto-memory — do NOT dump the full `railway variables` output anywhere; use `railway variables get DATABASE_URL` for the single value if needed.
  </how-to-verify>
  <verify>
    <human-check>Operator pastes `\d voice_captures` output (6 columns) + `pg_indexes` row listing `uq_voice_captures_user_client_capture_id` into `130-HARDWARE-UAT.md` with wallclock timestamp.</human-check>
  </verify>
  <resume-signal>Type "migrated" with the wallclock timestamp, OR describe failure mode (rollback needed?)</resume-signal>
  <acceptance_criteria>
    - CLI: production `\d voice_captures` lists all 6 columns (id, user_id, thought_id, client_capture_id, queued_at, retry_count)
    - CLI: production `\d voice_captures` (or pg_indexes query) lists `uq_voice_captures_user_client_capture_id` index
    - Source: `130-HARDWARE-UAT.md` contains the timestamp of completion + pasted psql output
    - Behavior: operator confirms no other prod migrations are pending (`drizzle-kit migrate` reports no work to do on second run)
  </acceptance_criteria>
  <done>Production migration 0023 applied; voice_captures table + partial unique index live on Railway production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator packs G2 plugin + installs via Even Hub + verifies VOICE-02/03/04/05/06/07/08 end-to-end</name>
  <files>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-HARDWARE-UAT.md
  </files>
  <read_first>
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-VALIDATION.md (Manual-Only Verifications table lines 120-128),
    .planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-CONTEXT.md (D-G1 + D-S1 + D-X1 + specifics §"8-s acceptance test methodology"),
    .planning/phases/128a-voice-01-pcm-feasibility-spike/128a-SPIKE-DECISION.md (Threshold Trace + Run 4 hardening)
  </read_first>
  <what-built>
    Plans 01-06 shipped: spike cleanup, production route + migration 0023, SSE fan-out, G2 voice screen with safeAudioControl `Promise<boolean>` hardening, offline queue, drift detectors. All automated tests green. This checkpoint confirms the integrated behavior on real hardware against Railway production.
  </what-built>
  <action>
    Operator manual UAT — pack G2 plugin, sideload via Even Hub portal, perform the seven checks documented in `<how-to-verify>` against the G2-tethered iPhone and dev laptop PWA browser, capture screenshots + stopwatch numbers, record results in `130-HARDWARE-UAT.md`.
  </action>
  <how-to-verify>
    **Pre-flight (operator workstation):**
    1. Pack the G2 plugin: `cd vigil-g2-plugin && npm run pack` (or whatever the project's plugin-pack command is — see vigil-g2-plugin/package.json + STATE.md memory for the exact command).
    2. Sideload via Even Hub portal → operator's G2-tethered iPhone.
    3. Open vigil-pwa dashboard in browser on operator's dev laptop (NOT the iPhone — VOICE-06 acceptance is cross-device).

    **VOICE-02/03/04/05/06 round-trip (≤ 8 s):**
    1. On G2: swipe to the production VOICE screen (carousel position: after Companion, before Tasks per CONTEXT specifics).
    2. DOUBLE_CLICK → confirm stateLine flips to `[REC 0:00]` with m:ss timer ticking.
    3. Speak a short test utterance (≤ 3 s).
    4. Swipe LEFT to Companion mid-recording → confirm m:ss timer is STILL ticking on Companion HUD via cross-screen state (VOICE-03). Swipe BACK to VOICE.
    5. Start stopwatch.
    6. DOUBLE_CLICK to stop → confirm flow `[UPLOADING…]` → `[DONE]` → `[IDLE]`.
    7. STOP stopwatch when the transcribed thought row VISIBLY appears on the PWA dashboard browser. **Pass = ≤ 8 s** (VOICE-06).
    8. Capture screenshot of the dashboard row + browser dev-tools console-log timing.

    **VOICE-02 [NO MIC] surface (Run 4 §5):**
    1. In the Even Hub portal app, revoke `g2-microphone` permission for the plugin.
    2. On G2: DOUBLE_CLICK on VOICE screen.
    3. Confirm body line shows `[NO MIC]` AND body line 2 reads `enable mic in Hub` (NOT `[ERR]` or `retry — tap to dismiss`).
    4. Re-grant `g2-microphone` permission in Even Hub.
    5. Confirm next DOUBLE_CLICK works normally (returns to `[REC 0:00]`).

    **VOICE-07 offline queue drain:**
    1. On iPhone: enable airplane mode (cellular + Wi-Fi off).
    2. On G2: record 2 short utterances back-to-back.
    3. Confirm body line 3 on Companion HUD reads `syncing 2 voice captures…`.
    4. Disable airplane mode on iPhone.
    5. Wait for backoff schedule (1 s → 2 s → 4 s → 8 s → 16 s → 30 s as needed).
    6. Confirm body line 3 drains: `syncing 2 voice captures…` → `syncing 1 voice captures…` → (hidden).
    7. Confirm both transcripts appear on PWA dashboard after drain.
    8. Capture screenshot of HUD with queue indicator at depth 2 + after drain.

    **D-D3 sanity check (negative path):**
    1. Force-quit G2 plugin mid-recording (swipe out of app or close iPhone Even Hub).
    2. Re-open plugin.
    3. Confirm `safeAudioControl(false, bridge)` cleanup hooks fired (per Phase 127 GUARD-02 cleanup pattern — no orphan `audioControl(true)` left over).

    **VOICE-08 telemetry sanity:**
    1. In PostHog dashboard, filter `voice_capture_completed` events from the last hour.
    2. Confirm events have keys `{ stop_to_http_ms, chunks, bytes, retry_count, transcript_chars }` and NO `audio` / `audioPcm` / `pcm` keys.
    3. If a drop-out fired, confirm `voice_capture_dropout` has only `{ gap_ms, recording_id }`.

    **Loom**: WAIVED per `[feedback_loom_waived_g2_not_screen_mirrorable]` auto-memory — G2 lenses are not screen-mirrorable. Portfolio artifacts are console-log timing screenshot + PWA dashboard render screenshot (already captured above).

    Record all timestamps, screenshots, and pass/fail per UAT line in `130-HARDWARE-UAT.md`.
  </how-to-verify>
  <verify>
    <human-check>Operator records seven UAT lines in `130-HARDWARE-UAT.md` with attached screenshots + stopwatch number for VOICE-06; PostHog event-key inspection paste; pass/fail per line.</human-check>
  </verify>
  <resume-signal>Type "uat-pass" with attached screenshots + stopwatch number, OR describe the specific UAT line that failed with logs.</resume-signal>
  <acceptance_criteria>
    - Source: `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-HARDWARE-UAT.md` exists with operator-recorded entries
    - Source: UAT log records stopwatch number ≤ 8 s for VOICE-06 round-trip
    - Source: UAT log records `[NO MIC]` body-line-2 string `enable mic in Hub` verbatim (NOT `[ERR]` or generic message)
    - Source: UAT log records queue indicator `syncing 2 voice captures` substring AND post-drain `syncing 0` (or hidden)
    - Source: UAT log includes references to 2 portfolio screenshots (PWA dashboard render + console-log timing) — Loom waived
    - Source: UAT log records PostHog event-key inspection — no `audio*` / `pcm*` keys leaked
    - Behavior: All 7 VOICE-02..08 requirements observably PASS per operator
  </acceptance_criteria>
  <done>Hardware UAT complete; portfolio screenshots captured; `130-HARDWARE-UAT.md` committed. Phase 130 ready to mark Complete in ROADMAP.md traceability table.</done>
</task>

</tasks>

<threat_model>
## No new threats — operator wallclock verification only

This plan does not change production code. It runs the prod migration (`db:migrate-prod`) which is gated by operator wallclock per `[feedback_wallclock_checkpoint_exempt]` auto-memory. Production credentials remain operator-only per `[Anthropic key sprawl]` + `[Railway variables leak]` auto-memories.

The full Phase 130 threat surface (T-130-02 route auth + audio cap + AI budget + dedup, T-130-04 audioControl pairing, T-130-06 drift regression) was mitigated by Plans 02 / 04 / 06 respectively. This plan confirms the mitigations hold on real hardware against Railway production.
</threat_model>

<verification>
- Production migration 0023 applied (operator-verified via psql)
- G2 plugin packed + sideloaded
- VOICE-06 stopwatch round-trip ≤ 8 s wallclock
- [NO MIC] body-line-2 exact string verified
- Offline queue depth indicator + drain verified
- PostHog events use safe keys only
- Portfolio screenshots captured (Loom waived)
</verification>

<success_criteria>
- All 7 VOICE-02..08 requirements observably PASS on real hardware against Railway production
- `130-HARDWARE-UAT.md` committed with operator signature
- Phase 130 ready to mark Complete in `ROADMAP.md` traceability table (lines 174-180 → Complete)
- Phase 133 can begin (Phase 130 dependency resolved)
</success_criteria>

<output>
Create `.planning/phases/130-voice-capture-full-implementation-scope-locked-by-128a/130-07-SUMMARY.md` when done (includes link to 130-HARDWARE-UAT.md + operator-signed stopwatch number).
</output>
