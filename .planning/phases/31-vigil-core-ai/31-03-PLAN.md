---
phase: 31-vigil-core-ai
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified: [vigil-core/src/routes/therapy.ts, vigil-core/src/index.ts]
autonomous: true
---

<objective>
Create all three therapy AI endpoints — classification, pattern detection, and session prep.

Purpose: Port the full therapy intelligence pipeline from Swift to Node.js
Output: Working POST /v1/therapy/classify, /v1/therapy/patterns, and /v1/therapy/prep endpoints
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@vigil-core/src/ai/client.ts
@vigil-core/src/ai/types.ts
@vigil-core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POST /v1/therapy/classify endpoint</name>
  <files>vigil-core/src/routes/therapy.ts</files>
  <action>
    1. Create `src/routes/therapy.ts` as a Hono app with all three therapy routes:
    2. POST /therapy/classify accepting JSON body `{ content: string }`:
       - Validate content non-empty, return 400 if missing
       - If AI client unavailable, return 503
       - Call `callClaude()` with:
         - system prompt (port exactly from Swift TherapyClassificationService):
           ```
           You are a therapy thought classifier for a personal journaling tool. Your role is to help the user understand which thoughts they can explore independently vs which would benefit from discussing with their therapist.

           IMPORTANT: You are a categorization tool, NOT a therapist. Your classifications are suggestions to help organize therapy prep, not clinical advice.

           Classify the thought into one of two categories:

           - selfLearnable: The user can likely work through this independently. Includes: self-reflection exercises, mindfulness practices, journaling prompts, cognitive reframing the user could do alone, gratitude practices, goal-setting, habit tracking observations, general emotional check-ins.

           - bringToTherapist: This would benefit from professional guidance. Includes: recurring distressing patterns, trauma-related content, relationship conflicts needing mediation perspective, feelings of hopelessness or being stuck, topics the user keeps circling back to without resolution, strong emotional reactions they don't understand, anything involving safety concerns.

           When in doubt, classify as bringToTherapist.

           Respond with ONLY a JSON object:
           {"classification": "<selfLearnable|bringToTherapist>", "confidence": <0.0-1.0>, "reasoning": "<1 sentence explaining why>"}
           ```
         - userMessage: the raw thought content
         - maxTokens: 150
       - Parse JSON into TherapyClassificationResult
       - Return 200 with result, or 502 on parse failure
  </action>
  <verify>cd vigil-core && npx tsc --noEmit</verify>
  <done>POST /v1/therapy/classify returns {classification, confidence, reasoning}</done>
</task>

<task type="auto">
  <name>Task 2: Create POST /v1/therapy/patterns endpoint</name>
  <files>vigil-core/src/routes/therapy.ts</files>
  <action>
    Add to the existing therapy.ts Hono app:
    1. POST /therapy/patterns accepting JSON body:
       ```
       { thoughts: Array<{ id: number, content: string, therapyClassification: string, createdAt: string }>, days?: number }
       ```
       - Validate: minimum 5 thoughts required (matching Swift), return 400 if fewer
       - If AI client unavailable, return 503
       - Call `callClaude()` with:
         - system prompt (port from Swift TherapyPatternService):
           ```
           You are a pattern detection tool, NOT a therapist. Your role is to surface observations to help the user prepare for therapy sessions.

           Analyze the user's therapy-related thoughts and identify recurring emotional themes, behavioral patterns, and unresolved concerns. For each pattern:
           - Name the theme concisely
           - Describe what you observe in 1-2 sentences
           - Count how many thoughts relate to this theme
           - Note whether the theme appears to be increasing, stable, or decreasing in frequency based on timestamps
           - List the thought IDs that exhibit this pattern
           - Rate your confidence (0.0-1.0) in the pattern being genuine, not surface-level

           Focus on genuine patterns. Look for: recurring emotional states or triggers, behavioral cycles (avoidance, rumination), unresolved concerns that keep appearing, relationship dynamics that repeat, progress or regression in specific areas.
           ```
         - userMessage:
           ```
           Here are my therapy-related thoughts from the last {days || 30} days:
           [{id}] ({therapyClassification}, {createdAt}) {content}
           ...

           Analyze these thoughts and identify recurring patterns. Return a JSON array:
           [{"theme": "...", "description": "...", "frequency": N, "trend": "increasing|stable|decreasing", "related_thought_ids": [], "confidence": 0.0-1.0}]

           Return ONLY the JSON array, no other text.
           ```
         - maxTokens: 1024
       - Parse JSON, map `related_thought_ids` → `relatedThoughtIds`
       - Filter to confidence >= 0.5
       - Return 200 with `{ patterns: TherapyPattern[] }`
  </action>
  <verify>cd vigil-core && npx tsc --noEmit</verify>
  <done>POST /v1/therapy/patterns accepts batch of therapy thoughts, returns filtered patterns with confidence >= 0.5</done>
</task>

<task type="auto">
  <name>Task 3: Create POST /v1/therapy/prep endpoint and mount therapy routes</name>
  <files>vigil-core/src/routes/therapy.ts, vigil-core/src/index.ts</files>
  <action>
    1. Add to therapy.ts:
       POST /therapy/prep accepting JSON body:
       ```
       {
         thoughts: Array<{ id: number, content: string, createdAt: string }>,
         patterns?: Array<{ theme: string, trend: string, confidence: number, description: string }>
       }
       ```
       - Validate: at least 1 thought required, return 400 if empty
       - If AI client unavailable, return 503
       - Call `callClaude()` with:
         - system prompt (port from Swift TherapyPrepService):
           ```
           You are organizing the user's own thoughts for their therapy prep, NOT providing therapy or clinical advice.

           Generate a structured therapy session preparation from the user's recent thoughts that they marked for therapist discussion. Your job is to:
           - Organize thoughts into clear discussion topics
           - Provide brief context for each topic so the user can reference it quickly
           - Assign urgency levels (high/medium/low) based on emotional intensity and how pressing the topic seems
           - If recurring patterns are provided, use them to add context about themes that keep appearing
           - Identify overall themes across all topics
           - Suggest a session focus based on what seems most pressing or important

           Keep topics concise and actionable. The user should be able to glance at this prep before their session and know exactly what to discuss.
           ```
         - userMessage:
           ```
           Here are my recent thoughts marked for discussion with my therapist:
           [{id}] ({createdAt}) {content}
           ...

           [If patterns provided:]
           Detected recurring patterns for additional context:
           - {theme} ({trend}, confidence: {confidence}): {description}

           Generate a structured therapy session prep. Return a JSON object:
           {"items": [{"topic": "...", "context": "...", "urgency": "high|medium|low", "related_thought_ids": []}], "overall_themes": ["..."], "suggested_focus": "..."}

           Return ONLY the JSON object, no other text.
           ```
         - maxTokens: 1024
       - Parse JSON, map snake_case keys to camelCase in items (related_thought_ids → relatedThoughtIds) and top-level (overall_themes → overallThemes, suggested_focus → suggestedFocus)
       - Return 200 with TherapyPrep
    2. Mount in `src/index.ts`: import therapy, add `app.route("/v1", therapy)`
  </action>
  <verify>cd vigil-core && npx tsc --noEmit</verify>
  <done>POST /v1/therapy/prep generates structured session prep, all 3 therapy routes mounted</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd vigil-core && npx tsc --noEmit` passes
- [ ] POST /v1/therapy/classify returns 400 for empty content
- [ ] POST /v1/therapy/patterns returns 400 with fewer than 5 thoughts
- [ ] POST /v1/therapy/prep returns 400 with empty thoughts array
- [ ] All three therapy routes mounted via single therapy.ts import
</verification>

<success_criteria>

- All tasks completed
- TypeScript compiles without errors
- All three therapy prompts match Swift originals exactly
- Pattern filtering at confidence >= 0.5 matches Swift behavior
- Therapy classification defaults to bringToTherapist when in doubt (prompt-level)
</success_criteria>

<output>
After completion, create `.planning/phases/31-vigil-core-ai/31-03-SUMMARY.md`
</output>
