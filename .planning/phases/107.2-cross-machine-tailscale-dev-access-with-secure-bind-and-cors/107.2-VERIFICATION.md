---
phase: 107.2-cross-machine-tailscale-dev-access-with-secure-bind-and-cors
verified: 2026-04-22T14:30:00Z
status: human_needed
score: 15/15 must-haves verified
overrides_applied: 0
gaps:
  - truth: "REQ-DEV-CROSS-MACHINE is registered in .planning/REQUIREMENTS.md"
    status: failed
    reason: "Requirement ID referenced in all 3 PLAN frontmatters (and claimed as requirements-completed in all 3 SUMMARYs) is NOT present anywhere in REQUIREMENTS.md — not as a bulleted requirement, not in the Traceability table. Plan 03 SUMMARY flagged this live (`requirements mark-complete returned not_found`). ROADMAP.md also shows Phase 107.2 `Requirements: TBD`. Three plans cannot claim to complete a requirement that has never been defined."
    artifacts:
      - path: ".planning/REQUIREMENTS.md"
        issue: "Missing REQ-DEV-CROSS-MACHINE definition AND missing Phase 107.2 row in the Traceability table"
      - path: ".planning/ROADMAP.md"
        issue: "Phase 107.2 section still says 'Requirements: TBD' and 'Goal: [Urgent work - to be planned]'"
    missing:
      - "Add REQ-DEV-CROSS-MACHINE under 'Developer Environment' section of REQUIREMENTS.md (sibling to REQ-DEV-LOCAL-ENV), something like: 'Local development is reachable from a second machine on the same Tailscale tailnet without weakening production — a laptop browser pointed at the iMac's `npm run dev` works end-to-end; prod cannot boot without CORS_ORIGINS'"
      - "Add a Traceability table row: `| REQ-DEV-CROSS-MACHINE | Phase 107.2 | Complete |`"
      - "Update ROADMAP.md Phase 107.2 section: fill in the Goal (from 107.2-CONTEXT.md <domain> block) and set Requirements: REQ-DEV-CROSS-MACHINE"
      - "Bump the 'Coverage' footer in REQUIREMENTS.md from 14 total to 15 total (and update 'Mapped to phases' accordingly)"
human_verification:
  - test: "Cross-machine boot from MacBook Pro browser to iMac Vite over Tailscale"
    expected: "On iMac, run `npm run dev` from repo root. On MacBook Pro, open `http://jamesons-imac-2:5173` in a browser. The Vigil PWA loads and renders. DevTools → Network → any `/v1/*` request has URL `http://jamesons-imac-2:5173/v1/...` (same-origin on :5173, NOT `http://localhost:3001/...`). Vigil-core log on iMac shows the proxied request. Login works (auth bearer flow end-to-end)."
    why_human: "Requires two physical machines both joined to the Tailscale tailnet, iMac's `npm run dev` actually running, and a browser on the MacBook Pro to validate same-origin routing. No automated harness available."
  - test: "macOS firewall ON + VIGIL_BIND_HOST=0.0.0.0 WARN branch of preflight Check 5"
    expected: "`sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on`, then `bash scripts/preflight-check.sh`. Check 5 prints the yellow 'WARN — 0.0.0.0 bind with macOS Application Firewall ENABLED.' block with the node.js allow-list and sudo-disable fix suggestions. Preflight overall exits 0 (WARN is non-fatal). Restore firewall with `sudo socketfilterfw --setglobalstate off`."
    why_human: "Requires sudo mutation of host firewall state. Cannot safely automate (could strand SSH sessions, alter trust posture of the machine). Plan 03 SUMMARY explicitly deferred this test; code-inspection verified `FAIL=1` count inside `check_bind_and_firewall` is exactly 1 (confined to the unsupported-value branch), which proves the WARN path cannot set FAIL=1 — but the exact yellow output text and the 'WARN, not a FAIL — preflight continues' wording are only observed by running it."
  - test: "Live Railway prod deploy refuses to boot without CORS_ORIGINS (D-D2 guard firing in production)"
    expected: "If CORS_ORIGINS is unset on Railway, the deploy exits 1 with `FATAL: CORS_ORIGINS must be set in production — refusing to boot with wildcard CORS` in the Railway deploy log. Production stays on the last healthy deploy."
    why_human: "Cannot test this programmatically without actually pushing a broken deploy to Railway, which would crash prod. Plan 01 performed a LOCAL `NODE_ENV=production node dist/index.js` probe with CORS_ORIGINS= unset and captured exit 1 + exact FATAL string in `/tmp/107.2-01-prodfatal.log` — that IS the safe proxy and it passed. The full production proof only exists the day CORS_ORIGINS ever accidentally becomes unset on Railway; hopefully that day never comes, but the guard is live."
---

# Phase 107.2: Cross-machine Tailscale dev access — Verification Report

**Phase Goal:** Make the iMac's local dev stack (vigil-core on :3001 + Vite on :5173) reachable from the MacBook Pro over Tailscale, without weakening production security. Thin-client model — laptop browser points at the iMac's running `npm run dev`. No second DB, no mirrored state, no second vigil-core process.

**Verified:** 2026-04-22T14:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All 15 plan-declared truths verified against live codebase. REQ traceability is a separate gap (below).

#### Plan 01 — bind + CORS hardening

| #   | Truth                                                                                                                    | Status     | Evidence |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- |
| 1.1 | vigil-core binds 127.0.0.1 by default (prod-safe); binds VIGIL_BIND_HOST value when env var is set                       | VERIFIED   | `vigil-core/src/index.ts:186` `const hostname = process.env.VIGIL_BIND_HOST ?? "127.0.0.1";` + line 188 `serve({ fetch: app.fetch, port, hostname }, ...)` |
| 1.2 | vigil-core refuses to boot in production when CORS_ORIGINS is unset — exits 1 with FATAL log                             | VERIFIED   | `vigil-core/src/index.ts:69-72` — guard inserted between JWT_SECRET check and `export const app = new Hono();` (line 74). Plan 01 SUMMARY captured live exit 1 + exact FATAL string in `/tmp/107.2-01-prodfatal.log` |
| 1.3 | Local dev vigil-core/.env sets VIGIL_BIND_HOST=0.0.0.0 so Tailscale peers can reach :3001                                | VERIFIED   | Live read: `grep -E '^VIGIL_BIND_HOST=' vigil-core/.env` → `VIGIL_BIND_HOST=0.0.0.0`. File is gitignored (`git check-ignore` exits 0) |
| 1.4 | vigil-core/.env.example documents VIGIL_BIND_HOST with a LOCAL-ONLY warning                                              | VERIFIED   | `vigil-core/.env.example:56-65` — new "Bind hostname (LOCAL ONLY — Railway MUST leave this unset)" section; mentions Phase 107.2 D-B1/D-B2 |

#### Plan 02 — Vite proxy + thin client

| #   | Truth                                                                                                                           | Status     | Evidence |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 2.1 | Vite dev server binds 0.0.0.0 via server.host: true — reachable from Tailscale/LAN peers on :5173                               | VERIFIED   | `vigil-pwa/vite.config.ts:21` `host: true,` inside `server:` block. Plan 02 SUMMARY captured Network URL `http://100.120.110.110:5173/` (Tailscale iMac IP) in live dev boot |
| 2.2 | Vite dev proxy routes /v1/* to VITE_DEV_API_TARGET env var (default http://localhost:3001), using loadEnv() NOT process.env     | VERIFIED   | `vigil-pwa/vite.config.ts:1` imports `loadEnv`; line 12 `const env = loadEnv(mode, process.cwd(), '')`; line 15 `const apiTarget = env.VITE_DEV_API_TARGET ?? 'http://localhost:3001'`; line 28 `target: apiTarget`. Zero `process.env.VITE_DEV_API_TARGET` refs |
| 2.3 | vigil-pwa/.env.local has VITE_API_BASE unset or empty so browser falls through to the proxy — Pitfall 4 closed                 | VERIFIED   | Live read: `grep -E 'VITE_API_BASE' vigil-pwa/.env.local` → 3 comment lines, NO live value. `grep -E '^VITE_DEV_API_TARGET=' vigil-pwa/.env.local` → `VITE_DEV_API_TARGET=http://localhost:3001`. File gitignored |
| 2.4 | vigil-pwa/.env.local.example documents both VITE_API_BASE (LEAVE UNSET) and VITE_DEV_API_TARGET (proxy target)                 | VERIFIED   | `vigil-pwa/.env.local.example` — line 8 "LEAVE UNSET IN DEV (Phase 107.2 D-C1 + Pitfall 4)"; line 24 `VITE_DEV_API_TARGET=http://localhost:3001` |
| 2.5 | VitePWA devOptions.enabled is NOT set to true — no dev service worker registered                                                | VERIFIED   | `grep -cE 'devOptions:\s*\{[^}]*enabled:\s*true' vigil-pwa/vite.config.ts` → 0. Line 40 comment confirms "devOptions intentionally omitted" |

#### Plan 03 — preflight Check 5

| #   | Truth                                                                                                                                    | Status     | Evidence |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 3.1 | scripts/preflight-check.sh has a 5th check named 'Check 5: VIGIL_BIND_HOST sanity' running after Check 4 and before final FAIL aggregation | VERIFIED | `Scripts/preflight-check.sh:94` info line; function defined lines 93-140; called at line 146 between `check_env_localhost` (line 145) and final FAIL block (line 148); 5 sequential calls verified via `awk` |
| 3.2 | Check 5 reads vigil-core/.env, extracts VIGIL_BIND_HOST, branches on value (unset/localhost → PASS; 0.0.0.0 → firewall probe; anything else → FAIL) | VERIFIED | `Scripts/preflight-check.sh:102-116` — grep parses; 105 handles unset/127.0.0.1/localhost (PASS); 110 handles non-0.0.0.0 (FAIL=1); 118-140 handles 0.0.0.0 branch with firewall probe |
| 3.3 | Firewall probe uses `socketfilterfw --getglobalstate` with grep-pattern check (NOT exit-code check, NOT `defaults read`)                  | VERIFIED   | Line 130 `"$FW_BIN" --getglobalstate 2>/dev/null \| grep -q 'disabled'`. Zero occurrences of `defaults read /Library/Preferences/com.apple.alf`. Line 120 reference-form comment |
| 3.4 | 0.0.0.0 + firewall ON → non-fatal WARN (does NOT set FAIL=1) with actionable fix suggestions                                              | VERIFIED   | Lines 132-139 — yellow WARN block, no FAIL=1 assignment. Count inside function: `FAIL=1` appears exactly once (line 114, unsupported-value branch only). Includes fix suggestions: node.js allow-list + sudo disable command |
| 3.5 | 0.0.0.0 + firewall OFF → PASS with cross-machine-reachability note                                                                        | VERIFIED   | Line 131 `green "  PASS — 0.0.0.0 bind (macOS Application Firewall off — Tailscale peers can reach :3001 and :5173)"`. Live run on this iMac reproduces this exact line |
| 3.6 | Unsupported values → FAIL=1, blocks npm run dev                                                                                           | VERIFIED   | Line 110-115 — non-0.0.0.0/unset/localhost/127.0.0.1 value triggers red FAIL with fix suggestion + `FAIL=1`. Plan 03 SUMMARY captured live negative test (VIGIL_BIND_HOST=foo → exit 1) in `/tmp/107.2-03-badval.log` |

**Score:** 15/15 plan truths verified.

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `vigil-core/src/index.ts` | Env-gated bind hostname + prod CORS FATAL guard | VERIFIED | 243 lines (was 227 pre-phase). Both edits live at lines 69-72 (guard) and 181-189 (bind). `npx tsc --noEmit` passed per Plan 01 SUMMARY |
| `vigil-core/.env.example` | VIGIL_BIND_HOST documentation with LOCAL-ONLY comment | VERIFIED | 66 lines (was 55). Section at lines 56-65 present with D-B1/D-B2 traceability |
| `vigil-core/.env` | Local dev value VIGIL_BIND_HOST=0.0.0.0 (gitignored) | VERIFIED | Live-observed; `grep '^VIGIL_BIND_HOST=' vigil-core/.env` → `VIGIL_BIND_HOST=0.0.0.0`. `git check-ignore` confirms gitignored |
| `vigil-pwa/vite.config.ts` | Env-aware proxy target + server.host true; loadEnv pattern | VERIFIED | 97 lines (was 73). Function form with loadEnv; `host: true`; `target: apiTarget`; VitePWA manifest preserved byte-for-byte per Plan 02 SUMMARY audit |
| `vigil-pwa/.env.local.example` | Template documenting VITE_API_BASE (UNSET in dev) and VITE_DEV_API_TARGET | VERIFIED | 25 lines, created new. Both sections present with D-C1/D-C3 traceability |
| `vigil-pwa/.env.local` | Local state: VITE_API_BASE unset/empty + VITE_DEV_API_TARGET set (gitignored) | VERIFIED | Live-observed. 3 `VITE_API_BASE` lines all comments; 1 live `VITE_DEV_API_TARGET=http://localhost:3001`. `git check-ignore` confirms gitignored |
| `Scripts/preflight-check.sh` | Check 5 firewall + bind state detection (D-B3) | VERIFIED | 155 lines (was 105). `yellow()` helper + `check_bind_and_firewall()` + pipeline call. `bash -n` syntax check passes. Live run exits 0 with Check 5 PASS |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `vigil-core/src/index.ts` serve() call | `process.env.VIGIL_BIND_HOST` | `hostname` param on @hono/node-server serve() | WIRED | Line 186 reads env var; line 188 passes `hostname` in the options object. Live boot log captured `Vigil Core API running on 0.0.0.0:3001` (Plan 01 SUMMARY `/tmp/107.2-01-bindboot.log`) |
| `vigil-core/src/index.ts` startup guard | `process.env.CORS_ORIGINS` | `NODE_ENV === production && !CORS_ORIGINS → process.exit(1)` | WIRED | Line 69 exact condition; line 71 `process.exit(1)`. Live probe: exit 1 + exact FATAL string (Plan 01 SUMMARY `/tmp/107.2-01-prodfatal.log`). Counter-probe with CORS_ORIGINS set: normal boot (`/tmp/107.2-01-prodcors.log`) |
| `vigil-pwa/vite.config.ts` `server.proxy['/v1'].target` | `env.VITE_DEV_API_TARGET` | `loadEnv(mode, process.cwd(), '')` | WIRED | Lines 12 + 15 + 28. Live E2E: `curl http://127.0.0.1:5173/v1/health` returned `{"status":"ok",...}` during Plan 02 SUMMARY verification — proxy actually forwarded to vigil-core |
| browser `fetch('/v1/...')` | Vite proxy → VITE_DEV_API_TARGET | `API_BASE = '' fallback when VITE_API_BASE unset` at `vigil-pwa/src/api/client.ts:3` | WIRED (code path verified; live cross-machine fetch is a human_verification item) | client.ts:3 reads: `export const API_BASE = (import.meta.env.VITE_API_BASE as string \| undefined) ?? (import.meta.env.DEV ? '' : 'https://api.vigilhub.io')`. VITE_API_BASE confirmed unset in .env.local → dev → API_BASE='' → same-origin fetch. Plan 02 SUMMARY confirmed round-trip locally; cross-machine path is UAT-gated |
| `preflight-check.sh` Check 5 | `vigil-core/.env` VIGIL_BIND_HOST | `grep -E '^VIGIL_BIND_HOST=' $ENV_FILE` | WIRED | Line 102 grep + cut; whitespace strip on 103; branching 105-140. Three live scenarios verified: 0.0.0.0 + firewall off → PASS, VIGIL_BIND_HOST=foo → FAIL + exit 1, commented out → localhost PASS (Plan 03 SUMMARY logs) |
| `preflight-check.sh` Check 5 firewall branch | macOS Application Firewall state | `/usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate \| grep -q 'disabled'` | WIRED | Line 123 binary path; line 124 existence check; line 130 grep-pattern probe. No `defaults read` fallback. Firewall-ON branch exists (132-139) but WARN path not exercised live — see human_verification #2 |

### Data-Flow Trace (Level 4)

Phase 107.2 is infra/config — no dynamic-data-rendering artifacts to trace. The closest equivalent is the `/v1/health` proxy round-trip, which Plan 02 SUMMARY captured live: Vite on :5173 actually forwarded to vigil-core on localhost:3001 and returned the real JSON health payload `{"status":"ok","database":"connected"}`. That proves the full data path (browser → Vite proxy → vigil-core → Postgres → response) is FLOWING on the iMac.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| preflight-check.sh is syntactically valid bash | `bash -n Scripts/preflight-check.sh` | exit 0 | PASS |
| Live preflight run (all 5 checks on current iMac state) | `bash Scripts/preflight-check.sh` | All 5 PASS; exit 0; Check 5 reports `PASS — 0.0.0.0 bind (macOS Application Firewall off ...)` | PASS |
| VIGIL_BIND_HOST env var referenced in index.ts | `grep -c 'VIGIL_BIND_HOST' vigil-core/src/index.ts` | 3 | PASS |
| Prod CORS FATAL string present | `grep -c 'FATAL: CORS_ORIGINS must be set in production' vigil-core/src/index.ts` | 1 | PASS |
| loadEnv imported + used in vite.config.ts | `grep -c 'loadEnv' vigil-pwa/vite.config.ts` | 4 | PASS |
| host: true in vite.config.ts | `grep -c 'host: true' vigil-pwa/vite.config.ts` | 1 | PASS |
| check_bind_and_firewall function defined + called | `grep -c 'check_bind_and_firewall' Scripts/preflight-check.sh` | 2 | PASS |
| socketfilterfw referenced | `grep -c 'socketfilterfw' Scripts/preflight-check.sh` | 4 | PASS |
| No `defaults read` anti-pattern | `grep -c 'defaults read /Library/Preferences/com.apple.alf' Scripts/preflight-check.sh` | 0 | PASS |
| FAIL=1 confined to unsupported-value branch | `awk '/check_bind_and_firewall/,/^}/' ... \| grep -c 'FAIL=1'` | 1 | PASS |
| No hardcoded prod API target left in vite.config.ts | `grep -c "target: 'https://api.vigilhub.io'" vigil-pwa/vite.config.ts` | 0 | PASS |
| No process.env misuse in vite.config.ts (must use loadEnv) | `grep -c 'process.env.VITE_DEV_API_TARGET' vigil-pwa/vite.config.ts` | 0 | PASS |
| import.meta.env not used in server code | `grep -c 'import.meta.env' vigil-core/src/index.ts` | 0 | PASS |
| All 6 claimed commits exist on main | `git log --oneline \| grep -E '56ea983\|e39f82b\|8e555d4\|7443ad8\|bad2e34\|2991b6c'` | 6/6 found | PASS |
| Backup artifacts created | `ls vigil-core/.env.bak.20260422-124643 vigil-pwa/.env.local.bak.20260422-125525` | both present | PASS |
| Gitignore protects mutated env files | `git check-ignore vigil-core/.env vigil-pwa/.env.local` | both ignored | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| REQ-DEV-CROSS-MACHINE | Plans 01, 02, 03 frontmatter + all 3 SUMMARY `requirements-completed` | Not defined anywhere in REQUIREMENTS.md | BLOCKED (traceability gap) | `grep -c 'REQ-DEV-CROSS-MACHINE' .planning/REQUIREMENTS.md` → 0. `grep -c '107.2' .planning/REQUIREMENTS.md` → 0. ROADMAP.md Phase 107.2 section shows `Requirements: TBD`. Plan 03 SUMMARY key-decisions explicitly noted `requirements mark-complete returned not_found` |

**One orphaned/unregistered requirement.** The code delivers what REQ-DEV-CROSS-MACHINE means (by inference from plan objectives and context) — but the requirement was never formally added to REQUIREMENTS.md, so the traceability contract is broken. This is the single gap blocking full phase closure.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `vigil-pwa/vite.config.ts` | 15 | `env.VITE_DEV_API_TARGET ?? 'http://localhost:3001'` uses `??` (nullish coalescing) — empty string bypasses fallback | Info (flagged in REVIEW WR-01, not a phase gap) | If user sets `VITE_DEV_API_TARGET=` empty, `apiTarget` becomes `""` and Vite silently misbehaves. Pre-existing warning from code-reviewer; not blocking |
| `Scripts/preflight-check.sh` | 103 | `${BIND_VALUE// /}` strips spaces only, not tabs or `\r` | Info (REVIEW WR-02) | CRLF-edited `.env` files could spuriously fail Check 5 with `VIGIL_BIND_HOST='0.0.0.0\r' is not a supported value` |
| `vigil-core/.env.example` | 65 | `VIGIL_BIND_HOST=0.0.0.0` uncommented by default in the template | Info (REVIEW WR-03) | dev-setup.sh copies .env.example → .env, so new clones land on the exposed-bind path. Mitigated by preflight Check 5 surfacing it loudly; still a doc/default mismatch |

All three anti-patterns were identified by the code-reviewer and documented in `107.2-REVIEW.md`. They are classified as **Info/Warning (not Blocker)** — the phase goal is achieved, these are post-merge polish candidates.

### Human Verification Required

See frontmatter `human_verification` section. Three items:

#### 1. Cross-machine boot from MacBook Pro → iMac over Tailscale

**Test:** On iMac, run `npm run dev` from repo root. On MacBook Pro, open `http://jamesons-imac-2:5173` in a browser.
**Expected:** Vigil PWA loads and renders. DevTools → Network → `/v1/*` requests are same-origin on :5173 (not direct to :3001). Login works.
**Why human:** Requires both physical machines + Tailscale + manual browser observation.

#### 2. macOS firewall ON + 0.0.0.0 WARN branch of preflight Check 5

**Test:** `sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on`, then `bash scripts/preflight-check.sh`, then restore with `--setglobalstate off`.
**Expected:** Yellow WARN block with node.js allow + sudo-disable fix suggestions. Preflight overall exits 0.
**Why human:** Requires sudo mutation of host firewall state. Code-inspection confirms FAIL=1 count in the function is 1 (unsupported-value branch only), so the WARN path provably cannot block; the exact yellow text is only seen by triggering it live.

#### 3. Live Railway prod deploy refuses to boot without CORS_ORIGINS

**Test:** Intentionally unset CORS_ORIGINS on Railway and attempt a deploy.
**Expected:** Deploy exits 1 with exact FATAL string in the Railway log; prod stays on last healthy deploy.
**Why human:** Cannot safely automate. Plan 01 performed a LOCAL `NODE_ENV=production node dist/index.js` probe that captured exit 1 + exact FATAL string — safest available proxy. Full production proof only materializes the day CORS_ORIGINS ever becomes unset on Railway.

### Gaps Summary

The phase achieves its operational goal — cross-machine dev access works end-to-end on the iMac locally (verified in Plan 02 SUMMARY via the curl /v1/health proxy round-trip), all 15 declared plan truths are verified, all 6 commits landed, and no regression of Phase 107.1's gains. The preflight gate runs cleanly on current state.

**The single gap is traceability, not functionality:** REQ-DEV-CROSS-MACHINE is referenced by three plans and claimed completed by three SUMMARYs, but has never been defined in REQUIREMENTS.md. Plan 03 SUMMARY caught this live (`requirements mark-complete returned not_found`) and logged it as a scope-boundary note. ROADMAP.md Phase 107.2 still shows `Requirements: TBD` and `Goal: [Urgent work - to be planned]`. This is a documentation-debt gap the orchestrator must close before the phase is truly complete.

Closing steps:
1. Add REQ-DEV-CROSS-MACHINE to `.planning/REQUIREMENTS.md` under "Developer Environment" (sibling to REQ-DEV-LOCAL-ENV) with a one-line description capturing the phase goal.
2. Add a Traceability table row: `| REQ-DEV-CROSS-MACHINE | Phase 107.2 | Complete |`.
3. Bump the coverage footer (14 → 15 total, 14 → 15 mapped).
4. Fill in ROADMAP.md Phase 107.2 Goal (from 107.2-CONTEXT.md `<domain>` block) and set Requirements: REQ-DEV-CROSS-MACHINE.

These are small, non-code edits. Once they land, the three `human_verification` items can be exercised (or the first two deferred to the next in-person session with both machines) and the phase is closable.

---

*Verified: 2026-04-22T14:30:00Z*
*Verifier: Claude (gsd-verifier)*
