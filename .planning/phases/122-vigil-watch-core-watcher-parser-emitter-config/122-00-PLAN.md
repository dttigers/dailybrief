---
phase: 122
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/VigilWatch/.gitkeep"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/vigil-watch/main.swift"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/PackageScaffoldTests.swift"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/.gitkeep"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/needs_input_assistant_tool_use.jsonl"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/tool_result_success.jsonl"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/tool_result_error.jsonl"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/assistant_end_turn.jsonl"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/user_default_permission.jsonl"
  - "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/non_spec_lines.jsonl"
autonomous: true
requirements:
  - AGENT-WATCH-01
  - AGENT-WATCH-02
  - AGENT-WATCH-03
  - AGENT-WATCH-06

must_haves:
  truths:
    - "swift build exits 0 in the vigil-watch repo"
    - "swift test exits 0 with at least one passing test"
    - "JSONL fixtures from Phase 120 verification corpus are present in Tests/VigilWatchTests/Fixtures/"
  artifacts:
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift"
      provides: "Swift Package manifest with VigilWatch library, vigil-watch executable, VigilWatchTests test target, platforms [.macOS(.v14)]"
      contains: "platforms: [.macOS(.v14)]"
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/vigil-watch/main.swift"
      provides: "Executable entry stub printing startup banner; replaced in Plan 09"
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/PackageScaffoldTests.swift"
      provides: "Smoke XCTest case so swift test exits 0"
    - path: "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/needs_input_assistant_tool_use.jsonl"
      provides: "Fixture grounding needs_input detection rule"
  key_links:
    - from: "Package.swift"
      to: "Sources/VigilWatch/, Sources/vigil-watch/, Tests/VigilWatchTests/"
      via: "SPM target paths"
      pattern: "path: \"Sources/VigilWatch\""
---

<objective>
Stand up the Swift Package skeleton at `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/` so all subsequent plans have a working `swift build` + `swift test` floor. This is Wave 0: it lands `Package.swift`, empty source/test directory placeholders, a minimal `main.swift` stub, a smoke XCTest, and fixture JSONL files copied from `vigil-watch/verification-log/excerpts/` so Wave 1 detection-rule tests can ground in real corpus data.

Purpose: Without this scaffold every Wave 1 plan would re-litigate the same `Package.swift`. Centralizing it here also avoids Wave 1 plans clashing on the same file.

Output: A Swift package that compiles, runs zero feature code, and has `swift test` returning success — the precondition for everything downstream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-CONTEXT.md
@.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-RESEARCH.md
@.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-VALIDATION.md

<interfaces>
<!-- Project layout (locked by CONTEXT.md "Claude's Discretion: Swift package layout") -->

vigil-watch/                       # repo root (existing: README, LICENSE, verification-log)
├── Package.swift                  # NEW (this plan)
├── Sources/
│   ├── VigilWatch/                # library target — Wave 1+ plans land .swift here
│   └── vigil-watch/
│       └── main.swift             # NEW (this plan, stub; final wiring in Plan 09)
└── Tests/
    └── VigilWatchTests/
        ├── PackageScaffoldTests.swift   # NEW (this plan)
        └── Fixtures/                    # NEW (this plan, populated from verification-log/)

Working directory for ALL Swift commands: `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/` (NOT the dailybrief monorepo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Create Package.swift + source/test directory layout</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/VigilWatch/.gitkeep,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/vigil-watch/main.swift,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/PackageScaffoldTests.swift
  </files>
  <read_first>
    - /Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-CONTEXT.md (Claude's Discretion: "Swift package layout" + "Swift / library picks")
    - /Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-RESEARCH.md ("Recommended Project Structure" + "Package.swift scaffold" code example, lines 210–243 and ~995–1028)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md (so executor sees current vigil-watch repo state — README + LICENSE + verification-log/ only)
  </read_first>
  <action>
    Create `Package.swift` at the vigil-watch repo root using the scaffold from RESEARCH.md §"Package.swift scaffold" verbatim:

    ```swift
    // swift-tools-version:5.10
    import PackageDescription

    let package = Package(
        name: "vigil-watch",
        platforms: [.macOS(.v14)],
        products: [
            .executable(name: "vigil-watch", targets: ["vigil-watch"]),
            .library(name: "VigilWatch", targets: ["VigilWatch"]),
        ],
        dependencies: [
            // Phase 122: zero external dependencies (CONTEXT.md "Swift / library picks").
            // Phase 123 will add swift-argument-parser; do NOT add it here.
        ],
        targets: [
            .target(name: "VigilWatch", path: "Sources/VigilWatch"),
            .executableTarget(
                name: "vigil-watch",
                dependencies: ["VigilWatch"],
                path: "Sources/vigil-watch"
            ),
            .testTarget(
                name: "VigilWatchTests",
                dependencies: ["VigilWatch"],
                path: "Tests/VigilWatchTests"
            ),
        ]
    )
    ```

    Create empty placeholder file `Sources/VigilWatch/.gitkeep` so SPM does not error on an empty target directory. Wave 1 plans will land real `.swift` files alongside.

    Create stub `Sources/vigil-watch/main.swift`:
    ```swift
    // Phase 122 Plan 00 stub — final wiring lands in Plan 09 (122-09-PLAN.md).
    // This is a temporary entry point so `swift build` succeeds before feature work.
    import Foundation
    print("vigil-watch scaffold (Phase 122 Plan 00) — feature wiring pending")
    ```

    Create smoke test `Tests/VigilWatchTests/PackageScaffoldTests.swift`:
    ```swift
    import XCTest
    @testable import VigilWatch

    final class PackageScaffoldTests: XCTestCase {
        func testPackageBuilds() {
            // Wave 0 sentinel — replaced by real coverage in Wave 1 plans.
            XCTAssertTrue(true)
        }
    }
    ```

    Working directory MUST be `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/` for all `swift` invocations — not the dailybrief monorepo.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" && swift build 2>&1 | tail -5 && swift test --filter PackageScaffoldTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift` exists
    - `grep -c "platforms: \[.macOS(.v14)\]" "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift"` returns 1
    - `grep -c '.executable(name: "vigil-watch"' "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift"` returns 1
    - `grep -c '.library(name: "VigilWatch"' "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Package.swift"` returns 1
    - File `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Sources/vigil-watch/main.swift` exists and contains the literal string `vigil-watch scaffold (Phase 122 Plan 00)`
    - File `/Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/PackageScaffoldTests.swift` exists
    - `cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" && swift build` exits 0
    - `cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" && swift test --filter PackageScaffoldTests` exits 0
  </acceptance_criteria>
  <done>swift build and swift test both exit 0 in the vigil-watch repo with the stub package.</done>
</task>

<task type="auto">
  <name>Task 0.2: Copy Phase 120 fixture JSONL into Tests/VigilWatchTests/Fixtures/</name>
  <files>
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/.gitkeep,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/needs_input_assistant_tool_use.jsonl,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/tool_result_success.jsonl,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/tool_result_error.jsonl,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/assistant_end_turn.jsonl,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/user_default_permission.jsonl,
    /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/Tests/VigilWatchTests/Fixtures/non_spec_lines.jsonl
  </files>
  <read_first>
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/verification-log/excerpts/ (list of 16 sanitized JSONL excerpts from Phase 120)
    - /Users/jamesonmorrill/Desktop/Local AI/vigil-watch/README.md (Excerpts 1–8 — they explain which excerpt grounds which detection rule)
    - /Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-VALIDATION.md ("Wave 0 Requirements" → fixture list)
  </read_first>
  <action>
    Copy these 6 specific fixture files from `verification-log/excerpts/` into `Tests/VigilWatchTests/Fixtures/`. Use `cp` (do NOT modify content — they are already sanitized per Phase 120):

    | Destination filename | Source excerpt | Grounds detection rule |
    |---|---|---|
    | `needs_input_assistant_tool_use.jsonl` | `corpus-01d74043-assistant-tool_use-line25.jsonl` | needs_input (assistant + inner tool_use) |
    | `tool_result_success.jsonl` | `corpus-0910ca8a-tool_result-success-line16.jsonl` | tool_result success path (no event) |
    | `tool_result_error.jsonl` | `corpus-01d74043-tool_result-error-line26.jsonl` | task_failed (is_error: true) |
    | `assistant_end_turn.jsonl` | `scripted-D-idle-context-lines21-22.jsonl` | task_complete (stop_reason end_turn + silence) |
    | `user_default_permission.jsonl` | `scripted-A-tool-approval-lines3-13.jsonl` | permissionMode field |
    | `non_spec_lines.jsonl` | concatenate first lines of `corpus-00697e82-attachment-command_permissions-line9.jsonl`, `corpus-00697e82-ai-title-line11.jsonl`, `corpus-04183ac2-system-line496.jsonl`, `corpus-910ba667-summary-lines1-5.jsonl`, `corpus-00697e82-file-history-snapshot-line10.jsonl`, `corpus-00697e82-queue-operation-lines1-5.jsonl`, `corpus-05803b96-last-prompt-line54.jsonl` | non-spec line types (parser must skip without emitting) |

    For `non_spec_lines.jsonl`, build it by `cat`ing one line from each of the 7 source excerpts and concatenating with `\n` separators.

    Add `Tests/VigilWatchTests/Fixtures/.gitkeep` so the directory survives even if SPM doesn't copy fixtures by default. Wave 1 ParserTests will reference these via `Bundle.module` (requires adding `resources: [.copy("Fixtures")]` to the test target — make that edit to `Package.swift` here).

    Update Package.swift's testTarget to include resources:
    ```swift
    .testTarget(
        name: "VigilWatchTests",
        dependencies: ["VigilWatch"],
        path: "Tests/VigilWatchTests",
        resources: [.copy("Fixtures")]
    ),
    ```

    Verify each destination file is non-empty and parses as valid JSON per line (use `jq`). `non_spec_lines.jsonl` should have exactly 7 lines (one per source excerpt's first line).
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/vigil-watch" && for f in needs_input_assistant_tool_use.jsonl tool_result_success.jsonl tool_result_error.jsonl assistant_end_turn.jsonl user_default_permission.jsonl non_spec_lines.jsonl; do test -s "Tests/VigilWatchTests/Fixtures/$f" || { echo "MISSING/EMPTY: $f"; exit 1; }; done && wc -l Tests/VigilWatchTests/Fixtures/non_spec_lines.jsonl && swift test --filter PackageScaffoldTests 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 6 destination files exist under `Tests/VigilWatchTests/Fixtures/` and are non-empty
    - `wc -l Tests/VigilWatchTests/Fixtures/non_spec_lines.jsonl` reports 7
    - `grep -c 'resources: \[.copy("Fixtures")\]' Package.swift` returns 1
    - `swift test --filter PackageScaffoldTests` still exits 0 after the Package.swift edit
    - Each fixture file contains at least one `\n`-terminated line that parses as JSON (validated by Plan 02 ParserTests when they land)
  </acceptance_criteria>
  <done>Fixtures grounded in Phase 120's verification corpus are checked in and reachable from XCTest at runtime via Bundle.module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo public/private | vigil-watch repo is public on GitHub (per Phase 120) — fixtures must contain no PII or credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-06 | I (Information disclosure) | Fixture JSONL files | mitigate | Source files in `verification-log/excerpts/` were already sanitized in Phase 120 (no Bearer tokens, no API keys, all attachment payloads truncated). Copy verbatim — do NOT regenerate from raw `~/.claude/projects/`. Plan 120-02 already locked this disposition. |
</threat_model>

<verification>
- `swift build` exits 0
- `swift test` exits 0 (one passing scaffold test)
- 6 fixture files present and non-empty
- Package.swift includes `resources: [.copy("Fixtures")]` for the test target
</verification>

<success_criteria>
Wave 0 satisfied: every Wave 1 plan can run `swift test --filter <suite>` with a passing baseline. No production code yet; just the floor.
</success_criteria>

<output>
After completion, create `.planning/phases/122-vigil-watch-core-watcher-parser-emitter-config/122-00-SUMMARY.md`
</output>
