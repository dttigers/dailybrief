---
phase: 34-mac-migration-first
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/JarvisCore/Storage/ThoughtRepository.swift
  - Sources/JarvisCore/Storage/ThoughtStore.swift
  - Sources/JarvisCore/Services/VigilAPIClient.swift
autonomous: true
---

<objective>
Create ThoughtRepository protocol abstraction and VigilAPIClient HTTP client to enable swappable data backends.

Purpose: The Mac app currently couples directly to ThoughtStore (GRDB). Extracting a protocol lets us swap in an API-backed implementation without changing any ViewModel or service code. The VigilAPIClient provides typed HTTP access to Vigil Core API.
Output: ThoughtRepository protocol, ThoughtStore conformance, VigilAPIClient ready for use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Sources/JarvisCore/Storage/ThoughtStore.swift
@Sources/JarvisCore/Models/Thought.swift
@Sources/JarvisCore/Services/CaptureService.swift
@Sources/DailyBriefMonitor/Dashboard/DashboardViewModel.swift
@Sources/DailyBriefMonitor/AppDelegate.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThoughtRepository protocol</name>
  <files>Sources/JarvisCore/Storage/ThoughtRepository.swift</files>
  <action>
Create a new file `ThoughtRepository.swift` in `Sources/JarvisCore/Storage/` defining a protocol that captures the data-layer interface used by DashboardViewModel and CaptureService.

The protocol must be `public` and require conforming types to be actors (use `protocol ThoughtRepository: Actor`).

Include ALL public methods from ThoughtStore EXCEPT CloudKit sync methods. Specifically EXCLUDE:
- `fetchPendingSync()`, `fetchPendingDeletions()`, `markSynced(id:)`, `markPendingDeletion(id:)`, `upsertFromCloud(_:)`, `fetchByCloudKitRecordID(_:)`, `deletePermanently(id:)`

These sync methods are local-only concerns and don't belong in the shared protocol.

Include all of these method groups:
- CRUD: `save`, `update`, `delete`, `fetch(id:)`
- List/Filter: `fetchAll`, `fetchFiltered`, `countFiltered`, `count`, `search`
- Tasks: `updateTaskStatus`, `fetchTasks`, `countTasks`
- Therapy: `fetchTherapyThoughts`, `countTherapy`, `countUnclassifiedTherapy`, `fetchTherapyThoughtsByDateRange`, `fetchRecentTherapyThoughts`, `bulkUpdateTherapyClassification`
- Tags: `addTag`, `removeTag`, `fetchByTag`, `allUniqueTags`, `bulkAddTag`, `bulkRemoveTag`
- Favorites: `toggleFavorite`, `fetchFavorites`, `countFavorites`
- Links: `linkThoughts`, `unlinkThoughts`, `fetchLinkedThoughts`, `countLinks`
- Bulk: `bulkDelete`, `bulkUpdateCategory`

Keep exact same method signatures as ThoughtStore. The `save` method uses `inout Thought` — for the protocol, change it to accept and return `Thought` instead (actor boundaries don't support inout). Add a new method signature: `func saveThought(_ thought: Thought) async throws -> Thought`. ThoughtStore will implement both.

Import Foundation and the Thought model types. Keep the file focused — just the protocol definition, no implementations.
  </action>
  <verify>swift build 2>&1 | head -20 — compiles without errors</verify>
  <done>ThoughtRepository protocol defined with all data-layer methods (excluding sync), compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Make ThoughtStore conform + Create VigilAPIClient</name>
  <files>Sources/JarvisCore/Storage/ThoughtStore.swift, Sources/JarvisCore/Services/VigilAPIClient.swift</files>
  <action>
**Part A: ThoughtStore conformance**

Add `ThoughtRepository` conformance to ThoughtStore's declaration: `public actor ThoughtStore: ThoughtRepository`.

Add the `saveThought(_ thought: Thought) async throws -> Thought` method that wraps the existing `save(&thought)` — creates a mutable copy, calls save, returns the result.

ThoughtStore already implements all other protocol methods, so conformance should be automatic once the declaration is added.

**Part B: VigilAPIClient**

Create `Sources/JarvisCore/Services/VigilAPIClient.swift` — a typed HTTP client for Vigil Core API.

Structure:
```
public actor VigilAPIClient {
    private let baseURL: URL  // e.g., http://localhost:3001/v1
    private let session: URLSession

    public init(baseURL: URL, session: URLSession = .shared)
```

Implement these HTTP methods (generic helpers):
- `func get<T: Decodable>(path: String, query: [String: String]) async throws -> T`
- `func post<T: Decodable, B: Encodable>(path: String, body: B) async throws -> T`
- `func put<T: Decodable, B: Encodable>(path: String, body: B) async throws -> T`
- `func delete(path: String) async throws`
- `func postNoResponse<B: Encodable>(path: String, body: B) async throws -> Int` (returns status code)

Response types (define in the same file or a separate types file):
```
struct PaginatedResponse<T: Decodable>: Decodable {
    let data: [T]
    let total: Int
    let limit: Int
    let offset: Int
}

struct CountResponse: Decodable {
    let updated: Int  // for bulk operations
}
```

Error handling:
- Define `VigilAPIError` enum: `.networkError(Error)`, `.httpError(statusCode: Int, message: String)`, `.decodingError(Error)`, `.serverUnavailable`
- Use ISO 8601 date decoding strategy on JSONDecoder
- Use ISO 8601 date encoding strategy on JSONEncoder
- Check for non-2xx status codes and throw appropriate errors

Use Foundation URLSession (already available in the project). No external HTTP library needed.
  </action>
  <verify>swift build 2>&1 | head -20 — compiles without errors, ThoughtStore conforms to ThoughtRepository</verify>
  <done>ThoughtStore conforms to ThoughtRepository, VigilAPIClient compiles with typed HTTP methods and error handling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] ThoughtStore declaration shows `: ThoughtRepository` conformance
- [ ] VigilAPIClient has all HTTP helper methods
- [ ] No changes to existing behavior (ThoughtStore methods unchanged)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ThoughtRepository protocol covers all non-sync ThoughtStore methods
- ThoughtStore conforms to ThoughtRepository
- VigilAPIClient provides typed HTTP access to Vigil Core API
</success_criteria>

<output>
After completion, create `.planning/phases/34-mac-migration-first/34-02-SUMMARY.md`
</output>
