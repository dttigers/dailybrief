---
phase: 79.1-close-oauth-schema-gap
plan: 01
subsystem: vigil-core-db
tags: [oauth, database, migration, schema, drizzle]
requirements: [OAUTH-04]
dependency-graph:
  requires: []
  provides:
    - "oauth_tokens.scopes jsonb column"
    - "oauth_tokens.account_email text column"
    - "drizzle/0008_add_oauth_scopes_and_account_email.sql"
  affects:
    - "Plan 79.1-02 (callback + status wiring can persist scopes + email)"
tech-stack:
  added: []
  patterns:
    - "Additive-only Drizzle migration (no DROP, no type change, safely re-runnable)"
    - "Jsonb array with default '[]'::jsonb to preserve existing rows"
key-files:
  created:
    - "vigil-core/drizzle/0008_add_oauth_scopes_and_account_email.sql"
    - "vigil-core/drizzle/meta/0008_snapshot.json"
  modified:
    - "vigil-core/src/db/schema.ts"
    - "vigil-core/drizzle/meta/_journal.json"
decisions:
  - "scopes column gets NOT NULL because default '[]'::jsonb covers existing rows safely"
  - "account_email stays nullable — legacy rows cannot be back-filled; Plan 02 handles empty case"
  - "Local db:push skipped (no DATABASE_URL); migration applies on next Railway deploy"
metrics:
  duration_seconds: 97
  completed_date: "2026-04-14"
  tasks_completed: 2
  files_touched: 4
---

# Phase 79.1 Plan 01: Close OAuth Schema Gap — Schema + Migration Summary

Added `scopes` (jsonb, default `[]`, notNull) and `account_email` (text, nullable) columns to the `oauth_tokens` table via Drizzle schema update + generated migration `0008_add_oauth_scopes_and_account_email.sql`, unblocking Plan 02 callback/status wiring.

## What Was Built

**Task 1 — Schema update (commit `74b896b`):**
- Extended `oauthTokens` pgTable in `vigil-core/src/db/schema.ts` with:
  - `scopes: jsonb("scopes").$type<string[]>().default([]).notNull()`
  - `accountEmail: text("account_email")` (nullable)
- Placed after `calendarSelections`, before `createdAt` — same block, no index changes.
- `tsc --noEmit` passes clean.

**Task 2 — Generated migration (commit `07a3cec`) [BLOCKING]:**
- Ran `drizzle-kit generate --name add_oauth_scopes_and_account_email`.
- Produced `vigil-core/drizzle/0008_add_oauth_scopes_and_account_email.sql`:
  ```sql
  ALTER TABLE "oauth_tokens" ADD COLUMN "scopes" jsonb DEFAULT '[]'::jsonb NOT NULL;--> statement-breakpoint
  ALTER TABLE "oauth_tokens" ADD COLUMN "account_email" text;
  ```
- Both statements match UAT Gap DDL exactly; no NOT NULL on `account_email`.
- `drizzle/meta/_journal.json` updated; `0008_snapshot.json` created.

## Verification Results

| Check | Result |
|-------|--------|
| `grep 'scopes: jsonb("scopes")' schema.ts` | 1 match (line 183) |
| `grep 'accountEmail: text("account_email")' schema.ts` | 1 match (line 184) |
| `test -f drizzle/0008_add_oauth_scopes_and_account_email.sql` | pass |
| `grep -c 'ADD COLUMN "scopes"'` in migration | 1 |
| `grep -c 'ADD COLUMN "account_email"'` in migration | 1 |
| `jsonb DEFAULT '[]'::jsonb` present on scopes | yes |
| `NOT NULL` absent on account_email | yes |
| `tsc --noEmit` | exit 0, no errors |

## Deviations from Plan

None — plan executed exactly as written.

One plan-allowed condition encountered:
- `drizzle-kit push` failed because `DATABASE_URL` is not set in this worktree env. Plan Task 2 Step 3 explicitly allows this: "The file itself is the deliverable; apply-to-live is downstream." Migration will apply on next Railway deploy via `db:migrate-prod`.

No Rule 1-3 auto-fixes. No Rule 4 checkpoints.

## Deferred Issues

None.

## Known Stubs

None.

## Threat Flags

None. Changes strictly additive; no new trust boundary introduced.

## Environment Notes

- Worktree had no `node_modules`; ran `npm install` inside `vigil-core/` to obtain `drizzle-kit` and `tsc` binaries before generate/typecheck. Install is worktree-local and not committed.

## Self-Check

**Files created/modified (absolute paths):**
- `/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.claude/worktrees/agent-aeea7bbc/vigil-core/src/db/schema.ts` — FOUND
- `/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.claude/worktrees/agent-aeea7bbc/vigil-core/drizzle/0008_add_oauth_scopes_and_account_email.sql` — FOUND
- `/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.claude/worktrees/agent-aeea7bbc/vigil-core/drizzle/meta/0008_snapshot.json` — FOUND
- `/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.claude/worktrees/agent-aeea7bbc/vigil-core/drizzle/meta/_journal.json` — FOUND

**Commits:**
- `74b896b` — FOUND (Task 1 schema)
- `07a3cec` — FOUND (Task 2 migration)

## Self-Check: PASSED
