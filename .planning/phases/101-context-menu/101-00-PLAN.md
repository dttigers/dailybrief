---
phase: 101
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vigil-pwa/src/components/ContextMenu.test.tsx
  - vigil-pwa/src/components/ToastHost.test.tsx
  - vigil-pwa/src/hooks/useToast.test.tsx
  - vigil-pwa/src/components/ThoughtRow.test.tsx
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05, CTX-06, CTX-07]
tags: [test-scaffold, vitest, context-menu, toast, wave-0]

must_haves:
  truths:
    - "Every Phase 101 behavior has a failing test that will later go green"
    - "No Wave 1/2/3 task claims 'MISSING — Wave 0 must create test file first' — all scaffolds exist"
    - "Test files are fail-by-default: every `it(…)` either asserts the production contract or is `it.skip` with a TODO for Wave 3 manual gates"
  artifacts:
    - path: "vigil-pwa/src/components/ContextMenu.test.tsx"
      provides: "Failing test scaffold for ContextMenu trigger, positioning, close, submenus, a11y, edit interlock"
      contains: "describe('ContextMenu'"
      min_lines: 120
    - path: "vigil-pwa/src/components/ToastHost.test.tsx"
      provides: "Failing test scaffold for toast render, auto-dismiss, replace-commits-first"
      contains: "describe('ToastHost'"
      min_lines: 40
    - path: "vigil-pwa/src/hooks/useToast.test.tsx"
      provides: "Failing test scaffold for showToast, dismiss, expire, variant"
      contains: "describe('useToast'"
      min_lines: 50
    - path: "vigil-pwa/src/components/ThoughtRow.test.tsx"
      provides: "Extended test file covering right-click, long-press (500ms/10px), edit-mode suppression, menu action routing"
      contains: "context menu"
      min_lines: 300
  key_links:
    - from: "vigil-pwa/src/components/ContextMenu.test.tsx"
      to: "ToastProvider"
      via: "import { ToastProvider } from '../hooks/useToast' — tests render within provider"
      pattern: "ToastProvider"
    - from: "vigil-pwa/src/components/ThoughtRow.test.tsx"
      to: "window event bus"
      via: "spies on vigil:edit-started for D-19 interlock test"
      pattern: "vigil:edit-started"
---

<objective>
Create fail-by-default vitest scaffolds for every Phase 101 behavior so Waves 1-3 implement against concrete test expectations (RED→GREEN per Phase 100 pattern).

Purpose: Nyquist validation — every subsequent task has an `<automated>` command targeting a file that already exists, and the test names pin down the contract before a single line of production code is written. Matches Phase 100's proven pattern (12 new test cases, one test file per surface).

Output: 4 test files (1 new, 3 new) containing ~30 failing `it(…)` cases mapped 1:1 to Phase 101 acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-context-menu/101-CONTEXT.md
@.planning/phases/101-context-menu/101-RESEARCH.md
@.planning/phases/101-context-menu/101-UI-SPEC.md
@.planning/phases/101-context-menu/101-VALIDATION.md
@.planning/phases/100-edit-refresh-pause/100-01-SUMMARY.md
@vigil-pwa/src/components/ThoughtRow.test.tsx
@vigil-pwa/src/hooks/useThoughts.test.tsx

<interfaces>
<!-- Contracts Wave 0 tests must exercise. Copied from api/client.ts + ThoughtRow.tsx + RESEARCH.md. -->

From `vigil-pwa/src/api/client.ts`:
```typescript
export interface ThoughtApiResponse {
  id: number
  content: string
  category: string | null
  confidence: number | null
  source: string
  createdAt: string
  modifiedAt: string
  taskStatus: string | null
  therapyClassification: string | null
  tags: string[]
  isFavorited: boolean
  projectId: number | null
}
export interface ProjectApiResponse { id: number; name: string; description: string | null; status: string | null; createdAt: string; updatedAt: string }
```

From Phase 100 (`vigil-pwa/src/components/ThoughtRow.tsx`):
```typescript
// Phase 100 invariant — Edit menu item MUST trigger this path:
function handleContentClick() {
  setDraft(thought.content)
  setIsEditing(true)
  window.dispatchEvent(new CustomEvent('vigil:edit-started', { detail: { id: thought.id } }))
}
```

ContextMenu props contract (to be implemented in Plan 02):
```typescript
interface ContextMenuProps {
  anchor: { x: number; y: number }
  thought: ThoughtApiResponse
  projects: ProjectApiResponse[]
  openedVia: 'mouse' | 'touch'           // drives desktop hover-submenu vs mobile inline-replace (RESEARCH Open Q1)
  onClose: () => void
  onStartEdit: () => void                // reuses ThoughtRow.handleContentClick — D-19 interlock
  onRetriage: (id: number) => void       // reuses existing onRetriage prop
  onMoveToCategory: (id: number, category: string) => void
  onAssignProject: (id: number, projectId: number) => void
  onDelete: (id: number) => void         // optimistic + toast deferred-commit
}
```

useToast contract (to be implemented in Plan 01):
```typescript
interface Toast {
  id: number
  body: string
  action?: string
  onAction?: () => void
  onExpire?: () => void
  variant: 'default' | 'error'
}
interface ToastContextValue {
  current: Toast | null
  showToast: (t: Omit<Toast, 'id'>) => void
  dismiss: () => void
}
const TOAST_DURATION_MS = 5_000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ContextMenu.test.tsx (fail-by-default)</name>
  <files>vigil-pwa/src/components/ContextMenu.test.tsx</files>
  <read_first>
    - vigil-pwa/src/components/ContextMenu.test.tsx (verify not yet created)
    - vigil-pwa/src/components/ThoughtRow.test.tsx (existing spy pattern, baseThought shape)
    - vigil-pwa/src/hooks/useThoughts.test.tsx (fake-timer + flushMicrotasks pattern)
    - .planning/phases/101-context-menu/101-CONTEXT.md (D-01..D-21)
    - .planning/phases/101-context-menu/101-RESEARCH.md (Patterns 1-6, Pitfalls 1-9, "Context menu test" example)
    - .planning/phases/101-context-menu/101-UI-SPEC.md (copy contract: labels, roles, toast copy)
    - .planning/phases/101-context-menu/101-VALIDATION.md (Per-Task Verification Map requirements)
  </read_first>
  <action>
Create `vigil-pwa/src/components/ContextMenu.test.tsx` as a fail-by-default scaffold.

Since `ContextMenu.tsx` does not yet exist, use a top-level `import ContextMenu from './ContextMenu'` — the import itself will fail until Plan 02 creates the module. That is the RED state.

Baseline imports:
```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, fireEvent, screen, waitFor, act } from '@testing-library/react'
import ContextMenu from './ContextMenu'
import { ToastProvider } from '../hooks/useToast'
import type { ThoughtApiResponse, ProjectApiResponse } from '../api/client'

const baseThought: ThoughtApiResponse = {
  id: 42, content: 'hello', category: 'idea', confidence: null, source: 'text',
  createdAt: '2026-04-17T00:00:00Z', modifiedAt: '2026-04-17T00:00:00Z',
  taskStatus: null, therapyClassification: null, tags: [], isFavorited: false, projectId: null,
}
const baseProjects: ProjectApiResponse[] = [
  { id: 1, name: 'Alpha', description: null, status: null, createdAt: '', updatedAt: '' },
  { id: 2, name: 'Beta', description: null, status: null, createdAt: '', updatedAt: '' },
]

function wrap(ui: React.ReactElement) {
  return render(<ToastProvider>{ui}</ToastProvider>)
}
```

Write the following `describe` blocks and `it(...)` cases. Every test body must contain at least one `expect(...)` that WILL fail once the import resolves (so the RED is real, not just a compile error):

`describe('ContextMenu — open + position')`:
1. `it('renders role="menu" on open')` — mount with `openedVia='mouse'` and anchor `{x:100,y:100}`; assert `screen.getByRole('menu')` exists.
2. `it('applies anchor position as fixed left/top')` — assert `screen.getByRole('menu')` has `style.left === '100px'` and `style.top === '100px'`.
3. `it('shifts left when right-edge would overflow')` — set viewport width via `window.innerWidth=300`; anchor at x=280; expect adjusted left < 280.
4. `it('flips above when bottom-edge would overflow')` — set viewport height via `window.innerHeight=200`; anchor at y=180; expect adjusted top < 180.

`describe('ContextMenu — close behavior (D-07)')`:
5. `it('closes on Escape key')` — `fireEvent.keyDown(window, { key: 'Escape' })`; assert `onClose` called.
6. `it('closes on outside pointerdown')` — `fireEvent.pointerDown(document.body)`; assert `onClose` called.
7. `it('closes on window scroll (capture)')` — `fireEvent.scroll(window)` OR `window.dispatchEvent(new Event('scroll'))`; assert `onClose` called.
8. `it('closes on window resize')` — `fireEvent.resize(window)`; assert `onClose` called.
9. `it('closes after selecting a menu item')` — click "Edit"; assert `onClose` called.

`describe('ContextMenu — item order and copy (D-09, D-10, UI-SPEC)')`:
10. `it('renders exactly 5 root menu items in order: Edit, Re-triage, Move to category, Add to project, Delete')` — assert `screen.getAllByRole('menuitem')` length === 5 AND `textContent` matches order.
11. `it('Delete item uses text-red-400 (UI-SPEC Color table)')` — query by name "Delete" and assert `className` includes `text-red-400`.
12. `it('Move to category and Add to project items show → arrow glyph')` — assert the items' textContent includes `→`.

`describe('ContextMenu — action routing (CTX-03..CTX-07, D-20)')`:
13. `it('Edit menuitem calls onStartEdit and closes')` — `const onStartEdit = vi.fn()`; click Edit; assert `onStartEdit` called once AND `onClose` called.
14. `it('Re-triage menuitem calls onRetriage(thought.id) and closes (CTX-06, D-18)')` — assert `onRetriage` called with `42`.
15. `it('Move to category submenu shows 5 hardcoded categories in alphabetical order')` — trigger submenu; assert `['idea','project','reflection','task','therapy']` order in items.
16. `it('Move to category selection calls onMoveToCategory(id, category)')` — assert `onMoveToCategory` called with `(42, 'task')` after selecting Task.
17. `it('Current category is marked with checkmark or (current) suffix (D-14)')` — thought.category='idea'; assert Idea item has visible indicator (data-current="true" OR textContent includes "(current)" OR a checkmark svg/glyph).
18. `it('Re-selecting current category still calls onMoveToCategory (no-op but wired, D-14)')` — select current; assert callback fires.
19. `it('Add to project submenu shows projects sorted alphabetically (D-13)')` — assert Alpha before Beta.
20. `it('Add to project selection calls onAssignProject(id, projectId)')` — assert called with `(42, 1)`.
21. `it('Add to project empty state shows "No projects yet. Create one on the Projects tab." (UI-SPEC)')` — render with projects=[]; open submenu; assert copy.
22. `it('Delete menuitem calls onDelete(thought.id) and closes (CTX-03)')` — assert `onDelete` called with `42`.

`describe('ContextMenu — submenu layout (D-11, D-12)')`:
23. `it('desktop (openedVia=mouse): hovering Move to category opens right-side submenu (not in-place)')` — fire `mouseenter` on the item; assert submenu mounted alongside root menu (two `[role="menu"]` OR the category list is rendered as a sibling).
24. `it('mobile (openedVia=touch): tapping Move to category replaces root contents with ← Categories header')` — click the item; assert root menu items gone, `← Categories` present.
25. `it('mobile: ← Categories back affordance returns to root')` — after entering, click back; assert 5 root items return.

`describe('ContextMenu — a11y (D-21)')`:
26. `it('root menu has role="menu"')` — already covered #1; keep as explicit a11y assertion.
27. `it.skip('TODO Plan 04: ArrowDown moves focus to next menuitem')` — placeholder for Wave 3.
28. `it.skip('TODO Plan 04: Enter activates focused menuitem')` — placeholder for Wave 3.
29. `it.skip('TODO Plan 04: Escape returns focus to triggering row')` — placeholder for Wave 3.

Every `it(...)` body must compile against the ContextMenu prop contract in `<interfaces>` above. Use `vi.fn()` for every callback. Do NOT stub `<ContextMenu>` — the import failure IS the RED signal for Plan 02. Every `expect(...)` should reference a behavior Plan 02 will implement.
  </action>
  <verify>
    <automated>cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ContextMenu.test.tsx 2>&amp;1 | grep -E "Error|Cannot find module|FAIL|failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vigil-pwa/src/components/ContextMenu.test.tsx` exits 0
    - `grep -c "it(" vigil-pwa/src/components/ContextMenu.test.tsx` &gt;= 22
    - `grep -c "it.skip(" vigil-pwa/src/components/ContextMenu.test.tsx` &gt;= 3
    - `grep "ToastProvider" vigil-pwa/src/components/ContextMenu.test.tsx` exits 0
    - `grep "import ContextMenu from './ContextMenu'" vigil-pwa/src/components/ContextMenu.test.tsx` exits 0
    - `grep "openedVia" vigil-pwa/src/components/ContextMenu.test.tsx` exits 0 (mouse vs touch distinction present)
    - `grep "Move to category\|Add to project\|Re-triage" vigil-pwa/src/components/ContextMenu.test.tsx` exits 0 (UI-SPEC copy pinned)
    - `grep "No projects yet" vigil-pwa/src/components/ContextMenu.test.tsx` exits 0 (empty state copy pinned)
    - `cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ContextMenu.test.tsx` exits NON-ZERO (fail-by-default confirmed; module not yet implemented)
  </acceptance_criteria>
  <done>
    ContextMenu.test.tsx exists with >= 22 active `it(...)` cases and >= 3 `it.skip` placeholders. `npm test` reports failure because `./ContextMenu` cannot be resolved — exactly the Wave 0 RED state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold ToastHost.test.tsx + useToast.test.tsx</name>
  <files>vigil-pwa/src/components/ToastHost.test.tsx, vigil-pwa/src/hooks/useToast.test.tsx</files>
  <read_first>
    - vigil-pwa/src/hooks/useThoughts.test.tsx (fake timer + flushMicrotasks pattern)
    - vigil-pwa/src/hooks/GoogleStatusContext.tsx (existing React context pattern — useToast will match)
    - .planning/phases/101-context-menu/101-CONTEXT.md (D-15, D-16, D-17)
    - .planning/phases/101-context-menu/101-RESEARCH.md (Minimal useToast hook code example, Pitfall 7)
    - .planning/phases/101-context-menu/101-UI-SPEC.md (toast copy, role="status"/role="alert", 5s, position)
  </read_first>
  <action>
Create TWO fail-by-default test scaffolds.

**File 1 — `vigil-pwa/src/hooks/useToast.test.tsx`:**

Imports:
```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, act, renderHook } from '@testing-library/react'
import { ToastProvider, useToast } from './useToast'
```

`describe('useToast')`:
1. `it('throws outside ToastProvider')` — `renderHook(() => useToast())` without wrapper; assert throw matches /inside ToastProvider/.
2. `it('current is null initially')` — wrap in ToastProvider; assert `result.current.current === null`.
3. `it('showToast sets current with id + body')` — call `showToast({ body: 'hi', variant: 'default' })`; assert `result.current.current?.body === 'hi'`.
4. `it('auto-dismisses after 5000ms (D-15)')` — `vi.useFakeTimers()`; show toast; `act(() => vi.advanceTimersByTime(5_000))`; assert `current === null`.
5. `it('dismiss() clears current immediately')` — show, call dismiss, assert null.
6. `it('second showToast replaces first and fires first onExpire (D-16)')` — show A with `onExpire=vi.fn()`; show B; assert A's onExpire called; assert current is B.
7. `it('onAction fires when user clicks action, then dismisses')` — show with `onAction=vi.fn()`; note: this test asserts the store semantics (dismiss after onAction is the hook's responsibility via ToastHost's click handler — actually the hook just exposes dismiss, so this test verifies `onAction` is preserved in `current` and calling it is safe). Assert `result.current.current?.onAction` is the spy.
8. `it('variant="error" sets current.variant === "error"')` — assert variant round-trips.
9. `it('onExpire is NOT called on manual dismiss (only on timeout)')` — show with onExpire spy; dismiss; assert spy NOT called.

**File 2 — `vigil-pwa/src/components/ToastHost.test.tsx`:**

Imports:
```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, fireEvent, screen, act } from '@testing-library/react'
import ToastHost from './ToastHost'
import { ToastProvider, useToast } from '../hooks/useToast'

// Helper that exposes showToast to the test via a button.
function Harness({ onReady }: { onReady: (show: ReturnType&lt;typeof useToast&gt;['showToast']) =&gt; void }) {
  const { showToast } = useToast()
  onReady(showToast)
  return null
}
```

`describe('ToastHost')`:
1. `it('renders nothing when no toast is current')` — mount `<ToastProvider><ToastHost /></ToastProvider>`; assert `screen.queryByRole('status')` and `screen.queryByRole('alert')` both null.
2. `it('renders role="status" with aria-live="polite" for default variant (UI-SPEC)')` — trigger showToast({body:'Thought deleted.', variant:'default'}); assert `screen.getByRole('status').getAttribute('aria-live') === 'polite'`.
3. `it('renders role="alert" with aria-live="assertive" for error variant')` — variant='error'; assert alert role.
4. `it('renders body text')` — assert `screen.getByText('Thought deleted.')` exists.
5. `it('renders action button when action provided (text-teal-400 per UI-SPEC)')` — action='Undo'; assert button exists AND className includes `text-teal-400`.
6. `it('action click calls onAction and dismisses')` — `fireEvent.click(screen.getByRole('button', { name: 'Undo' }))`; assert onAction called; toast gone.
7. `it('does not render action button when action absent')` — no action in payload; assert no button.
8. `it('mounts at fixed bottom position (UI-SPEC Spacing xl + safe area)')` — assert element has `style.bottom` reflecting safe area OR className with `bottom-` token.

Every `describe`/`it` must be real — no empty bodies. Use vi.fn() for all callbacks. Since `ToastHost.tsx` and `useToast.tsx` do not yet exist, imports will fail — that is the intended Wave 0 RED state.
  </action>
  <verify>
    <automated>cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ToastHost.test.tsx src/hooks/useToast.test.tsx 2>&amp;1 | grep -E "Error|Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vigil-pwa/src/components/ToastHost.test.tsx` exits 0
    - `test -f vigil-pwa/src/hooks/useToast.test.tsx` exits 0
    - `grep -c "it(" vigil-pwa/src/hooks/useToast.test.tsx` &gt;= 8
    - `grep -c "it(" vigil-pwa/src/components/ToastHost.test.tsx` &gt;= 7
    - `grep "aria-live" vigil-pwa/src/components/ToastHost.test.tsx` exits 0
    - `grep "5_000\|5000" vigil-pwa/src/hooks/useToast.test.tsx` exits 0 (D-15 duration pinned)
    - `grep "onExpire" vigil-pwa/src/hooks/useToast.test.tsx` exits 0 (D-16 replace semantics pinned)
    - `cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ToastHost.test.tsx src/hooks/useToast.test.tsx` exits NON-ZERO (modules not yet implemented)
  </acceptance_criteria>
  <done>
    Two test files exist with real `it(...)` cases covering the `useToast` store semantics (auto-dismiss, replace-commits-first, variants) and the `ToastHost` DOM contract (roles, aria-live, action button).
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend ThoughtRow.test.tsx with menu trigger + interlock tests</name>
  <files>vigil-pwa/src/components/ThoughtRow.test.tsx</files>
  <read_first>
    - vigil-pwa/src/components/ThoughtRow.test.tsx (existing 7 tests — Phase 100 edit-lifecycle; do NOT break)
    - vigil-pwa/src/components/ThoughtRow.tsx (handleContentClick dispatch point)
    - .planning/phases/101-context-menu/101-CONTEXT.md (D-01..D-04, D-19)
    - .planning/phases/101-context-menu/101-RESEARCH.md (Pattern 2 long-press, Pattern 3 edit interlock, Pitfall 4 pointer events in tests)
  </read_first>
  <action>
Append a new `describe('ThoughtRow — context menu triggers (Phase 101)')` block to `vigil-pwa/src/components/ThoughtRow.test.tsx`. Do NOT modify the existing `describe('ThoughtRow — edit lifecycle events')` — Phase 100's 7 cases must keep passing untouched.

New helper at top of file (or inside new describe):
```tsx
function clockAdvance(ms: number) {
  act(() => { vi.advanceTimersByTime(ms) })
}
```

New `it(...)` cases — EVERY ONE must reference concrete prop values and grep-able DOM queries. Use `fireEvent.pointerDown(el, { pointerType: 'touch', clientX: 100, clientY: 100 })` per Pitfall 4, NOT `touchStart`:

`describe('ThoughtRow — context menu triggers (Phase 101)')`:

1. `it('right-click dispatches onContextMenu and opens menu portal (CTX-01, D-01)')` — render `<ThoughtRow thought={baseThought} onUpdate={vi.fn()} onDelete={vi.fn()} onMoveToCategory={vi.fn()} onAssignProject={vi.fn()} onRetriage={vi.fn()} />`; `fireEvent.contextMenu(getByText('hello'), { clientX: 250, clientY: 300 })`; assert `document.querySelector('[role="menu"]')` truthy.

2. `it('right-click calls e.preventDefault to suppress native menu (D-01)')` — `const ev = createEvent.contextMenu(...)`; `fireEvent(row, ev)`; assert `ev.defaultPrevented === true`.

3. `it('right-click does NOT open menu while isEditing (D-03)')` — `fireEvent.click(getByText('hello'))` to enter edit; `fireEvent.contextMenu(textarea, { clientX: 100, clientY: 100 })`; assert NO `[role="menu"]`.

4. `it('long-press ≥500ms on touch opens menu (CTX-02, D-02)')` — `vi.useFakeTimers()`; `fireEvent.pointerDown(row, { pointerType: 'touch', clientX: 100, clientY: 100 })`; `clockAdvance(500)`; assert `[role="menu"]` exists.

5. `it('long-press <500ms does NOT open menu (D-02)')` — pointerDown; `clockAdvance(400)`; `fireEvent.pointerUp(row)`; assert no menu.

6. `it('long-press cancels if pointer moves >10px (D-02)')` — pointerDown at (100,100); `fireEvent.pointerMove(row, { pointerType: 'touch', clientX: 115, clientY: 100 })`; `clockAdvance(600)`; assert no menu.

7. `it('long-press tolerates ≤10px movement (D-02)')` — pointerDown at (100,100); pointerMove to (108,103); `clockAdvance(500)`; assert menu opens.

8. `it('pointerType=mouse does NOT trigger long-press (D-04)')` — `fireEvent.pointerDown(row, { pointerType: 'mouse', clientX: 100, clientY: 100 })`; `clockAdvance(600)`; assert no menu from long-press path (rely on contextMenu event separately).

9. `it('long-press does NOT open menu while isEditing (D-03)')` — enter edit; pointerDown touch + advance 500ms; assert no menu.

10. `it('Edit menuitem routes through handleContentClick — dispatches vigil:edit-started (D-19 INTERLOCK)')` — the critical test. Spy `window.addEventListener('vigil:edit-started', spy)`. Open menu via right-click. Click `screen.getByRole('menuitem', { name: 'Edit' })`. Assert spy called EXACTLY ONCE with `{ id: 42 }`. This is the test that fails if an executor takes the shortcut of setting `isEditing=true` inline in ContextMenu instead of routing through `onStartEdit`.

11. `it('Re-triage menuitem calls the onRetriage prop with thought.id (CTX-06)')` — `const onRetriage = vi.fn()`; open menu; click Re-triage; assert called with 42.

12. `it('Delete menuitem calls the onDelete prop with thought.id (CTX-03 trigger side)')` — `const onDelete = vi.fn()`; open menu; click Delete; assert called with 42.

13. `it('Move to category picker eventually calls onMoveToCategory(id, cat) (CTX-04)')` — open menu, expand Move to category, click "Task"; assert `onMoveToCategory` called with `(42, 'task')`.

14. `it('Add to project picker eventually calls onAssignProject(id, projectId) (CTX-07)')` — render with projects prop OR mock useProjects; open menu, expand, click "Alpha"; assert `onAssignProject` called with `(42, 1)`.

15. `it('row element has -webkit-touch-callout:none applied (iOS Safari Pitfall 1)')` — query row element; assert `className` includes `[-webkit-touch-callout:none]` OR computed style contains the value.

16. `it('row element has touch-action: manipulation (Pitfall 9)')` — assert `className` includes `touch-manipulation`.

17. `it('only one menu can be open per row at a time — re-right-click replaces not stacks')` — fire contextMenu twice; assert `document.querySelectorAll('[role="menu"]').length === 1`.

Update `ThoughtRow` props in test fixtures to include all new callbacks (`onDelete`, `onMoveToCategory`, `onAssignProject`). These callbacks are OPTIONAL in the existing Phase 100 tests, so do NOT break them — the type extension in Plan 03 will make them optional.

**Important:** Tests 4-9 use `vi.useFakeTimers()` inside the `it(...)`; remember to `vi.useRealTimers()` in `afterEach` or finally-block, matching Phase 100's pattern.
  </action>
  <verify>
    <automated>cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ThoughtRow.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('ThoughtRow" vigil-pwa/src/components/ThoughtRow.test.tsx` &gt;= 2 (Phase 100 block + new Phase 101 block)
    - `grep "context menu triggers" vigil-pwa/src/components/ThoughtRow.test.tsx` exits 0
    - `grep -c "pointerType: 'touch'" vigil-pwa/src/components/ThoughtRow.test.tsx` &gt;= 5
    - `grep "vigil:edit-started" vigil-pwa/src/components/ThoughtRow.test.tsx` exits 0 (interlock test #10 present)
    - `grep "webkit-touch-callout" vigil-pwa/src/components/ThoughtRow.test.tsx` exits 0 (iOS Pitfall 1 pinned)
    - `grep "touch-manipulation\|touch-action" vigil-pwa/src/components/ThoughtRow.test.tsx` exits 0 (Pitfall 9 pinned)
    - `grep -c "it(" vigil-pwa/src/components/ThoughtRow.test.tsx` &gt;= 24 (7 existing + 17 new)
    - Existing Phase 100 tests still present: `grep "dispatches vigil:edit-started on content click" vigil-pwa/src/components/ThoughtRow.test.tsx` exits 0
    - `cd vigil-pwa &amp;&amp; npm run test -- --run src/components/ThoughtRow.test.tsx` runs — Phase 100 tests pass (7/7); Phase 101 tests fail (expected — implementation not yet in ThoughtRow)
  </acceptance_criteria>
  <done>
    ThoughtRow.test.tsx has two describe blocks. The new block contains 17 failing tests exercising D-01 through D-04, D-19, and iOS pitfalls. Phase 100's 7 tests remain untouched and still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Scaffold-only phase; no user-facing surface, no untrusted input paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-00-01 | Tampering | ThoughtRow.test.tsx existing tests | mitigate | Acceptance criterion asserts Phase 100's `dispatches vigil:edit-started on content click` string still grep-matches — existing Phase 100 contract cannot be accidentally deleted. |
| T-101-00-02 | Repudiation | Wave 0 RED state | accept | If tests "accidentally" pass (e.g., stub ContextMenu in test file), Plan 02 can't verify GREEN transition. Acceptance criterion asserts `npm test` exits non-zero at end of Wave 0 — if it exits zero, executor wrote a stub instead of a failing test and the RED contract is violated. |

</threat_model>

<verification>
Run `cd vigil-pwa && npm run test -- --run src/components/ContextMenu.test.tsx src/components/ToastHost.test.tsx src/hooks/useToast.test.tsx src/components/ThoughtRow.test.tsx` — expect FAILURES (Cannot find module './ContextMenu', './ToastHost', './useToast'). Phase 100 ThoughtRow tests (7 cases) must still pass inside the same file.

Run `cd vigil-pwa && grep -c "it.skip\|it(" src/components/ContextMenu.test.tsx src/components/ToastHost.test.tsx src/hooks/useToast.test.tsx src/components/ThoughtRow.test.tsx` — total >= 55 test cases across the 4 files.
</verification>

<success_criteria>
- All 4 test files exist at the paths in `files_modified`
- Vitest reports module-resolution failures for ContextMenu, ToastHost, useToast — proving fail-by-default
- Phase 100's 7 ThoughtRow edit-lifecycle tests still pass inside the extended ThoughtRow.test.tsx
- Every Phase 101 behavior from CTX-01..CTX-07 + D-01..D-21 has at least one named test case
- D-19 interlock test (Edit menu dispatches `vigil:edit-started`) is present — the trap-test that catches shortcut implementations
</success_criteria>

<output>
After completion, create `.planning/phases/101-context-menu/101-00-SUMMARY.md` recording:
- Test file paths + `it(...)` counts
- Phase 100 test regression check (must be 7/7 pass)
- Exact failing-test output showing Cannot-find-module errors (proves RED state)
</output>
