---
phase: 127.5-g2-input-gesture-audit
plan: 01
subsystem: g2-input
tags: [audit, g2, gesture, click_event, even_sdk, protobuf-zero-omission, nullish-coalesce, phase-133-prep]

# Dependency graph
requires:
  - phase: 127-pre-spike-guardrails
    provides: GUARD-01..04 (audit-driven adjacent patches now safe behind redaction + budget guards)
  - phase: 124-claude-code-companion (D-08)
    provides: Companion HUD + DOUBLE_CLICK contract that the audit re-examined
provides:
  - REACTIVATE verdict for single-press G2 input plumbing (AUDIT-G2-INPUT-01)
  - Cited file:line evidence (24 citations) tying the failure mode to EVEN-SKILLS.md:38 protobuf-zero-omission
  - Phase 133 gesture-grammar direction: single-press for G2-ACTION-01/02 select+confirm; single-press cycles + DOUBLE_CLICK confirms for G2-REPLY-02/03
  - Deferred-items entry queuing the ≤10-line plumbing patch + UX wiring + regression test for Phase 133 plan-01
affects: [phase-133-g2-closeout, phase-128b-g2-reply-spike, work-orders.ts redesign, task-detail.ts dead-code resurrection]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Audit-first phase: 30-min code review with binary verdict + cited evidence shapes downstream gesture grammar before plan-authoring"
    - "Defer-patch-when-UX-undecided: plumbing fix without semantic decision ships behavior-neutral churn; better to bundle patch with UX wiring in the consuming phase"

key-files:
  created:
    - .planning/phases/127.5-g2-input-gesture-audit/127.5-AUDIT.md
  modified: []

key-decisions:
  - "Verdict: REACTIVATE — single-press is recoverable on G2 hardware via ≤10-line ?? 0 nullish-coalesce patch in main.ts:66-70/224/233/239/246-247"
  - "Patch deferred to Phase 133 implementer — adding CLICK_EVENT to NAV_EVENTS without handleNavEvent UX semantics ships plumbing without behavior change"
  - "Phase 133 G2-ACTION-01/02 uses single-press select + second single-press confirm (natural Even SDK listEvent.currentSelectItemIndex flow per EVEN-SKILLS.md:165)"
  - "Phase 133 G2-REPLY-02/03 uses single-press cycle prefabs + DOUBLE_CLICK confirm (preserves swipe-up/down for inter-screen navigation)"

patterns-established:
  - "Truthy-guard-on-eventType bug class: `event.X?.eventType && NAV_EVENTS.has(...)` short-circuits both undefined (protobuf-zero-omission) and 0 (CLICK_EVENT). Use `(event.X?.eventType ?? 0)` instead."
  - "Strict ===0 check on protobuf-omitted-zero field is the same bug class observed in Phase 45 work-orders.ts list-item taps (project_g2_tap_expand_broken). Same failure mode, same fix."

requirements-completed: [AUDIT-G2-INPUT-01]

# Metrics
duration: ~25min
completed: 2026-05-12
---

# Phase 127.5 Plan 01: G2 Input Gesture Audit Summary

**REACTIVATE verdict landed: single-press on G2 hardware is recoverable via a ≤10-line `?? 0` nullish-coalesce patch in main.ts; patch deferred to Phase 133 plan-01 where it bundles with UX semantics. Phase 133 G2-ACTION uses single-press select+confirm; G2-REPLY uses single-press cycle + DOUBLE_CLICK confirm.**

## Performance

- **Duration:** ~25 min (audit + write + commit; well under the 30-min target)
- **Started:** 2026-05-12 (local MDT)
- **Completed:** 2026-05-12
- **Tasks:** 1 of 1 (single-task audit plan)
- **Files modified:** 1 created (`127.5-AUDIT.md`), 0 source changes (patch deferred)

## Accomplishments

- **Binary verdict landed:** REACTIVATE (the `?? 0` nullish-coalesce fix is the cause; single-press is shipable post-patch).
- **24 file:line citations** across `main.ts`, `companion.ts`, `navigation.ts`, `work-orders.ts`, and EVEN-SKILLS.md — every code-behavior claim is grounded in source, not opinion.
- **Four-branch root-cause trace** of `vigil-g2-plugin/src/main.ts:221-260` identifying each guard expression that drops single-press on hardware:
  - `main.ts:66-70` — NAV_EVENTS missing CLICK_EVENT
  - `main.ts:224` — strict `=== 0` rejects `undefined`
  - `main.ts:233/239/247` — truthy `&&` rejects both `0` and `undefined`
- **Phase 133 gesture-grammar decided:**
  - G2-ACTION-01/02 (WORK_ORDERS, REMINDERS mark complete) → single-press select + second single-press confirm
  - G2-REPLY-02/03 (Companion reply mode) → single-press cycles 5 prefabs + DOUBLE_CLICK confirms
- **Patch deferred with rationale** to prevent plumbing-without-UX regression: Phase 133 plan-01 should land the ≤10-line `main.ts` patch + the `handleNavEvent` UX wiring + a regression test in a single 2-task plan.

## Verdict (quoted verbatim from 127.5-AUDIT.md)

> **Verdict:** REACTIVATE
>
> Single-press on G2 hardware is currently dropped by FOUR independent guard bugs in `vigil-g2-plugin/src/main.ts:221-260`, every one of which matches the EVEN-SKILLS.md:38 protobuf-zero-omission prediction verbatim. The plumbing is NOT correct; the `?? 0` nullish-coalesce guard is missing on every branch that would route a single-press. Commit `bfa3e0b3` ("single-press reactivated") only modified `.planning/PROJECT.md` and `.planning/research/EVEN-SKILLS.md` — it did NOT touch `vigil-g2-plugin/src/`, so the on-hardware code path is unchanged from Phase 124 D-08's pre-research state. Phase 133 G2-ACTION-01/02 and G2-REPLY-02/03 CAN ship on single-press once a ≤10-line patch lands in `main.ts` + `navigation.ts`. The patch itself is deferred to the Phase 133 implementer because the UX semantics of "what does CLICK_EVENT mean on each screen" is the gesture-grammar decision Phase 133 owns, not this audit.

## Phase 133 Gesture-Grammar Direction

See `127.5-AUDIT.md` § "Phase 133 gesture-grammar direction" for full requirement-level recommendations. Summary:

- **G2-ACTION-01 (WORK_ORDERS):** single-press select + second single-press confirm
- **G2-ACTION-02 (REMINDERS):** same as G2-ACTION-01
- **G2-REPLY-02/03 (Companion reply mode):** single-press cycle prefabs + DOUBLE_CLICK confirm (preserves swipe-up/down for inter-screen nav)

## Task Commits

1. **Task 1: Trace event flow + write verdict to 127.5-AUDIT.md** — `8fd88819` (docs)

**No patch landed** — audit-only. Patch deferred to Phase 133 implementer (see "Deferred Items" below). Per the plan's optional patch path: "the audit verdict markdown is still the primary ship."

## Files Created/Modified

- `.planning/phases/127.5-g2-input-gesture-audit/127.5-AUDIT.md` — REACTIVATE verdict with 24 file:line citations, code-level trace, behavioral trace, counter-evidence triangulation, Phase 133 gesture-grammar block, deferred-items queue.

## Decisions Made

- **Verdict shape:** REACTIVATE (the optional binary). Evidence converges: four broken guards, all matching EVEN-SKILLS.md:38, plus a counter-evidence triangulation against the Phase 45 work-orders.ts retro (`project_g2_tap_expand_broken` memory) showing the same failure mode on a different code path.
- **Patch deferral:** Even though the ≤10-line diff is well within the optional-patch budget, landing the plumbing without UX semantics in `navigation.ts:handleNavEvent` would ship behavior-neutral churn (single-press fires → falls through `default: return` → user sees no improvement). Bundling the patch with the UX call in Phase 133 plan-01 is the correct factoring.
- **Bypass D-08 docstring update for now:** `companion.ts:22-26` claims "single-tap is sim-only on G2" — that claim is now demonstrably wrong but mutating the docstring without landing the underlying patch would create a doc-vs-code drift. Queued as deferred-item 2 for Phase 133.

## Deviations from Plan

None — plan executed exactly as written. The plan explicitly authorized the "verdict-only, no patch" path; the auditor took it for the rationale documented in "Decisions Made".

## Issues Encountered

None. The audit targets (`main.ts` 271 lines, `companion.ts` 460 lines, `navigation.ts` 257 lines, `types.ts` 89 lines, `work-orders.ts` 112 lines, EVEN-SKILLS.md:1-180) were all in scope per the plan's `<read_first>` list, and the interfaces block in the plan front-loaded the load-bearing details (NAV_EVENTS membership, four-branch routing, OsEventTypeList numeric table) so the 30-min budget was comfortable.

## User Setup Required

None — this is a docs-only ship. No external services touched.

## Next Phase Readiness

**Phase 128a (VOICE-01 PCM spike):** Independent of this audit. Ready.

**Phase 128b (G2-REPLY-01 write-back spike):** Independent of this audit; the audit's G2-REPLY-02/03 gesture grammar is downstream of 128b's PASS/DEGRADE verdict. If 128b PASS, the gesture grammar this audit recommends (single-press cycle + DOUBLE_CLICK confirm) applies as-is. Ready.

**Phase 133 (G2 closeout bundle):** Now has its gesture grammar decided. Plan-authoring can proceed. The first plan in Phase 133 should be a 2-task plan: (1) land the ≤10-line plumbing patch in `vigil-g2-plugin/src/main.ts:66-70/224/233/239/246-247` + the matching CLICK_EVENT branch in `navigation.ts:handleNavEvent`, (2) wire the G2-ACTION-01/02 select+confirm state machine + add the regression test for `eventType ?? 0`.

## Deferred Items

Forwarded from `127.5-AUDIT.md` § "Deferred items":

1. **Plumbing patch + regression test** → Phase 133 plan-01 task-1. ≤10-line diff in `main.ts`; new `CLICK_EVENT` branch in `navigation.ts:handleNavEvent`; unit test asserting `{ textEvent: { eventType: undefined } }` reaches `handleNavEvent(CLICK_EVENT, bridge)`.
2. **`companion.ts:22-26` D-08 docstring revision** → Phase 133 same plan. Update once the plumbing patch makes the "sim-only on G2" claim false.
3. **Hardware retest** → Phase 133 hardware UAT close-out. 5-min physical test: single-press on Companion text container → expect handler invocation log; single-press on work-orders list container → expect new G2-ACTION-01 handler. This closes the SEED-011 "single-tap deferred" loop opened at Phase 124 D-08.

## Self-Check: PASSED

Automated verification per the plan's `<verify><automated>` block:

- `test -f 127.5-AUDIT.md` → PASS
- `grep -E "^\*\*Verdict:\*\* (REACTIVATE|CONFIRM-DEFER)"` → matched `**Verdict:** REACTIVATE`
- `grep -cE '(companion|main|navigation|types|work-orders)\.ts:[0-9]+'` → 24 (≥ 2 required)
- `grep -c 'G2-ACTION-01|G2-ACTION-02|G2-REPLY-02'` → 5 (≥ 3 required)
- `grep -ciE 'should work|looks good|seems fine|probably|might be'` → 0
- `grep -ciE 'clean|elegant'` → 0
- Commit `8fd88819` exists in `git log --oneline` → confirmed

---
*Phase: 127.5-g2-input-gesture-audit*
*Completed: 2026-05-12*
