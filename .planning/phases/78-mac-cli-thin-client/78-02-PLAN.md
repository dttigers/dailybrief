---
phase: 78-mac-cli-thin-client
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - Sources/DailyBrief/PDF/PDFGenerator.swift
  - Sources/DailyBrief/PDF/PDFStyles.swift
  - Sources/DailyBrief/PDF/PageOneRenderer.swift
  - Sources/DailyBrief/PDF/PageTwoRenderer.swift
  - Sources/DailyBrief/PDF/PageThreeRenderer.swift
  - Sources/DailyBrief/Services/SportsService.swift
  - Sources/DailyBrief/Services/ESPNSportsService.swift
  - Sources/DailyBrief/Services/RemindersService.swift
  - Sources/DailyBrief/Services/EmailService.swift
  - Sources/DailyBrief/Services/AIService.swift
  - Sources/DailyBrief/Services/WorkOrderPrioritizer.swift
  - Package.swift
autonomous: true
requirements:
  - CLI-03

must_haves:
  truths:
    - "The Sources/DailyBrief/PDF/ directory does not exist"
    - "No generate-only service files remain in Sources/DailyBrief/Services/"
    - "Package.swift no longer links CoreGraphics, CoreText, or EventKit for the DailyBrief target"
    - "swift build succeeds for all three targets (DailyBrief, DailyBriefMonitor, JarvisCore)"
  artifacts:
    - path: "Package.swift"
      provides: "Clean DailyBrief target without CoreGraphics/CoreText/EventKit linkerSettings"
      contains: "executableTarget"
  key_links:
    - from: "Package.swift"
      to: "DailyBrief target"
      via: "target definition"
      pattern: "name: \"DailyBrief\""
---

<objective>
Delete all CoreGraphics PDF rendering code and generate-only service files from the Mac CLI, then clean up Package.swift framework links.

Purpose: CLI-03 requires removing all dead CoreGraphics rendering code. After Plan 01 rewrites Generate as a thin client, these files have zero callers in the DailyBrief target.
Output: A clean codebase with no dead PDF rendering or generate-only service code. swift build passes for all targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-mac-cli-thin-client/78-CONTEXT.md
@.planning/phases/78-mac-cli-thin-client/78-RESEARCH.md
@.planning/phases/78-mac-cli-thin-client/78-01-SUMMARY.md

<interfaces>
<!-- Files to DELETE (verified generate-only by research grep analysis) -->

Sources/DailyBrief/PDF/ (entire directory — 5 files):
- PDFGenerator.swift — CoreGraphics PDF renderer
- PDFStyles.swift — PDFLayout + style constants
- PageOneRenderer.swift — Page 1 layout
- PageTwoRenderer.swift — Page 2 layout
- PageThreeRenderer.swift — Page 3 layout

Sources/DailyBrief/Services/ (6 generate-only files):
- SportsService.swift — MLB fetch, only used in Generate
- ESPNSportsService.swift — NFL/NBA/NHL fetch, only used in Generate
- RemindersService.swift — Apple Reminders fetch, only used in Generate
- EmailService.swift — IMAP work order fetch, only used in Generate
- AIService.swift — APIAIProvider (affirmation), only used in Generate
- WorkOrderPrioritizer.swift — APIWorkOrderPrioritizer, only used in Generate

<!-- Files to KEEP (verified shared) -->
Sources/DailyBrief/Services/CompletionStore.swift — used by Complete, Uncomplete, ListCompleted
Sources/DailyBrief/Utilities/PrintService.swift — used by Generate (thin client) and History reprint
Sources/DailyBrief/Utilities/Logger.swift — used by all commands

<!-- Package.swift current DailyBrief linkerSettings (lines 24-28) -->
```swift
linkerSettings: [
    .linkedFramework("EventKit"),
    .linkedFramework("CoreGraphics"),
    .linkedFramework("CoreText"),
]
```
These three frameworks must be removed entirely. No linkerSettings needed after removal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code files and clean Package.swift</name>
  <files>
    Sources/DailyBrief/PDF/PDFGenerator.swift
    Sources/DailyBrief/PDF/PDFStyles.swift
    Sources/DailyBrief/PDF/PageOneRenderer.swift
    Sources/DailyBrief/PDF/PageTwoRenderer.swift
    Sources/DailyBrief/PDF/PageThreeRenderer.swift
    Sources/DailyBrief/Services/SportsService.swift
    Sources/DailyBrief/Services/ESPNSportsService.swift
    Sources/DailyBrief/Services/RemindersService.swift
    Sources/DailyBrief/Services/EmailService.swift
    Sources/DailyBrief/Services/AIService.swift
    Sources/DailyBrief/Services/WorkOrderPrioritizer.swift
    Package.swift
  </files>
  <read_first>
    - Package.swift
    - Sources/DailyBrief/DailyBrief.swift
  </read_first>
  <action>
**Step 1: Delete the entire PDF directory (D-02):**
```bash
rm -rf Sources/DailyBrief/PDF/
```
This removes all 5 files: PDFGenerator.swift, PDFStyles.swift, PageOneRenderer.swift, PageTwoRenderer.swift, PageThreeRenderer.swift.

**Step 2: Delete generate-only service files (D-03):**
```bash
rm Sources/DailyBrief/Services/SportsService.swift
rm Sources/DailyBrief/Services/ESPNSportsService.swift
rm Sources/DailyBrief/Services/RemindersService.swift
rm Sources/DailyBrief/Services/EmailService.swift
rm Sources/DailyBrief/Services/AIService.swift
rm Sources/DailyBrief/Services/WorkOrderPrioritizer.swift
```

**DO NOT delete:**
- `Sources/DailyBrief/Services/CompletionStore.swift` — used by Complete (line 722), Uncomplete (line 763), ListCompleted (line 795) subcommands
- `Sources/DailyBrief/Utilities/PrintService.swift` — used by thin client Generate and History reprint
- `Sources/DailyBrief/Utilities/Logger.swift` — used by all commands
- Any files in `Sources/JarvisCore/` — shared library used by Monitor, models like DailyBriefData, SportData, BriefSnapshot, BriefRecord must stay

**Step 3: Clean Package.swift framework links:**

Edit Package.swift. Remove the entire `linkerSettings` block from the DailyBrief executableTarget (lines 24-28). The target should look like:

```swift
.executableTarget(
    name: "DailyBrief",
    dependencies: [
        .product(name: "ArgumentParser", package: "swift-argument-parser"),
        "JarvisCore",
    ]
),
```

No `linkerSettings` key at all — CoreGraphics, CoreText, and EventKit are no longer needed by any code in the DailyBrief target.

**DO NOT modify** the DailyBriefMonitor target — it still needs its SwiftUI and AppKit linkerSettings.

**Step 4: Verify no orphaned references remain in DailyBrief.swift.**
After Plan 01 rewrote Generate.run(), there should be no references to:
- `SportsService`, `ESPNSportsService`, `RemindersService`, `EmailService`
- `APIAIProvider`, `APIWorkOrderPrioritizer`, `APIThoughtStore`
- `APIInsightService`, `APITherapyPatternService`, `APITherapyPrepService`
- `PDFGenerator`, `PDFLayout`, `PDFStyles`
- `GoogleCalendarService` (was in Generate.run() only)

If any stray references remain in DailyBrief.swift (e.g., unused imports), remove them.

**Step 5: Full build verification:**
```bash
swift build
```
This must succeed for all targets: DailyBrief, DailyBriefMonitor, JarvisCore.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Directory `Sources/DailyBrief/PDF/` does not exist (`ls Sources/DailyBrief/PDF/ 2>&1` returns error)
    - `Sources/DailyBrief/Services/` contains only `CompletionStore.swift` (`ls Sources/DailyBrief/Services/` shows 1 file)
    - Package.swift DailyBrief target does NOT contain `linkedFramework("CoreGraphics")`
    - Package.swift DailyBrief target does NOT contain `linkedFramework("CoreText")`
    - Package.swift DailyBrief target does NOT contain `linkedFramework("EventKit")`
    - Package.swift DailyBriefMonitor target still contains `linkedFramework("SwiftUI")` and `linkedFramework("AppKit")`
    - `swift build` exits with code 0 (all targets compile)
    - `Sources/DailyBrief/Utilities/PrintService.swift` still exists
    - `Sources/DailyBrief/Utilities/Logger.swift` still exists
    - `Sources/DailyBrief/Services/CompletionStore.swift` still exists
  </acceptance_criteria>
  <done>All CoreGraphics PDF rendering code and generate-only services deleted, Package.swift cleaned of framework links, swift build succeeds for all targets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is pure code deletion — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | Denial of Service | Overzealous deletion | mitigate | Research verified every deletion target has no callers outside Generate via grep analysis. CompletionStore, PrintService, Logger explicitly preserved. JarvisCore models untouched. |
</threat_model>

<verification>
- `ls Sources/DailyBrief/PDF/ 2>&1` returns "No such file or directory"
- `ls Sources/DailyBrief/Services/` shows only CompletionStore.swift
- `grep -c "CoreGraphics\|CoreText\|EventKit" Package.swift` returns 0
- `swift build` exits 0
- `swift build --target DailyBriefMonitor` exits 0 (Monitor not broken)
</verification>

<success_criteria>
- Entire Sources/DailyBrief/PDF/ directory is gone (D-02, CLI-03)
- All 6 generate-only service files are gone (D-03, CLI-03)
- Package.swift has no CoreGraphics/CoreText/EventKit for DailyBrief target
- CompletionStore.swift preserved (Complete/Uncomplete/ListCompleted still work)
- PrintService.swift preserved (thin client and History still print)
- Logger.swift preserved (all commands still log)
- JarvisCore models preserved (Monitor/Dashboard unbroken)
- `swift build` succeeds for all targets
</success_criteria>

<output>
After completion, create `.planning/phases/78-mac-cli-thin-client/78-02-SUMMARY.md`
</output>
