---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 01
subsystem: infra
tags: [codesign, entitlements, icloud, ubiquity, gitignore, shell, probe]

# Dependency graph
requires: []
provides:
  - "Branch 999.1-ubiquity-dry-run checked out from main — all phase work isolated here"
  - "probes/verify-ubiquity.sh — UBIQ-01/02/04/05 automated checker with --static/--runtime/--full modes"
  - "Entitlements/*.provisionprofile gitignore entry — prevents accidental profile commit"
  - "Entitlements/embedded.provisionprofile.example.txt — portal-flow doc for fresh clones"
affects:
  - 999.1-02
  - 999.1-03
  - 999.1-04
  - 999.1-05
  - 999.1-06

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "probes/ directory under .planning/phases/ for phase-scoped probe scripts"
    - "verify-ubiquity.sh: set -euo pipefail + red/green/info helpers + FAIL counter + MODE dispatch — structural copy of verify-phase-114.sh"
    - "Final-success literal: 'verify-ubiquity: all checks passed' — grep target for Plans 04+06"
    - "amfid kill predicate: grep -E '(Killed|-67050|Invalid Code Signature)' — NOT bare bundle-ID grep"

key-files:
  created:
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh
    - Entitlements/embedded.provisionprofile.example.txt
  modified:
    - .gitignore

key-decisions:
  - "Branch 999.1-ubiquity-dry-run created from main; all phase 999.1 work stays on this branch until UBIQ-01..05 all green"
  - "REPO_ROOT calculated as 5 levels up from SCRIPT_DIR (probes/ is 5 deep under repo root)"
  - "amfid check uses FAIL=1 on non-empty kill output — NOT grep exit code — to avoid set -e conflict"
  - "Rollback rule documented in placeholder: git checkout main && ./Scripts/install.sh"

patterns-established:
  - "Phase-scoped probe scripts live under .planning/phases/<phase>/probes/ — first use of this convention in this repo"
  - "Final-success print is always 'verify-<phase>: all checks passed' — consumed by grep in downstream plans"

requirements-completed: [UBIQ-01, UBIQ-02, UBIQ-04, UBIQ-05]

# Metrics
duration: 12min
completed: 2026-05-19
---

# Phase 999.1 Plan 01: Safety Net Scaffold Summary

**verify-ubiquity.sh probe runner (UBIQ-01/02/04/05), Entitlements gitignore, and 999.1-ubiquity-dry-run branch — all Phase 999.1 safety infrastructure before any signing changes**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-19T18:09Z
- **Completed:** 2026-05-19T18:21Z
- **Tasks:** 4
- **Files modified:** 3

## Accomplishments
- Created and checked out branch `999.1-ubiquity-dry-run` from `main` — all phase work isolated here per Phase 58 lesson
- Authored `probes/verify-ubiquity.sh` with 6 check functions covering UBIQ-01/02/04/05, structural analog of `Scripts/verify-phase-114.sh`
- Added `Entitlements/*.provisionprofile` to `.gitignore` (verified via `git check-ignore`) to prevent Team ID disclosure
- Created `Entitlements/embedded.provisionprofile.example.txt` documenting the developer.apple.com portal flow for fresh clones

## Task Commits

Tasks 2+3+4 land in a single commit (per plan instructions — Task 1 does not commit):

1. **Task 1: Create 999.1-ubiquity-dry-run branch + probes/ scaffold** - no commit (per plan)
2. **Task 2: Author probes/verify-ubiquity.sh** - `52b9356` (chore)
3. **Task 3: Add gitignore + placeholder doc** - `52b9356` (chore)
4. **Task 4: Sanity-check ignore rule + commit** - `52b9356` (chore)

**Plan commit:** `52b9356` — chore(999.1): scaffold dry-run branch, probe runner, profile gitignore

## Files Created/Modified
- `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh` - Probe runner: UBIQ-01/02/04/05 checks, --static/--runtime/--full modes, exits non-zero today (no .app installed yet)
- `Entitlements/embedded.provisionprofile.example.txt` - Portal-flow documentation for the gitignored provisioning profile
- `.gitignore` - Added `Entitlements/*.provisionprofile` glob ignore with Phase 999.1 comment header

## Decisions Made
- Task 1 explicitly does not commit — the single commit in Task 4 covers all three file changes as planned
- `REPO_ROOT` computed as 5 levels up from `probes/` SCRIPT_DIR (probes/ path: `.planning/phases/<phase>/probes/`)
- amfid kill check uses `|| true` after grep chain + `[[ -n "$kills" ]]` trigger — avoids `set -e` on legitimate no-match; does NOT use `grep -qv` (inverted-logic bug noted in plan)
- Container ID uses literal `iCloud.com.jamesonmorrill.dailybriefmonitor` — no `$(TeamIdentifierPrefix)` macro (RESEARCH Pitfall 2)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None — this plan creates no external service dependencies. Plans 02 (portal work) and 03+ (code edits) depend on the artifacts created here.

## Next Phase Readiness
- Plan 02 (operator portal: App ID + iCloud container + profile download) can start immediately
- Plan 03 (code/config edits) can start immediately
- `bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --static` gives structured FAIL output today; will give PASS after Plan 04 completes signing
- Rollback rule baked into placeholder doc and PLAN.md must_haves.truths: `git checkout main && ./Scripts/install.sh`

## Self-Check

- [x] `probes/verify-ubiquity.sh` exists and is executable
- [x] `--static` runs without crashing, exits 1 (no .app — expected)
- [x] `--runtime` runs without crashing, exits 1 (no LaunchAgent — expected)
- [x] `git check-ignore -v Entitlements/embedded.provisionprofile` resolves to `.gitignore:40:Entitlements/*.provisionprofile`
- [x] `Entitlements/embedded.provisionprofile.example.txt` references `developer.apple.com` and `iCloud.com.jamesonmorrill.dailybriefmonitor`
- [x] Commit `52b9356` on branch `999.1-ubiquity-dry-run`
- [x] `verify-ubiquity: all checks passed` literal present in script
- [x] `(Killed|-67050|Invalid Code Signature)` amfid predicate present in script

## Self-Check: PASSED

---
*Phase: 999.1-restore-ubiquity-entitlement-for-icloud-download*
*Completed: 2026-05-19*
