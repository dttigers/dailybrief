---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 02
date: 2026-05-19
operator: jamesonmorrill1@gmail.com
status: portal-complete; rollback-rehearsal pending (Task 3)
---

# Phase 999.1 Plan 02 — Portal UAT Evidence Log

## Cross-Machine Architecture Note

This phase runs across two machines:
- **Linux** (`/home/morrillboss/dev/dailybrief`) — GSD planner + agent execution (this file lives here)
- **Mac** (`/Users/jamesonmorrill/Desktop/Local AI/dailybrief`) — build / sign / test / portal work
- **Shared remote:** `git@github.com:dttigers/dailybrief.git` — sync path between machines

The Mac repo was switched to `999.1-ubiquity-dry-run` and confirmed up-to-date with origin before
portal work began. Screenshots referenced below are on the Mac filesystem and are NOT copied into
this repo (screenshots contain no secrets, but the Mac-side file paths are reference-only from Linux).

---

## Portal Steps Executed

| Step | Description | Status |
|------|-------------|--------|
| 1 | Sign in to developer.apple.com with Apple ID owning team `5H57ADQS8G` | PASS |
| 2 | Register iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor` | PASS |
| 3 | Edit App ID `com.jamesonmorrill.dailybriefmonitor`: enable iCloud capability ("Compatible with Xcode 5" — NOT "Include CloudKit support"); assign container `iCloud.com.jamesonmorrill.dailybriefmonitor` | PASS |
| 4 | Generate Developer ID Provisioning Profile, select App ID `com.jamesonmorrill.dailybriefmonitor` + Developer ID Application cert; name `DailyBriefMonitor Developer ID Profile`; download | PASS |
| 5 | Move downloaded `.provisionprofile` to `Entitlements/embedded.provisionprofile` in local Mac repo | PASS |
| 6 | Decode profile via `security cms -D -i`; confirm `ubiquity-container-identifiers` present in Entitlements dict | PASS |
| 7 | Confirm `git check-ignore -v Entitlements/embedded.provisionprofile` exits 0 (file is gitignored, NOT untracked) | PASS |

---

## Container Identifier Used

Preferred container: `iCloud.com.jamesonmorrill.dailybriefmonitor`

The preferred container was available globally and was registered successfully. No fallback was needed.

---

## Profile Decode Output

Decoded via `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p -` on the Mac.

### File stat

```
ls -la Entitlements/embedded.provisionprofile:
-rw-r--r--@ 1 jamesonmorrill  staff  12790 May 19 12:28 Entitlements/embedded.provisionprofile
```

### ubiquity-container-identifiers entitlement

```
security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep -A 3 ubiquity-container-identifiers:
    "com.apple.developer.ubiquity-container-identifiers" => [
      0 => "5H57ADQS8G.*"
    ]
    "com.apple.developer.ubiquity-kvstore-identifier" => "5H57ADQS8G.*"
```

**Developer-ID wildcard shape:** The profile emits `5H57ADQS8G.*` (team-scoped wildcard), which is the
correct and expected form for Developer ID profiles. Per RESEARCH.md: "Developer ID emits
team-scoped wildcards; amfid authorizes any container in the team." The literal
`iCloud.com.jamesonmorrill.dailybriefmonitor` in Plan 03's entitlements XML will be authorized
because it is within team `5H57ADQS8G`.

### Expiration date

```
security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep ExpirationDate:
  "ExpirationDate" => 2044-05-14 18:28:03 +0000
```

18-year Developer ID validity — expected per RESEARCH.md ("Standard Stack — Supporting").

### Key fields confirmed

- `Name` — DailyBriefMonitor Developer ID Profile (per portal step 4)
- `AppIDName` — confirmed `com.jamesonmorrill.dailybriefmonitor`
- `TeamIdentifier` — `5H57ADQS8G` (not a secret; visible in decoded output)
- `com.apple.developer.ubiquity-container-identifiers` — `["5H57ADQS8G.*"]` (Developer-ID wildcard)
- `com.apple.developer.ubiquity-kvstore-identifier` — `"5H57ADQS8G.*"`
- `ExpirationDate` — 2044-05-14 (18-year Developer ID validity)

---

## Screenshots

Screenshots taken on the Mac during portal work. These are referenced-only (not copied into the
Linux repo — the planner agent on Linux cannot access Mac filesystem files).

| Screenshot | Mac path | Contents |
|------------|----------|----------|
| App ID edit page | `~/Screenshots/Screenshot 2026-05-19 at 12.24.23 PM.png` | App ID `com.jamesonmorrill.dailybriefmonitor` edit page — iCloud checkbox ON, "Compatible with Xcode 5" radio selected, container `iCloud.com.jamesonmorrill.dailybriefmonitor` assigned |
| portal Identifiers page | (on Mac — filename unrecorded) | Container registered in Identifiers list |
| Profiles list | (on Mac — filename unrecorded) | Developer ID profile `DailyBriefMonitor Developer ID Profile` present |

The `uat-evidence/` subdirectory in this phase is reserved for any screenshots committed
to the repo in a future session. None are present yet because the screenshots have not been
transferred to Linux.

---

## gitignore Confirmation

```
git check-ignore -v Entitlements/embedded.provisionprofile:
.gitignore:40:Entitlements/*.provisionprofile   Entitlements/embedded.provisionprofile
```

Exit code 0 — the file is gitignored by Plan 01's rule at `.gitignore:40`.
`git ls-files Entitlements/embedded.provisionprofile` returns empty (file is NOT tracked by git).

---

## Planner-Handoff Status

**Preferred container — Plan 03 may auto-run.**

Container `iCloud.com.jamesonmorrill.dailybriefmonitor` was registered successfully (preferred
container was NOT globally taken). Open Question 4 did not fire. No fallback literal change is
needed. Plan 03's hardcoded literal `iCloud.com.jamesonmorrill.dailybriefmonitor` is correct as
written and does not need planner propagation.

---

## Rollback Rehearsal

_Pending — Task 3 not yet executed. This section will be appended after the operator completes
the rollback rehearsal checkpoint (Task 3: `git checkout main && ./Scripts/install.sh` on the Mac,
confirming brctl-only build launches cleanly, then `git checkout 999.1-ubiquity-dry-run`)._

---

## Sign-Off

Operator signed off via resume signal at 2026-05-19T18:37:21+00:00.

Resume signal provided: "approved — preferred-container OK"

Portal work complete. Profile is present on Mac at `Entitlements/embedded.provisionprofile`,
gitignored, structurally valid (Developer ID wildcard entitlement confirmed).
Rollback rehearsal is the next gate before any code changes in Plan 03.
