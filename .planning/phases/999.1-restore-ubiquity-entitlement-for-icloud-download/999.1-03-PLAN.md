---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 03
type: execute
wave: 3
depends_on: [999.1-02]
files_modified:
  - Entitlements/DailyBriefMonitor.entitlements
  - Entitlements/DailyBriefMonitor-bare.entitlements
  - Scripts/install.sh
  - Sources/DailyBriefMonitor/FolderWatcherService.swift
autonomous: true
requirements: [UBIQ-01, UBIQ-02, UBIQ-04, UBIQ-05]
must_haves:
  truths:
    - "`Entitlements/DailyBriefMonitor.entitlements` lists `com.apple.developer.ubiquity-container-identifiers` with the literal array entry `iCloud.com.jamesonmorrill.dailybriefmonitor` (NOT `$(TeamIdentifierPrefix)` — per RESEARCH Pitfall 2). If Open Question 4's fallback fired in Plan 02, the planner must have updated this plan to carry the fallback literal BEFORE this plan runs."
    - "`Entitlements/DailyBriefMonitor-bare.entitlements` exists with an EMPTY entitlements `<dict>` and the explanatory comment block from PATTERNS.md."
    - "`Scripts/install.sh` (a) hard-fails with an actionable message if `Entitlements/embedded.provisionprofile` is missing — mirroring the line-44-56 cert-missing guard shape; (b) copies the profile into `$MONITOR_APP/Contents/embedded.provisionprofile` AFTER the .appex embedding (line 197) and BEFORE the outer `codesign --deep` (line 204) per RESEARCH Pitfall 3; (c) signs the bare binary at $INSTALL_DIR with the new `DailyBriefMonitor-bare.entitlements` instead of `DailyBriefMonitor.entitlements` — and CRITICALLY does NOT mutate the .app `--deep` codesign block at line 208 which must continue to reference `DailyBriefMonitor.entitlements`; (d) inserts `sleep 1` between `launchctl bootout` (line 281) and `launchctl bootstrap` (line 282) per RESEARCH Open Question 3; (e) post-sign verifies the ubiquity entitlement survived codesign by `codesign -d --entitlements - | grep ubiquity-container-identifiers`."
    - "`Sources/DailyBriefMonitor/FolderWatcherService.swift` adds ONE `NSLog` line at the TOP of the existing `public func start()` method (line 94, BEFORE the `guard config.enabled else` gate at line 95) that emits `NSLog(\"Vigil: ubiquity container = %@\", FileManager.default.url(forUbiquityContainerIdentifier: nil)?.path ?? \"nil\")`. The log MUST fire unconditionally on every service start, BEFORE any directory watch is established and BEFORE any file event is processed — Plan 04's UBIQ-02 startup-log probe depends on the line existing in the stderr log before the first VNODE_WRITE."
    - "`swift build -c release` exits 0 after every edit. The brctl block at lines 357-376 is NOT touched in this plan (UBIQ-06 is Plan 06)."
    - "Plan 03 commits land ONLY on `999.1-ubiquity-dry-run`. Re-assert HEAD before each commit."
    - "ROLLBACK RULE: if `swift build -c release` fails after any edit, `git checkout main && ./Scripts/install.sh` to restore the brctl-only working state. Do not attempt forward fixes on the dry-run branch."
  artifacts:
    - path: "Entitlements/DailyBriefMonitor.entitlements"
      provides: "Ubiquity entitlement claim with literal container ID — consumed by the .app `codesign --deep` block at install.sh line 208"
      contains: "com.apple.developer.ubiquity-container-identifiers"
    - path: "Entitlements/DailyBriefMonitor-bare.entitlements"
      provides: "Empty-dict entitlements for the bare binary (Pitfall 6 Option 1) — consumed by the bare-binary codesign block at install.sh line 217 (the one with NO `--deep` flag)"
      contains: "<dict>"
    - path: "Scripts/install.sh"
      provides: "Profile embed + sanity checks + bare-binary signing split + bootout/bootstrap race mitigation + post-sign entitlement-survival probe"
      contains: "embedded.provisionprofile"
    - path: "Sources/DailyBriefMonitor/FolderWatcherService.swift"
      provides: "Startup NSLog of ubiquity container URL at top of public func start() — line 94, BEFORE the config.enabled guard at line 95 (UBIQ-02 evidence trail)"
      contains: "Vigil: ubiquity container"
  key_links:
    - from: "Scripts/install.sh"
      to: "Entitlements/embedded.provisionprofile"
      via: "cp -f \"$PROFILE_SRC\" \"$MONITOR_APP/Contents/embedded.provisionprofile\" BEFORE codesign --deep"
      pattern: "embedded\\.provisionprofile"
    - from: "Scripts/install.sh:208 (.app --deep codesign block)"
      to: "Entitlements/DailyBriefMonitor.entitlements"
      via: "--entitlements arg on the OUTER .app codesign --deep call (line 208) — MUST NOT change to -bare"
      pattern: "DailyBriefMonitor\\.entitlements"
    - from: "Scripts/install.sh:217 (bare-binary codesign block, NO --deep)"
      to: "Entitlements/DailyBriefMonitor-bare.entitlements"
      via: "--entitlements arg on the bare-binary codesign call (line 217 — changed from DailyBriefMonitor.entitlements; the block immediately following `--identifier ... DailyBriefMonitor` and signing $INSTALL_DIR/DailyBriefMonitor)"
      pattern: "DailyBriefMonitor-bare\\.entitlements"
    - from: "Sources/DailyBriefMonitor/FolderWatcherService.swift:94 (top of public func start())"
      to: "~/Library/Logs/DailyBrief/monitor-stderr.log"
      via: "NSLog at the TOP of start(), before config.enabled guard at line 95; surfaces in stderr log; matched by verify-ubiquity.sh check_ubiq_02_startup_log"
      pattern: "Vigil: ubiquity container ="
---

<objective>
Land the actual code + config changes that turn the (now-downloaded) provisioning profile into a working entitlement chain. This plan edits FOUR files; nothing in this plan touches main, nothing actually installs or runs the binary (Plan 04 does that). The plan's only build-time gate is `swift build -c release` — confirming the Swift changes compile clean.

Purpose: produce a build that, when Plan 04 installs it, will (a) embed the profile correctly, (b) carry the ubiquity entitlement on the .app, (c) sign the bare binary with empty entitlements so amfid doesn't kill it, (d) log the ubiquity-container URL at startup so UBIQ-02 is provable from stderr.
Output: four modified files committed on `999.1-ubiquity-dry-run`; release build green.

**Wave note:** This plan now runs in Wave 3 (was Wave 2). Plan 01 → 02 → 03 cascade because Plan 02's UAT artifact is consumed at Task 1 (container literal pre-flight check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md
@Entitlements/DailyBriefMonitor.entitlements
@Entitlements/DailyBrief.entitlements
@Scripts/install.sh
@Sources/DailyBriefMonitor/FolderWatcherService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both entitlement plists (.app gets ubiquity, bare binary stays empty)</name>
  <read_first>
    - Entitlements/DailyBriefMonitor.entitlements (current empty-dict state with Phase 58 comment — lines 1-9; the Phase 58 comment block MUST be preserved)
    - Entitlements/DailyBrief.entitlements (the CLI analog — shows the canonical single-key XML shape)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md (the two entitlements-file sections — final shapes are spelled out byte-for-byte)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md (confirm the container literal used — if Open Question 4's fallback fired, the **bolded** `Container literal used:` line names the fallback. Planner gate MUST have re-approved this plan with the new literal before continuing.)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Pattern 2 — entitlements XML for ubiquity; Pitfall 2 — never use `$(TeamIdentifierPrefix)` in a shell-driven codesign flow)
  </read_first>
  <action>
    Confirm HEAD is `999.1-ubiquity-dry-run` and `Entitlements/embedded.provisionprofile` exists. If either fails, abort and surface the failure — do not continue.

    Read `999.1-02-PORTAL-UAT.md` and extract the container literal used (default `iCloud.com.jamesonmorrill.dailybriefmonitor`; fallback `iCloud.com.jamesonmorrill.vigil` if Open Question 4 fired). If the UAT log's `Planner-Handoff Status` line says "Fallback container — Plan 03 BLOCKED on planner gate" AND this plan has not been re-approved by the planner with the new literal, ABORT — do not proceed. Use that exact literal everywhere in this task. Per RESEARCH Pitfall 2, the value must be the literal — do NOT use `$(TeamIdentifierPrefix).com.jamesonmorrill.dailybriefmonitor`.

    Edit `Entitlements/DailyBriefMonitor.entitlements` to the shape specified in PATTERNS.md "Final target shape": preserve the existing Phase 58 historical comment block, add a new comment block explaining why ubiquity is now back (different entitlement family from CloudKit per the debug doc), and add the `com.apple.developer.ubiquity-container-identifiers` key with an `<array><string>...</string></array>` value carrying the container literal. The plist envelope (XML declaration, DOCTYPE, `<plist version="1.0">`) is unchanged.

    Create `Entitlements/DailyBriefMonitor-bare.entitlements` with the EMPTY-dict shape specified in PATTERNS.md, including the multi-line "Why two entitlement files?" comment block that explains the Pitfall 6 Option 1 rationale (the bare binary at $INSTALL_DIR is alive code — written by `UpdateService.installBuiltBinaries()` at UpdateService.swift:148, which COPIES the already-signed binary from `.build/release/` without re-invoking codesign — so its on-disk signature is whatever the most-recent `./Scripts/install.sh` run baked in. Because the bare binary has no `.app/Contents/embedded.provisionprofile` neighbor, it MUST sign with empty entitlements to avoid amfid -67050 kills).

    Validate both plists with `plutil -lint Entitlements/DailyBriefMonitor.entitlements Entitlements/DailyBriefMonitor-bare.entitlements` — must exit 0.

    Cross-check: decode the downloaded profile and confirm its `Entitlements:com.apple.developer.ubiquity-container-identifiers` array contains the exact literal used in `DailyBriefMonitor.entitlements`. Command: `security cms -D -i Entitlements/embedded.provisionprofile | /usr/libexec/PlistBuddy -c "Print :Entitlements:com.apple.developer.ubiquity-container-identifiers" /dev/stdin` (or pipe to a temp file if PlistBuddy can't read stdin on the running macOS version). Mismatch = STOP and re-do Plan 02 step 6.

    Print `git diff --stat Entitlements/DailyBriefMonitor.entitlements Entitlements/DailyBriefMonitor-bare.entitlements` so the executor has visibility into the Task 1 delta before proceeding.
  </action>
  <verify>
    <automated>plutil -lint Entitlements/DailyBriefMonitor.entitlements Entitlements/DailyBriefMonitor-bare.entitlements &amp;&amp; grep -q 'com.apple.developer.ubiquity-container-identifiers' Entitlements/DailyBriefMonitor.entitlements &amp;&amp; grep -q 'iCloud.com.jamesonmorrill' Entitlements/DailyBriefMonitor.entitlements &amp;&amp; ! grep -q 'TeamIdentifierPrefix' Entitlements/DailyBriefMonitor.entitlements &amp;&amp; test -f Entitlements/DailyBriefMonitor-bare.entitlements &amp;&amp; ! grep -q 'ubiquity' Entitlements/DailyBriefMonitor-bare.entitlements</automated>
  </verify>
  <done>Both plists parse clean, .app entitlements carries the ubiquity key with the literal container ID (no macro), bare-binary entitlements is empty-dict, Phase 58 comment preserved, downloaded profile's authorized-entitlements list contains the same literal.</done>
</task>

<task type="auto">
  <name>Task 2: Edit Scripts/install.sh — five surgical edits with per-edit `git diff --stat` checkpoints</name>
  <read_first>
    - Scripts/install.sh (the WHOLE file — every codesign + launchctl line is in scope; especially note that lines 204-209 are the .app `codesign --deep` block (MUST KEEP referencing `DailyBriefMonitor.entitlements`) and lines 213-218 are the bare-binary `codesign` block with NO `--deep` flag, signing `$INSTALL_DIR/DailyBriefMonitor` — that's the block whose --entitlements line gets renamed to `-bare`)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md (Scripts/install.sh section — has the exact insert-before-line-204 block, the exact post-sign probe block, the bare-binary entitlements-file rename, and the `sleep 1` insertion)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Code Examples — Pre-Sign + Post-Sign blocks; Pitfalls 1, 3, 5, 6; Open Question 3)
  </read_first>
  <action>
    Make exactly five edits to `Scripts/install.sh`, all on `999.1-ubiquity-dry-run`. After EACH edit, do TWO things:
      (i) Run `bash -n Scripts/install.sh` (syntax-only parse) to confirm no shell-syntax regression.
      (ii) Run `git diff --stat Scripts/install.sh` and print its output as a per-edit checkpoint. This gives the executor mid-task visibility without requiring a task split.

    **Edit 1 — Missing-profile guard.** Near the top of the file, after the existing Developer-ID cert guard ends at line 56 and before `echo "Signing identity: $IDENTITY"` on line 57, insert a profile-existence guard mirroring the cert-guard's multi-line stderr / blank-line-spacer / "Then re-run:" / parenthetical-footer shape (see PATTERNS.md "install.sh cert-guard pattern to mirror"). The guard checks `[[ -f "$REPO_DIR/Entitlements/embedded.provisionprofile" ]]`; on failure, print the long-form error pointing at the developer.apple.com flow and `Entitlements/embedded.provisionprofile.example.txt`, then `exit 1`. Per D-03 (Plan 02 portal) — this guard is the hard fail. *After this edit: `bash -n` + `git diff --stat`.*

    **Edit 2 — Profile embed + sanity checks.** Insert the block specified in PATTERNS.md "Concrete shape to insert" BETWEEN the `.appex` re-sign block ending at line 197 (the `codesign --force --sign "$IDENTITY" --options runtime "$MONITOR_PLUGINS/$SAFARI_APPEX_NAME"` call) AND the outer .app `codesign --deep` starting at line 204. The block declares `PROFILE_SRC`, runs `security cms -D -i` into a `mktemp` plist (with `trap` cleanup), checks that PlistBuddy can print the `:Entitlements:com.apple.developer.ubiquity-container-identifiers` key (FAIL with "profile does not authorize ubiquity-container-identifiers" if absent), then `cp -f "$PROFILE_SRC" "$MONITOR_APP/Contents/embedded.provisionprofile"`. Per RESEARCH Pitfall 3, this MUST happen BEFORE `codesign --deep` so the seal covers the profile. *After this edit: `bash -n` + `git diff --stat`.*

    **Edit 3 — Post-sign entitlement-survival probe.** After the existing `codesign --verify --verbose "$MONITOR_APP" 2>&1 || ...` at lines 210-211, insert the probe block specified in PATTERNS.md / RESEARCH "Post-Sign: Verify Entitlements Landed" — `EMBEDDED_ENTITLEMENTS=$(codesign -d --entitlements - "$MONITOR_APP" 2>/dev/null); grep -q 'com.apple.developer.ubiquity-container-identifiers' <<< "$EMBEDDED_ENTITLEMENTS"` (FAIL with "ubiquity entitlement did not survive signing" if not found). *After this edit: `bash -n` + `git diff --stat`.*

    **Edit 4 — Bare-binary entitlements file rename — surgical, anchored to the bare-binary block ONLY.**

    CRITICAL: the string `--entitlements "$REPO_DIR/Entitlements/DailyBriefMonitor.entitlements"` currently appears in TWO places in `Scripts/install.sh`:
      (a) Line 208, inside the `.app` codesign block (the one with `--deep --force` on line 204) signing `$MONITOR_APP`. This MUST CONTINUE to reference `DailyBriefMonitor.entitlements` (the ubiquity-bearing file). DO NOT MUTATE THIS LINE.
      (b) Line 217, inside the bare-binary codesign block (the one WITHOUT `--deep`, with `--identifier "com.jamesonmorrill.dailybriefmonitor"` on line 216) signing `$INSTALL_DIR/DailyBriefMonitor`. THIS is the line to rename.

    To anchor the edit unambiguously: find the line `--identifier "com.jamesonmorrill.dailybriefmonitor"` in the codesign block that signs `$INSTALL_DIR/DailyBriefMonitor` (NOT `$MONITOR_APP`) — that's the bare-binary block. The `--entitlements` line is the line IMMEDIATELY AFTER that `--identifier` line within the bare-binary block (currently line 217). The block has NO `--deep` flag (confirm by reading the block start at line 213 — `codesign --force \`, no `--deep`). Change ONLY that line 217 reference from `DailyBriefMonitor.entitlements` to `DailyBriefMonitor-bare.entitlements`. Lines 213-216 and 218 are unchanged. Line 208 (the .app `--deep` block's entitlements line) is unchanged.

    A naive global `sed -i 's/DailyBriefMonitor.entitlements/DailyBriefMonitor-bare.entitlements/g'` would mutate BOTH lines and strip ubiquity from the .app — DO NOT DO THIS. Use a context-anchored Edit (the Edit tool with old_string scoped to several lines including the `$INSTALL_DIR/DailyBriefMonitor` target line, or a `grep -n` to confirm the exact line number first).

    Verify after the edit (both must hold simultaneously):
      - The .app `codesign --deep` block (around line 204-209) STILL references `DailyBriefMonitor.entitlements` (non-bare). Concretely: `awk '/codesign --deep/,/"\$MONITOR_APP"$/' Scripts/install.sh | grep -q -- '--entitlements .*DailyBriefMonitor\.entitlements"'` (the .app block — `awk` range from the `--deep` line through the end-of-line-anchored `"$MONITOR_APP"` argument line — must contain the non-bare entitlements path).
      - The .app `codesign --deep` block does NOT contain the bare-entitlements path. Concretely (negate the exit code of `grep -q`, NOT use `grep -qv` which is per-line and silently passes on multi-line blocks): `! { awk '/codesign --deep/,/"\$MONITOR_APP"$/' Scripts/install.sh | grep -q -- 'DailyBriefMonitor-bare'; }`. This asserts "no line in the `.app --deep` block contains `DailyBriefMonitor-bare`" — `grep -q` returns 0 if ANY matching line exists; the leading `!` flips that so the whole expression is 0 only when NO matching line exists.
      - The bare-binary block (the codesign call that signs `$INSTALL_DIR/DailyBriefMonitor`, NO `--deep`) references `DailyBriefMonitor-bare.entitlements`. Concretely: `awk '/^codesign --force \\\\$/,/INSTALL_DIR\/DailyBriefMonitor"$/' Scripts/install.sh | tail -n 20 | grep -q -- '--entitlements .*DailyBriefMonitor-bare\.entitlements"'`. (The simpler robust check: `grep -q 'DailyBriefMonitor-bare.entitlements' Scripts/install.sh` returns true AND `grep -c 'DailyBriefMonitor\.entitlements"' Scripts/install.sh` returns exactly 1 — the .app block. The bare file's name CONTAINS the substring `DailyBriefMonitor.entitlements` only as `DailyBriefMonitor-bare.entitlements`, so use a literal-quote search to disambiguate.)

    All three assertions MUST pass; failing any means the surgical edit went wrong — revert with `git checkout -- Scripts/install.sh` and retry. *After this edit: `bash -n` + `git diff --stat`.*

    **Edit 5 — sleep 1 between bootout and bootstrap.** Between current lines 281 (`launchctl bootout ... || true`) and 282 (`launchctl bootstrap ...`), insert TWO new lines IN THIS ORDER:
      Line A: `# Phase 999.1: pause for amfid validation of new profile-bearing bundle (RESEARCH Open Question 3 — Phase 134 debug doc one-time I/O 5 error precedent).`
      Line B: `sleep 1`

    The comment goes ABOVE the `sleep 1` (not inline with `bootout`, not inline with `bootstrap`). Result is a 4-line ordered block: `launchctl bootout ... || true` then comment then `sleep 1` then `launchctl bootstrap ...`. Do NOT add a sleep to `Scripts/bootstrap.sh` — PATTERNS confirmed it loads a different label (vigil-core) and is not affected.

    Verify Edit 5 with a position-independent assertion (the `-A 1` window in the previous version of this plan failed because the Phase-999.1 comment landed in the `-A 1` slice and got stripped by `grep -v '^#'`):

        # awk: assert `sleep 1` appears AFTER `launchctl bootout` and BEFORE `launchctl bootstrap` in the file
        awk '
          /launchctl bootout/ { saw_bootout=1; next }
          saw_bootout && /^[[:space:]]*sleep 1[[:space:]]*$/ { saw_sleep=1; next }
          saw_sleep && /launchctl bootstrap/ { print "OK"; exit 0 }
          /launchctl bootstrap/ { print "FAIL: bootstrap reached before sleep 1"; exit 1 }
        ' Scripts/install.sh

    Expected: prints `OK` and exits 0. Use that command (NOT the `grep -A 1 ... | grep -v '^#' | grep -q 'sleep 1'` shape, which is brittle because comment stripping after a small `-A` window can drop the only sleep line). *After this edit: `bash -n` + `git diff --stat`.*

    After all five edits, validate the full script:
    - `bash -n Scripts/install.sh` (syntax check; exit 0)
    - Confirm no other line numbers shifted incorrectly by greping for sentinel strings: `grep -c "codesign --force" Scripts/install.sh` should still return ≥4 (CLI, .appex, .app outer, bare binary).
    - `grep -c "embedded.provisionprofile" Scripts/install.sh` should return ≥3 (the guard check, the `cp -f` line, the optional sanity-check `security cms -D -i` line).
    - Final `git diff --stat Scripts/install.sh` printout — gives the executor the full Task 2 delta.
  </action>
  <verify>
    <automated>bash -n Scripts/install.sh &amp;&amp; grep -q 'embedded\.provisionprofile' Scripts/install.sh &amp;&amp; grep -q 'cp -f.*embedded\.provisionprofile' Scripts/install.sh &amp;&amp; grep -q 'DailyBriefMonitor-bare\.entitlements' Scripts/install.sh &amp;&amp; awk '/launchctl bootout/{b=1;next} b &amp;&amp; /^[[:space:]]*sleep 1[[:space:]]*$/{s=1;next} s &amp;&amp; /launchctl bootstrap/{print "OK";exit 0} /launchctl bootstrap/{exit 1}' Scripts/install.sh | grep -q OK &amp;&amp; grep -q 'ubiquity entitlement did not survive signing' Scripts/install.sh &amp;&amp; grep -q 'profile does not authorize ubiquity-container-identifiers' Scripts/install.sh &amp;&amp; awk '/codesign --deep/,/"\$MONITOR_APP"$/' Scripts/install.sh | grep -q -- '--entitlements .*DailyBriefMonitor\.entitlements"' &amp;&amp; ! { awk '/codesign --deep/,/"\$MONITOR_APP"$/' Scripts/install.sh | grep -q -- 'DailyBriefMonitor-bare'; }</automated>
  </verify>
  <done>install.sh parses cleanly; profile guard, profile embed, post-sign survival probe, bare-binary entitlements rename (anchored to the bare block ONLY — line 208 .app block UNCHANGED), and bootout/bootstrap `sleep 1` (with comment above the sleep) are all present in correct locations. Per-edit `git diff --stat` checkpoints printed.</done>
</task>

<task type="auto">
  <name>Task 3: Add UBIQ-02 startup NSLog at the top of FolderWatcherService.start() + clean build verify</name>
  <read_first>
    - Sources/DailyBriefMonitor/FolderWatcherService.swift lines 1-160 (specifically: line 17 `public actor FolderWatcherService`, line 61 `public init(...)` which is a pure stored-property assigner — NSLog cannot go here without making init non-trivial; line 94 `public func start()` is the designated lifecycle method that AppDelegate.swift:99 calls via `Task { await watcher.start() }`; line 95 is the `guard config.enabled else { ... return }` gate — the NSLog must precede this guard so the entitlement check fires unconditionally even when folder watching is disabled in config)
    - Sources/DailyBriefMonitor/AppDelegate.swift lines 85-103 (confirms `FolderWatcherService(...)` is constructed at line 90 and `start()` is dispatched at line 99 inside `applicationDidFinishLaunching` — start() is the designated unconditional first-method-runs site, exactly what we need for the UBIQ-02 evidence trail)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Wave 0 Gaps line 521 — calls out this specific NSLog as the UBIQ-02 evidence trail)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Code Examples — Runtime Probe (Swift) section; the NSLog string format must produce a line greppable by `check_ubiq_02_startup_log` in verify-ubiquity.sh)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh (Plan 01's product — confirms the grep pattern is `Vigil: ubiquity container = /Users`)
  </read_first>
  <action>
    Add exactly ONE NSLog call to `Sources/DailyBriefMonitor/FolderWatcherService.swift`.

    **Exact placement (pinned, no ambiguity):** insert as the FIRST executable line of `public func start()` — i.e., between the line `public func start() {` (line 94) and the existing `guard config.enabled else {` (line 95). The NSLog MUST be BEFORE the `guard` gate so the entitlement check fires unconditionally on every service start, regardless of whether folder watching is enabled in config. (This matters because UBIQ-02's PASS criterion is "ubiquity container URL is resolvable at runtime" — not "folder watching is on.")

    Why `start()` and not `init`: (a) `init` (line 61) is a pure stored-property assigner — adding NSLog there would change the semantics of the init from "store deps" to "log side-effect on construction" which is a wider surface than needed. (b) `start()` is the designated lifecycle method called exactly once from AppDelegate.swift:99 (`Task { await watcher.start() }`) inside `applicationDidFinishLaunching`. It always runs before any directory watch is established and before any VNODE_WRITE can fire. (c) Plan 04's UBIQ-02 startup-log probe greps the stderr log for `Vigil: ubiquity container = /Users` and depends on this line existing before the first file event is processed — placing the NSLog at the top of `start()` guarantees this temporal ordering.

    The log MUST emit this exact format:

        NSLog("Vigil: ubiquity container = %@", FileManager.default.url(forUbiquityContainerIdentifier: nil)?.path ?? "nil")

    Rationale: the grep pattern in `verify-ubiquity.sh check_ubiq_02_startup_log` looks for `"Vigil: ubiquity container = /Users"` — the `/Users` prefix is what distinguishes a non-nil URL from the literal string `"nil"` (which would mean the entitlement is claimed but not authorized — UBIQ-02 fail). Use `%@` with the optional-chain producing either a `path` string or `"nil"`.

    Do NOT touch the brctl block at lines 357-376. Do NOT touch the existing `try? FileManager.default.startDownloadingUbiquitousItem(at: url)` on line 356. Do NOT touch the existing NSLog at line 153 (`FolderWatcherService: watching %@ and %@`) — that fires AFTER the watch is established, too late for the UBIQ-02 evidence trail. UBIQ-06 is Plan 06; this plan leaves brctl as belt-and-suspenders.

    After the edit, run `swift build -c release` from the repo root. The build MUST exit 0. If it fails with a Swift compiler error, the most likely cause is a misplaced NSLog (e.g., outside a method body, or with syntax broken by the optional chain) — fix and retry. If `swift build -c release` fails for unrelated reasons (e.g., another file is broken on `999.1-ubiquity-dry-run`), STOP — the dry-run branch should be clean from `main` aside from this plan's edits. Investigate before continuing.

    Print the last 10 lines of `swift build -c release` output (warnings + final "Build complete!" line) to surface any warnings.

    Also print `awk '/public func start\(\) {/,/guard config.enabled/' Sources/DailyBriefMonitor/FolderWatcherService.swift` so the executor can visually confirm the NSLog landed between the function open-brace and the `guard` gate.
  </action>
  <verify>
    <automated>grep -q 'Vigil: ubiquity container' Sources/DailyBriefMonitor/FolderWatcherService.swift &amp;&amp; grep -q 'url(forUbiquityContainerIdentifier:' Sources/DailyBriefMonitor/FolderWatcherService.swift &amp;&amp; awk '/public func start\(\) {/,/guard config.enabled/' Sources/DailyBriefMonitor/FolderWatcherService.swift | grep -q 'Vigil: ubiquity container' &amp;&amp; swift build -c release 2>&amp;1 | tail -5 | grep -q -E '(Build complete|Compiling DailyBriefMonitor)'</automated>
  </verify>
  <done>FolderWatcherService.swift carries the new NSLog as the FIRST executable line of `public func start()` (before the `guard config.enabled` gate at line 95); `swift build -c release` exits 0; brctl block at lines 357-376 is unchanged (verifiable by `grep -c 'brctl' Sources/DailyBriefMonitor/FolderWatcherService.swift` returning the same count as before this plan).</done>
</task>

<task type="auto">
  <name>Task 4: Commit Plan 03 edits on `999.1-ubiquity-dry-run`</name>
  <read_first>
    - git status output (confirm only Plan 03's four files are staged)
  </read_first>
  <action>
    Re-assert HEAD is on `999.1-ubiquity-dry-run`. Stage exactly these files:
    - Entitlements/DailyBriefMonitor.entitlements
    - Entitlements/DailyBriefMonitor-bare.entitlements
    - Scripts/install.sh
    - Sources/DailyBriefMonitor/FolderWatcherService.swift
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-03-PLAN.md (if not already committed)

    Do NOT stage `Entitlements/embedded.provisionprofile` — `git status` should still show it as ignored. If it appears as untracked, the Plan 01 gitignore rule broke; STOP and fix Plan 01 first.

    Commit with message: `feat(999.1): add ubiquity entitlement + profile embed + bare-binary entitlements split`. Single commit, do NOT push. Print `git log --oneline -3` so the executor sees the Plan 01 commit, Plan 02 commits, and the Plan 03 commit in order.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -qx '999.1-ubiquity-dry-run' &amp;&amp; git log --oneline -1 | grep -q 'add ubiquity entitlement' &amp;&amp; ! git ls-files Entitlements/embedded.provisionprofile | grep -q . &amp;&amp; git diff --name-only HEAD~1 HEAD | sort | diff - <(printf '%s\n' '.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-03-PLAN.md' 'Entitlements/DailyBriefMonitor-bare.entitlements' 'Entitlements/DailyBriefMonitor.entitlements' 'Scripts/install.sh' 'Sources/DailyBriefMonitor/FolderWatcherService.swift' | sort)</automated>
  </verify>
  <done>Plan 03 commit lands on `999.1-ubiquity-dry-run` touching exactly four source files + this plan's PLAN.md; profile remains gitignored; HEAD is on the dry-run branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository ↔ build pipeline | `swift build -c release` consumes entitlements files + source; bugs here cause amfid kills at runtime. |
| build pipeline ↔ codesign | install.sh's order-of-operations (profile copy before `codesign --deep`) is the security-critical seam — Pitfall 3 lives here. |
| bare binary ↔ amfid | Bare binary has no `embedded.provisionprofile` neighbor; if it claims ubiquity it gets killed (Pitfall 6). UpdateService.swift:148 COPIES the bare binary (no re-sign) on every Update click, inheriting whatever install.sh signed it with — that's why the bare-entitlements file MUST stay empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-08 | Tampering | Wrong literal in entitlements XML (`$(TeamIdentifierPrefix)` macro) → codesign embeds literal `$(...)`, profile doesn't match, amfid kills | mitigate | Task 1 explicit grep gate: `! grep -q 'TeamIdentifierPrefix' Entitlements/DailyBriefMonitor.entitlements`. Cross-check against profile's authorized list. |
| T-999.1-09 | Tampering | Profile copied AFTER `codesign --deep` → seal broken, Gatekeeper fails | mitigate | Task 2 Edit 2 places the `cp -f` between the `.appex` re-sign (line 197) and the outer `codesign --deep` (line 204). Post-sign probe (Edit 3) catches any regression. |
| T-999.1-10 | Denial of service | Bare binary at $INSTALL_DIR/DailyBriefMonitor claims ubiquity, has no profile neighbor → amfid kills it. UpdateService COPIES this binary on every Update click without re-signing (UpdateService.swift:148, FileManager.copyItem). | mitigate | Task 2 Edit 4 + Task 1 create the bare-entitlements file. The Edit 4 anchoring procedure (find the `--identifier ... DailyBriefMonitor` line in the no-`--deep` block) prevents accidental mutation of the .app `--deep` block's entitlements path. Both assertions (.app=non-bare, bare=bare) must pass post-edit. |
| T-999.1-10b | Tampering | Naive global sed in Edit 4 mutates BOTH the .app line 208 (--deep) and the bare line 217 → strips ubiquity from .app → UBIQ-01 silently fails post-install | mitigate | Task 2 Edit 4 explicit anchoring + triple assertion: (a) `.app --deep block still references DailyBriefMonitor.entitlements (non-bare)`, (b) `.app --deep block does NOT contain DailyBriefMonitor-bare` (using `! { ... grep -q ...; }` to negate exit code, NOT `grep -qv` which is per-line and would silently pass even when the bare path leaked into the block), (c) `bare block references DailyBriefMonitor-bare.entitlements`. Also anchors awk range close pattern to `/"\$MONITOR_APP"$/` (end-of-line) instead of bare `/MONITOR_APP/` so future install.sh refactors that mention MONITOR_APP earlier won't shrink the awk window. All three must pass for verify to pass. |
| T-999.1-11 | Denial of service | launchctl bootstrap races amfid validation on new profile-bearing bundle → I/O 5 error, LaunchAgent doesn't load | mitigate | Task 2 Edit 5 inserts `sleep 1` between bootout and bootstrap, with Phase-999.1 comment ABOVE the sleep. Verified by awk position-independent assertion (NOT `grep -A 1` which would fail with the comment in scope). Phase 134 debug doc records the precedent. |
| T-999.1-12 | Information disclosure | Profile leak via accidental `git add Entitlements/`. | mitigate | Task 4 explicit `! git ls-files Entitlements/embedded.provisionprofile | grep -q .` check. Plan 01 gitignore rule is the upstream control. |
| T-999.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. Swift Package Manager dependencies are not modified. |
</threat_model>

<verification>
- `plutil -lint` passes on both entitlement files.
- `swift build -c release` exits 0 after all source edits.
- `bash -n Scripts/install.sh` exits 0.
- `grep -c 'embedded.provisionprofile' Scripts/install.sh` ≥ 3.
- `grep -c 'codesign --force' Scripts/install.sh` ≥ 4 (no codesign call accidentally deleted).
- `grep -c 'brctl' Sources/DailyBriefMonitor/FolderWatcherService.swift` unchanged from main.
- awk assertion confirms `sleep 1` appears between `launchctl bootout` and `launchctl bootstrap`.
- awk assertion (close pattern anchored to `/"\$MONITOR_APP"$/`) confirms `.app codesign --deep` block still references `DailyBriefMonitor.entitlements` (non-bare).
- Negated-exit-code grep (`! { awk ... | grep -q 'DailyBriefMonitor-bare'; }`) confirms the `.app codesign --deep` block does NOT contain `DailyBriefMonitor-bare` — replaces the broken `grep -qv` per-line idiom that silently passed even when the bare path leaked into the block.
- `grep -q 'DailyBriefMonitor-bare.entitlements' Scripts/install.sh` returns true (bare block updated).
- `awk '/public func start\(\) {/,/guard config.enabled/' Sources/.../FolderWatcherService.swift | grep -q 'Vigil: ubiquity container'` returns true (NSLog placement confirmed).
- `git diff main..HEAD --stat` shows exactly the five expected files (four sources + this PLAN.md).
</verification>

<success_criteria>
- Plan 04 can run `./Scripts/install.sh` and the build will (a) hard-fail with an actionable message if the profile is absent (proves the guard works) OR (b) succeed end-to-end producing a profile-bearing, ubiquity-entitled .app at `~/.local/bin/DailyBriefMonitor.app`.
- Plan 04 can run `verify-ubiquity.sh --full` and UBIQ-01/04/05 will all PASS; UBIQ-02 will PASS once the LaunchAgent has logged the startup line.
- The .app retains ubiquity (Edit 4 did NOT mutate line 208); the bare binary signs with empty entitlements (Edit 4 DID mutate line 217).
</success_criteria>

<output>
Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-03-SUMMARY.md` when done.
</output>
