---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 02
subsystem: infra
tags: [codesign, apple-developer-portal, provisioning-profile, icloud, ubiquity, launchagent]

# Dependency graph
requires:
  - phase: 999.1-01
    provides: "gitignore rule for Entitlements/*.provisionprofile, embedded.provisionprofile.example.txt operator flow doc, verify-ubiquity.sh skeleton"
provides:
  - "Confirmed Developer ID Provisioning Profile exists on Mac at Entitlements/embedded.provisionprofile (gitignored, structurally valid)"
  - "UAT evidence log with portal steps, profile decode output (Developer-ID wildcard 5H57ADQS8G.* shape confirmed), and rollback rehearsal"
  - "Rollback rehearsal PASSED — main brctl-only build confirmed healthy (empty [Dict] entitlements, state=running)"
  - "Planner-Handoff Status: preferred container iCloud.com.jamesonmorrill.dailybriefmonitor — Plan 03 may auto-run"
affects:
  - 999.1-03
  - 999.1-04
  - 999.1-05

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-machine portal UAT log: Linux agent writes log skeleton, Mac operator fills evidence via resume signals"
    - "Rollback rehearsal before any code change: git checkout main && ./Scripts/install.sh proves safety net before irreversible steps"
    - "Developer ID wildcard entitlement shape: profile emits 5H57ADQS8G.* (team-scoped wildcard) not the literal container ID — amfid authorizes any container in the team"

key-files:
  created:
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-SUMMARY.md
  modified: []

key-decisions:
  - "Developer ID wildcard entitlement (5H57ADQS8G.*) is correct and expected — Plan 03 must hardcode the literal container ID iCloud.com.jamesonmorrill.dailybriefmonitor in the entitlements XML, which amfid will authorize because it falls within team 5H57ADQS8G"
  - "Preferred container iCloud.com.jamesonmorrill.dailybriefmonitor was available globally — Open Question 4 fallback did NOT fire; no planner gate needed before Plan 03"
  - "Cross-machine architecture: Linux agent writes plan/logs, Mac operator executes portal work and codesign/launchctl verifications via resume signals — screenshots reference Mac filesystem paths only (not copied to Linux repo)"
  - "Rollback rehearsal executed BEFORE any code change — Phase 58 lesson applied: prove the safety net works before irreversible steps"

patterns-established:
  - "Portal-UAT log pattern: header + cross-machine-arch note + portal-steps table + container-identifier + profile-decode + screenshots + gitignore-confirmation + planner-handoff-status + rollback-rehearsal + sign-off"
  - "Rollback rehearsal gate: mandatory brctl-only build verification on main before proceeding to Plans 03-05"

requirements-completed: [UBIQ-01, UBIQ-04, UBIQ-05]

# Metrics
duration: multi-session (human portal work + rehearsal gated)
completed: 2026-05-19
---

# Phase 999.1 Plan 02: Portal UAT + Rollback Rehearsal Summary

**Developer ID Provisioning Profile downloaded and gitignored (12 KB, expiry 2044), iCloud container iCloud.com.jamesonmorrill.dailybriefmonitor registered, Developer-ID wildcard entitlement 5H57ADQS8G.* confirmed in profile, rollback rehearsal passed — main brctl-only build healthy (empty [Dict], state=running)**

## Performance

- **Duration:** Multi-session — human portal work at developer.apple.com + rollback rehearsal on Mac
- **Started:** 2026-05-19T18:37:21Z (Task 1 resume signal received)
- **Completed:** 2026-05-19T18:42:58Z (Task 4 committed)
- **Tasks:** 4 of 4 (Tasks 1 + 3 = human checkpoints; Tasks 2 + 4 = auto)
- **Files modified:** 1 created (999.1-02-PORTAL-UAT.md), 1 created (this SUMMARY)

## Accomplishments

- Operator registered iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor` globally at developer.apple.com and assigned it to App ID `com.jamesonmorrill.dailybriefmonitor` with "Compatible with Xcode 5" iCloud capability (NOT "Include CloudKit support" — Phase 58 pitfall avoided).
- Developer ID Provisioning Profile generated and saved at `Entitlements/embedded.provisionprofile` (12,790 bytes, expiry 2044-05-14). Profile emits Developer-ID wildcard entitlement `5H57ADQS8G.*` for both `ubiquity-container-identifiers` and `ubiquity-kvstore-identifier` — correct and expected per RESEARCH.md.
- Profile is gitignored (`.gitignore:40:Entitlements/*.provisionprofile`) and NOT tracked by git — T-999.1-04 information-disclosure threat mitigated.
- Rollback rehearsal completed: `git checkout main && ./Scripts/install.sh` on Mac produced a clean brctl-only build. `codesign -d --entitlements` returned empty `[Dict]` (no ubiquity). `codesign --verify` said "valid on disk + satisfies Designated Requirement". `launchctl print` showed `state = running, last exit code = (never exited)`. Phase 58 safety net confirmed working.

## Task Commits

1. **Task 1: Operator portal work** — human (no commit; evidence captured in Task 2)
2. **Task 2: Write the UAT evidence log** — `6f2ece8` (docs)
3. **Task 3: Rollback rehearsal** — human checkpoint (no commit; evidence captured in Task 4)
4. **Task 4: Append rollback-rehearsal section to UAT log** — `0b7d512` (docs)

**Plan metadata:** (this SUMMARY commit — see final commit)

## Files Created/Modified

- `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md` — UAT evidence log: portal steps, profile decode, Developer-ID wildcard shape, gitignore confirmation, planner-handoff status, rollback rehearsal evidence
- `Entitlements/embedded.provisionprofile` — on Mac only; gitignored, not in Linux repo

## Decisions Made

1. **Developer-ID wildcard entitlement shape is correct.** The profile emits `5H57ADQS8G.*` not the literal container ID. Per RESEARCH.md: Developer ID profiles emit team-scoped wildcards; amfid authorizes any container within the team. Plan 03 should still hardcode the literal `iCloud.com.jamesonmorrill.dailybriefmonitor` in the entitlements XML — it will be authorized because it is within team `5H57ADQS8G`.

2. **Preferred container available — no planner gate.** `iCloud.com.jamesonmorrill.dailybriefmonitor` was globally available. Open Question 4 fallback did NOT fire. Plan 03's hardcoded literal is correct as written.

3. **Cross-machine architecture for portal work.** Linux agent runs GSD; Mac operator executes codesign/launchctl/portal steps and returns evidence via resume signals. Screenshots reference Mac filesystem paths only (not copied to Linux repo — no secrets in screenshots, but transfer not performed).

4. **Rollback rehearsal as pre-flight.** Executed the full `git checkout main && ./Scripts/install.sh` path on the Mac BEFORE any code change — Phase 58 lesson: prove the escape hatch works before you need it. Result: clean, confirmed.

## Deviations from Plan

**1. [Rule 2 - Cross-Machine Architecture] UAT log written on Linux from Mac-provided evidence**
- **Found during:** Task 2
- **Issue:** Plan was written assuming Linux-side and Mac-side are the same machine. Screenshots and profile file are only on the Mac.
- **Fix:** UAT log includes a "Cross-Machine Architecture Note" section documenting the two-machine setup; screenshots recorded as Mac-side path references only (not committed to Linux repo).
- **Impact:** Expected and correct — no functional impact. Plan 03's executor runs on Linux but reads profile via `Entitlements/embedded.provisionprofile` on the Mac (synced via origin).
- **Committed in:** `6f2ece8`

**Total deviations:** 1 auto-handled (cross-machine architecture documentation)
**Impact on plan:** No scope creep. Plan executed as designed; cross-machine note improves future-plan clarity.

## Issues Encountered

None beyond the cross-machine architecture clarification above.

## User Setup Required

All user setup was completed in this plan:
- `Entitlements/embedded.provisionprofile` present on Mac, gitignored, structurally valid.
- App ID `com.jamesonmorrill.dailybriefmonitor` has iCloud capability enabled with container assigned.
- Developer ID Provisioning Profile `DailyBriefMonitor Developer ID Profile` valid through 2044.

No additional external service configuration required for Plan 03.

## Next Phase Readiness

- **Plan 03 is UNBLOCKED.** Preferred container path — no planner gate. Literal `iCloud.com.jamesonmorrill.dailybriefmonitor` is correct as written in Plan 03 Task 1 and `verify-ubiquity.sh`.
- **Mac must `git pull origin 999.1-ubiquity-dry-run`** before Plan 03 executes (to sync UAT log + SUMMARY from Linux-side commits).
- **Profile location:** `Entitlements/embedded.provisionprofile` on Mac — Plan 03's `install.sh` change will embed this profile in the signed app bundle.
- **Rollback path confirmed:** If Plans 03–05 break signing, `git checkout main && ./Scripts/install.sh` restores the known-good brctl-only build (rehearsal proved this works).

---
*Phase: 999.1-restore-ubiquity-entitlement-for-icloud-download*
*Completed: 2026-05-19*
