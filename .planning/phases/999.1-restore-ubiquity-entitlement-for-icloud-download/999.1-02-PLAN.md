---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Entitlements/embedded.provisionprofile
  - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md
autonomous: false
requirements: [UBIQ-01, UBIQ-04, UBIQ-05]
must_haves:
  truths:
    - "App ID `com.jamesonmorrill.dailybriefmonitor` has its iCloud capability enabled at developer.apple.com (NOT 'Include CloudKit support' — only 'Compatible with Xcode 5') and the container `iCloud.com.jamesonmorrill.dailybriefmonitor` is assigned to it."
    - "iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor` exists globally (registered to team `5H57ADQS8G`) at developer.apple.com."
    - "A Developer ID Provisioning Profile bound to App ID `com.jamesonmorrill.dailybriefmonitor` + the operator's Developer ID Application cert is downloaded to `Entitlements/embedded.provisionprofile` on the dev machine."
    - "The downloaded profile lists `com.apple.developer.ubiquity-container-identifiers` in its Entitlements dict — verified before Plan 03 starts."
    - "The downloaded profile does NOT show up in `git status` (it must be gitignored by Plan 01's rule)."
    - "Rollback rehearsal: the operator has already run `git checkout main && ./Scripts/install.sh` once on a clean dry-run branch and confirmed the brctl-only build still launches cleanly. This MUST be done BEFORE any code changes in Plan 03."
    - "ROLLBACK RULE: if anything in Plans 03–05 breaks signing or the LaunchAgent, run `git checkout main && ./Scripts/install.sh`. Do not attempt forward fixes on the dry-run branch."
  artifacts:
    - path: "Entitlements/embedded.provisionprofile"
      provides: "Apple-CMS-signed binary plist authorizing `com.apple.developer.ubiquity-container-identifiers` for App ID `com.jamesonmorrill.dailybriefmonitor` + the operator's Developer ID cert"
      contains: "(binary) — verified via `security cms -D -i` decode"
    - path: ".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md"
      provides: "UAT evidence log — screenshots, profile decode output, rollback-rehearsal confirmation"
      contains: "Entitlements"
  key_links:
    - from: "Entitlements/embedded.provisionprofile"
      to: "App ID `com.jamesonmorrill.dailybriefmonitor`"
      via: "Apple developer-portal profile generation"
      pattern: "application-identifier.*com\\.jamesonmorrill\\.dailybriefmonitor"
    - from: "Entitlements/embedded.provisionprofile"
      to: "iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor`"
      via: "ubiquity-container-identifiers entitlement in profile's Entitlements dict"
      pattern: "com\\.apple\\.developer\\.ubiquity-container-identifiers"
user_setup:
  - service: developer.apple.com
    why: "Restricted ubiquity entitlement requires App-ID + container + provisioning-profile minted by Apple. No CLI/API path exists for any of this — every step is a human web action."
    env_vars: []
    dashboard_config:
      - task: "Sign in to developer.apple.com/account with the Apple ID that owns team 5H57ADQS8G"
        location: "https://developer.apple.com/account"
      - task: "Register iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor` (Open Question 4 — if taken globally, choose `iCloud.com.jamesonmorrill.vigil` instead and propagate the new literal everywhere)"
        location: "Certificates, IDs & Profiles → Identifiers → `+` → iCloud Containers"
      - task: "Edit App ID `com.jamesonmorrill.dailybriefmonitor`: enable iCloud capability with 'Compatible with Xcode 5' (NOT 'Include CloudKit support' — that re-introduces the Phase 58 break); assign the new container"
        location: "Certificates, IDs & Profiles → Identifiers → App IDs → com.jamesonmorrill.dailybriefmonitor → Edit"
      - task: "Generate Developer ID Provisioning Profile (Distribution → Developer ID), select the App ID, select the Developer ID Application cert, name it `DailyBriefMonitor Developer ID Profile`, download the .provisionprofile"
        location: "Certificates, IDs & Profiles → Profiles → `+`"
      - task: "Save downloaded profile to `Entitlements/embedded.provisionprofile` in the local repo"
        location: "(local filesystem)"
---

<objective>
Complete the one-time human/portal work that gates every code change in this phase. RESEARCH.md is explicit (Architectural Responsibility Map): there is no local-only alternative — Apple Developer Portal is the canonical source of truth for restricted entitlements, and only a human can drive its web UI. This plan also runs the rollback rehearsal that proves the throwaway-branch isolation works.

Purpose: produce the `embedded.provisionprofile` asset that Plan 03's install.sh change consumes; verify it's structurally valid; prove the rollback path works before any irreversible-feeling steps.
Output: a CMS-signed `.provisionprofile` file in `Entitlements/`, a UAT evidence log with screenshots and decoded-profile output, a confirmed rollback rehearsal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md
@Entitlements/embedded.provisionprofile.example.txt
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator portal work — App ID iCloud capability + container + profile download</name>
  <read_first>
    - Entitlements/embedded.provisionprofile.example.txt (the operator-facing flow doc Plan 01 dropped in)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Common Pitfalls 1, 2, 5 — what failure modes to watch for; "Installation (developer machine)" section lines 60-65 — exact portal steps; Open Question 4 — what to do if the container ID is globally taken)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Manual-Only Verifications table — exactly what to screenshot)
  </read_first>
  <action>Operator performs all developer.apple.com portal steps to enable iCloud capability on App ID `com.jamesonmorrill.dailybriefmonitor`, register iCloud container `iCloud.com.jamesonmorrill.dailybriefmonitor`, generate a Developer ID Provisioning Profile, and save it to `Entitlements/embedded.provisionprofile`. No CLI path exists — every step is a human web action. See `<what-built>` below for the exact 7-step flow.</action>
  <what-built>
    The operator (human) — there is no CLI/API for any of this. Steps to execute in order:

    1. Open https://developer.apple.com/account and sign in with the Apple ID owning team `5H57ADQS8G`.
    2. Navigate to Certificates, IDs & Profiles → Identifiers → `+` → iCloud Containers. Register `iCloud.com.jamesonmorrill.dailybriefmonitor`. **If the identifier is taken globally (RESEARCH Open Question 4)**: register `iCloud.com.jamesonmorrill.vigil` instead AND STOP — escalate back to the planner; the entire phase's container literal must change everywhere (this plan, Plan 03's entitlements XML, the verify-ubiquity.sh constant). Do NOT proceed with a mismatched literal.
    3. Identifiers → App IDs → `com.jamesonmorrill.dailybriefmonitor` → Edit. Enable the **iCloud** capability checkbox. **MUST select "Compatible with Xcode 5", NOT "Include CloudKit support"** — the latter re-introduces the Phase 58 break (RESEARCH "Anti-Patterns to Avoid" — adding icloud-services unintentionally). Click "Edit containers" inline and assign the container created in step 2. Save.
    4. Profiles → `+` → Distribution → **Developer ID** → continue. Select App ID `com.jamesonmorrill.dailybriefmonitor`. Select the Developer ID Application cert currently in your login keychain. Name the profile `DailyBriefMonitor Developer ID Profile`. Generate and download. (RESEARCH Pitfall 5: confirm at this point that the displayed App ID matches `com.jamesonmorrill.dailybriefmonitor` exactly — typos here are how amfid kills hit later.)
    5. Move the downloaded `.provisionprofile` to `Entitlements/embedded.provisionprofile` in this repo on the local machine.
    6. Decode the profile to confirm structure: `security cms -D -i Entitlements/embedded.provisionprofile > /tmp/profile.plist`; then `/usr/libexec/PlistBuddy -c "Print :Entitlements:com.apple.developer.ubiquity-container-identifiers" /tmp/profile.plist`. Expected output: an array containing `iCloud.com.jamesonmorrill.dailybriefmonitor` (or your fallback literal). If empty or error, the profile was misgenerated — regenerate from step 4.
    7. Confirm `git status` shows the file as ignored (NOT untracked). If it appears as untracked, Plan 01's gitignore rule failed; do NOT commit anything — return to Plan 01.
  </what-built>
  <how-to-verify>
    Run these commands and paste the output (with secrets redacted) into the UAT log (Task 2):
    1. `ls -la Entitlements/embedded.provisionprofile` → file exists, size ~7-12 KB typical
    2. `git check-ignore -v Entitlements/embedded.provisionprofile` → exits 0, names `.gitignore:N:Entitlements/*.provisionprofile`
    3. `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | head -60` → readable plist; `AppIDName`, `Name`, `TeamIdentifier` fields visible
    4. `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep -A 3 ubiquity-container-identifiers` → array containing `iCloud.com.jamesonmorrill.dailybriefmonitor` (or fallback)
    5. `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep ExpirationDate` → date approximately 18 years from today (per RESEARCH "Standard Stack — Supporting")
    6. Screenshots: (a) App ID edit page showing iCloud checkbox + assigned container, (b) Profiles list showing the new Developer ID profile, (c) `security cms -D` terminal output showing the ubiquity key.
  </how-to-verify>
  <resume-signal>Type "approved" with the decoded-profile output pasted (with TeamIdentifier visible — that's not a secret), OR describe any portal-step issue / Open Question 4 trigger.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the UAT evidence log</name>
  <read_first>
    - The Task 1 resume message (operator-provided decode output + screenshot file paths)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Manual-Only Verifications table — UAT log structure expected by `/gsd:verify-work` later in the phase)
  </read_first>
  <action>
    Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md` with the following sections, populated from the operator's Task 1 resume message:

    - **Header** — phase 999.1, plan 02, date (today, 2026-05-19 unless multi-day), operator email from `~/.claude/projects/.../MEMORY.md` (jamesonmorrill1@gmail.com).
    - **Portal Steps Executed** — bullet list mirroring Task 1's 7 steps, with PASS/FAIL/N-A for each.
    - **Container Identifier Used** — literal `iCloud.com.jamesonmorrill.dailybriefmonitor` (or the fallback if Open Question 4 fired — flag this case in **bold** so Plan 03's executor catches the literal change).
    - **Profile Decode Output** — paste the `security cms -D -i ... | plutil -p -` output (TeamIdentifier visible — not a secret), confirming `Name`, `AppIDName`, `Entitlements:com.apple.developer.ubiquity-container-identifiers`, `ExpirationDate`.
    - **Screenshots** — relative paths to the three screenshots from Task 1 step 6, saved under `.planning/phases/999.1-.../uat-evidence/` (create that subdirectory if not present). Screenshots themselves are part of the operator's clipboard or filesystem — copy the files into the repo if needed and gitignore them OR check them in if they contain no secrets (Team ID and App ID are not secrets, but operator's Apple-ID email might be — review each before committing).
    - **gitignore confirmation** — `git check-ignore -v Entitlements/embedded.provisionprofile` output verbatim.
    - **Sign-off** — operator signed off via resume signal "approved" at $(date -Iseconds).

    Then: `git add .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md` and any screenshots that passed the secret-review. Confirm `git status` does NOT list `Entitlements/embedded.provisionprofile` as staged or untracked. Commit on `999.1-ubiquity-dry-run` with message `docs(999.1): operator portal UAT — App ID iCloud + Developer ID profile`.

    Do NOT push.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md &amp;&amp; grep -q 'iCloud.com.jamesonmorrill' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md &amp;&amp; grep -q 'ubiquity-container-identifiers' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md &amp;&amp; ! git ls-files Entitlements/embedded.provisionprofile | grep -q .</automated>
  </verify>
  <done>UAT log committed on `999.1-ubiquity-dry-run` with decoded-profile evidence and gitignore confirmation; `Entitlements/embedded.provisionprofile` exists on disk but is NOT tracked by git.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Rollback rehearsal — prove the dry-run isolation works BEFORE any code change</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Wave 0 Requirements — "Rollback rehearsal" item)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Rollback (Belt-and-Suspenders) code block — exact rollback commands; Summary — "do not touch main until Checkpoint 5" rationale)
    - The Phase 58 lesson context: ROADMAP.md Phase 999.1 entry "Why it's in backlog" paragraph mentions Phase 58's CloudKit-adjacent signing issues. The rollback path is what makes this phase safe.
  </read_first>
  <action>Operator runs the rollback path once (git checkout main && ./Scripts/install.sh) and confirms the brctl-only build still launches cleanly. This is the Phase 58 lesson translated to an action — prove the safety net works BEFORE any code change in Plan 03. See `<what-built>` for the exact 5-step verification sequence.</action>
  <what-built>
    The operator runs the rollback path EXACTLY ONCE NOW (before any irreversible change) to prove the safety net works. This is the Phase 58 lesson translated into an action:

    1. From repo root, on `999.1-ubiquity-dry-run` branch with the (gitignored) profile in place: `git checkout main`.
    2. Run `./Scripts/install.sh` — this rebuilds and installs the brctl-only build (no ubiquity, no profile — main has neither).
    3. Wait 10s for the LaunchAgent to settle.
    4. Verify the brctl-only build is healthy:
       - `codesign -d --entitlements - ~/.local/bin/DailyBriefMonitor.app 2>&1 | grep -v ubiquity` (should NOT contain ubiquity — main has empty entitlements dict)
       - `codesign --verify --verbose ~/.local/bin/DailyBriefMonitor.app` (should pass)
       - `launchctl print gui/$(id -u)/com.jamesonmorrill.dailybriefmonitor | grep state` (should show `state = running`)
       - Drop a test file into `~/Library/Mobile Documents/com~apple~CloudDocs/Notebook/` and confirm the menu bar item processes it (or at minimum the LaunchAgent didn't crash).
    5. Return to the dry-run branch: `git checkout 999.1-ubiquity-dry-run`. (Do NOT re-run install.sh yet — Plan 03 changes install.sh first; running it now would just reinstall the same brctl-only build a second time, which is fine but wastes the operator's time.)

    If step 4 shows the brctl-only build is NOT healthy on main (e.g., LaunchAgent won't start, signature fails): STOP. Something is wrong with main itself, independent of Phase 999.1. File a separate bug; do NOT proceed with this phase.
  </what-built>
  <how-to-verify>
    Paste into the resume signal:
    - Output of `codesign -d --entitlements - ~/.local/bin/DailyBriefMonitor.app 2>&1` after the rehearsal install (should be empty `<dict/>` — main's state).
    - Output of `launchctl print gui/$(id -u)/com.jamesonmorrill.dailybriefmonitor | grep -E '(state|last exit)'` showing `state = running`.
    - Confirmation that `git rev-parse --abbrev-ref HEAD` is back to `999.1-ubiquity-dry-run` after the rehearsal.
    - One-line description of whether the operator observed the menu bar item actually processing a file during the rehearsal (anecdotal — not a blocking criterion, but useful evidence for the UAT log).
  </how-to-verify>
  <resume-signal>Type "rehearsal-passed" with the codesign/launchctl output AND confirmation HEAD is on `999.1-ubiquity-dry-run`. OR describe what failed and STOP the phase.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Append rollback-rehearsal section to UAT log + final commit</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md (the file written in Task 2)
    - The Task 3 resume message (rehearsal evidence)
  </read_first>
  <action>
    Append a new section `## Rollback Rehearsal` to `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md`. Include:
    - Timestamp.
    - The codesign `--entitlements -` output from Task 3 (should show empty `<dict/>` confirming main is brctl-only).
    - The `launchctl print` output excerpt showing `state = running`.
    - Confirmation HEAD returned to `999.1-ubiquity-dry-run` cleanly.
    - One sentence: "Rollback path confirmed working — Plans 03–05 may proceed."

    Stage the UAT log update and commit on `999.1-ubiquity-dry-run` with message `docs(999.1): rollback rehearsal confirmed — safe to proceed with code changes`.
  </action>
  <verify>
    <automated>grep -q '^## Rollback Rehearsal' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md &amp;&amp; grep -q 'state = running' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-PORTAL-UAT.md &amp;&amp; git log --oneline -1 | grep -q 'rollback rehearsal' &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -qx '999.1-ubiquity-dry-run'</automated>
  </verify>
  <done>UAT log carries both Portal Steps + Rollback Rehearsal sections; final commit landed on `999.1-ubiquity-dry-run`; HEAD is on the dry-run branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser ↔ developer.apple.com | Apple-account auth; Team ID + cert identity flow through here. |
| developer.apple.com ↔ local filesystem | Profile download crosses this boundary; profile is CMS-signed by Apple's CA so tampering would invalidate it. |
| local filesystem ↔ git | Profile MUST NOT cross — gitignore from Plan 01 is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | Information disclosure | Profile or Apple-ID email accidentally committed | mitigate | Task 2 explicit `! git ls-files Entitlements/embedded.provisionprofile` check; pre-commit secret-review on screenshots. |
| T-999.1-05 | Tampering | Wrong App ID picked at portal step 4 (typo / copy-paste error) — profile binds to a different identifier than `install.sh --identifier` line | mitigate | RESEARCH Pitfall 5: Task 1 step 6 decodes profile and grep-confirms `application-identifier` matches `com.jamesonmorrill.dailybriefmonitor`. Re-asserted in Plan 03's install.sh sanity check. |
| T-999.1-06 | Denial of service | Adding 'Include CloudKit support' instead of 'Compatible with Xcode 5' re-introduces Phase 58 break | mitigate | Task 1 step 3 explicitly warns; UAT log records which option was selected. |
| T-999.1-07 | Repudiation | No audit trail of which container literal was actually registered (Open Question 4 fallback case) | mitigate | UAT log records the literal used; if fallback fires, the planner is escalated to BEFORE Plan 03 starts so the literal propagates correctly. |
| T-999.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan. Operator's portal session does not run any local installers. |
</threat_model>

<verification>
- `Entitlements/embedded.provisionprofile` exists on disk; `git check-ignore` resolves to the Plan 01 rule; `git ls-files` does NOT list it.
- `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep -A 3 ubiquity` shows the expected container literal.
- `999.1-02-PORTAL-UAT.md` exists with both Portal Steps and Rollback Rehearsal sections.
- HEAD is on `999.1-ubiquity-dry-run`; main is unchanged by this plan.
- Rollback rehearsal explicitly confirmed `state = running` on main — Plans 03–05 have a known-good fallback.
</verification>

<success_criteria>
- Plan 03's executor can run `[[ -f Entitlements/embedded.provisionprofile ]]` and find the profile present.
- The literal container ID Plan 03 hardcodes in `Entitlements/DailyBriefMonitor.entitlements` matches what's in the profile (verified by both this plan's UAT log and Plan 03's pre-flight check).
- The operator has hands-on confirmation that the rollback path works, eliminating the Phase 58 "we have no escape hatch" risk.
</success_criteria>

<output>
Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-02-SUMMARY.md` when done.
</output>
