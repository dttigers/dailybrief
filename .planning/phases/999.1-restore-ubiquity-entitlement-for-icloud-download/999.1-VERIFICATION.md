---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
verified: 2026-05-19T20:42:33Z
status: human_needed
score: 5/6
overrides_applied: 0
human_verification:
  - test: "Canonical UBIQ-03 UAT on Optimize-Mac-Storage machine"
    expected: "FolderWatcherService: triggered iCloud download for <filename> appears in monitor-stderr.log within 5s of a HEIC stub arriving in the Notebook folder on an Optimize Mac Storage Mac"
    why_human: "Structurally unreachable on operator's 'Download Originals to this Mac' configuration — no .icloud stub placeholders ever appear. The code IS compiled in (confirmed via strings), the API IS authorized (UBIQ-02 proves it), but the specific code path calling startDownloadingUbiquitousItem cannot be exercised without toggling iCloud Drive to Optimize Mac Storage or using a second machine."
deferred:
  - truth: "brctl shim removable (UBIQ-06 optional)"
    addressed_in: "Plan 06 re-run (post merge-to-main + 7 days)"
    evidence: "Plan 06 SUMMARY: defer-bake-not-yet disposition. Re-pickup criteria: merge-to-main + 7 days + operator confirms no regressions. UBIQ-01..05 closure does not require UBIQ-06."
---

# Phase 999.1: restore-ubiquity-entitlement-for-icloud-download — Verification Report

**Phase Goal:** DailyBriefMonitor.app can call `FileManager.startDownloadingUbiquitousItem` natively on iCloud Drive HEIC stubs, without relying on the `/usr/bin/brctl` shell-out hack shipped in `fff8a15` (2026-04-21) as Option B. Once this entitlement lands, the brctl fallback can optionally stay as belt-and-suspenders or be removed.

**Verified:** 2026-05-19T20:42:33Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Cross-Machine Architecture Note

This phase spans two machines:

- **Linux** (`/home/morrillboss/dev/dailybrief`) — GSD planner + agent execution. All planning artifacts, probe scripts, and entitlement files live here. This is where verification runs.
- **Mac** (`/Users/jamesonmorrill/dev/dailybrief`) — Build / sign / install / test. `codesign`, `launchctl`, `log show`, `swift build`, and iCloud Drive interaction all run exclusively on the Mac. Runtime evidence was captured by the operator during Plans 04 and 05 and committed as UAT evidence logs.
- **Shared remote:** `git@github.com:dttigers/dailybrief.git` — sync bridge.

**Branch:** `999.1-ubiquity-dry-run` — intentionally NOT merged to `main` yet. This is by design: Plans 02–04 required the throwaway branch for safe iteration. Plan 06's 7-day bake clock begins only after `main` merge. The unmerged branch is not a regression; it is the documented terminal state of Plans 01–05.

---

## Plan 03 Hotfix History

During Plan 04's checkpoint loop, four issues unanticipated by Plan 03 were discovered and fixed. These commits are part of Phase 999.1's effective shipped code and are treated as Plan 03 deliverables for purposes of this verification:

| Commit | Fix | Impact |
|--------|-----|--------|
| `a6acd3d` | Entitlement format: changed `iCloud.com.jamesonmorrill.dailybriefmonitor` → `5H57ADQS8G.iCloud.com.jamesonmorrill.dailybriefmonitor`. Developer ID profiles authorize a team-scoped wildcard `5H57ADQS8G.*`; the friendly form does not begin with the team prefix and fails amfid's wildcard match. | Required for amfid authorization (UBIQ-01/05). |
| `001a892` | Intermediate experiment: listed BOTH friendly and team-prefixed forms in entitlement array. Immediately reversed — amfid requires ALL array entries to match the profile allow-list; the friendly form is not in the allow-list. | Reverted; no impact on final state. |
| `10e4fd9` | Added `NSUbiquitousContainers` dict to `install.sh`'s generated `Info.plist` using the friendly container ID (`iCloud.com.jamesonmorrill.dailybriefmonitor`) as the dict key. This is the bird/cloudd channel — bird reads `Info.plist` (not the codesign entitlement) to discover which containers to provision. | Required for bird to provision the container directory and for `url(forUbiquityContainerIdentifier:)` to return non-nil (UBIQ-02). |
| `6fe657a` | Downgraded UBIQ-04b (`spctl -a -vv`) from FAIL to advisory WARN. Notarization is orthogonal to the ubiquity entitlement chain; pre-phase builds on `main` also fail this probe. | UBIQ-04b correctly classified as advisory. |
| `6e6b5af` | Fixed UBIQ-04 SIGPIPE under `set -euo pipefail`: captured `codesign --verify` output into a shell variable before grepping, instead of piping directly. | Required for probe to correctly report UBIQ-04 PASS. |

**RESEARCH.md completeness:** Pitfall 2 covered the macro-expansion case but not the Developer ID wildcard-profile case. Commits `a6acd3d` + `10e4fd9` fill that gap with the canonical dual-channel pattern: entitlement carries team-prefixed literal (amfid channel), `NSUbiquitousContainers` in `Info.plist` carries friendly form (bird/cloudd channel).

---

## Goal Achievement

### Observable Truths (UBIQ-01 through UBIQ-06)

| # | Requirement | Truth | Status | Evidence |
|---|-------------|-------|--------|----------|
| 1 | UBIQ-01 | `codesign -d --entitlements -` on the installed .app shows `com.apple.developer.ubiquity-container-identifiers` | VERIFIED | Plan 04 INSTALL-PROBE.md: signed entitlements XML shows `5H57ADQS8G.iCloud.com.jamesonmorrill.dailybriefmonitor` under the ubiquity key. `Entitlements/DailyBriefMonitor.entitlements` carries the team-prefixed literal (confirmed in codebase: line 29). install.sh line 278 passes this file to `codesign --deep`. Post-sign survival probe at line 286 confirms the key survived sealing. |
| 2 | UBIQ-02 | `url(forUbiquityContainerIdentifier: nil)` returns non-nil at runtime | VERIFIED | Plan 04 INSTALL-PROBE.md: startup log confirms `Vigil: ubiquity container = /Users/jamesonmorrill/Library/Mobile Documents/iCloud~com~jamesonmorrill~dailybriefmonitor`. FolderWatcherService.swift line 101 emits this NSLog unconditionally at the top of `public func start()`, before the `guard config.enabled` gate. Container directory provisioned by bird at `~/Library/Mobile Documents/iCloud~com~jamesonmorrill~dailybriefmonitor/` (timestamp matches install). |
| 3 | UBIQ-03 | Dropping a fresh NotDownloaded HEIC produces end-to-end materialization driven by `startDownloadingUbiquitousItem` | VERIFIED (equivalent proof) | Canonical NSLog gate (`FolderWatcherService: triggered iCloud download for <filename>`) is structurally unreachable on operator's "Download Originals to this Mac" configuration — no `.icloud` stubs ever exist in Notebook. Equivalent proof: (a) UBIQ-02 non-nil URL proves runtime authorization of the ubiquity API family (same entitlement gate as `startDownloadingUbiquitousItem`); (b) UBIQ-04/05b PASS — amfid did NOT kill the process; (c) two HEIC drops processed end-to-end by Vigil (~1.2 MB outbound upload observed, files moved to `done/`). Code IS compiled in (`strings` confirms format string presence). See Human Verification section for canonical UAT path. |
| 4 | UBIQ-04 | codesign + Gatekeeper verify clean on the signed binary | VERIFIED | `codesign --verify --verbose` reports `valid on disk` + `satisfies its Designated Requirement` (Plan 04 INSTALL-PROBE.md). UBIQ-04b (Gatekeeper/notarization) is advisory WARN — `spctl -a -vv` fails on notarization, which is orthogonal to ubiquity entitlement chain and would also fail on pre-phase `main` builds (LaunchAgent deployment model bypasses Gatekeeper). |
| 5 | UBIQ-05 | LaunchAgent running, no amfid kills | VERIFIED | `launchctl print gui/.../com.jamesonmorrill.dailybriefmonitor` shows `state = running`, PID 8569, `last exit code = (never exited)`. amfid kill predicate (`grep -E '(Killed|-67050|Invalid Code Signature)'` against bundle ID) returned 0 matches in the last 10 minutes (Plan 04 evidence). Kill-anchored predicate correctly avoids false positives from routine amfid validation log lines. |
| 6 | UBIQ-06 | brctl shim removable (optional) | DEFERRED | Intentional — Plan 06 disposition is `defer-bake-not-yet`. 7-day bake precondition unfulfillable until merge to `main`. brctl shim remains in FolderWatcherService.swift (lines 363–384) as harmless belt-and-suspenders. On operator's "Download Originals" Mac, empirical subprocess fork rate is ~0/day (no stubs appear). Re-pickup criteria: merge-to-main + 7 days + operator confirms no regressions. UBIQ-01..05 closure is complete without UBIQ-06. |

**Score:** 5/6 truths verified (UBIQ-01..05 PASS, UBIQ-06 intentionally DEFERRED as documented in Plan 06)

---

### Deferred Items

Items not yet met but explicitly addressed in a later plan execution.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | brctl shim removable (UBIQ-06) | Plan 06 re-run (post merge-to-main + 7 days) | Plan 06 BRCTL-REMOVAL.md: temporal deferral disposition log. Re-pickup criteria explicit. `uat-evidence/task-1-signal.txt` contains `defer-bake-not-yet`. |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Entitlements/DailyBriefMonitor.entitlements` | Ubiquity entitlement with team-prefixed literal container ID | VERIFIED | Contains `com.apple.developer.ubiquity-container-identifiers` with `5H57ADQS8G.iCloud.com.jamesonmorrill.dailybriefmonitor`. No `TeamIdentifierPrefix` macro. Phase 58 comment block preserved. Plist lint-clean (confirmed in Plan 03). |
| `Entitlements/DailyBriefMonitor-bare.entitlements` | Empty dict for bare binary codesign | VERIFIED | File exists at correct path. Contains empty `<dict/>` with rationale comment explaining Pitfall 6 Option 1. Not tracked by git as a secret. |
| `Scripts/install.sh` | Profile guard + embed + post-sign probe + bare-binary entitlements split + sleep 1 | VERIFIED | All 5 surgical edits confirmed: (1) profile guard at lines 68–82; (2) profile embed block at line 266 (BEFORE `codesign --deep` at 274); (3) post-sign survival probe at lines 285–289; (4) bare-binary entitlements at line 301 (DailyBriefMonitor-bare.entitlements — .app block at line 278 unchanged); (5) `sleep 1` between `launchctl bootout` and `bootstrap` (awk position-independent assertion PASSES). Syntax check (`bash -n`) passes. |
| `Sources/DailyBriefMonitor/FolderWatcherService.swift` | Startup NSLog at top of `public func start()` before `guard config.enabled` | VERIFIED | NSLog at line 101, unconditionally before the `guard config.enabled` gate at line 102. Format: `NSLog("Vigil: ubiquity container = %@", FileManager.default.url(forUbiquityContainerIdentifier: nil)?.path ?? "nil")`. brctl block at lines 363–384 intentionally intact (Plan 06 deferral). |
| `probes/verify-ubiquity.sh` | 6 check functions, `--static`/`--runtime`/`--full` modes, SIGPIPE fix, UBIQ-04b advisory, final-success literal | VERIFIED | All 6 check functions present (12 grep matches for function names). SIGPIPE fix: `codesign --verify` output captured to variable before grep (lines 46–47). UBIQ-04b: advisory WARN, does not set FAIL=1 (line 63–69). amfid kill predicate: `grep -E '(Killed|-67050|Invalid Code Signature)'` (line 89). Final-success literal: `green "verify-ubiquity: all checks passed"` at line 145. |
| `.gitignore` | `Entitlements/*.provisionprofile` glob ignore | VERIFIED | Line 40: `Entitlements/*.provisionprofile`. Confirmed working (`git check-ignore -v` exits 0 per Plan 02 UAT). `Entitlements/embedded.provisionprofile` is NOT tracked by git. |
| `Entitlements/embedded.provisionprofile.example.txt` | Portal flow doc for fresh clones | VERIFIED | File exists. References `developer.apple.com`, `iCloud.com.jamesonmorrill.dailybriefmonitor`, rollback footnote. |
| `.planning/phases/999.1-.../999.1-02-PORTAL-UAT.md` | UAT evidence log with portal steps + rollback rehearsal | VERIFIED | Contains: portal steps table (all 7 PASS), container identifier used (`iCloud.com.jamesonmorrill.dailybriefmonitor`), profile decode output (Developer-ID wildcard `5H57ADQS8G.*`), gitignore confirmation, Planner-Handoff Status ("Preferred container — Plan 03 may auto-run"), Rollback Rehearsal section (`state = running`, `last exit code = (never exited)`, HEAD confirmation). |
| `.planning/phases/999.1-.../999.1-04-INSTALL-PROBE.md` | Install + probe-sweep evidence | VERIFIED | Contains: install.sh exit 0, signed entitlements XML (team-prefixed literal), bare-binary empty entitlements, profile-in-bundle (12790 bytes), container provisioned by bird, LaunchAgent state=running, probe sweep (all 6 checks), amfid negative result, UpdateService re-sign cross-check, 4-deviation trail. |
| `.planning/phases/999.1-.../999.1-05-HEIC-UAT.md` | HEIC UAT log with equivalent-proof rationale | VERIFIED | Contains: two HEIC drop run summaries, why canonical gate didn't fire (structural unreachability documented), equivalent-proof rationale (3 signals), merge recommendation (YES), Plan 06 implications. |
| `.planning/phases/999.1-.../999.1-06-BRCTL-REMOVAL.md` | Plan 06 disposition log | VERIFIED | `defer-bake-not-yet` signal. 7-day bake precondition documented as unfulfillable pre-merge. Re-pickup criteria explicit. `uat-evidence/task-1-signal.txt` contains `defer-bake-not-yet`. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `install.sh` | `Entitlements/embedded.provisionprofile` | `cp -f` at line 266 BEFORE `codesign --deep` at line 274 | VERIFIED | Pitfall 3 satisfied: profile embed precedes outer codesign seal. install.sh line 266 confirmed. |
| `install.sh` (.app codesign --deep, line 274–279) | `Entitlements/DailyBriefMonitor.entitlements` | `--entitlements` arg on `codesign --deep` block | VERIFIED | Line 278 references `DailyBriefMonitor.entitlements` (not bare). awk range check confirms .app block does NOT contain `DailyBriefMonitor-bare`. |
| `install.sh` (bare-binary codesign, line 297–302) | `Entitlements/DailyBriefMonitor-bare.entitlements` | `--entitlements` arg on bare-binary codesign block (no `--deep`) | VERIFIED | Line 301 references `DailyBriefMonitor-bare.entitlements`. Only one occurrence of `DailyBriefMonitor.entitlements` in install.sh (line 278, the .app block). |
| `FolderWatcherService.swift` line 101 | `~/Library/Logs/DailyBrief/monitor-stderr.log` | NSLog at top of `public func start()` before `guard config.enabled` | VERIFIED | NSLog confirmed at line 101. Plan 04 INSTALL-PROBE.md probe sweep: `check_ubiq_02_startup_log` PASS with actual URL in log. |
| `verify-ubiquity.sh` | Plan 04 / Plan 06 grep targets | Final-success literal `verify-ubiquity: all checks passed` at line 145 | VERIFIED | Literal confirmed on line 145. Plan 04 INSTALL-PROBE.md records `verify-ubiquity: all checks passed` as the final stdout line at exit 0. |
| `Entitlements/DailyBriefMonitor.entitlements` | amfid wildcard match | Team-prefixed literal `5H57ADQS8G.iCloud.com.jamesonmorrill.dailybriefmonitor` | VERIFIED | amfid policy: all array entries must match profile allow-list wildcard `5H57ADQS8G.*`. Friendly form would fail (empirically confirmed in Plan 04 checkpoint — Deviation 2). Final form passes (zero amfid kills post-fix). |
| `install.sh` generated `Info.plist` | bird/cloudd container provisioning | `NSUbiquitousContainers` dict with friendly container ID | VERIFIED | Lines 165–176 in install.sh generate `NSUbiquitousContainers` with key `iCloud.com.jamesonmorrill.dailybriefmonitor`. Plan 04: bird provisioned container at `~/Library/Mobile Documents/iCloud~com~jamesonmorrill~dailybriefmonitor/` immediately after install. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `FolderWatcherService.swift` startup NSLog | `url(forUbiquityContainerIdentifier:)` return value | `FileManager.default` API call — real OS IPC to cloudd | Yes — non-nil URL confirmed in Plan 04 log: `/Users/jamesonmorrill/Library/Mobile Documents/iCloud~com~jamesonmorrill~dailybriefmonitor` | FLOWING |
| `verify-ubiquity.sh` UBIQ-01 | `entitlements` shell variable | `codesign -d --entitlements -` on installed .app — reads actual codesign-embedded XML | Yes — signed XML confirmed in Plan 04 INSTALL-PROBE.md | FLOWING |
| `verify-ubiquity.sh` UBIQ-02 | log grep against `$MONITOR_LOG` | `~/Library/Logs/DailyBrief/monitor-stderr.log` — LaunchAgent stderr piped to disk | Yes — NSLog line confirmed in Plan 04 INSTALL-PROBE.md | FLOWING |

---

### Behavioral Spot-Checks

This phase produces Mac-only runnable code (`codesign`, `swift build`, `launchctl`). No spot-checks can be run on the Linux host. All behavioral verification was performed on the Mac by the operator during Plan 04 and Plan 05 human checkpoints. Evidence is committed in `999.1-04-INSTALL-PROBE.md` and `999.1-05-HEIC-UAT.md`.

| Behavior | Command (Mac-side) | Result | Status |
|----------|-------------------|--------|--------|
| install.sh exits 0 | `./Scripts/install.sh 2>&1` | Exit 0, `=== Installation Complete ===` | PASS (Plan 04 evidence) |
| verify-ubiquity.sh --full exits 0 | `bash .../probes/verify-ubiquity.sh --full` | Exit 0, final line `verify-ubiquity: all checks passed` | PASS (Plan 04 evidence) |
| LaunchAgent state = running | `launchctl print gui/.../com.jamesonmorrill.dailybriefmonitor` | `state = running`, PID 8569, `last exit code = (never exited)` | PASS (Plan 04 evidence) |
| No amfid kills | `log show --predicate 'process == "amfid"' --last 10m \| grep bundle ID \| grep -E '(Killed\|-67050\|Invalid Code Signature)'` | 0 matches | PASS (Plan 04 evidence) |
| Bare binary has empty entitlements | `codesign -d --entitlements - ~/.local/bin/DailyBriefMonitor` | `<dict/>` | PASS (Plan 04 evidence) |

---

### Probe Execution

The `verify-ubiquity.sh --full` probe was run on the Mac by the operator during Plan 04. Equivalent to Step 7c behavioral verification in the GSD verification process.

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `probes/verify-ubiquity.sh --full` | `bash .../probes/verify-ubiquity.sh --full` (Mac, commit `6e6b5af`) | Exit 0; all 6 checks resolved (UBIQ-01 PASS, UBIQ-04 PASS, UBIQ-04b advisory WARN, UBIQ-05 PASS, UBIQ-05b PASS, UBIQ-02 PASS); final line `verify-ubiquity: all checks passed` | PASS |

Static probe check (Linux, verifier-side — confirms script is well-formed, not that the .app is installed):

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `verify-ubiquity.sh` syntax | `bash -n .../probes/verify-ubiquity.sh` | Exit 0 | PASS |
| Final-success literal | `grep -q 'verify-ubiquity: all checks passed' .../probes/verify-ubiquity.sh` | Present at line 145 | PASS |
| 6 check functions present | Function name grep count | 12 matches (each named twice: definition + call) | PASS |
| amfid kill predicate | `grep -E '(Killed\|-67050\|Invalid Code Signature)' .../verify-ubiquity.sh` | Present (line 89) | PASS |
| CONTAINER_ID_LITERAL | `grep 'CONTAINER_ID_LITERAL' .../verify-ubiquity.sh` | `5H57ADQS8G.iCloud.com.jamesonmorrill.dailybriefmonitor` at line 11 | PASS |

---

### Requirements Coverage

UBIQ-01..06 are phase-internal requirements defined in RESEARCH.md and VALIDATION.md for this phase. They do not appear in the milestone `.planning/REQUIREMENTS.md` (that file tracks v3.9 voice/G2/companion requirements). This phase is a standalone maintenance phase outside the v3.9 milestone scope.

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| UBIQ-01 | Plans 01, 02, 03, 04 | ubiquity-container-identifiers entitlement embedded in .app | SATISFIED | Signed entitlements XML confirmed in Plan 04 INSTALL-PROBE.md; entitlements file verified on disk |
| UBIQ-02 | Plans 01, 03, 04 | `url(forUbiquityContainerIdentifier:)` returns non-nil at runtime | SATISFIED | NSLog confirmed in Plan 04 INSTALL-PROBE.md; container directory provisioned by bird |
| UBIQ-03 | Plan 05 | `startDownloadingUbiquitousItem` authorized at runtime | SATISFIED (equivalent proof) | See UBIQ-03 row in Observable Truths table above; human verification recommended for canonical UAT |
| UBIQ-04 | Plans 01, 02, 03, 04 | codesign verify clean | SATISFIED | `valid on disk` + `satisfies Designated Requirement` in Plan 04; UBIQ-04b advisory downgrade documented |
| UBIQ-05 | Plans 01, 02, 03, 04 | LaunchAgent running, no amfid kills | SATISFIED | `state = running`, zero amfid kills in Plan 04 |
| UBIQ-06 | Plan 06 | brctl shim removable (optional) | DEFERRED | `defer-bake-not-yet` — temporal deferral pending merge + 7 days |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `Sources/DailyBriefMonitor/FolderWatcherService.swift` | 363–384 | `brctl` subprocess fork block (redundant post-entitlement-fix) | INFO | Intentional belt-and-suspenders per Plan 06 deferral decision. brctl dispatch rate is ~0/day on operator's "Download Originals" Mac (no stubs appear). Plan 06 will clean this up after 7-day bake. Not a blocker. |

No `TBD`, `FIXME`, or `XXX` debt markers found in phase-modified files.

No `TODO` or `PLACEHOLDER` markers found in phase-modified files.

No empty implementations or hardcoded static returns found in phase-modified source files.

---

### Human Verification Required

#### 1. Canonical UBIQ-03 UAT — `startDownloadingUbiquitousItem` native-API attribution

**Test:** On a Mac configured for "Optimize Mac Storage" (System Settings → Apple Account → iCloud → iCloud Drive → "Optimize Mac Storage" ON), with `999.1-ubiquity-dry-run` installed:
1. Drop a HEIC photo into `~/Library/Mobile Documents/com~apple~CloudDocs/Notebook/` from a second device (iPhone, iPad, or web upload).
2. Tail `~/Library/Logs/DailyBrief/monitor-stderr.log`.
3. Within ~5s of the stub appearing, look for: `FolderWatcherService: <filename> not yet downloaded from iCloud (status: NSURLUbiquitousItemDownloadingStatusNotDownloaded), deferring` AND `FolderWatcherService: triggered iCloud download for <filename>`.

**Expected:** The `triggered iCloud download for <filename>` line appears — this confirms `FileManager.default.startDownloadingUbiquitousItem(at:)` was called and returned without throwing, i.e., the ubiquity entitlement is runtime-authorized for the native API call. File materializes fully within ~10s.

**Why human:** The operator's Mac is configured for "Download Originals to this Mac" — iCloud downloads files immediately as full local copies, never as `.icloud` stub placeholders. The `triggerICloudDownloads()` code path in `FolderWatcherService.swift` only executes when the `ubiquitousItemDownloadingStatusKey` is not `.current` (i.e., when a stub exists). This precondition is never met on the operator's machine. The code is present (`strings` confirms the format string); the API IS authorized (UBIQ-02 proves it via the same entitlement chain); but the specific code path cannot be exercised without the "Optimize Mac Storage" iCloud configuration.

**Equivalent proof status (verifier assessment):** The equivalent proof presented in `999.1-05-HEIC-UAT.md` is logically sound and the three signals are mutually reinforcing:
- Signal (a): UBIQ-02 non-nil URL — the ubiquity API family (same entitlement gate as `startDownloadingUbiquitousItem`) is provably runtime-authorized. If the entitlement were unauthorized, `url(forUbiquityContainerIdentifier:)` would also return nil.
- Signal (b): UBIQ-04/05b — amfid did not kill the process. During Plan 04 iteration, when the entitlement format was WRONG, amfid DID kill the process with -413 (empirically observed). The clean run with the correct team-prefixed format is positive evidence of authorization.
- Signal (c): End-to-end iCloud Drive file-monitoring pipeline works (two HEIC drops, network upload observed, files moved to `done/`).

The verifier accepts the equivalent proof as establishing UBIQ-03 PASS with high confidence on the available evidence. The canonical UAT is recommended as a future-enrichment item rather than a merge blocker. However, per the verification process, any item requiring human testing mandates `human_needed` status.

---

### Gaps Summary

No blocking gaps identified. The only open item requiring human attention is:

**UBIQ-03 canonical UAT** — the specific code path exercising `startDownloadingUbiquitousItem` is structurally unreachable on the operator's "Download Originals" Mac. The equivalent proof is accepted by the verifier and the phase goal is substantively achieved (the entitlement chain is authorized; the API works). The canonical UAT on an "Optimize Mac Storage" Mac would provide direct observational confirmation of the code path, but is not a blocker for merge based on the equivalent-proof evidence.

**UBIQ-06 deferral** — intentional, documented, and non-blocking. brctl shim remains harmless belt-and-suspenders. Plan 06 re-pickup criteria are explicit.

---

### Merge Recommendation

This verification supports merging `999.1-ubiquity-dry-run` to `main`:

- UBIQ-01 PASS: ubiquity entitlement present in signed .app with correct team-prefixed literal
- UBIQ-02 PASS: `url(forUbiquityContainerIdentifier:)` returned non-nil; bird provisioned container
- UBIQ-03 PASS (equivalent proof): API authorization proven via entitlement chain; end-to-end pipeline works
- UBIQ-04 PASS: codesign verify clean; UBIQ-04b (notarization) advisory-only
- UBIQ-05 PASS: LaunchAgent running stably; zero amfid kills
- UBIQ-06 DEFERRED: intentional; brctl harmless; re-run post merge + 7 days

No TCC regressions. No new amfid kills. No debt markers in modified files. Rollback path confirmed working (Plan 02 rehearsal).

**After merge:** Start the 7-day bake clock for Plan 06 re-pickup.

---

_Verified: 2026-05-19T20:42:33Z_
_Verifier: Claude (gsd-verifier)_
