---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 05
type: execute
wave: 5
depends_on: [999.1-04]
files_modified:
  - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md
autonomous: false
requirements: [UBIQ-03]
must_haves:
  truths:
    - "Dropping a fresh `NotDownloaded` HEIC stub into `~/Library/Mobile Documents/com~apple~CloudDocs/Notebook/` causes the file to materialize end-to-end within 10 seconds."
    - "UBIQ-03 PASS REQUIRES NATIVE-API ATTRIBUTION. The monitor's stderr log MUST contain the line `FolderWatcherService: triggered iCloud download for <filename>` on EACH run (Run 1 AND Run 2). This line is emitted by FolderWatcherService.swift line 210, which only fires after `FileManager.default.startDownloadingUbiquitousItem(at:)` returns without throwing — i.e., when the native ubiquity API actually drove the materialization. Materialization-by-brctl-alone (no `triggered iCloud download` line) is FAIL even if the file ends up `.current` — because the whole point of Phase 999.1 is the native API being the driver. If brctl is the only mechanism that worked, the entitlement chain is still broken at runtime and UBIQ-03 does NOT pass."
    - "After materialization, `mdls -name kMDItemFSSize <file>` returns the full size (not the stub size); `xattr -l <file>` no longer contains `com.apple.ubiquity.placeholder.metadata`."
    - "End-to-end ingest completes: the HEIC is uploaded to vigil-core and the resulting thought appears in the PWA. (This is anecdotal confirmation that the broader pipeline survived the entitlement change.)"
    - "The drop test is run at LEAST 2× with DIFFERENT HEICs to rule out one-off cache effects. If either run fails the driver-attribution check above, rollback (`git checkout main && ./Scripts/install.sh`) and STOP."
    - "ROLLBACK RULE: if materialization fails, OR if `triggered iCloud download for <filename>` does NOT appear on each run, OR if `dispatched brctl download` is the ONLY trigger (i.e., the native ubiquity API still no-ops with the entitlement in place), surface as a regression; do NOT proceed to Plan 06. The brctl belt-and-suspenders may have saved this UAT run from a materialization-stall — but the entitlement-only path needs more diagnosis before phase merge."
  artifacts:
    - path: ".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md"
      provides: "Hardware-class UAT log with timestamps, log excerpts (each Run section MUST include the `triggered iCloud download for <filename>` line), mdls/xattr output, PWA thought-row screenshot reference, brctl-also-fired observation"
      contains: "triggered iCloud download"
  key_links:
    - from: "Sources/DailyBriefMonitor/FolderWatcherService.swift (line 210 — the `triggered iCloud download for` log emitter)"
      to: "macOS fileproviderd / cloudd"
      via: "FileManager.startDownloadingUbiquitousItem (now authorized by ubiquity entitlement from Plan 03) — the log line confirms native API attribution, not just successful materialization"
      pattern: "triggered iCloud download for"
    - from: "fileproviderd materialization"
      to: "vigil-core API upload"
      via: "VNODE_WRITE event → FolderWatcherService.processFile → DailyBriefImageService.processPhoto"
      pattern: "FolderWatcherService.*processed"
---

<objective>
Prove UBIQ-03 — the human-perceivable test that the entitlement actually does work AND that the native API is the driver: drop a real HEIC, see it materialize end-to-end via `FileManager.startDownloadingUbiquitousItem`, no human-intervention needed, with the `triggered iCloud download for <filename>` log line as the evidence of native-API attribution.

Purpose: the entire phase exists for this user experience (drop photo on iPhone → Mac processes it without the brctl shim hack). UBIQ-01/02/04/05 prove the plumbing; UBIQ-03 proves the plumbing actually moves water — AND that the native API is the one moving it (not the brctl fallback silently doing all the work).
Output: UAT log capturing two successful drop runs with timestamps, log excerpts naming the native-API line, and merge recommendation.

**Wave note:** Wave 5 (was Wave 4) after the cascade fix for Plan 02's wave correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-04-INSTALL-PROBE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: First HEIC drop — native ubiquity end-to-end materialization with driver-attribution check</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Per-Requirement Verification Map row for UBIQ-03 — exact log lines + mdls/xattr commands to use as evidence)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Pitfall 4 — brctl race after adding ubiquity; this plan keeps brctl in place as belt-and-suspenders, so expect possibly BOTH log lines — and the native line MUST be present for PASS)
  </read_first>
  <action>Operator drops a fresh NotDownloaded HEIC into `~/Library/Mobile Documents/com~apple~CloudDocs/Notebook/` and tails `~/Library/Logs/DailyBrief/monitor-stderr.log`. PASS requires the line `FolderWatcherService: triggered iCloud download for <filename>` to appear within 5s — this is the native-API attribution gate, not just materialization completion. See `<what-built>` for full 8-step procedure + failure-mode rollback rules.</action>
  <what-built>
    Operator performs the canonical UBIQ-03 hardware test. Steps:

    1. From repo root, confirm HEAD is `999.1-ubiquity-dry-run` and the .app at `~/.local/bin/DailyBriefMonitor.app` carries the ubiquity entitlement (from Plan 04 — should still be installed; if not, re-run `./Scripts/install.sh`).
    2. Open a terminal tab tailing the monitor log: `tail -F ~/Library/Logs/DailyBrief/monitor-stderr.log`.
    3. On iPhone (or a Mac without iCloud Drive set up): take a new photo and "Save to Files" → iCloud Drive → Notebook. (Alternative path if no iPhone available: copy a HEIC from `~/Pictures/` into the iCloud Notebook folder via a different Mac's iCloud Drive web upload OR using `osascript` to trigger a `cp` to the iCloud Drive path while the file is in "not yet downloaded" state — though this is hard to engineer without a second device. The clean way is iPhone → Files → iCloud Drive → Notebook.)
    4. Note the timestamp the photo lands.
    5. Within ~5s of the drop, the tail MUST print: `FolderWatcherService: <filename> not yet downloaded from iCloud (status: NSURLUbiquitousItemDownloadingStatusNotDownloaded), deferring` AND CRITICALLY `FolderWatcherService: triggered iCloud download for <filename>` (the native-API line at FolderWatcherService.swift:210 — this is the UBIQ-03 PASS gate; emission proves `startDownloadingUbiquitousItem` returned without throwing, i.e. the entitlement is runtime-authorized).
    6. Within ~10s the file should materialize and the next VNODE_WRITE re-queues it: the tail should show normal processing lines (`FolderWatcherService: scanning ...`, then `FolderWatcherService: processed <filename>` or the equivalent image-ingest log).
    7. Confirm materialization by running on the file path: `mdls -name kMDItemFSSize ~/Library/Mobile\ Documents/com~apple~CloudDocs/Notebook/<filename>` → expect full size (≫ 100 bytes; a stub is ~70 bytes). `xattr -l ~/Library/Mobile\ Documents/com~apple~CloudDocs/Notebook/<filename>` → expect NO `com.apple.ubiquity.placeholder.metadata` extended attribute.
    8. Confirm broader pipeline: open the PWA dashboard and verify a new thought row corresponding to the photo appears within ~30s of the drop. (This is anecdotal but confirms no upstream pipeline regression.)

    Pitfall 4 (RESEARCH) — brctl race: with the brctl block STILL IN PLACE (Plan 06 removes it later), expect possibly BOTH `triggered iCloud download` (native) and `dispatched brctl download` (fallback) in the log. This is fine — RESEARCH says brctl `exit 0` happens almost instantly because the file is already in `.downloading` state from the native call. The pass criterion is that `triggered iCloud download` appears — that proves UBIQ-03's native-API attribution. The brctl-also-fired observation is what makes Plan 06 (optional removal) attractive.

    **Driver-attribution PASS gate (locked truth):** PASS requires the native line. Materialization-by-brctl-alone is FAIL. If only `dispatched brctl download` appears (no `triggered iCloud download`), the file may still end up `.current` — but that means brctl drove the materialization, the native API still no-ops despite the entitlement, and the entire 999.1 phase has failed to achieve its purpose. The brctl belt-and-suspenders has saved the UAT run from looking broken, but UBIQ-03 has NOT passed. Treat this as a STOP-and-rollback condition.

    Failure modes:
    - **No "triggered iCloud download" log line within 30s**: the native API is STILL no-op. Entitlement chain is broken at runtime even though static checks pass. STOP, rollback, surface for diagnosis. This would mean Plan 04's UBIQ-02 startup log lied (or that the entitlement is present but unauthorized at the runtime level — Pitfall 1 hidden form).
    - **"dispatched brctl download" appears but NOT "triggered iCloud download"**: same as above — native is no-op, brctl is doing all the work. Rollback.
    - **File never materializes (mdls still shows stub size, xattr still has placeholder)**: ubiquity infra is broken on this Mac irrespective of Phase 999.1. Try again in 1m; if still broken, STOP and check iCloud status in System Settings — this is NOT a phase regression.
  </what-built>
  <how-to-verify>
    Paste into the resume signal:
    1. The HEIC filename used.
    2. The timestamp the drop happened (HH:MM:SS).
    3. The relevant `tail -F` log excerpt covering ~30s around the drop, with the `FolderWatcherService: triggered iCloud download for <filename>` line clearly visible. If this line is absent, the run FAILS regardless of other outcomes.
    4. Output of `mdls -name kMDItemFSSize ~/Library/Mobile\ Documents/com~apple~CloudDocs/Notebook/<filename>` after materialization (full size).
    5. Output of `xattr -l ~/Library/Mobile\ Documents/com~apple~CloudDocs/Notebook/<filename>` (no placeholder.metadata attr).
    6. Whether the PWA dashboard saw the new thought row.
    7. Whether `dispatched brctl download` also fired (informational — for Plan 06's decision; not a PASS gate).
  </how-to-verify>
  <resume-signal>Type "heic-1-passed" with the seven outputs (the `triggered iCloud download` line MUST be present and quoted). OR describe what failed; if anything failed, rollback (`git checkout main && ./Scripts/install.sh`) BEFORE typing the resume signal.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Second HEIC drop — different photo, rule out one-off cache effects (driver-attribution still required)</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Per-Requirement Verification Map UBIQ-03 row)
  </read_first>
  <action>Operator repeats the Task 1 HEIC drop with a DIFFERENT photo to rule out cache-warmup effects. Same procedure, same PASS gate (the `triggered iCloud download for <filename>` log line MUST appear), same materialization timing. See `<what-built>` for the rationale (two independent successes with native-API attribution provide higher confidence than one).</action>
  <what-built>
    Operator repeats the Task 1 drop test with a DIFFERENT HEIC (different filename, different content). Same iPhone-to-Notebook path. Same observation steps (tail log, mdls, xattr, PWA confirmation). Same driver-attribution PASS gate: the `FolderWatcherService: triggered iCloud download for <filename>` line MUST appear; brctl-only materialization is FAIL.

    Why a second run: RESEARCH Open Question 1 noted that cloudd/fileproviderd may need a few seconds to re-evaluate the bundle's ubiquity-container membership on first launch — a single PASS doesn't rule out "first one was a coincidence." Two successful drops with different filenames AND both showing the native-API line provide higher confidence.

    Optional: drop a third HEIC if either of the first two showed marginal timing (e.g., 9s materialization right at the 10s edge). Repeat until two CLEAN PASSes with comfortable timing margins AND native-API attribution on each run are recorded.
  </what-built>
  <how-to-verify>
    Same shape as Task 1 resume — paste the filename, timestamp, log excerpt with the `FolderWatcherService: triggered iCloud download for` line, mdls size, xattr output, PWA confirmation, and brctl-also-fired observation.
  </how-to-verify>
  <resume-signal>Type "heic-2-passed" with the second-run evidence (native-API line MUST be present). OR describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write UAT log, recommend merge-to-main</name>
  <read_first>
    - The Task 1 + Task 2 resume messages (operator-provided UBIQ-03 evidence)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-04-INSTALL-PROBE.md (UBIQ-01/02/04/05 PASS evidence from Plan 04)
  </read_first>
  <action>
    Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md` with these sections:

    - **Header** — phase 999.1, plan 05, date, branch (`999.1-ubiquity-dry-run`), commit SHA.
    - **Run 1** — filename, timestamp, log excerpt (MUST quote the `FolderWatcherService: triggered iCloud download for` line), mdls/xattr output, PWA confirmation, brctl-also-fired observation (yes/no).
    - **Run 2** — same fields, different HEIC, native-API line again required.
    - **Driver-Attribution Audit** — one section explicitly stating that BOTH runs emitted `FolderWatcherService: triggered iCloud download for <filename>`. This is the UBIQ-03 PASS gate; if either run is missing the line, the UAT FAILS regardless of materialization outcome.
    - **Phase Requirement Closure Summary** — for each of UBIQ-01 through UBIQ-05, cite where the PASS evidence lives (Plan 04 PROBE log) and the final disposition. For UBIQ-03 specifically, name both runs AND explicitly cite the native-API log line on each.
    - **brctl Race Observation** — record whether both `triggered iCloud download` and `dispatched brctl download` appeared in the same drop. If both appeared, recommend Plan 06 (brctl removal) be picked up after the 7-day bake; if only the native line appeared, brctl is already silently no-op'ing on .current files, which is even cleaner — Plan 06 remains optional.
    - **TCC / Notarization Regression Check** — re-confirm from Plan 04's anecdotal observations that no TCC prompts surfaced; speech-recognition (voice-memo) still works; vigil-core API key still resolves from Keychain.
    - **Merge Recommendation** — explicit yes/no on "merge `999.1-ubiquity-dry-run` to `main`." YES requires: all of UBIQ-01..05 PASS, both HEIC runs PASS WITH native-API attribution, no TCC regression, no new amfid kills. Anything less = NO.
    - **Open Items for Plan 06** — note whether brctl removal is recommended after bake. The "must_haves.truths" of Plan 06 already gates it on `≥7 days` since 999.1 ships; this section is just the planner's recommendation strength.

    Stage + commit on `999.1-ubiquity-dry-run`: `docs(999.1): UBIQ-03 hardware UAT — 2× HEIC drop PASS (native-API attributed)`.

    If the merge recommendation is YES: do NOT actually merge in this plan. The operator decides when to merge (probably right after this commit, but explicitly out of scope here — this plan ends with the recommendation, not the merge). If/when the operator merges, that's where the phase formally closes for UBIQ-01..05; Plan 06 still defers ≥7 days.
  </action>
  <verify>
    <automated>test -f .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; grep -q 'triggered iCloud download' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; grep -q 'Run 1' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; grep -q 'Run 2' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; grep -q 'Driver-Attribution Audit' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; grep -q -E '(Merge Recommendation|MERGE)' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md &amp;&amp; git log --oneline -1 | grep -q 'UBIQ-03'</automated>
  </verify>
  <done>UAT log committed with both HEIC runs, Driver-Attribution Audit section confirming the native-API line fired on each, and merge recommendation; if recommendation is YES, the operator may merge `999.1-ubiquity-dry-run` → `main` at their discretion (out of scope for this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple iCloud daemon ↔ DailyBriefMonitor process | The entitlement gates whether `startDownloadingUbiquitousItem` actually queues a download or no-ops. UBIQ-03 is the only end-to-end proof. |
| iPhone → iCloud → Mac sync | External to this phase; failure here is not a phase regression but masks UBIQ-03 measurement. |
| brctl shim ↔ native API | Co-existing during UBIQ-03 test — Pitfall 4 race. Decision artifact for Plan 06. The risk of mis-attributing brctl's success to the native API is exactly why this plan locks the `triggered iCloud download for` log line as the PASS gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-17 | Tampering | UBIQ-03 PASS attributed to native API when brctl actually drove the materialization → Plan 06's "remove brctl" follow-up would break things | mitigate | Task 1/2 explicitly require the `FolderWatcherService: triggered iCloud download for` log line (native-API attribution) on each run. Materialization-by-brctl-alone is explicit FAIL. Task 3 Driver-Attribution Audit section makes this auditable. Plan 06 re-runs UBIQ-03 with brctl removed and rolls back if it breaks. |
| T-999.1-18 | Denial of service | Native API still no-op despite entitlement (Pitfall 1's hidden form — claimed but not runtime-authorized) | mitigate | Task 1 STOP-and-rollback gate on "no triggered iCloud download line within 30s" — even if the file materializes via brctl. |
| T-999.1-19 | Information disclosure | The HEIC content surfaces in PWA / vigil-core upload — but this is the EXPECTED behavior, not a regression | accept | UBIQ-03's whole point is the photo lands in Vigil. No new disclosure surface created. |
| T-999.1-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing — only drops files into a watched folder and reads logs. |
</threat_model>

<verification>
- Both HEIC drops produce `FolderWatcherService: triggered iCloud download for <filename>` within ~5s. (Native-API attribution — UBIQ-03 PASS gate.)
- Both drops materialize fully within ~10s — `mdls` shows full size, `xattr` shows no placeholder metadata.
- Both drops produce a PWA thought row within ~30s.
- No amfid kills of the monitor bundle during either drop window.
- UAT log written with explicit Driver-Attribution Audit section + merge recommendation.
</verification>

<success_criteria>
- UBIQ-03 closes with two clean PASSes — both runs showing native-API attribution via the `triggered iCloud download for` log line.
- The operator has a clear merge / no-merge recommendation backed by evidence.
- Plan 06 has the information it needs to decide whether brctl removal is worth pursuing (the brctl-also-fired observation from each run).
</success_criteria>

<output>
Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-SUMMARY.md` when done.
</output>
</content>
</invoke>