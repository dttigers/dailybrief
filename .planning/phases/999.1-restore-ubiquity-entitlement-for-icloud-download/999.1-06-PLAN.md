---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 06
type: execute
wave: 6
depends_on: [999.1-05]
files_modified:
  - Sources/DailyBriefMonitor/FolderWatcherService.swift
  - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md
  - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt
autonomous: false
requirements: [UBIQ-06]
must_haves:
  truths:
    - "DEFERRED until the operator confirms 999.1's UBIQ-01..05 changes have been live on `main` for ≥7 days without incident. This precondition is non-negotiable — RESEARCH.md is explicit that the brctl shim is the only fallback and removing it is reversible only by code revert + reinstall."
    - "After removal: dropping a fresh NotDownloaded HEIC into Notebook must still produce end-to-end materialization within 10s, with `triggered iCloud download for <filename>` as the SOLE driver — no `dispatched brctl download` log line."
    - "If the post-removal UBIQ-03 re-run FAILS, immediately `git revert` the brctl-removal commit, reinstall via `./Scripts/install.sh`, and mark UBIQ-06 as 'not pursued — brctl belt-and-suspenders retained.' Update STATE.md to reflect this disposition."
    - "Removal touches ONLY lines 357-376 of `Sources/DailyBriefMonitor/FolderWatcherService.swift` (the brctl block — PATTERNS.md confirms this range; RESEARCH had said 361-376 which was off-by-four). The `try?` ubiquity call on line 356 is RETAINED. The Belt-and-Suspenders comment on lines 353-355 is updated to reflect the new reality."
    - "The companion `triggerICloudDownloads()` path at lines 187-216 is NOT modified — RESEARCH says it's the cold path (hidden .icloud placeholders, which modern macOS emits less often than visible stubs)."
    - "Task 1's resume signal is recorded to `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt` (single-line file with one of `take-ubiq-06`, `defer-ubiq-06`, `defer-bake-not-yet`). Tasks 2/3/4/5 each read this file and gate BOTH their action AND their `<verify>` block on its contents. Missing file → loud fail; empty file → loud fail; mismatched signal → silent skip via `else true`. This is a filesystem-mediated contract — explicit and inspectable — replacing the env-var-mediated `TASK_1_SIGNAL` contract which depended on undocumented runner behavior."
    - "ROLLBACK RULE: same as all 999.1 plans — if anything breaks, `git revert HEAD && ./Scripts/install.sh` to restore the brctl block. If the rollback itself fails, `git checkout main && ./Scripts/install.sh` to fall back to pre-999.1 brctl-only state."
  artifacts:
    - path: "Sources/DailyBriefMonitor/FolderWatcherService.swift"
      provides: "Cleaned-up processFile: native ubiquity API only, brctl Process subprocess removed; comment block on lines 353-355 updated"
      contains: "Vigil 999.1"
    - path: ".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md"
      provides: "Decision log: 7-day bake confirmation, removal commit SHA, post-removal UBIQ-03 re-run result, final disposition (taken / not pursued)"
      contains: "7-day bake"
    - path: ".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt"
      provides: "Single-line signal file recording Task 1's resume signal verbatim. Read by Tasks 2/3/4/5's skip-guards as a filesystem-mediated dispatch contract."
      contains: "take-ubiq-06|defer-ubiq-06|defer-bake-not-yet"
  key_links:
    - from: "Sources/DailyBriefMonitor/FolderWatcherService.swift:356"
      to: "macOS fileproviderd / cloudd (no subprocess)"
      via: "FileManager.startDownloadingUbiquitousItem — sole driver after brctl removal"
      pattern: "startDownloadingUbiquitousItem"
    - from: "Task 1 (`<resume-signal>` block)"
      to: "uat-evidence/task-1-signal.txt"
      via: "Operator's resume signal echoed verbatim into the file as part of Task 1's `<action>` — belt-and-suspenders dual-write alongside the runner's resume-signal protocol."
      pattern: "task-1-signal\\.txt"
    - from: "Tasks 2/3/4/5 `<verify>` skip-guards"
      to: "uat-evidence/task-1-signal.txt"
      via: "Each verify reads the file via `$(< $SIGNAL_FILE)` after asserting `-f` and `-s`; missing → fail loud, empty → fail loud, mismatched → silent skip via `else true`."
      pattern: "task-1-signal\\.txt"
---

<objective>
Optional cleanup. Phase 999.1 ships fine without this plan — UBIQ-01..05 closing already retires the bug. UBIQ-06 removes the `/usr/bin/brctl` subprocess shim that was the Option B fix in `fff8a15` (2026-04-21), simplifying the file-watch hot path and removing the only undocumented-Apple-binary dependency in the Vigil daemon process.

This plan is DEFERRED until the operator confirms 999.1 has been live on `main` for ≥7 days without incident. The deferral is a real, non-negotiable gate — RESEARCH.md explicitly recommends `/usr/bin/brctl` remains as belt-and-suspenders OR is removed as a separate commit "so it can be reverted independently if ubiquity proves flaky." This plan IS that separate commit.

Purpose: simplify code, remove the one undocumented-Apple-binary fork in the file-watch path.
Output: 20 lines deleted from FolderWatcherService.swift; one decision log; either a clean merge OR a revert + "not pursued" disposition.

**Wave note:** Wave 6 (was Wave 5) after the cascade fix for Plan 02's wave correction.

**Skip-guard mechanism (checker warning #10, refined iteration 3 — filesystem-mediated):** Task 1 writes the operator's resume signal verbatim into `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt` (a single-line file containing one of `take-ubiq-06`, `defer-ubiq-06`, `defer-bake-not-yet`). Tasks 2/3/4/5 each read that file from their `<verify>` block's `<automated>` shell, asserting the file is present (`-f`) and non-empty (`-s`) before reading. Missing file → loud fail (`task-1-signal.txt missing — Task 1 must run before this task's verify`). Empty file → loud fail (`task-1-signal.txt is empty`). Mismatched signal (e.g., Task 2 wants `take-ubiq-06` but the file says `defer-ubiq-06`) → silent skip via `else true` — correct behavior, mirrors the original env-var-mediated design. This replaces the iteration-2 `${TASK_1_SIGNAL:-}` env-var idiom whose correctness depended on undocumented runner contract; the filesystem contract is explicit, inspectable (the operator can `cat` the file), and assertable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md
@Sources/DailyBriefMonitor/FolderWatcherService.swift
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm 7-day bake precondition + go/no-go decision + write signal file</name>
  <read_first>
    - .planning/STATE.md (find the date Plan 05's merge-to-main landed — that starts the bake clock)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md (the brctl-race observation from Plan 05 — informs whether removal is even attractive)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Don't Hand-Roll table — brctl is undocumented + Apple-removable; that's the "why bother" argument)
    - .planning/debug/resolved/icloud-photos-never-download.md (Option B context from the original diagnosis)
  </read_first>
  <action>
    Operator confirms ≥7 days have elapsed since 999.1's merge-to-main, reviews Plan 05's brctl-race observation, and chooses one of three paths via the `<resume-signal>` block: take-ubiq-06, defer-ubiq-06, or defer-bake-not-yet.

    AS PART OF THIS TASK'S COMPLETION (dual-write belt-and-suspenders), record the resume signal verbatim into a filesystem-mediated signal file so Tasks 2/3/4/5's verify blocks can read it deterministically (instead of relying on an undocumented `TASK_1_SIGNAL` env-var contract from the runner):

        SIGNAL_DIR=".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence"
        SIGNAL_FILE="$SIGNAL_DIR/task-1-signal.txt"
        mkdir -p "$SIGNAL_DIR"
        # Replace <RESUME_SIGNAL> below with the exact string the operator selected.
        # Must be exactly one of: take-ubiq-06, defer-ubiq-06, defer-bake-not-yet.
        printf '%s\n' '<RESUME_SIGNAL>' > "$SIGNAL_FILE"
        test -s "$SIGNAL_FILE" || { echo "FAIL: task-1-signal.txt is empty after write"; exit 1; }
        cat "$SIGNAL_FILE"

    The `uat-evidence/` subdirectory may already exist from Plan 02 Task 1 (which uses it for portal-UAT screenshots). The `mkdir -p` above is idempotent — it neither errors nor clobbers existing content. The signal file itself is gitignore-eligible if the operator prefers it stay local; default disposition is to leave it untracked (it's a runner-internal handoff, not a permanent artifact). If a gitignore line needs to be added, add `uat-evidence/task-1-signal.txt` to `.gitignore` — but check first whether `.planning/` is already ignored wholesale; if so, no .gitignore change is needed.

    After writing the file, the `<resume-signal>` block below still fires (the runner protocol is unchanged — the filesystem write is in ADDITION to the runner's resume-signal capture, not a replacement). Tasks 2/3/4/5 will read the file from disk; the runner's TASK_1_SIGNAL env var is no longer consulted.
  </action>
  <decision>Take UBIQ-06 (remove the brctl block) OR leave brctl as belt-and-suspenders.</decision>
  <context>
    UBIQ-01..05 already closed the original bug. UBIQ-06 is purely about code simplicity + reducing dependency on undocumented Apple internals. The cost is one revert if it breaks.

    Hard precondition: at least 7 days MUST have elapsed since Plan 05's merge-to-main landed. The bake clock starts on the merge commit date. If <7 days, this plan does NOT run yet — the operator should defer and re-pick-up later.

    Soft signals from Plan 05:
    - If `dispatched brctl download` ALSO fired on both UBIQ-03 runs → brctl is racing the native call but is harmless → removal is attractive (simplification with proven-native-works evidence).
    - If brctl only no-op'd silently (no log line) → brctl is already off the hot path because native materializes the file first → removal is purely cosmetic.
    - Either way: low risk, low reward. The 7-day bake confirms no surprise regressions.
  </context>
  <options>
    <option id="take-ubiq-06">
      <name>Take UBIQ-06: remove brctl block</name>
      <pros>One fewer subprocess fork in the file-watch hot path. No dependency on `/usr/bin/brctl` (undocumented; Apple has removed similar utilities). ~20 lines of code deleted.</pros>
      <cons>If macOS or iCloud infra has a future regression in `startDownloadingUbiquitousItem`, the belt-and-suspenders is gone. Requires a re-run of UBIQ-03 (~5 min hands-on) to confirm post-removal behavior. Easily revertible if it breaks.</cons>
    </option>
    <option id="defer-ubiq-06">
      <name>Defer UBIQ-06 indefinitely — leave brctl in place</name>
      <pros>Zero risk. Belt-and-suspenders preserved. No additional UAT round needed. Phase 999.1 can be marked complete without this plan ever executing.</pros>
      <cons>Code carries a `/usr/bin/brctl` Process subprocess that's now redundant 99% of the time (when native API has the entitlement). Future readers may not understand why brctl is still there.</cons>
    </option>
    <option id="defer-bake-not-yet">
      <name>Bake not yet complete — re-run this checkpoint later</name>
      <pros>Honors the 7-day rule. Catches any latent regression in 999.1's main-branch behavior before stacking another change on top.</pros>
      <cons>None — this is the safety case.</cons>
    </option>
  </options>
  <how-to-verify>
    Before resuming downstream tasks, the operator should confirm the signal file was written correctly:

    1. `test -s .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt` — must exit 0 (file exists AND is non-empty).
    2. `cat .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt` — must print exactly one of: `take-ubiq-06`, `defer-ubiq-06`, `defer-bake-not-yet` (single line, no leading/trailing whitespace beyond a single trailing newline from `printf '%s\n'`).
    3. The printed value must match the resume-signal the operator typed.

    If any of those three checks fail, the downstream tasks will halt loudly (their verify blocks assert `-f` and `-s` on the file). Re-run Task 1's `printf` block to fix.
  </how-to-verify>
  <resume-signal>Type "take-ubiq-06" (proceed with removal), "defer-ubiq-06" (mark phase complete, this plan never executes), or "defer-bake-not-yet" (come back in N days). The runner captures this verbatim as its resume signal; Task 1's `<action>` ALSO writes this exact value to `uat-evidence/task-1-signal.txt`. Tasks 2/3/4/5 read the file (not the env var). If "defer-ubiq-06" or "defer-bake-not-yet", Tasks 2-4 skip their action AND their verify (verify becomes no-op `true`), and Task 5 writes the "not pursued" disposition.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create branch for removal + delete brctl block at FolderWatcherService.swift:357-376</name>
  <read_first>
    - Sources/DailyBriefMonitor/FolderWatcherService.swift (lines 340-380 — the processFile ubiquity branch in full; identify the exact byte range to delete)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md (FolderWatcherService section — corrected line range 357-376, current code to delete spelled out, comment cleanup wording to apply at lines 353-355)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Pitfall 4 — explicit "delete entirely, don't leave commented out")
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt (Task 1's signal file — read its contents to know whether to proceed)
  </read_first>
  <action>
    SKIP-GUARD: Read the resume signal from `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt`. If the file is missing or empty, ABORT loudly — Task 1 must run first. If the file's contents (trimmed) are anything other than `take-ubiq-06`, skip this entire task — do not create the branch, do not edit any file. Print `"Task 2 skipped — signal=$(< uat-evidence/task-1-signal.txt)"` and exit Task 2 cleanly.

    Otherwise (signal == "take-ubiq-06"):

    Create a fresh branch from `main`: `git checkout main && git pull && git checkout -b 999.1-ubiq-06-brctl-removal`. (NOT `999.1-ubiquity-dry-run` — that branch was the dry-run for the main 999.1 work and was merged ≥7 days ago; this is a separate, smaller change.)

    Open `Sources/DailyBriefMonitor/FolderWatcherService.swift`. Find the brctl block — PATTERNS.md confirms the range is lines 357-376 (the `// Fallback: shell out to brctl download` comment through the closing `}` of the inner `do/catch`). The line numbers may have shifted slightly if 999.1's NSLog addition affected them — confirm by searching for the exact string `let brctlProcess = Process()` and `brctlProcess.executableURL = URL(fileURLWithPath: "/usr/bin/brctl")`. Delete every line from the `// Fallback:` comment through the final closing `}` of the brctl `do/catch` — NOT the `return` statement that follows (which still belongs to the `if status != .current` block).

    Update the comment block on lines 353-355 from:
    ```
    // Belt-and-suspenders: call the API first (no-ops without the
    // com.apple.developer.ubiquity-container-identifiers entitlement,
    // but kept in case Option A is applied later).
    ```
    to (per PATTERNS.md):
    ```
    // Trigger iCloud materialization. With the
    // com.apple.developer.ubiquity-container-identifiers entitlement
    // restored in Phase 999.1, this is the actual driver (no longer
    // a no-op). VNODE_WRITE on materialization re-queues the file.
    ```

    Do NOT touch the companion `triggerICloudDownloads()` path at lines 187-216 — RESEARCH says that's the cold path for hidden `.icloud` placeholders and remains useful. Do NOT delete the `try? FileManager.default.startDownloadingUbiquitousItem(at: url)` call (line 356 — the actual driver).

    Run `swift build -c release` to confirm the file still compiles. If compilation fails (probably because a brace got deleted unintentionally), `git checkout -- Sources/DailyBriefMonitor/FolderWatcherService.swift` and try again with a smaller diff.

    Confirm the brctl reference count went to ZERO in this file: `grep -c brctl Sources/DailyBriefMonitor/FolderWatcherService.swift` should return 0 (no remaining mentions of brctl, brctlProcess, or `/usr/bin/brctl`).
  </action>
  <verify>
    <automated>SIGNAL_FILE=".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt"; [[ -f "$SIGNAL_FILE" ]] || { echo "task-1-signal.txt missing — Task 1 must run before this task's verify"; exit 1; }; TASK_1_SIGNAL=$(< "$SIGNAL_FILE"); TASK_1_SIGNAL="${TASK_1_SIGNAL//[$'\t\r\n ']/}"; [[ -n "$TASK_1_SIGNAL" ]] || { echo "task-1-signal.txt is empty"; exit 1; }; if [[ "$TASK_1_SIGNAL" == "take-ubiq-06" ]]; then git rev-parse --abbrev-ref HEAD | grep -qx '999.1-ubiq-06-brctl-removal' &amp;&amp; ! grep -q 'brctl' Sources/DailyBriefMonitor/FolderWatcherService.swift &amp;&amp; grep -q 'startDownloadingUbiquitousItem' Sources/DailyBriefMonitor/FolderWatcherService.swift &amp;&amp; grep -q 'Phase 999.1' Sources/DailyBriefMonitor/FolderWatcherService.swift &amp;&amp; swift build -c release 2>&amp;1 | tail -3 | grep -q -E '(Build complete|Compiling)'; else true; fi</automated>
  </verify>
  <done>Either (a) signal file contains `take-ubiq-06` AND branch `999.1-ubiq-06-brctl-removal` exists, brctl block fully deleted, comment updated, `swift build -c release` clean; OR (b) signal file contains a `defer-*` value AND this task was skipped cleanly (verify returned 0 via the no-op `true` branch). Verify ALSO fails loud if signal file is missing or empty — the file-based contract is explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Reinstall + re-run UBIQ-03 hardware test</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md (the UBIQ-03 procedure — same drop test, same observations)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh (still applies — UBIQ-01/02/04/05 must still PASS post-removal; final-success literal is `verify-ubiquity: all checks passed` per Plan 01 contract)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt (read to determine whether to run or skip)
  </read_first>
  <action>
    SKIP-GUARD: Read the resume signal from `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt`. If the file is missing or empty, ABORT loudly. If the contents (trimmed) are anything other than `take-ubiq-06`, skip this entire task. Print `"Task 3 skipped — signal=$(< uat-evidence/task-1-signal.txt)"` and exit cleanly.

    Otherwise (signal == "take-ubiq-06"):

    Run `./Scripts/install.sh` to install the brctl-less build. Wait 10s for the LaunchAgent to settle.

    Re-run the probe sweep: `bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --full`. Must exit 0 AND end with the literal `verify-ubiquity: all checks passed` (Plan 01 Task 2 contract) — UBIQ-01..05 must all still PASS (removing brctl shouldn't have touched any of them, but verify).

    Re-run UBIQ-03: drop a fresh HEIC into `~/Library/Mobile Documents/com~apple~CloudDocs/Notebook/`. Watch `~/Library/Logs/DailyBrief/monitor-stderr.log`. Expectations:
    - `FolderWatcherService: triggered iCloud download for <filename>` — must appear within 5s.
    - `FolderWatcherService: dispatched brctl download for <filename>` — must NOT appear (we just deleted the code that emits this line).
    - The HEIC materializes fully within ~10s.
    - The thought row appears in the PWA within ~30s.

    Repeat with a second HEIC to confirm the first wasn't a fluke.

    If EITHER drop fails (no `triggered iCloud download` line within 30s, or materialization stalls): `git revert HEAD` (the brctl-removal commit) immediately, then `./Scripts/install.sh` to restore the brctl-bearing build. Record the failure in Task 5's disposition log. This is the "not pursued" path.

    If both drops PASS: commit on `999.1-ubiq-06-brctl-removal` with message `refactor(999.1): remove brctl shim — native ubiquity API is the sole driver`. Print the commit SHA — Task 5 records it.

    Do NOT push or merge yet — Task 5 records the disposition and then the operator decides on the merge.
  </action>
  <verify>
    <automated>SIGNAL_FILE=".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt"; [[ -f "$SIGNAL_FILE" ]] || { echo "task-1-signal.txt missing — Task 1 must run before this task's verify"; exit 1; }; TASK_1_SIGNAL=$(< "$SIGNAL_FILE"); TASK_1_SIGNAL="${TASK_1_SIGNAL//[$'\t\r\n ']/}"; [[ -n "$TASK_1_SIGNAL" ]] || { echo "task-1-signal.txt is empty"; exit 1; }; if [[ "$TASK_1_SIGNAL" == "take-ubiq-06" ]]; then bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --full 2>&amp;1 | tail -1 | grep -qx 'verify-ubiquity: all checks passed' &amp;&amp; ! grep -E 'dispatched brctl download' ~/Library/Logs/DailyBrief/monitor-stderr.log | tail -100 | grep -q . &amp;&amp; grep -E 'triggered iCloud download for' ~/Library/Logs/DailyBrief/monitor-stderr.log | tail -10 | grep -q .; else true; fi</automated>
  </verify>
  <done>Either (a) signal == `take-ubiq-06` AND verify-ubiquity.sh ends with literal `verify-ubiquity: all checks passed`, two fresh HEIC drops both materialized via native API alone with no brctl log lines; OR (b) signal contains a `defer-*` value AND this task was skipped cleanly. Verify ALSO fails loud if signal file is missing or empty.</done>
</task>

<task type="auto">
  <name>Task 4: Write decision log (taken path)</name>
  <read_first>
    - Output from Task 2 (commit SHA) and Task 3 (probe sweep + HEIC drop evidence)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt (read to determine whether to run or skip)
  </read_first>
  <action>
    SKIP-GUARD: Read the resume signal from `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt`. If the file is missing or empty, ABORT loudly. If the contents (trimmed) are anything other than `take-ubiq-06`, skip this task — Task 5 handles the defer path. Print `"Task 4 skipped — signal=$(< uat-evidence/task-1-signal.txt)"` and exit cleanly.

    Otherwise (signal == "take-ubiq-06"):

    Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md` with sections:

    - **Header** — phase 999.1, plan 06, date, branch `999.1-ubiq-06-brctl-removal`, removal commit SHA.
    - **7-Day Bake Confirmation** — date 999.1 was merged to main (from STATE.md), today's date, elapsed days (must be ≥7).
    - **Pre-Removal Snapshot** — note the brctl-also-fired observation from Plan 05's HEIC UAT (Yes/No).
    - **Removal Diff Summary** — `git diff --stat HEAD~1 HEAD` showing the 20-ish lines deleted from FolderWatcherService.swift, plus the comment-block update.
    - **Post-Removal Probe Sweep** — verify-ubiquity.sh --full output (must end with literal `verify-ubiquity: all checks passed`).
    - **Post-Removal HEIC Runs** — Run 1 + Run 2 evidence (filename, timestamp, log excerpts confirming `triggered iCloud download` is the SOLE driver — no brctl line).
    - **Disposition** — `taken` (clean PASS).
    - **Merge Recommendation** — yes/no on merging `999.1-ubiq-06-brctl-removal` to main. Yes if both HEIC drops PASS without any brctl log lines.

    Commit on `999.1-ubiq-06-brctl-removal` with message `docs(999.1): UBIQ-06 brctl removal — post-removal UBIQ-03 PASS`. Then the operator merges to main at their discretion.
  </action>
  <verify>
    <automated>SIGNAL_FILE=".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt"; [[ -f "$SIGNAL_FILE" ]] || { echo "task-1-signal.txt missing — Task 1 must run before this task's verify"; exit 1; }; TASK_1_SIGNAL=$(< "$SIGNAL_FILE"); TASK_1_SIGNAL="${TASK_1_SIGNAL//[$'\t\r\n ']/}"; [[ -n "$TASK_1_SIGNAL" ]] || { echo "task-1-signal.txt is empty"; exit 1; }; if [[ "$TASK_1_SIGNAL" == "take-ubiq-06" ]]; then test -f .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md &amp;&amp; grep -q '7-Day Bake' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md &amp;&amp; grep -q 'Disposition' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md &amp;&amp; grep -E 'taken|not pursued' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md | head -1 | grep -q .; else true; fi</automated>
  </verify>
  <done>Either (a) signal == `take-ubiq-06` AND removal-taken decision log committed on the removal branch with clear merge recommendation; OR (b) signal contains a `defer-*` value AND this task was skipped cleanly (Task 5 covers the defer path). Verify ALSO fails loud if signal file is missing or empty.</done>
</task>

<task type="auto">
  <name>Task 5: Write "not pursued" disposition log (deferred path)</name>
  <read_first>
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt (read to determine whether to run or skip — this task runs ONLY when signal == `defer-ubiq-06`)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-05-HEIC-UAT.md (whether the brctl-race observation is even attractive enough to revisit)
  </read_first>
  <action>
    SKIP-GUARD: Read the resume signal from `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt`. If the file is missing or empty, ABORT loudly. Run this task ONLY if the contents (trimmed) are exactly `defer-ubiq-06` (explicit defer; not the "bake not yet" temporal defer — that one comes back later and re-runs Task 1; and NOT the take path, which uses Task 4). If the signal is `take-ubiq-06` or `defer-bake-not-yet`, skip this task and exit cleanly (verify becomes no-op `true`).

    Otherwise (signal == "defer-ubiq-06"):

    Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md` on `main` (no special branch) with:

    - **Header** — phase 999.1, plan 06, date, disposition `not pursued`.
    - **Bake Confirmation** — date 999.1 merged + today + elapsed days (still must be ≥7 to make a defer decision — if <7, this section instead says "bake-not-yet, revisit YYYY-MM-DD").
    - **Rationale** — operator's reason for declining. Standard reasons: "Belt-and-suspenders preferred; no observed brctl noise on main; not worth a re-UAT round." OR "Code change low-value relative to risk."
    - **Future Re-Open Conditions** — what would change the operator's mind. E.g., "Apple removes /usr/bin/brctl in a future macOS version" — in that case, this plan is revived and Task 1 returns `take-ubiq-06`.

    Stage and commit on `main` directly (this is a docs-only update with no code change) with message: `docs(999.1): UBIQ-06 — not pursued; brctl belt-and-suspenders retained`. Do NOT push without operator confirmation.

    Mark the phase as complete in STATE.md and ROADMAP.md (this is a follow-up update — typically /gsd:verify-work or a similar phase-closeout command handles this, but if not, add a one-line `UBIQ-06: not pursued (Plan 06 disposition 2026-MM-DD)` under the Phase 999.1 entry).
  </action>
  <verify>
    <automated>SIGNAL_FILE=".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/uat-evidence/task-1-signal.txt"; [[ -f "$SIGNAL_FILE" ]] || { echo "task-1-signal.txt missing — Task 1 must run before this task's verify"; exit 1; }; TASK_1_SIGNAL=$(< "$SIGNAL_FILE"); TASK_1_SIGNAL="${TASK_1_SIGNAL//[$'\t\r\n ']/}"; [[ -n "$TASK_1_SIGNAL" ]] || { echo "task-1-signal.txt is empty"; exit 1; }; if [[ "$TASK_1_SIGNAL" == "defer-ubiq-06" ]]; then test -f .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md &amp;&amp; grep -q 'not pursued' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-BRCTL-REMOVAL.md; else true; fi</automated>
  </verify>
  <done>Either (a) signal == `defer-ubiq-06` AND disposition log explains the defer; STATE.md reflects UBIQ-06's not-pursued status; OR (b) signal == `take-ubiq-06` or `defer-bake-not-yet` AND this task was skipped cleanly. Verify ALSO fails loud if signal file is missing or empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native API ↔ macOS file-coordination | After brctl removal, this is the ONLY path triggering downloads. Any future macOS regression here cannot be papered over by brctl. |
| dry-run branch (999.1-ubiq-06-brctl-removal) ↔ main | Removal lands as a separate commit specifically so it can be reverted independently. RESEARCH-recommended pattern. |
| Task 1 resume signal ↔ Tasks 2–5 skip-guards | The signal is now mediated by a filesystem file (`uat-evidence/task-1-signal.txt`) instead of an undocumented env-var contract. Tasks 2/3/4/5's verify blocks assert `-f` and `-s` on the file (fail loud) and then read its contents for the dispatch decision. Mis-capture (operator types the wrong literal) would either execute removal when defer was intended (bad — but Task 2's branch creation is from `main` and revertible) or skip when take was intended (recoverable — re-run Task 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-20 | Denial of service | Future macOS change breaks `startDownloadingUbiquitousItem` for Developer ID apps → no fallback after brctl removal | accept | Plan is reversible; `git revert` restores brctl. The probability is low (Apple has not changed this mechanism in any documented release per RESEARCH A7). |
| T-999.1-21 | Tampering | Brace mismatch during delete corrupts adjacent `if status != .current` block | mitigate | Task 2 explicit `swift build -c release` gate. If compile fails, checkout-and-retry. |
| T-999.1-22 | Repudiation | UBIQ-06 disposition (taken / not pursued) lost over time | mitigate | Task 4 or Task 5 records the decision in a tracked .md; STATE.md is updated. |
| T-999.1-23 | Tampering | Skipped task's verify fires against missing artifacts → false-FAIL on the runner → operator misreads the plan as broken | mitigate | Tasks 2/3/4/5 verify blocks read `uat-evidence/task-1-signal.txt` and wrap the original verify in `if [[ "$TASK_1_SIGNAL" == "<expected>" ]]; then <original>; else true; fi`. Skipped tasks return 0 cleanly via `else true`. |
| T-999.1-23b | Tampering | Iteration-2 design used `${TASK_1_SIGNAL:-}` env-var idiom; if the runner does not propagate Task 1's resume signal as a shell variable, all four downstream verifies silently pass (empty string ≠ expected literal → `else true`) — masking a real planning failure. | mitigate | Iteration 3 replaces the env-var contract with a filesystem-mediated contract: Task 1 writes `uat-evidence/task-1-signal.txt`; Tasks 2/3/4/5 read the file with explicit `-f`/`-s` assertions that FAIL LOUD on missing or empty signal. The contract is now inspectable (operator can `cat` the file) and assertable (the runner's behavior is not on the trusted path). |
| T-999.1-24 | Information disclosure | task-1-signal.txt accidentally committed to git, leaking the operator's decision into history | accept | The signal value is one of three known literals — `take-ubiq-06`, `defer-ubiq-06`, `defer-bake-not-yet`. No secrets. If `.planning/` is git-tracked, the file may be tracked; that's acceptable since the value is decision-log material anyway. If the operator prefers it stay untracked, add `uat-evidence/task-1-signal.txt` to `.gitignore`. |
| T-999.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager changes. brctl removal is a pure code delete. |
</threat_model>

<verification>
- (If taken) `grep -c brctl Sources/DailyBriefMonitor/FolderWatcherService.swift` returns 0.
- (If taken) `swift build -c release` exits 0.
- (If taken) verify-ubiquity.sh --full exits 0 post-removal AND ends with literal `verify-ubiquity: all checks passed`.
- (If taken) UBIQ-03 re-run shows native log line, no brctl log line, materialization within 10s, twice.
- (Either disposition) `999.1-06-BRCTL-REMOVAL.md` exists with explicit `taken` or `not pursued` keyword.
- (All paths) `uat-evidence/task-1-signal.txt` exists, is non-empty, and contains exactly one of `take-ubiq-06`, `defer-ubiq-06`, `defer-bake-not-yet` (modulo trailing newline from `printf '%s\n'`).
- All skipped tasks' verifies return 0 via the `else true` branch — no false-FAIL on the deferred path. Missing or empty signal file → loud fail on every downstream verify (the file-mediated contract makes the dispatch state inspectable and assertable, unlike iteration 2's undocumented env-var contract).
</verification>

<success_criteria>
- The brctl shim is either gone (UBIQ-06 taken) or explicitly retained with documented rationale (UBIQ-06 not pursued).
- Either outcome closes UBIQ-06 — the choice itself, supported by evidence, is the artifact.
- STATE.md reflects the final disposition so future contributors don't re-open the question.
- The filesystem-mediated skip-guard mechanism (`uat-evidence/task-1-signal.txt`-gated action AND verify) cleanly handles defer paths without surfacing spurious verify failures, and fails loud if Task 1 was never run.
</success_criteria>

<output>
Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-06-SUMMARY.md` when done.
</output>
