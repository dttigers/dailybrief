---
phase: 999.1-restore-ubiquity-entitlement-for-icloud-download
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh
  - .gitignore
  - Entitlements/embedded.provisionprofile.example.txt
autonomous: true
requirements: [UBIQ-01, UBIQ-02, UBIQ-04, UBIQ-05]
must_haves:
  truths:
    - "Throwaway branch `999.1-ubiquity-dry-run` exists locally and is checked out before any other plan in this phase runs."
    - "Running `bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --static` prints PASS/FAIL lines for UBIQ-01, UBIQ-04 and exits non-zero today (no install yet) without crashing."
    - "Running `... verify-ubiquity.sh --runtime` prints PASS/FAIL lines for UBIQ-02, UBIQ-05 and exits non-zero today without crashing."
    - "`Entitlements/*.provisionprofile` is git-ignored — `git status` after dropping a fake file at `Entitlements/embedded.provisionprofile` shows the file as untracked-and-ignored, not staged-or-listed."
    - "ROLLBACK RULE (all plans): if `codesign --verify` fails or `launchctl print` shows the agent crashing post-install, run `git checkout main && ./Scripts/install.sh` to restore the brctl-only working state. Do not attempt forward fixes on the dry-run branch."
  artifacts:
    - path: ".planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh"
      provides: "UBIQ-01/02/04/05 probe runner — static + runtime modes, red/green helpers, FAIL counter, non-zero exit on any check fail"
      contains: "check_ubiq_01_entitlement_embedded"
    - path: ".gitignore"
      provides: "Entitlements/*.provisionprofile glob ignore line"
      contains: "Entitlements/*.provisionprofile"
    - path: "Entitlements/embedded.provisionprofile.example.txt"
      provides: "Placeholder doc so fresh clones surface the missing-profile gate loudly"
      contains: "developer.apple.com"
  key_links:
    - from: ".planning/phases/999.1-.../probes/verify-ubiquity.sh"
      to: "Scripts/verify-phase-114.sh"
      via: "structural copy — shebang, set -euo pipefail, SCRIPT_DIR/REPO_ROOT, red/green/info helpers, MODE dispatch, FAIL counter"
      pattern: "set -euo pipefail"
    - from: ".gitignore"
      to: "Entitlements/embedded.provisionprofile"
      via: "glob ignore line `Entitlements/*.provisionprofile`"
      pattern: "Entitlements/\\*\\.provisionprofile"
---

<objective>
Stand up Phase 999.1's safety net BEFORE touching production signing. This plan creates the throwaway branch, the executable probe runner, the gitignore rule for the (not-yet-downloaded) provisioning profile, and a placeholder doc so fresh clones don't silently fail later. Nothing in this plan changes the build, the entitlements, or the install pipeline — it is pure infrastructure that the remaining five plans depend on.

Purpose: Phase 58 hit signing regressions specifically because there was no isolated dry-run path; this plan creates that path so Plans 03–05 can fail safely.
Output: a working `verify-ubiquity.sh` that today exits non-zero (no install yet) but never crashes, a gitignore entry that prevents the profile from being committed by accident, and a checked-out `999.1-ubiquity-dry-run` branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md
@.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md
@Scripts/verify-phase-114.sh
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create throwaway branch `999.1-ubiquity-dry-run` and the probes/ scaffold</name>
  <read_first>
    - .planning/STATE.md (confirm current branch is `main`)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Wave 0 Requirements section, especially "Throwaway branch" + "Rollback rehearsal" items)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Summary section — explicit "do not touch main until Checkpoint 5" guidance; "Recommended Branch / Working Structure" tree)
  </read_first>
  <action>
    From the repository root: confirm `git status` is clean and the current branch is `main` (abort if not — do NOT stash). Create branch `999.1-ubiquity-dry-run` from `main` and check it out (`git checkout -b 999.1-ubiquity-dry-run`). Create the directory `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/` (empty for now — Task 2 fills it). Do NOT push the branch to any remote. Per RESEARCH.md (Summary, "Recommended Branch / Working Structure"): all 999.1 work lives on this branch until UBIQ-01..05 are all green; the operator will run a `git checkout main && ./Scripts/install.sh` rollback rehearsal as part of Plan 02's UAT. This task does not commit anything — Task 2 produces the first commit.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -qx '999.1-ubiquity-dry-run' &amp;&amp; test -d .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes</automated>
  </verify>
  <done>HEAD is on branch `999.1-ubiquity-dry-run`; `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/` directory exists; working tree is clean (no staged or unstaged changes yet).</done>
</task>

<task type="auto">
  <name>Task 2: Author `probes/verify-ubiquity.sh` aggregating UBIQ-01/02/04/05 checks</name>
  <read_first>
    - Scripts/verify-phase-114.sh (the structural analog — lines 1-30 for skeleton, 29-50 for per-check function shape, 238-260 for MODE dispatch + exit footer)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md (verify-ubiquity.sh section — the per-check-function table mapping function names to probe commands)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-VALIDATION.md (Per-Requirement Verification Map — exact `codesign -d --entitlements -`, `codesign --verify --verbose`, `spctl -a -vv`, `launchctl print`, and `log show` predicates with their expected-output strings)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Common Pitfalls — to write the FAIL-mode hints printed by each red() line; Validation Architecture — Phase Requirements → Test Map for the canonical probe commands)
  </read_first>
  <action>
    Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh` (file mode 0755). Mirror the structural pattern of `Scripts/verify-phase-114.sh` exactly: `#!/usr/bin/env bash` shebang, `set -euo pipefail`, SCRIPT_DIR/REPO_ROOT idiom, `red()`/`green()`/`info()` helpers, `MODE="${1:---static}"` dispatch, `FAIL=0` counter, exit-0-on-all-pass / exit-1-on-any-fail footer. The info-prefix is `[verify-ubiquity]` (not `[verify-114]`). Define `MONITOR_APP="$HOME/.local/bin/DailyBriefMonitor.app"`, `MONITOR_LABEL="com.jamesonmorrill.dailybriefmonitor"`, `MONITOR_LOG="$HOME/Library/Logs/DailyBrief/monitor-stderr.log"`, `CONTAINER_ID_LITERAL="iCloud.com.jamesonmorrill.dailybriefmonitor"` (per the PATTERNS-locked "no `$(TeamIdentifierPrefix)`" rule).

    Implement these check functions, in this order (PATTERNS.md verify-ubiquity.sh section is the authoritative per-check table):

    - `check_ubiq_01_entitlement_embedded` — `codesign -d --entitlements - "$MONITOR_APP" 2>&amp;1 | grep -q 'com.apple.developer.ubiquity-container-identifiers'`; on grep miss, red() the failure with hint "ubiquity entitlement missing — did install.sh pass the right --entitlements file?". Also assert the literal container string is present: pipe the same output to `grep -q "$CONTAINER_ID_LITERAL"` and fail with the "literal not $(TeamIdentifierPrefix) macro" hint (per RESEARCH Pitfall 2).
    - `check_ubiq_04_codesign_verify` — `codesign --verify --verbose "$MONITOR_APP" 2>&amp;1 | grep -q 'valid on disk'`. Hint on miss: "signature broken — most likely embedded.provisionprofile missing or added after `codesign --deep`" (RESEARCH Pitfall 3).
    - `check_ubiq_04b_spctl_accept` — `spctl -a -vv "$MONITOR_APP" 2>&amp;1 | grep -q 'accepted'`. Hint on miss: "Gatekeeper rejected — notarization regression or invalid Developer ID identity".
    - `check_ubiq_05_launchd_state` — `launchctl print "gui/$(id -u)/$MONITOR_LABEL" 2>/dev/null | grep -q 'state = running'`. Hint on miss: "LaunchAgent not running — `launchctl print | grep -E '(state|last exit)'` and `Console.app` filter on amfid for `-67050` kills (RESEARCH Pitfall 1)".
    - `check_ubiq_05b_no_amfid_kills` — `log show --predicate 'process == "amfid"' --last 5m 2>/dev/null | grep "$MONITOR_LABEL"` must return NO matches; if any match present, FAIL with the amfid output. (Negative test — `grep` exits 1 on no-match which is what we want; use `if grep ... ; then FAIL=1`, not `|| FAIL=1`.)
    - `check_ubiq_02_startup_log` — `grep "Vigil: ubiquity container = /Users" "$MONITOR_LOG"` (most-recent-line). Hint on miss: "no startup ubiquity-URL log — either the binary hasn't run since the NSLog was added (Plan 03 adds it) or the URL came back nil (UBIQ-02 fail — entitlement not authorized)".

    `MODE` dispatch:
    - `--static` → runs `check_ubiq_01_entitlement_embedded` + `check_ubiq_04_codesign_verify` + `check_ubiq_04b_spctl_accept` (these only need the .app on disk).
    - `--runtime` → runs `check_ubiq_05_launchd_state` + `check_ubiq_05b_no_amfid_kills` + `check_ubiq_02_startup_log` (need the LaunchAgent loaded + logs flowing).
    - `--full` (default if no arg) → runs both.

    Use `chmod +x` on the file. Do NOT add a fence around bash commands in the comments — write idiomatic bash. CRITICAL: each grep gate must be `grep -q` not bare `grep`, and AMFID/log greps must filter out comment lines if the source could contain them (it doesn't here — log files don't have shell comments — but use `grep -v '^#'` defensively when grepping any source file with the literal `MONITOR_LABEL` in case PATTERNS gets piped through).

    Commit with message: `chore(999.1): add verify-ubiquity.sh probe runner (UBIQ-01/02/04/05)` after Task 3 completes (single commit covers Tasks 2+3+4).
  </action>
  <verify>
    <automated>test -x .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh &amp;&amp; bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --static; rc=$?; test "$rc" -ne 0 &amp;&amp; grep -q 'check_ubiq_01_entitlement_embedded' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh &amp;&amp; grep -q 'check_ubiq_05b_no_amfid_kills' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh &amp;&amp; grep -q 'iCloud.com.jamesonmorrill.dailybriefmonitor' .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh</automated>
  </verify>
  <done>verify-ubiquity.sh exists, is executable, runs without crashing under `--static` (exits non-zero because no .app is installed yet — that is correct), and contains every check function name from the PATTERNS.md table plus the literal container ID string.</done>
</task>

<task type="auto">
  <name>Task 3: Add `Entitlements/*.provisionprofile` to `.gitignore` + check in placeholder doc</name>
  <read_first>
    - .gitignore (current contents — lines 1-37; the `.env`-family and `glasses_*.png` precedents are the structural patterns to mirror)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-RESEARCH.md (Pitfall 7 — full rationale for gitignoring the profile + placeholder strategy)
    - .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-PATTERNS.md (.gitignore section — exact append-line shape and the optional Phase-124-style comment block)
  </read_first>
  <action>
    Edit `.gitignore` to append (after the existing `glasses_*.png` block around line 37):

```
# Provisioning profiles — bind to one developer account + embed Team ID.
# Generated by developer.apple.com, never check in. See Phase 999.1.
Entitlements/*.provisionprofile
```

    Then create `Entitlements/embedded.provisionprofile.example.txt` (this file IS checked in — it documents what's expected at `Entitlements/embedded.provisionprofile`, which is gitignored). Contents must include:
    - A one-line summary: "Phase 999.1 — Developer ID Provisioning Profile for ubiquity-container-identifiers entitlement."
    - The full developer.apple.com portal flow numbered 1–5, copied verbatim from RESEARCH.md "Installation (developer machine):" lines 60-65 (login → Identifiers → iCloud Containers → register `iCloud.com.jamesonmorrill.dailybriefmonitor` → assign to App ID `com.jamesonmorrill.dailybriefmonitor` → Profiles → `+` → Distribution → Developer ID → download → save as `Entitlements/embedded.provisionprofile`).
    - The expected file location: literal `Entitlements/embedded.provisionprofile`.
    - A sanity-check one-liner: `security cms -D -i Entitlements/embedded.provisionprofile | plutil -p - | grep ubiquity` (per RESEARCH Pitfall 5).
    - A rollback footnote: if anything breaks, `git checkout main && ./Scripts/install.sh` (the Phase 58 lesson, repeated here so the operator sees it the first time they touch this file).

    Plain text only — no markdown formatting required, but headings/numbering are fine.
  </action>
  <verify>
    <automated>grep -q '^Entitlements/\*\.provisionprofile$' .gitignore &amp;&amp; test -f Entitlements/embedded.provisionprofile.example.txt &amp;&amp; grep -q 'developer.apple.com' Entitlements/embedded.provisionprofile.example.txt &amp;&amp; grep -q 'iCloud.com.jamesonmorrill.dailybriefmonitor' Entitlements/embedded.provisionprofile.example.txt</automated>
  </verify>
  <done>`.gitignore` contains the new ignore line + its comment header; `Entitlements/embedded.provisionprofile.example.txt` exists and documents the portal flow; touching `Entitlements/embedded.provisionprofile` (test only, do not actually create it) is confirmed to be ignored.</done>
</task>

<task type="auto">
  <name>Task 4: Sanity-check the ignore rule with a throwaway file, then commit Plan 01</name>
  <read_first>
    - .gitignore (verify the new lines from Task 3 landed)
  </read_first>
  <action>
    Verify the ignore rule actually works (defense against typos): run `git check-ignore -v Entitlements/embedded.provisionprofile`. Expected: exit 0 with output naming `.gitignore:N:Entitlements/*.provisionprofile`. If the command exits non-zero, the glob is wrong — fix the `.gitignore` line.

    Stage and commit all Plan 01 work in a single commit on `999.1-ubiquity-dry-run`:
    - `.gitignore`
    - `Entitlements/embedded.provisionprofile.example.txt`
    - `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh`
    - `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-01-PLAN.md` (this plan file itself, if not already committed)

    Use commit message: `chore(999.1): scaffold dry-run branch, probe runner, profile gitignore`. Do NOT push to any remote.

    Print the resulting `git log --oneline -1` so the executor can see the commit landed. Print `git rev-parse --abbrev-ref HEAD` so future plans can confirm they are still on the dry-run branch.
  </action>
  <verify>
    <automated>git check-ignore -v Entitlements/embedded.provisionprofile 2>/dev/null | grep -q 'Entitlements/\*\.provisionprofile' &amp;&amp; git log --oneline -1 | grep -q 'scaffold dry-run' &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -qx '999.1-ubiquity-dry-run'</automated>
  </verify>
  <done>`git check-ignore` confirms the glob matches; the Plan 01 commit is present on `999.1-ubiquity-dry-run`; HEAD is still on the dry-run branch with a clean working tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine ↔ git | Provisioning profile must NEVER cross this boundary into a commit. |
| filesystem ↔ git tracking | `Entitlements/` contains both tracked plists AND the to-be-gitignored profile — proximity risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Information disclosure | `Entitlements/embedded.provisionprofile` committed to git, leaking Team ID `5H57ADQS8G` + Apple-account identifier | mitigate | `.gitignore` glob `Entitlements/*.provisionprofile` (Task 3); verified by `git check-ignore` (Task 4); placeholder `.example.txt` instead of the real file. |
| T-999.1-02 | Tampering | Probe script silently exits 0 on no-input (e.g., empty grep) and masks a real regression in later plans | mitigate | Every probe uses `grep -q` with explicit FAIL=1 on miss; non-zero exit on any FAIL; defensive `grep -v '^#'` on any source-file grep (Task 2). |
| T-999.1-03 | Denial of service (long-horizon) | Wrong branch — committing dry-run experiments to `main` triggers a Phase-58-style production regression | mitigate | Plan 01 Task 1 hard-asserts current branch is `main` before creating `999.1-ubiquity-dry-run`; subsequent plans re-assert HEAD before any commit. Rollback rule baked into `must_haves.truths`. |
| T-999.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan or any plan in Phase 999.1. RESEARCH.md "Package Legitimacy Audit" section confirms zero external packages added. |
</threat_model>

<verification>
- Branch `999.1-ubiquity-dry-run` exists and is checked out.
- `bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --static` runs without crashing; exits non-zero (no .app installed yet — expected).
- `bash .planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/probes/verify-ubiquity.sh --runtime` runs without crashing.
- `.gitignore` contains `Entitlements/*.provisionprofile`; `git check-ignore -v Entitlements/embedded.provisionprofile` resolves to that line.
- `Entitlements/embedded.provisionprofile.example.txt` exists and references the developer.apple.com portal flow + literal `iCloud.com.jamesonmorrill.dailybriefmonitor`.
- One commit landed on `999.1-ubiquity-dry-run`; nothing on `main`.
</verification>

<success_criteria>
- Plan 02 (operator portal) and Plan 03 (code/config edits) can start without further branch-management overhead.
- A future executor of Plan 04 can run a single command (`bash .planning/phases/.../probes/verify-ubiquity.sh`) to get a structured PASS/FAIL report covering UBIQ-01/02/04/05.
- The rollback rule is discoverable from every plan in the phase via the `must_haves.truths` field.
</success_criteria>

<output>
Create `.planning/phases/999.1-restore-ubiquity-entitlement-for-icloud-download/999.1-01-SUMMARY.md` when done.
</output>
