---
phase: 107
plan: 04
type: execute
wave: 3
depends_on: [107-02, 107-03]
files_modified:
  - .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md
  - .planning/phases/107-safari-extension-persistence/107-VALIDATION.md
autonomous: false
requirements: [EXT-01]
must_haves:
  truths:
    - "scripts/verify-phase-107.sh --full exits 0 (all six D-07 automated checks green)"
    - "The fresh build produces a .app that launches without a visible window, shows the first-launch NSAlert once, and registers the login item"
    - "107-HUMAN-UAT.md updated with the actual build timestamp; SC#1 reboot check remains pending (ship-with-uat-pending) until user reboots"
    - "107-VALIDATION.md flipped to nyquist_compliant: true with final task IDs filled in"
  artifacts:
    - path: ".planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md"
      provides: "Finalized HUMAN-UAT with real build metadata + frontmatter status: ship-with-uat-pending"
      contains: "status: ship-with-uat-pending"
    - path: ".planning/phases/107-safari-extension-persistence/107-VALIDATION.md"
      provides: "Final per-task verification map with actual task IDs"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "scripts/verify-phase-107.sh --runtime"
      to: "xcodebuild + Vigil Capture.app launch + SMAppService.mainApp.status probe"
      via: "sfltool/launchctl post-launch check"
      pattern: "sfltool dumpbtm"
    - from: "107-HUMAN-UAT.md"
      to: "/gsd-progress"
      via: "status: ship-with-uat-pending"
      pattern: "ship-with-uat-pending"
---

<objective>
Final assembly: run the full verification harness, confirm the clean build produces a silently-launching .app, finalize `107-HUMAN-UAT.md` with post-build metadata, flip `107-VALIDATION.md` frontmatter to `nyquist_compliant: true`, and present the human-verification checkpoint that ships the phase with reboot-UAT pending.

Purpose: D-06 human-UAT pattern closure. Plans 01-03 shipped the code; this plan proves the pieces work together on the current build and hands the SC#1 reboot check to the user. Per D-07, all five automated checks must be green before the human-UAT item is recorded as pending.

Output: Updated HUMAN-UAT.md (build timestamp + any observations from the launch probe), updated VALIDATION.md (nyquist_compliant: true, final task IDs), one human-verify checkpoint for the "no window flash on first launch" eyeball check. Zero production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-safari-extension-persistence/107-CONTEXT.md
@.planning/phases/107-safari-extension-persistence/107-RESEARCH.md
@.planning/phases/107-safari-extension-persistence/107-VALIDATION.md
@.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md
@scripts/verify-phase-107.sh
@vigil-safari-extension/Vigil Capture/AppDelegate.swift
@vigil-safari-extension/Vigil Capture/ViewController.swift
@vigil-safari-extension/Vigil Capture/Info.plist

<interfaces>
<!-- HUMAN-UAT finalization fields to update (created in Plan 00 with placeholder timestamps) -->
Frontmatter keys to update:
  - `updated`: replace with the actual ISO timestamp of the build (`date -u +%Y-%m-%dT%H:%M:%SZ`)
  - `started`: leave untouched (Plan 00 set it)

Body sections — augment "## Current Test" from `[awaiting user reboot — see Test 1]` to `[ready — user reboots to run Test 1; build path: /path/to/Vigil Capture.app]`.

<!-- VALIDATION.md task map final IDs -->
Based on this plan decomposition, the final task IDs are:
  - 107-00-01: scripts/verify-phase-107.sh creation (Plan 00 Task 1)
  - 107-00-02: 107-HUMAN-UAT.md creation (Plan 00 Task 2)
  - 107-01-01: Info.plist LSUIElement=true (Plan 01 Task 1)
  - 107-02-01: AppDelegate register+status (Plan 02 Task 1)
  - 107-02-02: AppDelegate first-launch NSAlert (Plan 02 Task 2)
  - 107-03-01: Main.html persistence pill (Plan 03 Task 1)
  - 107-03-02: Script.js showPersistence (Plan 03 Task 2)
  - 107-03-03: Style.css persistence rules (Plan 03 Task 3)
  - 107-03-04: ViewController SMAppService bridge (Plan 03 Task 4)
  - 107-04-01: Full verify + HUMAN-UAT finalization (Plan 04 Task 1)
  - 107-04-02: Human visual checkpoint (Plan 04 Task 2)

<!-- xcodebuild app artifact location convention -->
After `xcodebuild build -project "vigil-safari-extension/Vigil Capture.xcodeproj" -scheme "Vigil Capture" -configuration Debug`:
  `find ~/Library/Developer/Xcode/DerivedData -maxdepth 6 -name 'Vigil Capture.app' -type d | head -1`

<!-- NSAlert first-launch test-reset mechanism -->
To reliably see the first-launch alert during the probe:
  `defaults delete io.vigilhub.extension io.vigilhub.extension.firstLaunchAlertShown 2>/dev/null || true`
  (deletes the UserDefaults flag — next launch shows the alert again; safe because Plan 02 Task 2 resets the flag after dismissal)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full verify harness + finalize HUMAN-UAT + flip VALIDATION to nyquist_compliant</name>
  <files>
    .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md
    .planning/phases/107-safari-extension-persistence/107-VALIDATION.md
  </files>
  <read_first>
    - scripts/verify-phase-107.sh (must be present and executable from Plan 00 Task 1)
    - .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md (Plan 00 Task 2 output — confirm shape matches 105-HUMAN-UAT.md)
    - .planning/phases/107-safari-extension-persistence/107-VALIDATION.md (current state — `nyquist_compliant: false`; task map has placeholder IDs 107-00-01, 107-01-01, etc.)
    - vigil-safari-extension/Vigil Capture/AppDelegate.swift (post-Plan-02 final state — confirm all D-07 static greps will match)
    - vigil-safari-extension/Vigil Capture/ViewController.swift (post-Plan-03 final state)
    - vigil-safari-extension/Vigil Capture/Info.plist (post-Plan-01 final state — LSUIElement=true)
    - .planning/phases/107-safari-extension-persistence/107-CONTEXT.md (D-06/D-07 — phase ships complete on automated checks; SC#1 reboot is pending-not-blocking)
  </read_first>
  <action>
Step 1 — Run the full static suite and capture output:
```bash
cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief"
bash scripts/verify-phase-107.sh --static 2>&1 | tee /tmp/verify-107-static.log
```
Expected: exit 0, all four static checks green. If any check fails, STOP — that's a Plan 01/02 regression. Do NOT attempt to hotfix here; return control to orchestrator to re-run the earlier plan.

Step 2 — Clean build + runtime probe:
```bash
# Reset UserDefaults flag so first-launch NSAlert fires during probe
defaults delete io.vigilhub.extension io.vigilhub.extension.firstLaunchAlertShown 2>/dev/null || true
# Unregister any prior test registration to get a clean slate
# (safe: register() will re-register on next launch)
bash scripts/verify-phase-107.sh --runtime 2>&1 | tee /tmp/verify-107-runtime.log
```
Expected: exit 0. Checks 5 (xcodebuild) + 6 (sfltool/launchctl post-launch).

Per RESEARCH Pitfall 5: if Check 6 fails with a sandbox error (`SMAppServiceErrorDomain` code 1 "Operation not permitted"), this is the entitlements fallback branch. STOP and surface to user — do NOT create an entitlements file in this plan. The fallback is a separate Plan 05.

Per RESEARCH Pitfall 4: if Check 6 fails because `sfltool dumpbtm` requires root on this macOS build, the script's fallback to `launchctl print gui/$UID` should catch it. If both fail, manually run `sudo sfltool dumpbtm | grep vigil` and record the actual status in the HUMAN-UAT note for Test 2.

Step 3 — Compute build artifact path:
```bash
APP_PATH="$(find ~/Library/Developer/Xcode/DerivedData -maxdepth 6 -name 'Vigil Capture.app' -type d 2>/dev/null | head -1)"
BUILD_TS="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
echo "App: $APP_PATH"
echo "Timestamp: $BUILD_TS"
```

Step 4 — Update `.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md`:

Use the Edit tool to replace the `updated:` frontmatter line with the actual build timestamp computed above:
  - From: `updated: 2026-04-20T00:00:00Z` (Plan 00 placeholder)
  - To: `updated: <BUILD_TS>` (actual ISO timestamp)

Replace the `## Current Test` body from:
```
[awaiting user reboot — see Test 1]
```
To:
```
[ready for user reboot. Build artifact: <APP_PATH>. Automated checks (Plans 00-03, verify-phase-107.sh --full) exit 0. Tests 1, 2, 5 require physical reboot. Tests 3, 4 require eyeball check on first + second launch.]
```

Keep all five `### N.` test entries with `result: pending`. Do NOT mark any as passed — this is the "ship with UAT pending" shape per D-06. Keep `status: ship-with-uat-pending` in frontmatter.

Step 5 — Update `.planning/phases/107-safari-extension-persistence/107-VALIDATION.md`:

Use the Edit tool to:

5a. Flip frontmatter `nyquist_compliant: false` to `nyquist_compliant: true`.
5b. Flip frontmatter `wave_0_complete: false` to `wave_0_complete: true`.
5c. Rewrite the "Per-Task Verification Map" table to match final task IDs. Replace the entire existing table body (lines between the table header and the "Status: legend" line) with:

```
| 107-00-01 | 00 | 0 | EXT-01 | T-107-W0-01 | Wave 0 verify script exists and is executable | static | `test -x scripts/verify-phase-107.sh && bash -n scripts/verify-phase-107.sh` | ✅ | ✅ green |
| 107-00-02 | 00 | 0 | EXT-01 | — | Wave 0 HUMAN-UAT file present with ship-with-uat-pending status | static | `grep -q 'status: ship-with-uat-pending' .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md` | ✅ | ✅ green |
| 107-01-01 | 01 | 1 | EXT-01 | T-107-01-01 | LSUIElement=true in container Info.plist | static | `plutil -extract LSUIElement raw "vigil-safari-extension/Vigil Capture/Info.plist" \| grep -q '^true$'` | ✅ | ✅ green |
| 107-02-01 | 02 | 2 | EXT-01 | T-107-02-01,T-107-02-02 | AppDelegate calls SMAppService.mainApp.register() with status-guard | static | `grep -q 'SMAppService.mainApp.register()' "vigil-safari-extension/Vigil Capture/AppDelegate.swift" && grep -q 'SMAppService.mainApp.status' "vigil-safari-extension/Vigil Capture/AppDelegate.swift"` | ✅ | ✅ green |
| 107-02-02 | 02 | 2 | EXT-01 | T-107-02-03,T-107-02-04 | First-launch NSAlert gated by UserDefaults with NSApp.activate | static | `grep -q 'firstLaunchAlertKey' "vigil-safari-extension/Vigil Capture/AppDelegate.swift" && grep -q 'NSApp.activate(ignoringOtherApps: true)' "vigil-safari-extension/Vigil Capture/AppDelegate.swift"` | ✅ | ✅ green |
| 107-03-01 | 03 | 2 | EXT-01 | — | Main.html persistence pill elements present | static | `grep -q 'id="persistence"' "vigil-safari-extension/Vigil Capture/Resources/Base.lproj/Main.html"` | ✅ | ✅ green |
| 107-03-02 | 03 | 2 | EXT-01 | — | Script.js showPersistence() function present | static | `grep -q 'function showPersistence' "vigil-safari-extension/Vigil Capture/Resources/Script.js"` | ✅ | ✅ green |
| 107-03-03 | 03 | 2 | EXT-01 | — | Style.css persistence-* visibility rules present | static | `grep -c 'body.persistence-' "vigil-safari-extension/Vigil Capture/Resources/Style.css" \| awk '$1 >= 4 { exit 0 } { exit 1 }'` | ✅ | ✅ green |
| 107-03-04 | 03 | 2 | EXT-01 | T-107-03-01 | ViewController imports ServiceManagement and bridges status to showPersistence | static | `grep -q 'import ServiceManagement' "vigil-safari-extension/Vigil Capture/ViewController.swift" && grep -q 'showPersistence' "vigil-safari-extension/Vigil Capture/ViewController.swift"` | ✅ | ✅ green |
| 107-04-01 | 04 | 3 | EXT-01 | — | Full harness (xcodebuild + post-launch SMAppService probe) green | integration | `bash scripts/verify-phase-107.sh --full` | ✅ | ✅ green |
| 107-04-02 | 04 | 3 | EXT-01 | — | Human eyeball: no visible window flash on first launch; NSAlert shows once | human | see 107-HUMAN-UAT.md Tests 3, 4 | ✅ | ⏳ ship-with-uat-pending |
```

5d. Under "## Wave 0 Requirements", check the boxes that are now complete:
```
- [x] `scripts/verify-phase-107.sh` — automated assertions for D-07
- [x] `.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md` — SC#1 reboot-required check
```

5e. Under "## Validation Sign-Off", check all completed boxes and set **Approval:** to `approved (ship-with-uat-pending — Test 1/2/5 reboot pending user action)`.

Step 6 — verify the updated files:
```bash
grep -q 'nyquist_compliant: true' ".planning/phases/107-safari-extension-persistence/107-VALIDATION.md" && \
grep -q 'status: ship-with-uat-pending' ".planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md" && \
echo "files OK"
```

Do NOT commit anything in this task — the orchestrator handles phase-level commits. This task's job is to leave the two documentation files in their final "ship" state.
  </action>
  <verify>
    <automated>bash "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/scripts/verify-phase-107.sh" --static &amp;&amp; grep -q 'nyquist_compliant: true' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/107-safari-extension-persistence/107-VALIDATION.md" &amp;&amp; grep -q 'status: ship-with-uat-pending' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-phase-107.sh --static` exits 0
    - `bash scripts/verify-phase-107.sh --runtime` exits 0 OR documented failure is sandbox-related per Pitfall 5 (in which case STOP and escalate, do not hotfix)
    - `grep -q 'nyquist_compliant: true' 107-VALIDATION.md` returns 0
    - `grep -q 'wave_0_complete: true' 107-VALIDATION.md` returns 0
    - `grep -q '^| 107-00-01 ' 107-VALIDATION.md` returns 0 (final task IDs in map)
    - `grep -q '^| 107-04-01 ' 107-VALIDATION.md` returns 0 (Plan 04 task in map)
    - `grep -q '^| 107-04-02 ' 107-VALIDATION.md` returns 0
    - `grep -q 'ship-with-uat-pending' 107-VALIDATION.md` returns 0 (Approval line updated)
    - `grep -q 'status: ship-with-uat-pending' 107-HUMAN-UAT.md` returns 0 (frontmatter preserved)
    - `grep -qE 'updated: 2026-04-[0-9][0-9]T[0-9:]+Z' 107-HUMAN-UAT.md` returns 0 (real timestamp, not placeholder all-zeros)
    - `grep -q 'Build artifact:' 107-HUMAN-UAT.md` returns 0 (Current Test updated)
    - `grep -c 'result: pending' 107-HUMAN-UAT.md` returns 5 (all tests still pending — ship-with-uat-pending is not passed)
    - No changes to any file under `vigil-safari-extension/` (this task is documentation-only; `git diff --stat vigil-safari-extension/` returns empty)
  </acceptance_criteria>
  <done>
    Static + runtime verify both green. HUMAN-UAT.md updated with real build timestamp + Current Test note pointing to the .app path. VALIDATION.md flipped to nyquist_compliant: true with final task-ID map. Approval line records ship-with-uat-pending. Ready for Task 2 human checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human eyeball check — first-launch NSAlert + no window flash + persistence pill</name>
  <files>.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md (post-Task-1 state — five pending tests; Tests 3 and 4 are the executable-now ones)
    - vigil-safari-extension/Vigil Capture/AppDelegate.swift (confirm NSAlert copy matches what user will see: "Vigil Capture is installed." + "The extension will stay enabled across reboots...")
    - vigil-safari-extension/Vigil Capture/Info.plist (confirm LSUIElement=true is in place before probe)
    - .planning/phases/107-safari-extension-persistence/107-CONTEXT.md (D-06 ship-with-uat-pending — Tests 1/2/5 stay pending even if Tests 3/4 pass)
  </read_first>
  <action>
This is a checkpoint:human-verify task. Claude automates everything that can be automated (launch the app, capture the window-count via osascript), then pauses for the human to confirm the visual/subjective checks.

**Part A — Claude-automated steps (run these before pausing):**

1. Reset the first-launch UserDefaults flag so the NSAlert will fire during this probe:
```bash
defaults delete io.vigilhub.extension io.vigilhub.extension.firstLaunchAlertShown 2>/dev/null || true
```

2. Ensure no instance is running:
```bash
pkill -f "Vigil Capture.app/Contents/MacOS/Vigil Capture" 2>/dev/null || true
```

3. Locate the fresh .app:
```bash
APP_PATH="$(find ~/Library/Developer/Xcode/DerivedData -maxdepth 6 -name \'Vigil Capture.app\' -type d | head -1)"
echo "$APP_PATH"
```

4. Launch the app:
```bash
open "$APP_PATH"
sleep 2
```

5. Capture visible-window count via osascript (automated eyeball proxy for "no window flash"):
```bash
osascript -e \'tell application "System Events" to count (every window of application process "Vigil Capture")\' 2>/dev/null || echo "0"
```
Expected: 1 (the NSAlert is technically a window). Expected NOT: 2+ (would indicate storyboard window appeared alongside the alert).

6. Print what the human should see now, and pause for user confirmation.

**Part B — Human confirms via resume signal:**

Present to the user (verbatim output):

```
Vigil Capture is launched at: $APP_PATH
osascript window count: <N>

Please confirm the following in the UI right now:
(3) Exactly ONE NSAlert appears titled "Vigil Capture is installed." — no storyboard window behind it, no Dock icon bouncing.
(3) NSAlert body reads "The extension will stay enabled across reboots..." with a single "OK" button.
(3) You may also see a separate macOS "Background Items Added" notification (that is macOS, not our app).

After clicking OK on the NSAlert:
(4) Run: open "$APP_PATH" — no alert, no window, nothing visible. In Console.app (filter: process "Vigil Capture"), see "Login item already enabled; skipping register()." os_log.

Bonus — persistence pill (D-04):
Open Safari > Settings > Extensions > Vigil Capture > "Open preferences" button — the webview briefly appears showing both the extension-state pill AND a new "Login item active..." pill before the app quits.

Reply with:
  - "approved" — if Tests 3 and 4 both pass. I will flip them to result: pass in 107-HUMAN-UAT.md Summary to total:5 passed:2 pending:3. Tests 1, 2, 5 remain pending for your reboot UAT.
  - Describe any issue (e.g., "window flashed on first launch" or "no alert appeared"). I will pause phase-complete and open a follow-up plan (Plan 05 .entitlements branch if the issue is sandbox-related per RESEARCH Pitfall 5).
```

**Part C — On "approved" resume signal (Claude does AFTER the pause):**

1. Edit 107-HUMAN-UAT.md: change `result: pending` to `result: pass` on Test 3 and Test 4 only. Leave Tests 1, 2, 5 pending.
2. Edit the Summary block:
```
total: 5
passed: 2
issues: 0
pending: 3
skipped: 0
blocked: 0
```
3. Keep `status: ship-with-uat-pending` in frontmatter (do NOT flip to `status: complete` — Tests 1/2/5 must flip first by the user after reboot).
4. Commit 107-HUMAN-UAT.md with message "docs(107): record Tests 3+4 pass; Tests 1/2/5 ship-with-uat-pending" — orchestrator handles the commit.

Failure modes and responses:
- Window flashes → LSUIElement=true not taking effect. STOP; run `plutil -extract LSUIElement raw` and confirm Info.plist is built into the .app bundle (check Contents/Info.plist of the built .app).
- No NSAlert → UserDefaults flag was not actually reset (the `defaults delete` may have hit a stale plist). Re-run step 1 with sudo, re-try.
- Alert behind other windows → Pitfall 3 regression; confirm `NSApp.activate(ignoringOtherApps: true)` is still in AppDelegate.swift via grep.
- Persistence pill shows "failed" → Pitfall 5 sandbox issue. STOP. Escalate to orchestrator for Plan 05 .entitlements branch.
  </action>
  <verify>
    <automated>test -f "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md" &amp;&amp; grep -q \'status: ship-with-uat-pending\' "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/.planning/phases/107-safari-extension-persistence/107-HUMAN-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Claude successfully launched the built .app and captured osascript window count
    - User confirmed Test 3 and Test 4 visually (typed "approved" or equivalent)
    - After resume: `grep -c \'result: pass\' 107-HUMAN-UAT.md` returns 2 (Tests 3 and 4 flipped)
    - After resume: `grep -c \'result: pending\' 107-HUMAN-UAT.md` returns 3 (Tests 1, 2, 5 remain pending — reboot UAT)
    - After resume: 107-HUMAN-UAT.md Summary block has `passed: 2` and `pending: 3`
    - `status: ship-with-uat-pending` remains in frontmatter (NOT flipped to `complete` — user reboot still outstanding)
    - OR: user reported an issue; phase-complete is PAUSED and the issue is recorded in the task output for orchestrator to create a follow-up plan
  </acceptance_criteria>
  <done>
    User confirmed first-launch NSAlert + no visible window (Test 3) and subsequent-launch silence (Test 4). 107-HUMAN-UAT.md Summary shows passed=2, pending=3; frontmatter remains ship-with-uat-pending. Tests 1, 2, 5 are the user's responsibility to flip after `sudo reboot` and they will surface in /gsd-progress until done. Phase 107 ready to mark complete per D-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine running verify script | Local shell access with user privileges; no network, no remote servers involved |
| HUMAN-UAT.md as integrity record | File is version-controlled; edits are git-auditable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-04-01 | Repudiation | Human test results recorded in HUMAN-UAT.md | accept | Git commits provide an audit trail of who recorded which result; no stronger attestation needed for a solo-dev project. Matches 105-HUMAN-UAT.md posture. |
| T-107-04-02 | Tampering | `defaults delete` resetting firstLaunchAlertShown flag | accept | User-scoped UserDefaults write; no privilege escalation. Required mechanism to test the first-launch path. |
| T-107-04-03 | Info Disclosure | `sfltool dumpbtm` output may include other BTM entries | accept | Output is scoped to user's BTM registry; data is non-secret metadata (bundle IDs, paths). Already accessible via System Settings > Login Items UI. |
| T-107-04-04 | DoS | xcodebuild may fail on deployment-target regression (Pitfall 6) | mitigate | Plan 00 Check 2 catches this statically BEFORE Plan 04 runs. If Plan 04 xcodebuild fails, it's a regression — STOP and escalate. |
| T-107-04-05 | Elevation | Running `open Vigil Capture.app` with BTM registration | mitigate | SMAppService.register() requires code-signed app; Automatic signing is enabled (project.pbxproj L503, L544). Unsigned builds would fail to register with code 1 "Operation not permitted", caught by RESEARCH Pitfall 5 path. |

All threats low-severity or actively mitigated by Plan 00's static harness. No high-severity issues. ASVS L1 compliant.
</threat_model>

<verification>
- `bash scripts/verify-phase-107.sh --full` exits 0
- 107-HUMAN-UAT.md frontmatter has `status: ship-with-uat-pending` and `updated:` timestamp is not the Plan 00 placeholder
- 107-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
- 107-VALIDATION.md per-task verification map contains rows `107-00-01` through `107-04-02`
- Human eyeball checkpoint completed with Tests 3 + 4 passing, Tests 1/2/5 remain ship-with-uat-pending
</verification>

<success_criteria>
- Automated harness green (all 6 D-07 checks)
- HUMAN-UAT.md finalized with real build metadata
- VALIDATION.md nyquist_compliant: true with final task IDs
- Human checkpoint Tests 3 + 4 pass (first-launch NSAlert fires once; no window flash)
- Tests 1, 2, 5 remain pending — user reboot UAT scheduled but non-blocking
- Phase 107 meets D-06 ship-with-uat-pending bar; phase marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/107-safari-extension-persistence/107-04-SUMMARY.md` capturing: full verify-phase-107 output (both --static and --runtime), the resolved .app build path, observations from Tests 3 + 4 (pass/fail with notes), and explicit listing of Tests 1, 2, 5 as reboot-pending. User remains responsible for flipping those three to pass/issue after running `sudo reboot`.
</output>
