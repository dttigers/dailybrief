---
phase: 82-cli-restructure
plan: 03
type: execute
wave: 3
depends_on:
  - 82-01
  - 82-02
files_modified:
  - LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist
  - Scripts/install.sh
autonomous: false
requirements:
  - CLI-08

must_haves:
  truths:
    - "The repo-committed plist (`LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist`) references a binary path that exists after `install.sh` runs"
    - "`install.sh` regenerates the installed plist from its template — the regenerated plist references only `DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor` (no `DailyBrief` CLI)"
    - "`dailybrief doctor` PASS on plist-binary check after `install.sh` is run"
    - "The installed `DailyBrief` binary responds to `dailybrief capture --help` with the new flag set"
  artifacts:
    - path: LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist
      provides: "Canonical plist committed to repo, binary path verified"
      contains: "DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor"
    - path: Scripts/install.sh
      provides: "Updated plist template with correct binary path; no CLI invocations"
  key_links:
    - from: "LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist ProgramArguments[0]"
      to: "~/.local/bin/DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor"
      via: "install.sh copies plist template"
      pattern: "DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor"
---

<objective>
Audit and commit the LaunchAgent plist changes atomically with the CLI restructure — ensuring the repo copy and the install.sh-generated copy reference only commands that exist after the restructure. Then run `install.sh` to install the new binary, and manually verify `dailybrief doctor` passes.

Purpose: Success criterion 6 — the plist is verified and committed in the same change. Also installs the final release binary so the user can run the new subcommands from the terminal.

Output: Updated `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist` committed; `install.sh` reviewed and optionally updated; new binary installed to `~/.local/bin/DailyBrief`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-cli-restructure/82-01-SUMMARY.md
@.planning/phases/82-cli-restructure/82-02-SUMMARY.md

<interfaces>
<!-- Facts the executor needs without codebase exploration. -->

Current repo plist path: `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist`
Current installed plist path: `~/Library/LaunchAgents/com.jamesonmorrill.dailybriefmonitor.plist`

Current repo plist ProgramArguments[0]:
  `/Users/jamesonmorrill/.local/bin/DailyBriefMonitor` (bare binary — STALE)

Current installed plist ProgramArguments[0]:
  `/Users/jamesonmorrill/.local/bin/DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor` (correct .app path)

Discrepancy: the repo copy is behind the installed copy. The repo copy needs to be updated to match the .app path that install.sh already generates.

install.sh plist template (line 153-186) — already generates the .app path correctly:
  `<string>$INSTALL_DIR/DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor</string>`

So install.sh itself is already correct. The repo copy just needs to be brought in sync.

Neither the repo plist nor install.sh references the `DailyBrief` CLI binary — the monitor plist invokes only `DailyBriefMonitor`. The CLI is invoked manually or via BriefScheduler inside the monitor process.

Binary install path: `~/.local/bin/DailyBrief` (the CLI)
install.sh requires: Developer ID Application cert in login keychain + `swift build -c release`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync repo plist to match install.sh output + release build</name>
  <files>LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist</files>
  <action>
Two actions:

**A. Update the repo plist to match what install.sh generates.**

The repo copy at `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist` currently has:
```xml
<string>/Users/jamesonmorrill/.local/bin/DailyBriefMonitor</string>
```
Change it to the .app bundle path (matching install.sh's template and the currently-installed version):
```xml
<string>/Users/jamesonmorrill/.local/bin/DailyBriefMonitor.app/Contents/MacOS/DailyBriefMonitor</string>
```

The rest of the plist (KeepAlive, LimitLoadToSessionType, StandardOutPath, EnvironmentVariables) is already correct — do not change it. No `DailyBrief` CLI invocations exist in this plist; confirm that before writing.

**B. Run release build.**

Run:
```bash
cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && swift build -c release 2>&1 | tail -10
```

This must succeed before the install step. If it fails, fix the compiler errors in DailyBrief.swift (from Plans 01/02) first.
  </action>
  <verify>
    <automated>cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && swift build -c release 2>&1 | grep -E "error:|Build complete"</automated>
  </verify>
  <done>
`swift build -c release` exits 0. `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist` ProgramArguments[0] is the .app bundle path. No `DailyBrief` CLI binary reference in the plist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 01 + 02 implemented and compiled to release. Repo plist updated. Ready to run install.sh to install the new binary and reload the LaunchAgent.

This checkpoint runs install.sh (which requires your Developer ID cert) and then verifies all 6 success criteria via live commands.
  </what-built>
  <how-to-verify>
Run these commands in sequence from your terminal:

**Step 1 — Install the new binary:**
```bash
cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief" && ./Scripts/install.sh
```
Expected: "Installation Complete" with DailyBrief and DailyBriefMonitor.app installed and signed.

**Step 2 — Verify subcommand surface:**
```bash
~/.local/bin/DailyBrief --help
```
Expected: `capture`, `triage`, `doctor`, `setup` listed as subcommands.

**Step 3 — Verify capture flags:**
```bash
~/.local/bin/DailyBrief capture --help
```
Expected: `TEXT` argument + `--category`, `--no-triage`, `--source` flags visible.

**Step 4 — Verify triage flags:**
```bash
~/.local/bin/DailyBrief triage --help
```
Expected: `--limit INT` and `--force` flag visible.

**Step 5 — Verify retired commands:**
```bash
~/.local/bin/DailyBrief complete CS0001
~/.local/bin/DailyBrief uncomplete CS0001
~/.local/bin/DailyBrief list-completed
```
Expected for all three: prints "Work order management has moved to the Vigil dashboard." and "Visit: https://app.vigilhub.io" — exits 0 (no error).

**Step 6 — Verify doctor:**
```bash
~/.local/bin/DailyBrief doctor
```
Expected: PASS/FAIL table with at least "LaunchAgent plist exists" and "LaunchAgent loaded" = PASS. All five checks printed. If VIGIL_API_KEY is not in your shell environment, that row will FAIL — that is expected and acceptable.

**Step 7 — Live capture test (optional but recommended):**
```bash
~/.local/bin/DailyBrief capture "cli restructure test capture" --no-triage
```
Expected: "Captured thought #NNN" with your content. Confirm the thought appears at https://app.vigilhub.io.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any failures for Claude to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh → launchctl | Shell script with elevated launchd access installs and reloads the LaunchAgent |
| LaunchAgent plist → binary | plist ProgramArguments[0] path must be verified to exist; wrong path = silent non-start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07 | Tampering | LaunchAgent plist binary path | mitigate | `doctor` check 5 explicitly verifies plist ProgramArguments[0] exists on disk |
| T-82-08 | Denial of Service | install.sh launchctl bootout | accept | bootout+bootstrap is idempotent; monitor restarts immediately after reload |
| T-82-09 | Elevation of Privilege | install.sh codesign with Developer ID | accept | Requires user's own cert; install.sh hard-fails if cert absent |
</threat_model>

<verification>
1. `LaunchAgent/com.jamesonmorrill.dailybriefmonitor.plist` ProgramArguments[0] = `.app/Contents/MacOS/DailyBriefMonitor` path (not bare binary)
2. `swift build -c release` exits 0
3. After `install.sh`: `dailybrief doctor` shows PASS for plist-exists + plist-loaded + plist-binary-exists
4. `dailybrief complete CS0001` exits 0 with dashboard message
5. `dailybrief capture "test" --no-triage` posts to API and returns thought ID
</verification>

<success_criteria>
- Repo plist and installed plist both reference `.app/Contents/MacOS/DailyBriefMonitor`
- `install.sh` runs cleanly with zero errors
- All 6 phase success criteria verified by the user in the checkpoint
- Changes committed atomically (plist + CLI in one commit)
</success_criteria>

<output>
After completion, create `.planning/phases/82-cli-restructure/82-03-SUMMARY.md` using the summary template.
</output>
