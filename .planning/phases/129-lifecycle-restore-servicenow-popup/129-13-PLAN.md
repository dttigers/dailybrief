---
phase: 129-lifecycle-restore-servicenow-popup
plan: 13
type: execute
wave: 3
depends_on: [129-07, 129-08, 129-09, 129-10, 129-11, 129-12]
files_modified:
  - .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RESULTS.md
  - .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md
autonomous: false
gap_closure: true
closes_gaps: []
requirements:
  - G2-LIFECYCLE-01
  - G2-LIFECYCLE-02
  - G2-LIFECYCLE-03
  - SVCNOW-01
  - SVCNOW-02
  - SVCNOW-03
  - SVCNOW-04
  - SVCNOW-05
requirements_addressed:
  - G2-LIFECYCLE-01
  - G2-LIFECYCLE-02
  - G2-LIFECYCLE-03
  - SVCNOW-01
  - SVCNOW-02
  - SVCNOW-03
  - SVCNOW-04
  - SVCNOW-05
tags: [uat, hardware, integration, runbook, operator-checkpoint, gap-closure-validation, phase-close-out]

must_haves:
  truths:
    - "All previously DEFERRED, BLOCKED, or FAILED scenarios from 129-UAT-RESULTS.md session 1 are re-run after the gap-closure plans (129-07 through 129-12) ship: Scenarios 1, 1b, 2, 3, 4b, 4c, 5, 6"
    - "Scenario 1 (G2 force-quit restore to TASK_DETAIL) is re-run at FULL FIDELITY — operator enters TASK_DETAIL via the DOUBLE_CLICK gesture from plan 129-09, force-quits, relaunches, and verifies the plugin restores to the same TASK_DETAIL with the same task id. PASS = correct screen + task id. FAIL = HOME, WORK_ORDERS list (correct if D-07 fallback fired), or wrong task."
    - "Scenario 1b (TTL 30-min boundary) is re-run; operator waits 31 minutes between force-quit and relaunch; expects HOME. May be marked DEFERRED-LONG-WAIT if impractical for the session, but at minimum the < 30-min path is exercised."
    - "Scenario 2 (phone-background → foreground Companion HUD cache) is re-run with a fresh hardware session (the original session-1 result was DEFERRED due to session-end pause); operator confirms no empty re-render."
    - "Scenario 3 (glassesMenu precedence) is re-run after plan 129-10's restore fix lands; expected to PASS now that restore is actually firing (previously inconclusive because everything fell to HOME)."
    - "Scenarios 4b and 4c (multi-tab POST race + retry-storm idempotency) are re-run after plan 129-08 deploys migration 0021 to production; expected to PASS (no more HTTP 500 from the missing client_capture_id column)."
    - "Scenario 5 (Safari parity smoke) is re-run after plan 129-07 ships the Safari content-script.js update (GAP-129-D lock-step) and the api-key setup view (GAP-129-B lock-step); operator confirms the Xcode Copy Bundle Resources step picks up the new files."
    - "Scenario 6 (title drift banner) is re-run after plan 129-07's content-script.js storage-session-backed lastCaseNumber persistence ships; expected to PASS (banner fires on real Polaris full-reload navigation)."
    - "Two NEW gap-closure UAT scenarios are added: (a) GAP-129-A confirmation — operator loads `vigil-extension/` as an unpacked Chrome extension and confirms no `_*` reserved-prefix error fires; (b) GAP-129-B confirmation — operator opens popup with empty chrome.storage.local and confirms the inline setup view renders, accepts a key, and transitions to svcnow-view."
    - "Results are recorded in 129-UAT-RESULTS.md as a new session-2 section that AUGMENTS (does not overwrite) the session-1 results; each scenario records pass/fail + timestamp + observed behavior; the original GAP inventory at the top of the file gets a closing status update (CLOSED / RE-OPENED / DEFERRED) per gap"
    - "After session-2 completes successfully, Phase 129 is closeable — the 5 ROADMAP success criteria are all validated on hardware"
  artifacts:
    - path: ".planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RESULTS.md"
      provides: "Session-2 results section + updated GAP inventory closing-status table"
  key_links:
    - from: "129-UAT-RESULTS.md session-2 section"
      to: "129-UAT-RUNBOOK.md scenarios (post-129-11 updates)"
      via: "1-to-1 mapping per scenario, including the two new gap-closure scenarios"
      pattern: "Scenario [0-9]"
---

<objective>
Re-run the failing / blocked / deferred UAT scenarios from session 1 (2026-05-15) after the gap-closure plans (129-07 through 129-12) have shipped. Phase 129 cannot close until ALL 5 ROADMAP success criteria are validated on hardware; session 1 left 7 of 9 scenarios in non-PASS states. This plan is the closing-out UAT pass.

This plan is `autonomous: false` because hardware (G2 glasses, iPhone Even Hub, macOS Safari, live ServiceNow Polaris page) is required. Claude's role is to update the runbook with the new gap-closure scenarios (GAP-129-A + GAP-129-B confirmations) and ensure the operator has clear pass/fail rules; the operator runs the scenarios on hardware and records results.

Dependencies: this plan depends on ALL upstream gap-closure plans:
- 129-07 (extension housekeeping) — gap A, B, D fixes must be live in the extension build the operator is testing.
- 129-08 (production migration deploy) — Scenarios 4b and 4c can only pass once prod has migration 0021.
- 129-09 (DOUBLE_CLICK entry gesture) — Scenario 1 full-fidelity test (enter TASK_DETAIL on hardware) requires this gesture.
- 129-10 (G2 restore diagnostic + fix) — Scenarios 1, 1b, 2, 3 ALL depend on the restore actually working on hardware.
- 129-11 (terminology cleanup) — the runbook's Scenario 1 + 1b descriptions are clarified by this plan; the operator reads the updated runbook.
- 129-12 (build-gate convention) — the gap-closure plans themselves have to compile cleanly before sideload.

Purpose: Validate end-to-end that every gap is closed AND that the original Phase 129 success criteria all PASS on real hardware. Close out Phase 129.

Output: A session-2 section in 129-UAT-RESULTS.md with pass/fail per scenario, plus a closing-status table at the top of the GAP inventory showing each GAP's resolution state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-CONTEXT.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-RESEARCH.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RESULTS.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-06-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-07-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-08-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-09-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-10-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-11-PLAN.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-12-PLAN.md

<interfaces>
This plan does NOT modify the runbook (`129-UAT-RUNBOOK.md`) directly — plan 129-11 updates the runbook's terminology for Scenarios 1 and 1b. This plan ADDS two new scenarios via the augmentation pattern: Task 1 of this plan writes two new scenario sections directly into the runbook (Scenarios 7 and 8 for the gap-closure confirmations), OR adds them as a "Session 2 — Gap-Closure Verification Scenarios" appendix at the bottom of the runbook. The executor picks the structurally cleanest approach.

The two new scenarios:

**Scenario 7 — GAP-129-A confirmation (Chrome unpacked load succeeds):**
- Operator: From the iMac, open Chrome → chrome://extensions → enable Developer mode → click "Load unpacked" → select the `vigil-extension/` directory.
- Expected: Chrome accepts the load (no `Could not load extension. Cannot load extension with file or directory name __tests__. Filenames starting with "_" are reserved for use by the system.` error).
- PASS: Extension loads; the action icon appears in the toolbar.
- FAIL: The reserved-prefix error fires (GAP-129-A regression — likely the __tests__ directory was re-added somewhere).

**Scenario 8 — GAP-129-B confirmation (inline setup view + key save flow):**
- Pre-condition: Operator clears `chrome.storage.local` for the extension (`chrome.storage.local.clear()` in popup DevTools, or remove + re-add the extension which clears its storage).
- Operator action 1: Click the Vigil extension button on a `*.service-now.com/*` page.
- Expected: Popup opens showing the setup-view (NOT the svcnow form); setup-view contains a password-style input + Save button.
- Operator action 2: Type a valid api key (the operator's prod or test key) + click Save.
- Expected: Setup-view disappears; svcnow-view appears; the current Polaris page's CS# is extracted and shown in the header; description textarea is focused.
- Operator action 3: Type a short description + click Send (or Cmd+Enter).
- Expected: HTTP 200; popup closes (D-03 behavior).
- PASS: All three actions produce expected results.
- FAIL: Any step diverges — record the specific divergence.

The runbook update for Scenarios 1, 1b also needs an explicit reference to the new DOUBLE_CLICK gesture (entered TASK_DETAIL via temple-touchpad double-tap on WORK_ORDERS list) — already covered by plan 129-11's Task 3.

GAP closing-status table format (appended to the top of `129-UAT-RESULTS.md` as a new section before the existing Gaps Inventory):

```
## Gap Closure Status (post-session-2)

| Gap | Resolution Plan | Session-2 Status | Notes |
|-----|-----------------|------------------|-------|
| GAP-129-A | 129-07 Task 1 | CLOSED | Scenario 7 PASS — Chrome load succeeds without __tests__ error |
| GAP-129-B | 129-07 Task 2 | CLOSED | Scenario 8 PASS — setup-view + save flow + svcnow transition |
| GAP-129-C | 129-08 | CLOSED | 129-08 deploy log shows post-deploy HTTP 200 + dedup synced:0 |
| GAP-129-D | 129-07 Task 3 | CLOSED | Scenario 6 PASS — drift banner fires on full-reload Polaris nav |
| GAP-129-E | 129-11 | CLOSED | Doc-only; verified by reading post-129-11 CONTEXT/RESEARCH/RUNBOOK |
| GAP-129-F | 129-09 | CLOSED | Scenario 1 setup PASS — DOUBLE_CLICK enters TASK_DETAIL on hardware |
| GAP-129-G | 129-10 | CLOSED | 129-10 diagnostic log shows Phase 3 validation PASS |
| GAP-129-H | 129-12 | CLOSED | Build-gate convention shipped; future plans inherit the rule |
```

Each row's "Session-2 Status" is one of CLOSED, RE-OPENED, DEFERRED, or BLOCKED. The operator fills in the actual status during Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 129-UAT-RUNBOOK.md with Scenarios 7 + 8 (gap-closure confirmations)</name>
  <files>.planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md (post-129-11 terminology-cleaned version)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-07-PLAN.md (Tasks 1 + 2 — GAP-129-A test move + GAP-129-B setup view; new scenarios need to match the actual behavior shipped)
  </read_first>
  <action>
    Append two new scenarios to `129-UAT-RUNBOOK.md`. The executor picks ONE of two structural approaches:

    **Approach A — Inline at the end of the Scenarios section:** Add Scenario 7 and Scenario 8 sections after Scenario 6 (the existing last scenario). Use the same heading style + structure as existing scenarios (Status line, Hardware line, Steps, Expected behavior, Pass / Fail decision rules).

    **Approach B — Appendix:** Add a new top-level section "Session 2 — Gap-Closure Verification Scenarios" near the end of the runbook (before the final "After running all scenarios" instruction line). Inside this section, the two new scenarios are nested under the new heading.

    Approach A is preferred for runbook continuity unless the executor judges the appendix cleaner.

    Content for both new scenarios is given in this plan's `<interfaces>` block — Scenario 7 (GAP-129-A confirmation) and Scenario 8 (GAP-129-B confirmation). Copy that text into the runbook with consistent formatting.

    Also: at the bottom of the runbook (the existing "After running all scenarios, fill in 129-UAT-RESULTS.md..." instruction), add a new bullet pointing the operator to record results under a fresh "## Session 2 — 2026-05-XX (Gap Closure Re-run)" section in the results file, with the GAP closing-status table at the top of the section.
  </action>
  <verify>
    <automated>grep -c '^## Scenario' .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md | grep -qE '^[8-9]|^[1-9][0-9]+' && grep -c 'GAP-129-A\|GAP-129-B' .planning/phases/129-lifecycle-restore-servicenow-popup/129-UAT-RUNBOOK.md | grep -qE '^[2-9]|^[1-9][0-9]+'</automated>
  </verify>
  <acceptance_criteria>
    - `129-UAT-RUNBOOK.md` contains at least 8 `## Scenario` headings (the original 6 from 129-06 — Scenarios 1, 1b, 2, 3, 4, 4b, 4c, 5, 6 — count varies; the verify allows 8+, accommodating either approach).
    - The runbook references both `GAP-129-A` and `GAP-129-B` at least once each (in the new Scenarios 7 and 8).
    - Scenario 7's pre-condition is "load `vigil-extension/` as unpacked extension in Chrome" and the PASS rule references the absence of the reserved-prefix error.
    - Scenario 8's pre-condition is "chrome.storage.local cleared / fresh extension state" and the PASS rule covers all three operator actions (setup-view renders → save key → svcnow-view + send).
    - Existing scenarios (1-6) are UNCHANGED in their procedural content (other than the terminology cleanup from plan 129-11 which is already in place).
    - The final instruction in the runbook directs the operator to a new "Session 2" section in 129-UAT-RESULTS.md.
  </acceptance_criteria>
  <done>
    Runbook covers all 8+ scenarios needed for session-2 closing-out UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs Session 2 UAT — all deferred/blocked/failed scenarios + new gap-closure scenarios</name>
  <what-built>
    Gap-closure plans 129-07 through 129-12 have all shipped. The extension load no longer crashes on `__tests__`; the popup has an inline setup view; the content-script persists lastCaseNumber via chrome.storage.session; production has migration 0021 applied; the G2 plugin has a DOUBLE_CLICK entry gesture for TASK_DETAIL; the restore mechanism is diagnosed and fixed on hardware; the runbook terminology is cleaned up; the build-gate convention is documented. The runbook (Task 1 of this plan) has two new scenarios for confirming the gap fixes.
  </what-built>
  <how-to-verify>
    1. Open `129-UAT-RUNBOOK.md` in your editor and skim the updated scenarios.
    2. Confirm the prerequisites are met:
       a. Chrome extension `vigil-extension/` directory has been re-built per plan 129-07 (no __tests__/ inside; popup.html has setup-view; content-script.js has chrome.storage.session).
       b. Safari extension Resources directory has been rebuilt + Xcode Copy Bundle Resources step run (per 129-05 SUMMARY follow-up + 129-07 Safari lock-step).
       c. Production has migration 0021 applied (per 129-08 deploy log shows status: completed).
       d. G2 plugin has been re-built + sideloaded with the 129-09 DOUBLE_CLICK gesture + 129-10 restore fix (per 129-10 diagnostic log shows Phase 3 validation PASS).

    3. Run each scenario IN ORDER. For each, paste the result into a new "## Session 2 — 2026-05-XX (Gap Closure Re-run)" section in `129-UAT-RESULTS.md`:

       - **Scenario 7 (GAP-129-A confirmation)**: load vigil-extension/ unpacked. Expected: succeeds. Paste outcome.
       - **Scenario 8 (GAP-129-B confirmation)**: setup-view + save + svcnow-view + send. Expected: all three actions succeed. Paste outcome.
       - **Scenario 4** (Polaris CS# extraction): re-confirm PASS (already PASS in session 1, but re-verify after the popup setup-view change).
       - **Scenario 4b** (multi-tab POST race): post-129-08, expect both POSTs return HTTP 200 + each creates a row (different clientCaptureIds). Paste outcome.
       - **Scenario 4c** (retry-storm idempotency): post-129-08, expect HTTP 200 + synced:1 then HTTP 200 + synced:0. Paste outcome.
       - **Scenario 6** (title drift banner): post-129-07 Task 3, expect drift banner to fire after full-reload Polaris case-to-case navigation. Paste outcome.
       - **Scenario 5** (Safari parity smoke): post-129-07 Safari lock-step, expect Safari popup renders identically + send succeeds. Paste outcome.
       - **Scenario 1** (G2 force-quit restore to TASK_DETAIL): full fidelity. Operator on WORK_ORDERS list → DOUBLE_CLICK to enter TASK_DETAIL → force-quit Even Hub → relaunch → expect TASK_DETAIL for the same task. Paste outcome.
       - **Scenario 1b** (TTL 30-min boundary): if practical to wait, run; else mark DEFERRED-LONG-WAIT.
       - **Scenario 2** (phone-background→foreground Companion cache): re-run fresh. Paste outcome.
       - **Scenario 3** (glassesMenu precedence): re-run after 129-10 restore fix. Paste outcome.

    4. At the top of the Session 2 results section, fill in the GAP Closure Status table (template in this plan's `<interfaces>` block) with the actual session-2 statuses (CLOSED / RE-OPENED / DEFERRED / BLOCKED) per gap.

    5. If ALL scenarios PASS (or are mark-DEFERRED for the long-wait case) AND the GAP table shows all 8 gaps as CLOSED:
       - Type "approved: phase 129 closed". The orchestrator will route to phase close-out (mark ROADMAP entries complete, update STATE.md).

    6. If ANY scenario FAILS:
       - Type "regression: scenario N failed" with a one-line description. The orchestrator will route the failing scenario to a new `/gsd:plan-phase --gaps` pass.

    7. If any scenario remains BLOCKED on something beyond the operator's control (e.g., unable to access prod, hardware not available):
       - Type "blocked: scenario N — <reason>". Defer to a future session.
  </how-to-verify>
  <resume-signal>Type "approved: phase 129 closed" if every scenario PASS or DEFERRED-LONG-WAIT and the GAP table shows all CLOSED. Otherwise type "regression: scenario N failed — <one-liner>" or "blocked: scenario N — <reason>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator hardware UAT environment → results.md | Manual data capture; observed behavior recorded. Same trust model as plan 129-06 Task 2. |
| Live ServiceNow page → operator's running Chrome / Safari | Same trust model as session 1. |
| Production database after 129-08 deploy → Scenarios 4b, 4c | Production data is touched (test rows with CS9999999 sentinel); cleanup specified in 129-08 deploy log. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-48 | Repudiation | UAT results not recorded; phase closed without evidence | mitigate | Task 2 requires the operator to populate `129-UAT-RESULTS.md` Session 2 section with per-scenario evidence. Acceptance criterion for the orchestrator's close-out: the GAP Closure Status table is filled in. |
| T-129-49 | Information Disclosure | Operator pastes sensitive Polaris case content into results.md | mitigate | Runbook explicitly says "paste the observed behavior in one-sentence form" — no body content requested. |
| T-129-50 | Tampering | Stale extension build sideloaded — gap fixes not actually present | mitigate | Task 2 step 2 (prerequisites) requires operator to confirm each gap-closure plan's artifacts are LIVE in the build under test (extension re-built, prod migration applied, G2 plugin re-sideloaded with the fix). Each session-2 scenario itself probes for the gap-closure behavior. |
| T-129-SC | Tampering | npm/pip/cargo installs | N/A | No new external packages installed. |
</threat_model>

<verification>
- `129-UAT-RUNBOOK.md` has ≥ 8 `## Scenario` headings AND references both GAP-129-A and GAP-129-B.
- Post-operator-completion: `129-UAT-RESULTS.md` contains a "Session 2" section with the GAP Closure Status table populated.
- Post-operator-completion: each scenario in Session 2 has a PASS / FAIL / DEFERRED status with operator-pasted observation.
- Phase 129 success criteria: all 5 ROADMAP Phase 129 criteria are validated on hardware via this plan's scenarios.
</verification>

<success_criteria>
- Every gap from session 1 (GAPs A-H) is verified CLOSED via session-2 evidence; all 5 ROADMAP Phase 129 success criteria PASS on hardware; Phase 129 is closeable.
</success_criteria>

<output>
Create `.planning/phases/129-lifecycle-restore-servicenow-popup/129-13-SUMMARY.md` when done.
</output>
