---
phase: 129-lifecycle-restore-servicenow-popup
plan: 05
type: execute
wave: 3
depends_on: [129-03]
files_modified:
  - vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json
  - vigil-safari-extension/Vigil Capture Extension/Resources/background.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.html
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.css
  - vigil-safari-extension/Vigil Capture Extension/Resources/popup.js
  - vigil-extension/__tests__/parity.test.ts
autonomous: true
requirements: [SVCNOW-05]
requirements_addressed: [SVCNOW-05]
tags: [safari-extension, lockstep, parity, drift-detector, wkwebextension]

must_haves:
  truths:
    - "Safari extension `Vigil Capture Extension/Resources/` directory contains byte-identical copies of `popup.html`, `popup.css`, `popup.js`, `popup-helpers.js`, `background.js`, `content-script.js` from `vigil-extension/`"
    - "Safari `manifest.json` and Chrome `manifest.json` have identical `permissions` arrays (set equality)"
    - "Safari `manifest.json` and Chrome `manifest.json` have identical `host_permissions` arrays (set equality)"
    - "Safari `manifest.json` and Chrome `manifest.json` have identical `content_scripts` matches and js arrays"
    - "Safari `manifest.json` declares `background.service_worker = 'background.js'` (matches Chrome)"
    - "Drift-detector parity test reads both Chrome and Safari file sets and asserts equivalence per the assertions above (Phase 114 EXT-02 lock-step pattern)"
    - "Vigil-brand-compliant styling renders in Safari (same popup.css; primary green #1D9E75 + error #E5534B preserved per D-13)"
  artifacts:
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json"
      provides: "Safari MV3 manifest mirroring Chrome (tabs perm, SN host perm, service worker, content scripts)"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/background.js"
      provides: "Safari service worker — byte-identical to Chrome"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js"
      provides: "Safari content script — byte-identical to Chrome"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js"
      provides: "Safari extractCaseNumber helper — byte-identical to Chrome"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html"
      provides: "Safari SVCNOW form — byte-identical to Chrome"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.css"
      provides: "Safari brand-compliant styling — byte-identical to Chrome"
    - path: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.js"
      provides: "Safari popup logic — byte-identical to Chrome"
    - path: "vigil-extension/__tests__/parity.test.ts"
      provides: "Drift-detector test asserting Chrome/Safari file-set equivalence (Phase 114 EXT-02 pattern)"
  key_links:
    - from: "vigil-extension/popup.html"
      to: "vigil-safari-extension/Vigil Capture Extension/Resources/popup.html"
      via: "byte-identical lock-step (Phase 114 EXT-02 D-02 pattern)"
      pattern: "Keep in lockstep with"
    - from: "vigil-extension/__tests__/parity.test.ts"
      to: "all 7 file pairs"
      via: "readFileSync + assertEqual (drift-detector)"
      pattern: "assert\\.(equal|deepEqual)"
---

<objective>
Ship SVCNOW-05 Safari extension lock-step port: byte-identical mirror of the Chrome popup, service worker, content script, and helpers into `vigil-safari-extension/Vigil Capture Extension/Resources/`, plus a parity drift-detector test that fails CI if any Chrome ↔ Safari file pair diverges. Mirrors Phase 114 EXT-02 lock-step pattern (D-13).

Purpose: Phase 114 established that every browser-extension feature ships in BOTH manifests + popups in the SAME commit — never Chrome-first-and-Safari-later. SVCNOW must reach Safari operators identically.
Output: 7 mirrored Safari files + parity test. The Xcode project (`SafariWebExtensionHandler.swift`) requires NO changes (RESEARCH Probe 3: native messaging is unaffected).

NOTE: Per RESEARCH Assumption A2, Safari WKWebExtension MV3 support may differ subtly for `chrome.action.disable()`. The byte-identical port targets the SAME source surface; if Safari fails at runtime, a one-line `globalThis.browser?.action ?? chrome.action` shim adjustment is the documented fallback (deferred to operator UAT in 129-06 if encountered). Per CONTEXT D-13 and Phase 114 EXT-02 invariant, byte-identical is the strict goal — fall back to a documented diff ONLY if operator UAT proves Safari incompatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-CONTEXT.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-RESEARCH.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md
@.planning/phases/114-extension-parity

@vigil-extension/manifest.json
@vigil-extension/background.js
@vigil-extension/content-script.js
@vigil-extension/popup-helpers.js
@vigil-extension/popup.html
@vigil-extension/popup.css
@vigil-extension/popup.js

<interfaces>
<!-- Source: vigil-safari-extension/Vigil Capture Extension/Resources/ (existing 1:1 mirror of vigil-extension/) -->
<!-- RESEARCH Probe 3: Resources folder is already a clean lock-step parity zone; popup.html/css/js mirror Chrome -->

<!-- PATTERNS.md Shared Patterns → lockstep comment header (popup.html line 2, popup.js line 2, popup.css line 1):
<!-- Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/<file> — Phase 129 (D-13) -->

<!-- Chrome side direction (already authored in 129-03): the source-of-truth files live in vigil-extension/. -->
<!-- Safari side is the COPY target — every byte of the Chrome popup files goes verbatim into Safari Resources. -->

<!-- Drift-detector test pattern -->
- Read both file pairs with readFileSync.
- Manifest comparison: parse JSON, compare structured fields (set-equality on permissions/host_permissions/content_scripts arrays).
- Code file comparison: byte-equal assertion (popup.html, popup.css, popup.js, popup-helpers.js, background.js, content-script.js).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Chrome → Safari for all 7 source files</name>
  <files>vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json, vigil-safari-extension/Vigil Capture Extension/Resources/background.js, vigil-safari-extension/Vigil Capture Extension/Resources/content-script.js, vigil-safari-extension/Vigil Capture Extension/Resources/popup-helpers.js, vigil-safari-extension/Vigil Capture Extension/Resources/popup.html, vigil-safari-extension/Vigil Capture Extension/Resources/popup.css, vigil-safari-extension/Vigil Capture Extension/Resources/popup.js</files>
  <read_first>
    - vigil-extension/manifest.json (source of truth from 129-03)
    - vigil-extension/background.js (source from 129-03)
    - vigil-extension/content-script.js (source from 129-03)
    - vigil-extension/popup-helpers.js (source from 129-03)
    - vigil-extension/popup.html (source from 129-03)
    - vigil-extension/popup.css (source from 129-03)
    - vigil-extension/popup.js (source from 129-03)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md (Safari extension files section)
  </read_first>
  <action>
    For each of the 7 Chrome files authored in plan 129-03, write a byte-identical copy into the Safari Resources directory at `vigil-safari-extension/Vigil Capture Extension/Resources/`. Use the `Write` tool with the exact content (read from Chrome side, write to Safari side — same content, same line endings).

    The lockstep comment header (Phase 129 D-13) is INTENTIONALLY identical on both sides — operators reading either source see the same lockstep reference; the parity test enforces this. Specifically:
    - In `popup.html`, line 2 reads `<!-- Keep in lockstep with ../vigil-safari-extension/Vigil Capture Extension/Resources/popup.html — Phase 129 (D-13) -->` on the CHROME side. On the SAFARI side, the lockstep comment is identical (or symmetrically points at the Chrome path) — pick one direction and apply consistently on BOTH sides so byte-equality holds. Recommended: keep the Chrome-side comment text verbatim on Safari (operators reading Safari source see the same reference pointing at the same "lockstep relationship").
    - Same approach for `popup.js` line 2, `popup.css` line 1, `background.js`, `content-script.js`, `popup-helpers.js`.

    The Xcode project file (`Vigil Capture Extension/Vigil Capture Extension.xcodeproj`) does NOT need editing — adding new resource files (`background.js`, `content-script.js`, `popup-helpers.js`) requires updating the Xcode project's `Copy Bundle Resources` build phase. Per RESEARCH Probe 3: `SafariWebExtensionHandler.swift` is for native messaging only. New JS files placed in `Resources/` are typically picked up by Xcode's project pattern, but if the project uses explicit file references, the operator must add the new files to the project in Xcode (DOCUMENT this in the SUMMARY and surface as a UAT step in plan 129-06; do NOT attempt to edit `.xcodeproj` from a Linux dev box).

    Per CONTEXT D-13 and PATTERNS.md, Safari uses the SAME `chrome.*` API surface (WKWebExtension polyfills it). If operator UAT in 129-06 surfaces a Safari incompatibility (RESEARCH Assumption A2), apply a one-line shim and re-enforce parity in a follow-up — do NOT pre-emptively diverge here.
  </action>
  <verify>
    <automated>cd /home/morrillboss/dev/dailybrief && for f in manifest.json background.js content-script.js popup-helpers.js popup.html popup.css popup.js; do diff -q "vigil-extension/$f" "vigil-safari-extension/Vigil Capture Extension/Resources/$f" || exit 1; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 files exists under `vigil-safari-extension/Vigil Capture Extension/Resources/`.
    - `diff -q vigil-extension/manifest.json "vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json"` reports no difference (or only whitespace, if the project standard tolerates that; strict goal is byte-identical).
    - Same `diff -q` clean for `popup.html`, `popup.css`, `popup.js`, `popup-helpers.js`, `background.js`, `content-script.js`.
    - Safari `manifest.json` parses as valid JSON.
    - Safari `manifest.json` includes `tabs` in `permissions`, `*://*.service-now.com/*` in `host_permissions`, `background.service_worker = "background.js"`, and `content_scripts` with the SN match + popup-helpers.js+content-script.js js array.
    - Safari `popup.html` references the SAME script tags in the same order as Chrome (popup-helpers.js before popup.js).
  </acceptance_criteria>
  <done>
    Seven Safari files are byte-identical to Chrome counterparts; Resources directory ready for Xcode rebuild (operator-side UAT step).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author parity drift-detector test</name>
  <files>vigil-extension/__tests__/parity.test.ts</files>
  <read_first>
    - vigil-extension/__tests__/popup-helpers.test.ts (existing test style — node:test + tsx, from 129-03)
    - vigil-g2-plugin/src/__tests__/main.test.ts (drift-detector readFileSync pattern, lines 20-39)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md (Pattern Assignments → parity.test.ts; Shared Patterns → Node:test drift-detector)
    - vigil-extension/manifest.json + vigil-safari-extension/Vigil Capture Extension/Resources/manifest.json (the pair under test)
  </read_first>
  <action>
    Create `vigil-extension/__tests__/parity.test.ts`. Use node:test + assert/strict. Compute absolute paths to both file sets via `fileURLToPath(import.meta.url)` + `dirname` + `resolve`.

    Required tests:
    1. `test("SVCNOW-05: Chrome + Safari manifest permissions arrays match (set equality)")` — parse both manifests, sort the `permissions` arrays, assert.deepEqual.
    2. `test("SVCNOW-05: Chrome + Safari manifest host_permissions match")` — same, on `host_permissions`.
    3. `test("SVCNOW-05: Chrome + Safari manifest background.service_worker matches")` — assert.equal both `manifest.background.service_worker` strings.
    4. `test("SVCNOW-05: Chrome + Safari manifest content_scripts[0].matches match")` — sort + deepEqual.
    5. `test("SVCNOW-05: Chrome + Safari manifest content_scripts[0].js array matches in order")` — deepEqual (preserve order — Chrome loads popup-helpers.js before content-script.js, Safari must too).
    6. `test("SVCNOW-05: Chrome + Safari popup.html are byte-identical")` — assert.equal(chromeHtml, safariHtml).
    7. `test("SVCNOW-05: Chrome + Safari popup.css are byte-identical")` — same.
    8. `test("SVCNOW-05: Chrome + Safari popup.js are byte-identical")` — same.
    9. `test("SVCNOW-05: Chrome + Safari popup-helpers.js are byte-identical")` — same.
    10. `test("SVCNOW-05: Chrome + Safari background.js are byte-identical")` — same.
    11. `test("SVCNOW-05: Chrome + Safari content-script.js are byte-identical")` — same.

    If, in any future plan, RESEARCH Assumption A2 forces a Safari-specific shim (`browser.action` vs `chrome.action`), the test should be relaxed at THAT point — for now strict byte-equality is the assertion (D-13 strict lock-step). Document this in the test file's header comment.

    Test must run via `npx tsx --test` from `vigil-extension/` (PATTERNS.md Shared Patterns) — same runner already used by `popup-helpers.test.ts`.
  </action>
  <verify>
    <automated>cd vigil-extension && npx tsx --test "__tests__/parity.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-extension/__tests__/parity.test.ts` exists.
    - The file has 11 distinct `test(...)` cases (covering manifest fields + 6 source-file byte-identity assertions).
    - Running `cd vigil-extension && npx tsx --test "__tests__/parity.test.ts"` exits with status 0.
    - Manually corrupting any Safari file (e.g., adding a stray newline to Safari `popup.html`) causes the parity test to fail — sanity check, do not commit the corrupt state.
    - The test header comment documents that strict byte-equality is the policy AND that RESEARCH Assumption A2 (Safari WKWebExtension API shim) is the documented exception path for future plans.
  </acceptance_criteria>
  <done>
    Parity drift-detector test green; future Chrome-only edits will fail CI until mirrored to Safari (Phase 114 EXT-02 invariant enforced).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome popup ↔ Safari popup | Different browsers, same source; drift introduces inconsistent operator experience. |
| WKWebExtension API surface | Safari's `chrome.*` polyfill may differ subtly from Chrome's; RESEARCH Assumption A2 flags this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-19 | Tampering | Chrome-only edit lands; Safari diverges | mitigate | Parity drift-detector test fails CI on any byte-difference; Phase 114 EXT-02 invariant. Acceptance test asserts byte-identity for all 6 source files + 5 manifest-field comparisons. |
| T-129-20 | Tampering | API key in Safari extension chrome.storage.local | accept | Existing Phase 84 storage scheme; no new risk introduced by SVCNOW port. |
| T-129-21 | Information Disclosure | document.title from third-party origin → Safari popup | mitigate | Same mitigation as Chrome (T-129-09): popup uses textContent only. Byte-identical port means same protection. |
| T-129-SC | Tampering | npm/pip/cargo installs | N/A | No new external packages installed. |
</threat_model>

<verification>
- `cd vigil-extension && npx tsx --test "__tests__/parity.test.ts"` green.
- `diff -qr vigil-extension/{manifest.json,popup.html,popup.css,popup.js,popup-helpers.js,background.js,content-script.js} "vigil-safari-extension/Vigil Capture Extension/Resources/"` shows no differences for each file.
- Manual UAT in 129-06: operator loads the Safari extension on macOS; same SVCNOW form renders.
</verification>

<success_criteria>
- All SVCNOW-05 truths in must_haves verifiable by automated tests; parity drift-detector locks future edits to lock-step; D-13 invariant enforced.
</success_criteria>

<output>
Create `.planning/phases/129-lifecycle-restore-servicenow-popup/129-05-SUMMARY.md` when done.
</output>
