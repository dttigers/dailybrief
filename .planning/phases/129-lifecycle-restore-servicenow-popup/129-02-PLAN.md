---
phase: 129-lifecycle-restore-servicenow-popup
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - vigil-g2-plugin/src/lib/screen-state-restore.ts
  - vigil-g2-plugin/src/lib/launch-source-helpers.ts
  - vigil-g2-plugin/src/navigation.ts
  - vigil-g2-plugin/src/main.ts
  - vigil-g2-plugin/src/lib/__tests__/screen-state-restore.test.ts
  - vigil-g2-plugin/src/__tests__/main.test.ts
autonomous: true
requirements: [G2-LIFECYCLE-01, G2-LIFECYCLE-02, G2-LIFECYCLE-03]
requirements_addressed: [G2-LIFECYCLE-01, G2-LIFECYCLE-02, G2-LIFECYCLE-03]
tags: [g2-plugin, lifecycle, background-state, local-storage, even-sdk]

must_haves:
  truths:
    - "Plugin registers `setBackgroundState('vigil-companion-state', ...)` and `setBackgroundState('vigil-screen-state', ...)` plus matching `onBackgroundRestore` handlers at MODULE SCOPE before `init()`"
    - "D-09: Both mechanisms required and non-overlapping — `setBackgroundState`/`onBackgroundRestore` (in-session background→foreground) AND `setLocalStorage`/`getLocalStorage` (cross-relaunch). Plan registers both at module scope."
    - "D-05: Every call to `navigateTo(screen, bridge)` and `navigateToTaskDetail(...)` writes `{screen, args?, savedAt: Date.now()}` to `bridge.setLocalStorage('vigil:v3:lastScreen', ...)` (fire-and-forget). Wall-clock TTL semantics from `savedAt`."
    - "D-05/D-06: On plugin re-launch with `source !== 'glassesMenu'`, `pickInitialScreen` reads the stored `lastScreen`; if `Date.now() - savedAt <= 30 * 60 * 1000` it returns the stored screen, else HOME. Pocket-gap fallback to HOME (D-06) is intentional and acceptance-tested via the 30:01 boundary case."
    - "D-08: Stored `args` shape is id-only — `navigateToTaskDetail` stores `{ screen: Screen.TASK_DETAIL, args: { id: task.caseNumber }, savedAt: Date.now() }` — never embedded entity snapshots. Re-fetch on restore is the source of truth."
    - "On plugin re-launch with `source === 'glassesMenu'`, restore logic is bypassed (D-10 invariant from Phase 124 G2-POLISH-06 preserved)"
    - "Companion HUD `activeSessions` + `bannerState` are restored from `setBackgroundState('vigil-companion-state', ...)` snapshot on background→foreground migration (D-11 free win)"
    - "Companion HUD restore callback receives the FakeBridge-emitted onBackgroundRestore payload verbatim (deep-equal) — the registered restoreCompanionSnapshot fn is observably invoked with `{ activeSessions, bannerState }` (D-11 behavioral, not just registration)"
    - "On 404 re-fetch during restore (D-07), navigation lands on WORK_ORDERS list (parent), not HOME (root) — restoreScreenFn with args={id:'WO-1234'} and a FakeBridge mocked re-fetch returning null|undefined routes to WORK_ORDERS"
    - "Existing D-07 drift test (21 assertions in main.test.ts) remains green"
    - "New drift test asserts `setBackgroundState(` source index < `function init(` source index in main.ts"
    - "New drift test asserts TTL constant `30 * 60 * 1000` is present verbatim in screen-state-restore.ts"
    - "Feature-detect guard (mirroring audio-session-guard.ts:136-141) wraps both `setBackgroundState` and `onBackgroundRestore` calls; dev-preview bridge without these methods does NOT crash"
  artifacts:
    - path: "vigil-g2-plugin/src/lib/screen-state-restore.ts"
      provides: "registerBackgroundStateHandlers(bridge, ...) module-scope registration helper + ScreenRestoreBridge interface + TTL constant + LAST_SCREEN_LS_KEY"
      min_lines: 60
    - path: "vigil-g2-plugin/src/lib/launch-source-helpers.ts"
      provides: "Extended pickInitialScreen with optional bridge.getLocalStorage parameter and TTL-gated restore branch"
    - path: "vigil-g2-plugin/src/navigation.ts"
      provides: "navigateTo + navigateToTaskDetail with fire-and-forget setLocalStorage writes"
    - path: "vigil-g2-plugin/src/main.ts"
      provides: "Module-scope registerBackgroundStateHandlers(bridgeInstance, ...) call inserted BEFORE function init()"
    - path: "vigil-g2-plugin/src/lib/__tests__/screen-state-restore.test.ts"
      provides: "TTL boundary tests (29:59 → restore, 30:01 → HOME), feature-detect tests, companion snapshot tests, D-07 404-fallback test"
    - path: "vigil-g2-plugin/src/__tests__/main.test.ts"
      provides: "Existing 21 D-07 assertions + new setBackgroundState-precedes-init drift test + glassesMenu-bypass assertion"
  key_links:
    - from: "vigil-g2-plugin/src/main.ts"
      to: "vigil-g2-plugin/src/lib/screen-state-restore.ts"
      via: "module-scope registerBackgroundStateHandlers call"
      pattern: "registerBackgroundStateHandlers\\("
    - from: "vigil-g2-plugin/src/navigation.ts"
      to: "vigil-g2-plugin/src/lib/screen-state-restore.ts"
      via: "LAST_SCREEN_LS_KEY constant export"
      pattern: "vigil:v3:lastScreen"
    - from: "vigil-g2-plugin/src/lib/launch-source-helpers.ts"
      to: "vigil-g2-plugin/src/lib/screen-state-restore.ts"
      via: "TTL constant + key constant import or duplication"
      pattern: "30\\s*\\*\\s*60\\s*\\*\\s*1000"
---

<objective>
Ship G2-LIFECYCLE-01/02/03: G2 plugin remembers its last-viewed screen across plugin re-launch via `bridge.setLocalStorage` (G2-LIFECYCLE-02) AND survives in-session phone-background → foreground migration via `setBackgroundState`/`onBackgroundRestore` (G2-LIFECYCLE-01), while preserving the Phase 124 `onLaunchSource('glassesMenu')` precedence invariant (G2-LIFECYCLE-03). Companion HUD active-session/banner cache folds into the same registration path as the documented "free win" (D-11).

Purpose: Operator workflow continuity — restart phone, reopen plugin, land back where they were (D-05 wall-clock TTL semantics). Pure G2-plugin work; no server changes.
Output: New `screen-state-restore.ts` module, modified `navigation.ts` + `launch-source-helpers.ts` + `main.ts`, and unit/drift tests for TTL boundary, glassesMenu bypass, and module-scope registration ordering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-CONTEXT.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-RESEARCH.md
@.planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md
@.planning/phases/124-g2-polish-bundle

@vigil-g2-plugin/src/lib/audio-session-guard.ts
@vigil-g2-plugin/src/lib/launch-source-helpers.ts
@vigil-g2-plugin/src/navigation.ts
@vigil-g2-plugin/src/main.ts
@vigil-g2-plugin/src/__tests__/main.test.ts
@vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts

<interfaces>
<!-- Canonical setBackgroundState/onBackgroundRestore registration pattern -->
<!-- Source: vigil-g2-plugin/src/lib/audio-session-guard.ts -->

AudioGuardBridge interface (lines 54-59) is the structural template for ScreenRestoreBridge — declare locally to avoid importing from SDK directly. The new interface MUST expose:
- setBackgroundState(key: string, snapshot: () => unknown): void
- onBackgroundRestore(key: string, restore: (saved: unknown) => void): void
- setLocalStorage(key: string, value: string): Promise<void>
- getLocalStorage(key: string): Promise<string | null>

Feature-detect wrapper (audio-session-guard.ts lines 136-154) — copy verbatim, swap key names and snapshot/restore callbacks.

Module-scope state pattern (audio-session-guard.ts lines 63-66):
- Module-level `let` for mutable state
- Module-level `const` for keys + TTL constant
- Export `__resetForTesting()` to clear state between tests

Existing `pickInitialScreen` (launch-source-helpers.ts lines 112-119):
- Signature: `(source: LaunchSource, fetchSessions: () => Promise<AgentSessionRow[]>) => Promise<ScreenName>`
- Extended signature: append optional `bridge?: { getLocalStorage: (key: string) => Promise<string | null> }`
- D-10 guard: the `source !== 'glassesMenu'` branch is the ONLY branch that gains the TTL-gated restore lookup; glassesMenu branch is UNTOUCHED.

Existing `navigateTo` (navigation.ts lines 139-147) and `navigateToTaskDetail` (navigation.ts lines 185-196):
- After `await bridge.rebuildPageContainer(container)`, insert a fire-and-forget `setLocalStorage` write of the new lastScreen JSON shape.
- `navigateToTaskDetail` stores `{ screen: Screen.TASK_DETAIL, args: { id: task.caseNumber }, savedAt: Date.now() }` (id-only per D-08).

Existing main.ts module-scope (lines 82-85):
- `const bridgeInstance = EvenAppBridge.getInstance()` is at module scope.
- `const launchSourcePromise = new Promise(...)` registers `onLaunchSource` at module scope.
- NEW `registerBackgroundStateHandlers(bridgeInstance, ...)` call MUST be inserted BETWEEN these and `async function init()` — same module-scope-before-init constraint as Phase 124 D-07.

D-07 drift test (main.test.ts lines 44-59) reads main.ts source with `readFileSync`, strips comments, and asserts `onLaunchSource(` source-index < `function init(` source-index. The new drift test mirrors this for `setBackgroundState(`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author screen-state-restore.ts module + unit tests (TDD)</name>
  <files>vigil-g2-plugin/src/lib/screen-state-restore.ts, vigil-g2-plugin/src/lib/__tests__/screen-state-restore.test.ts</files>
  <read_first>
    - vigil-g2-plugin/src/lib/audio-session-guard.ts (canonical setBackgroundState/onBackgroundRestore + feature-detect pattern — PATTERNS.md exact analog)
    - vigil-g2-plugin/src/lib/__tests__/audio-session-guard.test.ts (FakeBridge pattern, __resetForTesting reset between cases)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md (Pattern Assignments → screen-state-restore.ts)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-RESEARCH.md (Code Examples → setBackgroundState registration at module scope)
  </read_first>
  <behavior>
    - extractCaseNumber-equivalent helper `pickRestoredScreen(stored, now)` returns stored.screen when (now - stored.savedAt) is ≤ TTL_MS; returns HOME otherwise.
    - TTL_MS is exactly `30 * 60 * 1000` (locked by D-05).
    - At wall-clock boundary 29 min 59 sec elapsed → returns stored screen.
    - At wall-clock boundary 30 min 01 sec elapsed → returns HOME.
    - When stored.savedAt is missing/non-numeric → returns HOME.
    - When stored object is malformed/null → returns HOME without throwing.
    - registerBackgroundStateHandlers does NOT throw when bridge lacks setBackgroundState/onBackgroundRestore (dev-preview gap from RESEARCH Pitfall 7); emits console.warn instead.
    - registerBackgroundStateHandlers registers BOTH `vigil-companion-state` AND `vigil-screen-state` keys when bridge supports them.
    - Restoring `vigil-companion-state` snapshot calls the provided restoreCompanionSnapshot callback with the deserialized payload (D-11 behavioral coverage — verify callback observably invoked with `{ activeSessions, bannerState }` payload, not just registration).
    - Restoring `vigil-screen-state` snapshot calls the provided restoreScreenFn callback with the deserialized `{screen, args?}` payload (does NOT block on async).
    - On 404 re-fetch during restore of parameterized screens (D-07): when restoreScreenFn is invoked with `args={id:'WO-1234'}` and the FakeBridge-mocked re-fetch returns null/undefined (simulated 404 — entity deleted or out-of-scope), the resulting navigation target is the WORK_ORDERS list screen — NOT HOME, NOT an error screen. The fall-back to parent list is observable via the FakeBridge's recorded navigateTo invocation.
  </behavior>
  <action>
    Create `vigil-g2-plugin/src/lib/screen-state-restore.ts` following the `audio-session-guard.ts` shape:

    1. Declare module-local interface `ScreenRestoreBridge` exposing `setBackgroundState`, `onBackgroundRestore`, `setLocalStorage`, `getLocalStorage` (do NOT import from SDK; mirror AudioGuardBridge isolation rationale).
    2. Export constants: `SCREEN_STATE_KEY = 'vigil-screen-state'`, `COMPANION_STATE_KEY = 'vigil-companion-state'`, `LAST_SCREEN_LS_KEY = 'vigil:v3:lastScreen'`, `TTL_MS = 30 * 60 * 1000` (inline literal — preserves drift-detector visibility per D-05).
    3. Export `StoredLastScreen` type `{ screen: ScreenName; args?: { id: string | number }; savedAt: number }`.
    4. Export pure function `pickRestoredScreen(stored: unknown, now: number): ScreenName` — returns `stored.screen` if `stored` is a valid object with numeric `savedAt` within TTL, else returns the `HOME` constant from navigation.ts.
    5. Export `registerBackgroundStateHandlers(bridge: ScreenRestoreBridge, getCompanionSnapshot, restoreCompanionSnapshot, getScreenSnapshot, restoreScreenFn)` wrapping both registrations in a single feature-detect block mirroring audio-session-guard.ts:136-154. On dev-preview (no setBackgroundState), emit `console.warn('[screen-state-restore] Dev preview bridge — setBackgroundState/onBackgroundRestore unavailable')` and return without throwing.
    6. Export `__resetForTesting()` clearing any module-level mutable state (none expected, but include to match audio-session-guard.ts pattern).

    Then write `vigil-g2-plugin/src/lib/__tests__/screen-state-restore.test.ts` covering the behaviors listed in `<behavior>`. Use the FakeBridge shape from `audio-session-guard.test.ts`: in-memory Maps for localStorage + capture of bg state snapshot/restore callbacks. The FakeBridge MUST expose a recorded `navigateTo` mock so the D-07 404-fallback test can assert which screen was targeted. Also add a drift-detector test reading the source file: `assert.match(restoreSrc, /30\s*\*\s*60\s*\*\s*1000/)`.

    For the D-07 404-fallback test: construct restoreScreenFn so that when the stored screen is TASK_DETAIL with `args={id:'WO-1234'}` and the FakeBridge's re-fetch hook (e.g., `bridge.fetchWorkOrderById('WO-1234')`) returns `null`/`undefined`, the resulting navigation call lands on `Screen.WORK_ORDERS` (parent list). Assert via `bridge.recordedNavigateToCalls[0] === Screen.WORK_ORDERS`. The restoreScreenFn implementation lives in main.ts (Task 2 wires it), but Task 1's test exercises restoreScreenFn through a stub that mirrors the same code path.

    For the D-11 companion-snapshot test: construct a FakeBridge that fires the captured `onBackgroundRestore('vigil-companion-state', cb)` callback with payload `{ activeSessions: [mockSession], bannerState: 'active' }`. Assert that the registered `restoreCompanionSnapshot` mock received that payload verbatim (deep-equal via `assert.deepStrictEqual` — same object identity NOT required, but structure MUST match).

    Per D-05 / D-06, TTL is wall-clock from save; pocket-gap fallback to HOME is intentional. Per D-08, `args` shape stays id-only (never embedded snapshots).
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npx tsx --test "src/lib/__tests__/screen-state-restore.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `pickRestoredScreen({screen:'work-orders', savedAt: Date.now() - (30*60*1000 - 1000)}, Date.now())` returns `'work-orders'` (29min 59s — D-05 boundary).
    - `pickRestoredScreen({screen:'work-orders', savedAt: Date.now() - (30*60*1000 + 1000)}, Date.now())` returns `HOME` (30min 01s — TTL expired).
    - `pickRestoredScreen({screen:'work-orders'}, Date.now())` (missing savedAt) returns `HOME` without throwing.
    - `pickRestoredScreen(null, Date.now())` returns `HOME` without throwing.
    - `pickRestoredScreen('garbage', Date.now())` returns `HOME` without throwing.
    - `registerBackgroundStateHandlers(fakeNoBgSupport, ...)` does NOT throw; the test asserts no exception was thrown and that a console.warn fired.
    - `registerBackgroundStateHandlers(fakeWithBgSupport, ...)` registers BOTH `vigil-companion-state` AND `vigil-screen-state` (test asserts both keys appear in the FakeBridge's recorded snapshot map).
    - D-11 companion-snapshot hydration test: when FakeBridge invokes the captured `onBackgroundRestore('vigil-companion-state', cb)` with payload `{ activeSessions: [mockSession], bannerState: 'active' }`, the registered `restoreCompanionSnapshot` mock is called exactly once, AND `assert.deepStrictEqual(receivedPayload, { activeSessions: [mockSession], bannerState: 'active' })` passes (verbatim payload — covers D-11 behavioral, not registration-only).
    - D-07 404-fallback test: stored screen is `Screen.TASK_DETAIL` with `args={id:'WO-1234'}`; FakeBridge's mocked re-fetch returns `null` (simulated 404); restoreScreenFn is invoked with the stored payload; FakeBridge's `recordedNavigateToCalls` array's first entry equals `Screen.WORK_ORDERS` (parent list — NOT HOME, NOT an error screen). Also assert NO entry equals `Screen.HOME` in the recorded calls for this test case.
    - Drift-detector test: source file contains `/30\s*\*\s*60\s*\*\s*1000/` (verbatim TTL constant).
    - `npx tsc --noEmit` clean in vigil-g2-plugin against the new file.
  </acceptance_criteria>
  <done>
    `screen-state-restore.ts` exports work (interface + constants + pure helper + register fn + reset). Unit test file passes all behaviors above including D-11 companion payload hydration and D-07 404-fallback to WORK_ORDERS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire navigation.ts + launch-source-helpers.ts + main.ts; extend main.test.ts drift coverage</name>
  <files>vigil-g2-plugin/src/navigation.ts, vigil-g2-plugin/src/lib/launch-source-helpers.ts, vigil-g2-plugin/src/main.ts, vigil-g2-plugin/src/__tests__/main.test.ts</files>
  <read_first>
    - vigil-g2-plugin/src/navigation.ts (existing `navigateTo` at lines 139-147; `navigateToTaskDetail` at lines 185-196 — file to modify)
    - vigil-g2-plugin/src/lib/launch-source-helpers.ts (existing `pickInitialScreen` at lines 112-119 — file to modify)
    - vigil-g2-plugin/src/main.ts (existing `onLaunchSource` registration at lines 82-85 — module-scope insertion point)
    - vigil-g2-plugin/src/__tests__/main.test.ts (existing D-07 drift test at lines 44-59 — pattern for new drift test; existing readFileSync helpers at lines 29-39)
    - vigil-g2-plugin/src/lib/screen-state-restore.ts (the module from Task 1 — its exports are consumed here)
    - .planning/phases/129-lifecycle-restore-servicenow-popup/129-PATTERNS.md (Pattern Assignments → navigation.ts, launch-source-helpers.ts, main.ts, main.test.ts)
  </read_first>
  <behavior>
    - On every `navigateTo(screen, bridge)` call, AFTER `rebuildPageContainer` resolves, a fire-and-forget `bridge.setLocalStorage('vigil:v3:lastScreen', JSON.stringify({screen, savedAt: Date.now()}))` write is issued; failure is silently swallowed via `.catch(() => {})`.
    - On every `navigateToTaskDetail(itemIndex, bridge)` call, AFTER `rebuildPageContainer` resolves, the same write fires with `args: { id: task.caseNumber }` per D-08 (id-only).
    - `pickInitialScreen(source, fetchSessions, bridge?)` returns the stored screen when `source !== 'glassesMenu'` AND the bridge has fresh lastScreen JSON (within 30-min TTL); returns HOME on malformed/expired/missing; returns the existing COMPANION/HOME logic UNCHANGED when `source === 'glassesMenu'`.
    - main.ts module-scope `registerBackgroundStateHandlers(bridgeInstance, ...)` call is inserted between line 85 and the existing `async function init(` declaration; companion-snapshot callbacks read from companion.ts module-exported state, screen-snapshot callback reads from navigation.ts `getCurrentScreen()`.
    - main.ts restoreScreenFn implementation: for parameterized screens (TASK_DETAIL), re-fetch the entity by id; on 404 (null/undefined re-fetch result) navigate to the parent list (WORK_ORDERS), NOT HOME (D-07 invariant).
    - All existing 21 D-07 drift assertions in main.test.ts remain green.
    - NEW drift test asserts `mainNoComments.indexOf("setBackgroundState(")` is > 0 AND < `mainNoComments.search(/(?:async\s+)?function\s+init\s*\(/)`.
    - NEW drift test asserts `helpersNoComments.match(/30\s*\*\s*60\s*\*\s*1000/)` (TTL constant referenced in launch-source-helpers.ts or imported from screen-state-restore.ts — drift-detector tolerates either form).
    - NEW unit-level assertion: invoking the extended `pickInitialScreen` with `source: 'glassesMenu'` and a populated bridge still returns COMPANION/HOME via the existing fetchSessions path (D-10 glassesMenu precedence intact).
    - NEW source-level assertion: main.ts source contains a restoreScreenFn body that routes parameterized 404s to WORK_ORDERS (drift detector accepts regex `/Screen\.WORK_ORDERS/` near the restoreScreenFn definition; Task 1's behavioral test already covers the runtime semantics).
  </behavior>
  <action>
    Edit `vigil-g2-plugin/src/navigation.ts`:
    1. Import `LAST_SCREEN_LS_KEY` from `./lib/screen-state-restore.ts`.
    2. After `await bridge.rebuildPageContainer(container)` in `navigateTo`, append a fire-and-forget `setLocalStorage` write using the casting pattern shown in PATTERNS.md (cast through `unknown` to a structural interface exposing optional setLocalStorage; chain `.catch(() => {})`).
    3. In `navigateToTaskDetail`, do the same AFTER the rebuild, storing `{ screen: Screen.TASK_DETAIL, args: { id: task.caseNumber }, savedAt: Date.now() }` per D-08.

    Edit `vigil-g2-plugin/src/lib/launch-source-helpers.ts`:
    1. Append an optional third parameter `bridge?: { getLocalStorage: (key: string) => Promise<string | null> }` to `pickInitialScreen`.
    2. In the `source !== 'glassesMenu'` branch, attempt the TTL-gated restore via `bridge?.getLocalStorage(LAST_SCREEN_LS_KEY)`; parse the JSON; if `pickRestoredScreen(stored, Date.now()) !== HOME` return that screen; else fall through to HOME. Wrap the JSON.parse in a try/catch (malformed JSON → fall through to HOME — D-05 / RESEARCH Common-Pitfall edge). The `source === 'glassesMenu'` branch is UNCHANGED (D-10 invariant).
    3. Import `pickRestoredScreen`, `LAST_SCREEN_LS_KEY`, and `TTL_MS` from `./screen-state-restore.ts`. TTL constant is owned by screen-state-restore.ts; launch-source-helpers.ts consumes the imported value (RESEARCH Q1: avoid duplication while keeping the drift detector intact).

    Edit `vigil-g2-plugin/src/main.ts`:
    1. Import `registerBackgroundStateHandlers` from `./lib/screen-state-restore.ts`.
    2. AFTER line 85 (after `launchSourcePromise` declaration) and BEFORE the `async function init(` declaration, insert a top-level call to `registerBackgroundStateHandlers(bridgeInstance, ...)` with the four callbacks (companion-snapshot getter/setter from companion.ts, screen-snapshot getter from navigation.ts `getCurrentScreen()`, restoreScreenFn that calls `navigateTo(screen, bridge)` for plain screens and `navigateToTaskDetail(...)` for parameterized ones; on 404 from re-fetch — i.e., null/undefined return from the work-order lookup helper — fall back to `Screen.WORK_ORDERS` via `navigateTo(Screen.WORK_ORDERS, bridge)` per D-07; do NOT fall back to HOME).
    3. Update the existing `pickInitialScreen` call inside `init()` to pass `bridgeInstance` as the third argument.

    Edit `vigil-g2-plugin/src/__tests__/main.test.ts`:
    1. Append a new `test("D-129 drift: setBackgroundState registration at MODULE SCOPE (before init)")` mirroring lines 44-59. Use the existing `mainNoComments` variable already in scope. Assert `bgIdx > 0`, `initIdx > 0`, and `bgIdx < initIdx`.
    2. Append a new `test("D-129 drift: TTL constant 30 * 60 * 1000 present in helpers (via screen-state-restore import)")` using `helpersNoComments`. Accept either the literal `30 * 60 * 1000` OR an import of `TTL_MS` from `./screen-state-restore` — regex `/(?:30\s*\*\s*60\s*\*\s*1000|TTL_MS)/`.
    3. Append a new `test("D-129: pickInitialScreen with glassesMenu source bypasses restore (D-10)")` constructing a stub bridge whose `getLocalStorage` returns a still-fresh lastScreen JSON; call `pickInitialScreen('glassesMenu', () => Promise.resolve([{...activeSession}]), bridge)`; assert result is COMPANION (existing path returned because glassesMenu was given).
    4. Append a new `test("D-129 drift: main.ts restoreScreenFn routes 404 to WORK_ORDERS (D-07 source assertion)")` reading main.ts with readFileSync, stripping comments, then asserting `assert.match(mainNoComments, /Screen\.WORK_ORDERS/)` AND that the WORK_ORDERS reference appears within ~20 lines of the `restoreScreenFn` identifier (use `mainNoComments.indexOf('restoreScreenFn')` + windowed substring search). The behavioral assertion already lives in Task 1's screen-state-restore.test.ts; this is the drift-detector source pin.
  </action>
  <verify>
    <automated>cd vigil-g2-plugin && npx tsx --test "src/__tests__/main.test.ts" "src/lib/__tests__/screen-state-restore.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All 21 existing D-07 drift assertions in main.test.ts pass after edits (the line-85 insertion preserves `onLaunchSource(...)` source-index < `function init(` source-index).
    - New drift test: `mainNoComments.indexOf("setBackgroundState(")` returns a positive number that is strictly less than `mainNoComments.search(/(?:async\s+)?function\s+init\s*\(/)`.
    - New glassesMenu-bypass test: stubbed bridge with a fresh lastScreen value is IGNORED when source is `'glassesMenu'`; result equals COMPANION via the existing fetchSessions path.
    - New D-07 404→WORK_ORDERS drift test (source-level): main.ts contains a `Screen.WORK_ORDERS` reference within ~20 lines of the restoreScreenFn definition (pins the D-07 fall-back routing in source).
    - `navigateTo` and `navigateToTaskDetail` issue exactly one `setLocalStorage` call per invocation; the stored JSON has shape `{screen, args?, savedAt}` with `savedAt` a number; for `navigateToTaskDetail`, `args.id === task.caseNumber` (id-only per D-08; no `task` object embedded).
    - `setLocalStorage` failure (rejected promise from FakeBridge) does NOT propagate — the navigation function completes normally.
    - `pickInitialScreen(notGlassesMenu, fetchSessions, fakeBridge)` with stored JSON `savedAt = Date.now() - (30*60*1000 - 1000)` returns the stored screen.
    - `pickInitialScreen(notGlassesMenu, fetchSessions, fakeBridge)` with stored JSON `savedAt = Date.now() - (30*60*1000 + 1000)` returns HOME (TTL expired).
    - `pickInitialScreen(notGlassesMenu, fetchSessions, fakeBridge)` with malformed JSON in storage returns HOME without throwing.
    - `cd vigil-g2-plugin && npx tsc --noEmit` clean.
    - Full G2 plugin test suite (the subset that runs without the unavailable `@evenrealities/even_hub_sdk` package — same baseline as RESEARCH Validation Architecture notes) passes.
  </acceptance_criteria>
  <done>
    Wave 2 G2-LIFECYCLE-01/02/03 implementation complete; D-07 invariant preserved (both runtime via Task 1 behavioral test AND source-level via this task's drift test); new drift tests green; all behaviors verified by automated tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `bridge.setLocalStorage` payload → Even Hub Flutter WebView persistence | Stored JSON is parsed back on next launch; malformed/tampered values must not crash the plugin. |
| `bridge.setBackgroundState` snapshot → SDK serialization | Snapshot must be JSON-serializable; no class instances, Maps, Sets, Dates (RESEARCH Pitfall 1 + audio-session-guard.ts pattern). |
| `restoreScreenFn` 404 path on parameterized screens | Re-fetch on restore may surface deleted/out-of-scope entities; D-07 mandates fall-back to parent list, not HOME. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-04 | Information Disclosure | Stored args contain entity snapshot | mitigate | D-08 mandates id-only args; `navigateToTaskDetail` stores ONLY `{ id: task.caseNumber }`; acceptance criteria asserts no `task.*` other than `caseNumber` appears in stored JSON. |
| T-129-05 | Tampering | Malformed JSON in localStorage on next launch | mitigate | `pickRestoredScreen` + JSON.parse wrapped in try/catch (D-05 fall-through to HOME); acceptance test "malformed JSON → HOME without throwing". |
| T-129-06 | Denial of Service | Re-fetch on restore returns 404 for deleted entity | mitigate | D-07 fall-back to parent list (WORK_ORDERS), NOT home, NOT error state. Task 1 acceptance includes behavioral test (FakeBridge mocked 404 → recorded navigateTo lands on WORK_ORDERS). Task 2 acceptance includes source-level drift assertion (`Screen.WORK_ORDERS` near restoreScreenFn). |
| T-129-07 | Spoofing | glassesMenu launch override | mitigate | D-10 invariant: `source === 'glassesMenu'` bypass is the ONLY explicit guard before restore lookup. Drift test asserts the `glassesMenu` branch is unchanged. |
| T-129-08 | Tampering | Plugin crashes on dev-preview SDK gap | mitigate | Feature-detect wrapper (audio-session-guard.ts:136-141 pattern) — console.warn + early return when bridge lacks setBackgroundState. Acceptance test "dev-preview bridge does not throw". |
| T-129-SC | Tampering | npm/pip/cargo installs | N/A | No new external packages installed in this plan. |
</threat_model>

<verification>
- `cd vigil-g2-plugin && npx tsx --test "src/__tests__/main.test.ts" "src/lib/__tests__/screen-state-restore.test.ts"` green.
- `cd vigil-g2-plugin && npx tsc --noEmit` clean.
- Existing D-07 drift assertions (21/21) remain green per RESEARCH Validation Architecture baseline.
</verification>

<success_criteria>
- All G2-LIFECYCLE-01/02/03 truths in must_haves verifiable by automated tests; D-07 + D-10 invariants preserved; D-11 companion HUD fold delivered with behavioral payload hydration coverage (not registration-only).
</success_criteria>

<output>
Create `.planning/phases/129-lifecycle-restore-servicenow-popup/129-02-SUMMARY.md` when done.
</output>
