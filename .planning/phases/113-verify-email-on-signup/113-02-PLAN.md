---
phase: 113
plan: 02
type: execute
wave: 2
depends_on:
  - "113-01"
files_modified:
  - vigil-core/src/routes/auth.ts
  - vigil-core/src/routes/auth.test.ts
  - vigil-core/src/routes/auth-me.ts
  - vigil-core/src/routes/auth-me.test.ts
  - vigil-core/src/index.ts
autonomous: true
requirements:
  - AUTH-11
must_haves:
  truths:
    - "POST /v1/auth/register fresh-register branch issues an email_verify token row + fires sendEmailVerificationEmail in background (D-06, D-08)"
    - "POST /v1/auth/register claim-flow branch issues a token + send ONLY when existing.emailVerifiedAt IS NULL (D-07)"
    - "POST /v1/auth/login response includes user.emailVerifiedAt as ISO string or null (D-26)"
    - "GET /v1/auth/me returns { id: number, email: string, emailVerifiedAt: string | null } and requires bearerAuth (D-27)"
    - "/v1/auth/verify-email is in the bearerAuth bypass list in index.ts (D-12)"
    - "/v1/auth/me is mounted as a protected route (auth-me router via app.route)"
    - "Register response time stays at hash + INSERT cost — fire-and-forget Resend send does NOT block c.json(...,201) (D-08)"
  artifacts:
    - path: "vigil-core/src/routes/auth.ts"
      provides: "register handler with token issuance + login response with emailVerifiedAt"
      contains: "email_verify"
    - path: "vigil-core/src/routes/auth-me.ts"
      provides: "GET /v1/auth/me — bearerAuth protected, returns id+email+emailVerifiedAt"
      contains: "/auth/me"
    - path: "vigil-core/src/routes/auth.test.ts"
      provides: "test cases for register token issuance + login response shape"
      contains: "AUTH-11"
    - path: "vigil-core/src/routes/auth-me.test.ts"
      provides: "test cases for GET /v1/auth/me shape"
      contains: "auth-me"
    - path: "vigil-core/src/index.ts"
      provides: "bearerAuth bypass for /v1/auth/verify-email + auth-me route mount"
      contains: "/v1/auth/verify-email"
  key_links:
    - from: "vigil-core/src/routes/auth.ts (register handler)"
      to: "vigil-core/src/services/email-service.ts (sendEmailVerificationEmail)"
      via: ".catch() fire-and-forget after c.json(201) returns"
      pattern: "sendEmailVerificationEmail.*\\.catch"
    - from: "vigil-core/src/routes/auth.ts (register handler)"
      to: "vigil-core/src/db/schema.ts (passwordResetTokens with type='email_verify')"
      via: "db.insert(passwordResetTokens).values({ ..., type: 'email_verify' })"
      pattern: "type: \"email_verify\""
    - from: "vigil-core/src/routes/auth-me.ts"
      to: "vigil-core/src/db/schema.ts (users.emailVerifiedAt from Plan 01)"
      via: "db.select({ id, email, emailVerifiedAt }).from(users).where(eq(users.id, userId))"
      pattern: "users\\.emailVerifiedAt"
    - from: "vigil-core/src/index.ts (bearerAuth dispatcher)"
      to: "vigil-core/src/routes/verify-email.ts (Plan 03 — must be reachable without JWT)"
      via: "if (c.req.path === '/v1/auth/verify-email') return next();"
      pattern: "/v1/auth/verify-email"
---

<objective>
Wire up the SERVER-side email verification surface (excluding the verify-email and resend-verification handlers themselves — those are Plan 03):

1. **Register handler** issues an email_verify token + fires the verification email after responding 201.
2. **Login handler** response shape extended to include `emailVerifiedAt` so PWA can render the banner state immediately on login (no second round-trip required for first paint).
3. **GET /v1/auth/me** — new bearerAuth-protected endpoint at a NEW path (`/v1/auth/me`, NOT `/v1/me` — which already exists with a different shape and different consumers).
4. **bearerAuth bypass list** in `index.ts` adds `/v1/auth/verify-email` so Plan 03's unauthenticated POST is reachable without a JWT.

Purpose: Closes the auth surface so Plan 03 endpoints have everything they need at the framework level (route mount, bypass list, token table, /me lookup), and the PWA Plan 04 has both the immediate-on-login banner state (D-26) and the on-mount Settings refresh path (D-28).

Output: All four touch sites updated and tested. AUTH-11 server-side surface (minus the two verify endpoints) operational.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-verify-email-on-signup/113-CONTEXT.md
@.planning/phases/113-verify-email-on-signup/113-RESEARCH.md
@.planning/phases/113-verify-email-on-signup/113-VALIDATION.md
@vigil-core/src/routes/auth.ts
@vigil-core/src/routes/me.ts
@vigil-core/src/routes/forgot-password.ts
@vigil-core/src/db/schema.ts
@vigil-core/src/services/email-service.ts
@vigil-core/src/index.ts

<interfaces>
Source-of-truth signatures the executor needs. Use these directly.

`vigil-core/src/services/email-service.ts:284`:
```
export const sendEmailVerificationEmail: (to: string, verifyUrl: string) => Promise<EmailSendResult>;
```

`vigil-core/src/db/schema.ts` (after Plan 01):
- `users` columns include: `id` (serial PK), `email`, `passwordHash`, `createdAt`, `updatedAt`, `passwordChangedAt`, `emailVerifiedAt` (Plan 01 — nullable TIMESTAMPTZ).
- `passwordResetTokens` columns: `id`, `userId`, `tokenHash`, `type` (CHECK in 'password_reset','email_verify' — pre-locked Phase 112 0016), `expiresAt`, `usedAt`, `createdAt`.

`vigil-core/src/utils/jwt.ts`: `export function signToken(userId: number, email: string): Promise<string>`
`vigil-core/src/utils/password.ts`: `export function hashPassword(plain: string): Promise<string>`

Existing register response (auth.ts:96): `201 { id: number, email: string }` — Plan 02 keeps this body shape, only adds the token-row INSERT and background send before returning.
Existing login response (auth.ts:161): `200 { token: string, user: { id: number, email: string } }` — Plan 02 extends `user` with `emailVerifiedAt: string | null` (D-26 additive).
New `GET /v1/auth/me` response (D-27): `200 { id: number, email: string, emailVerifiedAt: string | null }` or `401 { error: "invalid_user" }`.

Pattern source — fire-and-forget shape from `forgot-password.ts:221-223`:
```
sendEmailFn(user.email, resetUrl).catch((err) => {
  console.error("[forgot-password] email send failed (background):", err);
});
return c.json(ENUM_SAFE_BODY, 200);
```

Existing bearerAuth bypass dispatcher in `index.ts:127-135` (current state, Plan 02 adds ONE line):
```
app.use("/v1/*", async (c, next) => {
  if (c.req.path === "/v1/health") return next();
  if (c.req.path === "/v1/auth/google/callback") return next();
  if (c.req.path === "/v1/auth/register") return next();
  if (c.req.path === "/v1/auth/login") return next();
  if (c.req.path === "/v1/auth/forgot-password") return next();
  if (c.req.path === "/v1/auth/reset-password") return next();
  // <- Plan 02 ADDS one line here for /v1/auth/verify-email
  return bearerAuth(c, next);
});
```

Existing `me.ts` route at `/v1/me` returns `{ userId: string, email: string }` and is consumed by `App.tsx:37` (PostHog identify) and `SettingsPage.tsx:100` (account email). DO NOT MODIFY `me.ts`. The new `/v1/auth/me` is a separate route at a separate path with a different shape (`id: number`, not `userId: string`).

Pattern source — `me.ts` factory + DI seam (mirror this exact shape in `auth-me.ts`):
```
export function createMeRouter(deps: MeDeps = defaultDeps): Hono { ... }
export const me: Hono = createMeRouter();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend register handler with email_verify token issuance + fire-and-forget send + extend login response</name>
  <files>vigil-core/src/routes/auth.ts, vigil-core/src/routes/auth.test.ts</files>
  <read_first>
    - vigil-core/src/routes/auth.ts (current register handler — fresh-register branch lines 87-97; claim-flow branch lines 99-114; login lines 121-162)
    - vigil-core/src/routes/forgot-password.ts (lines 193-223 — token generation + fire-and-forget pattern, the canonical reference)
    - vigil-core/src/services/email-service.ts (line 227 — sendEmailVerificationEmail signature)
    - vigil-core/src/db/schema.ts (passwordResetTokens table — type column CHECK constraint accepts 'email_verify')
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-06, D-07, D-08, D-26)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md sections "Register Handler Token Issuance" and "Fire-and-Forget Email Send"
    - vigil-core/src/routes/auth.test.ts (existing register tests describe block at line 42; login tests at line 142)
  </read_first>
  <behavior>
    - Test AUTH-11-R-01: Fresh register (allowlisted email, no existing row) returns 201 AND a `password_reset_tokens` row exists for that user with `type='email_verify'`, `used_at IS NULL`, `expires_at > now() + 23h AND < now() + 25h`. Verify by SELECT after the response.
    - Test AUTH-11-R-02: Fresh register fires `sendEmailVerificationEmail` spy EXACTLY ONCE with args `(email, verifyUrl)` where `verifyUrl` matches `/^https?:\/\/.+\/auth\/verify\?token=[A-Za-z0-9_-]{40,50}$/` (base64url 32-byte token is ~43 chars).
    - Test AUTH-11-R-03: Fresh register response is 201 EVEN IF the email send rejects synchronously — the `.catch()` swallows. Inject a spy that throws; assert response status = 201 AND no unhandled rejection escapes.
    - Test AUTH-11-R-04: Claim-flow (existing user with PLACEHOLDER_HASH_PREFIX) AND `existing.emailVerifiedAt IS NULL` → token issued + email sent (D-07 happy path).
    - Test AUTH-11-R-05: Claim-flow AND `existing.emailVerifiedAt` non-null (e.g. seed user post-Plan-01 backfill) → NO token row inserted, NO email send call. Assert spy call count = 0 AND no new row in password_reset_tokens for that user with type='email_verify'.
    - Test AUTH-11-L-01: POST /v1/auth/login response body matches `{ token: <non-empty string>, user: { id: <number>, email: <string>, emailVerifiedAt: <string | null> } }`. Specifically assert `'emailVerifiedAt' in body.user` is true (additive contract — D-26).
    - Test AUTH-11-L-02: For a user with non-null `email_verified_at` in DB, login response `user.emailVerifiedAt` is an ISO 8601 string (matches `/^\d{4}-\d{2}-\d{2}T/`).
    - Test AUTH-11-L-03: For a user with NULL `email_verified_at` in DB, login response `user.emailVerifiedAt` is JSON `null` (not undefined, not omitted).
  </behavior>
  <action>
    Step 1 — Add new imports and helpers at module scope in `vigil-core/src/routes/auth.ts`. Insert these BEFORE the existing `auth = new Hono()` line (currently line 38):

    Add to the imports block at the top of the file (alongside the existing imports on lines 1-6):
    - `import * as crypto from "node:crypto";`
    - `import { passwordResetTokens } from "../db/schema.js";`
    - `import { sendEmailVerificationEmail as realSendEmailVerificationEmail } from "../services/email-service.js";`

    Then add this constants + DI-seam + helper block immediately AFTER `const MAX_PASSWORD = 128;` (line 20):

    ```
    // Phase 113 (AUTH-11 D-06): TTL for email_verify tokens = 24h.
    const VERIFY_TOKEN_TTL_MS = 24 * 60 * 60 * 1000;

    // DI seam — tests inject a spy via __setSendEmailVerificationEmailForTest;
    // production singleton uses the real wrapper. Lives at module scope (not
    // inside the handler) so test-time patching is observable across both
    // register code paths (fresh + claim).
    let sendEmailVerificationEmailFn = realSendEmailVerificationEmail;
    export function __setSendEmailVerificationEmailForTest(
      fn: typeof realSendEmailVerificationEmail,
    ): void {
      sendEmailVerificationEmailFn = fn;
    }
    export function __resetSendEmailVerificationEmailForTest(): void {
      sendEmailVerificationEmailFn = realSendEmailVerificationEmail;
    }

    // Phase 113 (AUTH-11 D-06): generate raw base64url token + SHA-256 hex hash,
    // insert email_verify row, return raw token for URL construction. Mirrors
    // forgot-password.ts:195-203 token-issuance shape verbatim.
    async function issueEmailVerifyToken(
      userId: number,
      now: number,
    ): Promise<string> {
      if (!db) throw new Error("Database unavailable for token issuance");
      const rawToken = crypto.randomBytes(32).toString("base64url");
      const tokenHash = crypto.createHash("sha256").update(rawToken).digest("hex");
      await db.insert(passwordResetTokens).values({
        userId,
        tokenHash,
        type: "email_verify",
        expiresAt: new Date(now + VERIFY_TOKEN_TTL_MS),
      });
      return rawToken;
    }

    // Phase 113 (AUTH-11 D-08): fire-and-forget email send. Mirrors
    // forgot-password.ts:221-223 verbatim — `.catch()` attached synchronously
    // BEFORE c.json(...) returns, but the awaited send promise resolves AFTER.
    // Register response time stays at hash + INSERT cost (~50-150ms); Resend
    // network latency invisible to caller.
    function fireVerifyEmailInBackground(toEmail: string, rawToken: string): void {
      const origin =
        process.env["VIGIL_APP_BASE_URL"] || "https://app.vigilhub.io";
      const verifyUrl = `${origin}/auth/verify?token=${rawToken}`;
      sendEmailVerificationEmailFn(toEmail, verifyUrl).catch((err) => {
        console.error("[register] email send failed (background):", err);
      });
    }
    ```

    Step 2 — Modify the FRESH-REGISTER branch (currently lines 87-97). After the `[created]` row returns from INSERT and BEFORE `return c.json(...)`, issue the token and fire the send. Replace the existing branch body with:

    ```
      // Fresh registration (no existing row for this email)
      if (!existing) {
        const passwordHash = await hashPassword(password);
        const now = Date.now();
        const [created] = await db
          .insert(users)
          .values({ email, passwordHash, passwordChangedAt: new Date(now) })
          .returning({ id: users.id, email: users.email });
        // Phase 113 (AUTH-11 D-06): issue email_verify token row BEFORE response.
        // Token is durable in DB before 201 returns, so a crashed/failed background
        // send still leaves the user able to hit Resend later (Plan 03).
        const rawToken = await issueEmailVerifyToken(created.id, now);
        // Phase 113 (AUTH-11 D-08): fire-and-forget AFTER token row commits, BEFORE response.
        fireVerifyEmailInBackground(created.email, rawToken);
        return c.json({ id: created.id, email: created.email }, 201);
      }
    ```

    Step 3 — Modify the CLAIM-FLOW branch (currently lines 99-114). After the UPDATE that writes the new password and BEFORE `return c.json(...)`, conditionally issue token + send ONLY when `existing.emailVerifiedAt` is null (D-07). Replace the existing branch body with:

    ```
      // D-11: claim-flow — seed user with placeholder hash overwrites with the real password
      if (existing.passwordHash.startsWith(PLACEHOLDER_HASH_PREFIX)) {
        const passwordHash = await hashPassword(password);
        const nowDate = new Date();
        await db
          .update(users)
          .set({ passwordHash, passwordChangedAt: nowDate, updatedAt: nowDate })
          .where(eq(users.id, existing.id));
        // Phase 113 (AUTH-11 D-07): claim-flow ALSO issues a verify token + email,
        // but ONLY if existing.emailVerifiedAt IS NULL. After Plan 01's 0017
        // migration backfills the seed user to email_verified_at = created_at,
        // this check correctly skips the email send for the existing seed user.
        // Defensive guard for the edge case where a seed user was inserted
        // between migration and claim.
        if (existing.emailVerifiedAt === null) {
          const rawToken = await issueEmailVerifyToken(existing.id, nowDate.getTime());
          fireVerifyEmailInBackground(existing.email, rawToken);
        }
        return c.json(
          { id: existing.id, email: existing.email, claimed: true },
          201,
        );
      }
    ```

    Step 4 — Modify the LOGIN handler (currently lines 121-162). Replace the existing `return c.json({ token, user: { id: user.id, email: user.email } });` (line 161) with:

    ```
      const token = await signToken(user.id, user.email);
      // Phase 113 (AUTH-11 D-26): additive — emailVerifiedAt as ISO string or null.
      // PWA reads this on login to render the Settings banner state without
      // a second round-trip. Backwards-compatible: existing destructures
      // { id, email } continue to work; the new field is just additional.
      return c.json({
        token,
        user: {
          id: user.id,
          email: user.email,
          emailVerifiedAt: user.emailVerifiedAt
            ? user.emailVerifiedAt.toISOString()
            : null,
        },
      });
    ```

    Note: the existing `db.select().from(users)` on line 140-144 already returns the full row including the new `emailVerifiedAt` column from Plan 01. Drizzle's default `select()` (no field whitelist) returns every schema-declared column.

    Step 5 — Extend `vigil-core/src/routes/auth.test.ts`. Add a new top-level describe block AFTER the existing `describe("POST /v1/auth/login — generic errors + JWT mint (D-12..D-15)", () => {...})` block (around line 142+). Pattern: import `__setSendEmailVerificationEmailForTest` and `__resetSendEmailVerificationEmailForTest`, add a `beforeEach` that resets the spy, then write each behavior case using `mock.fn()` from `node:test`.

    Skeleton:
    ```
    import {
      __setSendEmailVerificationEmailForTest,
      __resetSendEmailVerificationEmailForTest,
    } from "./auth.js";
    import { passwordResetTokens } from "../db/schema.js";
    import { eq, and } from "drizzle-orm";

    describe("POST /v1/auth/register — email_verify token issuance (AUTH-11)", () => {
      beforeEach(() => __resetSendEmailVerificationEmailForTest());
      // AUTH-11-R-01..05 — see <behavior> block above for full assertions.
      // Skip individual tests with t.skip(...) when DATABASE_URL is unset
      // (mirror auth.test.ts:79-82 pattern).
    });

    describe("POST /v1/auth/login — emailVerifiedAt in response (AUTH-11 D-26)", () => {
      // AUTH-11-L-01..03 — see <behavior> block above.
    });
    ```

    Use the same mock-spy pattern as forgot-password.test.ts. For DB-required tests, skip with `t.skip("DATABASE_URL not set")` when `db` is unavailable.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && timeout 60 npx tsx --test src/routes/auth.test.ts 2>&1 | tail -40)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sendEmailVerificationEmail' vigil-core/src/routes/auth.ts` returns at least 3 (import alias + DI variable + .catch site).
    - `grep -c 'type: "email_verify"' vigil-core/src/routes/auth.ts` returns 1 (single token-issuance helper).
    - `grep -c 'crypto.randomBytes(32).toString("base64url")' vigil-core/src/routes/auth.ts` returns 1.
    - `grep -c '\.catch(' vigil-core/src/routes/auth.ts` returns at least 1 (fire-and-forget catch handler).
    - `grep -c 'emailVerifiedAt' vigil-core/src/routes/auth.ts` returns at least 3 (login response read + claim-flow null check + ISO conversion).
    - `grep -c 'existing.emailVerifiedAt === null' vigil-core/src/routes/auth.ts` returns 1 (D-07 conditional gate).
    - `grep -c 'VIGIL_APP_BASE_URL' vigil-core/src/routes/auth.ts` returns 1 (verifyUrl origin per RESEARCH).
    - `grep -c '__setSendEmailVerificationEmailForTest' vigil-core/src/routes/auth.ts` returns 1 (DI seam exported).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npx tsx --test src/routes/auth.test.ts` shows: all 5 AUTH-11-R-* tests pass + all 3 AUTH-11-L-* tests pass (8 new green tests). Pre-existing tests in this file remain green (no regression).
    - The `.catch()` handler in `fireVerifyEmailInBackground` is attached BEFORE the function returns — so a synchronously-throwing spy still results in a 201 response. Test AUTH-11-R-03 enforces this empirically.
  </acceptance_criteria>
  <done>
    Both register branches issue tokens and fire emails per D-06/D-07/D-08. Login response carries `emailVerifiedAt` per D-26. All 8 new tests pass. The `.catch()` swallow guarantees register response time stays at hash + INSERT cost.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GET /v1/auth/me route + tests (auth-me.ts) — distinct from /v1/me</name>
  <files>vigil-core/src/routes/auth-me.ts, vigil-core/src/routes/auth-me.test.ts</files>
  <read_first>
    - vigil-core/src/routes/me.ts (factory pattern to mirror — createMeRouter + DI deps; lines 27-127)
    - vigil-core/src/db/schema.ts (users.emailVerifiedAt added in Plan 01)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-27 — minimal field set, bearerAuth, fresh DB read)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md sections "GET /v1/auth/me Route Shape" and "Critical Integration Point: /v1/me vs /v1/auth/me"
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md Pitfall 2 — DO NOT modify me.ts; the new endpoint MUST live at /v1/auth/me with a separate file
  </read_first>
  <behavior>
    - Test AUTH-11-ME-01: GET /v1/auth/me with valid c.get("userId") for an existing users row with non-null `email_verified_at` returns 200 `{ id: <number>, email: <string>, emailVerifiedAt: <ISO string> }`. Specifically assert `typeof body.id === 'number'` (NOT string — distinct from /v1/me which returns userId as string).
    - Test AUTH-11-ME-02: GET /v1/auth/me with valid c.get("userId") for an existing users row with NULL `email_verified_at` returns 200 with `body.emailVerifiedAt === null` (JSON null).
    - Test AUTH-11-ME-03: GET /v1/auth/me when c.get("userId") points to a non-existent row (deleted user, but JWT still valid) returns 401 `{ error: "invalid_user" }` (mirrors me.ts D-18 pattern).
    - Test AUTH-11-ME-04: GET /v1/auth/me when c.get("userId") is missing/zero/non-integer returns 401 `{ error: "invalid_user" }` (defensive recheck).
    - Test AUTH-11-ME-05: Response body has EXACTLY 3 keys: `id`, `email`, `emailVerifiedAt`. Asserts no leakage of `passwordHash`, `passwordChangedAt`, `createdAt`, etc. (D-27 minimal field set).
  </behavior>
  <action>
    Step 1 — Create `vigil-core/src/routes/auth-me.ts` with this exact content (mirrors me.ts factory + DI seam pattern, with the AUTH-11 D-27 response contract):

    ```
    // ── Phase 113 Plan 02 — GET /v1/auth/me (AUTH-11 D-27) ─────────────────────
    // Distinct from /v1/me (Phase 103, me.ts) — different path, different shape,
    // different consumers:
    //   /v1/me          -> { userId: string, email: string }       (App.tsx + SettingsPage existing)
    //   /v1/auth/me     -> { id: number, email: string, emailVerifiedAt: string | null }
    //                      (Settings banner — Plan 04)
    //
    // RESEARCH Pitfall 2: do NOT modify me.ts to add emailVerifiedAt. App.tsx's
    // PostHog identify reads { userId, email } and would break on shape change.
    // The two endpoints coexist; SettingsPage will fetch BOTH on mount (D-28).
    //
    // Auth: bearerAuth required (mounted under the dispatcher in index.ts —
    // protected route). Body always reflects c.get("userId") from the JWT,
    // never from request body/query (IDOR mitigation matches me.ts:13).

    import { Hono } from "hono";
    import { eq } from "drizzle-orm";
    import { db as defaultDb } from "../db/connection.js";
    import { users } from "../db/schema.js";

    /**
     * DI seam for unit tests. Production uses the real Postgres `db`.
     */
    export interface AuthMeDeps {
      /**
       * Returns { id, email, emailVerifiedAt } for the given userId, or null
       * when the row does not exist. null -> 401 invalid_user (D-27 / me.ts D-18).
       * Throws when DB is unavailable -> 503 surface for the caller.
       */
      userLookupFn: (
        userId: number,
      ) => Promise<{ id: number; email: string; emailVerifiedAt: Date | null } | null>;
    }

    /** Typed sentinel for DB unavailability (mirrors me.ts pattern). */
    class DbUnavailableError extends Error {
      constructor() {
        super("db_unavailable");
        this.name = "DbUnavailableError";
      }
    }

    const defaultDeps: AuthMeDeps = {
      userLookupFn: async (userId) => {
        if (!defaultDb) throw new DbUnavailableError();
        const [row] = await defaultDb
          .select({
            id: users.id,
            email: users.email,
            emailVerifiedAt: users.emailVerifiedAt,
          })
          .from(users)
          .where(eq(users.id, userId))
          .limit(1);
        return row ?? null;
      },
    };

    export function createAuthMeRouter(deps: AuthMeDeps = defaultDeps): Hono {
      const router = new Hono();

      router.get("/auth/me", async (c) => {
        const userId = c.get("userId") as number | undefined;
        // bearerAuth dispatcher in index.ts guarantees userId on protected paths;
        // defensive recheck protects against misconfigured mount order (mirrors me.ts:77).
        if (!Number.isInteger(userId) || (userId as number) <= 0) {
          return c.json({ error: "invalid_user" }, 401);
        }

        let row: { id: number; email: string; emailVerifiedAt: Date | null } | null;
        try {
          row = await deps.userLookupFn(userId as number);
        } catch (err) {
          if (err instanceof DbUnavailableError) {
            return c.json({ error: "Database unavailable" }, 503);
          }
          throw err; // app.onError captures
        }

        if (!row) {
          return c.json({ error: "invalid_user" }, 401);
        }

        // D-27: minimal field set — id (number), email, emailVerifiedAt (ISO|null).
        // No passwordHash, no passwordChangedAt, no createdAt. Future fields
        // get added when a feature needs them.
        return c.json(
          {
            id: row.id,
            email: row.email,
            emailVerifiedAt: row.emailVerifiedAt
              ? row.emailVerifiedAt.toISOString()
              : null,
          },
          200,
        );
      });

      return router;
    }

    /** Production singleton — Plan 02 Task 4 mounts this in index.ts via app.route("/v1", authMe). */
    export const authMe: Hono = createAuthMeRouter();
    ```

    Step 2 — Create `vigil-core/src/routes/auth-me.test.ts` with all 5 behavior cases. Use `node:test` + `assert/strict` + Hono's `app.request()`. Inject a fake `userLookupFn` for cases 1, 2, 3, 5; for case 4, do not set `c.set("userId", ...)` to test the defensive guard.

    File content:
    ```
    process.env["JWT_SECRET"] = "test-secret-32-chars-minimum-value-xxxxxx";

    import { describe, it } from "node:test";
    import assert from "node:assert/strict";
    import { Hono } from "hono";
    import { createAuthMeRouter } from "./auth-me.js";

    function buildAppWithUserId(router: Hono, userId: number | undefined): Hono {
      const app = new Hono();
      app.use("*", async (c, next) => {
        if (userId !== undefined) c.set("userId", userId);
        await next();
      });
      app.route("/v1", router);
      return app;
    }

    describe("GET /v1/auth/me (AUTH-11 D-27)", () => {
      it("AUTH-11-ME-01: returns id+email+emailVerifiedAt (ISO) for verified user", async () => {
        const router = createAuthMeRouter({
          userLookupFn: async () => ({
            id: 42,
            email: "u@x.io",
            emailVerifiedAt: new Date("2026-04-25T12:00:00Z"),
          }),
        });
        const app = buildAppWithUserId(router, 42);
        const res = await app.request("/v1/auth/me");
        assert.equal(res.status, 200);
        const body = await res.json();
        assert.equal(body.id, 42);
        assert.equal(typeof body.id, "number"); // NOT string — distinct from /v1/me
        assert.equal(body.email, "u@x.io");
        assert.equal(body.emailVerifiedAt, "2026-04-25T12:00:00.000Z");
      });

      it("AUTH-11-ME-02: emailVerifiedAt === null when DB column is null", async () => {
        const router = createAuthMeRouter({
          userLookupFn: async () => ({ id: 7, email: "u@x.io", emailVerifiedAt: null }),
        });
        const res = await buildAppWithUserId(router, 7).request("/v1/auth/me");
        const body = await res.json();
        assert.strictEqual(body.emailVerifiedAt, null);
      });

      it("AUTH-11-ME-03: 401 invalid_user when row does not exist", async () => {
        const router = createAuthMeRouter({ userLookupFn: async () => null });
        const res = await buildAppWithUserId(router, 999).request("/v1/auth/me");
        assert.equal(res.status, 401);
        assert.deepEqual(await res.json(), { error: "invalid_user" });
      });

      it("AUTH-11-ME-04: 401 when c.get('userId') is missing", async () => {
        const router = createAuthMeRouter({
          userLookupFn: async () => {
            throw new Error("must not call");
          },
        });
        const res = await buildAppWithUserId(router, undefined).request("/v1/auth/me");
        assert.equal(res.status, 401);
      });

      it("AUTH-11-ME-05: response body has exactly 3 keys (D-27 minimal)", async () => {
        const router = createAuthMeRouter({
          userLookupFn: async () => ({ id: 1, email: "a@b.c", emailVerifiedAt: null }),
        });
        const res = await buildAppWithUserId(router, 1).request("/v1/auth/me");
        const body = await res.json();
        assert.deepEqual(Object.keys(body).sort(), ["email", "emailVerifiedAt", "id"]);
      });
    });
    ```
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && timeout 30 npx tsx --test src/routes/auth-me.test.ts 2>&1 | tail -20)</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-core/src/routes/auth-me.ts` exists with `export const authMe` and `export function createAuthMeRouter`.
    - File `vigil-core/src/routes/auth-me.test.ts` exists with all 5 AUTH-11-ME-* test cases.
    - `grep -c 'router.get("/auth/me"' vigil-core/src/routes/auth-me.ts` returns 1.
    - `grep -c 'emailVerifiedAt' vigil-core/src/routes/auth-me.ts` returns at least 3 (DI type, lookup select, response build).
    - `grep -c '"invalid_user"' vigil-core/src/routes/auth-me.ts` returns at least 2 (deleted-row 401 + defensive 401).
    - `npx tsc --noEmit` exits 0.
    - `npx tsx --test src/routes/auth-me.test.ts` shows all 5 tests pass.
    - The new file does NOT import from `./me.js` (separate file, separate concern).
    - `id` is returned as number directly — `grep -c 'String(row.id)' vigil-core/src/routes/auth-me.ts` returns 0 (NOT string-ified like me.ts).
  </acceptance_criteria>
  <done>
    GET /v1/auth/me is implemented with the D-27 contract, fully unit-tested via DI seam. Mounting in index.ts is Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire bearerAuth bypass for /v1/auth/verify-email AND mount auth-me router in index.ts</name>
  <files>vigil-core/src/index.ts</files>
  <read_first>
    - vigil-core/src/index.ts (lines 127-135 — current bearerAuth dispatcher with 6 bypass checks; lines 153-186 — protected route mounts)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-12 — verify endpoint is unauthenticated; the token IS the auth; D-27 — /v1/auth/me requires bearerAuth)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md sections "bearerAuth Bypass List" and "Route Mounting Order"
  </read_first>
  <action>
    Two edits to `vigil-core/src/index.ts`:

    Edit 1 — Add the verify-email bypass. Locate the bearerAuth dispatcher block at lines 127-135. Insert ONE new line for the verify-email bypass DIRECTLY AFTER the existing line `if (c.req.path === "/v1/auth/reset-password") return next();` (the Phase 112 reset-password bypass — already shipped, do NOT re-add). Resulting block:

    ```
        if (c.req.path === "/v1/auth/forgot-password") return next(); // Phase 112 (AUTH-10) — already present
        if (c.req.path === "/v1/auth/reset-password") return next();  // Phase 112 (AUTH-10) — already present
        if (c.req.path === "/v1/auth/verify-email") return next();    // Phase 113 (AUTH-11 D-12) — NEW: token IS the auth
        return bearerAuth(c, next);
    ```

    Only the third line (verify-email) is new in this plan. The first two lines are existing Phase 112 code shown for context — do not modify them.

    DO NOT add a bypass for `/v1/auth/resend-verification` (Plan 03 — bearerAuth REQUIRED per D-15; user must be logged in).
    DO NOT add a bypass for `/v1/auth/me` (this task — bearerAuth REQUIRED per D-27; routes added under the dispatcher are auto-protected).

    Edit 2 — Mount the auth-me router. First add the import alongside the other route imports at the top of the file (currently imports end around line 49). Add this AFTER the existing `import { me } from "./routes/me.js";` (line 39):

    ```
    import { authMe } from "./routes/auth-me.js"; // Phase 113 (AUTH-11 D-27) — distinct from /v1/me
    ```

    Then mount the router AFTER the existing `app.route("/v1", me);` line (line 186) so it sits with the other protected routes (the bearerAuth dispatcher at line 127 already protects all `/v1/*` paths not in the bypass list — `/v1/auth/me` is auto-protected by mount position):

    ```
    app.route("/v1", me);  // Phase 103 Plan 03 — AUTH-08, behind bearerAuth catch-all (D-17)
    app.route("/v1", authMe); // Phase 113 (AUTH-11 D-27) — GET /v1/auth/me, bearerAuth-protected
    ```

    DO NOT mount it before the bearerAuth dispatcher (lines 110-135) — that would create a silent auth bypass (mirrors the WR-02 mount-order comment at line 165-170 about `change-password`).
    DO NOT modify any other route mount or middleware order.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c '/v1/auth/verify-email' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'import { authMe }' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'app.route("/v1", authMe)' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/v1/auth/verify-email' vigil-core/src/index.ts` returns at least 1 (bypass line added).
    - `grep -c 'import { authMe }' vigil-core/src/index.ts` returns 1 (import added).
    - `grep -c 'app.route("/v1", authMe)' vigil-core/src/index.ts` returns 1 (mount added).
    - The verify-email bypass line appears AFTER the reset-password bypass line: `awk '/reset-password/{f=NR} /verify-email/{v=NR} END{exit (v>f)?0:1}' vigil-core/src/index.ts` exits 0.
    - The `app.route("/v1", authMe)` line appears AFTER the `app.use("/v1/*", async (c, next) => { ... bearerAuth ... });` block: `awk '/bearerAuth\(c, next\)/{b=NR} /app.route\("\/v1", authMe\)/{a=NR} END{exit (a>b)?0:1}' vigil-core/src/index.ts` exits 0.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - No other line in the file was modified — `git diff vigil-core/src/index.ts` shows ONLY 3 inserted lines (1 import, 1 bypass, 1 mount) plus possibly trailing comments.
  </acceptance_criteria>
  <done>
    `/v1/auth/verify-email` is publicly reachable (Plan 03 will register the handler). `/v1/auth/me` is mounted as bearerAuth-protected and ready for the PWA Plan 04 to call.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → POST /v1/auth/register | Allowlist + rate limit + password length already enforced upstream; new email_verify token is server-issued (random 256-bit), no untrusted input crosses |
| unauthenticated client → POST /v1/auth/login | Email/password already enforced upstream; new emailVerifiedAt response is read-only from DB |
| authenticated client → GET /v1/auth/me | bearerAuth gate enforces userId; row read scoped by JWT-derived userId only (never body/query) |
| Resend SDK boundary (background) | sendEmailVerificationEmail wrapper already handles WR-01 escape and click_tracking:false; Plan 02 only invokes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04 | Information Disclosure | Token in verify URL exposed via PWA query string | mitigate | vigil-core does not log request bodies or query strings by default (Hono default loggers are not enabled in this project — verified by reading index.ts middleware list). PWA reads ?token via useSearchParams (not logged). |
| T-113-06 | Tampering / EoP | bearerAuth bypass list growth | mitigate | Task 3 adds EXACTLY ONE entry (`/v1/auth/verify-email`); resend + /v1/auth/me are NOT bypassed (mounted under dispatcher). Acceptance grep enforces `awk` ordering check. Bypass list rationale documented inline (`token IS the auth`). |
| T-113-08 | DoS (lockout on deploy) | claim-flow seed user re-emails | mitigate | D-07 conditional `existing.emailVerifiedAt === null` skips token + send for the post-Plan-01-backfilled seed user. Test AUTH-11-R-05 enforces empirically. |
| T-113-IDOR-01 | EoP | /v1/auth/me userId source | mitigate | userId read ONLY from `c.get("userId")` (set by bearerAuth from JWT). Never from body/query/header. Mirrors me.ts:13 IDOR-mitigation pattern. |
| T-113-LEAK-01 | Information Disclosure | /v1/auth/me response shape | mitigate | D-27 minimal field set: ONLY id, email, emailVerifiedAt. Test AUTH-11-ME-05 asserts `Object.keys(body).sort() === ["email","emailVerifiedAt","id"]` — no passwordHash, no passwordChangedAt, no createdAt leakage. |
| T-113-FAF-01 | DoS (response-time leak) | Register fire-and-forget | mitigate | `.catch()` attached synchronously BEFORE c.json returns; Resend network latency does not block response. Test AUTH-11-R-03 verifies a synchronously-throwing spy still results in 201. |
</threat_model>

<verification>
- Tasks 1 + 2 each have automated test runs that exit green.
- Task 3 has grep + tsc + awk-order checks that prove the bypass and mount sit in the correct dispatcher position.
- Cross-task: after all 3 tasks, the full vigil-core test suite must remain green: `(cd vigil-core && timeout 90 npm test 2>&1 | tail -30; pkill -f tsx 2>/dev/null || true)` — accept the known post-suite hang and force-kill (STATE.md Blockers/Concerns).
</verification>

<success_criteria>
- Register fresh-register branch: 201 + email_verify token row inserted + sendEmailVerificationEmail fired in background.
- Register claim-flow branch: token + email ONLY when existing.emailVerifiedAt IS NULL.
- Login response carries `user.emailVerifiedAt` as ISO string or null.
- GET /v1/auth/me returns the D-27 minimal shape with id (number), email, emailVerifiedAt; 401 on missing/invalid userId.
- /v1/auth/verify-email is publicly accessible (bypass added); /v1/auth/me requires bearerAuth (mounted under dispatcher).
- All new tests green (8 in auth.test.ts + 5 in auth-me.test.ts = 13 new green tests).
- Existing test suite remains green (no regressions).
- TypeScript build: `npx tsc --noEmit` exits 0 across vigil-core.
</success_criteria>

<output>
After completion, create `.planning/phases/113-verify-email-on-signup/113-02-SUMMARY.md` documenting:
- Token issuance helper signature (`issueEmailVerifyToken(userId, now)`) — for Plan 03 reuse decision.
- Whether the planner chose to extract a shared `tokenIssue.ts` helper (Claude's Discretion item from CONTEXT — defer if INSERT site count is still 2 here; reconsider in Plan 03).
- Test count delta + any pre-existing flaky tests observed.
- Confirm the bypass list in index.ts now has 7 entries (health, google callback, register, login, forgot, reset, verify-email).
</output>
