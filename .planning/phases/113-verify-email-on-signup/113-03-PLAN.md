---
phase: 113
plan: 03
type: execute
wave: 2
depends_on:
  - "113-01"
files_modified:
  - vigil-core/src/routes/verify-email.ts
  - vigil-core/src/routes/verify-email.test.ts
  - vigil-core/src/routes/resend-verification.ts
  - vigil-core/src/routes/resend-verification.test.ts
  - vigil-core/src/index.ts
autonomous: true
requirements:
  - AUTH-11
must_haves:
  truths:
    - "POST /v1/auth/verify-email accepts { token }, atomically claims via UPDATE-RETURNING with type='email_verify' filter, sets users.email_verified_at = now(), returns 200 { ok: true } (D-09, D-10, D-11, D-14)"
    - "POST /v1/auth/verify-email is unauthenticated (in bypass list — wired by Plan 02 Task 3)"
    - "POST /v1/auth/verify-email returns 400 'Invalid or expired token' on 0-row claim (single-bucket — invalid OR expired OR used)"
    - "POST /v1/auth/verify-email rate limit: 5/hour per-IP only (D-13) — 429 with Retry-After header on excess"
    - "POST /v1/auth/resend-verification requires bearerAuth (D-15); rate limit 3/hour per userId with key 'verify-resend:userId:{id}' (D-16)"
    - "POST /v1/auth/resend-verification idempotent on already-verified: returns 200 { ok: true, already_verified: true } and does NOT issue token or send email (D-18)"
    - "POST /v1/auth/resend-verification invalidates all prior unused email_verify tokens before issuing new one (D-17 — most-recent-link wins)"
    - "Both endpoints mounted in index.ts with the correct order — verify-email mounted BEFORE bearerAuth dispatcher; resend-verification mounted AFTER"
  artifacts:
    - path: "vigil-core/src/routes/verify-email.ts"
      provides: "POST /v1/auth/verify-email — atomic UPDATE-RETURNING claim + users.email_verified_at update + per-IP rate limit"
      contains: "/auth/verify-email"
    - path: "vigil-core/src/routes/resend-verification.ts"
      provides: "POST /v1/auth/resend-verification — bearerAuth + per-userId rate limit + idempotency + invalidate-prior + new token + fire-and-forget send"
      contains: "/auth/resend-verification"
    - path: "vigil-core/src/routes/verify-email.test.ts"
      provides: "Test cases AUTH-11-V1..V4 + AUTH-11-G grandfathering"
      contains: "verify-email"
    - path: "vigil-core/src/routes/resend-verification.test.ts"
      provides: "Test cases AUTH-11-S1..S3"
      contains: "resend-verification"
    - path: "vigil-core/src/index.ts"
      provides: "Mounts verifyEmail + resendVerification routers in correct order"
      contains: "verifyEmail"
  key_links:
    - from: "vigil-core/src/routes/verify-email.ts"
      to: "password_reset_tokens table (atomic UPDATE-RETURNING)"
      via: "drizzle .update().set({usedAt:now}).where(and(tokenHash, type='email_verify', isNull(usedAt), gt(expiresAt, sql`now()`))).returning({userId})"
      pattern: "type, \"email_verify\""
    - from: "vigil-core/src/routes/verify-email.ts"
      to: "users.email_verified_at column (Plan 01)"
      via: "db.update(users).set({emailVerifiedAt: new Date(now)}).where(eq(users.id, claimed[0].userId))"
      pattern: "emailVerifiedAt"
    - from: "vigil-core/src/routes/resend-verification.ts"
      to: "vigil-core/src/services/email-service.ts (sendEmailVerificationEmail)"
      via: ".catch() fire-and-forget after c.json returns"
      pattern: "sendEmailVerificationEmail.*\\.catch"
    - from: "vigil-core/src/index.ts"
      to: "verify-email + resend-verification routers"
      via: "app.route('/v1', verifyEmail) (BEFORE dispatcher) + app.route('/v1', resendVerification) (AFTER dispatcher)"
      pattern: "app\\.route\\(\"/v1\", (verifyEmail|resendVerification)\\)"
---

<objective>
Ship the two verify-email handlers that close the AUTH-11 server surface:

1. **POST /v1/auth/verify-email** — unauthenticated, body `{ token }`. Atomic UPDATE-RETURNING claim against `password_reset_tokens` (filter by `type='email_verify'`), then UPDATE `users.email_verified_at = now()`, return `200 { ok: true }`. Rate limit: 5/hour per-IP. Mirrors Phase 112 reset-password.ts shape verbatim, only the `type` discriminant and the post-claim mutation differ.

2. **POST /v1/auth/resend-verification** — bearerAuth required, empty body. userId from JWT. Idempotent on already-verified (200 + `already_verified: true` skip). Rate limit 3/hour per userId with key `verify-resend:userId:{id}`. Most-recent-link wins (invalidate prior unused email_verify tokens before issuing new). Fire-and-forget send.

Plus mount both routers in `index.ts` in the correct order (verify-email BEFORE the bearerAuth dispatcher; resend-verification AFTER it).

Purpose: Closes the SC#2/SC#3/SC#5 success-criteria surface. After this plan, the API behavior for verify + resend is complete; PWA Plan 04 just consumes these endpoints.

Output: Two new route files + tests + mount lines in index.ts. AUTH-11 server-side surface 100% complete after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-verify-email-on-signup/113-CONTEXT.md
@.planning/phases/113-verify-email-on-signup/113-RESEARCH.md
@.planning/phases/113-verify-email-on-signup/113-VALIDATION.md
@vigil-core/src/routes/reset-password.ts
@vigil-core/src/routes/forgot-password.ts
@vigil-core/src/db/schema.ts
@vigil-core/src/services/email-service.ts
@vigil-core/src/index.ts

<interfaces>
Source-of-truth signatures the executor needs.

`vigil-core/src/services/email-service.ts:284`:
```
export const sendEmailVerificationEmail: (to: string, verifyUrl: string) => Promise<EmailSendResult>;
```

`vigil-core/src/db/schema.ts` (after Plan 01):
- `users.emailVerifiedAt` — nullable TIMESTAMPTZ
- `passwordResetTokens` — `id`, `userId`, `tokenHash`, `type` (CHECK 'password_reset' | 'email_verify'), `expiresAt`, `usedAt`, `createdAt`

Pattern source — atomic UPDATE-RETURNING claim from `reset-password.ts:159-171`:
```
const tokenHash = crypto.createHash("sha256").update(token).digest("hex");
const claimed = await db
  .update(passwordResetTokens)
  .set({ usedAt: new Date(now) })
  .where(
    and(
      eq(passwordResetTokens.tokenHash, tokenHash),
      eq(passwordResetTokens.type, "password_reset"),  // <- Plan 03 changes to "email_verify"
      isNull(passwordResetTokens.usedAt),
      gt(passwordResetTokens.expiresAt, sql`now()`),
    ),
  )
  .returning({ userId: passwordResetTokens.userId });

if (claimed.length === 0) {
  return c.json({ error: "Invalid or expired token" }, 400);
}
```

Pattern source — per-IP rate limit + Retry-After from `reset-password.ts:48-87, 108-116`:
```
const RATE_LIMIT_MAX = 5;
const RATE_LIMIT_WINDOW_MS = 60 * 60 * 1000;
const ipBuckets = new Map<string, number[]>();

setInterval(() => { /* sweep stale buckets */ }, RATE_LIMIT_WINDOW_MS).unref();

function takeSlot(key: string, now: number): { ok: boolean; retryAfterSec: number } {
  // sliding window — see reset-password.ts for full impl. Math.max(1, ...) floor on Retry-After.
}

// In handler:
const slot = takeSlot(ip, now);
if (!slot.ok) {
  c.header("Retry-After", String(slot.retryAfterSec));
  return c.json({ error: "Too many requests" }, 429);
}
```

Pattern source — DI factory + singleton from `reset-password.ts:95-103, 209-210`:
```
export interface ResetPasswordDeps {
  dbOverride?: typeof defaultDb;
  nowFn?: () => number;
}
export function createResetPasswordRoute(deps?: ResetPasswordDeps): Hono { ... }
export const resetPassword = createResetPasswordRoute();
```

Pattern source — fire-and-forget send from `forgot-password.ts:221-223`:
```
sendEmailFn(user.email, resetUrl).catch((err) => {
  console.error("[forgot-password] email send failed (background):", err);
});
return c.json(ENUM_SAFE_BODY, 200);
```

Existing index.ts route mounts (current state — Plan 02 Task 3 will add the verify-email bypass):
```
app.route("/v1", forgotPassword);   // BEFORE bearerAuth dispatcher
app.route("/v1", resetPassword);    // BEFORE bearerAuth dispatcher
app.use("/v1/*", async (c, next) => { /* bearerAuth dispatcher */ });
// Plan 02 Task 3 mounts authMe AFTER the dispatcher.
```

Plan 03 mount order:
- `app.route("/v1", verifyEmail);` — MUST be BEFORE the bearerAuth dispatcher (mirrors forgotPassword/resetPassword position).
- `app.route("/v1", resendVerification);` — MUST be AFTER the bearerAuth dispatcher (bearerAuth required).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement verify-email.ts (POST /v1/auth/verify-email) + tests</name>
  <files>vigil-core/src/routes/verify-email.ts, vigil-core/src/routes/verify-email.test.ts</files>
  <read_first>
    - vigil-core/src/routes/reset-password.ts (verbatim template — atomic claim pattern, per-IP rate limit, DI factory)
    - vigil-core/src/db/schema.ts (passwordResetTokens.type CHECK constraint accepts 'email_verify'; users.emailVerifiedAt added in Plan 01)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-09, D-10, D-11, D-12, D-13, D-14)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md sections "Atomic Claim Pattern" and "Code Examples"
    - vigil-core/src/routes/reset-password.test.ts (test pattern reference — node:test + Hono app.request())
  </read_first>
  <behavior>
    - Test AUTH-11-V1-01 (happy path, requires DATABASE_URL): seed user → seed unused email_verify token row with expiresAt in 24h → POST /v1/auth/verify-email with raw token → 200 `{ ok: true }` AND `users.email_verified_at` is non-null after the call AND `password_reset_tokens.used_at` is non-null after the call.
    - Test AUTH-11-V2-01 (invalid token): POST with a random base64url string that hashes to no row → 400 `{ error: "Invalid or expired token" }`.
    - Test AUTH-11-V2-02 (expired token, requires DB): seed token row with `expiresAt = now() - 1h` → POST → 400 (same body — single-bucket, no expired/used/unknown differentiation).
    - Test AUTH-11-V2-03 (already-used token, requires DB): seed token row with `usedAt = now() - 1m` → POST → 400 (same body).
    - Test AUTH-11-V2-04 (wrong type — password_reset row): seed `type='password_reset'` row with valid expiresAt → POST → 400 (the type filter in the WHERE clause prevents cross-type claim).
    - Test AUTH-11-V3-01 (single-use, requires DB): seed token row → POST twice in succession with the same token → first response 200, second response 400 (atomic claim semantics — `used_at IS NULL` filter in WHERE makes the second statement match 0 rows).
    - Test AUTH-11-V4-01 (rate limit): 6 successive POSTs from the same IP within 1 second → first 5 may succeed/fail by token validity but the 6th MUST be 429 with `Retry-After` header set to a positive integer string.
    - Test AUTH-11-V4-02 (rate limit isolation): 5 POSTs from IP-A then 1 POST from IP-B → IP-B's request is NOT rate-limited (per-IP key isolation).
    - Test AUTH-11-G-01 (grandfathering — DB integration): for a pre-existing user with `email_verified_at` already non-null (post-Plan-01 backfill), trying to verify with any token shows the verify-email endpoint sets `email_verified_at = now()` overwriting the backfilled value (acceptable — the user simply re-verifies; not a bug). Validation: assert response 200 and `users.email_verified_at` is within last 5s.
    - Test AUTH-11-V-MALFORMED (body parse): POST with malformed JSON body → 400 `{ error: "Invalid JSON body" }`.
    - Test AUTH-11-V-MISSING-TOKEN: POST with valid JSON but `{}` (no token field) → 400 `{ error: "token is required" }`.
  </behavior>
  <action>
    Step 1 — Create `vigil-core/src/routes/verify-email.ts` with the following content. This is a near-verbatim copy of `reset-password.ts` shape with these substitutions: route path `/auth/verify-email`, body shape `{ token }` (no `newPassword`), atomic claim filter `type = 'email_verify'`, post-claim mutation = `UPDATE users SET email_verified_at = now()`, success body = `{ ok: true }`.

    File content:
    ```
    // ── Phase 113 Plan 03 — POST /v1/auth/verify-email (AUTH-11) ─────────────────
    // Unauthenticated endpoint. Closes the AUTH-11 verify-email flow:
    //   - D-12: in the bearerAuth bypass list (token IS the auth credential).
    //   - D-13: rate limit 5/hour per-IP only (no per-user axis — body has no
    //     user identifier; per-IP defends against brute-force token guessing
    //     on top of 256-bit token entropy).
    //   - D-10: atomic single-use UPDATE-RETURNING claim with type='email_verify'
    //     filter — first DB op. PG row-lock makes it safe under concurrent
    //     claims without a transaction wrapper.
    //   - D-11 mutation order (LOAD-BEARING):
    //       1. Atomic claim (UPDATE password_reset_tokens RETURNING user_id)
    //       2. UPDATE users SET email_verified_at = now()
    //       3. Return 200 { ok: true }
    //     If step 2 fails AFTER step 1, the token is already burned. User
    //     requests a fresh link via /resend-verification. Acceptable failure
    //     mode — mirrors Phase 112 reset-password.ts D-11.
    //   - D-14: 200 { ok: true }. NO JWT, NO auto-login, NO token in response.
    //
    // Single-bucket error UX (D-21 mirror): every failure path → 400
    // { error: "Invalid or expired token" } — no expired/used/invalid/wrong-
    // type differentiation. Prevents info leak; simplifies state machine.
    //
    // DI seam via createVerifyEmailRoute(deps) — tests inject `dbOverride` to
    // simulate failures and `nowFn` for deterministic time. Production
    // singleton uses real `db` from connection.js.
    // -----------------------------------------------------------------------------

    import { Hono } from "hono";
    import { eq, and, isNull, gt, sql } from "drizzle-orm";
    import * as crypto from "node:crypto";
    import { db as defaultDb } from "../db/connection.js";
    import { users, passwordResetTokens } from "../db/schema.js";

    // ── Rate limit (D-13: per-IP only, 5/h, sliding window) ─────────────────────
    // Mirrors reset-password.ts:48-87 verbatim. No per-user axis — body has no
    // email/userId field; the token IS the auth credential. Per-IP catches
    // brute-force token guessing (although 256-bit entropy makes that
    // effectively impossible — belt-and-suspenders against T-113-02).
    const RATE_LIMIT_MAX = 5;
    const RATE_LIMIT_WINDOW_MS = 60 * 60 * 1000;
    const ipBuckets = new Map<string, number[]>();

    // Periodic sweep — drop entries whose newest timestamp is outside the window.
    // .unref() so the timer doesn't keep the test process alive.
    setInterval(() => {
      const cutoff = Date.now() - RATE_LIMIT_WINDOW_MS;
      for (const [k, arr] of ipBuckets) {
        const last = arr[arr.length - 1];
        if (arr.length === 0 || (last !== undefined && last < cutoff)) {
          ipBuckets.delete(k);
        }
      }
    }, RATE_LIMIT_WINDOW_MS).unref();

    function takeSlot(
      key: string,
      now: number,
    ): { ok: boolean; retryAfterSec: number } {
      const cutoff = now - RATE_LIMIT_WINDOW_MS;
      const arr = (ipBuckets.get(key) ?? []).filter((t) => t >= cutoff);
      if (arr.length >= RATE_LIMIT_MAX) {
        const oldest = arr[0]!;
        const retryAfterSec = Math.max(
          1,
          Math.ceil((oldest + RATE_LIMIT_WINDOW_MS - now) / 1000),
        );
        ipBuckets.set(key, arr);
        return { ok: false, retryAfterSec };
      }
      arr.push(now);
      ipBuckets.set(key, arr);
      return { ok: true, retryAfterSec: 0 };
    }

    /** Test-only helper. MUST NOT be called from production code. */
    export function __resetBucketsForTest(): void {
      ipBuckets.clear();
    }

    // ── Factory + default singleton ─────────────────────────────────────────────
    export interface VerifyEmailDeps {
      /** DI seam for tests — defaults to the real `db` from connection.js */
      dbOverride?: typeof defaultDb;
      /** DI seam for tests — defaults to Date.now */
      nowFn?: () => number;
    }

    export function createVerifyEmailRoute(deps?: VerifyEmailDeps): Hono {
      const db = deps?.dbOverride ?? defaultDb;
      const nowFn = deps?.nowFn ?? Date.now;
      const router = new Hono();

      router.post("/auth/verify-email", async (c) => {
        // D-13: per-IP rate limit FIRST — token-guessing brute force is the threat.
        const ip =
          c.req.header("x-forwarded-for")?.split(",")[0]?.trim() ?? "unknown";
        const now = nowFn();
        const slot = takeSlot(ip, now);
        if (!slot.ok) {
          c.header("Retry-After", String(slot.retryAfterSec));
          return c.json({ error: "Too many requests" }, 429);
        }

        // Body parse
        let body: unknown;
        try {
          body = await c.req.json();
        } catch {
          return c.json({ error: "Invalid JSON body" }, 400);
        }
        const { token } = (body ?? {}) as { token?: unknown };
        if (typeof token !== "string" || token.length === 0) {
          return c.json({ error: "token is required" }, 400);
        }

        if (!db) {
          return c.json({ error: "Database unavailable" }, 503);
        }

        // D-10: atomic single-use claim via UPDATE-RETURNING.
        // PostgreSQL row-lock makes the WHERE+SET atomic — concurrent requests
        // serialize and only the first sees `used_at IS NULL` true. The second's
        // WHERE no longer matches (used_at was just set), so it returns 0 rows.
        // No transaction wrapper needed. type='email_verify' filter prevents
        // cross-type claim against a password_reset row.
        //
        // gt(expiresAt, sql`now()`) uses PG clock for byte-exact symmetry with
        // CONTEXT D-10. JS clock would drift sub-millisecond; sql`now()` is
        // the documented choice (mirrors reset-password.ts:168).
        const tokenHash = crypto.createHash("sha256").update(token).digest("hex");
        const claimed = await db
          .update(passwordResetTokens)
          .set({ usedAt: new Date(now) })
          .where(
            and(
              eq(passwordResetTokens.tokenHash, tokenHash),
              eq(passwordResetTokens.type, "email_verify"),
              isNull(passwordResetTokens.usedAt),
              gt(passwordResetTokens.expiresAt, sql`now()`),
            ),
          )
          .returning({ userId: passwordResetTokens.userId });

        if (claimed.length === 0) {
          // Single-bucket: invalid OR expired OR used OR wrong-type. Prevents
          // info leak; one error string for the PWA to render (D-21 mirror).
          return c.json({ error: "Invalid or expired token" }, 400);
        }

        // D-11 step 2: UPDATE users SET email_verified_at = now().
        // Mutation order (LOAD-BEARING): claim BEFORE this update BEFORE 200.
        // If THIS update throws AFTER the claim succeeded above, the token is
        // already burned. User requests a fresh link via /resend-verification.
        // Acceptable failure mode (mirrors reset-password.ts:185-196).
        await db
          .update(users)
          .set({ emailVerifiedAt: new Date(now) })
          .where(eq(users.id, claimed[0]!.userId));

        // D-14: 200 { ok: true }. NO JWT, NO auto-login, NO token in response.
        // PWA reads the 200 and swaps to success state in place (Plan 04 D-19).
        return c.json({ ok: true }, 200);
      });

      return router;
    }

    /** Default singleton — what index.ts mounts. */
    export const verifyEmail = createVerifyEmailRoute();
    ```

    Step 2 — Create `vigil-core/src/routes/verify-email.test.ts` covering AUTH-11-V1, V2, V3, V4, G, MALFORMED, MISSING-TOKEN. Mirror `vigil-core/src/routes/reset-password.test.ts` test layout. Set `JWT_SECRET` before import. Use `__resetBucketsForTest()` in `beforeEach` to isolate rate limit state. DB-required tests skip with `t.skip("DATABASE_URL not set")` when `db` is unavailable (mirror `auth.test.ts:79-82`). Use unique IPs per test to dodge bucket cross-contamination.

    Test file skeleton:
    ```
    process.env["JWT_SECRET"] = "test-secret-32-chars-minimum-value-xxxxxx";

    import { describe, it, beforeEach } from "node:test";
    import assert from "node:assert/strict";
    import { eq } from "drizzle-orm";
    import * as crypto from "node:crypto";

    import {
      createVerifyEmailRoute,
      __resetBucketsForTest,
    } from "./verify-email.js";
    import { db } from "../db/connection.js";
    import { users, passwordResetTokens } from "../db/schema.js";

    function sha256Hex(s: string): string {
      return crypto.createHash("sha256").update(s).digest("hex");
    }

    async function seedUser(email: string): Promise<number | null> {
      if (!db) return null;
      const placeholderHash =
        "$argon2id$v=19$m=19456,t=2,p=1$dGVzdHNhbHR0ZXN0c2FsdA$dGVzdGhhc2h0ZXN0aGFzaHRlc3RoYXNodGVzdA";
      const [created] = await db
        .insert(users)
        .values({
          email: email.toLowerCase(),
          passwordHash: placeholderHash,
          passwordChangedAt: new Date(),
          emailVerifiedAt: null,
        })
        .onConflictDoNothing()
        .returning({ id: users.id });
      if (created) return created.id;
      const [existing] = await db
        .select({ id: users.id })
        .from(users)
        .where(eq(users.email, email.toLowerCase()))
        .limit(1);
      return existing?.id ?? null;
    }

    async function seedToken(
      userId: number,
      rawToken: string,
      type: "email_verify" | "password_reset",
      expiresAt: Date,
      usedAt: Date | null = null,
    ): Promise<void> {
      if (!db) return;
      await db.insert(passwordResetTokens).values({
        userId,
        tokenHash: sha256Hex(rawToken),
        type,
        expiresAt,
        usedAt,
      });
    }

    describe("POST /v1/auth/verify-email (AUTH-11)", () => {
      beforeEach(() => __resetBucketsForTest());

      // AUTH-11-V1-01..G-01..V-MALFORMED..V-MISSING-TOKEN
      // — Use a unique IP via the x-forwarded-for header for each test that
      //   doesn't specifically test rate limit isolation.
      // — DB-required tests: if (!db) t.skip("DATABASE_URL not set");
      // — For atomic claim assertions, build raw token via crypto.randomBytes(32).toString('base64url')
      //   and seed the row with sha256Hex(rawToken).
      // — Build the request: app.request("/v1/auth/verify-email", { method: "POST",
      //     headers: { "Content-Type": "application/json", "x-forwarded-for": uniqueIp },
      //     body: JSON.stringify({ token: rawToken }) });
    });
    ```

    Implement all 11 test cases (V1-01, V2-01, V2-02, V2-03, V2-04, V3-01, V4-01, V4-02, G-01, V-MALFORMED, V-MISSING-TOKEN) with concrete assertions per the <behavior> block.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && timeout 60 npx tsx --test src/routes/verify-email.test.ts 2>&1 | tail -30)</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-core/src/routes/verify-email.ts` exists with `export const verifyEmail` and `export function createVerifyEmailRoute`.
    - File `vigil-core/src/routes/verify-email.test.ts` exists with all 11 test cases.
    - `grep -c 'router.post("/auth/verify-email"' vigil-core/src/routes/verify-email.ts` returns 1.
    - `grep -c 'eq(passwordResetTokens.type, "email_verify")' vigil-core/src/routes/verify-email.ts` returns 1 (type filter in atomic claim).
    - `grep -c 'set({ emailVerifiedAt: new Date(now) })' vigil-core/src/routes/verify-email.ts` returns 1 (D-11 step 2).
    - `grep -c 'returning({ userId: passwordResetTokens.userId })' vigil-core/src/routes/verify-email.ts` returns 1 (atomic claim returning).
    - `grep -c '"Invalid or expired token"' vigil-core/src/routes/verify-email.ts` returns 1 (single-bucket).
    - `grep -c 'Retry-After' vigil-core/src/routes/verify-email.ts` returns 1 (rate limit header).
    - `grep -c '__resetBucketsForTest' vigil-core/src/routes/verify-email.ts` returns 1 (test seam exported).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npx tsx --test src/routes/verify-email.test.ts` shows all 11 tests pass (or DB-required tests skip cleanly when DATABASE_URL is unset, but unit tests V2-01, V4-01, V4-02, MALFORMED, MISSING-TOKEN MUST pass without DB).
    - Verify ordering invariant via grep: rate-limit `takeSlot` call appears BEFORE the body parse, body parse appears BEFORE the DB claim — `awk '/takeSlot\(ip, now\)/{r=NR} /c.req.json/{p=NR} /db.update\(passwordResetTokens\)/{d=NR} END{exit (r<p && p<d)?0:1}' vigil-core/src/routes/verify-email.ts` exits 0.
  </acceptance_criteria>
  <done>
    POST /v1/auth/verify-email handler is implemented per D-09..D-14 with the atomic UPDATE-RETURNING claim pattern, per-IP rate limit, single-bucket error UX, and the LOAD-BEARING claim-then-update mutation order. All 11 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resend-verification.ts (POST /v1/auth/resend-verification) + tests</name>
  <files>vigil-core/src/routes/resend-verification.ts, vigil-core/src/routes/resend-verification.test.ts</files>
  <read_first>
    - vigil-core/src/routes/forgot-password.ts (DI seam + fire-and-forget pattern + invalidate-prior pattern lines 182-191)
    - vigil-core/src/routes/reset-password.ts (per-key rate limit + Retry-After pattern lines 48-87)
    - vigil-core/src/db/schema.ts (passwordResetTokens, users.emailVerifiedAt)
    - vigil-core/src/services/email-service.ts (sendEmailVerificationEmail)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-15, D-16, D-17, D-18)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md sections "Resend Verification Route Shape" and "Pitfall 6"
  </read_first>
  <behavior>
    - Test AUTH-11-S1-01 (already-verified idempotency): inject DI lookup returning `{ email, emailVerifiedAt: <non-null Date> }` for the JWT userId → POST /v1/auth/resend-verification → 200 `{ ok: true, already_verified: true }` AND sendEmailVerificationEmail spy was NOT called AND no token row was inserted.
    - Test AUTH-11-S2-01 (rate limit hit): 4 successive POSTs from the same userId → first 3 succeed (200), 4th returns 429 with `Retry-After` header set.
    - Test AUTH-11-S2-02 (rate limit isolation per-userId): 3 POSTs from userId-A then 1 POST from userId-B → userId-B's request is NOT rate-limited (per-userId key).
    - Test AUTH-11-S2-03 (rate limit key prefix): the rate limit key uses the EXACT format `verify-resend:userId:{id}` (read-side test — extract via test seam OR observe by interleaving with a hypothetical other endpoint that uses the same Map; simpler: verify by greppability — the literal string `verify-resend:userId:` appears in the source).
    - Test AUTH-11-S3-01 (invalidate-prior + new token, requires DB): seed user (unverified) + 2 unused email_verify token rows for that user → POST /v1/auth/resend-verification → after the call: SELECT shows the 2 prior rows have `used_at` non-null (invalidated) AND a new row exists with `used_at IS NULL`. Most-recent-link wins (D-17).
    - Test AUTH-11-S3-02 (fire-and-forget send): POST → spy called EXACTLY ONCE with `(user.email, verifyUrl)` matching `/^https?:\/\/.+\/auth\/verify\?token=[A-Za-z0-9_-]{40,50}$/` AND response is 200 EVEN IF the spy throws synchronously.
    - Test AUTH-11-S-INVALID-USER: when DI lookup returns null (user row deleted) → 401 `{ error: "invalid_user" }`.
    - Test AUTH-11-S-MISSING-USERID: when c.get("userId") is missing → 401 `{ error: "invalid_user" }` (defensive — the bearerAuth dispatcher should prevent this in production but the route is defensive).
  </behavior>
  <action>
    Step 1 — Create `vigil-core/src/routes/resend-verification.ts`:

    File content:
    ```
    // ── Phase 113 Plan 03 — POST /v1/auth/resend-verification (AUTH-11) ──────────
    // bearerAuth required (D-15). Body: empty {}. userId from JWT (c.get("userId")).
    //
    // Behavior (D-16, D-17, D-18):
    //   1. D-18: idempotency — read users.email_verified_at; if non-null, return
    //      200 { ok: true, already_verified: true } and skip everything else.
    //   2. D-16: per-userId rate limit 3/hour with key `verify-resend:userId:{id}`.
    //      429 + Retry-After on excess.
    //   3. D-17: most-recent-link wins — UPDATE password_reset_tokens
    //      SET used_at = now() WHERE user_id = $1 AND type = 'email_verify'
    //      AND used_at IS NULL. Then INSERT a new email_verify row (24h expiry).
    //   4. Fire-and-forget sendEmailVerificationEmail.catch() (D-08 mirror).
    //   5. Return 200 { ok: true }.
    //
    // DI factory mirrors forgot-password.ts shape: createResendVerificationRoute(deps?)
    // accepts sendEmailFn (test spy) + dbOverride + nowFn + userLookupFn. Production
    // singleton uses the real wrappers/db.
    // -----------------------------------------------------------------------------

    import { Hono } from "hono";
    import { eq, and, isNull } from "drizzle-orm";
    import * as crypto from "node:crypto";
    import { db as defaultDb } from "../db/connection.js";
    import { users, passwordResetTokens } from "../db/schema.js";
    import { sendEmailVerificationEmail as realSendEmailVerificationEmail } from "../services/email-service.js";

    // ── Constants ────────────────────────────────────────────────────────────────
    const VERIFY_TOKEN_TTL_MS = 24 * 60 * 60 * 1000; // 24h (matches Plan 02 register helper)
    const RATE_LIMIT_MAX = 3;                         // D-16: 3 per hour per userId
    const RATE_LIMIT_WINDOW_MS = 60 * 60 * 1000;
    const RATE_KEY_PREFIX = "verify-resend:userId:"; // D-16 exact format — namespacing prevents Map collisions

    // ── Per-userId rate limit (mirrors reset-password.ts:48-87 shape) ────────────
    const userBuckets = new Map<string, number[]>();

    setInterval(() => {
      const cutoff = Date.now() - RATE_LIMIT_WINDOW_MS;
      for (const [k, arr] of userBuckets) {
        const last = arr[arr.length - 1];
        if (arr.length === 0 || (last !== undefined && last < cutoff)) {
          userBuckets.delete(k);
        }
      }
    }, RATE_LIMIT_WINDOW_MS).unref();

    function takeSlot(
      key: string,
      now: number,
    ): { ok: boolean; retryAfterSec: number } {
      const cutoff = now - RATE_LIMIT_WINDOW_MS;
      const arr = (userBuckets.get(key) ?? []).filter((t) => t >= cutoff);
      if (arr.length >= RATE_LIMIT_MAX) {
        const oldest = arr[0]!;
        const retryAfterSec = Math.max(
          1,
          Math.ceil((oldest + RATE_LIMIT_WINDOW_MS - now) / 1000),
        );
        userBuckets.set(key, arr);
        return { ok: false, retryAfterSec };
      }
      arr.push(now);
      userBuckets.set(key, arr);
      return { ok: true, retryAfterSec: 0 };
    }

    /** Test-only helper. MUST NOT be called from production code. */
    export function __resetBucketsForTest(): void {
      userBuckets.clear();
    }

    // ── Factory + default singleton ─────────────────────────────────────────────
    export interface ResendVerificationDeps {
      /** DI seam — defaults to email-service.sendEmailVerificationEmail */
      sendEmailFn?: typeof realSendEmailVerificationEmail;
      /** DI seam — defaults to the real `db` from connection.js */
      dbOverride?: typeof defaultDb;
      /** DI seam — defaults to Date.now */
      nowFn?: () => number;
      /**
       * DI seam — replaces the SELECT users WHERE id=$1 lookup. Tests use this
       * to inject already-verified vs unverified states without seeding the DB.
       * Returns null when the user row does not exist → 401 invalid_user.
       */
      userLookupFn?: (
        userId: number,
      ) => Promise<{ email: string; emailVerifiedAt: Date | null } | null>;
    }

    export function createResendVerificationRoute(
      deps?: ResendVerificationDeps,
    ): Hono {
      const sendEmailFn = deps?.sendEmailFn ?? realSendEmailVerificationEmail;
      const db = deps?.dbOverride ?? defaultDb;
      const nowFn = deps?.nowFn ?? Date.now;

      const defaultLookup = async (
        userId: number,
      ): Promise<{ email: string; emailVerifiedAt: Date | null } | null> => {
        if (!db) return null;
        const [u] = await db
          .select({ email: users.email, emailVerifiedAt: users.emailVerifiedAt })
          .from(users)
          .where(eq(users.id, userId))
          .limit(1);
        return u ?? null;
      };
      const userLookupFn = deps?.userLookupFn ?? defaultLookup;

      const router = new Hono();

      router.post("/auth/resend-verification", async (c) => {
        const userId = c.get("userId") as number | undefined;
        if (!Number.isInteger(userId) || (userId as number) <= 0) {
          return c.json({ error: "invalid_user" }, 401);
        }

        const now = nowFn();

        // D-18: idempotency — read users.email_verified_at FIRST (before rate
        // limit slot consumption, so an already-verified user doesn't burn
        // their rate limit on a no-op).
        const user = await userLookupFn(userId as number);
        if (!user) {
          return c.json({ error: "invalid_user" }, 401);
        }
        if (user.emailVerifiedAt !== null) {
          return c.json({ ok: true, already_verified: true }, 200);
        }

        // D-16: per-userId rate limit 3/hour. Key prefixed `verify-resend:userId:{id}`
        // to prevent Map collisions if other endpoints ever share a Map (defensive
        // namespacing — RESEARCH Pitfall 6).
        const slot = takeSlot(`${RATE_KEY_PREFIX}${userId}`, now);
        if (!slot.ok) {
          c.header("Retry-After", String(slot.retryAfterSec));
          return c.json({ error: "Too many requests" }, 429);
        }

        if (!db) {
          return c.json({ error: "Database unavailable" }, 503);
        }

        // D-17: invalidate prior unused email_verify tokens — most-recent-link wins.
        // Mirrors forgot-password.ts:182-191 verbatim with type filter changed.
        await db
          .update(passwordResetTokens)
          .set({ usedAt: new Date(now) })
          .where(
            and(
              eq(passwordResetTokens.userId, userId as number),
              eq(passwordResetTokens.type, "email_verify"),
              isNull(passwordResetTokens.usedAt),
            ),
          );

        // Generate raw + hash + INSERT new email_verify row (24h expiry).
        // Same shape as Plan 02's issueEmailVerifyToken helper. Not extracted
        // to a shared module yet — Claude's Discretion (CONTEXT) — defer until
        // call-site count justifies; revisit in v3.7.
        const rawToken = crypto.randomBytes(32).toString("base64url");
        const tokenHash = crypto
          .createHash("sha256")
          .update(rawToken)
          .digest("hex");
        await db.insert(passwordResetTokens).values({
          userId: userId as number,
          tokenHash,
          type: "email_verify",
          expiresAt: new Date(now + VERIFY_TOKEN_TTL_MS),
        });

        // Fire-and-forget send (D-08 mirror) — .catch() attached synchronously
        // before the response so a synchronously-throwing spy still returns 200.
        const origin =
          process.env["VIGIL_APP_BASE_URL"] || "https://app.vigilhub.io";
        const verifyUrl = `${origin}/auth/verify?token=${rawToken}`;
        sendEmailFn(user.email, verifyUrl).catch((err) => {
          console.error("[resend-verification] email send failed (background):", err);
        });

        return c.json({ ok: true }, 200);
      });

      return router;
    }

    /** Default singleton — what index.ts mounts. */
    export const resendVerification = createResendVerificationRoute();
    ```

    Step 2 — Create `vigil-core/src/routes/resend-verification.test.ts`. Use the same `buildAppWithUserId(router, userId)` helper pattern from `auth-me.test.ts` to inject `c.get("userId")`. For DI lookup tests, inject `userLookupFn` to return synthetic states; for invalidate-prior + new-token assertions, require DB and skip when DATABASE_URL is unset.

    Test file skeleton:
    ```
    process.env["JWT_SECRET"] = "test-secret-32-chars-minimum-value-xxxxxx";

    import { describe, it, beforeEach, mock } from "node:test";
    import assert from "node:assert/strict";
    import { Hono } from "hono";
    import { eq, and, isNull } from "drizzle-orm";

    import {
      createResendVerificationRoute,
      __resetBucketsForTest,
    } from "./resend-verification.js";
    import { db } from "../db/connection.js";
    import { users, passwordResetTokens } from "../db/schema.js";

    function buildAppWithUserId(router: Hono, userId: number | undefined): Hono {
      const app = new Hono();
      app.use("*", async (c, next) => {
        if (userId !== undefined) c.set("userId", userId);
        await next();
      });
      app.route("/v1", router);
      return app;
    }

    describe("POST /v1/auth/resend-verification (AUTH-11)", () => {
      beforeEach(() => __resetBucketsForTest());

      // AUTH-11-S1-01..S-MISSING-USERID — see <behavior> for full assertions.
    });
    ```

    Implement all 8 test cases (S1-01, S2-01, S2-02, S2-03, S3-01, S3-02, S-INVALID-USER, S-MISSING-USERID).
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && timeout 60 npx tsx --test src/routes/resend-verification.test.ts 2>&1 | tail -30)</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-core/src/routes/resend-verification.ts` exists with `export const resendVerification` and `export function createResendVerificationRoute`.
    - File `vigil-core/src/routes/resend-verification.test.ts` exists with all 8 test cases.
    - `grep -c 'router.post("/auth/resend-verification"' vigil-core/src/routes/resend-verification.ts` returns 1.
    - `grep -c '"verify-resend:userId:"' vigil-core/src/routes/resend-verification.ts` returns 1 (D-16 exact key prefix, namespaced to avoid Map collisions).
    - `grep -c 'already_verified: true' vigil-core/src/routes/resend-verification.ts` returns 1 (D-18 idempotency).
    - `grep -c 'isNull(passwordResetTokens.usedAt)' vigil-core/src/routes/resend-verification.ts` returns 1 (D-17 invalidate-prior).
    - `grep -c 'type: "email_verify"' vigil-core/src/routes/resend-verification.ts` returns at least 2 (UPDATE invalidate filter + INSERT new row).
    - `grep -c 'sendEmailFn.*\.catch' vigil-core/src/routes/resend-verification.ts` returns 1 (fire-and-forget catch).
    - `grep -c 'RATE_LIMIT_MAX = 3' vigil-core/src/routes/resend-verification.ts` returns 1 (D-16 limit value).
    - `grep -c 'Retry-After' vigil-core/src/routes/resend-verification.ts` returns 1.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npx tsx --test src/routes/resend-verification.test.ts` shows all 8 tests pass (DB-required tests skip cleanly when unset; unit tests S1-01, S2-01, S2-02, S2-03, S3-02, S-INVALID-USER, S-MISSING-USERID MUST pass without DB).
    - The idempotency check appears BEFORE the rate-limit slot consumption: `awk '/already_verified: true/{i=NR} /takeSlot\(\$\{RATE_KEY_PREFIX\}/{r=NR} END{exit (i<r)?0:1}' vigil-core/src/routes/resend-verification.ts` exits 0 (already-verified user does not burn rate limit slot — D-18 nuance).
  </acceptance_criteria>
  <done>
    POST /v1/auth/resend-verification handler is implemented per D-15..D-18. All 8 tests pass. Idempotency-before-rate-limit ordering enforced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount verifyEmail and resendVerification routers in index.ts in correct order</name>
  <files>vigil-core/src/index.ts</files>
  <read_first>
    - vigil-core/src/index.ts (lines 110-186 — current route mount layout: forgotPassword/resetPassword BEFORE dispatcher, others AFTER)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-12 verify-email unauth; D-15 resend bearerAuth)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md "Route Mounting Order" section
  </read_first>
  <action>
    Two edits to `vigil-core/src/index.ts`:

    Edit 1 — Add imports. After the existing `import { resetPassword } from "./routes/reset-password.js";` (line 38) and `import { me } from "./routes/me.js";` (line 39), add:
    ```
    import { verifyEmail } from "./routes/verify-email.js";          // Phase 113 (AUTH-11) — unauthenticated; mount BEFORE bearerAuth dispatcher
    import { resendVerification } from "./routes/resend-verification.js"; // Phase 113 (AUTH-11) — bearerAuth required; mount AFTER dispatcher
    ```

    Edit 2 — Add the two mount lines. The verify-email mount goes BEFORE the bearerAuth dispatcher block (lines 127-135). Locate the existing block:
    ```
    // Phase 112 Plan 03 — reset-password is unauthenticated (the opaque token
    // IS the auth credential). Mount BEFORE bearerAuth and exempt the path below.
    app.route("/v1", resetPassword);
    ```
    Insert DIRECTLY AFTER the `app.route("/v1", resetPassword);` line:
    ```
    // Phase 113 (AUTH-11) — verify-email is unauthenticated (the opaque token IS
    // the auth credential — D-12). Mount BEFORE bearerAuth and ensure Plan 02
    // Task 3 added the path to the dispatcher's exempt list.
    app.route("/v1", verifyEmail);
    ```

    The resend-verification mount goes AFTER the bearerAuth dispatcher, alongside the other protected routes. Insert it right after the existing `app.route("/v1", me);` line (the protected `/v1/me` route from Phase 102 — this position exists today, before any Plan 02 edit). If Plan 02 has already inserted `app.route("/v1", authMe);` after that line, insert this new line AFTER `authMe` instead — either position works; the awk acceptance check below verifies only that this mount lands AFTER the bearerAuth dispatcher block, not its exact neighbor. Insert:
    ```
    // Phase 113 (AUTH-11 D-15) — resend-verification is bearerAuth-protected
    // (user must be logged in; rate limit keyed by JWT-derived userId). Mounted
    // AFTER the dispatcher so it inherits the bearerAuth gate automatically.
    app.route("/v1", resendVerification);
    ```

    Critical: DO NOT mount `verifyEmail` AFTER the dispatcher (would 401 every request — token-IS-auth design broken). DO NOT mount `resendVerification` BEFORE the dispatcher (would silently bypass bearerAuth — silent IDOR via body userId would be possible — though the route reads `c.get("userId")` not body, the bearerAuth dispatcher is the source of truth for setting that context value).
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'import { verifyEmail }' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'import { resendVerification }' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'app.route("/v1", verifyEmail)' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && grep -c 'app.route("/v1", resendVerification)' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && awk '/app.route\("\/v1", resetPassword\)/{rp=NR} /app.route\("\/v1", verifyEmail\)/{ve=NR} /bearerAuth\(c, next\)/{disp=NR} /app.route\("\/v1", resendVerification\)/{rv=NR} END{ if(ve>rp && ve<disp && rv>disp){ print "ORDER OK ve="ve" rp="rp" disp="disp" rv="rv; exit 0 } else { print "ORDER WRONG"; exit 1 } }' src/index.ts) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-core" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { verifyEmail }' vigil-core/src/index.ts` returns 1.
    - `grep -c 'import { resendVerification }' vigil-core/src/index.ts` returns 1.
    - `grep -c 'app.route("/v1", verifyEmail)' vigil-core/src/index.ts` returns 1.
    - `grep -c 'app.route("/v1", resendVerification)' vigil-core/src/index.ts` returns 1.
    - The mount-order awk check (above) prints `ORDER OK` and exits 0: `verifyEmail` mount line number > `resetPassword` mount line number AND < the `bearerAuth(c, next)` dispatcher line number; `resendVerification` mount line number > `bearerAuth(c, next)` dispatcher line number.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - The full server boots without errors: `(cd vigil-core && timeout 5 npx tsx --eval "import('./src/index.js').then(()=>{console.log('IMPORT OK');process.exit(0)}).catch(e=>{console.error('IMPORT FAIL',e);process.exit(1)})" 2>&1 | tail -5)` shows `IMPORT OK` (or boots into the listen state — accept either; the import-only test isolates module resolution from runtime DB connection).
    - No other line in the file was modified — `git diff vigil-core/src/index.ts` shows ONLY 4 inserted lines (2 imports + 2 mounts) plus comment lines.
  </acceptance_criteria>
  <done>
    Both routers are mounted in the correct positions. Live request to `POST /v1/auth/verify-email` (no JWT) reaches the handler; live request to `POST /v1/auth/resend-verification` (no JWT) returns 401 from bearerAuth (correct gate); request with valid JWT reaches the handler.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → POST /v1/auth/verify-email | The token IS the auth credential. Untrusted body `{ token }` crosses; SHA-256 hash + atomic UPDATE-RETURNING is the gate. |
| authenticated client → POST /v1/auth/resend-verification | bearerAuth gate enforces userId; rate limit + idempotency further restrict abuse. |
| Resend SDK boundary (background) | sendEmailVerificationEmail wrapper handles WR-01 escape and click_tracking; Plan 03 only invokes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure (email enumeration via verify endpoint) | Verify-email response shape | accept | Token entropy (256 bits via crypto.randomBytes(32)) + atomic claim is the real defense — guessing a valid token to even reach the user-row-update is infeasible. Single-bucket 400 response on failure prevents differentiation of user existence. SC#3 says success sets emailVerifiedAt (observable to a logged-in user via /v1/auth/me) but a successful verify by definition required possession of a server-issued token, so the disclosure adds no new information beyond what the user already knew. |
| T-113-02 | Tampering (token brute force) | POST /v1/auth/verify-email | mitigate | (a) 256-bit token entropy = 2^256 search space; (b) per-IP rate limit 5/hour (D-13) with Retry-After. takeSlot uses sliding window per reset-password.ts:48-87. Test AUTH-11-V4-01 enforces 6th request returns 429. |
| T-113-03 | Tampering (token replay) | POST /v1/auth/verify-email | mitigate | Atomic UPDATE-RETURNING with `used_at IS NULL` filter + PG row-lock — concurrent claims serialize, only first sees the row. Test AUTH-11-V3-01 enforces second claim returns 400. |
| T-113-04 | Information Disclosure (token leak via server logs) | Plan 03 routes | mitigate | (a) Token in POST body, not URL (D-09); (b) vigil-core does not enable Hono's request body logger by default — verified by reading index.ts middleware list (rateLimiter, secureHeaders, timeout, cors, metricsMiddleware — no body logger). Console.error in `.catch()` logs only the error, not the request body. |
| T-113-06 | EoP (verify endpoint accessible without JWT — by design) | bearerAuth bypass | mitigate | Plan 02 Task 3 adds EXACTLY ONE bypass entry for `/v1/auth/verify-email`. Plan 03 Task 3 mounts the route BEFORE the dispatcher (correctness gate). The mount-order awk check verifies layout. /v1/auth/resend-verification is NOT bypassed and IS mounted after dispatcher — correct gate. |
| T-113-07 | DoS (Resend endpoint abuse) | POST /v1/auth/resend-verification | mitigate | (a) bearerAuth required (D-15) so rate limit keyed by authenticated userId; (b) 3/hour per userId (D-16) — Math.max(1, Math.ceil(...)) Retry-After floor prevents 0-second retry advice; (c) idempotency check before rate limit consumption (already-verified user doesn't burn slots — defensive UX choice). |
| T-113-DOS-RES-02 | DoS (Map memory growth from rate-limit buckets) | userBuckets / ipBuckets in-process Map | mitigate | setInterval sweep with .unref() drops stale buckets per RATE_LIMIT_WINDOW_MS — copies reset-password.ts:59-67 / forgot-password.ts:62-77 pattern. Single-instance Railway scale; multi-instance scale-out would need Redis (deferred to v3.7+). |
| T-113-MUTORDER-01 | Tampering (incomplete state mutation) | verify-email D-11 ordering | mitigate | LOAD-BEARING order documented + enforced: claim THEN update users THEN return 200. If users update fails after claim: token burned (acceptable failure mode), user requests new via resend. Mirrors reset-password.ts:185-196 pattern. |
| T-113-IDEM-01 | EoP (already-verified rate-limit burn) | resend-verification D-18 | mitigate | userLookupFn check fires BEFORE takeSlot consumption — already-verified user gets `200 already_verified` without consuming a slot. The awk acceptance check enforces this ordering at file-content level. |
</threat_model>

<verification>
- Tasks 1 + 2 each have automated test runs that exit green (DB-required tests skip cleanly when DATABASE_URL unset).
- Task 3 has grep + awk-order checks proving correct mount layout.
- Cross-task: `(cd vigil-core && timeout 90 npm test 2>&1 | tail -30; pkill -f tsx 2>/dev/null || true)` — accept the known post-suite hang and force-kill (STATE.md Blockers).
- Live smoke test belongs in Plan 05.
</verification>

<success_criteria>
- POST /v1/auth/verify-email implemented per D-09..D-14 with atomic UPDATE-RETURNING, single-bucket 400 errors, 5/hour per-IP rate limit + Retry-After.
- POST /v1/auth/resend-verification implemented per D-15..D-18 with bearerAuth, 3/hour per-userId rate limit, idempotency-before-rate-limit, invalidate-prior-tokens, fire-and-forget send.
- Both endpoints mounted correctly in index.ts (verify-email BEFORE dispatcher, resend AFTER).
- All 19 new tests green (11 in verify-email.test.ts + 8 in resend-verification.test.ts).
- TypeScript build green; full vigil-core test suite remains green (no regressions).
- Server boots without errors after the mount.
</success_criteria>

<output>
After completion, create `.planning/phases/113-verify-email-on-signup/113-03-SUMMARY.md` documenting:
- Whether the planner extracted a shared `tokenIssue.ts` helper after Plan 03 (now 4 INSERT sites: register-fresh, register-claim, forgot-password, resend-verification — Claude's Discretion).
- Test count delta + any pre-existing flaky tests observed.
- Confirmation of mount order via awk check output.
- Confirmation that the bypass list now satisfies the verify-email handler reachability.
</output>
