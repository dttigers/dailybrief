---
phase: 113
plan: 04
type: execute
wave: 3
depends_on:
  - "113-02"
  - "113-03"
files_modified:
  - vigil-pwa/src/pages/VerifyEmailPage.tsx
  - vigil-pwa/src/pages/VerifyEmailPage.test.tsx
  - vigil-pwa/src/pages/SettingsPage.tsx
  - vigil-pwa/src/pages/SettingsPage.test.tsx
  - vigil-pwa/src/pages/AuthPage.tsx
  - vigil-pwa/src/App.tsx
autonomous: true
requirements:
  - AUTH-11
must_haves:
  truths:
    - "PWA route /auth/verify renders VerifyEmailPage; route is OUTSIDE the protected Layout cluster (mirrors /auth/forgot and /auth/reset placement in App.tsx) — no isAuthenticated guard"
    - "VerifyEmailPage parses ?token=... at mount via useSearchParams and does NOT fire any API call until the user clicks Confirm (Apple Mail prefetch defense — D-19)"
    - "VerifyEmailPage uses raw fetch() (NOT vigilFetch) for POST /v1/auth/verify-email — endpoint is unauthenticated; avoids 401-redirect side effects (UI-SPEC §Notes-3)"
    - "VerifyEmailPage renders 4 terminal states per UI-SPEC: idle (token present), success (200), error (single-bucket — 400/5xx/network), missing_token (no ?token in URL)"
    - "Success state swaps in-place with no redirect, no URL change (D-20) — secondary link 'Go to app' → '/' is the only nav"
    - "SettingsPage banner renders at the very top of /settings when meData.emailVerifiedAt === null (D-22, D-23) — uses bg-warning-50 + border-warning-400 + text-warning-400 tokens"
    - "Settings banner is non-dismissible (D-24) — no × control rendered"
    - "Settings Resend button has 5 lifecycle states per UI-SPEC: idle/sending/sent (10s timer)/rate_limited/error"
    - "SettingsPage adds a NEW useEffect that calls vigilFetch('/v1/auth/me') alongside the existing /v1/me call (D-28 — local component state, no global store)"
    - "AuthPage login response handler updated to type-annotate user.emailVerifiedAt (D-26 additive — does not store anywhere yet, but the type is correct for future consumers)"
    - "VerifyEmailPage test asserts 'does NOT call fetch on mount' (Apple Mail prefetch regression guard)"
  artifacts:
    - path: "vigil-pwa/src/pages/VerifyEmailPage.tsx"
      provides: "/auth/verify static page with Confirm-gated POST"
      contains: "VerifyEmailPage"
    - path: "vigil-pwa/src/pages/VerifyEmailPage.test.tsx"
      provides: "vitest cases AUTH-11-P + AUTH-11-P2 + state-machine coverage"
      contains: "VerifyEmailPage"
    - path: "vigil-pwa/src/pages/SettingsPage.tsx"
      provides: "Verify banner + Resend lifecycle button + new /v1/auth/me useEffect"
      contains: "emailVerifiedAt"
    - path: "vigil-pwa/src/pages/SettingsPage.test.tsx"
      provides: "vitest cases AUTH-11-B + AUTH-11-B2"
      contains: "Verify your email"
    - path: "vigil-pwa/src/pages/AuthPage.tsx"
      provides: "Login response type extended with emailVerifiedAt"
      contains: "emailVerifiedAt"
    - path: "vigil-pwa/src/App.tsx"
      provides: "Route /auth/verify mounted as sibling unauthenticated route"
      contains: "/auth/verify"
  key_links:
    - from: "vigil-pwa/src/pages/VerifyEmailPage.tsx"
      to: "POST /v1/auth/verify-email (Plan 03)"
      via: "raw fetch(`${API_BASE}/v1/auth/verify-email`, {method:'POST', body: JSON.stringify({token})}) on Confirm click"
      pattern: "fetch\\(.*v1/auth/verify-email"
    - from: "vigil-pwa/src/pages/SettingsPage.tsx"
      to: "GET /v1/auth/me (Plan 02)"
      via: "vigilFetch('/v1/auth/me') in useEffect on mount"
      pattern: "vigilFetch\\('/v1/auth/me'\\)"
    - from: "vigil-pwa/src/pages/SettingsPage.tsx"
      to: "POST /v1/auth/resend-verification (Plan 03)"
      via: "vigilFetch('/v1/auth/resend-verification', {method:'POST'}) on Resend click"
      pattern: "vigilFetch\\('/v1/auth/resend-verification'"
    - from: "vigil-pwa/src/App.tsx"
      to: "VerifyEmailPage"
      via: "<Route path='/auth/verify' element={<VerifyEmailPage />} />"
      pattern: "/auth/verify"
---

<objective>
Ship the PWA surface for email verification per the locked UI-SPEC and CONTEXT D-19 through D-28. Three components touched, one new component created, one App.tsx route added:

1. **NEW** `vigil-pwa/src/pages/VerifyEmailPage.tsx` — `/auth/verify` static page. Parses `?token=...` at mount via `useSearchParams`. NO API call until user clicks Confirm. Uses raw `fetch()` (NOT vigilFetch) so the 401-redirect handler doesn't fire. 4 terminal states: idle/success/error/missing_token. In-place swap on success — no redirect.

2. **MODIFY** `vigil-pwa/src/pages/SettingsPage.tsx` — Add the NEW `vigilFetch('/v1/auth/me')` call in a parallel useEffect (alongside the existing `/v1/me` call — do NOT consolidate, per UI-SPEC §Notes-4 and D-28). Render the verify banner at the TOP of the page (before the existing `{banner && ...}` block and `<h1>Settings</h1>`). Implement the 5-state Resend button lifecycle.

3. **MODIFY** `vigil-pwa/src/pages/AuthPage.tsx` — Update the login response destructure type annotation to include `emailVerifiedAt: string | null` (D-26 additive). Login flow does not store this field anywhere in this plan — Settings refetches via `/v1/auth/me`. Type correctness is the deliverable.

4. **MODIFY** `vigil-pwa/src/App.tsx` — Mount `<Route path="/auth/verify" element={<VerifyEmailPage />} />` as a sibling unauthenticated route, alongside `/auth/forgot` and `/auth/reset` (lines 69-70).

Purpose: Closes SC#2 and SC#3 from the user-visible side. After this plan, the entire AUTH-11 user-facing flow works end-to-end against the live Plan 02/03 endpoints.

Output: 6 files modified/created. PWA test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-verify-email-on-signup/113-CONTEXT.md
@.planning/phases/113-verify-email-on-signup/113-RESEARCH.md
@.planning/phases/113-verify-email-on-signup/113-UI-SPEC.md
@.planning/phases/113-verify-email-on-signup/113-VALIDATION.md
@vigil-pwa/src/pages/ResetPasswordPage.tsx
@vigil-pwa/src/pages/SettingsPage.tsx
@vigil-pwa/src/pages/AuthPage.tsx
@vigil-pwa/src/App.tsx
@vigil-pwa/src/api/client.ts
@vigil-pwa/src/index.css

<interfaces>
Source-of-truth signatures for the PWA surfaces this plan touches.

`vigil-pwa/src/api/client.ts`:
- `export const API_BASE: string` — base URL for all fetches (`'' ` in dev or `'https://api.vigilhub.io'` in prod).
- `export async function vigilFetch(path: string, init?: RequestInit): Promise<Response>` — adds Authorization Bearer header from sessionStorage, redirects to /auth on 401 with body `{ error: 'Session expired' }`.
- `getStoredKey(): string | null` — reads sessionStorage `'vigil_jwt'`.

`react-router` (v7) — already in deps:
- `useSearchParams()` returns `[URLSearchParams, ...]`.
- `Link` for in-app nav.
- `useNavigate()` returns navigate function.

Existing pattern source — `vigil-pwa/src/pages/ResetPasswordPage.tsx` lines 28-78:
- `useMemo(searchParams.get('token'))` at mount, NO useEffect fetch.
- Submit handler is the only place `fetch()` is called.
- 200/400/429/5xx all distinct branches; 400 swaps to terminal error state.
- Same dark shell: `min-h-screen flex items-center justify-center bg-gray-900 / bg-gray-900 rounded-lg p-8 w-full max-w-md mx-4`.

`vigil-pwa/src/pages/AuthPage.tsx` line 56 (current login response destructure):
```
const { token, user } = (await res.json()) as { token: string; user: { id: number; email: string } }
```
Plan 04 changes the type annotation to:
```
const { token, user } = (await res.json()) as { token: string; user: { id: number; email: string; emailVerifiedAt: string | null } }
```
The destructure itself doesn't change (no new variable extracted); the type is widened so future consumers can read `user.emailVerifiedAt` without a TS error.

`vigil-pwa/src/index.css` `@theme` tokens (lines 24-25 — UI-SPEC verified):
- `--color-warning-50: #FAEEDA` → use class `bg-warning-50`
- `--color-warning-400: #BA7517` → use classes `border-warning-400` + `text-warning-400`

Existing SettingsPage useEffect for /v1/me (lines 91-107 — DO NOT modify, just add a NEW useEffect alongside):
```
useEffect(() => {
  vigilFetch('/v1/me')
    .then((r) => r.ok ? r.json() : undefined)
    .then((data?: { userId: string; email: string }) => {
      if (data?.email) setAccountEmail(data.email)
      setAccountLoading(false)
    })
    .catch(() => { setAccountLoading(false) })
}, [])
```

Existing App.tsx unauthenticated route layout (lines 65-70):
```
<Route path="/auth/forgot" element={<ForgotPasswordPage />} />
<Route path="/auth/reset" element={<ResetPasswordPage />} />
// <- Plan 04 ADDS one route here for /auth/verify
```

Existing SettingsPage outermost layout (around line 200+, the page render):
```
<div className="p-4 max-w-2xl mx-auto text-gray-50">
  {banner && (...)}                  // Existing dismissible banner — DO NOT touch
  <h1 ...>Settings</h1>              // Existing heading
  ...                                // Existing sections
</div>
```
UI-SPEC §Component 2 §Placement: the verify banner renders FIRST inside this div, BEFORE the existing `{banner && ...}` block and BEFORE `<h1>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VerifyEmailPage.tsx + VerifyEmailPage.test.tsx + mount /auth/verify route in App.tsx</name>
  <files>vigil-pwa/src/pages/VerifyEmailPage.tsx, vigil-pwa/src/pages/VerifyEmailPage.test.tsx, vigil-pwa/src/App.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/ResetPasswordPage.tsx (the canonical "no fetch on mount + form-submit-gated POST" pattern; full file)
    - vigil-pwa/src/pages/ResetPasswordPage.test.tsx (test pattern reference — vitest + @testing-library/react)
    - vigil-pwa/src/App.tsx (lines 65-70 — sibling unauthenticated route placement)
    - .planning/phases/113-verify-email-on-signup/113-UI-SPEC.md sections "Component 1: VerifyEmailPage" and "State Diagrams" + "Copywriting Contract Surface 2/3/4/5"
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-19, D-20, D-21)
    - vigil-pwa/src/api/client.ts (API_BASE export, vigilFetch — but VerifyEmailPage uses raw fetch, NOT vigilFetch)
  </read_first>
  <behavior>
    - Test AUTH-11-P-MOUNT-NO-FETCH: render `<VerifyEmailPage />` inside `<MemoryRouter initialEntries={['/auth/verify?token=abc123']}>`. After mount, assert `global.fetch` mock was NOT called (Apple Mail prefetch defense). This is the SINGLE most important regression guard.
    - Test AUTH-11-P-IDLE-RENDERS-COPY: with `?token=abc123` in URL, assert the rendered DOM contains the LOCKED copy strings: "Verify your email" (h1), "Click the button below to confirm your email address." (p), "Confirm" (button label).
    - Test AUTH-11-P-MISSING-TOKEN: render with `initialEntries={['/auth/verify']}` (no query param) → renders the "This verification link is malformed" h1 + "Please use the button in the email we sent you." body + "Back to app" link to `/` (LOCKED copy from UI-SPEC Surface 5). Asserts global.fetch was still NOT called.
    - Test AUTH-11-P2-CONFIRM-200: mock fetch to return `{ ok: true, status: 200, json: async () => ({ ok: true }) }`. Render with token → click Confirm → wait for state swap → assert success copy is rendered: "Email verified" h1 + "You can close this tab, or" body + "Go to app" link to `/`. Assert fetch was called EXACTLY ONCE with URL `${API_BASE}/v1/auth/verify-email`, method POST, body `{"token":"abc123"}`.
    - Test AUTH-11-P2-CONFIRM-400: mock fetch to return `{ ok: false, status: 400, json: async () => ({ error: 'Invalid or expired token' }) }`. Click Confirm → assert error copy is rendered: "This link is no longer valid" h1 + "Verification links expire after 24 hours and can only be used once." body + "Request a new link" button + "Back to app" secondary link.
    - Test AUTH-11-P2-CONFIRM-500: mock fetch to return `{ ok: false, status: 500, ... }`. Click Confirm → assert SAME error UX as 400 (single-bucket — D-21 collapses 400/5xx/network).
    - Test AUTH-11-P2-CONFIRM-NETWORK: mock fetch to throw a network error. Click Confirm → assert SAME error UX (single-bucket).
    - Test AUTH-11-P2-RAW-FETCH: spy on `vigilFetch` import (mock the module) and assert it was NEVER called from VerifyEmailPage — confirms the page uses raw `fetch()` not `vigilFetch` (UI-SPEC §Notes-3 anti-401-redirect-handler).
    - Test AUTH-11-P-LOGIN-DEST-LOGGED-IN: in error state, with `sessionStorage.getItem('vigil_jwt')` returning a non-null value, the "Request a new link" button navigates to `/settings`. (Use a mock `useNavigate` and assert navigate('/settings') was called.)
    - Test AUTH-11-P-LOGIN-DEST-LOGGED-OUT: in error state, with sessionStorage clear, "Request a new link" navigates to `/auth`.
    - Test AUTH-11-P-USES-USE-MEMO: assert the token parsing happens via `useMemo(() => searchParams.get('token'), [searchParams])` (or a one-shot useState initializer), NOT via `useState` re-derivation each render that could hide a useEffect leak. Verify by source grep: `grep -c useEffect vigil-pwa/src/pages/VerifyEmailPage.tsx` returns 0 (NO useEffect at all in VerifyEmailPage — D-19 prefetch defense).
  </behavior>
  <action>
    Step 1 — Create `vigil-pwa/src/pages/VerifyEmailPage.tsx`:

    File content:
    ```
    import { useState, useMemo } from 'react'
    import { Link, useNavigate, useSearchParams } from 'react-router'
    import { API_BASE } from '../api/client'

    /**
     * Phase 113 (AUTH-11) — Verify Email Page (UI-SPEC Component 1).
     *
     * Reads `?token=...` from the URL at mount via useSearchParams. FOUR
     * terminal visual states (UI-SPEC State Diagrams):
     *   1. MISSING_TOKEN — no ?token in URL  → render malformed-link UX
     *   2. IDLE          — token present     → "Verify your email" + Confirm button
     *   3. SUCCESS       — POST returned 200 → "Email verified" + "Go to app"
     *   4. ERROR         — 400/5xx/network   → single-bucket "This link is no longer valid"
     *
     * **CRITICAL — D-19 prefetch-safe gate:** This component does NOT call any
     * API on mount. The token is parsed from the URL but ONLY POSTed when the
     * user clicks Confirm. Apple Mail Privacy Protection (iOS 15.4+/macOS 12.3+)
     * silently fetches every link in an email through Apple's edge proxy with a
     * generic browser UA — a useEffect-fired POST would burn the token before
     * the human ever sees the email. Resend's link tracking is disabled at the
     * domain level (Phase 111 first defense layer); this Confirm gate is the
     * second.
     *
     * **Do NOT add a useEffect that triggers fetch.** A regression test
     * (AUTH-11-P-MOUNT-NO-FETCH) asserts global.fetch is NOT called on mount.
     *
     * **D-21 single-bucket UX:** 400 (invalid/expired/used) AND 5xx AND network
     * errors all collapse to the same error UX. Deliberately hides which sub-
     * bucket fired — that's the security feature, not a bug.
     *
     * **Raw fetch() — not vigilFetch():** The /v1/auth/verify-email endpoint is
     * unauthenticated (bypass-listed in bearerAuth — Plan 02 Task 3). Using
     * vigilFetch could inject a stale bearer token AND trigger the 401-redirect
     * handler on unexpected errors. UI-SPEC §Notes-3 — load-bearing choice.
     */
    export default function VerifyEmailPage() {
      const [searchParams] = useSearchParams()
      const token = useMemo(() => searchParams.get('token'), [searchParams])
      const navigate = useNavigate()

      type VerifyState = 'idle' | 'loading' | 'success' | 'error' | 'missing_token'
      // Initialize directly from the URL — no useEffect transition. If token
      // is null/empty at mount, the terminal MISSING_TOKEN state renders
      // immediately with no further interaction possible.
      const [state, setState] = useState<VerifyState>(
        token && token.length > 0 ? 'idle' : 'missing_token',
      )

      async function handleConfirm() {
        if (!token) return
        setState('loading')
        try {
          // Raw fetch (NOT vigilFetch) — UI-SPEC §Notes-3.
          const res = await fetch(`${API_BASE}/v1/auth/verify-email`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ token }),
          })
          if (res.ok) {
            setState('success')
          } else {
            // D-21 single-bucket: 400 / 5xx all collapse here.
            setState('error')
          }
        } catch {
          // Network error — same single-bucket as 4xx/5xx.
          setState('error')
        }
      }

      function handleRequestNewLink() {
        // UI-SPEC §Notes-5: runtime check at click time so it stays accurate
        // if the session expires between page mount and button click.
        const isLoggedIn = sessionStorage.getItem('vigil_jwt') !== null
        navigate(isLoggedIn ? '/settings' : '/auth')
      }

      // ── Render ───────────────────────────────────────────────────────────
      if (state === 'missing_token') {
        return (
          <div className="min-h-screen flex items-center justify-center bg-gray-900">
            <div className="bg-gray-900 rounded-lg p-8 w-full max-w-md mx-4">
              <h1 className="text-2xl font-medium text-white mb-4">This verification link is malformed</h1>
              <p className="text-sm text-gray-300 mb-6">
                Please use the button in the email we sent you.
              </p>
              <Link
                to="/"
                className="block w-full py-2 bg-teal-600 hover:bg-teal-800 text-white rounded font-medium text-center focus:outline-none focus:ring-2 focus:ring-teal-400"
              >
                Back to app
              </Link>
            </div>
          </div>
        )
      }

      if (state === 'success') {
        return (
          <div className="min-h-screen flex items-center justify-center bg-gray-900">
            <div className="bg-gray-900 rounded-lg p-8 w-full max-w-md mx-4">
              <h1 className="text-2xl font-medium text-white mb-4">Email verified</h1>
              <p className="text-sm text-gray-300 mb-6">You can close this tab, or</p>
              <Link
                to="/"
                className="text-sm text-teal-400 hover:text-teal-300 focus:outline-none focus:ring-2 focus:ring-teal-400"
              >
                Go to app
              </Link>
            </div>
          </div>
        )
      }

      if (state === 'error') {
        return (
          <div className="min-h-screen flex items-center justify-center bg-gray-900">
            <div className="bg-gray-900 rounded-lg p-8 w-full max-w-md mx-4">
              <h1 className="text-2xl font-medium text-white mb-4">This link is no longer valid</h1>
              <p className="text-sm text-gray-300 mb-6">
                Verification links expire after 24 hours and can only be used once.
              </p>
              <button
                type="button"
                onClick={handleRequestNewLink}
                className="w-full py-2 bg-teal-600 hover:bg-teal-800 text-white rounded font-medium focus:outline-none focus:ring-2 focus:ring-teal-400"
              >
                Request a new link
              </button>
              <div className="mt-4 text-center">
                <Link to="/" className="text-sm text-gray-400 hover:text-gray-200">
                  Back to app
                </Link>
              </div>
            </div>
          </div>
        )
      }

      // 'idle' | 'loading' — Confirm button is the only interactive element
      const loading = state === 'loading'
      return (
        <div className="min-h-screen flex items-center justify-center bg-gray-900">
          <div className="bg-gray-900 rounded-lg p-8 w-full max-w-md mx-4">
            <h1 className="text-2xl font-medium text-white mb-6">Verify your email</h1>
            <p className="text-sm text-gray-400 mb-4">Click the button below to confirm your email address.</p>
            <button
              type="button"
              onClick={handleConfirm}
              disabled={loading}
              aria-disabled={loading}
              className="w-full py-2 bg-teal-600 hover:bg-teal-800 text-white rounded font-medium disabled:opacity-50 focus:outline-none focus:ring-2 focus:ring-teal-400"
            >
              {loading ? 'Confirming…' : 'Confirm'}
            </button>
          </div>
        </div>
      )
    }
    ```

    Step 2 — Create `vigil-pwa/src/pages/VerifyEmailPage.test.tsx`:

    File content (skeleton — implement all 10 behavior cases):
    ```
    import { describe, it, expect, vi, beforeEach } from 'vitest'
    import { render, screen, fireEvent, waitFor } from '@testing-library/react'
    import { MemoryRouter } from 'react-router'
    import VerifyEmailPage from './VerifyEmailPage'

    // Hoisted mock for useNavigate.
    const navigateSpy = vi.fn()
    vi.mock('react-router', async () => {
      const actual = await vi.importActual<typeof import('react-router')>('react-router')
      return { ...actual, useNavigate: () => navigateSpy }
    })

    // Hoisted mock for vigilFetch — used to verify it is NEVER called.
    const vigilFetchSpy = vi.fn()
    vi.mock('../api/client', async () => {
      const actual = await vi.importActual<typeof import('../api/client')>('../api/client')
      return { ...actual, vigilFetch: vigilFetchSpy }
    })

    function renderAt(path: string) {
      return render(
        <MemoryRouter initialEntries={[path]}>
          <VerifyEmailPage />
        </MemoryRouter>,
      )
    }

    beforeEach(() => {
      navigateSpy.mockReset()
      vigilFetchSpy.mockReset()
      vi.stubGlobal('fetch', vi.fn())
      sessionStorage.clear()
    })

    describe('VerifyEmailPage (AUTH-11)', () => {
      it('AUTH-11-P-MOUNT-NO-FETCH: does NOT call fetch on mount with a valid token', () => {
        renderAt('/auth/verify?token=abc123')
        expect(globalThis.fetch).not.toHaveBeenCalled()
        expect(vigilFetchSpy).not.toHaveBeenCalled()
      })

      it('AUTH-11-P-IDLE-RENDERS-COPY: renders idle copy when token present', () => {
        renderAt('/auth/verify?token=abc123')
        expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('Verify your email')
        expect(screen.getByText('Click the button below to confirm your email address.')).toBeInTheDocument()
        expect(screen.getByRole('button', { name: 'Confirm' })).toBeInTheDocument()
      })

      it('AUTH-11-P-MISSING-TOKEN: renders missing-token copy when ?token absent', () => {
        renderAt('/auth/verify')
        expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('This verification link is malformed')
        expect(screen.getByText('Please use the button in the email we sent you.')).toBeInTheDocument()
        expect(screen.getByRole('link', { name: 'Back to app' })).toHaveAttribute('href', '/')
        expect(globalThis.fetch).not.toHaveBeenCalled()
      })

      // AUTH-11-P2-CONFIRM-200, AUTH-11-P2-CONFIRM-400, AUTH-11-P2-CONFIRM-500,
      // AUTH-11-P2-CONFIRM-NETWORK, AUTH-11-P2-RAW-FETCH,
      // AUTH-11-P-LOGIN-DEST-LOGGED-IN, AUTH-11-P-LOGIN-DEST-LOGGED-OUT,
      // AUTH-11-P-USES-USE-MEMO — implement per <behavior>.
    })
    ```

    Step 3 — Mount the route in `vigil-pwa/src/App.tsx`. Add the import alongside the other page imports (after line 11):
    ```
    import VerifyEmailPage from './pages/VerifyEmailPage'
    ```

    Then insert the route line between `<Route path="/auth/reset" ... />` (line 70) and the `<Route path="/*" ... />` block (line 72):
    ```
          {/* Phase 113 (AUTH-11) — verify-email page. Sibling to /auth/forgot
              and /auth/reset; OUTSIDE the protected Layout cluster (no
              isAuthenticated guard) so users hitting verify links from email
              while logged-out can still complete the verify action. */}
          <Route path="/auth/verify" element={<VerifyEmailPage />} />
    ```
    DO NOT modify any other route. DO NOT remove the existing /auth/forgot or /auth/reset routes.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && timeout 60 npx vitest run src/pages/VerifyEmailPage.test.tsx 2>&1 | tail -30) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && grep -c "/auth/verify" src/App.tsx)</automated>
  </verify>
  <acceptance_criteria>
    - File `vigil-pwa/src/pages/VerifyEmailPage.tsx` exists.
    - File `vigil-pwa/src/pages/VerifyEmailPage.test.tsx` exists with at least 8 `it(` blocks covering the behavior cases.
    - `grep -c useEffect vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `0` — NO useEffect anywhere (Apple Mail prefetch defense — load-bearing).
    - `grep -c "vigilFetch" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `0` — uses raw `fetch()`, NOT vigilFetch.
    - `grep -c 'fetch(`${API_BASE}/v1/auth/verify-email`' vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1`.
    - `grep -c "Verify your email" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1` (LOCKED idle h1).
    - `grep -c "Email verified" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1` (LOCKED success h1).
    - `grep -c "This link is no longer valid" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1` (LOCKED error h1).
    - `grep -c "This verification link is malformed" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1` (LOCKED missing-token h1).
    - `grep -c "Request a new link" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns `1`.
    - `grep -c "/auth/verify" vigil-pwa/src/App.tsx` returns `1` (route mount added; the Plan 04 import counts in the import statement separately — the path string match is what matters).
    - `grep -c "import VerifyEmailPage" vigil-pwa/src/App.tsx` returns `1`.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/pages/VerifyEmailPage.test.tsx` shows all behavior tests pass (at least 8 green it() blocks).
    - The `<Route path="/auth/verify"` line appears AFTER the `<Route path="/auth/reset"` line: `awk '/path="\/auth\/reset"/{r=NR} /path="\/auth\/verify"/{v=NR} END{exit (v>r)?0:1}' vigil-pwa/src/App.tsx` exits 0.
  </acceptance_criteria>
  <done>
    /auth/verify route is reachable in the PWA. The page renders the 4 terminal states with LOCKED copy. Confirm-only POST gate is enforced via no-useEffect architecture and verified by the AUTH-11-P-MOUNT-NO-FETCH regression guard.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add verify banner + Resend lifecycle to SettingsPage.tsx + extend SettingsPage.test.tsx</name>
  <files>vigil-pwa/src/pages/SettingsPage.tsx, vigil-pwa/src/pages/SettingsPage.test.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/SettingsPage.tsx (FULL FILE — existing useEffect for /v1/me on lines 91-107; existing top-of-page banner on render around line 200+; existing change-password lifecycle as reference for setTimeout pattern lines 65-87)
    - vigil-pwa/src/pages/SettingsPage.test.tsx (existing layout — extend, do not replace)
    - vigil-pwa/src/index.css (warning tokens lines 24-25)
    - .planning/phases/113-verify-email-on-signup/113-UI-SPEC.md sections "Component 2: Settings Verify Banner" and "Resend button states (D-25)"
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-22, D-23, D-24, D-25, D-28)
    - vigil-pwa/src/api/client.ts (vigilFetch — used for both /v1/auth/me and /v1/auth/resend-verification calls)
  </read_first>
  <behavior>
    - Test AUTH-11-B-VISIBLE-WHEN-UNVERIFIED: mock vigilFetch to return `/v1/auth/me` body `{ id: 1, email: 'u@x.io', emailVerifiedAt: null }` → render SettingsPage → assert banner is in the DOM with role="alert" AND text matches `/Verify your email — we sent a link to u@x\.io\. Click it to confirm\./` AND a "Resend" button is present.
    - Test AUTH-11-B-HIDDEN-WHEN-VERIFIED: mock /v1/auth/me to return `emailVerifiedAt: '2026-04-25T12:00:00.000Z'` (non-null) → render → assert NO banner (no element with text matching `/Verify your email/`).
    - Test AUTH-11-B-HIDDEN-WHEN-FETCH-PENDING: mock vigilFetch to return a never-resolving promise → assert NO banner appears (D-28 race-safe — no banner flash on verified users).
    - Test AUTH-11-B-NO-DISMISS-CONTROL: render with unverified state → assert NO close/× button anywhere inside the banner element (D-24).
    - Test AUTH-11-B2-RESEND-IDLE-LABEL: render with unverified state → assert Resend button initial label is exactly "Resend".
    - Test AUTH-11-B2-RESEND-SENDING: mock /v1/auth/resend-verification to return a never-resolving promise → click Resend → assert label changes to "Sending…" AND button has `aria-disabled="true"`.
    - Test AUTH-11-B2-RESEND-SENT-200: mock /v1/auth/resend-verification to return 200 → click Resend → assert label "Sent! Check your inbox." appears AND a 10-second timer is scheduled. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(10_000)` to assert label returns to "Resend" after 10s.
    - Test AUTH-11-B2-RESEND-RATE-LIMITED: mock /v1/auth/resend-verification to return 429 → click Resend → assert the inline text "You've requested too many. Try again later." is rendered AND the Resend button is hidden/removed from the DOM (D-25 rate-limited state hides the button until next page mount).
    - Test AUTH-11-B2-RESEND-NETWORK-ERROR: mock vigilFetch to throw → click Resend → assert "Could not send. Try again." appears AND button re-enables to "Resend" (label back to idle).
    - Test AUTH-11-B2-RESEND-5XX: mock /v1/auth/resend-verification to return 500 → click Resend → assert SAME error UX as network error.
    - Test AUTH-11-B-NEW-ME-CALL: assert `vigilFetch` was called with `'/v1/auth/me'` AND with `'/v1/me'` (both calls — D-28 says NEW useEffect alongside existing).
  </behavior>
  <action>
    Step 1 — Modify `vigil-pwa/src/pages/SettingsPage.tsx`. Three coordinated changes:

    Change A — Add new state declarations alongside the existing ones (around lines 60-77 after `cpSuccessTimerRef`):
    ```
      // Phase 113 (AUTH-11 D-28) — verify banner state from /v1/auth/me.
      // Local component state only — no global store, no react-query, no SWR
      // (D-28). Banner re-renders on every Settings mount via fresh /v1/auth/me.
      const [meData, setMeData] = useState<{
        id: number
        email: string
        emailVerifiedAt: string | null
      } | null>(null)

      // Phase 113 (AUTH-11 D-25) — Resend button lifecycle: idle → sending →
      // sent (10s) → idle | rate_limited (until next mount) | error → idle.
      type ResendState = 'idle' | 'sending' | 'sent' | 'rate_limited' | 'error'
      const [resendState, setResendState] = useState<ResendState>('idle')
      const resendSentTimerRef = useRef<number | null>(null)
    ```
    (`useRef` is already imported on line 1. Verify; if not, add it to the imports.)

    Change B — Add a new useEffect for /v1/auth/me, INSERTED DIRECTLY AFTER the existing /v1/me useEffect (after the closing `}, [])` of lines 91-107):
    ```
      // Phase 113 (AUTH-11 D-28) — fetch /v1/auth/me on mount alongside /v1/me.
      // The two endpoints coexist by design (UI-SPEC §Notes-4):
      //   /v1/me        → { userId, email }       — App.tsx PostHog identify + accountEmail display
      //   /v1/auth/me   → { id, email, emailVerifiedAt }  — verify banner sentinel
      // Do NOT consolidate or replace /v1/me — App.tsx depends on its shape.
      useEffect(() => {
        vigilFetch('/v1/auth/me')
          .then((r) => r.ok ? r.json() : undefined)
          .then((data?: { id: number; email: string; emailVerifiedAt: string | null }) => {
            if (data) setMeData(data)
          })
          .catch(() => {
            // Silent — banner absent if /v1/auth/me fails (UI-SPEC §Notes-6:
            // no flash of banner on verified users; null meData → no render).
          })
      }, [])

      // D-25: clear the 10s "Sent!" → idle timer on unmount to prevent setState
      // on unmounted component (mirrors cpSuccessTimerRef cleanup pattern, lines 80-87).
      useEffect(() => {
        return () => {
          if (resendSentTimerRef.current !== null) {
            window.clearTimeout(resendSentTimerRef.current)
            resendSentTimerRef.current = null
          }
        }
      }, [])
    ```

    Change C — Add the Resend click handler. Insert as a function inside the component body (anywhere after the state declarations):
    ```
      // Phase 113 (AUTH-11 D-25) — Resend button click handler.
      // Lifecycle: idle → sending → (200 → sent → 10s → idle) | (429 → rate_limited terminal)
      //                                                       | (5xx/network → error → idle)
      async function handleResendClick() {
        if (resendState !== 'idle' && resendState !== 'error') return
        setResendState('sending')
        try {
          const res = await vigilFetch('/v1/auth/resend-verification', { method: 'POST' })
          if (res.status === 429) {
            setResendState('rate_limited')
            return
          }
          if (!res.ok) {
            setResendState('error')
            return
          }
          setResendState('sent')
          // 10s timer back to idle (D-25 — user can retry if email never arrives).
          if (resendSentTimerRef.current !== null) {
            window.clearTimeout(resendSentTimerRef.current)
          }
          resendSentTimerRef.current = window.setTimeout(() => {
            setResendState('idle')
            resendSentTimerRef.current = null
          }, 10_000)
        } catch {
          setResendState('error')
        }
      }
    ```

    Change D — Render the banner at the TOP of the page render. Locate the outermost render (currently `<div className="p-4 max-w-2xl mx-auto text-gray-50">`). Insert the verify banner block as the FIRST child of that div, BEFORE the existing `{banner && (...)}` block and BEFORE `<h1>Settings</h1>`:
    ```
      <div className="p-4 max-w-2xl mx-auto text-gray-50">
        {/* Phase 113 (AUTH-11 D-22..D-25) — verify-email banner. Renders ONLY
            when meData?.emailVerifiedAt === null. Non-dismissible (D-24); no
            close control. Uses warning tokens defined in index.css @theme. */}
        {meData?.emailVerifiedAt === null && (
          <div
            role="alert"
            className="mb-4 rounded border border-warning-400 bg-warning-50 px-4 py-3 flex items-center justify-between"
          >
            <p className="text-sm text-warning-400">
              Verify your email — we sent a link to {meData.email}. Click it to confirm.
            </p>
            <div aria-live="polite" className="ml-3">
              {resendState === 'rate_limited' ? (
                <span className="text-xs text-red-400">
                  You've requested too many. Try again later.
                </span>
              ) : resendState === 'sent' ? (
                <span className="text-sm text-warning-400">Sent! Check your inbox.</span>
              ) : resendState === 'error' ? (
                <>
                  <span className="text-sm text-red-400 mr-2">Could not send. Try again.</span>
                  <button
                    type="button"
                    onClick={handleResendClick}
                    className="px-3 py-1 bg-teal-600 hover:bg-teal-500 rounded text-white text-sm focus:outline-none focus:ring-2 focus:ring-teal-400"
                  >
                    Resend
                  </button>
                </>
              ) : (
                <button
                  type="button"
                  onClick={handleResendClick}
                  disabled={resendState === 'sending'}
                  aria-disabled={resendState === 'sending'}
                  className="px-3 py-1 bg-teal-600 hover:bg-teal-500 rounded text-white text-sm disabled:opacity-50 disabled:cursor-not-allowed focus:outline-none focus:ring-2 focus:ring-teal-400"
                >
                  {resendState === 'sending' ? 'Sending…' : 'Resend'}
                </button>
              )}
            </div>
          </div>
        )}
        {banner && (...)}    {/* <-- existing block, unchanged */}
        <h1 ...>Settings</h1> {/* <-- existing heading, unchanged */}
        ...
      </div>
    ```

    DO NOT modify the existing `/v1/me` useEffect (lines 91-107). DO NOT modify the existing `accountEmail` state. DO NOT remove or reorder any other section.

    Step 2 — Extend `vigil-pwa/src/pages/SettingsPage.test.tsx` with the 11 behavior cases. Mirror the existing test layout (top-level `describe('SettingsPage', () => { describe(...) })`). Add a NEW top-level describe block for the verify banner behavior:
    ```
    describe('verify-email banner (AUTH-11)', () => {
      // AUTH-11-B-VISIBLE-WHEN-UNVERIFIED, AUTH-11-B-HIDDEN-WHEN-VERIFIED,
      // AUTH-11-B-HIDDEN-WHEN-FETCH-PENDING, AUTH-11-B-NO-DISMISS-CONTROL,
      // AUTH-11-B2-* (resend lifecycle), AUTH-11-B-NEW-ME-CALL.
      // Mock vigilFetch to return appropriate /v1/auth/me + /v1/auth/resend-verification responses.
      // Use vi.useFakeTimers() for the 10s timer test.
    })
    ```

    Implement all 11 test cases per the <behavior> block. Mock `vigilFetch` per-test; the existing tests already mock it via `vi.mock('../api/client')` so just extend the mock implementation per test case.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -10) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && timeout 60 npx vitest run src/pages/SettingsPage.test.tsx 2>&1 | tail -40)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vigilFetch('/v1/auth/me')" vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (NEW useEffect — D-28).
    - `grep -c "vigilFetch('/v1/me')" vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (existing useEffect — UNCHANGED).
    - `grep -c "vigilFetch('/v1/auth/resend-verification'" vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (Resend handler).
    - `grep -c 'meData?.emailVerifiedAt === null' vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (banner visibility gate — D-28).
    - `grep -c 'role="alert"' vigil-pwa/src/pages/SettingsPage.tsx` returns at least 1 (verify banner — UI-SPEC §Accessibility).
    - `grep -c 'aria-live="polite"' vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (Resend status region wrapper — UI-SPEC §Accessibility).
    - `grep -c "Verify your email — we sent a link to" vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (LOCKED banner copy — D-23).
    - `grep -c "Sent! Check your inbox." vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (LOCKED — D-25).
    - `grep -c "You've requested too many" vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (LOCKED — D-25).
    - `grep -c "Could not send. Try again." vigil-pwa/src/pages/SettingsPage.tsx` returns 1 (LOCKED — D-25).
    - `grep -c 'bg-warning-50' vigil-pwa/src/pages/SettingsPage.tsx` returns at least 1 (warning token — UI-SPEC §Color).
    - `grep -c 'border-warning-400' vigil-pwa/src/pages/SettingsPage.tsx` returns at least 1.
    - `grep -c 'setTimeout.*10_000\|setTimeout.*10000' vigil-pwa/src/pages/SettingsPage.tsx` returns at least 1 (D-25 10s timer).
    - `grep -c 'resendSentTimerRef' vigil-pwa/src/pages/SettingsPage.tsx` returns at least 3 (declared + setTimeout assignment + cleanup useEffect).
    - The verify banner renders BEFORE the existing `<h1>Settings</h1>` in the JSX: `awk '/meData\?\.emailVerifiedAt === null/{m=NR} /<h1[^>]*>Settings/{h=NR} END{exit (m<h)?0:1}' vigil-pwa/src/pages/SettingsPage.tsx` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/pages/SettingsPage.test.tsx` shows ALL existing tests still pass + at least 11 new AUTH-11-B* tests pass.
  </acceptance_criteria>
  <done>
    SettingsPage renders the verify banner only when unverified, runs the full 5-state Resend lifecycle, and fetches /v1/auth/me alongside /v1/me without consolidation. All accessibility hooks (role="alert", aria-live, focus rings) present per UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update AuthPage.tsx login response type to include emailVerifiedAt</name>
  <files>vigil-pwa/src/pages/AuthPage.tsx</files>
  <read_first>
    - vigil-pwa/src/pages/AuthPage.tsx (lines 47-58 and 72-83 — both login response destructures: post-login + post-register-then-auto-login)
    - .planning/phases/113-verify-email-on-signup/113-CONTEXT.md (D-26 — additive type change; PWA does not need to STORE the field, just type it correctly for future consumers)
    - .planning/phases/113-verify-email-on-signup/113-RESEARCH.md Pitfall 7 — additive emailVerifiedAt field; tsc must not regress
  </read_first>
  <action>
    Modify `vigil-pwa/src/pages/AuthPage.tsx` at TWO locations (the file has two login response destructures — login flow on line 56 and the post-register auto-login on line 81):

    Edit 1 (line ~56, login mode):
    Replace:
    ```
            const { token, user } = (await res.json()) as { token: string; user: { id: number; email: string } }
    ```
    With:
    ```
            // Phase 113 (AUTH-11 D-26) — login response now includes emailVerifiedAt
            // (ISO string or null). PWA does not store it here; SettingsPage refetches
            // via /v1/auth/me on mount. Type is widened so future consumers can read
            // the field without a TS regression. Backwards-compatible: existing
            // destructure { token, user: { id, email } } continues to work.
            const { token, user } = (await res.json()) as {
              token: string
              user: { id: number; email: string; emailVerifiedAt: string | null }
            }
    ```

    Edit 2 (line ~81, post-register auto-login):
    Replace:
    ```
            const { token, user } = (await loginRes.json()) as { token: string; user: { id: number; email: string } }
    ```
    With:
    ```
            // Phase 113 (AUTH-11 D-26) — same shape change for the post-register
            // auto-login. Newly-registered user always has emailVerifiedAt: null
            // here (the verify email is sent by Plan 02's register handler in
            // background; user must click the link to flip the column). Settings
            // banner will render on first /settings visit until verify completes.
            const { token, user } = (await loginRes.json()) as {
              token: string
              user: { id: number; email: string; emailVerifiedAt: string | null }
            }
    ```

    DO NOT add code that stores `emailVerifiedAt` anywhere (no sessionStorage, no global store). The type widening is the only behavior change. The runtime call to `onAuthSuccess?.(String(user.id), user.email)` is unchanged — it ignores `emailVerifiedAt` for now.

    DO NOT change `onAuthSuccess` signature. DO NOT modify any other line.
  </action>
  <verify>
    <automated>(cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && grep -c 'emailVerifiedAt: string | null' src/pages/AuthPage.tsx) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5) && (cd "/Users/jamesonmorrill/Desktop/Local AI/dailybrief/vigil-pwa" && timeout 30 npx vitest run src/pages/AuthPage.test.tsx 2>&1 | tail -15)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'emailVerifiedAt: string | null' vigil-pwa/src/pages/AuthPage.tsx` returns 2 (both login response destructures updated).
    - The destructure variables themselves are UNCHANGED — `grep -c 'const { token, user }' vigil-pwa/src/pages/AuthPage.tsx` still returns 2 (no new variable extracted).
    - `grep -c 'storeKey(token)' vigil-pwa/src/pages/AuthPage.tsx` still returns 2 (the token storage flow is unchanged).
    - `grep -c 'onAuthSuccess?.(String(user.id), user.email)' vigil-pwa/src/pages/AuthPage.tsx` still returns 2 (the auth-success callback shape is unchanged — does not gain a third arg).
    - `npx tsc --noEmit` exits 0.
    - Existing `AuthPage.test.tsx` tests remain green: `npx vitest run src/pages/AuthPage.test.tsx` shows no regressions.
  </acceptance_criteria>
  <done>
    Login response type matches the new server contract (D-26). PWA can read `user.emailVerifiedAt` in future plans without TS errors. Runtime behavior is unchanged in this plan; SettingsPage Plan 04 Task 2 is the runtime consumer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email client (Apple Mail / Gmail / Outlook) → /auth/verify page | Email clients prefetch links with generic browser UA; the page must render WITHOUT triggering the verify POST. The Confirm-button gate is the mitigation. |
| user → POST /v1/auth/verify-email (via PWA) | Token from URL query string crosses; raw fetch (not vigilFetch) avoids stale-bearer side effects + 401 redirect handler. |
| user → POST /v1/auth/resend-verification (via PWA) | Authenticated; vigilFetch attaches bearer; rate limit on server (Plan 03) enforces 3/hour per userId. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Tampering (Apple Mail prefetch burning tokens) | VerifyEmailPage | mitigate | NO useEffect anywhere in VerifyEmailPage. Token parsed via useMemo at render; POST only fires on Confirm onClick. Test AUTH-11-P-MOUNT-NO-FETCH asserts `globalThis.fetch` NOT called on mount. Acceptance grep: `grep -c useEffect vigil-pwa/src/pages/VerifyEmailPage.tsx` returns 0. |
| T-113-04 | Information Disclosure (token leak via PWA) | URL query string | mitigate | Token only appears in the URL on the /auth/verify page (necessarily — it's how the user arrives from email). PWA does NOT log the URL anywhere. The token is read into local state, sent in the POST body (not URL) on Confirm, and never persisted client-side. |
| T-113-XSS-01 | XSS via banner email interpolation | SettingsPage banner | mitigate | React JSX escapes `{meData.email}` automatically — no `dangerouslySetInnerHTML`. Email is server-validated upstream (auth.ts isValidEmailShape) so the only attack vector would be a compromised /v1/auth/me response, which still gets escaped at the JSX boundary. |
| T-113-VIGFETCH-01 | EoP (vigilFetch 401 redirect side effect on verify page) | /auth/verify | mitigate | VerifyEmailPage uses raw `fetch()` not `vigilFetch`. Acceptance grep: `grep -c "vigilFetch" vigil-pwa/src/pages/VerifyEmailPage.tsx` returns 0. UI-SPEC §Notes-3 documents the rationale. |
| T-113-RESEND-FLASH-01 | UI flash (banner appears briefly on verified user) | SettingsPage | mitigate | Banner gate is `meData?.emailVerifiedAt === null` — when meData is null (loading), the optional chaining short-circuits and banner does NOT render. UI-SPEC §Notes-6 documents this. Test AUTH-11-B-HIDDEN-WHEN-FETCH-PENDING enforces. |
| T-113-RESEND-TIMER-01 | DoS / setState-on-unmounted-component | resendSentTimerRef | mitigate | useEffect cleanup clears the 10s timeout on unmount (mirrors cpSuccessTimerRef Phase 110 WR-02 fix at lines 80-87). Acceptance grep enforces 3+ references to `resendSentTimerRef`. |
</threat_model>

<verification>
- Tasks 1, 2, 3 each have automated test runs that exit green.
- The full PWA test suite must remain green: `(cd vigil-pwa && timeout 90 npm test 2>&1 | tail -30)`.
- Pre-existing flaky test SettingsPage.test.tsx:104 (Phase 110 STATE.md note) — if still failing, log to deferred-items.md but do NOT block this plan.
- Live verification belongs in Plan 05 manual UAT.
</verification>

<success_criteria>
- /auth/verify route exists in App.tsx and renders VerifyEmailPage with all 4 terminal states per UI-SPEC.
- VerifyEmailPage has zero useEffect calls (Apple Mail prefetch defense).
- VerifyEmailPage uses raw fetch() not vigilFetch.
- SettingsPage renders the verify banner at the top when meData.emailVerifiedAt === null.
- SettingsPage Resend button cycles through 5 states per D-25.
- SettingsPage adds NEW useEffect for /v1/auth/me alongside existing /v1/me (D-28 — both calls coexist).
- AuthPage login response types include emailVerifiedAt (D-26 additive — runtime unchanged this plan).
- All new vitest cases green (10 in VerifyEmailPage.test.tsx + 11 in SettingsPage.test.tsx = 21 new green tests).
- TypeScript build green; existing test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/113-verify-email-on-signup/113-04-SUMMARY.md` documenting:
- Confirmation that VerifyEmailPage has 0 useEffect calls (load-bearing for Apple Mail defense).
- Confirmation that SettingsPage now has TWO /me-family fetches on mount (existing /v1/me + new /v1/auth/me).
- Test count delta + any pre-existing PWA flaky tests observed (SettingsPage.test.tsx:104 carried from Phase 110).
- Note that Plan 05 will exercise the full flow against live Railway after deploy.
</output>
